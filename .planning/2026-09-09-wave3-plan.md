# Wave 3 orchestration plan — prepared 2026-09-09 ~23:55, corrections 2026-09-10 ~00:15

Goal: close Wave 3 (shared-layer reuse + lifecycle, goals 2 + 4) per
`.planning/2026-09-08-adapter-convergence-MASTER-PLAN.md`, with all premises re-verified on
develop `1ce966e64` (docs commits pushed; Wave 2 closure in `.planning/2026-09-09-wave2-closure.md`).

## Plan-validation review corrections (reviewer @ 1ce966e64, verdict CONCERNS — all baked into the relaunch)

1. **L1 named-fix target wrong**: route through `common.MapContentToNFS4` (content_errmap.go ~64;
   maps ErrStoreClosed→STALE, preserves ErrRemoteUnavailable→IO per TestMapContentToNFS4), NOT
   `MapToNFS4` — ReadFromBlockStore/WriteToBlockStore wrap errors as fmt.Errorf
   (read_payload.go ~71), so MapToNFS4 returns default SERVERFAULT and regresses IO.
   Two more bypass sites found: v4/handlers/commit.go ~111, deallocate.go ~94 — all five wired.
2. **L0↔L2 overlap real**: L2's deletion breaks errmap_test.go (TestMapLockToNFS3/4 ~:269-299,
   TestExoticErrorCodes ~:355-380 assert the dead columns). LANES COMBINED into one
   common-package PR (fix/errmap-coverage-dead-columns).
3. **L0 third guard**: exoticCodes() (~:305-315) and e2eTriggerableCodes (~:345-364) don't list
   ErrConflict — the ErrConflict row must also join exoticCodes() or TestCrossProtocolUnitConformance
   fails.
4. **L3 second production caller**: handler.go ~2038 `getUserIdentity(sess.User)` — convert or
   retain a renamed helper (uidGIDFromSessionUser) there; test call site ~65 also covered.
5. **L4 sync.Once guard**: base.go ~225 closes listenerReady unconditionally on the normal path —
   the Stop-path close must be Once-guarded or double-close panics.
6. **L5 SessionManager nil-check**: async completions can fire where the sync path never ran —
   nil-check connInfo.SessionManager too; mirror sync order GrantCredits→SequenceWindow.Gramt
   (response.go ~869→880); exactly two async sites (1232, 1303), no third.
7. **L6 line drift**: the CLAUDE.md citation is line 103, not 105.

## Fan-out state

- First launch (e6c9b38b): 6 workers failed at allocation (bad explicit cwd paths — worktrees must
  be host-allocated via worktree:true, not pre-named /private/tmp paths); plan-validation reviewer
  completed with the corrections above.
- Relaunch (952fee54): 4 workers — common-errmap-cleanup (combined L0+L2), v4-content-wiring (5
  sites), smb-resolved-identity, adapter-lifecycle-accept, async-credits-and-citations (2 branches).
  All corrections baked in. Next: review passes per branch, PR opens (guard first), serialized merges.

## Lane structure (fan out immediately — do not over-invest in any one lane first)

### Lane 0 — GUARD PR (land FIRST, blocks nothing else but unguards everything)
`TestErrorMapCoverage` rewrite in `internal/adapter/common/errmap_test.go`: replace the
hand-typed `const expectedCount = 25` with an enum walk (`for c := merrs.ErrNotFound; c <=
merrs.ErrConflict; c++` — ErrorCode is a contiguous iota, valid) and fix the count to 26. Add
an errorMap row for `ErrConflict` in the same PR or the test will fail — WAIT: the plan says
land the rewrite FIRST and it will fail until the row exists. Resolution: land the rewrite +
the ErrConflict row together as PR 1 (the plan's items 3+4 collapse into one small PR), OR
land the rewrite with just the count fixed and keep ErrConflict unguarded until its own PR.
Decision: **rewrite + ErrConflict row + count 26 in one PR** — the row is small, and naming
the observing handler is satisfied by pinning `lookupErrorRow`/`MapToNFS4` with a table test.

### Lane 1 — NFSv4 content-path wiring (files: internal/adapter/nfs/v4/handlers/)
read.go:143, write.go:215, read_plus.go:125 return literal `types.NFS4ERR_IO` after
`common.ReadFromBlockStore`/`WriteToBlockStore`/`buildReadPlusContents` failures, bypassing
`lookupErrorRow`'s ErrStoreClosed → STALE mapping. Fix: route through `common.MapToNFS4(err)`
(general context) so ErrStoreClosed draws NFS4ERR_STALE and merrs codes draw their rows.
Verify: red-without-fix (inject engine.ErrStoreClosed), unit tests per handler, full
v4/handlers race suite. COMMIT/closes: pairs with #2467 if that issue covers the same paths —
check the issue body before opening.

### Lane 2 — lock-errmap dead columns (file: internal/adapter/common/lock_errmap.go)
Delete the NFS3/NFS4 columns + `MapLockToNFS3`/`MapLockToNFS4` (only production caller is
SMB via `MapLockToSMB`); resolve `ErrLockLimitExceeded` Jukebox (LOCK-context) vs IO
(general) drift — keep ONE NFS3 answer; add the missing spot-check rows to tests.
Disjoint from Lane 1's files.

### Lane 3 — identity (files: internal/adapter/smb/handlers/)
Delete `getUserIdentity` (auth_helper.go:102); `buildIdentity` must take the resolved
UID/GID directly. Kerberos/Netlogon paths already resolve through
`identity.ResolvedIdentity` (`synthUserFromResolved` lists primary GID first) — use it
instead of re-deriving from `models.User`. Fix `kerberos_identity_test.go:43`. SCOPE RULE:
only resolved identities get unified treatment; AUTH_SYS/AUTH_UNIX wire credentials are
exempt (RFC 5531/5532 — the client dictates UID/GID by design). Disjoint from Lanes 1-2.

### Lane 4 — adapter lifecycle (file: pkg/adapter/base.go)
(a) Stop()-before-bind: `started` flipped and `listenerReady` closed inside ServeWithFactory
after net.Listen; Stop() before that leaves listenerReady open forever (GetListenerAddr
blocks). Fix: close listenerReady on the Stop path (or make Stop set started + close ready
with a bind-failed sentinel). (b) Accept busy-loop: unexpected Accept error `continue`s with
no backoff — EMFILE/ENFILE spins. Fix: bounded backoff (e.g. 10ms doubling capped 1s) on
non-shutdown errors, reset on success. RFC-reference: this is the plan's "3 core HIGHs, one
root cause". Disjoint from all other lanes.

### Lane 5 — async credit grants (files: internal/adapter/smb/)
response.go:1207-1210 + 1278-1281 call `connInfo.SequenceWindow.Grant(1)` directly, never
Session.credits — GetOutstanding under-counted, defeating grantAdaptive's throttle input and
GetStats. Fix: route through `Manager.GrantCredits` (or a Manager method updating both
atomically). MIND: sharing sm.mu-adjacent locks; the session manager has its own mu. Verify
with a unit test asserting GrantCredits fields advance on an async completion. Small; may
pair with Lane 4's PR if the diff stays tiny, but prefer separate PRs (different packages).

### Lane 6 — citations (files: CLAUDE.md, internal/adapter/smb/handlers/create.go)
`CLAUDE.md:105` and `create.go:1874` cite `CheckExportAccess` which no longer exists; the
real symbols are `ResolveSharePermission` (internal/adapter/nfs/auth/share_permission.go) and
the Tree Connect ACL check (tree_connect.go:442). Rename in both places. NOTE: CLAUDE.md is
repo-instructions — the citation fix is a one-line edit, safe to do at top level.

## Sequencing rules
- ONE writer per worktree; Lanes 1-5 are disjoint file sets → parallel worktrees from
  origin/develop, one worker child each, premise re-verified inside the prompt.
- Lane 0 is 30 minutes of work — do it at top level first (or one child) so later errmap
  fixes land guarded.
- Per-PR pipeline (unchanged): implement → three reviews (correctness, simplification,
  adversarial) → PR open (Closes #N, --assignee marmos91, humanized title) → bg_run
  Copilot/CI watcher → address comments → serialized squash-merge with rebase-on-conflict.
- All touching `internal/adapter/...` but different packages; the only shared file risk is
  errmap_test.go (Lane 0 owns it; Lanes 1-2 touch their own test files).
- `graphify update .` after the last merge of the day.
- KNOWN_FAILURES rows: walk out only on demonstrated CI passes (not applicable to Wave 3
  singles unless a pynfs/smbtorture row flips).

## Open question resolved by the plan (no user questions needed)
- ErrConflict's observing handler: the coordinator wraps ObjectID conflicts into
  engine.ErrObjectIDConflict via errors.Join, so a raw StoreError{ErrConflict} reaches
  callers only when the store layer is called directly. Pin `MapToNFS4`/`MapToSMB` table
  coverage for it and note the aspirational claim in the PR body.
