[{"title": "Data race: DOC propagation writes OpenFile.DeletePending without lock", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/close.go", "line": 396, "severity": "HIGH", "what": "In the DOC propagation block, `other.DeletePending = true`, `other.DeleteOnCloseParentKey`, and `other.HasDeleteOnCloseParentKey` are written directly to sibling `*OpenFile` structs inside a `sync.Map Range` callback without holding `other.mu`. Concurrent goroutines (e.g. a parallel `Create` calling `isFileDeletePending`) read `existing.DeletePending` at handler.go:1909 also without `mu`.", "why": "Go's race detector will flag this as a data race. A concurrent `Create` on another goroutine can read a partially-written `DeletePending` (torn bool) or observe stale zero while the CLOSE goroutine is mid-write, causing a new open to succeed when it should return `STATUS_DELETE_PENDING`, or vice versa. The `mu` comment on `OpenFile` says it guards mutable fields but the DOC propagation block never acquires it.", "fix": "Acquire `other.mu.Lock()` before writing `other.DeletePending`, `other.DeleteOnCloseParentKey`, `other.HasDeleteOnCloseParentKey` and release after. Mirror the pattern already used for `SmbWriteTriggered` in the same file (line 565). Similarly protect reads in `isFileDeletePending`.", "dimension": "bugs", "area": "smb-create-open", "confirmed": true, "verdict": "close.go:396 writes other.DeletePending/other.DeleteOnCloseParentKey/other.HasDeleteOnCloseParentKey inside sync.Map Range callback with no other.mu.Lock(). handler.go:1909 reads existing.DeletePending in isFileDeletePending also without lock. Real data race."}, {"title": "Data race: BaseFileDeletePending propagation writes without lock", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/close.go", "line": 412, "severity": "HIGH", "what": "`other.BaseFileDeletePending`, `other.BaseFileDeleteParentHandle`, `other.BaseFileDeleteFileName`, `other.DeleteOnCloseParentKey`, and `other.HasDeleteOnCloseParentKey` are written to stream-handle `*OpenFile` structs inside `rangeStreamsOfBase` without `other.mu`. The same fields are read in `isFileOrBaseDeletePending` (handler.go:1949) also without a lock.", "why": "Same race class as the previous finding. A concurrent `Create` opening a stream while the base file's CLOSE is propagating `BaseFileDeletePending` can observe torn state and miss the `STATUS_DELETE_PENDING` gate, opening a stream whose base is in the process of being deleted.", "fix": "Acquire `other.mu.Lock()` around the write block in `rangeStreamsOfBase` callback; similarly hold `existing.mu.RLock()` when reading `existing.BaseFileDeletePending` in `isFileOrBaseDeletePending`.", "dimension": "bugs", "area": "smb-create-open", "confirmed": true, "verdict": "close.go:412-416 writes other.BaseFileDeletePending/BaseFileDeleteParentHandle/BaseFileDeleteFileName/DeleteOnCloseParentKey/HasDeleteOnCloseParentKey inside rangeStreamsOfBase callback with no other.mu.Lock(). handler.go:1949 reads existing.BaseFileDeletePending without lock. Same race class as idx 0."}, {"title": "Data race: Session identity fields mutated without lock during re-auth", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 1729, "severity": "HIGH", "what": "tryReauthUpdate writes sess.Username/Domain/User/IsGuest/IsNull (plain struct fields, no mutex) while concurrent goroutines dispatching pipelined requests on the same session read those fields via primeAuthContext/BuildAuthContext without any lock. Same race in reauthKerberosSession (kerberos_auth.go:195-200) for sess.User/Username/Domain/IsGuest/IsNull/ExpiresAt.", "why": "Session struct comment says 'Identity fields (read-only after creation)' but re-auth violates the invariant. go -race detects this: the dispatch goroutine reads sess.User (auth_helper.go:223) concurrently with the SESSION_SETUP goroutine writing it. A torn read of the User pointer can cause a nil-deref or wrong-identity in permission checks on in-flight handles.", "fix": "Add a sync.RWMutex (identityMu) to Session. Write-lock it in tryReauthUpdate and reauthKerberosSession around the identity field assignments. Read-lock it in GetSession callers that read identity fields (primeAuthContext, BuildAuthContext, CaptureOpenerIdentity, IsExpired). Alternatively use atomic.Pointer[models.User] for the User field and atomic stores for the string/bool fields, but a single RWMutex covering all five fields is cleaner.", "dimension": "bugs", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "session_setup.go:1729-1733 writes sess.Username/Domain/User/IsGuest/IsNull directly. Session.mu (line 161 in session.go) only guards credit mutations per comment. Identity fields comment says 'read-only after creation' but re-auth violates this. Concurrent dispatch reads these fields without any lock."}, {"title": "Auth bypass: user with no NT hash gets authenticated non-guest session without credential check", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 1241, "severity": "HIGH", "what": "When UserStore.GetUser succeeds and user.Enabled is true but hasNTHash is false (line 1086), execution falls to the 'no NT hash' block at line 1218. CreateSessionWithUser is called (line 1241) and buildAuthenticatedResponse is returned with nil exportedSessionKey. No NTLMv2 challenge-response is ever validated. Any client that knows a valid username but presents any (including garbage) NtChallengeResponse is authenticated as that user.", "why": "The NTLM TYPE_3 parse at line 1032 extracts the username. GetUser at line 1083 succeeds. hasNTHash (line 1086) is false so the ValidateNTLMv2Response block is skipped entirely. The session is created as ctx.IsGuest=false with the target user's identity. This is an explicit auth bypass, acknowledged in the comment ('any client knowing the username can authenticate'). Session signing is also absent (nil key), so subsequent signed requests silently pass without verification.", "fix": "Reject authentication when hasNTHash is false: return NewErrorResult(types.StatusLogonFailure) instead of creating a session. Operators must set NT hashes before users can authenticate. If a transitional grace period is needed, gate it behind an explicit insecure-auth config flag (default false) and log at Error, not Warn.", "dimension": "bugs", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "session_setup.go:1218-1241: when hasNTHash=false, NTLMv2 validation block skipped entirely. CreateSessionWithUser called with nil exported session key (no signing). Comment at line 1220 explicitly acknowledges: 'any client knowing the username can authenticate'. Real auth bypass."}, {"title": "flushWatcher races on re-arm: timer from old watcher prematurely flushes new watcher", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/change_notify.go", "line": 1181, "severity": "HIGH", "what": "`bufferEventLocked` captures `fileID` by value in the `time.AfterFunc` closure; `flushWatcher` then looks up `r.byFileID[fileID]` to get the notify instance. When `Register` replaces an existing watcher (same FileID, line 589-590) and calls `r.unregisterLocked(old)` which calls `old.flushTimer.Stop()`, `Stop()` returns false if the timer already fired (goroutine blocked on `r.mu`). After lock is released, the stale timer goroutine wakes, finds the NEW watcher under `fileID`, drains its (possibly empty) buffer and calls `r.unregisterLocked` on it \u2014 permanently evicting the freshly-registered watcher before any real event is delivered.", "why": "One-shot watcher is destroyed before the client receives any notification. The client never sees STATUS_PENDING turn into a notification response; from the client's perspective the CHANGE_NOTIFY silently vanishes. Reproducible in any scenario where a re-arm races with the 100\u00b5s flush timer firing (e.g. smbtorture smb2.notify.mask, cancel+re-register flows).", "fix": "Store the notify pointer itself in the timer closure instead of just fileID, then in `flushWatcher` compare `byFileID[fileID] == notifyPtr` before draining. Alternatively, use a per-watcher generation counter: increment on every `Register`, capture in the closure, and bail in the callback when the stored generation doesn't match.", "dimension": "bugs", "area": "smb-lease-oplock-notify", "confirmed": true, "verdict": "change_notify.go:1170 captures fileID by value in AfterFunc closure. Register (line 589) calls unregisterLocked(old) which calls old.flushTimer.Stop() \u2014 if timer already fired and goroutine is blocked on r.mu, Stop() returns false. After r.mu released, stale goroutine calls flushWatcher(fileID), finds NEW watcher under same fileID in r.byFileID, drains it and unregisters it."}, {"title": "COPYCHUNK access check uses DesiredAccess (pre-DACL) instead of GrantedAccess", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/ioctl_copychunk.go", "line": 250, "severity": "HIGH", "what": "`validateCopyChunkAccess` reads `src.DesiredAccess` and `dst.DesiredAccess` (the requested, pre-DACL mask) to gate COPYCHUNK source read and destination write access.", "why": "Every other access gate in the codebase (read.go hasReadAccess, write.go hasWriteAccess, set_info.go, query_info.go, ioctl_sparse.go, ioctl_fsctl.go) explicitly consults `GrantedAccess` (post-DACL intersection at CREATE). The gap is documented in the codebase as bug class B (#619): a MAXIMUM_ALLOWED open whose DACL stripped FILE_READ_DATA still has it in DesiredAccess, so the check passes when it should deny. An unprivileged user can abuse COPYCHUNK to read or write a file their DACL forbids.", "fix": "In `validateCopyChunkAccess`, change `srcAccess := types.AccessMask(src.DesiredAccess)` \u2192 `srcAccess := types.AccessMask(src.GrantedAccess)` and `dstAccess := types.AccessMask(dst.DesiredAccess)` \u2192 `dstAccess := types.AccessMask(dst.GrantedAccess)`. Update the log format strings from `src.DesiredAccess`/`dst.DesiredAccess` to `src.GrantedAccess`/`dst.GrantedAccess` accordingly.", "dimension": "bugs", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "ioctl_copychunk.go:250-251: validateCopyChunkAccess reads src.DesiredAccess and dst.DesiredAccess. All other access gates in the codebase use GrantedAccess. DesiredAccess is the pre-DACL requested mask; GrantedAccess is the post-DACL effective mask set at CREATE time."}, {"title": "Compound NextCommand < HeaderSize causes panic in ParseCompoundCommand", "file": "/tmp/dittofs-audit/internal/adapter/smb/compound.go", "line": 553, "severity": "HIGH", "what": "ParseCompoundCommand validates NextCommand is 8-byte aligned (line 541) but never checks NextCommand >= header.HeaderSize (64). A value like 8, 16, 24, 32, 40, 48, or 56 passes the alignment guard and reaches `body = data[header.HeaderSize:bodyEnd]` where bodyEnd = NextCommand < 64, producing `data[64:N]` with N < 64 \u2014 index out of range panic.", "why": "Any unauthenticated client can send a compound request with NextCommand = 8 (non-zero, 8-byte aligned, passes every existing check). The panic propagates to handleRequestPanic recover(), killing only that connection goroutine. Repeated across new connections = per-connection DoS from unauthenticated peers.", "fix": "Add `if hdr.NextCommand > 0 && hdr.NextCommand < uint32(header.HeaderSize) { return nil, nil, nil, fmt.Errorf(\"compound NextCommand too small: %d\", hdr.NextCommand) }` after the alignment check at line 541.", "dimension": "bugs", "area": "smb-framing-dispatch-compound", "confirmed": true, "verdict": "compound.go:541-553: only checks 8-byte alignment (NextCommand%8!=0), no check for NextCommand>=HeaderSize(64). Values like 8,16..56 pass. Then bodyEnd=NextCommand<64, body=data[64:bodyEnd] where bodyEnd<64 \u2014 index out of range panic."}, {"title": "Compound NextCommand < HeaderSize causes panic in splitCompoundBody", "file": "/tmp/dittofs-audit/internal/adapter/smb/framing.go", "line": 306, "severity": "HIGH", "what": "splitCompoundBody processes the FIRST command of a compound (called from parseSMB2Message in the connection read loop). bodyEnd = min(int(hdr.NextCommand), len(message)). When NextCommand is e.g. 56 and message length >= 56, bodyEnd = 56 and `message[header.HeaderSize:bodyEnd]` = `message[64:56]` \u2014 index out of range panic. Same class as the ParseCompoundCommand bug; the framing path has no prior NextCommand < HeaderSize guard either.", "why": "parseSMB2Message is invoked in the Serve read loop before any goroutine spawn. The panic propagates to the deferred handleConnectionClose recover(), terminating the connection goroutine. However, unlike the compound.go path, this fires on the FIRST compound command before any authentication handshake completes \u2014 any client connecting on port 12049 can trigger it.", "fix": "In splitCompoundBody, guard before slicing: `if hdr.NextCommand > 0 && int(hdr.NextCommand) < header.HeaderSize { body = nil; remaining = nil; return }` \u2014 or return an error sentinel and let parseSMB2Message surface it. Alternatively add the same check in parseSMB2Message before calling splitCompoundBody: if hdr.NextCommand > 0 && hdr.NextCommand < uint32(header.HeaderSize), return STATUS_INVALID_PARAMETER.", "dimension": "bugs", "area": "smb-framing-dispatch-compound", "confirmed": true, "verdict": "framing.go:303-306: splitCompoundBody has bodyEnd=min(int(hdr.NextCommand),len(message)). With NextCommand=56 and len(message)>=56, bodyEnd=56, body=message[64:56] \u2014 panic. No NextCommand>=HeaderSize guard. Fires before any auth on the connection read loop."}, {"title": "LockNew: orphaned lock-owner + lock-state on seqid error", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 1761, "severity": "HIGH", "what": "In LockNew, new lockOwner inserted into sm.lockOwners (line 1761) and new lockState inserted into openState.LockStates + sm.lockStateByOther (lines 1788-1789) BEFORE seqid validation at lines 1794-1817. On ErrBadSeqid return both objects stay in maps permanently.", "why": "Orphaned lock-owner + lock-state persist until lease expiry. Subsequent LockNew from same (clientID, ownerData) finds ownerExists=true; subsequent CLOSE fails NFS4ERR_LOCKS_HELD on phantom lock, blocking file close. Client is wedged until lease expires.", "fix": "Move seqid validation (step 6) BEFORE state creation (steps 4-5). Only allocate lockOwner/lockState after confirming the seqid is a valid new request \u2014 mirror Linux nfsd nfsd4_lock which validates seqids before allocating state.", "dimension": "bugs", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "manager.go LockNew: lockOwner inserted at line 1761, lockState inserted at lines 1788-1789, THEN seqid validation at lines 1794-1817. ErrBadSeqid return leaves both objects in sm.lockOwners and sm.lockStateByOther permanently until lease expiry."}, {"title": "freeLockStateidLocked: unconditionally deletes lock-owner when freeing one stateid", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/stateid.go", "line": 392, "severity": "HIGH", "what": "freeLockStateidLocked deletes the lock-owner from sm.lockOwners (line 392) when freeing a single lock stateid, even when the owner has sibling lock states on other open files still live in sm.lockStateByOther.", "why": "Sibling LockState objects in sm.lockStateByOther still hold a *LockOwner pointer. After the delete, the owner is absent from sm.lockOwners so ReleaseLockOwner becomes a no-op for that owner, and onLeaseExpired / EvictV40Client skip its cleanup path, leaking the sibling lock states and the underlying byte-range locks in the LockManager forever.", "fix": "Before deleting from sm.lockOwners, verify no other LockState in sm.lockStateByOther still references this owner. Only delete when reference count drops to zero \u2014 same approach ReleaseLockOwner already uses (scan lockStateByOther for matching LockOwner pointer).", "dimension": "bugs", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "stateid.go:390-393 freeLockStateidLocked unconditionally deletes lock-owner from sm.lockOwners regardless of sibling lock states still in sm.lockStateByOther. After delete, onLeaseExpired/EvictV40Client skip cleanup for the owner, leaking sibling LockState objects and their byte-range locks."}, {"title": "reapExpiredSessions: BackchannelSender goroutines leak on lease-expiry reap path", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 2645, "severity": "HIGH", "what": "reapExpiredSessions manually deletes expired-client sessions from sessionsByID/sessionsByClientID (lines 2645-2648) then calls purgeV41Client (line 2666). Neither path calls stopBackchannelSender. The BackchannelSender goroutine (started via StartBackchannelSender) is only stopped inside destroySessionLocked, which reapExpiredSessions never calls.", "why": "One goroutine leaks per expired v4.1 client. The goroutine holds a reference to the session object (deleted from the map but not freed) and continues attempting backchannel sends indefinitely, burning goroutine stack and potentially accessing freed/replaced state.", "fix": "In the reaper's lease-expired branch, before deleting sessions call sm.stopBackchannelSender(session.SessionID) for each session, OR replace the manual delete loop with calls to sm.destroySessionLocked(session.SessionID, true, 'lease_expired') which already handles backchannel teardown cleanly.", "dimension": "bugs", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "purgeV41Client (v41_client.go:298) destroys sessions via map delete but never calls stopBackchannelSender. The reapExpiredSessions path (manager.go:2644-2648) also deletes sessionsByID/sessionsByClientID directly, bypassing destroySessionLocked. stopBackchannelSender is only called from destroySessionLocked (line 2538). BackchannelSender goroutines (started via StartBackchannelSender) remain running indefinitely for every expired v4.1 client."}, {"title": "CREATE default-case XDR stream desync \u2014 objname+fattr4 not consumed", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/create.go", "line": 103, "severity": "HIGH", "what": "The `default` branch of the `objType` switch returns `NFS4ERR_BADTYPE` immediately without consuming `objname` (component4 = XDR string) and `createattrs` (fattr4 = bitmap + opaque) from the reader. Any unknown/unexpected `createtype4` value (e.g. `NF4ATTRDIR`=8 or future types) leaves the reader positioned mid-stream.", "why": "COMPOUND relies on strict sequential XDR decoding; once desynchronised every subsequent op in the same COMPOUND decodes garbage, producing cascading `NFS4ERR_BADXDR` errors or silent wrong-op dispatch. The `NF4REG` case (lines 91-101) correctly consumes `DecodeString + skipFattr4` before returning \u2014 the `default` case must do the same.", "fix": "Before returning in the `default` case, add `_, _ = xdr.DecodeString(reader)` (consume objname) then `skipFattr4(reader)` (consume createattrs), mirroring the `NF4REG` case.", "dimension": "bugs", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "create.go default case (line 103-109) returns NFS4ERR_BADTYPE immediately. objname decode (line 112) and createattrs decode (line 131) come after the switch, so they are never reached. NF4REG explicitly consumes string+fattr4 before returning (lines 94-95); default consumes nothing, leaving the XDR reader mid-stream. Subsequent ops in the same COMPOUND decode garbage."}, {"title": "OPEN CLAIM_NULL: file created before delegation-conflict check \u2014 orphan on NFS4ERR_DELAY", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/open.go", "line": 293, "severity": "HIGH", "what": "`handleOpenClaimNull` with `OPEN4_CREATE` calls `metaSvc.CreateFile` (line 293, sets `created=true`) and then checks `CheckDelegationConflict` (line 326). When a conflict exists, `NFS4ERR_DELAY` is returned (line 332). The newly-created file is committed in metadata but has no open state and no owner.", "why": "On client retry: (a) with `GUARDED4` the file now exists \u2192 `NFS4ERR_EXIST` (incorrect; client never got `NFS4_OK`). (b) with `UNCHECKED4` the orphaned empty file is opened as existing \u2014 correct result but wrong causal sequence. The comment at line 322 says 'check BEFORE creating state' but the file create has already happened. RFC 7530 \u00a716.16 requires atomic create-or-open; the delegation conflict must be tested before any metadata mutation.", "fix": "Move `CheckDelegationConflict` to before the `metaSvc.CreateFile` call (i.e., before the `if openType == types.OPEN4_NOCREATE` block or at minimum before the create branch at line 286). For `OPEN4_CREATE` with a non-existing file, check the delegation conflict against the parent directory entry first; if conflict, return `NFS4ERR_DELAY` without creating.", "dimension": "bugs", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "open.go lines 293-306: metaSvc.CreateFile runs and sets created=true before CheckDelegationConflict (line 326). If conflict exists, NFS4ERR_DELAY is returned with the file already committed in metadata. With GUARDED4 the client's retry receives NFS4ERR_EXIST; file is an orphan with no open state or owner."}, {"title": "OPEN CLAIM_DELEGATE_CUR missing v4.1 auto-confirm \u2014 open-owner stuck unconfirmed", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/open.go", "line": 666, "severity": "HIGH", "what": "`handleOpenClaimDelegateCur` does not strip `OPEN4_RESULT_CONFIRM` from `openResult.RFlags` nor call `StateManager.ConfirmOpenV41` when `ctx.SkipOwnerSeqid` is true (v4.1 session). `StateManager.OpenFile` sets `OPEN4_RESULT_CONFIRM` for any new open-owner (state/manager.go:1196).", "why": "`CLAIM_NULL` (line 358-363) and `CLAIM_PREVIOUS` (line 452-455) both guard this with `if ctx.SkipOwnerSeqid && openResult.RFlags&types.OPEN4_RESULT_CONFIRM != 0`. `CLAIM_DELEGATE_CUR` omits it entirely. A v4.1 client receives `OPEN4_RESULT_CONFIRM` in the response, cannot send `OPEN_CONFIRM` (removed in v4.1), and the open-owner remains unconfirmed. All subsequent LOCK operations require a confirmed open-owner and will fail with `NFS4ERR_BAD_STATEID` or equivalent.", "fix": "Add the same guard used in `CLAIM_NULL` immediately after the `IsReplay` check (line 666): `if ctx.SkipOwnerSeqid && openResult.RFlags&types.OPEN4_RESULT_CONFIRM != 0 { openResult.RFlags &^= types.OPEN4_RESULT_CONFIRM; _ = h.StateManager.ConfirmOpenV41(&openResult.Stateid) }`", "dimension": "bugs", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "handleOpenClaimDelegateCur (open.go:666-681) has no SkipOwnerSeqid/OPEN4_RESULT_CONFIRM guard. CLAIM_NULL (lines 358-362) and CLAIM_PREVIOUS (lines 452-454) both strip OPEN4_RESULT_CONFIRM and call ConfirmOpenV41 when ctx.SkipOwnerSeqid is set; CLAIM_DELEGATE_CUR omits this entirely. v4.1 clients receive OPEN4_RESULT_CONFIRM, cannot send OPEN_CONFIRM, and the open-owner stays unconfirmed, causing LOCK/CLOSE to fail."}, {"title": "ReturnDelegation drops sm.mu mid-function \u2014 concurrent RevokeDelegation races on same delegation", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/delegation.go", "line": 306, "severity": "HIGH", "what": "ReturnDelegation holds sm.mu, calls StopRecallTimer, then for directory delegations drops sm.mu (line 306) to call flushDirNotifications, then reacquires (line 308). During the unlocked window the delegation still exists in delegByOther with RecallTimer stopped but not removed. A concurrent RevokeDelegation timer (already past Stop because Stop has a race window per time.Timer docs) can fire, set deleg.Revoked=true, call removeDelegFromFile, delete delegStateidMap entry, and call LockManager.RevokeDelegation. When ReturnDelegation reacquires (line 308) it then also calls delete(delegByOther) and LockManager.ReturnDelegation on the same lmDelegID \u2014 double-calling LockManager on the same delegation ID.", "why": "The lock-drop TOCTOU window is reachable whenever the recall timer fires with a short timeout (5s) at the same moment DELEGRETURN arrives. LockManager called with both RevokeDelegation and ReturnDelegation on the same ID corrupts per-file lock accounting.", "fix": "After reacquiring sm.mu at line 308, check if deleg.Revoked is now true and return nil (delegation was concurrently revoked, client returning a revoked delegation is valid). Also consider removing the delegation from delegByOther before dropping the lock so RevokeDelegation's exists-check fails cleanly.", "dimension": "bugs", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "ReturnDelegation (delegation.go:306-308) drops sm.mu for directory delegations to call flushDirNotifications. During that window, RevokeDelegation (manager.go:785) can fire, delete delegStateidMap entry, and call lockMgr.RevokeDelegation. When ReturnDelegation reacquires, it calls lockMgr.ReturnDelegation on the same lmDelegID. Whether this corrupts LockManager state depends on LockManager semantics, but the double-call is real. Window is only for directory delegations."}, {"title": "CB_SEQUENCE seqID and RPC XID share single atomic counter \u2014 seqIDs skip every other value", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/backchannel.go", "line": 270, "severity": "HIGH", "what": "sendCallback calls bs.nextXID.Add(1) at line 270 to get seqID, then bs.nextXID.Add(1) again at line 284 to get xid. Both values come from the same atomic counter, so seqID advances 1,3,5,... (skipping even values) and xid advances 2,4,6,.... Per RFC 8881 \u00a72.10.6.3 the server MUST increment the slot sequence by exactly 1 per new request; a client seeing slot 0 sequence jump from 1 to 3 MUST reply NFS4ERR_SEQ_MISORDERED.", "why": "All v4.1 CB callbacks after the very first are rejected by well-behaved clients. sendCallbackWithRetry exhausts 3 retries, calls setBackchannelFault, and all further recalls use the 5-second revocation timer. Effectively all v4.1 delegations are revoked after 5s instead of honoring the lease period \u2014 cross-protocol consistency broken.", "fix": "Use two separate counters: keep bs.nextXID for RPC XIDs only. Add a separate per-slot seqID field (e.g. in SlotTable or as a dedicated atomic on BackchannelSender) incremented by 1 for each new CB_SEQUENCE send.", "dimension": "bugs", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "backchannel.go lines 270 and 284 both call bs.nextXID.Add(1) on the same atomic counter. seqID increments 1,3,5,... and xid increments 2,4,6,... Per RFC 8881 \u00a72.10.6.3, slot sequence must increment by exactly 1 per new request. Well-behaved clients reject the 1\u21923 jump with NFS4ERR_SEQ_MISORDERED, causing all callbacks to fail and delegations to be revoked after 5s."}, {"title": "CheckDelegationConflict recalls only first conflicting delegation \u2014 remaining READ holders never recalled", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/delegation.go", "line": 475, "severity": "HIGH", "what": "The loop marks RecallSent and launches go sendRecall for the first conflicting delegation, then immediately returns true (line 477). If multiple clients each hold a READ delegation and an incoming OPEN requests WRITE access, only the first READ delegation is recalled. All other READ delegation holders are never sent CB_RECALL.", "why": "RFC 7530 \u00a710.4.5: when a WRITE open conflicts with existing READ delegations all READ delegation holders must be recalled. With multiple READ holders only one is recalled; others continue caching stale data. The WRITE opener sees NFS4ERR_DELAY repeatedly until each holder happens to be the first in the map iteration on successive OPEN retries, requiring O(n) client retries in the worst case and leaving the write-delegation exclusivity invariant violated until all accidental misses are covered.", "fix": "Remove the early return inside the if-block. Accumulate all conflicting delegations, mark all RecallSent/RecallTime, launch goroutines for all, then return true after the loop completes if any conflict was found.", "dimension": "bugs", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "CheckDelegationConflict (delegation.go:463-478): on first conflicting delegation, sets RecallSent, launches sendRecall goroutine, then returns true immediately. Multiple READ holders are never all recalled. RFC 7530 \u00a710.4.5 requires all READ delegation holders be recalled on WRITE conflict. Remaining holders must be hit by accident on successive OPEN retries."}, {"title": "DESTROY skips call-header MIC verification \u2014 unauthenticated context teardown", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/framework.go", "line": 412, "severity": "HIGH", "what": "handleDestroy receives only cred (no verifBody, no headerPreimage). It deletes the context immediately without verifying the call-header MIC. The dispatch at line 412 passes neither the verifier body nor the header preimage to handleDestroy.", "why": "RFC 2203 Section 5.3.3.3 states DESTROY is formulated like a DATA request and MUST carry a valid sequence number and header checksum verifier. Any on-path observer who captures a context handle from the INIT reply (sent in plaintext) can craft a DESTROY packet and force-delete an active client's security context, causing a DoS: the victim's next DATA request returns CREDPROBLEM. With krb5 svc_none where request bodies are unencrypted, this also allows replay of context destruction.", "fix": "Pass verifBody and headerPreimage to handleDestroy from Process (same as handleData). Inside handleDestroy, call verifyHeaderMIC and gssCtx.SeqWindow.Accept before p.contexts.Delete, returning RPCSEC_GSS_CREDPROBLEM on failure.", "dimension": "bugs", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "framework.go line 413: handleDestroy receives only cred, no verifBody or headerPreimage. No sequence number or header MIC validation is done before deleting the context. Any attacker who captures a context handle from INIT replies can forge a DESTROY packet and kill active sessions. RFC 2203 \u00a75.3.3.3 requires DESTROY to carry a valid header checksum verifier."}, {"title": "readXDROpaque uses reader.Read instead of io.ReadFull \u2014 silent short-read on truncated input", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/integrity.go", "line": 155, "severity": "HIGH", "what": "readXDROpaque calls reader.Read(data) on a *bytes.Reader. bytes.Reader.Read returns (n, nil) with n < len(data) when fewer bytes remain than requested; the trailing data[n:] bytes stay zero-filled. The short read goes undetected.", "why": "A crafted integrity/privacy-wrapped message with a falsely large opaque-length field causes databodyInteg or checksumBytes to be partially zeroed. For integrity, the MIC is verified over wrong data producing a misleading error. For privacy (same function called from privacy.go:69), a truncated ciphertext is passed to crypto.DecryptMessage; if the cipher does not enforce minimum length it may produce zero-filled plaintext where bodySeqNum == 0 matches credSeqNum == 0 on a fresh context, potentially bypassing the dual seq_num validation.", "fix": "Replace reader.Read(data) with io.ReadFull(reader, data). io.ReadFull returns io.ErrUnexpectedEOF on a short read, propagating a clear error.", "dimension": "bugs", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "integrity.go line 155: reader.Read(data) on *bytes.Reader \u2014 returns (n<len(data), nil) when insufficient bytes remain, leaving data[n:] zero-filled with no error. Short-read goes undetected; databodyInteg or checksumBytes may be partially zeroed, causing MIC verification over wrong data or passing zero-filled ciphertext to crypto.DecryptMessage."}, {"title": "Double-reply for unknown NFSv4 procedure: writeReply + sendReply both fire on same XID", "file": "/tmp/dittofs-audit/pkg/adapter/nfs/handlers.go", "line": 359, "severity": "HIGH", "what": "`handleNFSv4Procedure` default case calls `c.writeReply(call.XID, errorReply)` to send PROC_UNAVAIL, then returns `(nil, writeReply_err)`. When `writeReply` succeeds it returns nil, so caller `handleRPCCall` receives `(replyData=nil, err=nil)`, skips all error branches, and falls through to `c.sendReply(call.XID, nil)` (line 251), sending a second empty RPC success reply for the same XID.", "why": "Two reply frames for the same XID on the same TCP connection violate RFC 5531 and corrupt the RPC record stream. The client's RPC layer will misparse subsequent replies, causing cascading failures on any NFSv4 session that triggers the unknown-procedure branch (procedure number > 1).", "fix": "Return before `sendReply` fires. Either (a) use `return c.handleUnsupportedVersion(...)` pattern and let the caller not reach `sendReply`, or (b) return a sentinel so `handleRPCCall` knows a reply was already sent. Simplest: change the default case to not call `writeReply` at all \u2014 just return an error or a pre-formed reply bytes, consistent with how other error paths work:\n```go\ndefault:\n    errorReply, err := rpc.MakeErrorReply(call.XID, rpc.RPCProcUnavail)\n    if err != nil {\n        return nil, fmt.Errorf(\"make NFSv4 proc unavail reply: %w\", err)\n    }\n    return errorReply, nil  // let handleRPCCall send via sendReply\n```\nOr, if the intent is to send immediately and skip `sendReply`, use `return` directly from `handleRPCCall` like the `handleUnsupportedVersion` calls do.", "dimension": "bugs", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "handlers.go line 359: default case calls c.writeReply (sends PROC_UNAVAIL reply) then returns (nil, nil) when writeReply succeeds. In dispatch.go line 174, replyData=nil, err=nil. No nil guard exists; falls through to sendReply(xid, nil) at line 251, sending a second empty RPC success reply for the same XID. Two reply frames corrupt the RPC record stream."}, {"title": "Flush propagates ErrChunkLostBeforeMirror as hard error \u2014 protocol handlers fail on retryable condition", "file": "/tmp/dittofs-audit/pkg/block/engine/syncer.go", "line": 339, "severity": "HIGH", "what": "mirrorOnce returns block.ErrChunkLostBeforeMirror when a pending hash has no local bytes (chunk lost before first mirror). Flush at line 339 propagates this as a non-nil error to callers (NFS COMMIT handler, SMB Flush handler). syncLocalBlocks (upload.go:32) and uploadBlock (upload.go:69) both explicitly catch and swallow ErrChunkLostBeforeMirror as a non-fatal retry condition. Flush does not.", "why": "Flush godoc defines err!=nil as 'hard failure, do not retry until addressed'. Protocol handlers receiving this error return a protocol-level error to NFS/SMB clients (e.g. NFS COMMIT fails, client may abort writes). The correct contract for this condition is Finalized=false, err=nil (soft condition), letting the client re-issue COMMIT/Flush on its own schedule. The inconsistency causes spurious hard failures on a retry-next-tick condition that other callers already handle gracefully.", "fix": "In Flush after mirrorOnce: if errors.Is(err, block.ErrChunkLostBeforeMirror) { return &block.FlushResult{Finalized: false}, nil }. This mirrors the handling in syncLocalBlocks and uploadBlock and matches the Flush soft-condition contract (Finalized=false, err=nil).", "dimension": "bugs", "area": "block-engine-gc-syncer", "confirmed": true, "verdict": "`mirrorOnce` returns `block.ErrChunkLostBeforeMirror` at syncer.go:448. `Flush` at line 339 propagates it as `return nil, err`. `CommitBlockStore` returns it verbatim. NFSv4 COMMIT handler at commit.go:124 returns `NFS4ERR_IO`. The periodic uploader (upload.go:32) and `uploadBlock` (upload.go:69) both swallow it as non-fatal \u2014 but `Flush`/`CommitBlockStore` do not. Godoc says `err!=nil` is a hard failure; this condition is retryable."}, {"title": "groupCommit.Sync failure leaves lf.eofPos stale, corrupting logIndex on next write", "file": "/tmp/dittofs-audit/pkg/block/local/fs/appendwrite.go", "line": 423, "severity": "HIGH", "what": "When lf.groupCommit.Sync(ctx) returns an error (ctx cancellation while piggybacked on in-flight fsync, or raw fsync error in bypass path), the function returns at line 424 without advancing lf.eofPos and without evicting the fd. writeRecord at line 372 already wrote the full record to lf.f, advancing the OS file position by n bytes. The next AppendWrite for the same payload calls writeRecord again \u2014 the OS continues appending at old_eof+n \u2014 but logPos := lf.eofPos (line 433) equals old_eof, pointing at the abandoned record's frame. lf.eofPos += uint64(n) (line 434) then lands at old_eof+n_new, not old_eof+n+n_new. Every subsequent logIndex entry is off by n bytes, so readRecordAt will pread the wrong position, causing CRC mismatches and silent data loss in rollup.", "why": "lf.eofPos is the canonical log-position cursor used by the logIndex to record where each record lands. It must equal the actual OS file position at all times while the fd is live. Failing fsync without evicting the fd leaves eofPos behind the true file position. The writeRecord error path (lines 403-409) correctly evicts the fd, but the Sync error path at line 423 does not apply the same recovery posture despite the same invariant violation.", "fix": "On groupCommit.Sync failure, apply the same fd-eviction posture as the writeRecord error path: mu.Unlock(); tsh.mu.Lock(); delete(tsh.logFDs, payloadID); tsh.mu.Unlock(); _ = lf.f.Close(). Alternatively, capture n as the pre-advance value before calling Sync and advance lf.eofPos unconditionally after writeRecord succeeds (before Sync), since the bytes are already on disk regardless of fsync outcome. The second approach matches the invariant more directly: eofPos tracks physical file size, not durability.", "dimension": "bugs", "area": "block-local-fs-appendlog", "confirmed": true, "verdict": "`writeRecord` at line 372 writes data to `lf.f`, advancing the OS file position by `n` bytes. If `lf.groupCommit.Sync` at line 423 fails, the function returns at line 424 WITHOUT advancing `lf.eofPos` and WITHOUT evicting the fd. The next AppendWrite writes at OS position `old_eof+n`, but `logPos := lf.eofPos` (line 433) = `old_eof`, so the logIndex entry points to the abandoned record frame, not the new record. The `writeRecord` error path (lines 403-409) correctly evicts the fd; the Sync error path does not."}, {"title": "ResetLocalState skips subdirectory .log files, leaving stale append-log data after restore", "file": "/tmp/dittofs-audit/pkg/block/local/fs/drain_reset.go", "line": 272, "severity": "HIGH", "what": "Best-effort residual-log sweep in ResetLocalState uses os.ReadDir(logsDir) and explicitly skips directories (`if e.IsDir() { continue }`). PayloadIDs with a share prefix (e.g. `share1/myfile`) produce log files at `<logsDir>/share1/myfile.log`, nested inside a subdirectory. ReadDir returns that subdirectory as a DirEntry with IsDir()==true and it is skipped. The nested `.log` file is never unlinked.", "why": "After a snapshot restore, RestoreSnapshot calls ResetLocalState to drop all append-log state so reads resolve purely through the restored CAS manifest. If any share-prefixed payload has a log file (the common case \u2014 recovery.go:236-240 explicitly documents share-prefixed payloadIDs stored in subdirectories), that file survives the reset. The next Recover() call uses filepath.WalkDir (recursive) and reinstalls the stale log file. ReadPayloadAt then replays the surviving post-snapshot append-log records on top of the restored CAS content ('last record wins'), silently returning post-snapshot mutated bytes instead of the restored content \u2014 exactly the data corruption ResetLocalState is designed to prevent.", "fix": "Replace the os.ReadDir flat sweep with filepath.WalkDir (or os.RemoveAll on the entire logsDir and recreate it). Minimal fix: `_ = filepath.WalkDir(logsDir, func(path string, d os.DirEntry, err error) error { if err != nil || d.IsDir() { return nil }; _ = os.Remove(path); return nil })`. Alternatively, `os.RemoveAll(logsDir)` followed by `os.MkdirAll(logsDir, 0755)` is simpler and atomic for the whole tree.", "dimension": "bugs", "area": "block-local-fs-rollup-chunk", "confirmed": true, "verdict": "`ResetLocalState` at drain_reset.go:272-283 uses `os.ReadDir(logsDir)` (non-recursive) and skips directories (`if e.IsDir() { continue }`). `logPath` at appendwrite.go:58-59 uses `filepath.Join(bc.baseDir, \"logs\", payloadID+\".log\")` \u2014 a payloadID with a slash (e.g. `share1/myfile`) produces `logs/share1/myfile.log` nested in a subdirectory. recovery.go:18-21 documents share-prefix payloadIDs as normal. The nested `.log` survives reset and is re-installed by the recursive `filepath.WalkDir` in Recover, replaying stale post-snapshot data."}, {"title": "updateDescendantPaths silently discards tx.PutFile error", "file": "/tmp/dittofs-audit/pkg/metadata/file_modify.go", "line": 829, "severity": "HIGH", "what": "Error from tx.PutFile(ctx, child) is discarded with `_`. Any child whose path fails to persist returns no error; updateDescendantPaths returns nil and the whole Move transaction commits, leaving descendant File.Path values stale.", "why": "A failed PutFile means the child's path in the store still holds the old prefix. On postgres (path-keyed) this silently corrupts the namespace: snapshot/restore, GetFileByPayloadID, and path-based lookups resolve to dead/wrong paths. The caller's comment at line 758\u2013763 explicitly says 'Propagate the error: a partial descendant rewrite leaves stale child Paths that diverge from the parent' \u2014 but the inner PutFile error is not propagated, so the entire protection is a no-op.", "fix": "Return the error instead of discarding it: `if err := tx.PutFile(ctx, child); err != nil { return fmt.Errorf(\"update path for %s: %w\", child.Path, err) }`", "dimension": "bugs", "area": "metadata-service-auth", "confirmed": true, "verdict": "file_modify.go:829 discards `tx.PutFile(ctx, child)` error with `_`. `updateDescendantPaths` returns `nil` even when child path update fails. The caller comment at line 758-763 says partial rewrite leaves stale paths, but the protection is a no-op. On postgres (path-keyed) this silently corrupts the namespace after Move."}, {"title": "PrepareWrite owner-bypass skips DOS READONLY enforcement", "file": "/tmp/dittofs-audit/pkg/metadata/io.go", "line": 143, "severity": "HIGH", "what": "PrepareWrite unconditionally skips checkWritePermission when the caller is the file owner (isOwner == true). checkWritePermission \u2192 checkFilePermissions \u2192 calculatePermissions is the only code path that enforces modeDOSReadonly (0x100000). An SMB client sets FILE_ATTRIBUTE_READONLY; the file owner on NFS can then write despite that flag.", "why": "calculatePermissions clears the write permBit when mode&0x100000 is set (line 334) and has a dedicated conformance test (TestCalculatePermissions_DOSReadonlyDeniesOwnerWriteCrossProtocol in auth_permissions_sid_test.go:43). The owner bypass at io.go:145 short-circuits before that test is ever reached, breaking the documented cross-protocol READONLY contract. Root bypass is elsewhere and separately correct; this is the non-root owner path.", "fix": "Remove the owner short-circuit in PrepareWrite. Always call checkWritePermission regardless of ownership \u2014 the write permission check already handles owner vs. non-owner mode bit evaluation inside calculatePermissions. The comment 'Owner can always write to their own files (even if mode is 0444)' is wrong for DOS READONLY files; POSIX open-time check semantics don't apply to the SMB-layer READONLY flag.", "dimension": "bugs", "area": "metadata-service-auth", "confirmed": true, "verdict": "`PrepareWrite` at io.go:143-150 skips `checkWritePermission` when `isOwner==true`. The DOS readonly enforcement (`attr.Mode&0x100000 != 0` clears write permBit) lives inside `calculatePermissions` at auth_permissions.go:334, which is only reachable via `checkFilePermissions` \u2192 `checkWritePermission`. Owner bypass short-circuits before that path. A file owner can write to a `FILE_ATTRIBUTE_READONLY` file, violating the cross-protocol readonly contract."}, {"title": "AdjustACLForMode drops Protected/AutoInherited/NullDACL/Source on chmod", "file": "/tmp/dittofs-audit/pkg/metadata/acl/mode.go", "line": 99, "severity": "HIGH", "what": "AdjustACLForMode returns `&ACL{ACEs: newACEs}` \u2014 a fresh struct with only the ACEs field set. The input ACL's `Protected` (SE_DACL_PROTECTED), `AutoInherited` (SE_DACL_AUTO_INHERITED), `NullDACL`, and `Source` fields are silently discarded.", "why": "Protected=true means the DACL blocks further inheritance from ancestors. Dropping it on chmod means a file/dir that was shielded from ancestor propagation silently becomes inheritable again on the next `ComputeInheritedACL` pass \u2014 wrong. Dropping AutoInherited means `ComputeInheritedACL` no longer stamps `ACE4_INHERITED_ACE` on children, causing inheritance-flag drift visible to smbtorture. Called from `pkg/metadata/file_modify.go:284` on every SETATTR mode change.", "fix": "Copy ACL metadata fields from the input: `return &ACL{ACEs: newACEs, Source: a.Source, Protected: a.Protected, AutoInherited: a.AutoInherited, NullDACL: a.NullDACL}`", "dimension": "bugs", "area": "metadata-acl", "confirmed": true, "verdict": "`AdjustACLForMode` at mode.go:99 returns `&ACL{ACEs: newACEs}` \u2014 only the `ACEs` slice is set. Input ACL fields `Protected` (SE_DACL_PROTECTED), `AutoInherited`, `NullDACL`, and `Source` are all zero-valued in the returned struct. Dropping `Protected=true` means a DACL that blocks ancestor propagation silently loses that protection on the next chmod, allowing `ComputeInheritedACL` to overwrite explicitly shielded ACEs."}, {"title": "PropagateACL drops existingACL.Protected \u2014 SE_DACL_PROTECTED silently cleared on re-propagation", "file": "/tmp/dittofs-audit/pkg/metadata/acl/inherit.go", "line": 340, "severity": "HIGH", "what": "PropagateACL builds `result := &ACL{ACEs: combined}` without copying `existingACL.Protected` (SE_DACL_PROTECTED). The code's own comment (line 308) correctly states Protected is per-SD and never inherited, but that means it must be PRESERVED from the child's existing SD, not silently zeroed.", "why": "A child directory with Protected=true (set by Windows ACL editor or explicit SetInfo) is supposed to block further ancestor propagation. After any parent-ACL change triggers recursive PropagateACL, the child's Protected flag is lost on the returned ACL. Subsequent propagation passes will overwrite the child's ACEs with parent-derived ACEs that the operator explicitly blocked. AutoInherited from existingACL is also discarded, though the parent's value overwrites it anyway \u2014 this is a lesser issue. The real harm is the Protected erasure.", "fix": "Preserve the existing child SD flags: after constructing `result`, add `result.Protected = existingACL.Protected` and `result.Source = existingACL.Source`. The AutoInherited override from parent is intentional per the spec but Protected must survive from the child.", "dimension": "bugs", "area": "metadata-acl", "confirmed": true, "verdict": "`PropagateACL` at inherit.go:340 builds `result := &ACL{ACEs: combined}` and only conditionally copies `parentACL.AutoInherited` (line 344-346). `existingACL.Protected` is never copied to `result`. A child with `Protected=true` (blocking ancestor propagation) has that flag erased on every parent-ACL-change-triggered propagation pass, causing subsequent passes to overwrite the child's explicitly protected ACEs with parent-derived ones."}, {"title": "OpLockBreakScanner uses AcquiredAt (grant time) instead of BreakStarted for break timeout, immediately force-revoking long-held leases", "file": "/tmp/dittofs-audit/pkg/metadata/lock/oplock_break.go", "line": 274, "severity": "HIGH", "what": "`scanExpiredLeaseBreaks` computes `breakDeadline = pl.AcquiredAt.Add(timeout)`. `AcquiredAt` is the lease *grant* time (set once in `ToPersistedLock` from `lock.AcquiredAt`, never updated). `BreakStarted` (the actual break-start timestamp) is explicitly not persisted (`store.go:434`). For any lease older than `DefaultOpLockBreakTimeout` (35s) at the time the break is initiated, `AcquiredAt + timeout` is already in the past, so every scan tick immediately force-revokes the lease.", "why": "A long-lived RWH lease (e.g., 60 seconds old) that just had its break initiated will have `AcquiredAt` 60 seconds in the past. With the 35s default timeout, `breakDeadline = AcquiredAt + 35s` is already 25 seconds in the past. On the next 1-second scan tick, `now.After(breakDeadline)` is true and the lease is force-revoked before the client has any time to ACK. This violates MS-SMB2 \u00a73.3.6.5 which requires the server to wait at least 35 seconds from break *initiation*, not from lease *grant*.", "fix": "Persist `BreakStarted` in `PersistedLock` (add a `BreakStarted time.Time` field, populate it in `ToPersistedLock` when `lock.Lease.Breaking`, restore in `FromPersistedLock`). Update `scanExpiredLeaseBreaks` to use `pl.BreakStarted` as the deadline base. The test at `oplock_break_test.go:151` already sets `AcquiredAt: breakStart` to paper over this gap \u2014 updating it to use the proper `BreakStarted` field will make both the scanner and the test correct.", "dimension": "bugs", "area": "metadata-lock-manager", "confirmed": true, "verdict": "`scanExpiredLeaseBreaks` at oplock_break.go:274 computes `breakDeadline = pl.AcquiredAt.Add(timeout)`. `AcquiredAt` is the lock grant time (set at creation, manager.go:826/837), never updated when `Breaking=true` is set. Break initiation sets `lock.Lease.BreakStarted = time.Now()` (manager.go:2671) but `BreakStarted` is runtime-only and not persisted (store.go:434). Comment at line 273 claiming 'AcquiredAt is updated when break initiated' is incorrect. Any lease older than 35s has `AcquiredAt+35s` already past, so the scanner force-revokes it immediately."}, {"title": "scanExpiredLeaseBreaks deletes lease from store but never updates in-memory Manager, leaving WaitForBreakCompletion permanently blocked", "file": "/tmp/dittofs-audit/pkg/metadata/lock/oplock_break.go", "line": 289, "severity": "HIGH", "what": "`scanExpiredLeaseBreaks` calls `lockStore.DeleteLock(ctx, pl.ID)` to remove a timed-out breaking lease from the persistent store, then invokes `s.callback.OnLeaseBreakTimeout(leaseKey)`. The in-memory `Manager.unifiedLocks` map is NOT updated. The `Manager` (`s.lockManager`) has `forceCompleteBreaksExceptKey` which performs the in-memory teardown, but it is never called from this path. The `OpLockBreakCallback` interface's `OnLeaseBreakTimeout` only notifies \u2014 it does not clear in-memory state.", "why": "`WaitForBreakCompletion` (manager.go:2263) polls `lm.unifiedLocks[handleKey]` for any `lock.Lease.Breaking` entries. After the scanner deletes from the store, the in-memory record still has `Breaking=true`. Callers waiting on `WaitForBreakCompletion` will never see the break resolve through the signal path; they must wait until their context deadline triggers `forceCompleteBreaks` themselves. `HasOtherBreakingLeases` and `AnyHolderHasLeaseBits` also return stale true. `signalBreakWaitLocked` is never called, so all parked CREATEs stall until their own context expires.", "fix": "After `lockStore.DeleteLock` succeeds, call `lm.forceCompleteBreaksExceptKey(handleKey, [16]byte{})` on `s.lockManager` if set, or expose a `RevokeLease(leaseKey)` method that clears both store and in-memory state atomically. Alternatively, reuse the `ReleaseLease` path which already signals waiters via `signalBreakWaitLocked`.", "dimension": "bugs", "area": "metadata-lock-manager", "confirmed": true, "verdict": "`scanExpiredLeaseBreaks` calls `lockStore.DeleteLock(ctx, pl.ID)` (persistent store only) then `s.callback.OnLeaseBreakTimeout(leaseKey)`. `OnLeaseBreakTimeout` has no production implementation (only a test mock). `Manager.unifiedLocks` still has the entry with `Lease.Breaking=true`. `WaitForBreakCompletion` at manager.go:2267-2299 polls `lm.unifiedLocks[handleKey]` \u2014 finding `Breaking=true` \u2014 and waits on `breakWaitChans`, which is never signaled. Callers stall until their context deadline triggers `forceCompleteBreaks`."}, {"title": "memory DeleteShare leaks objectIndex entries and usedBytes counter", "file": "/tmp/dittofs-audit/pkg/metadata/store/memory/shares.go", "line": 151, "severity": "HIGH", "what": "Non-transactional DeleteShare iterates store.files and deletes from files/parents/children/linkCounts/deviceNumbers but never touches store.objectIndex or store.usedBytes. The transaction-path DeleteShare (transaction.go:653-671) correctly cleans both.", "why": "Stale objectIndex entries survive after share deletion. Next PutFile with same ObjectID gets spurious ErrConflict (objectIndex key still maps to deleted file's key). usedBytes counter is permanently inflated by the deleted share's regular-file sizes \u2014 GetUsedBytes/FSSTAT reports wrong usage forever.", "fix": "In the loop at line 151-158, add: if fd.Attr != nil && !fd.Attr.ObjectID.IsZero() { if mapped, ok := store.objectIndex[fd.Attr.ObjectID]; ok && mapped == key { delete(store.objectIndex, fd.Attr.ObjectID) } }. Also subtract regular-file sizes: if fd.Attr.Type == metadata.FileTypeRegular && fd.Attr.Size > 0 { store.usedBytes.Add(-int64(fd.Attr.Size)) }. Or collapse both paths so store.DeleteShare calls store.WithTransaction(ctx, func(tx) { return tx.DeleteShare(ctx, shareName) }).", "dimension": "bugs", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: memory/shares.go DeleteShare (lines 151-158) iterates store.files deleting files/parents/children/linkCounts/deviceNumbers but never touches store.objectIndex or store.usedBytes. transaction.go:653-671 correctly handles both (lines 660-663 delete objectIndex, line 657 adjusts pendingDelta). Stale objectIndex entries survive share deletion; usedBytes permanently inflated."}, {"title": "badger badgerTransaction.DeleteLocksByClient/DeleteLocksByFile open independent transactions, breaking outer-tx atomicity and OCC retry safety", "file": "/tmp/dittofs-audit/pkg/metadata/store/badger/locks.go", "line": 625, "severity": "HIGH", "what": "badgerTransaction.DeleteLocksByClient (line 625-633) and DeleteLocksByFile (line 635-641) delegate to tx.store.lockStore.DeleteLocksByClient/DeleteLocksByFile, which open new db.Update transactions independent of the caller's badger.Txn. Similarly, IncrementServerEpoch (line 651-657) and SetCleanShutdown (line 667-673) call store-level methods that open new db.Update transactions.", "why": "If the outer WithTransaction encounters ErrConflict and retries its fn closure, lock deletions from the first attempt are already committed and cannot be rolled back \u2014 locks are permanently deleted even though the outer operation will be retried. IncrementServerEpoch called inside a retried tx increments the epoch multiple times (once per retry). SetCleanShutdown commits out-of-band regardless of whether the outer tx succeeds.", "fix": "Implement in-transaction versions: DeleteLocksByClient/DeleteLocksByFile on badgerTransaction must collect lock IDs via the existing index (iterating tx.txn, same as listLocksTx) and call s.deleteLockTx(tx.txn, lockID) for each \u2014 all within tx.txn. For IncrementServerEpoch: use s.getServerEpochTx(tx.txn) + txn.Set inside tx.txn. For SetCleanShutdown: write directly to tx.txn (txn.Set([]byte(keyCleanShutdown), val)).", "dimension": "bugs", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: badger/locks.go lines 625-633 (DeleteLocksByClient) and 635-641 (DeleteLocksByFile) delegate to store.lockStore methods that open new db.Update transactions independent of caller's badger.Txn. Lines 651-657 (IncrementServerEpoch) and 667-673 (SetCleanShutdown) do same. When outer WithTransaction retries on ErrConflict, lock deletions from first attempt are already committed and irrevocable; epoch incremented once per retry."}, {"title": "Retry loop in WithTransaction never fires \u2014 mapPgError strips *pgconn.PgError from error chain", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/transaction.go", "line": 97, "severity": "HIGH", "what": "WithTransaction retries on isRetryableError(err), which does errors.As(err, &pgErr) to find *pgconn.PgError. But every operation inside fn (PutFile, DeleteFile, GetChild, etc.) wraps the PG error through mapPgError, which returns a new *metadata.StoreError{Code: ErrIOError, Message: ...} with no Unwrap(). StoreError has no Unwrap method (pkg/metadata/errors/errors.go). So errors.As never finds the original *pgconn.PgError; isRetryableError always returns false; the retry on deadlock (40P01) and serialization failure (40001) never triggers.", "why": "Every concurrent-write workload under Postgres READ COMMITTED can produce 40001/40P01 errors. The declared retry mechanism is the only defense against these transient errors, but it is unconditionally bypassed. Callers receive ErrIOError instead of a transparent retry, causing spurious failures under load.", "fix": "Either (a) have mapPgError preserve the original error with %w so StoreError implements Unwrap, or (b) check the PG error code BEFORE calling mapPgError in the retry path: extract pgcode from err immediately in fn's return path before wrapping, or (c) add an Unwrap() method to StoreError that returns the wrapped cause. Simplest: in mapPgError, return fmt.Errorf(\"...: %w\", pgErr) so errors.As can still find the underlying PgError through the StoreError.", "dimension": "bugs", "area": "metadata-store-postgres", "confirmed": true, "verdict": "Confirmed: postgres/errors.go mapPgErrorCode converts 40001 and 40P01 into new *metadata.StoreError with no Unwrap() (StoreError struct has no Unwrap method). isRetryableError (transaction.go:40-51) does errors.As(err, &pgErr) looking for *pgconn.PgError \u2014 fails on StoreError since there is no error chain. Retry on serialization/deadlock never fires; callers get ErrIOError instead."}, {"title": "SettingsWatcher.Stop() deadlocks when Start() never called", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/settings_watcher.go", "line": 127, "severity": "HIGH", "what": "`Stop()` blocks forever on `<-w.stopped` when `Start()` was never called. `stopped` is an unbuffered channel closed only inside the goroutine spawned by `Start()`. Any caller that constructs a `SettingsWatcher` and calls `Stop()` without a preceding `Start()` \u2014 including tests, early-error shutdown paths, or any code path where `lifecycle.shutdown` fires before `lifecycle.serve` reached `settings.Start(ctx)` \u2014 will deadlock.", "why": "`NewSettingsWatcher` allocates `stopped: make(chan struct{})` but no goroutine closes it unless `Start()` was called. `lifecycle.Service.shutdown()` calls `settings.Stop()` unconditionally; if the server exits before `serve()` reaches `settings.Start(ctx)`, the process hangs.", "fix": "Initialize `stopped` as an already-closed channel and re-create it inside `Start()` before launching the goroutine, or add a started flag: `if !atomic.CompareAndSwap(&w.started, 0, 1) { return }` in `Start()` and guard `<-w.stopped` in `Stop()` with the same flag. Simplest: in `Stop()`, add `select { case <-w.stopped: return; default: }` before blocking, only block if `Start()` was ever called.", "dimension": "bugs", "area": "controlplane-runtime-shares", "confirmed": true, "verdict": "Confirmed: settings_watcher.go Stop() (lines 118-129) closes stopCh then blocks on <-w.stopped. stopped channel is only closed inside goroutine spawned by Start() (line 95). If Start() never called, stopped is never closed, Stop() deadlocks forever. NewSettingsWatcher allocates stopped as unbuffered make(chan struct{}) with no goroutine to close it unless Start() runs."}, {"title": "EvictBlockStore holds s.mu.RLock() across unbounded per-file disk I/O", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/shares/service.go", "line": 1703, "severity": "HIGH", "what": "`EvictBlockStore` acquires `s.mu.RLock()` at line 1703 with `defer s.mu.RUnlock()` and then calls `bs.DestroyCache()`, `bs.ListFiles()`, and `bs.EvictLocal()` (which calls `local.DeleteAppendLog` \u2014 disk I/O) for every file in every share in the registry. All write-path callers (`AddShare`, `RemoveShare`, `UpdateShare`, `DisableShare`, `EnableShare`) need `s.mu.Lock()` and will starve for the entire duration of the eviction loop.", "why": "On a share with thousands of files, `EvictLocal` per file involves append-log deletion on disk. Holding the service-level RLock throughout serializes all share lifecycle operations against an unbounded I/O loop, causing indefinite write-lock starvation. `AddShare` / `RemoveShare` operations issued concurrently with an evict call will block until the entire eviction completes.", "fix": "Snapshot the target `*engine.Store` pointers under `s.mu.RLock()` into a local slice, release the lock, then operate on the engine stores outside the lock. The engine stores are independently thread-safe (`bs.enter()` / `bs.closeMu`). Pattern already used correctly in `DrainAllBlockStores` (lines 1596-1602).", "dimension": "bugs", "area": "controlplane-runtime-shares", "confirmed": true, "verdict": "Confirmed: shares/service.go EvictBlockStore (line 1703) acquires s.mu.RLock() with defer s.mu.RUnlock() then calls bs.DestroyCache(), bs.ListFiles(), and bs.EvictLocal() per file (line 1742) \u2014 disk I/O \u2014 for all shares. AddShare (line 470) and RemoveShare (line 966) need s.mu.Lock() and starve for entire eviction loop duration."}, {"title": "Response.Encode silently truncates stub data without error", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/dcerpc.go", "line": 348, "severity": "HIGH", "what": "When fragLen > MaxFragmentSize (65535), code clamps fragLen and allocates a smaller buf, but copy(buf[24:], r.StubData) silently truncates stub data. FragLength header field still claims MaxFragmentSize bytes are present. Caller gets no error \u2014 returns a syntactically-valid but content-corrupted response.", "why": "Client receives a complete-looking RPC PDU but reads garbage/zero bytes for the truncated portion. For LookupSids responses with many SIDs this produces incorrect SID-to-name mappings or parse errors. Should either error out or fragment the response per DCE/RPC fragmentation.", "fix": "Return an error (and a fault PDU) when stub data exceeds MaxFragmentSize-24, or implement proper multi-fragment response splitting (FlagFirstFrag/FlagLastFrag). At minimum: if fragLen > MaxFragmentSize { return buildFault(callID, 0x1C000009) } before building the response.", "dimension": "bugs", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: dcerpc.go Encode (line 342-373): when fragLen > MaxFragmentSize (65535), fragLen is clamped and buf allocated to fragLen, then copy(buf[24:], r.StubData) silently truncates stub data to fit. No error returned. FragLength header still claims MaxFragmentSize. Caller gets syntactically valid but content-corrupted PDU."}, {"title": "Data race on leaseTimeSeconds global \u2014 non-atomic uint32 written per GETATTR, read concurrently during encoding", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/attrs/encode.go", "line": 87, "severity": "HIGH", "what": "leaseTimeSeconds (plain uint32 package global) is written in SetLeaseTime (line 87) on every GETATTR call (getattr.go:45) and read concurrently in encodeSingleAttr (lines 326, 554) during parallel GETATTR/READDIR encoding. The neighboring fsMax* globals explicitly use atomic.Uint64 to avoid the same race (line 97-101), but leaseTimeSeconds does not.", "why": "Concurrent unsynchronized read+write of a non-atomic uint32 is a data race per Go memory model. go test -race will detect it. On real hardware the word is 4-byte aligned so tearing is unlikely, but the race is real and the inconsistency is clearly an oversight given the atomics used right above.", "fix": "Change leaseTimeSeconds to atomic.Uint32: var leaseTimeSeconds atomic.Uint32 and init to DefaultLeaseTime in init(). Use leaseTimeSeconds.Store(seconds) in SetLeaseTime and leaseTimeSeconds.Load() in the two read sites.", "dimension": "bugs", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "Confirmed: attrs/encode.go line 82 declares leaseTimeSeconds as plain var uint32. SetLeaseTime (line 86-88) writes it non-atomically. Neighboring fsMaxFileSize/fsMaxReadSize/fsMaxWriteSize (lines 99-101) explicitly use atomic.Uint64 with comment 'to avoid data races'. leaseTimeSeconds is read concurrently during GETATTR encoding (line 325+). Clear oversight; data race under go -race."}, {"title": "LINK: nil deref on updatedFile after GetFile error", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/link.go", "line": 239, "severity": "HIGH", "what": "After CreateHardLink succeeds, GetFile is called to refresh attributes (line 233). If it returns an error the code logs and continues (line 236), but line 239 unconditionally dereferences `updatedFile.FileAttr` \u2014 `updatedFile` is nil, causing a nil pointer panic.", "why": "The error branch says 'Continue with cached attributes' but there are no cached attributes: `updatedFile` was declared at line 233 with no prior value, so it is nil. Any transient GetFile failure (e.g. context cancelled right after CreateHardLink succeeds) crashes the server.", "fix": "Guard the dereference: if updatedFile == nil, fall back to the pre-link `fileAttr.FileAttr` captured before CreateHardLink. For example:\n  var nfsFileAttr *types.NFSFileAttr\n  if updatedFile != nil {\n      nfsFileAttr = h.convertFileAttrToNFS(fileHandle, &updatedFile.FileAttr)\n  } else {\n      nfsFileAttr = h.convertFileAttrToNFS(fileHandle, &fileAttr.FileAttr)\n  }", "dimension": "bugs", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed: nfs/v3/handlers/link.go line 233 declares updatedFile via GetFile; line 234-237 logs error but continues without setting updatedFile to anything. Line 239 unconditionally dereferences &updatedFile.FileAttr \u2014 nil pointer panic when GetFile returns error (context cancel, transient store error). Comment says 'continue with cached attributes' but no cached value exists."}, {"title": "CREATE EXCLUSIVE: verifier=0 always treated as conflict instead of retry", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/create.go", "line": 199, "severity": "HIGH", "what": "The EXCLUSIVE retry check is `existingFile.IdempotencyToken == req.Verf && req.Verf != 0`. When both are 0 the guard `req.Verf != 0` fails and the code falls through to 'different token \u2014 return NFS3ErrExist'. RFC 1813 does not forbid a zero verifier; some clients send 0 or the store may not have persisted the token (e.g. after restart). The client's valid retry is rejected as a conflict.", "why": "RFC 1813 \u00a73.3.8 says the server compares the stored verifier with the client's verifier; there is no exclusion for the zero value. Excluding zero means: (a) a client using verf=0 can never successfully retry a CREATE EXCLUSIVE, and (b) if IdempotencyToken was lost (store restart) every retry returns EXIST even though the file was created by that client.", "fix": "Remove the `req.Verf != 0` guard. The comparison `existingFile.IdempotencyToken == req.Verf` is sufficient. If you want to avoid accidental matches when the token was never stored, check `existingFile.IdempotencyToken != 0 || req.Verf != 0` as the guard for the conflict path instead of blocking the retry path.", "dimension": "bugs", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed at create.go:199. Guard `existingFile.IdempotencyToken == req.Verf && req.Verf != 0` explicitly excludes zero verifier. When req.Verf==0 (or both are 0), falls through to the 'different token' branch at line 228 and returns NFS3ErrExist. RFC 1813 has no exclusion for zero verifier."}, {"title": "NLM FREE_ALL handler is a no-op \u2014 crashed-client locks never released via direct FREE_ALL", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/free_all.go", "line": 77, "severity": "HIGH", "what": "FreeAll() decodes the request and logs it, then returns immediately without releasing any locks. The comment says 'actual lock release is triggered by the adapter's OnClientCrash callback', but OnClientCrash is only wired through the NSM notifier path (nsm.Notifier.handleClientCrash). A Linux client that reboots and sends NLM FREE_ALL directly to the NLM port \u2014 without having previously registered via NSM SM_MON, or when NSM notification races \u2014 will receive an empty void response while all its byte-range locks remain held on the server.", "why": "Per RFC 1813 / X/Open NLMv4, FREE_ALL is the definitive crash-recovery RPC for the lock manager. rpc.lockd sends it to release all locks on reboot regardless of NSM state. If the handler does nothing, stale locks persist indefinitely, blocking other clients from acquiring those byte ranges until server restart.", "fix": "Wire the crash-cleanup directly in the handler: after decoding req.Name, call h.onClientCrash (add a callback field to Handler similar to how nsm.Notifier wires it) or accept an NLMLockService extension method ReleaseAllLocksForClient(clientID string). Minimally: h.blockingQueue.RemoveClientWaiters(req.Name) must be called, and the lock manager's ReleaseByOwnerPrefix(\"nlm:\"+req.Name+\":\") must be invoked across all shares.", "dimension": "bugs", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed at free_all.go:77-93. Handler decodes request, logs it, returns void \u2014 zero lock release. Comment says OnClientCrash handles it, but OnClientCrash is only wired through nsm/notifier.go handleClientCrash (SM_NOTIFY path). Direct NLM FREE_ALL RPC does not trigger it. Stale locks persist indefinitely."}, {"title": "NLM GRANTED callback hardcodes port 12049 \u2014 breaks blocking-lock wake-up for all real NFS clients", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/lock.go", "line": 259, "severity": "HIGH", "what": "extractCallbackAddr() strips the client port and replaces it with 12049, the server's NFS port. The NLM_GRANTED callback must be sent to the port the NLM client is listening on, which is an ephemeral port registered with the client-side portmapper. No real NFS client listens for NLM callbacks on port 12049; they register an arbitrary port with their portmapper and pass it implicitly via the RPC source address.", "why": "Every blocking NLM LOCK that pends will never be woken up: ProcessGrantedCallback dials port 12049 on the client host, which will either be refused or hit a different service. The lock is acquired server-side but the client never receives NLM_GRANTED, leaving the client blocked forever (or until timeout). This makes all blocking NLM locks effectively broken.", "fix": "The NLM callback port must be obtained by querying the client's portmapper: dial clientHost:111 (or client's portmap port), issue PMAP_GETPORT(prog=NLM, vers=4, prot=TCP), use the returned port. Store the resolved callback address in the Waiter at enqueue time. Alternatively, use the RPC source port if the NLM spec permits (it does not \u2014 the callback goes to the client's NLM listener, not the originating socket).", "dimension": "bugs", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed at lock.go:249-259. extractCallbackAddr() strips client port and hardcodes 12049. Used at line 170 to set CallbackAddr on the Waiter, then passed to ProcessGrantedCallback\u2192SendGrantedCallback. NLM clients register an ephemeral port; none listen on 12049. All blocking NLM locks permanently stall."}, {"title": "lz4EncoderHandle.Close() pools writer unconditionally on error \u2014 poisoned encoder recycled", "file": "/tmp/dittofs-audit/pkg/block/compression/codec.go", "line": 172, "severity": "HIGH", "what": "lz4WriterPool.Put(h.w) executes on line 172 regardless of whether h.w.Close() returned an error on line 171. Same pattern in zstdEncoderHandle.Close() at line 94. A failed Close() means the lz4/zstd encoder has not flushed its internal frame (lz4 end-mark not written, zstd stream not finalized); its internal state is indeterminate. The next caller pops this writer from the pool, calls Reset(newWriter), and compresses into it \u2014 producing a silently truncated or corrupt compressed frame.", "why": "lz4.Writer.Close() can fail if the underlying bytes.Buffer write errors. When decorator.Put() also calls enc.Close() from the error path (decorator.go:62, error discarded with '_'), a write-error encoder is guaranteed to be pooled. On the next Put the pool returns this dirty writer; Reset() rebinds its output but does not reset internal LZ4 block/frame state. The corrupted compressed body may be smaller than the original by coincidence, pass the size check at decorator.go:72, get framed, and go to S3 \u2014 later Get decodes garbage plaintext. If the compression decorator sits below the encryption decorator the garbage ciphertext still authenticates (AEAD is over the compressed body), so the corruption is undetected until the decompressed plaintext is consumed by the caller.", "fix": "Guard the Put with the error: `if err == nil { lz4WriterPool.Put(h.w) }` (and same for zstdEncoderPool.Put at codec.go:94). On error, let the writer be GC'd rather than recycled. The encoder-write error path in decorator.go:62 should propagate the Close error or at minimum not rely on the pool being clean: `if closeErr := enc.Close(); closeErr != nil { /* don't pool */ }`.", "dimension": "bugs", "area": "block-remote-s3-encryption-compression", "confirmed": true, "verdict": "Confirmed at codec.go:166-174. lz4EncoderHandle.Close(): err=h.w.Close() line 171, then lz4WriterPool.Put(h.w) line 172 executes unconditionally. Failed Close() leaves encoder with unflushed frame; pooled writer produces corrupt lz4 stream on next use. decorator.go line 62 also calls enc.Close() with discarded error on write-error path, guaranteeing a dirty lz4 encoder enters the pool."}, {"title": "zstdEncoderHandle.Close() pools encoder unconditionally on error \u2014 same pool-poisoning as lz4", "file": "/tmp/dittofs-audit/pkg/block/compression/codec.go", "line": 94, "severity": "HIGH", "what": "zstdEncoderPool.Put(h.enc) at line 94 executes whether or not h.enc.Close() (line 93) returned an error. A zstd encoder whose Close() failed has not written its final frame bytes; its next Reset()+use produces a stream that begins mid-frame from the perspective of a zstd decoder, causing ErrCompressedFrameCorrupt or silent wrong output on Get.", "why": "zstd.Encoder.Close() flushes and writes the end-of-stream marker. If the underlying bytes.Buffer write fails (e.g., OOM), Close returns an error and the encoder's internal flush queue is non-empty. Put recycles this encoder; Reset() changes the output writer but zstd's internal state (unflushed blocks, checksums) carries over. Subsequent compression produces an undecodable frame. The framed body passes the compression/size check at decorator.go:72 and is written to S3 verbatim. All subsequent Get calls for that block fail at decompression.", "fix": "Same as lz4: `if err == nil { zstdEncoderPool.Put(h.enc) }`. On error discard; the pool's New() will create a fresh encoder on the next Get().", "dimension": "bugs", "area": "block-remote-s3-encryption-compression", "confirmed": true, "verdict": "Confirmed at codec.go:88-97. zstdEncoderHandle.Close(): err=h.enc.Close() line 93, then zstdEncoderPool.Put(h.enc) line 94 executes unconditionally. Same pool-poisoning as lz4: failed Close leaves zstd stream without end-of-frame marker; next caller gets broken encoder after Reset()."}, {"title": "testLock_CleanShutdownMarker: toggle-to-false never read back \u2014 backend silent-ignore bug passes", "file": "/tmp/dittofs-audit/pkg/metadata/storetest/lock_persistence.go", "line": 127, "severity": "HIGH", "what": "SetCleanShutdown(false) called at line 127 but no GetCleanShutdown follows before immediately overwriting with SetCleanShutdown(true) at line 133", "why": "The function comment and the named contract both declare 'Toggling back to false reads false again' as an asserted property. Without a read-back, a backend that ignores SetCleanShutdown(false) when the stored value is true passes this test. Such a backend would always enter grace-period on restart (90-second wedge) since the crash marker is never correctly cleared.", "fix": "After line 128, add: `got, err = store.GetCleanShutdown(ctx); if err != nil { t.Fatalf(\"GetCleanShutdown (after toggle false): %v\", err) }; if got { t.Fatalf(\"after toggle SetCleanShutdown(false), GetCleanShutdown = true; unclean marker not stored\") }`", "dimension": "bugs", "area": "metadata-storetest-conformance", "confirmed": true, "verdict": "Confirmed at lock_persistence.go:127-133. SetCleanShutdown(false) at line 127 is called but there is no GetCleanShutdown read-back before SetCleanShutdown(true) at line 133 overwrites it. A backend that ignores Set(false) when value is true silently passes this test, allowing grace-period wedge on restart."}, {"title": "reconcileINV02: per-hash dedup skips RefCount of same-hash duplicate FileBlock rows \u2014 INV02 checker under-counts", "file": "/tmp/dittofs-audit/pkg/metadata/storetest/inv02_fuzz.go", "line": 513, "severity": "HIGH", "what": "seen map deduplicates enumerated hashes so GetByHash is called at most once per ContentHash; only that one row's RefCount contributes to totalRefCount", "why": "The contract explicitly allows multiple FileBlock rows with the same ContentHash (testPut_TwoIDsSameHash pins this). Each such row has an independent RefCount. If two rows share a hash with RefCount=1 each, reconcileINV02 counts only RefCount=1 (one row) while totalRefs counts both file references. The INV02 invariant check therefore produces false violations and\u2014critically\u2014misses real leaks on same-hash duplicate rows, making the PropertyFuzz blind to a whole class of refcount bugs.", "fix": "Remove the seen-dedup loop. Instead enumerate all FileBlock IDs (via ListFileBlocks across all payloads or the legacy interface) and sum their RefCounts directly. Alternatively, after EnumerateFileBlocks, iterate all blocks via ListFileBlocks to sum per-ID RefCounts rather than per-hash.", "dimension": "bugs", "area": "metadata-storetest-conformance", "confirmed": true, "verdict": "Confirmed at inv02_fuzz.go:511-525. reconcileINV02 uses a `seen` map keyed by ContentHash and only counts RefCount of one row per hash via GetByHash. testPut_TwoIDsSameHash (file_block_ops.go:680) pins that two rows with same hash and independent RefCount=1 each are valid. The INV02 checker counts 1 instead of 2, producing false violations and missing real refcount leaks on same-hash duplicate rows."}, {"title": "SetUserSharePermission / SetGroupSharePermission silently drops new permissions", "file": "/tmp/dittofs-audit/pkg/controlplane/store/permissions.go", "line": 37, "severity": "HIGH", "what": "Both SetUserSharePermission and SetGroupSharePermission call db.Save(perm) on structs with composite primary keys (UserID+ShareID and GroupID+ShareID). GORM Save decides INSERT vs UPDATE by checking whether the primary key is the zero value \u2014 but for a composite PK that is never zero, so Save always issues UPDATE. When the row does not yet exist, the UPDATE matches 0 rows; GORM returns nil error and the permission is silently discarded.", "why": "Interface contract says 'creates or updates'. Any caller that relies on Save to insert a new permission for the first time (which is the normal first-call path) silently gets no row in the DB. Share access is then evaluated against the share default, not the explicit permission that was supposedly set.", "fix": "Replace Save with an explicit upsert. For PostgreSQL use clause.OnConflict; for SQLite do a manual select-then-insert-or-update inside a transaction. Example: use db.Clauses(clause.OnConflict{Columns:[]clause.Column{{Name:\"user_id\"},{Name:\"share_id\"}}, DoUpdates: clause.AssignmentColumns([]string{\"permission\",\"share_name\"})}).Create(perm).", "dimension": "bugs", "area": "controlplane-store-models", "confirmed": true, "verdict": "Confirmed at permissions.go:37,102. Both SetUserSharePermission and SetGroupSharePermission use db.Save(perm). Models UserSharePermission (user.go:154-155) and GroupSharePermission (group.go:51-52) have composite primaryKey (UserID+ShareID and GroupID+ShareID) as non-zero strings. GORM v1.31.1 Save always issues UPDATE for non-zero PKs; first-time insert silently drops with nil error."}, {"title": "DSN() does not quote Postgres credentials \u2014 spaces in host/password/dbname corrupt the DSN", "file": "/tmp/dittofs-audit/pkg/controlplane/store/gorm.go", "line": 86, "severity": "HIGH", "what": "DSN() builds the libpq keyword=value string via fmt.Sprintf concatenation without quoting any field. A password, username, host, or dbname containing a space (e.g. password='foo bar') produces a malformed DSN: the parser sees 'bar' as an unknown keyword and returns a connection error. Special characters like single-quote or backslash in those fields also corrupt the string.", "why": "libpq keyword=value format requires values containing spaces or special chars to be enclosed in single quotes with internal single-quotes escaped as \\'. Raw Sprintf interpolation never does this. Users who configure a password with a space get an opaque 'invalid dsn' or 'unknown option' error with no indication of the real cause.", "fix": "Either use pgx's pgconn.Config / url.Values URL form (postgres://user:pass@host/db?sslmode=...) where net/url handles escaping, or write a quoteLibpqValue helper that wraps the value in single quotes and escapes internal ' and \\.", "dimension": "bugs", "area": "controlplane-store-models", "confirmed": true, "verdict": "Confirmed at gorm.go:86-87. DSN() builds libpq keyword=value string via raw fmt.Sprintf with no quoting. Password/host/dbname/user containing spaces or single-quotes produce malformed DSN \u2014 libpq parser fails with opaque error. Fix: wrap each value in single quotes with internal single-quote escaping."}, {"title": "ChangeOwnPassword: partial-update leaves user locked out on UpdateUser failure", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/users.go", "line": 378, "severity": "HIGH", "what": "UpdatePassword (line 371) succeeds, then UpdateUser (line 378) is called to clear MustChangePassword. If UpdateUser fails, handler returns 500 and no new token is issued. Password is now changed but MustChangePassword flag remains true in DB. User's old token is stale; every subsequent protected request is blocked by RequirePasswordChange. No self-recovery path \u2014 admin must manually clear the flag.", "why": "Non-atomic two-step write with no rollback or compensation. UpdatePassword and UpdateUser are separate DB calls. A transient DB error between them permanently locks the user out of the system, including the /users/me/password endpoint itself (which re-reads MustChangePassword from DB).", "fix": "Combine the password hash update and MustChangePassword=false clear into a single atomic store operation (e.g. UpdatePasswordAndClearMustChange), or wrap both in a transaction. If the store interface cannot be changed, at minimum re-attempt UpdateUser with a retry loop and log a critical alert on persistent failure.", "dimension": "bugs", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed at users.go:371-379. UpdatePassword succeeds at line 371 (password changed in DB), then UpdateUser at line 378 clears MustChangePassword. If UpdateUser fails, handler returns 500 with no rollback. Password is changed but MustChangePassword=true remains; old token is stale; user cannot login or reach the password-change endpoint again. No recovery path short of admin DB fix."}, {"title": "DITTOFS_* env vars silently ignored when no config file exists", "file": "/tmp/dittofs-audit/pkg/config/config.go", "line": 413, "severity": "HIGH", "what": "Load() returns GetDefaultConfig() directly when no config file is found, bypassing Viper entirely. All DITTOFS_* env vars bound in setupViper (via AutomaticEnv + BindEnv loop) are never read.", "why": "Container deployments that rely purely on env vars (no config file at default path) get hard-coded defaults regardless of any DITTOFS_DATABASE_TYPE, DITTOFS_CONTROLPLANE_SECRET, DITTOFS_KERBEROS_* etc. settings. DITTOFS_DATABASE_TYPE=postgres silently boots SQLite; DITTOFS_CONTROLPLANE_SECRET is ignored meaning JWT signing uses the default/empty secret.", "fix": "Remove the early-return shortcut. When no config file is found, still call v.Unmarshal into a Config (Viper will supply zeros for all missing keys), then ApplyDefaults + Validate, so env vars take effect. Or: unmarshal before returning defaults, and only fall back to GetDefaultConfig() if Unmarshal also fails.", "dimension": "bugs", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed. pkg/config/config.go:413 returns GetDefaultConfig() directly when configFileFound=false, skipping the v.Unmarshal call entirely. setupViper at line 404 bound all DITTOFS_* env vars via AutomaticEnv+BindEnv loop, but that Viper instance is never queried. All env-var overrides silently ignored for container-only deployments."}, {"title": "provisionOperatorAccount: login with freshly-generated password when user already exists (409)", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/auth_reconciler.go", "line": 250, "severity": "HIGH", "what": "When `CreateUser` returns HTTP 409 (user already exists), the code falls through to `apiClient.Login` at line 250 using `operatorPassword` \u2014 the freshly-generated random password for this reconcile. But the existing user was created with a different password stored in a previously-deleted credentials secret. The login will fail with 401, causing permanent auth failure requiring manual intervention.", "why": "The credentials secret is absent (caller checked `IsNotFound` before entering `provisionOperatorAccount`), so the old password is irrecoverable. The new random password won't match the existing user's stored password, so `Login` returns 401. `isTransientError` returns false for 401, so the error propagates as a permanent failure with no requeue.", "fix": "On 409: either (a) call admin API to reset the existing operator user's password to `operatorPassword` before logging in, or (b) delete the existing user and recreate it, or (c) treat 409 as an unrecoverable signal and emit a warning event asking the operator to delete the user manually.", "dimension": "bugs", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. auth_reconciler.go:242-246: 409 path logs and falls through. Line 250 calls apiClient.Login with freshly-generated operatorPassword. If the user was previously created (with a different password) and the credentials secret was deleted, Login returns 401. isTransientError returns false for 401 so error propagates as permanent failure with no requeue \u2014 operator stuck."}, {"title": "MFsymlink auto-conversion: client-controlled symlink target injected without path validation", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/close.go", "line": 912, "severity": "HIGH", "what": "On CLOSE, checkAndConvertMFsymlink reads a 1067-byte file written by the SMB client, decodes the symlink target via mfsymlink.Decode, and calls metaSvc.CreateSymlink with that target verbatim. ValidateSymlinkTarget (pkg/metadata/validation.go:201) only rejects empty string. No restriction on target content \u2014 absolute paths, `../../..` escapes, or `/etc/shadow` are all accepted.", "why": "An authenticated SMB client can write a specially crafted 1067-byte file (XSym\\n magic + MD5 + any target string up to 1023 bytes), then CLOSE it. The server creates a real symlink with the attacker-controlled target in the virtual filesystem. NFS clients (or future SMB follow-symlink paths) can then dereference that symlink to reach paths outside the share root \u2014 classic path traversal via symlink. This bypasses the share-boundary protection that file handle opaqueness and path normalization provide for direct CREATEs.", "fix": "In convertToRealSymlink (close.go:873) or in ValidateSymlinkTarget (validation.go:201), reject or sanitize targets that are absolute paths pointing outside the share (e.g., starting with / and not under the share root) or that contain `../` sequences escaping the share tree. Alternatively, require the SMB session to hold FILE_WRITE_ATTRIBUTES + a per-share 'allow-symlink-creation' capability before auto-converting MFsymlinks. At minimum, add a configurable toggle (default: disabled) for MFsymlink auto-conversion so operators control the attack surface.", "dimension": "security", "area": "smb-create-open", "confirmed": true, "verdict": "Confirmed. close.go:912 calls metaSvc.CreateSymlink with mfsymlink.Decode output as target verbatim. CreateSymlink (file_create.go:26) calls ValidateSymlinkTarget which only rejects empty string (validation.go:201-208). No path traversal check. Authenticated SMB client can write crafted 1067-byte XSym file and trigger symlink creation to arbitrary target including /etc/shadow or ../../ escapes."}, {"title": "NTLM auth bypass: user with no NT hash gets full authenticated session", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 1218, "severity": "HIGH", "what": "When `hasNTHash=false` (user exists + enabled, but `GetNTHash()` returns no hash) the code falls through to line 1241: `CreateSessionWithUser` is called with `IsGuest=false`, mapping the client-supplied username to a real authenticated identity. No credential is verified. Any client that knows a valid username can authenticate as that user by sending an NTLM AUTHENTICATE with any (or no) NtChallengeResponse.", "why": "Missing auth \u2014 the credential-check block at line 1088 (`if hasNTHash && len(authMsg.NtChallengeResponse) > 0`) is guarded only when a hash exists. The fallthrough at line 1218 grants the full user identity with a comment explicitly calling this a security bypass. The resulting session has `IsGuest=false` and carries the user's UID/GID/SID for all subsequent ACL checks, bypassing all DACL enforcement.", "fix": "Return `StatusLogonFailure` when `!hasNTHash` (no hash configured = account not usable for NTLM). Remove the 'transitional' path entirely; the admin action `dittofs user passwd` should be required before any authentication is possible. At minimum: treat no-NT-hash identically to wrong-password and call `destroySessionOnReauthFailure` then return `StatusLogonFailure`.", "dimension": "security", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Confirmed. session_setup.go:1218-1241: when hasNTHash=false, code skips credential validation and calls CreateSessionWithUser with IsGuest=false. Code comment at 1218 explicitly says 'SECURITY: User exists but no valid NT hash configured... Any client knowing the username can authenticate - this bypasses credential checks entirely.' Real auth bypass, not a configuration edge-case."}, {"title": "Anonymous/null SMB session mapped to UID 0 (root) in AuthContext", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/auth_helper.go", "line": 69, "severity": "HIGH", "what": "`BuildAuthContext` for `ctx.User==nil && !ctx.IsGuest` (anonymous/null session) sets UID=0, GID=0 on the `metadata.AuthContext`. Any metadata store implementation that grants elevated access to UID 0 (root-bypass in POSIX permission checks) will grant that access to an unauthenticated anonymous SMB connection.", "why": "An anonymous NTLM logon (empty `Username` or `FlagAnonymous` set) creates a null session (`IsNull=true`, `IsGuest=false`, `User=nil`). `createAnonymousSession` does not set `ctx.IsGuest=true`, so `BuildAuthContext` takes the `else` branch at line 68 and synthesises UID=0. Downstream metadata stores performing a UID-0 root-bypass (documented in `auth_priming_test.go:7`) will treat this anonymous connection as root.", "fix": "Map null/anonymous sessions to a safe unprivileged UID (e.g. 65534 `nobody`) the same as guest, or reject anonymous access at `checkGuestPolicy` when the operator has not explicitly enabled it. At minimum change the `else` branch in `BuildAuthContext` to use UID=65534/GID=65534 instead of 0/0, removing the comment 'use root (for now)'.", "dimension": "security", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Confirmed. auth_helper.go:62-74: when ctx.User==nil && !ctx.IsGuest (anonymous/null session), UID=0, GID=0 assigned. Comment says 'Anonymous/null session - use root (for now)'. Any anonymous NTLM logon gets UID 0 in AuthContext, bypassing POSIX permission checks in stores that special-case root."}, {"title": "LEASE_BREAK_ACK missing session ownership check \u2014 any session can ack another session's lease break", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/stub_handlers.go", "line": 808, "severity": "HIGH", "what": "handleLeaseBreakAck passes client-supplied LeaseKey directly to LeaseManager.AcknowledgeLeaseBreak with no check that ctx.SessionID matches the session recorded in LeaseManager.sessionMap for that key. Epoch is hardcoded 0, bypassing the epoch staleness guard in acknowledgeLeaseBreakImpl (leases.go:919: `if epoch != 0 && ...`).", "why": "Any authenticated session can send OPLOCK_BREAK with a forged/guessed 16-byte LeaseKey to forcibly complete a different session's in-progress lease break, downgrading that session's cached lease (Write\u2192None). Attacker on same server can suppress another client's write caching, causing data-integrity failures or DoS. The epoch=0 bypass also lets replayed stale acks pass unchecked.", "fix": "Before calling AcknowledgeLeaseBreak, verify ctx.SessionID matches the owner: call lm.GetSessionForLease(ack.LeaseKey) and compare to ctx.SessionID; return STATUS_ACCESS_DENIED on mismatch. Also pass the epoch from the wire message (wire format bytes 2-3 in the LeaseBreakAcknowledgment; currently DecodeLeaseBreakAcknowledgment skips them via r.Skip(6)) to AcknowledgeLeaseBreak instead of hardcoded 0, so stale acks are rejected.", "dimension": "security", "area": "smb-lease-oplock-notify", "confirmed": true, "verdict": "Confirmed. handleLeaseBreakAck at stub_handlers.go:808 calls h.LeaseManager.AcknowledgeLeaseBreak with hardcoded epoch=0 and no check that ctx.SessionID matches sessionMap[leaseKey]. LeaseManager.sessionMap records the owning sessionID per leaseKey (lease/manager.go:89,285) but AcknowledgeLeaseBreak (lease/manager.go:375) only resolves the shareName from leaseShare, never verifies session ownership. Epoch=0 bypasses the staleness guard at leases.go:919."}, {"title": "COPYCHUNK access gate checks DesiredAccess instead of GrantedAccess", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/ioctl_copychunk.go", "line": 250, "severity": "HIGH", "what": "validateCopyChunkAccess reads src.DesiredAccess and dst.DesiredAccess (lines 250-251) when checking FILE_READ_DATA/FILE_EXECUTE on source and FILE_WRITE_DATA/FILE_APPEND_DATA/FILE_READ_DATA on destination. DesiredAccess is the client-supplied mask before DACL evaluation \u2014 it can contain MAXIMUM_ALLOWED or GENERIC_ALL even when the DACL denies the specific bits. Every other access gate in the SMB handler layer (READ line 196, SET_SPARSE line 78, QUERY_INFO line 294, security.go) uses GrantedAccess (the post-DACL intersection stored at CREATE time).", "why": "A client can open a file with MAXIMUM_ALLOWED or GENERIC_ALL DesiredAccess but receive a restricted GrantedAccess from the DACL (e.g., read-only). validateCopyChunkAccess will still pass the write check on the destination because MAXIMUM_ALLOWED is non-zero and includes FILE_WRITE_DATA after the bitwise AND. This bypasses DACL-controlled write restrictions for server-side copy, allowing unauthorized data writes to the destination file. Same bug class explicitly called out for CHANGE_NOTIFY (#616) and WRITE (#603) in codebase comments.", "fix": "Replace src.DesiredAccess with src.GrantedAccess and dst.DesiredAccess with dst.GrantedAccess in validateCopyChunkAccess (lines 250-251 and log lines 256/263/270). The function already has access to both fields via OpenFile. Example: `srcAccess := types.AccessMask(src.GrantedAccess)` / `dstAccess := types.AccessMask(dst.GrantedAccess)`. Also expand GENERIC_* bits before checking if GrantedAccess may contain unresolved generic bits (use the existing hasAccessRight helper or resolveAccessFlags).", "dimension": "security", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "Confirmed. validateCopyChunkAccess at ioctl_copychunk.go:250-251 reads src.DesiredAccess and dst.DesiredAccess. All other access gates (read.go:196, security.go) use GrantedAccess (post-DACL). A MAXIMUM_ALLOWED open whose DACL grants only read-only access will still pass the write check on destination because DesiredAccess contains MAXIMUM_ALLOWED|FILE_WRITE_DATA bits pre-DACL."}, {"title": "Related compound subcommand signatures never verified \u2014 auth bypass", "file": "/tmp/dittofs-audit/internal/adapter/smb/compound.go", "line": 596, "severity": "HIGH", "what": "VerifyCompoundCommandSignature is called with the raw parsed hdr before SessionID inheritance. Related subcommands carry SessionID=0xFFFFFFFFFFFFFFFF (sentinel). GetSession(0xFFFFFFFFFFFFFFFF) returns ok=false at line 596-599, causing the function to return nil (skip). SessionID is only resolved to the real value at line 860-862, AFTER the signature check. Result: every related compound subcommand (WRITE, SET_INFO, READ, CLOSE, etc.) on a non-encrypted signing-required session has its signature silently accepted regardless of content.", "why": "MS-SMB2 \u00a73.3.5.2.7.2 requires each command in a compound to be individually signed and verified. The spec does not exempt related operations. An on-path attacker or a rogue SMB client on a non-encrypted, signing-required session can substitute arbitrary body bytes in any related compound subcommand and the server will execute the tampered request. Attacker controls: file contents (WRITE), attributes/ACLs (SET_INFO), rename targets (SET_INFO rename), delete-on-close flag \u2014 any operation reachable via a related compound. The NeedsSession gate in prepareDispatch ensures the session must exist, but does not re-verify the signature on the modified body.", "fix": "Resolve the session before calling VerifyCompoundCommandSignature, or pass the inherited session ID explicitly. In processRemaining, perform the SessionID inheritance block (lines 859-866) BEFORE the signature verification block (lines 803-810). Alternatively, in VerifyCompoundCommandSignature accept an optional override sessionID and use it when hdr.SessionID is the sentinel 0xFFFFFFFFFFFFFFFF, falling back to s.lastSessionID passed by the caller.", "dimension": "security", "area": "smb-framing-dispatch-compound", "confirmed": true, "verdict": "Confirmed. VerifyCompoundCommandSignature is called at compound.go:804 with pre-inheritance hdr. Related commands carry hdr.SessionID=0xFFFFFFFFFFFFFFFF; GetSession(0xFFFF...) returns ok=false at line 596-598; function returns nil (skip). SessionID is only patched to the real value at lines 860-862, AFTER signature verification. Every related compound subcommand on a signing-required non-encrypted session has its signature silently accepted."}, {"title": "Missing NFS4ERR_CLID_INUSE check \u2014 principal mismatch allows session/lease hijack", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 319, "severity": "HIGH", "what": "SetClientID and all five case-handlers (createNewClient, reuseConfirmedClient, handleClientReboot, replaceUnconfirmed) store the principal but never compare it against the existing confirmed record's principal. RFC 7530 \u00a79.1.1 Case 5: when a confirmed record exists with the same clientIDStr+verifier but a DIFFERENT RPCSEC_GSS / AUTH_SYS principal, the server MUST return NFS4ERR_CLID_INUSE. ErrClientIDInUse is defined in client.go:21 but is never returned anywhere.", "why": "Any client that knows a victim's nfs_client_id4 string and verifier (both visible on the wire) can call SETCLIENTID from a different UID/principal and immediately obtain the same clientID+confirmVerifier. After SETCLIENTID_CONFIRM it replaces the victim's confirmed record, inheriting all open state, locks and delegations \u2014 a complete session takeover without authentication.", "fix": "In reuseConfirmedClient (Case 5) and reuseConfirmedClient (Case 2 fallback), after verifier match, compare principal against confirmed.Principal. When both are non-empty and differ, return ErrClientIDInUse. Example guard in reuseConfirmedClient before line ~419:\n\nif princ != \"\" && confirmed.Principal != \"\" && princ != confirmed.Principal {\n    return nil, ErrClientIDInUse\n}\n\nSame check needed in handleClientReboot (Case 3) to reject a different-principal client from stealing the reboot slot.", "dimension": "security", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "Confirmed. SetClientID at manager.go:319-354 implements five cases. Case 5 (confirmed!=nil, verifier matches) goes to reuseConfirmedClient with no principal comparison against confirmed.Principal. ErrClientIDInUse defined in client.go:21 but never returned anywhere in the codebase. RFC 7530 \u00a79.1.1 mandates NFS4ERR_CLID_INUSE when principal differs on same clientIDStr+verifier."}, {"title": "SSRF via client-controlled callback address in SETCLIENTID / CB_NULL and CB_RECALL", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/callback.go", "line": 124, "severity": "HIGH", "what": "dialCallback dials an arbitrary host:port derived from the client-supplied cbNetID+cbAddr fields of SETCLIENTID (setclientid.go:50-65, manager.go:612-631). No validation is performed on the resolved host \u2014 loopback (127.x), link-local (169.254.x), RFC-1918, or cloud-metadata addresses are all accepted. The CB_NULL probe fires asynchronously in a goroutine from ConfirmClientID at manager.go:614, and CB_RECALL fires on every delegation recall.", "why": "Any authenticated (or unauthenticated, if AUTH_NONE is in use) NFS client can force the server to make outbound TCP connections to arbitrary addresses. In cloud environments this trivially reaches the instance metadata service (169.254.169.254) or internal services. Because the connection sends a valid RPC CALL frame and reads back a response, it can be used for port-scanning internal networks or exploiting metadata-credential endpoints.", "fix": "In dialCallback (or ParseUniversalAddr caller), validate the resolved host before dialing:\n1. Resolve the host to an IP.\n2. Reject loopback (127.0.0.0/8, ::1), link-local (169.254.0.0/16, fe80::/10), cloud-metadata (169.254.169.254), and RFC-1918 ranges unless the server is explicitly configured to allow them.\nAlternatively, restrict acceptable netids to 'tcp'/'tcp6' and validate the uaddr octets are in the client-network range.", "dimension": "security", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "Confirmed. dialCallback at callback.go:124 calls ParseUniversalAddr then net.Dialer.DialContext with no host validation. CB_NULL fires in goroutine from ConfirmClientID at manager.go:614. No check for loopback, RFC-1918, or cloud metadata addresses (169.254.169.254). Any authenticated NFS client controls the callback host:port."}, {"title": "RPCSEC_GSS auth claimed without verified identity \u2014 COMPOUND proceeds as anonymous", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/context.go", "line": 50, "severity": "HIGH", "what": "ExtractV4HandlerContext: when AuthFlavor==AuthRPCSECGSS but gss.IdentityFromContext returns nil (no authenticated Kerberos principal in context), logs a warning and returns a CompoundContext with nil UID/GID. The COMPOUND is not rejected.", "why": "buildV4AuthContext treats nil UID/GID as anonymous (authMethod=\"anonymous\") and passes it to ResolveSharePermission. A client can claim RPCSEC_GSS flavor without completing the GSS handshake \u2014 bypassing RequireKerberos enforcement that only checks flavor at mount time, not per-COMPOUND. Operations execute under anonymous/squash identity instead of being refused with NFS4ERR_WRONGSEC.", "fix": "Return NFS4ERR_WRONGSEC (or refuse to build the CompoundContext) when AuthFlavor==AuthRPCSECGSS but no GSS identity is present in the context. Add: if compCtx.AuthFlavor == rpc.AuthRPCSECGSS && gss.IdentityFromContext(ctx) == nil { return nil, NFS4ERR_WRONGSEC } before the COMPOUND dispatch, or reject in ProcessCompound before dispatching ops.", "dimension": "security", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "Confirmed. ExtractV4HandlerContext at context.go:36-53: when AuthFlavor==AuthRPCSECGSS but gss.IdentityFromContext returns nil, logs warning and returns CompoundContext with nil UID/GID (lines 51-53). buildV4AuthContext at helpers.go:33-36 sets authMethod='anonymous' (AuthRPCSECGSS != AuthUnix). ResolveSharePermission at share_permission.go:55 short-circuits allow-with-defaults on nil uid. COMPOUND executes as anonymous without rejection."}, {"title": "SSRF: client-controlled callback address dialed without host validation", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/callback.go", "line": 133, "severity": "HIGH", "what": "dialCallback resolves callback.Addr via ParseUniversalAddr then passes the result directly to net.Dialer.DialContext with no check on the resolved host. callback.Addr comes from the SETCLIENTID (v4.0) or EXCHANGE_ID (v4.1) request body, fully controlled by the NFS client.", "why": "Any authenticated (or unauthenticated, if AUTH_NULL is accepted) NFS client can set its callback address to 169.254.169.254 (cloud IMDS), ::1 (loopback), or any RFC-1918/internal address. The server will TCP-connect to that host and write the CB_NULL probe and later CB_RECALL payloads, enabling port scanning and SSRF against internal services from the server's network identity.", "fix": "After ParseUniversalAddr succeeds, resolve the host to net.IP and reject loopback (ip.IsLoopback()), link-local (169.254.0.0/16 and fe80::/10), and optionally RFC-1918 ranges unless an allow-list is configured. Example guard in dialCallback before net.JoinHostPort: parse IP, call ip.IsLoopback() || isLinkLocal(ip) and return error if true.", "dimension": "security", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "Confirmed (same root as idx 4). dialCallback at callback.go:133 passes client-supplied address directly to net.Dialer.DialContext with no host restriction. Both CB_NULL (SendCBNull) and CB_RECALL (SendCBRecall) use this path. Confirmed no validation in ParseUniversalAddr \u2014 it only parses format, not address ranges."}, {"title": "RPCSEC_GSS DESTROY unauthenticated \u2014 any observer can evict any context", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/framework.go", "line": 851, "severity": "HIGH", "what": "handleDestroy() deletes the named GSS context with zero authentication. It looks up the handle and calls p.contexts.Delete(cred.Handle) without verifying the call-header MIC or any other credential. The Process() dispatcher (line 413) routes gss_proc=DESTROY directly to handleDestroy, explicitly skipping MIC verification that is mandatory for DATA requests.", "why": "An attacker who observes a 16-byte context handle in cleartext TCP traffic (or brute-forces the 2^128 space with repeated attempts on a long-lived server) can forge a DESTROY packet and evict any authenticated session, forcing re-authentication or causing a DoS. RFC 2203 Section 5.3.3.2 requires the verifier MIC to be checked on all non-INIT control messages. Linux sunrpc (svcauth_gss.c) verifies the MIC on DESTROY. Not verifying it breaks the security contract of RPCSEC_GSS.", "fix": "Before calling p.contexts.Delete, look up the context and call verifyHeaderMIC(gssCtx.SessionKey, headerPreimage, verifBody). If verification fails, return RPCSEC_GSS_CREDPROBLEM without deleting the context. Pass headerPreimage and verifBody into handleDestroy the same way handleData receives them, and route them through Process().", "dimension": "security", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed. handleDestroy at framework.go:851 calls p.contexts.Delete(cred.Handle) with zero authentication. Process() at line 412-413 routes RPCGSSDestroy directly to handleDestroy, bypassing MIC verification performed for DATA requests. RFC 2203 \u00a75.3.3.2 requires verifier MIC on all non-INIT messages. Any observer of a 16-byte handle can evict any GSS context."}, {"title": "AUTH_NULL / failed AUTH_UNIX parse grants full write access on any non-read-only share", "file": "/tmp/dittofs-audit/internal/adapter/nfs/auth/share_permission.go", "line": 55, "severity": "HIGH", "what": "ResolveSharePermission returns allow-with-defaults (ReadOnly=false) when uid==nil. A client that sends AUTH_NULL, or whose AUTH_UNIX credential fails to parse (e.g. malformed body), produces handlerCtx.UID=nil in ExtractHandlerContext. That nil UID reaches BuildAuthContextWithMapping \u2192 ResolveSharePermission, which short-circuits at line 55 and grants unrestricted read-write access on any share that is not explicitly marked ReadOnly. The NeedsAuth flag in the dispatch table (dispatch_nfs.go lines 29\u2013124) is set on WRITE, SETATTR, CREATE, MKDIR, SYMLINK, REMOVE, RMDIR, RENAME, LINK, COMMIT but is never read in dispatchNFSv3Procedure (dispatch.go line 248\u2013262) or dispatchMount \u2014 it is dead metadata. No AUTH_NULL rejection gate exists anywhere between the TCP connection and the individual handler call.", "why": "Any unauthenticated NFS client on the network can issue WRITE/CREATE/REMOVE/RENAME RPCs against any writable share. AUTH_UNIX credentials are client-controlled cleartext (no cryptographic binding), so an attacker can also deliberately send AUTH_NULL to avoid the UID lookup path and land on the nil-UID fast path that bypasses all per-user deny rules. Shares whose DefaultPermission is 'none' also rely on the uid!=nil path reaching line 82; with uid==nil the 'none' default is never evaluated.", "fix": "In ResolveSharePermission, when uid==nil and identityStore!=nil and share!=nil, treat the caller as a guest and apply the share's DefaultPermission (the same logic at lines 82-90) rather than blindly allowing. Separately, add an auth-flavor gate in dispatchNFSv3Procedure: if procedure.NeedsAuth && handlerCtx.AuthFlavor == rpc.AuthNull { return NFS3ErrAccess reply }. The NeedsAuth field should then be actively enforced or removed.", "dimension": "security", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "Confirmed. ResolveSharePermission at share_permission.go:55: 'if identityStore == nil || share == nil || uid == nil { return result, nil }' returns allow-with-defaults when uid==nil. AUTH_NULL or malformed AUTH_UNIX produces nil UID in ExtractHandlerContext (dispatch_test.go:96 confirms). NeedsAuth flag is defined in dispatch table (dispatch.go:402) but never read or enforced in dispatchNFSv3Procedure (lines 248-262) \u2014 it's dead metadata. DefaultPermission='none' shares are also bypassed."}, {"title": "reclaimLeaseImpl: missing caller-identity check \u2014 any client can steal another client's lease during grace", "file": "/tmp/dittofs-audit/pkg/metadata/lock/reclaim.go", "line": 40, "severity": "HIGH", "what": "reclaimLeaseImpl has no clientID parameter and issues ListLocks with no ClientID filter (line 40). Match is purely on the 16-byte leaseKey (lines 48-56). Any authenticated client who can observe or guess a leaseKey can reclaim another client's lease during the grace window, acquiring that client's Read/Write/Handle caching rights. V4ClientRecoveryRecord.Principal (client_recovery_store.go:39) is documented as a 'lease-stealing guard at reclaim time' but is never consulted here.", "why": "Missing authz: RFC 7530 \u00a79.1.4 / RFC 8881 \u00a78.4.2 require that reclaim be restricted to the original client identity (verified via boot verifier + principal). Without the ClientID gate, a malicious client that knows victim's leaseKey (observable on the wire or via GETATTR) reclaims the lease, gains Write caching rights, and then writes corrupt data under the original holder who no longer receives a break notification.", "fix": "Add clientID string param to reclaimLeaseImpl and pass it from every call site. Filter the ListLocks query: LockQuery{IsLease: &isLease, ClientID: clientID}. After matching leaseKey, assert pl.ClientID == clientID and return ErrLockNotFound on mismatch. For NFSv4.1 reclaim also retrieve the matching V4ClientRecoveryRecord and verify the incoming principal matches V4ClientRecoveryRecord.Principal before granting reclaim.", "dimension": "security", "area": "metadata-lock-manager", "confirmed": true, "verdict": "Confirmed: reclaim.go:40 ListLocks uses LockQuery{IsLease:&isLease} with no ClientID filter. Lines 48-56 match purely on leaseKey bytes. No Principal/ClientID gate anywhere in reclaimLeaseImpl. Any client knowing leaseKey reclaims another client's lease."}, {"title": "NSM MonName KV key injection: attacker-controlled MonName pollutes Badger prefix scan \u2192 unintended lock/registration deletion", "file": "/tmp/dittofs-audit/pkg/metadata/store/badger/clients.go", "line": 76, "severity": "HIGH", "what": "The NSM SM_MON handler (internal/adapter/nfs/nsm/handlers/mon.go:70) accepts `mon.MonID.MonName` directly from the network (unauthenticated XDR field) and stores it unescaped into the Badger secondary-index key at line 76: `nsm:monname:<MonName>:<clientID>`. `DeleteClientRegistrationsByMonName` (line 251) builds the prefix as `nsm:monname:<monName>:` and scans it; no length/character validation is applied to monName before either operation.", "why": "A network NFS client can send SM_MON with a crafted `mon_name` containing `:` (e.g. `legithost:victimClientID`). The stored key becomes `nsm:monname:legithost:victimClientID:<attackerClientID>`. Later, `DeleteClientRegistrationsByMonName(ctx, \"legithost\")` scans prefix `nsm:monname:legithost:`, which matches both real `legithost` entries AND the attacker-planted entry, causing unintended deletion of `victimClientID`'s registration. This breaks NFS lock-recovery for that client (their SM_NOTIFY callbacks and file-lock state are silently wiped). The scan also incorrectly extracts `clientID` via `strings.TrimPrefix` leaving the attacker-injected suffix as the extracted ID (line 262), causing a subsequent `deleteClientRegistrationTx` on a non-existent or wrong client. NSM has no authentication \u2014 any NFS-reachable host can trigger this.", "fix": "Sanitize or encode `MonName` before embedding in Badger keys. Options: (a) hex-encode or URL-encode MonName in the key (`hex.EncodeToString([]byte(reg.MonName))`), keeping the decode symmetric in the extractor; (b) use a length-prefixed key format so the separator is not ambiguous; (c) reject MonName values containing `:` in the XDR decoder (RFC 1094 \u00a7A.2 restricts mon_name to hostname chars \u2014 no colon). The same discipline should be applied to `clientID` in the `nsm:client:` primary key and to `FileID`/`OwnerID`/`ClientID` in the lock secondary index keys (locks.go:84,90,96).", "dimension": "security", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: badger/clients.go:76 key is prefixNSMByMonName+reg.MonName+\":\"+reg.ClientID. DeleteClientRegistrationsByMonName line 251 scans prefix prefixNSMByMonName+monName+\":\". MonName containing \":victimID\" (e.g. \"legithost:victimClientID\") plants a key matched by the victim's prefix scan, causing unintended deletion of victimClientID's registration. No character validation on MonName before storage."}, {"title": "DSN parameter injection via unquoted ConnectionString()", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/config.go", "line": 122, "severity": "HIGH", "what": "ConnectionString() builds libpq key=value DSN via fmt.Sprintf with no quoting of Host, Port, Database, User, Password, or SSLMode values. pgx's libpq parser splits on whitespace and equals signs. A Password (or any other field) containing a space followed by a keyword (e.g. 'mypass sslmode=disable') injects a new DSN parameter that overrides the one already present in the string.", "why": "Config values come from operator YAML or env vars (DITTOFS_* prefix via viper). A compromised config file, Kubernetes Secret, or env injection allows bypassing the intended sslmode (e.g. forcing sslmode=disable even when the operator wrote sslmode=verify-full), disabling TLS for the metadata store connection. This is a classic libpq DSN injection pattern; pgx's parseKeywordValueSettings confirms it splits on unquoted whitespace.", "fix": "Use URL-format DSN (postgres://user:pass@host:port/db?sslmode=...) which naturally percent-encodes special chars, or quote each value with single-quotes and escape internal single-quotes/backslashes per libpq rules. Simplest fix: replace fmt.Sprintf with url.URL construction: u := &url.URL{Scheme: \"postgres\", User: url.UserPassword(c.User, c.Password), Host: fmt.Sprintf(\"%s:%d\", c.Host, c.Port), Path: c.Database}; q := u.Query(); q.Set(\"sslmode\", c.SSLMode); ... u.RawQuery = q.Encode(); return u.String(). pgxpool.ParseConfig accepts URL format.", "dimension": "security", "area": "metadata-store-postgres", "confirmed": true, "verdict": "Confirmed: ConnectionString() at config.go:121-132 is plain fmt.Sprintf with no quoting of any field. Password containing space+keyword (e.g. \"pass sslmode=disable\") injects extra DSN params parsed by libpq/pgx. Can override sslmode or other security-relevant params from operator config/env."}, {"title": "Postgres metadata store TLS disabled by factory default", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/init.go", "line": 113, "severity": "HIGH", "what": "`CreateMetadataStoreFromConfig` sets `pgCfg.SSLMode = \"disable\"` when the operator config blob omits a `sslmode` key. `NewPostgresMetadataStore` calls `ApplyDefaults()` which only writes `\"prefer\"` when the field is empty \u2014 but `\"disable\"` is not empty, so the safe default never fires. All metadata traffic (auth credentials, file paths, ACLs, lock state) traverses the network in plaintext.", "why": "Hardcoded insecure default overrides the struct's own safe default (`prefer`). An attacker on the same network segment can passively read or actively MITM all metadata store traffic, stealing credentials and ACL data or injecting rogue metadata.", "fix": "Remove the hardcoded `\"disable\"` fallback. Let the field stay empty so `ApplyDefaults()` sets `\"prefer\"`, or explicitly set `pgCfg.SSLMode = \"prefer\"` to match the struct contract. Document that operators who need no-TLS must explicitly set `sslmode: disable` in their config.", "dimension": "security", "area": "controlplane-runtime-shares", "confirmed": true, "verdict": "Confirmed: init.go:113 hardcodes pgCfg.SSLMode=\"disable\" when sslmode key absent. ApplyDefaults only sets \"prefer\" when SSLMode==\"\" (empty). \"disable\" is non-empty so safe default never fires. All metadata traffic goes plaintext by default."}, {"title": "Netgroup hostname access control bypassed via attacker-controlled PTR records", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/netgroups.go", "line": 205, "severity": "HIGH", "what": "`matchHostname` authorizes NFS mount access by performing a reverse DNS lookup (`net.LookupAddr`) on the connecting client IP and comparing the returned PTR hostname against the netgroup pattern. PTR records are controlled by whoever manages the reverse-DNS zone for that IP block \u2014 in cloud or co-lo environments this is the tenant, not the operator.", "why": "An attacker who controls their own PTR record (e.g. on any cloud provider where reverse DNS is self-service) can set it to `host.allowed-domain.example.com` and gain access to any share whose netgroup uses a `hostname`-type member or `*.example.com` wildcard. This is a classic reverse-DNS spoofing bypass for network access control.", "fix": "Either (a) remove hostname-type netgroup members entirely and document that only IP/CIDR entries are safe, or (b) after resolving the PTR hostname, perform a forward DNS lookup of that hostname and verify it resolves back to the connecting IP (forward-confirmed reverse DNS, FCrDNS) before accepting the match. Document that PTR-only matching is insecure.", "dimension": "security", "area": "controlplane-runtime-shares", "confirmed": true, "verdict": "Confirmed: netgroups.go:204 matchHostname calls pkgDNSCache.lookupAddr (reverse PTR only), then string-matches against the pattern. No forward-confirmed reverse DNS. Cloud tenant controlling PTR can set it to an allowed-domain hostname and gain mount access."}, {"title": "Unauthenticated IPC$ access via SessionID=0 bypass", "file": "/tmp/dittofs-audit/internal/adapter/smb/response.go", "line": 344, "severity": "HIGH", "what": "prepareDispatch skips ALL session validation when reqHeader.SessionID==0 (condition: cmd.NeedsSession && reqHeader.SessionID != 0). handleIPCShare then calls GetSession(0) which always returns the pre-allocated anonymous session (ID 0, created unconditionally in NewManager). The !found guard never fires, so any client that sends TREE_CONNECT with SessionID=0 \u2014 before completing SESSION_SETUP \u2014 is granted an IPC$ tree connection and can subsequently open and use lsarpc (SID-to-name resolution) and srvsvc (full share enumeration) pipes without authenticating.", "why": "The anonymous session at ID 0 is pre-created in session/manager.go:47 and is never deleted. It satisfies the GetSession lookup in handleIPCShare. The intent of the SessionID!=0 guard is to handle pre-auth credit tracking, but it has the side effect of making every NeedsSession=true command (TREE_CONNECT, CREATE, READ, WRITE, \u2026) reachable without a valid authenticated session when the client sets SessionID=0. An attacker can leak share names and resolve SIDs for any UID/GID without credentials.", "fix": "In handleIPCShare (tree_connect.go ~line 276), after GetSession succeeds, add: if sess.IsNull || sess.IsGuest { return NewErrorResult(types.StatusAccessDenied), nil } to reject unauthenticated/null sessions from IPC$. Alternatively, add a dedicated SessionID==0 early-reject in prepareDispatch for all commands except NEGOTIATE, SESSION_SETUP, and ECHO which legitimately arrive with no session.", "dimension": "security", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: response.go:344 gate is \"if cmd.NeedsSession && reqHeader.SessionID != 0\" \u2014 skips auth check entirely when SessionID=0. handleIPCShare at tree_connect.go:276 calls GetSession(ctx.SessionID) where ctx.SessionID=0. manager.go:47-48 unconditionally creates and stores anonymous session at ID 0 in NewManagerWithStrategy. GetSession(0) returns found=true, non-nil session. IPC$ tree connect granted. Client can then open lsarpc/srvsvc pipes without authenticating."}, {"title": "DESTROY_SESSION missing caller-ownership authz \u2014 any client can destroy any session", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/v41/handlers/destroy_session.go", "line": 28, "severity": "HIGH", "what": "HandleDestroySession decodes args.SessionID from the wire and calls StateManager.DestroySession(args.SessionID) with zero check that the requesting client owns that session. DESTROY_SESSION is session-exempt (no SEQUENCE required), so any NFS client with network access can send a DESTROY_SESSION targeting another client's SessionId4.", "why": "RFC 8881 \u00a718.37.3 states: 'If the session is owned by a client that is not the same as the client that sent the request, the server MUST return NFS4ERR_NOT_SAME.' Missing authz lets a hostile client tear down any active session once it learns or brute-forces the 16-byte session ID (e.g., via CB_SEQUENCE logging, pcap, or enumeration). Denial-of-service against all connected NFSv4.1 clients is trivially achievable from any unauthenticated NFS connection.", "fix": "Before calling StateManager.DestroySession, verify the requesting client owns the target session. For SEQUENCE-bearing compounds, extract ClientID from the SEQUENCE slot; for session-exempt DESTROY_SESSION sent standalone, require the DESTROY_SESSION args include the owning ClientID (RFC 8881 \u00a718.37.1: dsa_sessionid is the only arg) and cross-check it against the session record. In StateManager.destroySessionLocked, accept an optional callerClientID and return NFS4ERR_NOT_SAME when session.ClientID != callerClientID. Pass ctx.ClientState.ClientID (set during EXCHANGE_ID / CREATE_SESSION on that connection) as the caller identity.", "dimension": "security", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "Confirmed: destroy_session.go:28 calls d.StateManager.DestroySession(args.SessionID) with zero ownership check. No comparison of requesting client identity against session owner. Any NFS client with network access can tear down any session by known/guessed SessionId4."}, {"title": "READ handler bypasses file permission check \u2014 any client with a valid handle reads arbitrary file content", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/read.go", "line": 101, "severity": "HIGH", "what": "Handler.Read never calls GetCachedAuthContext, BuildAuthContextWithMapping, or any permission check. It resolves the block store, calls getFileOrError (plain ctx, no auth), then reads content via common.ReadFromBlockStore \u2014 all without consulting the caller's UID/GID or file mode/ACL. The service exposes PrepareRead(*AuthContext, FileHandle) which enforces read permission; it is never called from this handler.", "why": "Any NFS client that has obtained a file handle (e.g. via LOOKUP on a world-executable directory) can READ the file's bytes regardless of owner, mode bits (e.g. 0600), ACLs, or ShareReadOnly flag. This completely voids DAC enforcement for reads. An attacker NFS-mounting a share as UID=1001 can read root-owned 0600 files if they know or can guess the file handle.", "fix": "Call metaSvc.PrepareRead(authCtx, fileHandle) after building authCtx via h.GetCachedAuthContext(ctx), and propagate NFS3ErrAcces on permission denial. Pattern from WRITE: build authCtx first, then call the permission-checking service method before accessing block store.", "dimension": "security", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed: read.go never calls PrepareRead (which does checkReadPermission at io.go:540) or any auth/permission check. getFileOrError is a plain GetFile with no auth context. After file-type check, goes directly to ReadFromBlockStore. DAC enforcement completely absent for NFSv3 reads."}, {"title": "NSM SM_MON stores client-supplied callback hostname used for outbound TCP dial (SSRF)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nsm/handlers/mon.go", "line": 65, "severity": "HIGH", "what": "mon.MonID.MyID.MyName (a fully client-controlled string from the SM_MON wire request) is stored verbatim as lock.NSMCallback.Hostname at line 65. That value is later passed to callback/client.go:Client.Send as the addr parameter and dialed as TCP at callback/client.go:98 (`dialer.DialContext(callbackCtx, \"tcp\", addr)`). No validation, no allowlist, no RFC-1918 block. The same path applies to the server-restart notify loop in nsm/callback/notify.go:44-57.", "why": "SSRF: any NFS client that can reach the NSM port can register my_name=<internal-IP:port> and cause the server to open a TCP connection to arbitrary targets on the internal network whenever an SM_NOTIFY is dispatched. With prog/vers/proc also client-controlled, the server will send a crafted RPC payload to the target.", "fix": "Validate MyName against the client's own source IP on registration (i.e. require MyName resolves to / equals ctx.ClientAddr's IP), or restrict to IP-only form and reject hostnames that are not the registering client's address. At minimum, block RFC-1918 / loopback / link-local destinations before dialing in Client.Send.", "dimension": "security", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed: mon.go:65 stores mon.MonID.MyID.MyName (fully client-controlled wire field) verbatim into lock.NSMCallback{Hostname:...}. callback/notify.go:44 passes reg.CallbackInfo.Hostname as addr to client.Send. client.go:98 dials dialer.DialContext(callbackCtx, \"tcp\", addr) with no RFC-1918 block, no allowlist, no validation. prog/vers/proc also client-controlled. Classic SSRF from any NFS-reachable host."}, {"title": "SSRF + plaintext S3 traffic via unvalidated http:// endpoint", "file": "/tmp/dittofs-audit/pkg/block/remote/s3/store.go", "line": 183, "severity": "HIGH", "what": "normalizeEndpoint passes through any URL with a recognized scheme unchanged, including http://. An admin who supplies endpoint=http://169.254.169.254/... or any internal http:// URL causes the AWS SDK to issue unauthenticated HTTP requests to that host \u2014 SSRF. Block data also travels in plaintext regardless of whether the encryption decorator is stacked above.", "why": "Line 189-192: when strings.Index(endpoint,'://') > 0 and isValidScheme passes, function returns endpoint verbatim. No scheme whitelist exists. The field flows from user-controlled REST API config (internal/controlplane/api/handlers/block_stores.go Create/Update handlers) directly to s3.NewFromConfig at service.go:2030 and blockstoreprobe/probe.go:196 with no further validation. AWS SDK v2 honors a BaseEndpoint with an http:// scheme and will issue HTTP GET/PUT to it.", "fix": "In normalizeEndpoint, reject non-https schemes: after the isValidScheme check, add `if !strings.EqualFold(scheme, \"https\") { return \"\", fmt.Errorf(\"s3 endpoint scheme %q not allowed; use https\") }`. Return an error from normalizeEndpoint (change signature to (string, error)) and propagate through NewFromConfig. For Localstack/MinIO dev use, allow http:// only when an explicit AllowInsecure bool is set in Config and the build tag is not production.", "dimension": "security", "area": "block-remote-s3-encryption-compression", "confirmed": true, "verdict": "Confirmed. normalizeEndpoint at store.go:189-192: isValidScheme('http') returns true (RFC 3986 valid), so http:// endpoint passes unchanged to s3.Options.BaseEndpoint at store.go:165. AWS SDK v2 honors http:// BaseEndpoint and issues plaintext HTTP. No scheme whitelist exists. endpoint flows from user-controlled REST config via blockstoreprobe/probe.go:196. Fix: whitelist 'https' only, or document that http:// is only for local dev and add a config-level warning."}, {"title": "SSRF + internal-network probe via metadata store Create endpoint", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/metadata_stores.go", "line": 86, "severity": "HIGH", "what": "POST /api/v1/store/metadata calls `runtime.CreateMetadataStoreFromConfig` with fully admin-controlled `type` and `config` (host, port, sslmode) before persisting. For type=postgres the server opens a TCP connection to the caller-supplied host:port; for type=badger it calls `pathutil.ExpandPath` + `badger.NewBadgerMetadataStoreWithDefaults` on a caller-supplied filesystem path. The raw `err.Error()` from these calls is returned in the 400 response body (line 90).", "why": "Admin can point the server at any internal address (cloud metadata endpoint 169.254.169.254, internal DB, k8s API) and observe TCP reachability + connection error text (cert details, banner, timeout vs refused) in the response. Combines SSRF with internal network enumeration and internal error detail disclosure. The `badger` path allows confirming or reading partial data from arbitrary on-disk paths the server process can access.", "fix": "Separate validation (structural only \u2014 type allow-list, required fields present, path absolute) from instantiation. Do NOT open network connections or file handles during config validation in the Create handler. Move the actual `CreateMetadataStoreFromConfig` call to a post-persist async probe, or at minimum strip err.Error() from the 400 response body and log internally only. For postgres, validate that host is not a link-local / loopback / RFC-1918 address if the deployment policy forbids it.", "dimension": "security", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. metadata_stores.go:86-90: CreateMetadataStoreFromConfig opens TCP to caller-supplied type+config (postgres host:port, badger path) before persisting; err.Error() returned verbatim in 400 body at line 90. Admin can probe internal addresses (169.254.169.254, internal DB) and read TCP reachability + error text (cert details, banner, timeout vs refused). badger path allows confirming on-disk paths. Fix: sanitize error before returning; add host allowlist or network policy."}, {"title": "No request body size limit \u2014 OOM/DoS on all JSON endpoints", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/helpers.go", "line": 187, "severity": "HIGH", "what": "`decodeJSONBody` passes `r.Body` directly to `json.NewDecoder(...).Decode(v)` with no `http.MaxBytesReader` guard. Every POST/PUT/PATCH handler (login, create user, create share, create block store, create metadata store, update settings, etc.) is affected. The server's `ReadTimeout` (default 10 s, config.go:197) limits wall-clock time but not bytes read; a slow-sender can stream gigabytes within that window.", "why": "Unauthenticated endpoints (POST /api/v1/auth/login, POST /api/v1/auth/refresh) are reachable without a token. A client sending a multi-GB JSON body to /login causes the Go JSON decoder to buffer arbitrarily large input, exhausting server memory. Authenticated endpoints are also affected for any compromised or malicious admin.", "fix": "Wrap `r.Body` with `http.MaxBytesReader(w, r.Body, maxBodyBytes)` at the top of `decodeJSONBody` (or in a router-level middleware). A conservative limit like 1 MiB covers all legitimate control-plane payloads; snapshot/restore bodies are already small structs.", "dimension": "security", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. helpers.go:187: json.NewDecoder(r.Body).Decode(v) with no http.MaxBytesReader guard. Grep for MaxBytesReader/MaxBytesHandler across entire codebase returns zero results. ReadTimeout=10s limits wall-clock not bytes. Unauthenticated POST /api/v1/auth/login can stream multi-GB JSON body within 10s window on slow sender. Fix: wrap r.Body with http.MaxBytesReader(w, r.Body, maxBodyBytes) in decodeJSONBody."}, {"title": "NLM lock owner identity taken entirely from unauthenticated wire fields", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/lock.go", "line": 91, "severity": "HIGH", "what": "buildOwnerID assembles the lock OwnerID from wire fields CallerName, Svid, and OH (owner handle). handleNLMProcedure in pkg/adapter/nfs/handlers.go:198-253 only parses AUTH_UNIX credentials into ctx.UID \u2014 it never validates that the wire CallerName matches the authenticated credential, and with AUTH_NULL ctx.UID is nil. The ownerID goes directly into LockFileNLM / UnlockFileNLM.", "why": "Any client can send NLM_LOCK with CallerName set to a victim hostname to acquire locks attributed to that host, or send NLM_UNLOCK / NLM_CANCEL with the victim's CallerName and known OH to release/cancel the victim's locks. AUTH_UNIX CallerName is also trivially spoofable because it is not verified. This allows lock poisoning and lock denial-of-service for any NFS client.", "fix": "Bind the NLM OwnerID to the authenticated transport address (TCP source IP) rather than the wire CallerName. For AUTH_UNIX, cross-check that the wire CallerName is consistent with the credential UID or source IP. Reject NLM requests arriving with AUTH_NULL from untrusted source ranges, or enforce host-based access lists at mount time.", "dimension": "security", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed. lock.go:91: buildOwnerID uses wire-supplied CallerName, Svid, OH with no binding to authenticated credential. handlers.go:198-222: AUTH_NULL sets ctx.UID=nil; AUTH_UNIX CallerName is trivially spoofable (not verified against source IP). Any client can send NLM_LOCK/UNLOCK/CANCEL with forged CallerName to acquire or release victim locks. Fix: bind ownerID to source IP:port + file handle at lock acquisition; reject CallerName-only identity when credential is AUTH_NULL."}, {"title": "SSRF via attacker-controlled server-url in operator credentials Secret", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/adapter_reconciler.go", "line": 59, "severity": "HIGH", "what": "getAuthenticatedClient reads `server-url` from the operator credentials Secret and passes it verbatim as the HTTP base URL. The operator holds `secrets: ...patch;delete` on its namespace (RBAC marker line 102 in dittoserver_controller.go). Any principal with that permission (or who can escalate to it) can overwrite the Secret's `server-url` to an arbitrary address \u2014 internal cluster services, cloud metadata endpoint (169.254.169.254), or external. The operator will then send a Bearer token-authenticated DELETE /api/v1/users request to the attacker-controlled URL, leaking the operator service-account JWT.", "why": "server-url is stored in a mutable K8s Secret; operator RBAC grants itself Secret write. No scheme, host, or port validation before use in http.NewRequestWithContext. Attacker controls destination; Bearer token is forwarded unconditionally (dittofs_client.go line 124).", "fix": "Derive the target URL exclusively from the live CR spec via ds.GetAPIServiceURL() instead of trusting the stored server-url. Remove server-url from the credentials Secret entirely; recompute it on each request from the immutable CR spec, which is admission-webhook-validated and namespace-scoped.", "dimension": "security", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. adapter_reconciler.go:59-72: serverURL read from credSecret.Data['server-url'] verbatim; newAPIClient performs no URL scheme/host validation. dittofs_client.go:115: c.baseURL+path passed to http.NewRequestWithContext, Bearer token added at line 124. Operator RBAC at dittoserver_controller.go:102 grants secrets:patch;delete on namespace \u2014 attacker with that permission can overwrite server-url to internal cluster address or metadata endpoint. Fix: validate serverURL scheme (https only) and check it matches expected service hostname before use."}, {"title": "Admin bootstrap password logged to structured logger (credential in logs)", "file": "/tmp/dittofs-audit/cmd/dfs/commands/start.go", "line": 115, "severity": "HIGH", "what": "`logger.Info(\"Admin user created\", \"username\", \"admin\", \"password\", adminPassword)` emits the plaintext admin password as a structured log field at INFO level. If `logging.output` is a file (or a log-drain/SIEM), the credential is persisted there.", "why": "First-run credential must never appear in logs \u2014 only in the operator's terminal (stdout). Logging it at INFO means any log rotation, remote syslog, or log-aggregation pipeline captures the bootstrap password. An attacker with log read access gets full admin credentials.", "fix": "Remove the `password` key-value pair from the `logger.Info` call. Keep only `fmt.Printf` to stdout (already present on line 116). The comment \"It will not be shown again\" is already correct for the stdout path \u2014 just don't contradict it by writing it to the log.", "dimension": "security", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Confirmed. start.go:115: logger.Info('Admin user created', 'username', 'admin', 'password', adminPassword) emits plaintext bootstrap password as structured log field at INFO level. Line 116 also prints to stdout (correct). Log-drain/SIEM/file output captures credential. Fix: remove password from logger.Info call; keep only fmt.Printf to stdout."}, {"title": "UnlockAllForSession(sessionID=0) is a no-op for disconnected handles in ProcessAppInstanceId", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/durable_context.go", "line": 1033, "severity": "HIGH", "what": "ProcessAppInstanceId calls metaSvc.UnlockAllForSession(ctx, h.MetadataHandle, 0) with sessionID=0 when displacing persisted durable handles. doUnlockAllForSession filters by existing[i].SessionID == sessionID; real SMB session IDs are non-zero, so the call removes nothing. Byte-range locks from the displaced disconnected session are never released.", "why": "The displaced persisted handle's locks were recorded under the original (non-zero) session ID. Passing 0 matches no lock entries. After the AppInstanceId failover, stale byte-range locks from the old session remain in the lock manager for that metadata handle. A subsequent lock or write from the new claimant can block on phantom locks, breaking the Hyper-V failover contract (MS-SMB2 \u00a73.3.5.9.13). Classic AI-slop: plausible placeholder value (0 = 'no session') that is actually a no-op.", "fix": "Replace with UnlockAllForOpen keyed on the persisted handle's OriginalFileID: metaSvc.UnlockAllForOpen(ctx, h.MetadataHandle, fmt.Sprintf(\"%x\", h.OriginalFileID)). PersistedDurableHandle already carries OriginalFileID (added by buildPersistedDurableHandle). Fall back to h.FileID when OriginalFileID is zero (pre-field legacy rows).", "dimension": "slop", "area": "smb-create-open", "confirmed": true, "verdict": "Confirmed: durable_context.go:1033 calls UnlockAllForSession(ctx, h.MetadataHandle, 0). manager.go doUnlockAllForSession filters by existing[i].SessionID==sessionID; sessionID=0 never matches. More critically: SMB byte-range locks use OpenID-based cleanup (UnlockAllForOpen), not session-based (per lock/manager.go:1310-1318 comment). Displaced durable handle locks are never released. fix: call UnlockAllForOpen with the handle's OpenID instead."}, {"title": "SMBAuthenticator validates Type-3 against any pending challenge, not session-specific one", "file": "/tmp/dittofs-audit/internal/adapter/smb/auth/authenticator.go", "line": 256, "severity": "HIGH", "what": "`validateAgainstPendingAuths` iterates ALL pending NTLM challenges with `a.pendingAuths.Range` and accepts any matching challenge. There is no session-ID parameter \u2014 the Type-3 from connection A can match the challenge issued to connection B.", "why": "NTLM challenge-response must be bound to the specific challenge issued in the same session/connection. Accepting any in-flight challenge breaks the session-binding invariant and allows cross-session replay: if attacker can issue a concurrent NTLM handshake they control the challenge and can authenticate as any user whose Type-3 they relay to this server.", "fix": "Thread the session ID (or the specific serverChallenge) from the pending-auth store through to `handleNTLMAuthenticate`, look up the specific `pendingNTLMAuth` by that key, and validate against only that entry \u2014 same pattern as the real path in `handlers/session_setup.go` which uses `GetPendingAuth(ctx.SessionID, ctx.ConnID)`.", "dimension": "slop", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Confirmed: validateAgainstPendingAuths (line 256) iterates ALL entries in a.pendingAuths with no session/connection parameter. pendingAuths keyed by internal counter; any in-flight challenge can match any Type-3 from any connection. No session binding enforced. Enables NTLM relay cross-connection within the same server instance."}, {"title": "COPYCHUNK access check uses DesiredAccess (pre-DACL) instead of GrantedAccess (post-DACL)", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/ioctl_copychunk.go", "line": 250, "severity": "HIGH", "what": "validateCopyChunkAccess reads src.DesiredAccess and dst.DesiredAccess to gate FILE_READ_DATA/FILE_EXECUTE on source and FILE_WRITE_DATA/FILE_APPEND_DATA/FILE_READ_DATA on destination. Every other access check in the SMB handler layer (READ, WRITE, SET_INFO, QUERY_INFO, SET_SPARSE, SET_ZERO_DATA, QUERY_ALLOCATED_RANGES) uses GrantedAccess (post-DACL intersection computed at CREATE).", "why": "DesiredAccess is what the client asked for; GrantedAccess is what the DACL actually granted. A MAXIMUM_ALLOWED or GENERIC_ALL open whose DACL strips FILE_READ_DATA still has those bits in DesiredAccess, so the check always passes even when the effective rights are absent. This is the same vulnerability class fixed by #616 (ChangeNotify), explicitly documented in multiple handler comments as 'Gate consults Open.GrantedAccess (post-DACL intersection at CREATE), not the pre-DACL DesiredAccess'.", "fix": "Change lines 250\u2013251 from `src.DesiredAccess` / `dst.DesiredAccess` to `src.GrantedAccess` / `dst.GrantedAccess`, and update the fmt.Sprintf log lines on 256, 263, 270 to match. Match the pattern used in handleSetSparse (line 78), handleQueryAllocatedRanges (line 161), and handleSetZeroData (line 392).", "dimension": "slop", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "Confirmed: ioctl_copychunk.go:250-251 uses src.DesiredAccess and dst.DesiredAccess. Every other handler uses GrantedAccess (post-DACL). auth_priming_test.go explicitly documents this as bug class B. DesiredAccess=MAXIMUM_ALLOWED bypasses DACL stripping."}, {"title": "reapExpiredSessions leaks BackchannelSender goroutines on lease expiry", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 2644, "severity": "HIGH", "what": "Lines 2644\u20132648 delete expired-client sessions from sessionsByID/sessionsByClientID directly, then call purgeV41Client (line 2665) which also deletes map entries directly. Neither path calls stopBackchannelSender. The BackchannelSender goroutine (started by StartBackchannelSender) only terminates on Stop() or context cancellation \u2014 neither happens here. Shutdown() at line 842 iterates sessionsByID to stop senders, but by then those sessions are already deleted.", "why": "Goroutine without lifecycle/cancellation (Go best-practice violation). Every v4.1 client whose lease expires at runtime leaks its BackchannelSender goroutine. Under steady NFS workload (clients reconnecting across lease periods) this accumulates unboundedly. The goroutine holds a *BackchannelSender (containing a channel + references to StateManager) preventing GC.", "fix": "In reapExpiredSessions, replace the direct session-delete loop (lines 2644\u20132648) with calls to sm.destroySessionLocked(session.SessionID, true, \"lease_expired\") for each session \u2014 this already calls stopBackchannelSender. Alternatively, in purgeV41Client, iterate sm.sessionsByClientID[record.ClientID] and call sm.stopBackchannelSender(session.SessionID) before deleting from the maps.", "dimension": "slop", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "Confirmed: reapExpiredSessions (manager.go:2645-2648) deletes session entries from sessionsByID/sessionsByClientID directly without calling stopBackchannelSender. purgeV41Client (v41_client.go:322-323) also deletes without stopping senders. DestroySession (line 2537-2538) correctly calls stopBackchannelSender first \u2014 reapExpiredSessions bypasses that path. Shutdown iterates sessionsByID which is already empty for reaped sessions, missing those goroutines."}, {"title": "Map write under RLock in isRecentlyRecalled causes data race", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/delegation.go", "line": 695, "severity": "HIGH", "what": "`isRecentlyRecalled` calls `delete(sm.recentlyRecalled, fhKey)` (lazy TTL cleanup) while `ShouldGrantDelegation` holds only `sm.mu.RLock()`. Multiple concurrent goroutines can hold RLock simultaneously, so two concurrent `ShouldGrantDelegation` calls \u2014 or one `ShouldGrantDelegation` and any read-locked operation that incidentally calls `isRecentlyRecalled` \u2014 produce a concurrent map write, triggering a Go data race detector panic and undefined behaviour.", "why": "Go maps are not goroutine-safe for mixed read/write access. `sync.RWMutex.RLock` allows multiple concurrent readers; a `delete` while any other reader holds RLock is a concurrent write. `go test -race` will catch this as a DATA RACE on `sm.recentlyRecalled`.", "fix": "Remove the lazy delete from `isRecentlyRecalled`. Either (a) call it only from paths that hold `sm.mu.Lock()` (e.g. `GrantDelegation`, `RevokeDelegation`, `CheckDelegationConflict`) and keep the lazy cleanup there, or (b) run a periodic cleanup goroutine. For the read-only check path, just return `true`/`false` without modifying the map: remove lines 695-696 (`delete(sm.recentlyRecalled, fhKey); return false`) and simply `return false` when TTL expired \u2014 let the write-lock paths prune stale entries.", "dimension": "slop", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "Confirmed: delegation.go:695 calls delete(sm.recentlyRecalled, fhKey) while ShouldGrantDelegation holds only sm.mu.RLock() (line 406-407). The docstring 'Caller must hold sm.mu (RLock or Lock)' is incorrect for a write operation. Concurrent RLock holders can race on the map write. Go data race detector will panic."}, {"title": "CB_SEQUENCE seqid skips every other value \u2014 RFC 8881 \u00a720.9 violation", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/backchannel.go", "line": 270, "severity": "HIGH", "what": "`sendCallback` calls `bs.nextXID.Add(1)` twice: once at line 270 for `seqID` (used in CB_SEQUENCE) and again at line 284 for `xid` (RPC XID). Both are drawn from the same `atomic.Uint32` counter, so every call increments it by 2. Slot-0 CB_SEQUENCE seqids are 1, 3, 5, 7\u2026 instead of 1, 2, 3, 4\u2026. RFC 8881 \u00a720.9 (CB_SEQUENCE4args) requires the server to use a value exactly one greater than the last value sent on that slot. Linux `nfsd` and macOS kernel clients enforce this; a non-incrementing-by-one seqid results in `NFS4ERR_SEQ_MISORDERED` and the callback is rejected.", "why": "Two logically independent counters \u2014 CB_SEQUENCE per-slot seqid and RPC XID \u2014 share one `atomic.Uint32`. The double-Add per call is a design mistake: seqid must advance by 1 per slot use (RFC 8881 \u00a72.10.6.3), while XID only needs uniqueness per outstanding call.", "fix": "Use two separate counters: a dedicated `cbSeqID atomic.Uint32` for CB_SEQUENCE seqids and keep `nextXID` for RPC XIDs only. Change line 270 to `seqID := bs.cbSeqID.Add(1)` and add `cbSeqID atomic.Uint32` to the `BackchannelSender` struct. This gives monotonically-increasing-by-1 seqids while XIDs remain independently unique.", "dimension": "slop", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "Confirmed: backchannel.go:270 seqID=bs.nextXID.Add(1) then line 284 xid=bs.nextXID.Add(1) \u2014 same atomic counter incremented twice per sendCallback call. seqids sent are 1,3,5,7... instead of 1,2,3,4... RFC 8881 \u00a720.9 requires exactly +1 per slot use. Linux/macOS NFS clients will reject with NFS4ERR_SEQ_MISORDERED."}, {"title": "ResetLocalState residual sweep non-recursive \u2014 misses subdirectory logs", "file": "/tmp/dittofs-audit/pkg/block/local/fs/drain_reset.go", "line": 272, "severity": "HIGH", "what": "`os.ReadDir(logsDir)` is a single-level scan; the loop at line 274 explicitly skips directories. PayloadIDs like `share1/file.txt` produce logs at `<logsDir>/share1/file.txt.log` \u2014 inside a subdirectory. After the live-fd teardown loop (which handles every fd in `sh.logFDs`), the residual sweep is only meant to catch orphaned logs that had no live fd. Those orphaned logs in subdirectories are silently missed.", "why": "Doc comment says \"removes the on-disk `.log` files under `<baseDir>/logs/`\". `recoverAppendLogs` and `cleanupCompactTemps` both use `filepath.WalkDir` for the same reason (comment in `compaction.go:415` explicitly calls out the two-level layout). The test (`TestResetLocalState_DropsStaleLog`) only uses flat payload IDs and never exercises the subdirectory case. On a store with share-prefixed payloads, after `ResetLocalState` an orphaned `logs/<share>/<file>.log` survives on disk. The next boot's `recoverAppendLogs` re-installs it as a live fd, and `replayLogIntoDest` overlays stale post-snapshot bytes on top of the restored CAS content \u2014 exactly the C2 silent corruption the function is supposed to prevent.", "fix": "Replace `os.ReadDir` + manual loop with `filepath.WalkDir(logsDir, ...)` skipping directories and removing every `.log` file, matching the pattern used by `cleanupCompactTemps` and `recoverAppendLogs`.", "dimension": "slop", "area": "block-local-fs-rollup-chunk", "confirmed": true, "verdict": "Confirmed: logPath() (appendwrite.go:59) uses flat `{baseDir}/logs/{payloadID}.log` where payloadID contains `/` (recovery.go:237-240 comment confirms `<logsDir>/<share>/<file>.log` nested layout). ResetLocalState residual sweep (drain_reset.go:272-282) uses os.ReadDir (single-level) and explicitly skips dirs at line 274. Orphaned nested logs survive. recoverAppendLogs uses filepath.WalkDir for the same reason (recovery.go:229)."}, {"title": "updateDescendantPaths silently discards PutFile error on every descendant write", "file": "/tmp/dittofs-audit/pkg/metadata/file_modify.go", "line": 829, "severity": "HIGH", "what": "tx.PutFile error blank-assigned with `_ = tx.PutFile(ctx, child)` inside the path-rewrite loop. Every descendant whose Path update fails is silently skipped.", "why": "The caller comment at line 759-761 explicitly states 'Propagate the error: a partial descendant rewrite leaves stale child Paths that diverge from the parent, corrupting the Path-keyed postgres namespace. Roll the rename back instead.' The implementation contradicts the stated invariant. On postgres backend, a rename of any directory with children that hits a transient write error leaves child File.Path values pointing to the old location while the directory entry points to the new one \u2014 corrupting the postgres path-keyed namespace permanently. No panic, no error, transaction commits in a broken state.", "fix": "Return the error instead of discarding it: `if err := tx.PutFile(ctx, child); err != nil { return fmt.Errorf(\"update path for %q: %w\", child.Path, err) }`", "dimension": "slop", "area": "metadata-service-auth", "confirmed": true, "verdict": "Confirmed: updateDescendantPaths line 829 is `_ = tx.PutFile(ctx, child)`. Caller comment at file_modify.go:759-761 explicitly states 'Propagate the error: a partial descendant rewrite leaves stale child Paths that diverge from the parent, corrupting the Path-keyed postgres namespace. Roll the rename back instead.' Implementation contradicts stated invariant."}, {"title": "SetFileAttributes: AtimeNow/MtimeNow boolean flags never applied \u2014 UTIME_NOW is a silent no-op", "file": "/tmp/dittofs-audit/pkg/metadata/file_modify.go", "line": 219, "severity": "HIGH", "what": "SetAttrs.AtimeNow and SetAttrs.MtimeNow are consumed only in the permission-check branch (onlySettingTimesToNow, line 219-220) but are never used in the attribute-mutation block. The mutation block only handles attrs.Atime != nil (line 387) and attrs.Mtime != nil (line 392). NFS/SMB callers signalling UTIME_NOW pass AtimeNow=true with Atime=nil, so neither branch fires and the timestamp is never updated.", "why": "POSIX utimensat(UTIME_NOW) and NFSv4 SETATTR time_access_set=SET_TO_SERVER_TIME / time_modify_set=SET_TO_SERVER_TIME require the server to stamp the current time. DittoFS accepts the SETATTR (permission check passes correctly via onlySettingTimesToNow), returns success, but leaves file.Atime and file.Mtime unchanged. NFS clients relying on server-side timestamp updates (e.g. after O_RDWR write without explicit mtime) see stale timestamps.", "fix": "After the existing attrs.Atime/attrs.Mtime blocks add:\n  if attrs.AtimeNow { file.Atime = now; modified = true }\n  if attrs.MtimeNow { file.Mtime = now; modified = true }", "dimension": "slop", "area": "metadata-service-auth", "confirmed": true, "verdict": "Confirmed: AtimeNow/MtimeNow appear only at lines 220 and 224 (permission-check branch). Mutation block at lines 387-405 only handles `attrs.Atime != nil` and `attrs.Mtime != nil`. No code path sets file.Atime/file.Mtime to `now` when AtimeNow=true/MtimeNow=true with nil Atime/Mtime. UTIME_NOW is silent no-op."}, {"title": "scanExpiredLeaseBreaks uses AcquiredAt (grant time) instead of break-start time for timeout computation", "file": "/tmp/dittofs-audit/pkg/metadata/lock/oplock_break.go", "line": 274, "severity": "HIGH", "what": "breakDeadline computed as pl.AcquiredAt.Add(timeout). Comment on line 273 claims 'AcquiredAt is updated when break initiated' \u2014 fabricated: no code path ever updates AcquiredAt on break initiation. applyBreakStageLocked writes BreakStarted=time.Now() (manager.go:2671) on the in-memory OpLock, but BreakStarted is explicitly 'runtime-only, not persisted' (store.go:434). AcquiredAt in PersistedLock maps to UnifiedLock.AcquiredAt, set at grant time and never changed.", "why": "Any lease held longer than the break timeout (35s default) before a break is initiated has an AcquiredAt-derived deadline already in the past. The scanner will force-revoke such leases on the very next 1-second scan tick, treating a correctly-responding client as having timed out. Leases on long-lived open files are the common case.", "fix": "Add a BreakStartedAt field to PersistedLock (store.go), populate it in ToPersistedLock from lock.Lease.BreakStarted when Breaking=true, restore it in FromPersistedLock, and use pl.BreakStartedAt in the deadline calculation. Drop the false AcquiredAt-update comment.", "dimension": "slop", "area": "metadata-lock-manager", "confirmed": true, "verdict": "Confirmed: oplock_break.go:274 uses pl.AcquiredAt (persisted, set at grant time per store.go:69-70). BreakStarted=time.Now() set in manager.go:2671 but store.go:434 marks it 'runtime-only, not persisted'. Comment in oplock_break.go:273 'AcquiredAt is updated when break initiated' is false \u2014 no code path ever updates AcquiredAt on break."}, {"title": "accessModesConflict logic contradicts doc: requires BOTH modes non-None, so existing deny-mode holders never block None-mode openers", "file": "/tmp/dittofs-audit/pkg/metadata/lock/types.go", "line": 354, "severity": "HIGH", "what": "accessModesConflict returns true only when both a != AccessModeNone && b != AccessModeNone. Doc says 'Any deny mode (DenyRead, DenyWrite, DenyAll) conflicts with any non-None mode on the other side' \u2014 the implementation contradicts this. existing=DenyWrite, new=None returns false (no conflict), meaning an open with share-deny-write does not block a subsequent None opener.", "why": "Share-deny semantics in SMB: a holder with AccessModeDenyWrite should prevent new opens that want write access, regardless of the new opener's own deny flags. The AND condition means the check is effectively a no-op whenever any side opens with AccessModeNone, which is the default for most opens. ConflictsWith relies entirely on this for case 1 (access mode conflicts).", "fix": "The correct check is asymmetric: existing's deny mode constrains what the new opener can request. Replace with: 'return (a == AccessModeDenyRead || a == AccessModeDenyAll) && (b needs read) || (a == AccessModeDenyWrite || a == AccessModeDenyAll) && (b needs write)' \u2014 or at minimum 'return a != AccessModeNone || b != AccessModeNone' to match the doc description.", "dimension": "slop", "area": "metadata-lock-manager", "confirmed": true, "verdict": "Confirmed: accessModesConflict (types.go:353-354) returns `a != AccessModeNone && b != AccessModeNone` \u2014 requires BOTH non-None. Doc at lines 350-352 says 'Any deny mode (DenyRead, DenyWrite, DenyAll) conflicts with any non-None mode on the other side' \u2014 implementation contradicts doc. existing=DenyWrite, new=None returns false (no conflict)."}, {"title": "memory DeleteShare skips usedBytes update and objectIndex cleanup", "file": "/tmp/dittofs-audit/pkg/metadata/store/memory/shares.go", "line": 134, "severity": "HIGH", "what": "Direct-store `DeleteShare` deletes all files for the share without adjusting `store.usedBytes` (atomic counter) and without removing their entries from `store.objectIndex`. The transactional counterpart at transaction.go:639 does both correctly via `tx.pendingDelta` and explicit `delete(tx.store.objectIndex, ...)` calls.", "why": "After a share is deleted via the non-transactional path (called by storetest and any caller that invokes the `metadata.Store` surface directly), `usedBytes` is permanently over-reported by the deleted files' sizes, and stale ObjectID\u2192handle entries remain in `objectIndex`. A later `FindByObjectID` for a recycled ObjectID can collide with the orphaned entry and return a false hit or a spurious ErrConflict from `PutFile`.", "fix": "Delegate to `WithTransaction`: `return store.WithTransaction(ctx, func(tx metadata.Transaction) error { return tx.DeleteShare(ctx, shareName) })`. The tx version already handles both `pendingDelta` and `objectIndex` cleanup correctly.", "dimension": "slop", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: memory/shares.go DeleteShare (lines 151-162) iterates and deletes files but never touches store.usedBytes or store.objectIndex. transaction.go correctly handles both (lines 169, 261, 311, 662). After DeleteShare, usedBytes permanently over-reported and stale objectIndex entries can cause false FindByObjectID hits."}, {"title": "poolRow leaks connection when Scan never called", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/pool_helpers.go", "line": 174, "severity": "HIGH", "what": "poolRow.Scan is the ONLY path that calls conn.Release(). queryRow() manually acquires a dedicated pool connection via pool.Acquire(), wraps it in poolRow, then returns. If anything panics between queryRow() and .Scan() \u2014 or if a caller discards the returned pgx.Row without scanning \u2014 the acquired connection is never returned to the pool. No defer and no finalizer exist as a fallback.", "why": "Connection pool exhaustion: each leaked connection permanently reduces pool capacity by one. Under sustained load (e.g. panics in a handler, or callers that short-circuit before scanning) the pool drains and subsequent operations block then time out. The native pgxpool Row returned by conn.QueryRow() internally tracks the connection and releases it on Scan, but the custom poolRow wrapper breaks that lifecycle.", "fix": "Add a sync.Once or a deferred release inside poolRow so the connection is always returned. Simplest: store a 'released' flag and call conn.Release() in both Scan and a Close method, or use a runtime.SetFinalizer as a last-resort backstop. Better still, drop the manual Acquire/release pattern and use pgxpool.Pool.QueryRow directly with a context that carries the acquire timeout \u2014 pgxpool manages the connection lifecycle internally: 'acquireCtx, cancel := context.WithTimeout(ctx, poolConnectionAcquireTimeout); defer cancel(); return s.pool.QueryRow(acquireCtx, sql, args...)'.", "dimension": "slop", "area": "metadata-store-postgres", "confirmed": true, "verdict": "Confirmed: poolRow.Scan (pool_helpers.go:174-178) is the only path calling conn.Release(). queryRow acquires connection via pool.Acquire (line 48) and wraps in poolRow (line 60) with no defer. If caller panics between queryRow() and .Scan(), or discards the pgx.Row without scanning, connection leaks permanently."}, {"title": "Response.Encode silently truncates stub data > 65511 bytes", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/dcerpc.go", "line": 348, "severity": "HIGH", "what": "When fragLen > MaxFragmentSize (65535), code caps fragLen then allocates buf of that capped size, but copy(buf[24:], r.StubData) silently drops stub bytes beyond offset 65511. FragLength header claims 65535 but actual payload is truncated. No error returned, no fragmentation attempted.", "why": "Plausible-but-wrong logic: the 'validate' comment implies safety but produces a malformed single-fragment response. LsarLookupSids with many/long domain names or a large NetrShareEnum response can exceed 65511 stub bytes. Client parses truncated NDR and sees corrupt/garbage data. This is slop \u2014 real DCE/RPC requires multi-fragment or a transport-level chunking path, not silent truncation.", "fix": "Return an error (or panic) if fragLen > MaxFragmentSize rather than silently capping. Alternatively implement multi-fragment responses. At minimum: if fragLen > MaxFragmentSize { return nil, fmt.Errorf(\"DCE/RPC response too large: %d bytes\", fragLen) } and propagate the error up through HandleRequest/ProcessWrite.", "dimension": "slop", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: dcerpc.go:348-371 caps fragLen=65535, allocates buf[65535], then copy(buf[24:], r.StubData) silently drops bytes beyond offset 65511. FragLength header claims 65535 but payload is truncated. No error returned, no fragmentation attempted."}, {"title": "defaultSIDMapper package-level var read/written without synchronization \u2014 data race", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/security.go", "line": 107, "severity": "HIGH", "what": "defaultSIDMapper is a package-level *sid.SIDMapper var. SetSIDMapper (line 112) writes it; BuildSecurityDescriptor, principalToSID, ParseSecurityDescriptorWithOptions (lines 156, 282, 437, 447, 556) read it during concurrent request handling. No mutex or atomic protects the pointer.", "why": "Go memory model requires synchronization for concurrent read/write to the same variable. Any test with t.Parallel() or any server that calls SetSIDMapper after Serve() starts will trigger a data race detectable by -race. The comment 'Must be called before any connections are accepted' is not enforced. AI-slop: the SetSIDMapper nil-guard looks safety-conscious but the actual race is on the pointer assignment itself.", "fix": "Replace the bare var with a sync/atomic or sync.RWMutex-protected accessor. Simplest: use sync/atomic.Pointer[sid.SIDMapper]. Change var to var defaultSIDMapper atomic.Pointer[sid.SIDMapper] and init via defaultSIDMapper.Store(sid.NewSIDMapper(0,0,0)); update SetSIDMapper to call defaultSIDMapper.Store(m); update all reads to defaultSIDMapper.Load().", "dimension": "slop", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: defaultSIDMapper is plain var *sid.SIDMapper (security.go:107). SetSIDMapper writes it (line 114) with no lock. Read at lines 156, 282, 298, 437, 447, 556 during concurrent request handling with no mutex/atomic. Comment says 'Must be called before any connections' \u2014 not enforced; any test or late-init caller triggers data race."}, {"title": "leaseTimeSeconds global mutable uint32 \u2014 data race with concurrent GETATTR handlers", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/attrs/encode.go", "line": 82, "severity": "HIGH", "what": "Plain `var leaseTimeSeconds uint32` written by SetLeaseTime() and read concurrently by every GETATTR encoding goroutine (lines 326, 554) with no synchronization. Immediately below it, the comment on line 97 explicitly says `fsMaxFileSize/Read/Write` use `atomic.Uint64` *to avoid data races with concurrent GETATTR encoding* \u2014 the same race class \u2014 but `leaseTimeSeconds` was inconsistently left as a bare global.", "why": "Concurrent unsynchronized read+write on a plain Go variable is a data race; `go test -race` will flag it. Under concurrent GETATTR load, reads at lines 326/554 race with writes at line 87.", "fix": "Replace `var leaseTimeSeconds uint32` with `var leaseTimeSeconds atomic.Uint32`, update SetLeaseTime to `leaseTimeSeconds.Store(seconds)`, GetLeaseTime to `return leaseTimeSeconds.Load()`, and both read sites to `leaseTimeSeconds.Load()`.", "dimension": "slop", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "Confirmed: leaseTimeSeconds is plain `var uint32` (encode.go:82). SetLeaseTime() writes it (line 87) without atomic. Read at lines 326 and 554 by concurrent GETATTR encoding goroutines. Lines 97-101 adjacent comment explicitly notes fsMaxFileSize/Read/Write use atomic.Uint64 'to avoid data races with concurrent GETATTR encoding' \u2014 leaseTimeSeconds was inconsistently omitted."}, {"title": "Nil-deref after GetFile error-discard in LINK post-success attr fetch", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/link.go", "line": 239, "severity": "HIGH", "what": "After successful CreateHardLink, GetFile is called to fetch updated nlink attrs. On error the error is logged with a comment saying 'handle gracefully', but execution falls through unconditionally to `h.convertFileAttrToNFS(fileHandle, &updatedFile.FileAttr)` \u2014 dereferencing a nil pointer.", "why": "Lines 233-237: `updatedFile, err := metaSvc.GetFile(...)` followed by `if err != nil { logError(...) // Continue }`. No return or guard. Line 239 then does `&updatedFile.FileAttr` which panics when `updatedFile == nil`. Any transient store error or context cancellation after a successful hard-link creation will crash the server process.", "fix": "Guard the dereference: `if updatedFile == nil { return &LinkResponse{...NFS3ErrIO...}, nil }`. Or use the pre-fetched fileAttr as fallback: replace line 239 with `var nfsFileAttr *types.NFSFileAttr; if updatedFile != nil { nfsFileAttr = h.convertFileAttrToNFS(fileHandle, &updatedFile.FileAttr) } else { nfsFileAttr = h.convertFileAttrToNFS(fileHandle, &fileAttr.FileAttr) }`.", "dimension": "slop", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed. link.go:233-239: `updatedFile, err := metaSvc.GetFile(...)` \u2014 on error logs and falls through with no return/guard. Line 239 `h.convertFileAttrToNFS(fileHandle, &updatedFile.FileAttr)` dereferences nil `updatedFile`. convertFileAttrToNFS takes `*metadata.FileAttr`; `&nilPtr.Field` panics. Any transient store error after successful CreateHardLink crashes server."}, {"title": "Nil-deref pattern: error-discarding GetFile then unconditional FileAttr dereference in MKDIR (5 sites)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/mkdir.go", "line": 177, "severity": "HIGH", "what": "Five sites in mkdir.go use `updatedParentFile, _ := metaSvc.GetFile(ctx.Context, parentHandle)` (ignoring error) immediately followed by `h.convertFileAttrToNFS(parentHandle, &updatedParentFile.FileAttr)`. If GetFile fails \u2014 e.g. during context cancellation (line 177 is literally inside a ctx.Context.Err() branch), store error, or handle not found \u2014 `updatedParentFile` is nil and `&updatedParentFile.FileAttr` panics.", "why": "Lines 177-178, 192-193, 210-211, 268-269, 281-282 all follow the same pattern. The most egregious is line 177 which is inside the `ctx.Context.Err() != nil` branch \u2014 GetFile will routinely fail on a cancelled context. This is AI-slop copy-paste: the author intended best-effort WCC but implemented a certain nil-deref.", "fix": "Check nil before dereferencing: `if updatedParentFile != nil { wccAfter = h.convertFileAttrToNFS(parentHandle, &updatedParentFile.FileAttr) }`. Same fix needed at all 5 sites. Alternatively use `dirWccPair` which already handles nil safely.", "dimension": "slop", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed. mkdir.go lines 177-178, 192-193, 210-211, 268-269, 281-282 all use `updatedParentFile, _ := metaSvc.GetFile(...)` then `&updatedParentFile.FileAttr`. nil-deref on GetFile failure. Most egregious is line 177 inside the `ctx.Context.Err() != nil` branch where GetFile on cancelled ctx will routinely return nil."}, {"title": "Nil-deref pattern: error-discarding GetFile then unconditional FileAttr dereference in MKNOD (2 sites)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/mknod.go", "line": 206, "severity": "HIGH", "what": "Two sites in mknod.go use `updatedParentFile, _ := metaSvc.GetFile(ctx.Context, parentHandle)` (error discarded) then immediately `h.convertFileAttrToNFS(parentHandle, &updatedParentFile.FileAttr)`. Nil-deref panic if GetFile fails.", "why": "Lines 206-207 and 277-278 are identical to the mkdir.go pattern. Same root cause: copy-paste WCC best-effort code that silently crashes instead of returning nil WCC attrs.", "fix": "Add nil check: `if updatedParentFile != nil { wccAfter = h.convertFileAttrToNFS(parentHandle, &updatedParentFile.FileAttr) }` at both sites (lines 206-207 and 277-278).", "dimension": "slop", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed. mknod.go lines 206-207 and 277-278: `updatedParentFile, _ := metaSvc.GetFile(...)` immediately followed by `h.convertFileAttrToNFS(parentHandle, &updatedParentFile.FileAttr)`. Same nil-deref pattern as mkdir/link. Panic on store error or context cancellation."}, {"title": "Nil-deref pattern: error-discarding GetFile then unconditional FileAttr dereference in LINK WCC error paths (2 sites)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/link.go", "line": 187, "severity": "HIGH", "what": "Two sites in link.go use `updatedDirFile, _ := metaSvc.GetFile(ctx.Context, dirHandle)` (error discarded) then `h.convertFileAttrToNFS(dirHandle, &updatedDirFile.FileAttr)`. Nil-deref panic if GetFile fails.", "why": "Lines 187-188 (LINK NFS3ErrExist path) and 216-217 (LINK store error path) both ignore GetFile errors. If the directory was concurrently removed or context is cancelled, `updatedDirFile` is nil and the subsequent dereference panics.", "fix": "Add nil check: `if updatedDirFile != nil { dirWccAfter = h.convertFileAttrToNFS(dirHandle, &updatedDirFile.FileAttr) }` at both sites.", "dimension": "slop", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed. link.go lines 187-188 (NFS3ErrExist path) and 216-217 (store error path): `updatedDirFile, _ := metaSvc.GetFile(...)` then `h.convertFileAttrToNFS(dirHandle, &updatedDirFile.FileAttr)`. nil-deref if GetFile fails. Both are error-handling paths where context cancellation or concurrent dir removal is plausible."}, {"title": "provisionOperatorAccount logins with wrong (freshly-generated) password after 409 conflict", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/auth_reconciler.go", "line": 250, "severity": "HIGH", "what": "When CreateUser returns 409 (operator account already exists \u2014 e.g. credentials Secret was deleted but DittoFS retained the user), code at line 242 catches the conflict and falls through to Login at line 250 using operatorPassword, the freshly-generated random password from line 232. The existing operator account has a completely different password; this Login call returns 401. isTransientError returns false for 401, so reconcileAuth treats it as a permanent failure, sets Authenticated=False, and the operator is permanently stuck.", "why": "Plausible-but-wrong control flow: comment says 'proceed to login' as if the new password applies to the pre-existing account. It does not. The only way a 409 can arrive with no credentials Secret is if the Secret was externally deleted while the DittoFS user persisted. Logging in with an unrelated new password cannot succeed.", "fix": "On 409 conflict, delete the existing operator user via the admin token (apiClient.DeleteUser) and then re-create with the freshly-generated password, or alternatively re-use admin-token to reset the password via a PUT/PATCH /api/v1/users/{username} endpoint if available. Simplest safe fix: treat 409 as a re-provision signal \u2014 delete + recreate \u2014 so the new operatorPassword is authoritative.", "dimension": "slop", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. auth_reconciler.go:242-250: 409 conflict branch logs 'already exists, proceeding to login' then calls Login with freshly-generated `operatorPassword` (line 250). Existing account has a different password \u2192 Login returns 401 \u2192 reconcileAuth treats as permanent failure \u2192 operator stuck in Authenticated=False forever. Scenario: operator credentials Secret deleted but DittoFS user persisted. Fix: on 409 either re-read from existing credentials Secret or attempt password reset."}, {"title": "LoginAsAdmin writes/reads developer's real ~/.config/dfsctl/config.json \u2014 no XDG isolation", "file": "/tmp/dittofs-audit/test/e2e/helpers/cli.go", "line": 190, "severity": "HIGH", "what": "LoginAsAdmin runs `dfsctl login` which writes a JWT token into ~/.config/dfsctl/config.json (or $XDG_CONFIG_HOME/dfsctl/config.json). Then extractTokenFromLogin reads that file back. No XDG_CONFIG_HOME isolation is applied; the developer's real credentials file is overwritten on every test run. context_test.go shows the correct pattern (setupIsolatedCredentials calls t.Setenv(\"XDG_CONFIG_HOME\", t.TempDir())), but LoginAsAdmin \u2014 used in 37 test files \u2014 never applies it.", "why": "Every call to LoginAsAdmin corrupts the real developer credentials. In parallel test runs across 37 test files all talking to different short-lived test servers, entries accumulate indefinitely. More critically, if the developer is logged into a production dfsctl context and runs the e2e suite, their session is overwritten/corrupted.", "fix": "Add `t.Setenv(\"XDG_CONFIG_HOME\", t.TempDir())` inside LoginAsAdmin before invoking RunRaw(\"login\", ...). This isolates each test's credentials to a temp dir that is cleaned up automatically.", "dimension": "slop", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. LoginAsAdmin (cli.go:190) calls runner.RunRaw(\"login\"...) which invokes real dfsctl binary. extractTokenFromLogin at line 236 reads XDG_CONFIG_HOME from env, falls back to ~/.config. LoginAsAdmin never calls t.Setenv(\"XDG_CONFIG_HOME\", t.TempDir()). The isolation pattern exists only in context_test.go's setupIsolatedCredentials, not in LoginAsAdmin itself. 60 call sites unprotected."}, {"title": "cleanupAndDelete has no synchronization \u2014 concurrent callers double-cleanup same handle", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/durable_scavenger.go", "line": 136, "severity": "MED", "what": "expireHandles (background ticker goroutine) and HandleConflictingOpen/ForceExpireDurableHandle (SMB handler goroutines) can race to call cleanupAndDelete for the same PersistedDurableHandle simultaneously. No mutex or in-flight tracking on the scavenger struct guards this.", "why": "Double-unlock of byte-range locks via UnlockAllForSession(sessionID=0) on a handle that a different session already holds. Double block-cache flush is benign but wasteful. Double RemoveFile on a delete-pending file can fail silently or hit a TOCTOU window. The lock double-release is the dangerous path \u2014 could release a lock belonging to a new opener that claimed the file after the first unlock.", "fix": "Add a sync.Map of in-flight handle IDs: before calling cleanupAndDelete, attempt to Store(h.ID, struct{}{}); if key already present, skip (another goroutine owns it). Delete after cleanup completes. Alternatively serialize all cleanup calls through a single channel.", "dimension": "bugs", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: durable_scavenger.go cleanupAndDelete (line 136) has no mutex or in-flight tracking. Called from expireHandles ticker goroutine (line 124) and from HandleConflictingOpen (line 223) and ForceExpireDurableHandle (line 205) \u2014 both exported methods with no sync. Current callers are not yet wired to handler goroutines, so race is latent but the struct has no guard for concurrent calls on same handle."}, {"title": "Nil-deref panic in reconcileContainerPorts: Containers[0] unguarded on re-fetch", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/service_reconciler.go", "line": 513, "severity": "MED", "what": "`fresh.Spec.Template.Spec.Containers[0].Ports = desiredPorts` with no guard on `len(fresh.Spec.Template.Spec.Containers)`. The first fetch at line 440 guards against the empty-containers case, but between that check and the second fetch at line 508 (done for optimistic locking) the StatefulSet could be replaced/recreated with 0 containers, causing an index-out-of-range panic.", "why": "Race window between two separate Get calls. The container-count guard at line 450 is on `statefulSet` (first fetch), not on `fresh` (second fetch at line 508). Any reconcile that recreates the StatefulSet in that window produces a panic.", "fix": "After the re-fetch at line 508, add `if len(fresh.Spec.Template.Spec.Containers) == 0 { return fmt.Errorf(\"re-fetched StatefulSet has no containers\") }` before line 513.", "dimension": "bugs", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. service_reconciler.go:450 guards len(statefulSet.Spec.Template.Spec.Containers)==0 on first fetch. Line 513 indexes fresh.Spec.Template.Spec.Containers[0] on the second fetch (line 508) with no equivalent guard. Race window between the two Get calls is real; a reconcile-triggered StatefulSet replacement with 0 containers causes index-out-of-range panic."}, {"title": "IP ACL bypass: clientAddr includes port, MatchesIPPattern never matches", "file": "/tmp/dittofs-audit/pkg/metadata/auth_permissions.go", "line": 90, "severity": "MED", "what": "CheckShareAccess passes raw clientAddr (format \"IP:port\") directly to MatchesIPPattern at lines 90 and 109. MatchesIPPattern calls net.ParseCIDR(pattern) which succeeds for CIDR patterns, but then net.ParseIP(\"1.2.3.4:12345\") returns nil (port not stripped), so CIDR contains() fails. The exact-match fallback compares \"1.2.3.4:12345\" == \"1.2.3.4\" which is always false. Result: DeniedClients list never fires (banned IPs pass through), AllowedClients list never matches (permitted IPs are rejected).", "why": "MatchesIPPattern expects a bare IP address (auth_identity.go:333 doc says 'clientIP'). The function signature, comment, and test file all use 'clientIP'. But CheckShareAccess receives 'clientAddr' which callers pass as conn.RemoteAddr().String() \u2014 always 'IP:port'. Port is stripped for NFS mount (extractClientIP) and xdr.ExtractClientIP, but CheckShareAccess has no equivalent strip, so the raw ':port' suffix breaks both CIDR and exact matching silently.", "fix": "Strip port before calling MatchesIPPattern. In CheckShareAccess, add: host, _, err := net.SplitHostPort(clientAddr); if err == nil { clientAddr = host }. Apply the same strip before building AuthContext.ClientAddr. Alternatively add port-stripping inside MatchesIPPattern itself since its documented contract is an IP address.", "dimension": "security", "area": "metadata-service-auth", "confirmed": true, "verdict": "Bug confirmed in code. MatchesIPPattern at auth_identity.go:333 expects bare IP; CheckShareAccess at auth_permissions.go:90,109 passes raw clientAddr which would include port. net.ParseIP('IP:port') returns nil, CIDR check fails; exact match 'IP:port'=='IP' also fails. However CheckShareAccess has no callers in production code \u2014 AllowedClients/DeniedClients enforcement is dead code. The IP ACL feature simply never fires (neither deny nor allow). Severity downgraded: bug is real but not reachable in current production paths."}, {"title": "NLM FREE_ALL accepts unauthenticated lock-drop from any remote host", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/dispatch.go", "line": 100, "severity": "MED", "what": "NLMProcFreeAll entry has NeedsAuth:false with comment 'uses AUTH_NULL'. The handler (free_all.go:77-93) accepts any caller_name from any source IP and logs it; the adapter OnClientCrash callback then releases all locks for that caller_name. No source-address restriction (unlike portmap SET/UNSET which gates on IsLocalhost). Any remote attacker can send FREE_ALL with Name=<victim_hostname> and cause all NLM locks held by that host to be released.", "why": "Missing auth/authz: rpc.statd is the only legitimate sender; without a localhost-or-monitored-peer gate any network-adjacent host can forge FREE_ALL name=<active_client> to drop advisory locks, enabling a competing client to seize byte ranges -> silent data corruption. This is the canonical NLM spoofing primitive documented in CVE-2011-2500 class.", "fix": "Add source-IP restriction equivalent to portmap's IsLocalhost check, or verify that the sender's IP matches an SM_MON registration for the claimed name (same pattern as NSM Notify H16 gate in nsm/handlers/notify.go). Simplest: in the FreeAll handler or dispatch wrapper, reject calls whose ctx.ClientAddr is not loopback \u2014 rpc.statd always runs on the same host.", "dimension": "security", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed NeedsAuth:false at dispatch.go:100 \u2014 any remote host can send FREE_ALL. Handler at free_all.go:77-93 decodes, logs, returns void \u2014 it does NOT call OnClientCrash and does NOT release any locks (comment at line 88-90 says 'actual lock release is triggered by the adapter's OnClientCrash' but that call is absent). Auth bypass is real; however claimed impact (lock drop enabling data corruption) is overstated since handler is currently a stub no-op. Downgrade to MED: real auth bypass, but locks not actually released."}, {"title": "Postgres DSN keyword injection via unquoted config values", "file": "/tmp/dittofs-audit/pkg/controlplane/store/gorm.go", "line": 86, "severity": "MED", "what": "DSN() builds a libpq keyword=value string via fmt.Sprintf with no quoting of Host, User, Password, Database, SSLMode, or SSLRootCert. A value containing a space injects additional DSN keywords. For example, a password of `secret sslmode=disable dbname=postgres` silently overrides downstream parameters already set by the caller \u2014 bypassing TLS enforcement or redirecting to a different database.", "why": "libpq keyword=value parsing treats unquoted spaces as parameter separators. The Validate() function only checks non-empty; no character sanitization or quoting is performed. An operator-supplied or env-var-supplied password/host with embedded spaces (or single quotes) can alter the effective connection parameters after the server has already started, violating the SSL posture or reaching an unintended schema.", "fix": "Quote every value in the DSN and escape interior single-quotes and backslashes per libpq rules. Minimal fix: write a helper `func libpqQuote(s string) string` that wraps the value in single quotes and replaces `\\` \u2192 `\\\\` and `'` \u2192 `\\'`, then use it for all six fields:\n\n```go\nfunc libpqQuote(s string) string {\n    s = strings.ReplaceAll(s, `\\`, `\\\\`)\n    s = strings.ReplaceAll(s, `'`, `\\'`)\n    return `'` + s + `'`\n}\n\nfunc (c *PostgresConfig) DSN() string {\n    dsn := fmt.Sprintf(\"host=%s port=%d user=%s password=%s dbname=%s\",\n        libpqQuote(c.Host), c.Port, libpqQuote(c.User),\n        libpqQuote(c.Password), libpqQuote(c.Database))\n    if c.SSLMode != \"\" {\n        dsn += \" sslmode=\" + libpqQuote(c.SSLMode)\n    }\n    if c.SSLRootCert != \"\" {\n        dsn += \" sslrootcert=\" + libpqQuote(c.SSLRootCert)\n    }\n    return dsn\n}\n```\nAlternatively, use the `pgx` URL form (`postgres://user:pass@host/db?sslmode=...`) with `url.QueryEscape` for each component, which has unambiguous encoding semantics.", "dimension": "security", "area": "controlplane-store-models", "confirmed": true, "verdict": "Confirmed. gorm.go:86-96: DSN built via fmt.Sprintf with Host, User, Password, Database, SSLMode, SSLRootCert unquoted. A password containing ' sslmode=disable' or ' dbname=other' injects extra libpq keyword=value pairs, silently overriding TLS posture or target database. Validate() at line 149 only checks non-empty; no character sanitization. Severity lowered: requires config-file/env-var access (operator trust), not REST input. Fix: use pgx/v5 url.URL builder or single-quote-escape each value."}, {"title": "qcow2 SHA256 pin never set \u2014 downloaded binary accepted without integrity check", "file": "/tmp/dittofs-audit/test/e2e/framework/qcow2_fixture.go", "line": 34, "severity": "MED", "what": "qcow2BaseSHA256 is set to the placeholder string \"<freeze-at-first-run>\". DownloadQcow2Base explicitly skips the integrity check when this placeholder is present (lines 77-81, 125-128), logging the actual hash but never failing. Any file served from qcow2BaseURL \u2014 including a MITM-substituted or CDN-rotated image \u2014 will be accepted and stored, then used to synthesize test clones written into the DittoFS share under test.", "why": "The intent documented in the constant comment is that the observed SHA256 would be frozen in a follow-up commit. That commit never happened; the placeholder remains live. During a test run the framework performs an unauthenticated HTTP download (no TLS pinning beyond the CDN certificate) and writes the result to the local filesystem without verifying content. A supply-chain substitution or network attacker could inject arbitrary bytes that execute when the qcow2 is processed.", "fix": "Freeze the known-good SHA256 of the Alpine 3.20.3 cloud image into qcow2BaseSHA256 (the value should have been logged on first run; re-run the test, capture the logged hash, and set qcow2BaseSHA256 = \"<actual hex digest>\"). After freezing, the mismatch branch at line 131-135 will reject any future rotation or MITM.", "dimension": "security", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed: qcow2BaseSHA256==\"<freeze-at-first-run>\" sentinel present at line 34. Lines 77-81 return early with only a log (no fail) when placeholder detected and cache exists; lines 125-128 log but don't Fatalf when fresh download happens. Integrity check is intentionally skipped in placeholder mode. The follow-up freeze commit never happened."}, {"title": "Wrong RPC accept_stat for unknown program: RPCProcUnavail instead of RPCProgUnavail", "file": "/tmp/dittofs-audit/pkg/adapter/nfs/dispatch.go", "line": 206, "severity": "MED", "what": "Unknown-program default branch calls `rpc.MakeErrorReply(call.XID, rpc.RPCProcUnavail)` (value 3 = PROC_UNAVAIL) instead of `rpc.RPCProgUnavail` (value 1 = PROG_UNAVAIL). RFC 5531 \u00a79 mandates PROG_UNAVAIL when the server does not recognise the program number; PROC_UNAVAIL means the program is known but the procedure is not. Sending the wrong accept_stat will confuse NFS clients that check the exact code before retrying via portmap.", "why": "Plausible-but-wrong constant: both `RPCProcUnavail` and `RPCProgUnavail` exist in the rpc package with adjacent values (3 vs 1). Parallel inner/outer dispatch.go files both handle the same default case \u2014 `internal/adapter/nfs/dispatch.go:203` correctly uses `rpc.RPCProgUnavail`, but the outer `pkg/adapter/nfs/dispatch.go:206` copy uses the wrong constant.", "fix": "Change line 206 from `rpc.RPCProcUnavail` to `rpc.RPCProgUnavail`.", "dimension": "slop", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "Confirmed: pkg/adapter/nfs/dispatch.go:206 uses rpc.RPCProcUnavail (value 3, PROC_UNAVAIL) for unknown program default case. internal/adapter/nfs/dispatch.go:203 correctly uses rpc.RPCProgUnavail. RFC 5531 \u00a79 requires PROG_UNAVAIL for unknown program numbers. Two parallel dispatch files, one has wrong constant."}, {"title": "OnRead prefetches already-read hashes instead of upcoming hashes", "file": "/tmp/dittofs-audit/pkg/block/engine/cache.go", "line": 363, "severity": "MED", "what": "allHashes accumulates hashes passed to OnRead (blocks already read). toPrefetch = allHashes[len-want:] selects the most-recently-seen hashes \u2014 the same blocks just read, not the next blocks. Comment says 'schedule depth upcoming hashes' but logic is inverted. After 3 reads of h0/h1/h2 with depth=1, prefetch fires for h2 (last read), not h3 (next block).", "why": "Prefetch never warms truly upcoming blocks. It re-fetches the trailing already-read window, which is either a cache hit (no-op) or redundant re-load of blocks the caller already has. Sequential read performance gain is zero. Prefetch goroutines waste cycles on guaranteed-stale requests.", "fix": "OnRead must receive the full ordered hash sequence for the file (or at least a lookahead window beyond the current read position). Separate the 'already-read window' used for sequential detection from a 'future hashes' lookahead slice populated from the file's block manifest. Alternatively, engine.ReadAt should pass the upcoming BlockRef hashes alongside the just-satisfied ones so the tracker can schedule genuinely future blocks.", "dimension": "slop", "area": "block-engine-gc-syncer", "confirmed": true, "verdict": "Confirmed: cache.go:374 start=len(tr.allHashes)-want then toPrefetch=tr.allHashes[start:] selects last `want` hashes from already-read window. tr.allHashes contains only hashes passed to OnRead (already-read blocks). Prefetch fires for the trailing already-seen blocks, not upcoming ones. Sequential read prefetch provides zero benefit."}, {"title": "UmntAll removes ALL clients' mounts instead of just the requesting client's", "file": "/tmp/dittofs-audit/internal/adapter/nfs/mount/handlers/umountall.go", "line": 51, "severity": "MED", "what": "UmntAll calls h.Registry.RemoveAllMounts() which delegates to mountTracker.RemoveAll() and flushes the entire global mount table \u2014 all clients, all shares.", "why": "RFC 1813 Appendix I UMNTALL removes mounts for the calling client only. RemoveAll() at runtime.go:616 contains no client filter. Every other client's active mount record is silently wiped when any single client sends UMNTALL, corrupting DUMP output and potentially interfering with mount lifecycle tracking.", "fix": "Add a RemoveMountsByClient(clientAddr string) method to the mountRuntime interface and mountTracker, then call it here with extractClientIP(ctx.ClientAddr). RemoveAllMounts() should only be used for server shutdown, not for UMNTALL.", "dimension": "slop", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed. umountall.go:51 calls `h.Registry.RemoveAllMounts()` \u2192 runtime.RemoveAllMounts() \u2192 Tracker.RemoveAll() which wipes entire mount table with no client filter (`mt.mounts = make(map[string]*MountInfo)`). RFC 1813 UMNTALL must remove only the calling client's mounts. Any client sending UMNTALL destroys all other clients' mount records. Severity MED not HIGH: mount table is advisory/tracking only; NFS access itself is not gated on it."}, {"title": "FreeAll stub \u2014 decodes crash client name then discards it; no lock release wired", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/free_all.go", "line": 77, "severity": "MED", "what": "FreeAll decodes req.Name (the crashing client's hostname) and immediately returns without releasing any locks or invoking any crash-cleanup path. The Handler struct (handler.go:44) has fields nlmService and blockingQueue only \u2014 no crash callback reference. The comment at line 88 claims 'actual lock release is triggered by the adapter's OnClientCrash callback' but there is no such wiring: OnClientCrashFunc lives on nsm.Notifier (notifier.go:61), a separate type with no link to NLM Handler.", "why": "When rpc.statd detects a client crash and sends NLM_FREE_ALL, the server must release all locks held by that client. This handler is a log-only stub: crash-recovery via FREE_ALL is entirely non-functional. Stale NLM locks persist indefinitely after a client crash, blocking any future lock attempt on those files by other clients.", "fix": "Add a CrashCleanupFunc field to Handler (or extend NLMLockService with ReleaseClientLocks(ctx, callerName)). In FreeAll, after decoding req.Name, call h.nlmService.ReleaseClientLocks(ctx.Context, req.Name) and h.blockingQueue.RemoveClientWaiters(req.Name) (the queue method already exists at queue.go:174).", "dimension": "slop", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed stub. free_all.go:FreeAll() logs and returns \u2014 no lock release. Comment claims `adapter OnClientCrash` handles it but that path is wired to the NSM notifier (nsm/notifier.go:OnClientCrash via nlm.go:473) for SM_NOTIFY, not NLM_FREE_ALL. The NLM FREE_ALL RPC itself never triggers lock cleanup. However NSM SM_NOTIFY path IS wired and functional, so crash recovery is partially available via rpc.statd's SM_NOTIFY. Severity MED: lock cleanup exists via alternate path, but FREE_ALL specifically is a no-op."}, {"title": "adapter.Adapter interface SetRuntime signature mismatch \u2014 interface unusable", "file": "/tmp/dittofs-audit/pkg/adapter/adapter.go", "line": 60, "severity": "MED", "what": "Adapter interface declares SetRuntime(rt *runtime.Runtime) but every concrete implementation \u2014 BaseAdapter (base.go:179), NFSAdapter (nfs/adapter.go:376), smb.Adapter (smb/adapter.go:160) \u2014 declares SetRuntime(rt any). The signatures are incompatible in Go: a method with parameter type any does not satisfy an interface method with parameter type *runtime.Runtime. No compile-time assertion (var _ adapter.Adapter = ...) exists anywhere, so the mismatch is invisible. Any code that stores a concrete adapter in an adapter.Adapter variable and then calls SetRuntime would fail to compile or would require a type assertion that bypasses the interface.", "why": "Broken interface contract. The service layer routes around this via its own RuntimeSetter interface (adapters/service.go:36-38) that takes any, meaning the intended interface method is completely dead. Callers who type-assert to adapter.Adapter (as suggested in service.go comments at line 25-26) and call SetRuntime would get a compile error, which indicates the interface definition was copied from the pre-refactor signature without updating it to match the any-based design needed to break the import cycle.", "fix": "Change adapter.Adapter.SetRuntime to SetRuntime(rt any) to match all concrete implementations, or remove it from the public interface entirely since the service layer dispatches via RuntimeSetter. Add compile-time assertions: var _ adapter.Adapter = (*nfs.NFSAdapter)(nil) and var _ adapter.Adapter = (*smb.Adapter)(nil) in their respective packages.", "dimension": "slop", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed. adapter.Adapter interface (pkg/adapter/adapter.go:60) declares `SetRuntime(rt *runtime.Runtime)` but all concrete impls (base.go:179, nfs/adapter.go:376, smb/adapter.go:160) declare `SetRuntime(rt any)`. Method signatures incompatible in Go \u2014 concrete types do NOT satisfy the adapter.Adapter interface's SetRuntime method. No compile-time assertion anywhere. Service layer routes around via adapters.RuntimeSetter (takes `any`) making the adapter.Adapter.SetRuntime method entirely dead. Severity MED not HIGH: no callers actually use adapter.Adapter.SetRuntime (service uses RuntimeSetter), so no runtime crash \u2014 just a broken/dead interface contract."}, {"title": "provisionOperatorAccount ignores PasswordSecretRef.Key \u2014 hardcodes \"password\" key", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/auth_reconciler.go", "line": 215, "severity": "MED", "what": "When user sets spec.identity.admin.passwordSecretRef, both provisionOperatorAccount (line 203-215) and cleanupOperatorServiceAccount (line 366-383) capture ref.Name but discard ref.Key. adminPassword is always read as adminSecret.Data[\"password\"] regardless of what key the user specified in PasswordSecretRef.Key. When user's Secret stores the password under any key other than \"password\", the read returns empty string and admin login silently fails with wrong credentials.", "why": "Fabricated/no-op use of a struct field: ref is assigned at line 203 (provisionOperatorAccount) and line 366 (cleanupOperatorServiceAccount) but only ref.Name is used; ref.Key \u2014 the whole point of SecretKeySelector \u2014 is never read. Same dead-pattern at cleanupOperatorServiceAccount line 383. The auto-generated credentials Secret happens to use key \"password\" so the default path works, masking the bug on user-provided paths.", "fix": "Use ref.Key when reading the user-provided secret: adminPassword := string(adminSecret.Data[ref.Key]). Also fix the same pattern at cleanupOperatorServiceAccount line 383. Guard against empty Key with a fallback to \"password\" only for the auto-generated secret path.", "dimension": "slop", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. auth_reconciler.go:203-215: `ref := ds.Spec.Identity.Admin.PasswordSecretRef` captures ref.Name but adminPassword is always `adminSecret.Data[\"password\"]` \u2014 ref.Key is never consulted. User-provided Secret with password under any key other than `\"password\"` silently reads empty string \u2192 login fails. Same pattern in cleanupOperatorServiceAccount:366-383. Severity MED: affects only users who specify non-default key in PasswordSecretRef.Key; default path (key=password) works."}, {"title": "Lease rollback removes wrong entry when baseName includes ADS stream suffix", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/create_post_break.go", "line": 964, "severity": "MED", "what": "When `ErrLeaseKeyInUse` fires after `createAction == types.FileCreated` for an ADS open, the rollback calls `metaSvc.RemoveFile(authCtx, parentHandle, baseName)` where `baseName` is the ADS-qualified name (e.g. `\"file.txt:StreamName\"`) \u2014 that is correct for the stream entry. However, if the base file was auto-created by the ADS creation path (create.go lines 1269-1292) it is NOT rolled back, leaving an orphaned empty base file in the metadata store.", "why": "The auto-create of the base file happens unconditionally if the base did not already exist. On lease rejection the stream entry is removed but the base stays. A subsequent `FILE_CREATE` disposition on the same base name then fails with `ErrAlreadyExists` rather than creating cleanly. The base auto-creation block has no corresponding rollback in the lease-rejection path.", "fix": "In the `ErrLeaseKeyInUse` / `FileCreated` branch, after removing the stream entry, also check if the base file was just auto-created (i.e. `isADS && adsBaseFileName != \"\"`) and remove it if it was not already present before this CREATE (save a `baseCreatedByUs bool` flag in the ADS auto-create block).", "dimension": "bugs", "area": "smb-create-open", "confirmed": true, "verdict": "Confirmed. create.go:1268-1292 auto-creates base file (adsBaseFileName) before the lease request. Rollback at create_post_break.go:964 calls RemoveFile with baseName (the ADS-qualified name e.g. \"file.txt:StreamName\") \u2014 removes only the stream entry. adsBaseFileName is a local in create.go, not in createDraft; base file orphan is unreachable and unremovable by rollback path. Subsequent FILE_CREATE on base name hits ErrAlreadyExists."}, {"title": "Malformed SecurityBuffer silently downgrades to guest session", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 93, "severity": "MED", "what": "parseSessionSetupRequest computes bufferStart = int(secBufferOffset) - 64, clamps to sessionSetupFixedSize if negative, then sets SecurityBuffer=nil (no error) when bufferStart+secBufferLength > len(body) (line 98-100). A client sending a plausible StructureSize=25 but secBufferOffset/secBufferLength combination that places the buffer past the end of body gets req.SecurityBuffer=nil. SessionSetup then hits the 'no recognized auth mechanism' branch at line 357 and calls createGuestSession.", "why": "MS-SMB2 \u00a73.3.5.5 mandates STATUS_INVALID_PARAMETER for a malformed SESSION_SETUP request. Silently granting guest access when GuestEnabled=true lets a client that cannot pass NTLM (e.g. one that lacks valid credentials) deliberately send a malformed buffer to force guest login. The guest check (checkGuestPolicy) only blocks when GuestEnabled=false or SigningConfig.Required is true.", "fix": "After computing bufferStart, validate it: if secBufferLength > 0 and bufferStart+int(secBufferLength) > len(body), return nil + fmt.Errorf(...) from parseSessionSetupRequest (so the caller returns StatusInvalidParameter) instead of silently setting SecurityBuffer=nil.", "dimension": "bugs", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Confirmed. parseSessionSetupRequest at lines 93-100: when bufferStart+secBufferLength > len(body), SecurityBuffer stays nil (no error returned). SessionSetup then falls through all auth branches (line 141: len(nil)==0, line 303: len(nil)<2, line 333: ntlmToken nil) to line 359: createGuestSession(ctx). MS-SMB2 \u00a73.3.5.5 requires STATUS_INVALID_PARAMETER. When GuestEnabled=true, credential-less client can force guest login via deliberately malformed SecurityBufferOffset."}, {"title": "NotifyRename double-charges armed ancestor watchers in cross-directory renames", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/change_notify.go", "line": 1003, "severity": "MED", "what": "`NotifyRename` calls `chargeArmedBuffer` twice when `newParentPath != oldParentPath`: once with `(oldParentPath, {oldFileName, newFileName})` and once with `(newParentPath, {oldFileName, newFileName})`. An armed WatchTree handle whose `WatchPath` is an ancestor of **both** paths matches in both calls; its `BufferedBytes` is incremented by 4 \u00d7 `encodedNotifyEntrySize` entries instead of the correct 2 (one OLD_NAME entry + one NEW_NAME entry). The over-count triggers premature `Overflowed=true` and fires `OnOverflow`, causing the next CHANGE_NOTIFY on that handle to return `STATUS_NOTIFY_ENUM_DIR` when the buffer has not actually overflowed.", "why": "Recursive directory watchers watching a common ancestor of the rename source and destination prematurely report overflow and force the client to re-enumerate the directory. Under repeated cross-directory renames a watcher can get stuck in a permanent overflow loop. Required by smbtorture smb2.notify.tcon and any test exercising WatchTree across rename boundaries.", "fix": "For the second `chargeArmedBuffer` call (newParentPath), pass only the names not already charged in the first call. For a cross-directory rename, the correct charge per ancestor watcher is exactly two entries (relativeOldPath + relativeNewPath). One approach: compute the distinct relative paths for each armed ancestor handle once (across both parent paths) and charge only those, rather than calling `chargeArmedBuffer` twice with the full name list.", "dimension": "bugs", "area": "smb-lease-oplock-notify", "confirmed": true, "verdict": "Confirmed. Armed ancestor watcher (WatchTree=true, ancestor of both paths) is NOT in liveFileIDs skip map. chargeArmedBuffer called at line 1003 with oldParentPath charges 2\u00d7encodedSize; called again at line 1005 with newParentPath, same watcher matches again via pathIsAncestor, charged another 2\u00d7encodedSize = 4\u00d7 total. But only 2 events were buffered (lines 996-999). BufferedBytes over-counted \u2192 premature Overflowed=true."}, {"title": "WRITE handler never updates PositionInfo (CurrentByteOffset), violating MS-FSA \u00a72.1.5.3", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/write.go", "line": 444, "severity": "MED", "what": "After a successful write, `openFile.PositionInfo` is not updated. The equivalent READ path calls `recordReadProgress(openFile, req.Offset, uint64(len(readResult.Data)))` at line 416 in read.go.", "why": "MS-FSA \u00a72.1.5.3 (Server Algorithm for Writing a File) requires CurrentByteOffset to be set to `ByteOffset + BytesWritten` on success. QUERY_INFO FilePositionInformation and FILE_ALL_INFORMATION.PositionInformation (offset 80) will return a stale value after any write. Conformance tests that issue a WRITE then query FilePositionInformation (analogous to smb2.read.position) will fail.", "fix": "After `CommitWrite` succeeds (after line 424), add: `openFile.PositionInfo = req.Offset + uint64(len(req.Data))`. The existing `h.StoreOpenFile(openFile)` call at line 446 will persist the updated value.", "dimension": "bugs", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "Confirmed. write.go has no call to recordReadProgress or any equivalent PositionInfo update after CommitWrite at line 424. read.go has recordReadProgress at lines 416 and 507. WRITE never updates OpenFile.PositionInfo (handler.go:574). QueryInfo FilePositionInformation returns stale value after any write."}, {"title": "TOCTOU race on mode bits in handleSetSparse and handleSetCompression", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/ioctl_sparse.go", "line": 101, "severity": "MED", "what": "Both `handleSetSparse` (ioctl_sparse.go:101-118) and `handleSetCompression` (ioctl_fsctl.go:104-132) implement a read-modify-write on `file.Mode`: `GetFile` \u2192 compute `newMode` \u2192 `SetFileAttributes`. No per-file lock is held across this sequence.", "why": "Two concurrent requests (e.g., SET_SPARSE from one handle and SET_COMPRESSION from another) can both observe the same `file.Mode` base, compute their respective `newMode` independently, then the second `SetFileAttributes` call overwrites the first's bit. `go test -race` will detect this. The `modeDOSSparse` (0x200000) and `modeDOSCompressed` (0x40000) bits are independent; whichever writer completes last silently drops the other's update. Unlike QUERY_DIRECTORY (which holds `openFile.Lock()`), these IOCTL handlers have no serialization.", "fix": "Move the mode bit manipulation into `SetFileAttributes` itself as a bitwise OR/AND-NOT patch (pass a `SetModeBit`/`ClearModeBit` field in `SetAttrs`), so the metadata store applies the change atomically under its own transaction. Alternatively, introduce a per-file-handle mutex around the GetFile\u2192SetFileAttributes sequence for these two IOCTLs, matching the pattern used in SET_INFO BasicInformation (set_info.go:396\u2013632).", "dimension": "bugs", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "Confirmed. handleSetSparse (ioctl_sparse.go:101-118) and handleSetCompression (ioctl_fsctl.go:104-132) both do GetFile \u2192 compute newMode \u2192 SetFileAttributes with no per-file lock. Two concurrent requests observe same base Mode, compute independent newMode, second writer silently drops first's bit. modeDOSSparse (|= 0x200000) and modeDOSCompressed (|= 0x40000) can be lost. No serialization unlike QUERY_DIRECTORY which holds openFile.Lock()."}, {"title": "QueryInfo swallows all metadata/filesystem errors as STATUS_NOT_SUPPORTED", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/query_info.go", "line": 405, "severity": "MED", "what": "After dispatching to `buildFileInfoFromStore`, `buildFilesystemInfo`, or `BuildSecurityDescriptor`, any returned error is unconditionally mapped to `types.StatusNotSupported` (line 406).", "why": "`buildFilesystemInfo` returns errors from `metaSvc.GetFilesystemStatistics` (e.g., backend unavailable \u2192 should be StatusInternalError). `buildFileStreamInformation` can return errors from `common.ResolveForRead`. Mapping real I/O errors to STATUS_NOT_SUPPORTED confuses clients: Windows will retry with a different info class rather than surfacing the real failure, making store errors invisible. Only `types.ErrNotSupported` from an unsupported info class should produce STATUS_NOT_SUPPORTED.", "fix": "Distinguish error types: if `errors.Is(err, types.ErrNotSupported)` return STATUS_NOT_SUPPORTED; otherwise return `common.MapToSMB(err)` or STATUS_INTERNAL_ERROR.", "dimension": "bugs", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "Confirmed. query_info.go:405-406: any error from buildFileInfoFromStore, buildFilesystemInfo, or BuildSecurityDescriptor maps unconditionally to StatusNotSupported. buildFilesystemInfo at lines 1098-1101 and 1139-1142 propagates real GetFilesystemStatistics errors; these reach line 406 and become StatusNotSupported instead of StatusInternalError. Only types.ErrNotSupported at line 1189 should produce StatusNotSupported."}, {"title": "ConfirmClientID CB_NULL goroutine may set CBPathUp on stale record", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 614, "severity": "MED", "what": "Goroutine launched at line 614 re-acquires sm.mu.Lock (line 617) and looks up sm.clientsByID[clientID] to write rec.CBPathUp. For Case-5 re-SETCLIENTID (same clientID), the old ClientRecord pointer stays in clientsByID until the new unconfirmed record is confirmed; the goroutine finds the old pointer and sets CBPathUp on the old record, not the newly confirmed one.", "why": "The new confirmed client record ends up with CBPathUp=false permanently (CB_NULL result never lands on it). Delegations remain disabled for the re-confirmed client even when the callback path is healthy.", "fix": "Capture the ClientRecord pointer before goroutine launch: recordPtr := record. Inside the goroutine, after re-acquiring sm.mu, compare rec == recordPtr; if different, return early. This is the same guard pattern used in lease timer callbacks.", "dimension": "bugs", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "Confirmed. Case-5 path (reuseConfirmedClient): comment at lines 435-436 explicitly says clientsByID[confirmed.ClientID] is NOT overwritten. After ConfirmClientID promotes the unconfirmed record, clientsByID[clientID] still points to the OLD confirmed record. Goroutine at line 618 looks up sm.clientsByID[clientID] \u2192 gets old record \u2192 sets CBPathUp on old record. New confirmed record (in clientsByName via line 592) never gets CBPathUp=true. Delegations permanently disabled for re-confirmed client."}, {"title": "sendRecallV41 goroutine blocks on time.After(30s) with no cancellation path \u2014 leaks on session destroy", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/delegation.go", "line": 541, "severity": "MED", "what": "sendRecallV41 is invoked from goroutines spawned by CheckDelegationConflict and NFSBreakHandler. The select at line 527 has two cases: resultCh receive and time.After(30s). There is no case for BackchannelSender.stopCh or a server context. When BackchannelSender.Stop() is called during Shutdown, if the stop fires during a retry-delay sleep (lines 229\u2013239 in sendCallbackWithRetry) before the request is processed, the sender exits via stopCh without writing to resultCh. The sendRecallV41 goroutine then blocks for the full 30s before the timeout case fires.", "why": "On session destroy or server shutdown, one goroutine per in-flight recall stays alive for up to 30s. Under a delegation-heavy workload with a forced restart, this leaks O(maxDelegations) goroutines for 30s, delaying clean shutdown and potentially exhausting goroutine resources.", "fix": "Add a server-lifetime context to sendRecall and pass it through to sendRecallV41. Replace the time.After select case with context cancellation: case <-ctx.Done(). Alternatively, share BackchannelSender.stopCh as a channel parameter so the waiting goroutine exits immediately when the sender stops.", "dimension": "bugs", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "Confirmed: sendRecallV41 select at lines 527-545 has only resultCh and time.After(30s) cases. BackchannelSender.sendCallbackWithRetry exits via stopCh at line 234 without writing to resultCh. Goroutine blocks full 30s before timeout fires. No context or stopCh case in sendRecallV41's select."}, {"title": "ContextStore.Stop panics on double-call \u2014 no sync.Once guard on channel close", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/context.go", "line": 269, "severity": "MED", "what": "Stop() calls close(cs.stopCh) unconditionally. Closing an already-closed channel panics in Go. No guard prevents double invocation.", "why": "If GSSProcessor.Stop is called twice during server shutdown (e.g., graceful and forced paths both invoke it, or a test re-uses a processor), the server panics. This crashes the entire NFS server process.", "fix": "Add stopOnce sync.Once to ContextStore. Replace Stop body with: cs.stopOnce.Do(func() { close(cs.stopCh); cs.cleanupTicker.Stop() }).", "dimension": "bugs", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed: ContextStore.Stop() at line 268 calls close(cs.stopCh) unconditionally with no sync.Once or closed-check guard. Double-call panics with 'close of closed channel'."}, {"title": "ContextStore.Store capacity enforcement is non-atomic \u2014 maxContexts limit races under concurrent INIT", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/context.go", "line": 208, "severity": "MED", "what": "Store calls Count() (O(n) sync.Map range), then conditionally evictOldest(), then contexts.Store(). These three steps are not atomic. Under concurrent INIT load N goroutines can all observe count < maxContexts, skip eviction, and all store, growing the store to maxContexts + N - 1.", "why": "The maxContexts limit is a resource cap preventing unbounded memory use from unauthenticated INIT floods. Violating it under concurrency allows an attacker to exhaust server memory by sending parallel INIT requests, defeating the intended DoS protection.", "fix": "Track count with atomic.Int64. Atomically increment before contexts.Store; if new count exceeds maxContexts, call evictOldest and decrement post-eviction. Or hold a mutex over the check-evict-store triple.", "dimension": "bugs", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed: Store() at lines 207-215 calls Count() (sync.Map range, O(n)), then evictOldest(), then contexts.Store() with no mutex across the three steps. N concurrent INIT goroutines can all observe count < maxContexts and all store, exceeding the cap by N-1."}, {"title": "Wrong RPC accept_stat for unknown program: RPCProcUnavail (3) instead of RPCProgUnavail (1)", "file": "/tmp/dittofs-audit/pkg/adapter/nfs/dispatch.go", "line": 206, "severity": "MED", "what": "Default case of the program switch uses `rpc.RPCProcUnavail` (3 = PROC_UNAVAIL) when the requested program number is unknown.", "why": "RFC 5531 \u00a79 requires PROG_UNAVAIL (accept_stat=1) when the server does not recognise the program number. PROC_UNAVAIL (3) means 'program known, procedure not implemented'. An RFC-compliant client (e.g. Linux nfsd retransmit logic, mount automount) may retry or fall back differently on PROG_UNAVAIL vs PROC_UNAVAIL \u2014 wrong status can cause hangs or mis-diagnosis.", "fix": "Change `rpc.RPCProcUnavail` to `rpc.RPCProgUnavail` at line 206: `errorReply, err := rpc.MakeErrorReply(call.XID, rpc.RPCProgUnavail)`", "dimension": "bugs", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "Confirmed: dispatch.go line 206 uses rpc.RPCProcUnavail (3) for unknown program number in default case. RFC 5531 requires RPCProgUnavail (1). Wrong accept_stat returned to clients for unrecognised programs."}, {"title": "Syncer.Close DrainAllUploads races stopCh: periodic uploader goroutine may hold uploading gate", "file": "/tmp/dittofs-audit/pkg/block/engine/syncer.go", "line": 938, "severity": "MED", "what": "`Syncer.Close` sets `m.closed=true`, closes `stopCh`, then calls `DrainAllUploads`. `SyncNow` (inside DrainAllUploads) busy-waits on `uploading.CompareAndSwap` for up to 30 s (defaultShutdownTimeout). If the periodic uploader goroutine is in the middle of `syncLocalBlocks` (holding `uploading=true`) when `stopCh` is closed, the goroutine will NOT exit until `syncLocalBlocks` returns \u2014 it only checks `stopCh` in the outer `for { select }` loop, not inside the inner `func() { defer uploading.Store(false); syncLocalBlocks(ctx) }()` closure. `SyncNow`'s CAS-wait loop therefore spins for up to the full timeout before it can acquire the gate and run the final drain.", "why": "Under a slow remote (S3 timeout up to 30 s per chunk), a mirror pass can easily take longer than the 30 s shutdown timeout. `DrainAllUploads` returns `ctx.Err()` (DeadlineExceeded) with un-mirrored data, and the parent `Store.Close` proceeds to tear down the remote store while the periodic uploader is still active and may call `m.remoteStore.Put` after the remote store is closed.", "fix": "Close `stopCh` AFTER the drain completes, not before. Move `close(m.stopCh)` to after `_ = m.DrainAllUploads(ctx)`. Alternatively, pass the `uploading`-gated context into the periodic goroutine so it can be cancelled during the inner mirror pass. At minimum, pass a separate shutdown context (not `context.Background()`) to `DrainAllUploads` so it can be cancelled when the remote store is truly gone.", "dimension": "bugs", "area": "block-engine-core", "confirmed": true, "verdict": "Confirmed: Syncer.Close (lines 938-965) closes stopCh then calls DrainAllUploads (30s ctx). Periodic uploader only checks stopCh in outer loop, not inside syncLocalBlocks goroutine. If syncLocalBlocks is running when stopCh closes, DrainAllUploads spins up to 30s on uploading CAS before acquiring gate. Potential for data-loss on slow/hung remote exceeding timeout."}, {"title": "reapSupersededFileBlocks skips reap entirely when newBlocks is empty \u2014 stale FileBlock rows accumulate", "file": "/tmp/dittofs-audit/pkg/block/engine/engine.go", "line": 414, "severity": "MED", "what": "`reapSupersededFileBlocks` returns immediately when `len(newBlocks) == 0` (third guard on line 414). This is called from `ObjectIDPersister` for both the conflict-retry path (line 300) and the normal path (line 304). A rollup pass that produces zero blocks (e.g., a file truncated to 0 between rollup and persist) skips all reap logic, leaving every prior-generation FileBlock row alive in the per-payload manifest.", "why": "If the file is subsequently re-written, `ListFileBlocks` returns stale rows from the zero-block rollup pass alongside the new rows. `readLocalByHash` may hit an old-generation row whose CAS chunk has been GC'd (RefCount 0 from the prior reap that didn't run), returning `ErrChunkNotFound` and zero-filling bytes that should have valid content. The accumulation also inflates `snapshotPendingBlockRefs` output so the speculative-dedup trigger evaluates against stale blocks.", "fix": "When `newBlocks` is empty but `priorOffsets` is non-empty, reap ALL prior offsets (the entire file was re-chunked to nothing, e.g. after a truncate-to-zero followed by a rollup). Change the guard to: `if bs.coordinator == nil || len(priorOffsets) == 0 { return nil }` and handle the `newBlocks==0` case as 'region covers everything' (reap all priorOffsets unconditionally).", "dimension": "bugs", "area": "block-engine-core", "confirmed": true, "verdict": "Confirmed: reapSupersededFileBlocks at line 414 returns early when len(newBlocks)==0, skipping all prior-generation reap. Zero-block rollup (truncate-to-0 race) leaves stale FileBlock rows alive. Subsequent re-writes may encounter GC'd CAS chunks via stale rows."}, {"title": "SyncQueue.Stop() panics on double-call \u2014 close of closed channel", "file": "/tmp/dittofs-audit/pkg/block/engine/sync_queue.go", "line": 125, "severity": "MED", "what": "Stop() checks `if !q.started` to guard early exit, but `started` is set to true on first Start() and never reset. A second call to Stop() passes the guard (started=true, !started=false) and calls close(q.stopCh) on an already-closed channel, panicking.", "why": "Go panics on close of a closed channel. While the current single caller (Syncer.Close, which is idempotent) only calls Stop once, the API has no contract preventing double-Stop. Any refactor, test, or shutdown race that calls Stop twice will produce a hard panic rather than a no-op. The fix-pattern already used by HealthMonitor.Stop() (sync.Once) is standard Go idiom.", "fix": "Add a sync.Once field to SyncQueue and replace the `close(q.stopCh)` line with: q.stopOnce.Do(func() { close(q.stopCh) }). Remove or keep the `!q.started` guard as a fast-path.", "dimension": "bugs", "area": "block-engine-gc-syncer", "confirmed": true, "verdict": "Confirmed: SyncQueue.Stop() at lines 116-125 checks !q.started but started is set true once and never reset. Second call passes the guard and reaches close(q.stopCh) on an already-closed channel, panicking. No sync.Once guard present."}, {"title": "Close() double-timeout \u2014 graceful shutdown can block up to 60s instead of 30s", "file": "/tmp/dittofs-audit/pkg/block/engine/syncer.go", "line": 956, "severity": "MED", "what": "Close() creates a context with defaultShutdownTimeout (30s) and passes it to DrainAllUploads (line 958). After DrainAllUploads returns (which may have consumed the full 30s), it then calls queue.Stop(defaultShutdownTimeout) (line 962) with a fresh full 30s timeout. Total worst-case shutdown duration is 60s.", "why": "The intent is a single 30s graceful-shutdown window. With two sequential 30s timeouts, a server with a stuck remote store will stall for 60s at process shutdown, delaying pod restarts, rolling updates, and operator-triggered restarts. During the second 30s window the queue workers are already draining against a cancelled workerCtx (stopCh was closed), but slow or hung S3 connections can still pin them.", "fix": "Capture the start time before DrainAllUploads, compute remaining = defaultShutdownTimeout - elapsed, pass max(remaining, 0) to queue.Stop. Alternatively, derive the queue.Stop timeout from the same context: pass ctx.Done() or use the remaining deadline via context.WithDeadline.", "dimension": "bugs", "area": "block-engine-gc-syncer", "confirmed": true, "verdict": "Confirmed: Syncer.Close() at lines 956-962 creates a 30s context for DrainAllUploads, then calls queue.Stop(defaultShutdownTimeout) with a fresh 30s timeout sequentially. Worst-case shutdown is 60s. Intent was a single 30s window."}, {"title": "RegisterStoreForShare publishes store before lock manager is ready", "file": "/tmp/dittofs-audit/pkg/metadata/service.go", "line": 180, "severity": "MED", "what": "At line 180, s.stores[shareName] = store is written under s.mu and the lock is released at line 186. Between lines 186 and 245 (where s.mu is reacquired to publish the lock manager), the store is visible in s.stores but no lock manager exists yet for the share. Any concurrent call to lockManagerForHandle during this window returns ErrStaleHandle even though the store is live.", "why": "A protocol handler racing with RegisterStoreForShare can successfully resolve the store via storeForHandle (line 532) but fail on lockManagerForHandle (line 561) with a stale-handle error, causing spurious NFS ESTALE or SMB STATUS_FILE_CLOSED for lock operations on a share that was just mounted. The two-phase publish (store first, lock manager later) creates an observable inconsistency window.", "fix": "Build and recover the lock manager before publishing the store, then publish both atomically under a single s.mu acquisition. Alternatively, return ErrNotReady (not ErrStaleHandle) from lockManagerForHandle when the store exists but no LM exists yet, so callers can retry rather than surfacing ESTALE.", "dimension": "bugs", "area": "metadata-service-auth", "confirmed": true, "verdict": "Confirmed: RegisterStoreForShare publishes s.stores[shareName]=store at line 180 under s.mu, releases at line 186. Lock manager not published until line 245-246 (second s.mu acquisition). Window exists where storeForHandle succeeds but lockManagerForHandle returns ErrStaleHandle, causing spurious ESTALE/STATUS_FILE_CLOSED for lock ops on a freshly mounted share."}, {"title": "badger backup Restore resets usedBytes to zero after restore but does not reinitialize it", "file": "/tmp/dittofs-audit/pkg/metadata/store/badger/backup.go", "line": 163, "severity": "MED", "what": "Badger Restore streams KV entries into the store via WriteBatch but never reinitializes the in-memory usedBytes atomic counter. After restore the counter stays at whatever value it had before (typically 0 for a fresh store, but could be non-zero if the destination was Reset then Restored).", "why": "GetUsedBytes and FSSTAT report 0 (or stale) after a Badger restore until the server restarts and initUsedBytesCounter runs from NewBadgerMetadataStore. Any quota/monitoring path reading usedBytes during the session after Restore sees wrong data.", "fix": "After the final wb.Flush() and CRC check in Restore, call s.initUsedBytesCounter() to recompute the counter from the newly-restored file rows. Or inline the scan: iterate f: prefix, sum sizes of FileTypeRegular entries, call s.usedBytes.Store(total).", "dimension": "bugs", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "badger/backup.go:163 Restore() streams KV via WriteBatch but never calls initUsedBytesCounter() or resets s.usedBytes atomic. Counter stays at pre-restore value (typically 0 for fresh store). GetUsedBytes/FSSTAT report stale 0 until next server restart."}, {"title": "memory/transaction.go ListChildren cursor restart when cursor name deleted between pages", "file": "/tmp/dittofs-audit/pkg/metadata/store/memory/transaction.go", "line": 407, "severity": "MED", "what": "Cursor resolution (lines 407-414) searches sortedNames for the cursor string. If the cursor entry was deleted between two paginated ListChildren calls, the loop finds no match, startIdx stays 0, and the next page returns entries from the beginning of the directory \u2014 duplicating entries already returned to the client.", "why": "NFS READDIR clients with large directories (multiple pages) may see duplicate directory entries if any file in the directory is deleted between paginated calls. The conformance suite may not catch this because tests rarely interleave deletion with multi-page READDIR.", "fix": "Use a binary search on sortedNames with the cursor as lower bound (return entries where name > cursor, not name == cursor + 1). Replace the exact-match loop with: startIdx = sort.SearchStrings(sortedNames, cursor); if startIdx < len(sortedNames) && sortedNames[startIdx] == cursor { startIdx++ }. This correctly advances past the cursor even when it no longer exists.", "dimension": "bugs", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "memory/transaction.go:407-413 \u2014 cursor not found in sortedNames leaves startIdx=0, so next ListChildren page re-emits entries from directory start. Happens if cursor entry deleted between paginated READDIR calls. Duplicate entries returned as-if-complete to NFS/SMB client."}, {"title": "ListChildren (pool and tx paths) silently swallows rows.Err() \u2014 network errors produce truncated listings", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/files.go", "line": 363, "severity": "MED", "what": "After the for rows.Next() loop in ListChildren (files.go:246 pool path, transaction.go:563 tx path), neither implementation calls rows.Err(). A network error or server-side abort mid-stream will cause rows.Next() to return false early; the function then returns whatever partial entries were accumulated plus an empty cursor, with no error. Callers (NFS READDIR, SMB QueryDirectory) believe the listing is complete.", "why": "NFS clients rely on the cursor/EOF signal to know they have seen all directory entries. A truncated listing returned as complete causes clients to miss files \u2014 POSIX readdir conformance failure. Under long directory scans with Postgres checkpoints or network hiccups this is realistic.", "fix": "After the rows loop, check `if err := rows.Err(); err != nil { return nil, \"\", mapPgError(err, \"ListChildren\", \"\") }` in both files.go and transaction.go ListChildren implementations. Same fix needed in ListShares (shares.go:237, transaction.go:998).", "dimension": "bugs", "area": "metadata-store-postgres", "confirmed": true, "verdict": "postgres/files.go:246 (pool path) and transaction.go:563 (tx path) ListChildren loops end without calling rows.Err(). Mid-stream network error or server abort causes rows.Next() to return false early; partial listing returned with empty cursor as if complete. NFS/SMB clients see missing files."}, {"title": "ListShares (pool and tx paths) missing rows.Err() check", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/shares.go", "line": 237, "severity": "MED", "what": "ListShares in shares.go (pool path) and transaction.go:1006 (tx path) iterate rows.Next() without checking rows.Err() afterward. A mid-stream error returns a partial share list as if complete.", "why": "Runtime.ListShares feeds the control-plane share enumeration; a partial list causes the operator reconciler to think shares are absent and potentially trigger spurious delete/recreate cycles.", "fix": "Add `if err := rows.Err(); err != nil { return nil, err }` after each rows.Next() loop.", "dimension": "bugs", "area": "metadata-store-postgres", "confirmed": true, "verdict": "postgres/shares.go:230 (pool path) and transaction.go:998 (tx path) ListShares loops never call rows.Err(). Partial share list silently returned as complete on mid-stream error. Operator reconciler may treat absent shares as deleted and trigger spurious delete/recreate."}, {"title": "encodeUTF16LE truncates non-BMP codepoints instead of encoding surrogate pairs", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/srvsvc.go", "line": 301, "severity": "MED", "what": "binary.LittleEndian.PutUint16(result[i*2:], uint16(r)) silently truncates any rune > 0xFFFF. Characters such as emoji or CJK extension-B produce a wrong UTF-16 code unit and corrupt the wire string.", "why": "Windows clients receive a malformed UTF-16 share name or domain name string. The NDR unicode string length fields will also be wrong (pre-computed from len(runes)*2 not surrogate-pair-aware byte count), potentially causing a client-side parse fault.", "fix": "Use golang.org/x/text/encoding/unicode or manual surrogate-pair encoding: for r > 0xFFFF, encode as two uint16 surrogate pair values (0xD800 + high10, 0xDC00 + low10) and resize result accordingly.", "dimension": "bugs", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "srvsvc.go:297-304 encodeUTF16LE uses uint16(r) which silently truncates runes > 0xFFFF (emoji, CJK ext-B, etc.) instead of encoding surrogate pairs. Result length pre-computed as len(runes)*2 also wrong for non-BMP chars. Corrupted UTF-16 wire strings to Windows clients."}, {"title": "parseSIDsFromRequest misparses NULL SID pointer entries in NDR conformant array", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/lsarpc.go", "line": 369, "severity": "MED", "what": "After skipping the SID-pointer array, the loop unconditionally reads 4 bytes as NDR SubAuthorityCount and then sidSize more bytes as SID binary for every entry in numSids. NDR pointer arrays include NULL pointers (4-byte zero referent) for absent entries; those entries have no following SID data. If any SID pointer slot is NULL, all subsequent SIDs are parsed at the wrong offset.", "why": "A Windows client that sends a LsarLookupSids2 request with a sparse SID array (some NULL pointers) will receive misresolved or empty name mappings for all SIDs after the first NULL. The pointer array is already skipped in bulk (ptrArrayEnd calculation), so the NULL/non-NULL distinction is lost and subsequent SID data alignment is wrong.", "fix": "Before jumping to SID data, scan each pointer slot: if pointer is 0 (NULL), skip that slot's SID data (no SubAuthorityCount+SID follows for a NULL pointer referent). Track non-NULL count separately and only loop over those when reading the deferred SID bodies.", "dimension": "bugs", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "lsarpc.go:369-387 parseSIDsFromRequest iterates numSids unconditionally reading SubAuthorityCount+SID data. NDR pointer arrays can contain NULL 4-byte referents for absent entries with no following SID blob. Null entries not checked; all subsequent SIDs parsed at wrong offset, misresolving names for all SIDs past the first NULL."}, {"title": "BACKCHANNEL_CTL handler unconditionally dereferences v41ctx without nil guard \u2014 inconsistent with all other v4.1 handlers requiring session context", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/v41/handlers/backchannel_ctl.go", "line": 39, "severity": "MED", "what": "HandleBackchannelCtl dereferences v41ctx.SessionID at line 39 without first checking v41ctx != nil. FREE_STATEID (free_stateid.go:40) and RECLAIM_COMPLETE (reclaim_complete.go:39) both guard with if v41ctx == nil { return NFS4ERR_OP_NOT_IN_SESSION } before use. BACKCHANNEL_CTL is not a session-exempt op so the current dispatcher always passes non-nil, but the absence of the guard makes this a latent nil-panic if dispatch logic changes or an op is misdirected.", "why": "Inconsistency with every other handler that needs v41ctx: all others defensively check. If BACKCHANNEL_CTL is ever invoked via a path that does not guarantee a SEQUENCE has run (e.g., future ops table reorganization, direct call in tests), line 39 will panic with a nil pointer dereference.", "fix": "Add the same guard at the top of the function body, after the Decode check: if v41ctx == nil { return &types.CompoundResult{Status: types.NFS4ERR_OP_NOT_IN_SESSION, OpCode: types.OP_BACKCHANNEL_CTL, Data: EncodeStatusOnly(types.NFS4ERR_OP_NOT_IN_SESSION)} }", "dimension": "bugs", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "backchannel_ctl.go:39 dereferences v41ctx.SessionID with no nil guard. FREE_STATEID (free_stateid.go:40) and RECLAIM_COMPLETE (reclaim_complete.go:39) both guard `if v41ctx == nil { return NFS4ERR_OP_NOT_IN_SESSION }` before touching v41ctx. BACKCHANNEL_CTL skips that guard entirely."}, {"title": "GetCachedAuthContext: ShareReadOnly cached indefinitely \u2014 share mode change not reflected", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/doc.go", "line": 95, "severity": "MED", "what": "`ShareReadOnly` is stored in the `authCache` sync.Map keyed by (share, flavor, uid, gid, gids). There is no TTL or invalidation. If a share is changed from read-write to read-only at runtime (via API), the cached `ShareReadOnly=false` entries remain and the write-permission gate in `authCtx.ShareReadOnly` is never re-evaluated for existing connections.", "why": "The cache never expires and there is no hook from the share-config mutator to clear entries. A user whose session was established before the share went read-only continues to have write access until the process restarts. This is a security invariant violation.", "fix": "Either (a) do not cache `ShareReadOnly` \u2014 re-derive it on every request by calling `auth.ResolveSharePermission` while still caching the identity-mapped Identity (the expensive part), or (b) add a short TTL (e.g. 30s) to the cache by storing a timestamp alongside the entry and re-checking when stale.", "dimension": "bugs", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "doc.go:95-99 stores ShareReadOnly in authCache sync.Map with no TTL or invalidation. Share mutator has no hook to clear entries. A client whose authCache entry was populated before a share goes read-only retains write-permission until process restart."}, {"title": "UMNTALL removes ALL clients' mount records, not just the calling client's", "file": "/tmp/dittofs-audit/internal/adapter/nfs/mount/handlers/umountall.go", "line": 51, "severity": "MED", "what": "UmntAll() calls h.Registry.RemoveAllMounts() which invokes Tracker.RemoveAll(), deleting every mount entry across all clients. RFC 1813 Appendix I \u00a75.4 says UMNTALL removes all mount entries for the calling client only. The clientIP is extracted at line 48 but never passed to the removal call.", "why": "Any client calling UMNTALL will silently unmount all other clients' recorded sessions. The DUMP response will then show zero active mounts even though other clients are still using the server. While NFS mount-table tracking is advisory, this causes incorrect showmount output and can interfere with per-client logic that keys on mount state.", "fix": "Replace h.Registry.RemoveAllMounts() with h.Registry.RemoveMount(clientIP) in a loop, or add a RemoveMountsByClient(clientIP) method to the runtime interface that filters by client address. The Tracker already has RemoveAllByProtocol; a RemoveAllByClient(clientAddr string) following the same pattern would be clean.", "dimension": "bugs", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "umountall.go:51 calls h.Registry.RemoveAllMounts() with no clientIP argument \u2014 removes every client's mounts. clientIP is extracted at line 46 but only used for logging. RFC 1813 \u00a7I.5.4 scopes UMNTALL to the calling client only."}, {"title": "NSM admitPeerState int32 monotonicity gate rejects legitimate reboots when peer state wraps", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nsm/handlers/handler.go", "line": 213, "severity": "MED", "what": "admitPeerState stores and compares int32 NSM state numbers with a strict greater-than check. NSM state numbers are unsigned in practice and can wrap: after 2^31 - 1 increments, the int32 representation goes negative. A reboot that produces a new state of e.g. 1 (after many reboots) will compare as 1 <= large_stored_value and be dropped silently, meaning crash recovery (lock release + reclaim) is never triggered for that host.", "why": "NSM spec (RFC 1813 \u00a7B.2.2) states state is int32 on the wire but semantically an ever-increasing counter. Linux rpc.statd uses uint32 internally and wraps around. After enough reboots the gate becomes permanently closed for the host until the server also restarts. Stale locks from a wrapped-state host persist indefinitely.", "fix": "Use sequence-number comparison: treat the state numbers as uint32 and compare using the half-open window trick (RFC 1982 serial number arithmetic): admit if (uint32(incoming) - uint32(stored)) & 0x80000000 == 0 && incoming != stored. Alternatively store as uint32 in peerState map.", "dimension": "bugs", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "handler.go:213 stores and compares int32 NSM state with strict `incoming <= stored`. After int32 wraps negative, any subsequent small positive state (e.g. 1 after reboot) compares <= large stored value and is silently dropped, blocking crash-recovery lock release."}, {"title": "getDurableStore: bare type assertion panics instead of skipping", "file": "/tmp/dittofs-audit/pkg/metadata/storetest/durable_handles.go", "line": 101, "severity": "MED", "what": "Bare `store.(DurableHandleStoreProvider)` with no ok-idiom", "why": "Panics if any caller invokes getDurableStore directly against a store that doesn't implement DurableHandleStoreProvider. RunDurableHandleStoreTests guards via ok-check at line 28, but getDurableStore is a package-level helper callable from any test. A panic aborts the whole test binary rather than skipping the subtest.", "fix": "Use two-value form: `provider, ok := store.(DurableHandleStoreProvider); if !ok { t.Skip(\"store does not implement DurableHandleStoreProvider\"); return nil }; return provider.DurableHandleStore()`", "dimension": "bugs", "area": "metadata-storetest-conformance", "confirmed": true, "verdict": "durable_handles.go:101 uses bare `store.(DurableHandleStoreProvider)` without ok-idiom. RunDurableHandleStoreTests guards at line 28 but getDurableStore is exported at package level; any test calling it directly against a non-implementing store panics instead of skipping, aborting the whole test binary."}, {"title": "pruneEmptyDirs skips pages 2+ of bin subdirectories", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/trash/service.go", "line": 439, "severity": "MED", "what": "pruneEmptyDirs calls svc.ReadDirectory(ctx, dirHandle, 0, 0) once and iterates only that single page. No loop on page.HasMore / page.NextCookie. Compare walkBin (line 144\u2013184) which correctly loops.", "why": "After Empty purges all recycled roots, pruneEmptyDirs sweeps the now-empty intermediary path-component dirs (e.g. 'a/b/' left behind by 'a/b/c.txt'). If the bin root holds >1 ReadDirectory page of child dirs, pages 2+ are silently skipped. Those dirs remain non-empty from the store's view, causing OnDisable's subsequent RemoveDirectory on the #recycle root to fail with ErrNotEmpty \u2014 the bin cannot be fully disabled. Even without OnDisable, the stale dirs accumulate and are never reclaimed.", "fix": "Mirror the walkBin loop pattern: wrap the ReadDirectory call and the for-range in a for-loop that breaks when !page.HasMore and advances cookie = page.NextCookie each iteration. Since each pass removes entries that may shift subsequent pages, always restart from cookie=0 after any RemoveDirectory succeeds (same restart-from-zero pattern used in purgeChildren line 531).", "dimension": "bugs", "area": "controlplane-snapshot-gc", "confirmed": true, "verdict": "trash/service.go:439 pruneEmptyDirs calls ReadDirectory once with no HasMore/NextCookie loop. walkBin at line 144 correctly loops. Bins with >1 page of child dirs leave pages 2+ unswept, preventing full bin disable and accumulating stale dirs."}, {"title": "UpdateSnapshotState TOCTOU: RowsAffected=0 disambiguated by a separate COUNT that races with concurrent deletes", "file": "/tmp/dittofs-audit/pkg/controlplane/store/snapshots.go", "line": 122, "severity": "MED", "what": "When the conditional UPDATE matches 0 rows, UpdateSnapshotState fires a second SELECT COUNT to decide between ErrSnapshotNotFound and ErrSnapshotStateConflict. Between the UPDATE and the COUNT another goroutine (e.g. DeleteSnapshot called by a concurrent rollback) can delete the row. The COUNT then returns 0, returning ErrSnapshotNotFound to the caller even though the correct answer was ErrSnapshotStateConflict. MarkSnapshotReady has the same pattern at line 222.", "why": "The runtime CreateSnapshot retry path and the REST 409 mapping both branch on errors.Is(err, ErrSnapshotStateConflict). A spurious ErrSnapshotNotFound causes the retry to re-create a snapshot row rather than surfacing a conflict, potentially producing duplicate snapshot metadata or a skipped retry.", "fix": "Encode the disambiguation in the single UPDATE by using a RETURNING clause (Postgres) or a separate query inside the same transaction. Alternatively, accept the ambiguity and return ErrSnapshotStateConflict for all 0-rows cases (the caller can re-fetch to distinguish), since that is the safer failure mode.", "dimension": "bugs", "area": "controlplane-store-models", "confirmed": true, "verdict": "snapshots.go:122-131 and :222-230 both use UPDATE then COUNT to distinguish ErrSnapshotNotFound from ErrSnapshotStateConflict. Concurrent DeleteSnapshot between the two queries returns ErrSnapshotNotFound when ErrSnapshotStateConflict was correct, causing the retry path to re-create a snapshot row."}, {"title": "DeleteNetgroup / GetSharesByNetgroup: UUID substring LIKE search produces false positives", "file": "/tmp/dittofs-audit/pkg/controlplane/store/netgroups.go", "line": 58, "severity": "MED", "what": "Both DeleteNetgroup (line 58) and GetSharesByNetgroup (line 137) search for netgroup references using: WHERE config LIKE '%<netgroupID>%'. The netgroup UUID can appear as a substring in an unrelated JSON field value (e.g. a share name, another UUID that happens to share a prefix, or a stored path). This causes ErrNetgroupInUse to fire for netgroups that are not actually in use, making them permanently undeletable.", "why": "JSON substring matching without structural awareness is unreliable. The netgroup_id field is nested inside the config blob; the check must match the exact JSON key-value pair, not a bare substring.", "fix": "Use a JSON path query: for SQLite, json_extract(config, '$.netgroup_id') = ? ; for PostgreSQL, config::jsonb->>'netgroup_id' = ?. This is structurally precise and eliminates false positives.", "dimension": "bugs", "area": "controlplane-store-models", "confirmed": true, "verdict": "netgroups.go:59 and :137 both use `config LIKE '%'+netgroup.ID+'%'`. A UUID can appear as a substring in unrelated JSON fields, triggering false ErrNetgroupInUse and making legitimately unused netgroups permanently undeletable."}, {"title": "CreateUser and UpdateUser silently ignore SharePerms and Groups request fields", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/users.go", "line": 112, "severity": "MED", "what": "CreateUserRequest.SharePerms (line 41) is accepted in the JSON body but never passed to CreateUserWithGroups or any other call \u2014 silently dropped. UpdateUserRequest.Groups (line 50) and SharePerms (line 52) are declared fields that are never read inside the Update handler (lines 205-229). Callers who specify these fields get a 201/200 with no error but the operation is a no-op.", "why": "Dead request fields create a contract lie: the API schema advertises functionality that is not implemented. Clients that rely on setting share permissions at user creation time will observe silent data loss.", "fix": "For Create: apply req.SharePerms after CreateUserWithGroups returns, calling SetUserSharePermission for each entry. For Update: apply req.Groups via a store call (e.g. UpdateUserGroups) and req.SharePerms via SetUserSharePermission. If intentionally deferred, remove the fields from the request structs and document the omission.", "dimension": "bugs", "area": "controlplane-api-rest", "confirmed": true, "verdict": "users.go Create handler (line 112) passes req.Groups to CreateUserWithGroups but req.SharePerms is never used. Update handler (lines 205-232) reads Email/DisplayName/Role/Enabled/UID but never reads req.Groups or req.SharePerms \u2014 both fields silently dropped with 200/201."}, {"title": "BlockStoreStatsHandler.Stats and Evict leak raw internal error strings in response body", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/blockstore.go", "line": 43, "severity": "MED", "what": "Stats handler calls NotFound(w, err.Error()) at line 43 and Evict handler calls BadRequest(w, err.Error()) at line 75. Both inline the raw error message \u2014 which can contain filesystem paths, share-name internals, or store-layer details \u2014 into the HTTP response body visible to API clients.", "why": "All other error paths in adjacent handlers (block_gc.go, blockstore_audit.go) explicitly strip err.Error() from responses and log at Debug. These two handlers are inconsistent: an unauthenticated attacker cannot reach them (admin-gated), but any admin-level API consumer or operator tool will see unexpectedly detailed internal error strings, which is an information-disclosure inconsistency and violates the established sanitization pattern.", "fix": "Replace NotFound(w, err.Error()) with logger.Debug(..., err) + NotFound(w, \"share not found\") or an appropriate static message. Replace BadRequest(w, err.Error()) with logger.Debug(..., err) + BadRequest(w, \"eviction failed\").", "dimension": "bugs", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. blockstore.go:43 calls NotFound(w, err.Error()) and line 75 calls BadRequest(w, err.Error()), inlining raw error strings. Adjacent block_gc.go uses static messages ('GC failed', 'share not found: '+name) \u2014 never raw err.Error(). Pattern inconsistency confirmed from source."}, {"title": "Settings.Get maps all store errors to 404, hiding DB failures", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/settings.go", "line": 61, "severity": "MED", "what": "GetSetting error handling at line 61-63 maps every error \u2014 including connectivity failures, context cancellations, and internal store errors \u2014 to HTTP 404 Not Found. A transient DB outage returns 404 to the caller, indistinguishable from a genuinely missing key.", "why": "Callers (operator tools, the operator controller) cannot distinguish 'key does not exist' from 'store is down'. This causes operators to incorrectly conclude settings are missing and may trigger destructive re-creation actions. All other resource handlers (users, shares, adapters) distinguish ErrNotFound from other errors with appropriate 500 fallback.", "fix": "Check errors.Is(err, models.ErrSettingNotFound) for 404; fall through to InternalServerError for anything else, matching the pattern used in every other GET handler.", "dimension": "bugs", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. settings.go:60-63: all GetSetting errors unconditionally return 404 with static 'Setting not found'. Cannot distinguish genuinely missing key from transient DB failure/context cancel. Other handlers (block_gc.go, shares.go) use errors.Is checks and 500 fallback. Semantic ambiguity real; static message means no raw-error leak but the 404-for-all-errors bug stands."}, {"title": "ResetPassword: non-atomic UpdatePassword + UpdateUser leaves MustChangePassword inconsistent on failure", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/users.go", "line": 310, "severity": "MED", "what": "ResetPassword calls UpdatePassword (line 300) then UpdateUser (line 310) to set MustChangePassword. If UpdatePassword succeeds but UpdateUser fails, the password is reset but MustChangePassword stays at its prior value. For an admin user this means MustChangePassword stays true (or false if it was already false), which is incorrect.", "why": "Same two-step non-atomic write pattern as ChangeOwnPassword. Admin-only path, so blast radius is smaller, but an admin resetting another admin's password via the API can leave the target account in an inconsistent state silently (the 500 response does not indicate which step failed).", "fix": "Combine both writes into a single atomic store operation or transaction. At minimum log which step failed to allow operator recovery.", "dimension": "bugs", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. users.go:300-313: UpdatePassword then UpdateUser \u2014 two separate store calls, no transaction. If UpdatePassword succeeds and UpdateUser fails, MustChangePassword stays at prior value. Non-atomic two-step write confirmed from source."}, {"title": "wireIdentityResolver registers duplicate OnIdentityMappingChange subscription on Kerberos provider re-injection", "file": "/tmp/dittofs-audit/pkg/adapter/smb/adapter.go", "line": 504, "severity": "MED", "what": "wireIdentityResolver unconditionally appends a new unsub to shareUnsubscribers and overwrites handler.IdentityResolver each time it is called when both KerberosProvider and runtime are non-nil. SetKerberosProvider calls it at line 485; if SetRuntime was already called first (both non-nil at that point), a first subscription is registered. A subsequent SetKerberosProvider call (provider replacement / settings reload) registers a SECOND subscription for a new resolver while the OLD resolver's unsub remains in shareUnsubscribers.", "why": "Two OnIdentityMappingChange callbacks are active: the live one (new resolver) and the stale one (old resolver). Every identity-mapping change event fires InvalidateCache on the discarded resolver unnecessarily. More critically, on Stop() both unsubs are called, but if stop/restart cycles accumulate unmatched calls without Stop in between, the subscriber list in the runtime grows unboundedly.", "fix": "Track whether a subscription was already registered (e.g. a wireIdentityUnsub field on the Adapter). In wireIdentityResolver, call the old unsub before appending a new one: `if s.wireIdentityUnsub != nil { s.wireIdentityUnsub() }; s.wireIdentityUnsub = rt.OnIdentityMappingChange(...)`. Do not also append to shareUnsubscribers \u2014 manage it separately so Stop still unsubscribes.", "dimension": "bugs", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed. wireIdentityResolver (line 497-505) always appends a new unsub to shareUnsubscribers without checking for existing subscription. SetRuntime (line 263) and SetKerberosProvider (line 485) both call it. When both are non-nil at SetKerberosProvider call, second subscription appended while first still in slice, leaving duplicate callbacks and double unsub on Stop."}, {"title": "BaseAdapter.Stop ctx-cancellation path skips forceCloseConnections unlike gracefulShutdown timeout path", "file": "/tmp/dittofs-audit/pkg/adapter/base.go", "line": 512, "severity": "MED", "what": "When Stop(ctx) is called and ctx is cancelled before all connections drain, the function returns ctx.Err() immediately (line 516) without calling forceCloseConnections(). The goroutine launched at line 501 (waiting on activeConns.Wait()) is abandoned. In contrast, gracefulShutdown() calls forceCloseConnections() on timeout. initiateShutdown() sets a 100ms read deadline, so connections drain quickly in practice, but if a connection is mid-write or blocked on writeMu the goroutine may outlive the Stop call.", "why": "Callers that pass a context with a short deadline (e.g. the outer DittoServer shutdown orchestrator) expect Stop to clean up after returning. The goroutine leak is bounded (initiateShutdown's 100ms deadline bounds drain), but remaining connections are not force-closed, which is asymmetric with the nil-ctx/gracefulShutdown path and means the goroutine silently outlives Stop.", "fix": "In the ctx.Done() branch of Stop, call forceCloseConnections() before returning, mirroring gracefulShutdown(). The leaked goroutine should also be given a way to exit: either use the same done channel with a post-forceClose drain, or accept that the goroutine finishes after the forced close (bounded, safe).", "dimension": "bugs", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed. base.go Stop(): ctx.Done path (line 512-516) returns ctx.Err() immediately without calling forceCloseConnections(). gracefulShutdown() (line 437) does call forceCloseConnections() on timeout. Asymmetry confirmed. Background goroutine waiting on activeConns.Wait() is abandoned on ctx cancellation path."}, {"title": "generateRandomSecret: modular bias on JWT secret bytes", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/dittoserver_controller.go", "line": 712, "severity": "MED", "what": "`chars[int(randomBytes[i])%len(chars)]`: `len(chars)` is 62. Since 256 is not divisible by 62 (256 % 62 = 6), the first 6 characters in `chars` get marginally higher probability (~1.6% extra) than the rest. This affects the JWT signing secret auto-generated when no user-provided secret is configured.", "why": "Modulo bias is a well-known weakness in CSPRNG sampling. For a JWT secret this is a security concern even if exploitation is impractical.", "fix": "Use rejection sampling: read enough bytes, reject any byte \u2265 `(256/len(chars))*len(chars)`, take the rest modulo `len(chars)`. Or use `math/rand/v2.IntN` with a CSPRNG source, or use `base64.URLEncoding` like `generateRandomPassword` already does.", "dimension": "bugs", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. dittoserver_controller.go:712: chars[int(randomBytes[i])%len(chars)] with len(chars)=62. 256%62=6, so first 6 characters get ~1.6% higher selection probability. Modulo bias on JWT secret generation confirmed."}, {"title": "retryOnConflict: busy-wait sleep blocks reconcile goroutine", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/dittoserver_controller.go", "line": 79, "severity": "MED", "what": "`time.Sleep(time.Duration(i+1) * retryBackoffBase)` inside `retryOnConflict` blocks the calling goroutine. This is called from `reconcileStatefulSet` and `createOrUpdateService` which are on the controller's reconcile path. With maxRetries=3 and base=100ms, worst-case blocks for 600ms holding the reconcile worker thread.", "why": "controller-runtime reconcilers should not hold goroutines with blocking sleeps; the correct pattern is to return `ctrl.Result{RequeueAfter: ...}` so the worker is free for other work. Under load (many DittoServer CRs) this reduces controller throughput.", "fix": "Remove the `time.Sleep`; on conflict return an error that is caught by the caller and converted to a `ctrl.Result{RequeueAfter: retryBackoffBase}` requeue, or use context-aware `time.After` inside a select. Alternatively, keep it for the short CreateOrUpdate path but cap at 1 retry.", "dimension": "bugs", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. dittoserver_controller.go:79: time.Sleep(time.Duration(i+1)*retryBackoffBase) inside retryOnConflict, maxRetries=3, retryBackoffBase=100ms, worst-case 600ms blocking sleep on reconcile goroutine. Called from reconcileStatefulSet and createOrUpdateService on reconcile hot path."}, {"title": "setAuthCondition: status update on stale in-memory object after setRetryCount may conflict", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/auth_reconciler.go", "line": 427, "severity": "MED", "what": "`setAuthCondition` does a fresh Get + `r.Status().Update(ctx, latest)`. Before it is called, `setRetryCount` (line 162 or 181) also does a fresh Get + `r.Update(ctx, latest)` \u2014 a metadata update that bumps `resourceVersion`. The `setAuthCondition` re-fetch is independent so it sees the post-setRetryCount resourceVersion, which is fine. However, `reconcileAuth` is called without holding any lock, and both `setRetryCount` and `setAuthCondition` touch the same CR object in rapid succession. If another reconcile fires concurrently (e.g., from a watch event), the second `r.Status().Update` in `setAuthCondition` will get a 409 conflict and silently return without updating the condition (the error is only logged, never propagated).", "why": "Line 427: `if err := r.Status().Update(ctx, latest); err != nil { logger.Error(err, ...) }` \u2014 conflict errors are swallowed. The `Authenticated` condition may remain stale, causing `updateReadyCondition` to compute a wrong `Ready` value, blocking production workloads that gate on `Ready=True`.", "fix": "Wrap `setAuthCondition`'s status update in a retry loop (similar to `retryOnConflict`), re-fetching on 409 conflict. Or merge both the retry-count annotation update and condition update into a single re-fetch+update cycle.", "dimension": "bugs", "area": "k8s-operator", "confirmed": true, "verdict": "Confirmed. auth_reconciler.go:427: r.Status().Update error only logged, never propagated. setRetryCount and setAuthCondition both do independent Get+Update on same CR in rapid succession without coordination; conflict errors on setAuthCondition silently swallowed, leaving Authenticated condition stale."}, {"title": "CopyPayload: Ctime not updated on dst file after content change", "file": "/tmp/dittofs-audit/internal/adapter/common/copy_payload.go", "line": 77, "severity": "MED", "what": "CopyPayload updates dstFile.Mtime but never sets dstFile.Ctime = time.Now(). After the txn commits, dst's ctime reflects its original creation time, not the copy operation.", "why": "POSIX: ctime must advance on any metadata or content change. Clients (NFS, SMB) cache file identity on (mtime, ctime, size) triplet. A stale ctime means clients observing the file before and after the copy see identical ctimes and may serve stale cached data. metadata/io.go WriteFile explicitly sets Ctime (io.go:230, 315, 429); CopyPayload bypasses that path and writes directly via tx.PutFile.", "fix": "Add `dstFile.Ctime = dstFile.Mtime` (or `time.Now()`) immediately after setting dstFile.Mtime at line 77, before the tx.PutFile call.", "dimension": "bugs", "area": "internal-adapter-common", "confirmed": true, "verdict": "Confirmed. copy_payload.go:77: dstFile.Mtime set to time.Now() but dstFile.Ctime never updated. tx.PutFile commits with stale ctime. metadata/io.go WriteFile explicitly sets both Mtime and Ctime. Direct tx.PutFile bypass omits Ctime update."}, {"title": "followLogs goroutine and signal-handler leak \u2014 signal.Stop never called", "file": "/tmp/dittofs-audit/cmd/dfs/commands/logs.go", "line": 175, "severity": "MED", "what": "`signal.Notify(sigCh, syscall.SIGINT, syscall.SIGTERM)` registers `sigCh` but `signal.Stop(sigCh)` is never called before the function returns. The goroutine at line 177 blocks on `<-sigCh` forever after `ctx` is cancelled via the watcher-error path (line 209) or the watcher-closed path \u2014 the goroutine has no exit path other than receiving a signal.", "why": "On normal watcher-error return the goroutine leaks. The signal registration also leaks: subsequent calls to `dfs logs -f` (e.g. in scripts that restart the command) accumulate signal registrations. On Go's signal delivery model this is harmless for process lifetime but is a correctness/resource bug.", "fix": "Call `signal.Stop(sigCh)` before returning in all paths, and close `sigCh` or use a `context`-aware pattern. Simplest: replace the manual goroutine+sigCh with `signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)` which handles cleanup automatically, then `defer cancel()`.", "dimension": "bugs", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Confirmed. logs.go:174-180: signal.Notify registers sigCh; goroutine blocks on <-sigCh. On watcher-error return (line 209) or watcher-closed return, function exits but goroutine has no exit path besides receiving a signal \u2014 it leaks. signal.Stop(sigCh) never called anywhere in the function."}, {"title": "followLogs silently drops partial log lines (no trailing newline)", "file": "/tmp/dittofs-audit/cmd/dfs/commands/logs.go", "line": 197, "severity": "MED", "what": "`reader.ReadString('\\n')` returns `(partialData, io.EOF)` when a write event delivers a line fragment that has no trailing newline yet. The inner loop `break`s on any error including `io.EOF` without printing `line`. The partial content is discarded. On the next write event, the `bufio.Reader` position is past the partial data so it is never re-read.", "why": "Log lines written in two flushes (common for structured JSON loggers that write fields then newline separately, or for large lines) are silently dropped in `dfs logs -f`. Operators tailing logs during an incident would miss entries.", "fix": "Print the partial line before breaking: `if line != \"\" { fmt.Print(line) }` before the `break`. This is the standard `tail -f` pattern. Alternatively buffer across iterations using a `strings.Builder`.", "dimension": "bugs", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Confirmed. logs.go:196-202: inner loop calls ReadString('\\n'); on io.EOF with partial data, err != nil so break fires before fmt.Print(line). Partial line silently dropped. bufio.Reader position advanced past partial data so it's never re-read on next write event."}, {"title": "CleanupBucket silently leaves objects when bucket has >1000 items", "file": "/tmp/dittofs-audit/test/e2e/framework/containers.go", "line": 223, "severity": "MED", "what": "CleanupBucket calls ListObjectsV2 once (no continuation loop) and deletes only the first page of up to 1000 objects. It then calls DeleteBucket. If the bucket contains >1000 objects, DeleteBucket fails with BucketNotEmpty and the error is silently swallowed (_ = lh.Client.DeleteBucket(...)). The bucket and all remaining objects are leaked. ListS3PrefixWithSizes at line 174 correctly paginates; CleanupBucket does not.", "why": "S3 ListObjectsV2 returns at most 1000 keys per call. Tests that create many small chunks (dedup, vmfleet, store-matrix) can easily exceed 1000 objects, causing silent bucket leaks that accumulate across CI runs and eventually cause Localstack resource exhaustion.", "fix": "Replace the single ListObjectsV2 call with the same pagination loop used in ListS3PrefixWithSizes, collecting all keys before deleting them. Alternatively call lh.ListS3PrefixWithSizes and delete in batches (S3 DeleteObjects accepts up to 1000 keys per call).", "dimension": "bugs", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. CleanupBucket (line 223) does single ListObjectsV2 with no ContinuationToken loop, deletes only first <=1000 objects, then calls DeleteBucket with error silently swallowed via _, _. ListS3PrefixWithSizes at line 174 correctly paginates; CleanupBucket does not. Silent leak for >1000-object buckets."}, {"title": "uint32 wraparound in verifier-length skip lets malicious callback server bypass RPC acceptStat check", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/callback_common.go", "line": 299, "severity": "MED", "what": "ValidateCBReply reads verfLen (uint32) from the wire and computes paddedLen = int64((verfLen + 3) &^ 3). If verfLen = 0xFFFFFFFD the uint32 addition overflows to 0x00000000, paddedLen = 0, and reader.Seek(0, SeekCurrent) is a no-op. The verifier body bytes remain in the reader and are then parsed as acceptStat.", "why": "A malicious NFS client running a fake callback server (reachable via the SSRF path above, or directly for v4.1 back-bound connections) can craft a CB_NULL or CB_COMPOUND reply with verfLen=0xFFFFFFFD followed by four zero bytes. ValidateCBReply returns nil (success). For CB_RECALL this causes the server to start the full leaseDuration revocation timer instead of the 5-second timer, letting the client retain a delegation it should have been forced to return \u2014 delegation state desync across the cross-protocol lock.LockManager.", "fix": "Add a bound check before the seek: if verfLen > MaxFragmentSize { return fmt.Errorf(\"verf body too large: %d\", verfLen) }. This is consistent with the existing MaxFragmentSize guard in ReadFragment and eliminates the overflow.", "dimension": "security", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "Confirmed. callback_common.go:297-302: guard is `if verfLen > 0` so 0xFFFFFFFD passes. uint32 addition: 0xFFFFFFFD+3=0x00000000, paddedLen=int64(0). reader.Seek(0, SeekCurrent) is no-op. Verifier body bytes remain in reader and are parsed as acceptStat. Mechanism: crafted CB reply bypasses verifier skip, forces garbage acceptStat parse."}, {"title": "all_squash bypass: Username/Domain preserved through MapAllToAnonymous, named-principal ACEs still match", "file": "/tmp/dittofs-audit/pkg/metadata/auth_identity.go", "line": 261, "severity": "MED", "what": "MapAllToAnonymous branch (lines 261-267) copies identity.Username and identity.Domain into the squashed Identity while replacing UID/GID/SID with anonymous values. buildFileAccessEvalContext (auth_permissions.go:1034-1038) sets evalCtx.Who = squashedIdentity.Username + \"@\" + squashedIdentity.Domain. aceMatchesWhoWithOwnerRights (acl/evaluate.go:346) matches ACEs via ace.Who == evalCtx.Who. A file with a named ACE (e.g. ALLOW \"root@localdomain\" WRITE_DATA) will still grant that access to the squashed identity because Who is preserved despite all_squash.", "why": "all_squash / MapAllToAnonymous is a security control to strip all client identity. Preserving Username+Domain leaks the original identity into the ACL evaluation path and lets named-principal ACEs grant elevated rights that the squash is supposed to remove. The SID path is correctly zeroed (mapping.AnonymousSID) but the username path is not.", "fix": "Zero Username and Domain in the MapAllToAnonymous branch: return &Identity{UID: mapping.AnonymousUID, GID: mapping.AnonymousGID, SID: mapping.AnonymousSID}. Do not copy Username/Domain from the original identity. Also clear GroupSIDs for the same reason (currently not copied but verify). The squashed identity must carry no information from the original.", "dimension": "security", "area": "metadata-service-auth", "confirmed": true, "verdict": "Confirmed. auth_identity.go:261-267: MapAllToAnonymous copies identity.Username and identity.Domain into squashed Identity. buildFileAccessEvalContext (auth_permissions.go:1035-1038) sets evalCtx.Who = Username+\"@\"+Domain. Named-principal ACEs matching Who still grant elevated rights to all_squash clients. UID/GID/SID correctly zeroed but username path leaks through."}, {"title": "leaseTimeSeconds and identityMapper globals written without synchronization \u2014 data race on every GETATTR", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/attrs/encode.go", "line": 87, "severity": "MED", "what": "leaseTimeSeconds (plain var uint32, line 82) is written by SetLeaseTime (line 87) and read unsynchronized on every GETATTR encode at lines 326 and 554. identityMapper (plain var identity.IdentityMapper interface, line 122) is written by SetIdentityMapper (line 128) and read at line 710 with an unsynchronized nil-check. The adjacent fsMax* vars correctly use atomic.Uint64 (lines 99-101), but these two globals do not.", "why": "Concurrent GETATTR handling (one goroutine per RPC) races with initialization-time SetLeaseTime / SetIdentityMapper calls. A torn read of leaseTimeSeconds produces a garbage lease time on the wire. A torn read of the identityMapper interface value (two machine words: type pointer + data pointer) can yield a non-nil interface whose type pointer is zero, causing a nil-pointer dereference in the Resolve call at line 713 and crashing the server. This is in scope as unsafe deserialization: corrupted wire output or server crash triggered by external GETATTR traffic during startup.", "fix": "Replace leaseTimeSeconds with atomic.Uint32: 'var leaseTimeSeconds atomic.Uint32' with Store/Load calls in SetLeaseTime/GetLeaseTime and the two encode sites. For identityMapper, use atomic.Pointer[identity.IdentityMapper] or protect with a sync.RWMutex. Pattern is already established in the same file for fsMaxFileSize/Read/Write (lines 99-101).", "dimension": "security", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "Confirmed. encode.go:82-88: leaseTimeSeconds plain var uint32, SetLeaseTime writes without sync, read at lines 326 and 554 in concurrent GETATTR. identityMapper plain interface var at line 122, written at line 128, read at line 710 with unsynchronized nil-check. Adjacent fsMax* vars use atomic.Uint64 (lines 99-101) \u2014 inconsistency confirms oversight. Interface torn read can yield nil-type non-nil interface \u2192 crash."}, {"title": "SIDMapper polynomial-hash fallback can collide with real user/group RIDs", "file": "/tmp/dittofs-audit/pkg/auth/sid/mapper.go", "line": 211, "severity": "MED", "what": "PrincipalToSID falls back to a polynomial hash (rid = rid*31 + uint32(c)) for principal strings that are not well-known, not a numeric UID, and not a sid: prefix. The resulting RID is used to construct a full domain SID identical in format to UserSID(uid) = uid*2+1000.", "why": "A crafted principal string whose polynomial hash equals uid*2+1000 for some target uid will produce the same Windows SID as that uid's domain user SID. SMB/NFSv4 ACL evaluation compares SIDs for equality; a successful collision lets an attacker-supplied ACE principal match a real user's SID, granting or denying access intended for that user. The 32-bit hash space is small enough for collision search (birthday probability).", "fix": "Reserve the hash-based RID range separately from the uid/gid RID ranges. For example, use RIDs >= 0x80000000 for hash-based entries (users use uid*2+1000 which stays well below 2^31 for realistic UIDs). Alternatively, refuse to construct domain SIDs for unknown principals and return WellKnownEveryone or a sentinel null SID instead of a hash-derived domain SID.", "dimension": "security", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "mapper.go:219 confirmed: hash fallback calls makeDomainSID(rid) directly with polynomial rid. UserSID uses uid*2+1000 formula and same makeDomainSID. No guard prevents hash(principalString)==uid*2+1000 for a target uid. 32-bit polynomial hash space is small; collision search feasible. Same domain SID produced for attacker-controlled principal and real UID."}, {"title": "Missing url.PathEscape on username/group/store names \u2014 client-side path traversal to unintended REST endpoints", "file": "/tmp/dittofs-audit/pkg/apiclient/users.go", "line": 61, "severity": "MED", "what": "`resourcePath(\"/api/v1/users/%s\", username)` uses plain `fmt.Sprintf` with no `url.PathEscape`. Same pattern in `UpdateUser` (line 71), `DeleteUser` (line 76), `ResetUserPassword` (line 82), `groups.go` `GetGroup`/`UpdateGroup`/`DeleteGroup`/`AddGroupMember`/`RemoveGroupMember`, `stores.go` `GetMetadataStore`/`UpdateMetadataStore`/`DeleteMetadataStore`/`GetBlockStore`/`UpdateBlockStore`/`DeleteBlockStore`, `netgroups.go`, and the `username`/`groupName` second segment in `shares.go:248/253/259/264`.", "why": "A value like `../groups/admins` in `username` produces path `/api/v1/users/../groups/admins`; Go's `net/http` transport canonicalizes `..` segments before sending, resolving to `/api/v1/groups/admins`. This routes the request to a completely different endpoint. An operator command like `dfsctl user get '../groups/admins'` silently hits the group endpoint, potentially returning or deleting the wrong resource. With crafted names an attacker who can name users/stores could manipulate other API consumers.", "fix": "Replace `resourcePath(\"/api/v1/users/%s\", username)` with `\"/api/v1/users/\" + url.PathEscape(username)` (or add escaping inside `resourcePath`). Apply consistently to all bare-interpolated path segments: `groups.go`, `stores.go`, `netgroups.go`, and the second `%s` in `shares.go:248/253/259/264`. Shares already escape the first segment via `url.PathEscape(normalizeShareNameForAPI(...))` \u2014 the same must apply to the `username`/`groupName` segments.", "dimension": "security", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "helpers.go:82 confirmed: resourcePath = plain fmt.Sprintf with no url.PathEscape. users.go:61,71,76 all use this. Go net/http sends raw .. path segments without normalization. '../groups/admins' in username would produce /api/v1/users/../groups/admins and router may normalize it to /api/v1/groups/admins, hitting a different endpoint."}, {"title": "CIFS mount option injection via comma in SMB username or password (Linux)", "file": "/tmp/dittofs-audit/cmd/dfsctl/commands/share/mount_unix.go", "line": 136, "severity": "MED", "what": "`opts := fmt.Sprintf(\"vers=2.1,port=%d,username=%s,password=%s\", port, username, password)` embeds raw username and password directly into the comma-delimited `-o` option string passed to `mount -t cifs`. A comma in the password (e.g. `p@ss,sec=none`) is parsed by the kernel CIFS driver as a new option.", "why": "CIFS `-o` options are comma-separated. An attacker who controls the password (e.g. via the `DITTOFS_PASSWORD` env var or `--password` flag) can append arbitrary mount options such as `sec=none` (bypass auth), `vers=1.0`, or `forceuid=0`. When `dfsctl share mount` is run with `sudo`, this becomes a local privilege escalation: injected options can override security settings for the kernel mount.", "fix": "Reject usernames and passwords that contain commas before building `opts`, or pass credentials via a credentials file (`credentials=/tmp/creds`) with 0600 permissions rather than inline in the `-o` string. Validate `username` and `password` contain no `,` or `=` characters and return an error if they do.", "dimension": "security", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "mount_unix.go:136 confirmed: opts = fmt.Sprintf(\"vers=2.1,port=%d,username=%s,password=%s\", port, username, password) with no escaping. Comma in password injects new CIFS mount options. When run with sudo, injected options like sec=none bypass auth."}, {"title": "process-global os.Setenv(KRB5CCNAME) not cleaned up, leaks across parallel tests", "file": "/tmp/dittofs-audit/test/e2e/kerberos_test.go", "line": 263, "severity": "MED", "what": "testConcurrentUsers calls `os.Setenv(\"KRB5CCNAME\", aliceCC)` and then `os.Setenv(\"KRB5CCNAME\", bobCC)` without ever restoring the original value. These are process-global mutations; if the test runs in parallel with other tests that rely on KRB5CCNAME they will use Bob's credential cache. The same pattern appears at nfsv4_kerberos_test.go:355,364.", "why": "os.Setenv is process-global and permanent for the duration of the process. t.Setenv (used correctly in framework/kerberos.go:98-100) registers a cleanup that restores the previous value on test exit. Using bare os.Setenv here means the variable is not restored if the test fails mid-way, races against parallel test goroutines that read KRB5CCNAME, or is subsequently read by KinitWithCC helpers that inherit the wrong cache path.", "fix": "Replace both `os.Setenv(\"KRB5CCNAME\", aliceCC)` and `os.Setenv(\"KRB5CCNAME\", bobCC)` with `t.Setenv(\"KRB5CCNAME\", aliceCC)` / `t.Setenv(\"KRB5CCNAME\", bobCC)`. Apply the same fix to nfsv4_kerberos_test.go:355 and 364.", "dimension": "security", "area": "e2e-test-framework", "confirmed": true, "verdict": "kerberos_test.go:263,273 and nfsv4_kerberos_test.go:355,364 confirmed: bare os.Setenv used, never t.Setenv. No cleanup registered. framework/kerberos.go:100 correctly uses t.Setenv for same variable. Process-global leak if test fails mid-way or runs in parallel."}, {"title": "NT hash prefix logged in plaintext to debug log on auth failure", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 1134, "severity": "MED", "what": "`logger.Debug(... \"ntHashPrefix\", fmt.Sprintf(\"%x\", ntHash[:4]) ...)` emits 4 bytes of the server-side NT hash in hex to the debug log on every NTLMv2 validation failure.", "why": "NT hash is equivalent to a password. Even 4 bytes of it in logs gives an attacker significant oracle information when combined with brute-force. Debug logs are often captured at higher levels in production (log aggregators, crash reports). This is an AI-slop addition \u2014 no legitimate diagnostic purpose requires exposing any fraction of a credential hash.", "fix": "Remove the `ntHashPrefix` field entirely from the `logger.Debug` call. The username, domain variants tried, and error are sufficient for diagnosis.", "dimension": "slop", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "session_setup.go:1086 confirmed: ntHash comes from user.GetNTHash() \u2014 stored server-side NT hash. Line 1134 logs ntHash[:4] in hex at Debug level on every auth failure. NT hash is password-equivalent; even 4 bytes provide oracle information for brute-force attacks. No legitimate diagnostic purpose for exposing credential hash bytes."}, {"title": "SET_INFO FileBasicInformation clears modeDOSSparse when recomputing mode", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/set_info.go", "line": 333, "severity": "MED", "what": "When fileAttrs != 0, SET_INFO FileBasicInformation calls SMBModeFromAttrs then preserves modeDOSCompressed from the current metadata (line 333: `mode |= curFile.Mode & modeDOSCompressed`). modeDOSSparse (0x200000) is not preserved. Any SET_INFO FileBasicInformation with a non-zero FileAttributes field after FSCTL_SET_SPARSE will silently clear the sparse bit.", "why": "FSCTL_SET_SPARSE persists the sparse attribute in modeDOSSparse so subsequent QUERY_INFO reflects FILE_ATTRIBUTE_SPARSE_FILE (ioctl_sparse.go handleSetSparse). A follow-up SET_INFO BasicInfo (e.g. setting ARCHIVE or READONLY) resets the mode via SMBModeFromAttrs which has no knowledge of the FSCTL-managed sparse bit, just as FILE_ATTRIBUTE_COMPRESSED is FSCTL-managed and was already protected. The smbtorture sparse_file_flag test chain exercises both FSCTL_SET_SPARSE and subsequent attribute queries; a client mixing attribute sets with sparse operations will observe the flag disappear.", "fix": "On line 333, preserve modeDOSSparse alongside modeDOSCompressed: change `mode |= curFile.Mode & modeDOSCompressed` to `mode |= curFile.Mode & (modeDOSCompressed | modeDOSSparse)`. Add a comment mirroring the existing modeDOSCompressed rationale: 'modeDOSSparse is FSCTL-managed (FSCTL_SET_SPARSE); preserve it so SET_INFO does not accidentally clear sparse state.'", "dimension": "slop", "area": "smb-io-query-ioctl", "confirmed": true, "verdict": "set_info.go:329-334 confirmed: when fileAttrs!=0, mode = SMBModeFromAttrs(fileAttrs) then only modeDOSCompressed is OR'd back from curFile.Mode. modeDOSSparse (0x200000) defined in converters.go:66 and used in ioctl_sparse.go is not preserved. Any SET_INFO FileBasicInformation with non-zero attrs after FSCTL_SET_SPARSE silently clears the sparse bit."}, {"title": "acquireLock LOCK4denied Owner.OwnerData contains raw format string instead of decoded bytes", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/manager.go", "line": 1999, "severity": "MED", "what": "In acquireLock, when a conflicting lock is found via IsUnifiedLockConflicting, the LOCK4denied response is built with Owner.ClientID=0 and Owner.OwnerData=[]byte(el.Owner.OwnerID) where el.Owner.OwnerID is the formatted string 'nfs4:{clientid}:{owner_hex}'. The comment says 'Parse OwnerID to extract clientID and ownerData' but does not actually parse. The TestLock function (line 2089) correctly calls parseConflictOwner for the same case.", "why": "Clients receive a malformed lock_owner4 in LOCK4denied: the 'owner' field contains the ASCII format string bytes instead of the opaque owner identifier, and ClientID is always 0. RFC 7530 Section 16.10.4 requires the owner of the conflicting lock in LOCK4denied. Linux nfs client uses this to display lock owner info; malformed data is a protocol violation causing incorrect diagnostics. Plausible-but-wrong logic: the fix exists in parseConflictOwner but was not called here.", "fix": "Replace lines 1997\u20132000 with a call to parseConflictOwner: `parseConflictOwner(el.Owner.OwnerID, denied)`. This is exactly what TestLock does at line 2089 for the same Owner.OwnerID format.", "dimension": "slop", "area": "nfs-v4-state-manager", "confirmed": true, "verdict": "manager.go:1998-1999 confirmed: denied.Owner.ClientID=0 and denied.Owner.OwnerData=[]byte(el.Owner.OwnerID) (raw format string bytes). parseConflictOwner at line 2284 exists and correctly parses 'nfs4:{clientid}:{owner_hex}' format but is called only in TestLock (line 2089), not in acquireLock. RFC 7530 \u00a716.10.4 violation \u2014 malformed lock_owner4 in LOCK4denied."}, {"title": "READDIR cookieverf mismatch silently ignored instead of NFS4ERR_BAD_COOKIE", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/readdir.go", "line": 140, "severity": "MED", "what": "When cookie != 0 and cookieVerf mismatches the current directory verifier, the code logs at Debug and continues serving entries with current cookies. NFSv4 RFC 7530 \u00a716.24.4 requires the server to return NFS4ERR_BAD_COOKIE in this case (the directory changed between READDIR pages \u2014 cookies from the old listing are stale/invalid). The comment says 'matches NFSv3 behavior' but NFSv4 behavior is explicitly stronger than NFSv3 on this point.", "why": "Plausible-but-wrong logic copied from NFSv3 semantics. Clients relying on NFSv4 cookie-verifier contract to detect mid-scan directory modifications will silently receive incorrect/duplicated/skipped entries instead of a clean error+retry signal. Linux nfs client with vers=4 can end up with phantom or missing entries in large directories.", "fix": "Replace the debug-and-continue block with: `return &types.CompoundResult{Status: types.NFS4ERR_BAD_COOKIE, OpCode: types.OP_READDIR, Data: encodeStatusOnly(types.NFS4ERR_BAD_COOKIE)}`. The client will restart the READDIR from cookie=0 with the updated verifier.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "readdir.go:140-146 confirmed: when cookie!=0 and incomingVerf!=0 and mismatch, code only logs at Debug and continues serving entries. No NFS4ERR_BAD_COOKIE returned. Comment explicitly says 'matches NFSv3 behavior' but RFC 7530 \u00a716.24.4 requires NFS4ERR_BAD_COOKIE for NFSv4. Clients will silently get stale/incorrect entries instead of a clean retry signal."}, {"title": "Fabricated test assertion masks CB_SEQUENCE seqid double-increment bug", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/state/backchannel_test.go", "line": 389, "severity": "MED", "what": "`TestBackchannelSender_SequenceIDIncrement` asserts only `seqID2 > seqID1` (line 392), not `seqID2 == seqID1 + 1`. The actual buggy implementation (seqids 1, 3, 5\u2026) satisfies `>` so the test passes green while the RFC violation goes undetected. The test name and comment imply it verifies increment-by-one semantics, but the assertion is deliberately weakened to only check monotonicity.", "why": "AI-slop pattern: test asserts a looser property than the protocol requires, appears to be exercising the constraint, but provides no protection against the real bug. A correct test would fail immediately if the fix in the prior finding is absent.", "fix": "Change the final assertion block to: `if seqID2 != seqID1+1 { t.Errorf(\"seqID must increment by exactly 1: got seqID1=%d seqID2=%d\", seqID1, seqID2) }` and remove the now-redundant `seqID1 == seqID2` check.", "dimension": "slop", "area": "nfs-v4-delegation-callback", "confirmed": true, "verdict": "backchannel.go:270,284 confirmed: seqID=bs.nextXID.Add(1) then xid=bs.nextXID.Add(1) \u2014 two increments per sendCallback call, so seqID increments by 2 each call (1,3,5...). Test at backchannel_test.go:389-393 only asserts seqID2!=seqID1 and seqID2>seqID1, both satisfied by skip-by-2 behavior. RFC requires increment-by-1; weaker assertion masks the bug."}, {"title": "privacy.go comments claim KeyUsageAcceptorSeal=26 but constant is 22", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/privacy.go", "line": 13, "severity": "MED", "what": "Package doc comment and two inline comments (lines 13, 256, 311) write `KeyUsageAcceptorSeal (26)`. The actual constant in types.go is `KeyUsageAcceptorSeal uint32 = 22`. RFC 4121 Section 2 defines KG-USAGE-ACCEPTOR-SEAL = 22; 26 does not exist in the RFC.", "why": "Fabricated constant value in comments (AI-slop). The code calls `encType.EncryptMessage(..., KeyUsageAcceptorSeal)` so the runtime value is correct (22), but the comments say `(26)`. Any reader auditing the crypto key-usage choices against the comments will be misled into thinking the code is wrong, or may use `26` if they hand-code a client-side decrypt or write a conformance test.", "fix": "Change all three occurrences of `(26)` in privacy.go to `(22)`: line 13 `KeyUsageAcceptorSeal (26)` \u2192 `(22)`, line 256 `acceptor seal key usage (26)` \u2192 `(22)`, line 311 same substitution.", "dimension": "slop", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed: privacy.go lines 13 and 311 comment say KeyUsageAcceptorSeal (26); types.go:145 defines KeyUsageAcceptorSeal uint32 = 22. RFC 4121 \u00a72 KG-USAGE-ACCEPTOR-SEAL=22. Runtime calls use correct constant but comments mislead auditors."}, {"title": "NeedsAuth field on nfsProcedure/mountProcedure is dead \u2014 populated but never read in dispatch", "file": "/tmp/dittofs-audit/internal/adapter/nfs/dispatch.go", "line": 399, "severity": "MED", "what": "`nfsProcedure.NeedsAuth` (line 402) and `mountProcedure.NeedsAuth` (line 440) are declared and carefully set in `dispatch_nfs.go` and `dispatch_mount.go` for every procedure. The dispatch functions `dispatchNFSv3Procedure` (line 248) and `dispatchMount` (line 267) look up the procedure entry but never check `procedure.NeedsAuth`. The field is read only by a unit test that validates the flag values themselves \u2014 a fabricated assertion that confirms the flag is set correctly while the gate it describes does nothing.", "why": "Stub presenting as done: the field comment says 'If true and AUTH_UNIX parsing fails, the procedure may still execute but with nil credentials' \u2014 implying an enforcement gate. No such gate exists. `ExtractHandlerContext` always runs regardless; no early rejection happens when `NeedsAuth==true` and credentials are nil. The test `TestNFSDispatchTable_AuthRequirements` only asserts flag values, not enforcement behaviour.", "fix": "Either (a) add enforcement in `dispatchNFSv3Procedure`: after `ExtractHandlerContext`, if `procedure.NeedsAuth && handlerCtx.UID == nil` return `NFS3ErrAccess`; or (b) delete the field and test entirely to avoid the misleading implication of a security gate.", "dimension": "slop", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "Confirmed: NeedsAuth field declared at dispatch.go:402,440 and set in dispatch_nfs.go/dispatch_mount.go for every entry. dispatchNFSv3Procedure (line 248) and dispatchMount (line 267) look up procedure but never check procedure.NeedsAuth. Only dispatch_test.go asserts flag values \u2014 no enforcement gate exists."}, {"title": "populateBlockCounts misclassifies all CAS-path pending blocks as Dirty", "file": "/tmp/dittofs-audit/pkg/block/engine/stats.go", "line": 136, "severity": "MED", "what": "Check `b.LocalPath != \"\" || b.BlockStoreKey != \"\"` inside `BlockStatePending` case always false for post-Phase-17 CAS blocks. ObjectIDPersister (engine.go:223-228) and ChunkEmitter (engine.go:344-349) create FileBlock rows with State=Pending, LocalPath=\"\", BlockStoreKey=\"\". So every live CAS-path block hits `BlocksDirty++` instead of `BlocksLocal++`. `BlocksLocal` stays 0 in normal operation; `BlocksDirty` is inflated to include all locally-present-but-not-yet-mirrored blocks.", "why": "Post-Phase-17 CAS surface tracks local presence via the CAS chunk store (local.Has(hash)), not via FileBlock.LocalPath. The pre-Phase-17 lifecycle set LocalPath on pending blocks; the new lifecycle omits it. The stats code was not updated: comment even says 'Pending now covers both legacy Dirty (no LocalPath/no key) and Local (complete, awaiting sync)' but the discriminator still uses the old fields. Plausible-but-wrong logic after rewrite.", "fix": "Replace `b.LocalPath != \"\" || b.BlockStoreKey != \"\"` with `!b.Hash.IsZero()`: a CAS-pending block whose hash is populated is locally present in the CAS chunk store. Alternatively use `b.IsDirty()` for dirty (hash zero) vs local (hash non-zero, pending). Keep the `BlockStateSyncing` \u2192 BlocksLocal and `BlockStateRemote` \u2192 BlocksRemote branches unchanged.", "dimension": "slop", "area": "block-engine-core", "confirmed": true, "verdict": "Confirmed: stats.go:136 checks b.LocalPath != \"\" || b.BlockStoreKey != \"\". engine.go:223-228 (ObjectIDPersister) and :344-350 (ChunkEmitter) both create FileBlock{State:BlockStatePending} with no LocalPath or BlockStoreKey, so all CAS-path pending blocks fall to BlocksDirty++ instead of BlocksLocal++."}, {"title": "TestCache_SequentialDetection_CACHE03 assertion does not verify prefetched hash identity", "file": "/tmp/dittofs-audit/pkg/block/engine/cache.go", "line": 214, "severity": "MED", "what": "Test comment (line 206) says 'should trigger prefetch of h3 (depth=1)' but assertion is only loader.waitForCalls(1, ...) \u2014 any loader invocation passes, including re-loading h2 (the wrong block). The test passes with the wrong prefetch behavior described in the companion finding above.", "why": "Fabricated/insufficient assertion masks the logic bug: test gives a false green signal that prefetch is working correctly when it is prefetching the wrong hash. A reviewer sees a passing test named CACHE03 and concludes prefetch correctness is verified.", "fix": "After loader.waitForCalls(1), inspect loader.calls and assert it contains hashN(3) (the next-block hash), not hashN(0)/hashN(1)/hashN(2). Add a parallel negative assertion that the calls slice does NOT contain only already-seen hashes.", "dimension": "slop", "area": "block-engine-gc-syncer", "confirmed": true, "verdict": "Confirmed: TestCache_SequentialDetection_CACHE03 at cache_test.go:214 calls loader.waitForCalls(1, 500ms) \u2014 only checks call count >=1. recordingLoader.calls slice records actual hashes but test never inspects it. Wrong prefetch hash (e.g. re-loading h2 instead of h3) would still pass."}, {"title": "Non-atomic floor in releaseLogBytes loses concurrent Add(frameSize) increments", "file": "/tmp/dittofs-audit/pkg/block/local/fs/appendwrite.go", "line": 214, "severity": "MED", "what": "releaseLogBytes does `Add(-n) < 0` then `Store(0)` as two separate atomic ops. Between them, a concurrent AppendWrite's `logBytesTotal.Add(int64(n))` (line 435) can fire \u2014 the subsequent `Store(0)` clobbers that increment, permanently losing frameSize bytes from the budget counter.", "why": "Budget under-count means the pressure gate (`logBytesTotal.Load() > maxLogBytes`) stays false when it should be true, allowing new writes past the ceiling. Under sustained create/delete churn with many concurrent AppendWrites, repeated racing floors cause cumulative drift, defeating the backpressure mechanism and enabling unbounded log growth until ErrPressureTimeout or OOM. Classic TOCTOU on two separate atomic ops masquerading as a single operation.", "fix": "Replace the two-op pattern with a CAS loop:\n```go\nfunc (bc *FSStore) releaseLogBytes(n int64) {\n    if n <= 0 {\n        return\n    }\n    for {\n        old := bc.logBytesTotal.Load()\n        next := old - n\n        if next < 0 {\n            next = 0\n        }\n        if bc.logBytesTotal.CompareAndSwap(old, next) {\n            break\n        }\n    }\n    bc.signalPressure()\n}\n```\nThis ensures the floor and the decrement are atomic with respect to concurrent Add calls.", "dimension": "slop", "area": "block-local-fs-appendlog", "confirmed": true, "verdict": "Confirmed: releaseLogBytes at appendwrite.go:214-215 does Add(-n) then Store(0) as two separate atomic ops. Concurrent AppendWrite's logBytesTotal.Add(int64(n)) at line 435 can fire between them; the Store(0) then clobbers that increment, permanently losing frameSize bytes from the budget counter."}, {"title": "reclaimLeaseImpl no-lockStore path creates UnifiedLock but never adds it to lm.unifiedLocks \u2014 orphaned stub", "file": "/tmp/dittofs-audit/pkg/metadata/lock/reclaim.go", "line": 126, "severity": "MED", "what": "When lockStore is nil (lines 115-144), if no existing lease is found in memory, a new reclaimedLock is constructed with an empty FileHandle and returned \u2014 but it is never appended to lm.unifiedLocks[handleKey]. The caller receives a non-nil *UnifiedLock suggesting success, but no subsequent call (GetLeaseState, findLeaseByKey, AcknowledgeLeaseBreak, etc.) can locate this lease.", "why": "Fabricated stub passing as done: the function returns a lock object that is immediately unreachable by any other manager method. Any code path that reclaims a lease and then attempts to use it (e.g., NFS state manager calling GetLeaseState to verify reclaim state) silently sees 'not found'. The path is documented as 'for testing' but no test exercises the orphaning.", "fix": "After constructing reclaimedLock, append it to lm.unifiedLocks[handleKey] (using FileHandle if available, else skip the append and return an explicit error: 'cannot reclaim: no file handle and no lock store'). Alternatively gate the function to always require lockStore and remove the stub path.", "dimension": "slop", "area": "metadata-lock-manager", "confirmed": true, "verdict": "Confirmed: reclaim.go lines 127-144 construct reclaimedLock with empty FileHandle (field not set) and return it without appending to lm.unifiedLocks[handleKey]. In contrast the lockStore path appends at line 101. The returned lock is unreachable by findLeaseByKey, GetLeaseState, AcknowledgeLeaseBreak, etc."}, {"title": "memoryTransaction.ListChildren omits EAs clone \u2014 EA aliasing with stored view", "file": "/tmp/dittofs-audit/pkg/metadata/store/memory/transaction.go", "line": 436, "severity": "MED", "what": "`memoryTransaction.ListChildren` shallow-copies `attr` from `fileData.Attr`, clones `Blocks` and `ACL`, but never clones `EAs`. The store-level `ListChildren` in `files.go:346-347` clones all three. The tx variant diverged by copy-paste.", "why": "A caller that mutates the returned `DirEntry.Attr.EAs` map (or an individual value slice) inside a `WithTransaction` closure modifies the stored map in place. Subsequent reads within the same transaction or after commit will see corrupted EA bytes. The Badger and Postgres backends round-trip through JSON and never alias, so this is a memory-only cross-backend divergence.", "fix": "Add `attr.EAs = cloneEAs(fileData.Attr.EAs)` after line 437 (after the ACL clone), mirroring `files.go:347` and `buildFileWithNlink` in `store.go:499`.", "dimension": "slop", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: memoryTransaction.ListChildren at lines 434-438 shallow-copies attr, clones Blocks and ACL, but never calls cloneEAs. files.go:347 (store-level ListChildren) does call cloneEAs. Callers mutating returned EAs inside a transaction modify the stored map in-place."}, {"title": "Badger GetFilesystemStatistics counts Size of ALL file types, not just regular files", "file": "/tmp/dittofs-audit/pkg/metadata/store/badger/transaction.go", "line": 1143, "severity": "MED", "what": "`GetFilesystemStatistics` accumulates `file.Size` for every key under the `f:` prefix regardless of `file.Type`. `initUsedBytesCounter` (store.go:424) and all `pendingDelta` bookkeeping are guarded by `file.Type == metadata.FileTypeRegular`. The scan at line 1143 has no such guard.", "why": "Directories, symlinks, and device nodes all appear under `f:` with a `Size` field (typically 0 or a small value), but any non-zero size on such entries inflates `UsedBytes` in FSSTAT responses. More critically, the `UsedBytes` returned by `GetFilesystemStatistics` is structurally inconsistent with `GetUsedBytes()`, causing FSSTAT to report a different value than the atomic counter \u2014 a plausible-but-wrong copy of the full-scan pattern without the regular-files filter.", "fix": "Add a type guard: `if file.Type == metadata.FileTypeRegular { usedSize += file.Size }` at line 1143, mirroring the guard in `initUsedBytesCounter` (store.go:427).", "dimension": "slop", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "Confirmed: badgerTransaction.GetFilesystemStatistics at line 1143 accumulates usedSize += file.Size for every entry under prefixFile with no file.Type check. initUsedBytesCounter in store.go:430 guards with file.Type == metadata.FileTypeRegular. Structural inconsistency between the two UsedBytes sources."}, {"title": "PutFile silently swallows old-size scan error, miscounting usedBytes", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/transaction.go", "line": 297, "severity": "MED", "what": "The pre-update old-size query '_ = tx.tx.QueryRow(ctx, ...).Scan(&oldSizeVal)' discards ALL errors with a blank identifier. The intent is 'not found \u2192 oldSize = 0', but the same discard also swallows genuine DB errors (type mismatch, connection drop, serialization failure during a nested tx). On a real scan error oldSizeVal remains Invalid and oldSize stays 0, so pendingDelta accumulates int64(file.Size) - 0 = file.Size instead of the correct delta, permanently over-crediting the atomic usedBytes counter.", "why": "The usedBytes counter is the authoritative source for statfs UsedBytes reported to NFS/SMB clients. Systematic over-counting drifts statfs until the store is restarted. Because the scan error is discarded, the bug is invisible \u2014 no log entry, no error return. This is the plausible-but-wrong error discard pattern.", "fix": "Distinguish ErrNoRows from real errors: 'if err := tx.tx.QueryRow(ctx, query, ...).Scan(&oldSizeVal); err != nil && !errors.Is(err, pgx.ErrNoRows) { return mapPgError(err, \"PutFile\", \"read old size\") }'. Keep oldSize = 0 only on ErrNoRows; propagate other errors.", "dimension": "slop", "area": "metadata-store-postgres", "confirmed": true, "verdict": "Confirmed: transaction.go:296 uses _ = tx.tx.QueryRow(...).Scan(&oldSizeVal) discarding all errors. On a genuine DB error oldSizeVal stays zero-valued (Invalid), oldSize stays 0, pendingDelta accumulates full file.Size instead of delta. Invisible \u2014 no log, no error return."}, {"title": "GetFilesystemStatistics ignores share-scoped handle, aggregates all shares globally", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/transaction.go", "line": 1244, "severity": "MED", "what": "Both the transaction-level GetFilesystemStatistics (transaction.go:1244) and the pool-level counterpart (server.go:146) execute 'SELECT COUNT(*) FROM files' / 'COALESCE(SUM(size), 0) ... FROM files' with no WHERE clause. The handle parameter (which encodes the share) is decoded nowhere and never used. In a multi-share server every share reports aggregated totals for the entire filesystem.", "why": "NFS FSSTAT and SMB QueryFSInfo are per-share operations; clients use the returned avail/total to enforce quotas and display disk usage. When two shares exist, each share sees double the real used-bytes and half the correct available space. The service-layer quota path (service_quota_test.go) overrides TotalBytes from the quota table, masking this for quota-capped shares, but UsedFiles and UsedBytes still aggregate cross-share. This is a fabricated implementation that silently returns wrong data.", "fix": "Decode the handle to extract share_name, then add 'WHERE share_name = $1' to both COUNT and SUM queries. The tx-path already has access to the pool through tx.store; the store-path already has s.usedBytes (which is global \u2014 that counter also needs per-share tracking for a complete fix, but scoping the SQL query is the minimum).", "dimension": "slop", "area": "metadata-store-postgres", "confirmed": true, "verdict": "Confirmed: transaction.go:1244 query is SELECT COALESCE(SUM(size),0), COUNT(*) FROM files with no WHERE clause. server.go:146 similarly SELECT COUNT(*) FROM files with no share filter. handle parameter decoded nowhere. Multi-share servers report cross-share aggregates to every client."}, {"title": "LoadSharesFromStore: dead fallback passes UUID as name to GetMetadataStore", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/init.go", "line": 162, "severity": "MED", "what": "When GetMetadataStoreByID fails, the fallback at line 162 calls s.GetMetadataStore(ctx, share.MetadataStoreID), passing the UUID value (share.MetadataStoreID, always a 36-char UUID) as the name parameter. GetMetadataStore searches by the human-readable name column, not by ID. The fallback can never succeed: metadata store names are labels like \"default\" or \"badger-1\", not UUIDs. The code path is dead but presents as a legacy-compatibility shim.", "why": "Plausible-but-wrong AI-generated logic: the comment says \"fall back to name lookup\" but the argument passed is still the ID, not a name. If GetMetadataStoreByID ever fails (e.g. after a store config deletion mid-restart, or a DB schema mismatch), every share referencing that store is silently skipped at boot with only a Warn log \u2014 the operator sees no actionable error and the server starts with missing shares.", "fix": "Remove the dead fallback branch entirely, or if legacy rows genuinely stored a name in MetadataStoreID, pass the correct field: metaStoreCfg, err = s.GetMetadataStore(ctx, share.MetadataStoreName) where MetadataStoreName is the name field. Since models.Share has no separate name field for this, the correct fix is to drop the fallback and let the Warn + continue path handle the failure: if err != nil { logger.Warn(...); continue }", "dimension": "slop", "area": "controlplane-runtime-shares", "confirmed": true, "verdict": "Confirmed: init.go:162 calls s.GetMetadataStore(ctx, share.MetadataStoreID) where s is a store.Store. store/metadata.go:13 shows GetMetadataStore queries by 'name' column. UUID passed as name never matches a human-readable name like 'default'."}, {"title": "encodeUTF16LE truncates supplementary Unicode code points (> U+FFFF)", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/srvsvc.go", "line": 301, "severity": "MED", "what": "encodeUTF16LE converts each rune to uint16 via truncation (uint16(r)). Runes > U+FFFF (e.g. emoji in share names/comments) require surrogate pairs in UTF-16LE but are silently truncated to the low 16 bits, producing invalid UTF-16 and corrupting the NDR wire encoding for any non-BMP character.", "why": "Fabricated UTF-16 encoder missing surrogate pair logic \u2014 classic AI-slop. The golang.org/x/text/encoding/unicode package or a manual surrogate-pair expansion is needed. Also used in lsarpc.go writeNDRUnicodeString (via the shared encodeUTF16LE import) for domain/user name encoding.", "fix": "Replace with a correct surrogate-aware encoder: import golang.org/x/text/encoding/unicode and use unicode.UTF16(unicode.LittleEndian, unicode.IgnoreBOM).NewEncoder(), or manually: if r > 0xFFFF { r -= 0x10000; append uint16(0xD800 + r>>10), uint16(0xDC00 + r&0x3FF) } else { append uint16(r) }.", "dimension": "slop", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: srvsvc.go:301 does uint16(r) truncation on every rune. No surrogate pair expansion for codepoints > U+FFFF. Produces invalid UTF-16LE for any non-BMP character."}, {"title": "buildShareEnumLevel1Response NDR conformant array MaxCount uses byte-count instead of wchar count", "file": "/tmp/dittofs-audit/internal/adapter/smb/rpc/srvsvc.go", "line": 213, "severity": "MED", "what": "nameLen = len(share.Name) + 1 is the UTF-8 byte length + 1. Written as NDR MaxCount and ActualCount for a wchar_t conformant array. NDR \u00a714.3.3.4 specifies conformant array bounds in element count (wchar_t = 2 bytes), not byte count. For pure ASCII names len(UTF-8)==len(UTF-16)/2 so values match by accident. For any multi-byte UTF-8 name (accented chars, CJK) len(share.Name) != wchar count \u2192 NDR frame is malformed.", "why": "Plausible-but-wrong: the code looks correct for ASCII but violates the NDR spec for non-ASCII. The fix would use len([]rune(share.Name))+1 (or the UTF-16 output length / 2 + 1) as the element count.", "fix": "Compute nameWcharCount := len([]rune(share.Name)) + 1 (rune count + null terminator in wide chars). Use nameWcharCount for MaxCount/ActualCount fields. Same for commentLen. The actual UTF-16LE bytes appended remain the same.", "dimension": "slop", "area": "smb-security-ea-rpc", "confirmed": true, "verdict": "Confirmed: srvsvc.go:213 uses len(share.Name)+1 (UTF-8 byte length) as NDR MaxCount/ActualCount for wchar_t conformant array. Breaks for any multi-byte UTF-8 name where byte count differs from rune count."}, {"title": "identityMapper interface var \u2014 data race between SetIdentityMapper and concurrent GETATTR resolveOwnerString", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/attrs/encode.go", "line": 122, "severity": "MED", "what": "Plain `var identityMapper identity.IdentityMapper` written by SetIdentityMapper() (line 128) and read by resolveOwnerString() (line 710) from concurrent GETATTR handlers. No mutex or atomic protects the interface value, which is a two-word pointer+type pair; torn reads are possible.", "why": "Go interface values are not atomically readable/writable; a concurrent store and load of a plain interface var is a data race detectable by -race and can produce a torn read in practice.", "fix": "Protect with a sync/atomic.Pointer[identity.IdentityMapper] or a sync.RWMutex. Simplest: `var identityMapperMu sync.RWMutex; var identityMapper identity.IdentityMapper`; RLock on read, Lock on write \u2014 or use `atomic.Pointer`.", "dimension": "slop", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "Confirmed: encode.go:122 declares plain var identityMapper identity.IdentityMapper. SetIdentityMapper (line 128) and resolveOwnerString (line 710) access it without mutex or atomic. sync/atomic is imported only for fsMaxFileSize/fsMaxReadSize/fsMaxWriteSize \u2014 not identityMapper."}, {"title": "extractCallbackAddr hardcodes DittoFS server port 12049 for NLM GRANTED callbacks", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/lock.go", "line": 259, "severity": "MED", "what": "extractCallbackAddr strips the client's ephemeral port and replaces it with 12049 (the DittoFS NFS server port). The constructed address is stored in Waiter.CallbackAddr and later dialed by callback.SendGrantedCallback when a blocked lock is granted.", "why": "NLM clients listen for GRANTED callbacks on their own local NLM port, which is registered with portmapper and typically different from the server's port. Dialing the client IP:12049 will almost certainly be refused (nothing listening there on the client) causing every blocking lock grant to fail, the lock to be immediately released, and the client to never receive GRANTED \u2014 effectively making blocking NLM locks non-functional. The client callback address should come from the portmapper lookup on the client side or from the NLM transport layer, not from a hardcoded server-side constant.", "fix": "Remove extractCallbackAddr. Use ctx.ClientAddr (which contains the actual client source address) as the base for the callback but resolve the client's NLM callback port via a portmapper GETPORT call to the client (prog=100021, vers=4, prot=TCP), or expose the callback port from the lock's transport context. At minimum, document this as unimplemented and return NLM4DeniedNoLocks for blocking requests until the callback port is properly resolved.", "dimension": "slop", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "Confirmed: lock.go:259 returns net.JoinHostPort(host, \"12049\") \u2014 hardcoded DittoFS server port. NLM GRANTED callbacks must go to the client's registered NLM port, not the server's port."}, {"title": "Unguarded type assertion on KMIP ResponsePayload panics on nil/unexpected type", "file": "/tmp/dittofs-audit/pkg/block/encryption/keyprovider/kmip.go", "line": 145, "severity": "MED", "what": "Bare type assertion `item.ResponsePayload.(ttlv.TTLV)` with no comma-ok form. After `ttlv.Unmarshal` into a generic `kmip.ResponseMessage`, `ResponsePayload` is normally `ttlv.TTLV`, but if a server sends a `ResultStatusSuccess` response with a missing/empty payload the field is nil \u2014 the assertion panics instead of returning an error.", "why": "A panic in key-unwrap path during daemon startup (or mid-operation if provider is re-init) crashes the process. This is exactly the class of bug introduced by AI-generated code that assumes the happy-path type is always present: the success guard on line 140 checks `ResultStatus`, not `ResponsePayload` presence.", "fix": "Use comma-ok: `raw, ok := item.ResponsePayload.(ttlv.TTLV); if !ok { return nil, fmt.Errorf(\"keyprovider: kmip Get payload is not TTLV (got %T)\", item.ResponsePayload) }` then pass `raw` to `ttlv.Unmarshal`.", "dimension": "slop", "area": "block-remote-s3-encryption-compression", "confirmed": true, "verdict": "Confirmed: kmip.go:145 bare type assertion item.ResponsePayload.(ttlv.TTLV) with no nil check or comma-ok form. ResultStatus check at line 140 does not guarantee payload is non-nil. Panics if KMIP server returns success with empty payload."}, {"title": "pruneEmptyDirs reads only first directory page \u2014 silently skips intermediary dirs beyond page boundary", "file": "/tmp/dittofs-audit/pkg/controlplane/runtime/trash/service.go", "line": 439, "severity": "MED", "what": "pruneEmptyDirs calls svc.ReadDirectory with cookie=0 once and iterates only page.Entries, with no HasMore/NextCookie loop. walkBin immediately above uses a correct pagination loop. If a #recycle directory holds more intermediary directories than fit on one page, the excess are never visited and never removed.", "why": "After Empty() purges all recycled roots, leftover empty intermediary directories that spill past the first page are silently skipped. The #recycle tree accumulates orphaned empty dirs across repeated Empty calls. The pattern is copy-paste of walkBin's inner loop body without the surrounding for { ... if !page.HasMore { return nil }; cookie = page.NextCookie } scaffolding \u2014 AI-slop incomplete port.", "fix": "Wrap the existing body in a pagination loop matching walkBin: `var cookie uint64; for { page, err := svc.ReadDirectory(ctx, dirHandle, cookie, 0); ...; if !page.HasMore { return nil }; cookie = page.NextCookie }`", "dimension": "slop", "area": "controlplane-snapshot-gc", "confirmed": true, "verdict": "Confirmed: pruneEmptyDirs at service.go:439 calls ReadDirectory once with cookie=0 and iterates page.Entries with no HasMore/NextCookie loop. walkBin above (line 143-184) has the correct pagination loop. If #recycle contains more dirs than one page, overflow is silently skipped."}, {"title": "Stats handler leaks raw internal error string in HTTP response body", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/blockstore.go", "line": 43, "severity": "MED", "what": "NotFound(w, err.Error()) passes raw runtime error string directly into the 404 response body", "why": "Every analogous handler in the codebase (block_gc.go:102, blockstore_audit.go:82) strips the internal error string and emits a fixed operator-safe message. This one diverges: err.Error() from GetBlockStoreStats can contain filesystem paths, share-internal state, or runtime internals. Also wrong status code class \u2014 a missing/unknown share is not a 404 from the caller's perspective here; it is a misconfiguration. Copy-paste drift: the log line at line 42 was added but the response sanitization was omitted.", "fix": "Replace with a fixed safe message: `NotFound(w, \"share not found\")` (or `InternalServerError(w, \"stats unavailable\")` if the error is not strictly a missing-share sentinel). Never interpolate err.Error() into the response body.", "dimension": "slop", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed: blockstore.go:43 calls NotFound(w, err.Error()) \u2014 raw runtime error string in HTTP response body. Reference handlers (block_gc.go, blockstore_audit.go) use fixed strings or errors.Is-gated messages."}, {"title": "Evict handler leaks raw internal error string in HTTP response body", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/blockstore.go", "line": 75, "severity": "MED", "what": "BadRequest(w, err.Error()) passes raw runtime error string directly into the 400 response body", "why": "Same class as line 43: EvictBlockStore errors can contain filesystem paths or internal runtime details. The log line at line 74 already captures the full error for operators. Every other handler that strips errors (block_gc.go RunGC, blockstore_audit.go RunAudit) uses a fixed safe string. This is the only handler in the file that does not.", "fix": "Replace with `BadRequest(w, \"eviction failed\")` or map the error to a typed sentinel first (e.g. shares.ErrShareNotFound -> NotFound). Never interpolate err.Error() into the response body.", "dimension": "slop", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed: blockstore.go:75 calls BadRequest(w, err.Error()) \u2014 raw runtime error string in 400 response body. Same class as idx 7."}, {"title": "smb.Adapter.OnReconnect is a no-op stub never called \u2014 SMB lease reclaim on reconnect silently absent", "file": "/tmp/dittofs-audit/pkg/adapter/smb/adapter.go", "line": 575, "severity": "MED", "what": "OnReconnect(ctx, sessionID, clientID) is an exported method that only calls logger.Info and returns \u2014 no lease reclaim, no LockStore query. The comment explicitly states 'A full implementation would: 1. Query LockStore \u2026 2. Prepare them for reclaim \u2026 3. Notify client' but none of that code exists. More critically, grep across the entire codebase finds zero callers of this method in production code: it is exported but never invoked.", "why": "Stub passing as done. SMB clients that reconnect after server restart silently lose their held leases rather than reclaiming them during the grace window. This is a functional gap in SMB durable/persistent state recovery. The method being unreachable (no callers) means the gap is compounded \u2014 even if fixed internally, nothing drives it.", "fix": "Either remove the method if no caller exists (dead code) or wire it into the session reconnect path (session.Manager or the durable handle scavenger) and implement the LockStore enumeration described in the comment. At minimum add a //nolint:unused or explicit TODO issue reference so the gap is tracked; do not leave a documented stub as a public method that pretends to be functional.", "dimension": "slop", "area": "adapter-shells-auth-config", "confirmed": true, "verdict": "Confirmed: adapter.go:575 OnReconnect only calls logger.Info and returns. grep across entire codebase finds zero production callers of .OnReconnect \u2014 method is exported but never invoked."}, {"title": "dfs version prints wrong binary name at runtime", "file": "/tmp/dittofs-audit/cmd/dfs/commands/version.go", "line": 22, "severity": "MED", "what": "Runtime output of `dfs version` prints `dittofs <version>` instead of `dfs <version>`.", "why": "Copy-paste drift from a placeholder name. The binary is `dfs` (rootCmd.Use=\"dfs\"), so `dfs version` emits `dittofs 1.0.0` \u2014 misleading operator output every time.", "fix": "Change `fmt.Printf(\"dittofs %s\\n\", Version)` to `fmt.Printf(\"dfs %s\\n\", Version)` or `fmt.Printf(\"%s %s\\n\", cmd.Root().Name(), Version)`.", "dimension": "slop", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Line 22 confirmed: `fmt.Printf(\"dittofs %s\\n\", Version)` \u2014 binary is `dfs` (rootCmd.Use=\"dfs\") but version output hardcodes string `dittofs`. Fix: replace \"dittofs\" with \"dfs\"."}, {"title": "nfsv4_locking_test cross-protocol lock tests accept both outcomes \u2014 no hard assertion on actual lock conflict", "file": "/tmp/dittofs-audit/test/e2e/cross_protocol_lock_test.go", "line": 175, "severity": "MED", "what": "XPRO-01 through XPRO-03 and the byte-range tests (testOverlappingByteRangeLockConflict) use `if err != nil { t.Logf(...) } else { t.Logf(...) framework.UnlockFileRange(...) }` branching where BOTH the lock-succeeded and lock-blocked paths are treated as passing. Concretely, testOverlappingByteRangeLockConflict (line 599) says \"SMB lock on [0:512] unexpectedly acquired (platform-specific lock namespaces)\" and proceeds to just unlock and pass. The server never enforcing cross-protocol lock conflicts would pass this test silently.", "why": "These are supposed to be correctness tests for cross-protocol lock enforcement (XPRO-03, byte-range overlap). If the server has a bug where SMB ignores NFS byte-range locks, the test logs a non-fatal message and continues. This is fabricated-passing-assertion slop: the test can never fail even on known-broken behavior.", "fix": "For overlapping-range conflict tests, add a hard assert: `assert.ErrorIs(t, err, framework.ErrLockWouldBlock, \"overlapping cross-protocol lock must be blocked\")`. Document platform-specific exceptions explicitly with `t.Skip` rather than silently branching.", "dimension": "slop", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed lines 174-183 (XPRO-01), 599-605 (byte-range overlap). Both lock-conflict and lock-success paths only call t.Logf \u2014 no t.Errorf/t.Fatalf. Server never enforcing cross-protocol locks passes silently."}, {"title": "SMB password prompt abort silently proceeds with empty password", "file": "/tmp/dittofs-audit/cmd/dfsctl/commands/share/mount.go", "line": 166, "severity": "LOW", "what": "`resolveSMBPassword` calls `cmdutil.HandleAbort(err)` on password prompt error and returns `(\"\", nil)`. `HandleAbort` returns `nil` for Ctrl-C aborts. Caller `mountSMB` checks only `err != nil`, so on abort it proceeds to call `mountSMBLinux`/`mountSMBDarwin` with an empty password string instead of stopping.", "why": "User presses Ctrl-C at the password prompt expecting cancellation; instead the command continues and attempts an SMB mount with `password=''`, producing a confusing auth-failure mount error rather than a clean abort message.", "fix": "After `prompt.Password` returns, check if the returned password is empty AND err is nil (i.e. abort was swallowed): `if err != nil { return \"\", cmdutil.HandleAbort(err) }` is correct but `HandleAbort` masks the abort as nil. Fix by propagating abort directly: `if err != nil { if prompt.IsAborted(err) { fmt.Println(\"\\nAborted.\"); return \"\", fmt.Errorf(\"aborted\") }; return \"\", err }`. Or restructure to not use HandleAbort in functions that return (string, error).", "dimension": "bugs", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Confirmed. cmdutil.HandleAbort returns nil for prompt.IsAborted errors (util.go:295-300). resolveSMBPassword therefore returns (\"\", nil) on Ctrl-C. mountSMB in mount_unix.go:124-127 checks only err != nil, so proceeds to mountSMBLinux/mountSMBDarwin with empty password string."}, {"title": "Double process.Wait() race in WaitForExit + ForceKill", "file": "/tmp/dittofs-audit/test/e2e/helpers/server.go", "line": 234, "severity": "LOW", "what": "WaitForExit (line 234) and ForceKill (line 261) each spawn a goroutine calling sp.process.Wait() on the same os.Process. Tests that call WaitForExit (or StopGracefully which wraps it) AND register t.Cleanup(sp.ForceKill) \u2014 e.g. server_test.go testGracefulShutdownSIGTERM lines 146+157 \u2014 have two goroutines racing on the same Wait(). The second Wait() returns ECHILD; on some OS configurations this can also leave a zombie or trigger undefined behaviour in the Go runtime's internal process tracking.", "why": "os.Process.Wait() must be called at most once per process. Concurrent calls are a data race. Go's os package documents 'Wait waits for the Process to exit' with no concurrency guarantee. The race is reliably triggered whenever both StopGracefully and ForceKill are called for the same ServerProcess (ForceKill is deferred via t.Cleanup in all signal-shutdown tests).", "fix": "Add a sync.Once (or a 'waited' bool guarded by a mutex) inside ServerProcess so the first caller to Wait() wins and subsequent calls return immediately. Alternatively, after StopGracefully succeeds, nil out sp.process so ForceKill's early-return guard fires.", "dimension": "bugs", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. WaitForExit (line 234) and ForceKill (line 261) each spawn a goroutine calling sp.process.Wait(). server_test.go testGracefulShutdownSIGTERM registers t.Cleanup(sp.ForceKill) at line 146 and then calls sp.WaitForExit (via StopGracefully) at line 157 \u2014 two concurrent Wait() calls on the same os.Process."}, {"title": "Shared singleton holds stale *testing.T after first test ends", "file": "/tmp/dittofs-audit/test/e2e/framework/containers.go", "line": 114, "severity": "LOW", "what": "sharedLocalstackHelper and sharedPostgresHelper are package-level globals. NewLocalstackHelper/NewPostgresHelper store the first caller's *testing.T in the struct (field T). Every subsequent test reuses the singleton and therefore its T. After the first test finishes, any call to lh.T.Helper(), lh.T.Fatalf(), lh.T.Logf(), or lh.Cleanup() (line 247: lh.T.Helper()) operates on a completed test's T \u2014 this is explicitly forbidden by the testing package and causes panic 'panic: testing: t.Helper called after test finished'.", "why": "The testing package panics if Helper/Fatal/Log are called after the test function returns. With -parallel or any test ordering where the initialising test completes before a helper method is called on the singleton, the panic fires. The Cleanup() method at line 247 calls lh.T.Helper() unconditionally, so CleanupSharedContainers() will panic if called from TestMain after the first test has finished.", "fix": "Remove the T field from both helpers. Pass *testing.T as a parameter to every method that needs it (createClient, Cleanup, CleanupBucket, ListS3PrefixWithSizes) rather than storing it in the struct. The singleton pattern is fine; the stored T is the problem.", "dimension": "bugs", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. sharedLocalstackHelper/sharedPostgresHelper are package-level singletons storing the first caller's *testing.T. Subsequent tests reuse the singleton. Cleanup() at containers.go:247 calls lh.T.Helper() on the stale T. If called after first test finishes (e.g. from TestMain via CleanupSharedContainers path that terminates the container, or any test calling lh.Cleanup()), panics per testing package rules."}, {"title": "Shell injection in KDC AddPrincipal via unquoted password in bash -c", "file": "/tmp/dittofs-audit/test/e2e/framework/kerberos.go", "line": 161, "severity": "LOW", "what": "AddPrincipal builds a kadmin.local command via fmt.Sprintf with `password` interpolated unquoted into a shell string, then passes the whole string to `bash -c`. Any password containing shell metacharacters (`;`, `$()`, backticks, spaces, `\"`) executes arbitrary commands inside the KDC container.", "why": "The caller controls `password`; tests pass values like \"alice123\" today but the API is public and nothing prevents a caller passing `\"; arbitrary-cmd #`. Because the exec target is `bash -c cmd` the shell parses the entire interpolated string, not just the kadmin argument.", "fix": "Use kadmin.local's stdin (-stdin-password / -pass) or pass the add-princ query as a discrete argv element instead of a single shell string: `container.Exec(ctx, []string{\"kadmin.local\", \"-q\", fmt.Sprintf(\"addprinc -pw %s %s@%s\", password, principal, k.realm)})` still goes through bash but the outer quotes are now literal \u2014 better still, read the password via stdin: write the password to stdin and use `addprinc +needchange <principal>` or the `-stdin` flag so no shell interpolation is needed.", "dimension": "security", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. kerberos.go:161: cmd := fmt.Sprintf(\"kadmin.local -q \\\"addprinc -pw %s %s@%s\\\"\", password, principal, k.realm) \u2014 password and principal interpolated unquoted into bash -c string. Shell metacharacters in password (';', '$(...)', backticks) execute arbitrary commands in KDC container. Test/e2e code only, not production path. Fix: pass kadmin args as separate exec argv elements or properly shell-quote both fields."}, {"title": "SMBAuthenticator silently grants guest when user not found and NtChallengeResponse present", "file": "/tmp/dittofs-audit/internal/adapter/smb/auth/authenticator.go", "line": 202, "severity": "LOW", "what": "`handleNTLMAuthenticate`: when `userStore.GetUser` returns not-found (err != nil || user == nil), the code returns `AuthResult{IsGuest: true}` even when the client presented a full NTLMv2 `NtChallengeResponse`. An unknown user silently becomes a guest rather than receiving `STATUS_LOGON_FAILURE`.", "why": "The production `session_setup.go` path (line 1256-1260) correctly returns `StatusLogonFailure` for unknown users. This divergence means any code that wires `SMBAuthenticator` instead of the handler path would grant unauthenticated guest access to anyone who presents a valid (but unresolvable) username.", "fix": "Return an error (`ErrAuthenticationFailed` or equivalent) when `user == nil || !user.Enabled`, consistent with `session_setup.go:1256`. Only fall through to guest when the token itself is empty/anonymous (which is already handled above).", "dimension": "slop", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Confirmed: authenticator.go:201-206 returns IsGuest:true for unknown/not-found users even when NtChallengeResponse is present. However NewSMBAuthenticator is never called outside test files \u2014 not wired to production SMB server path. Bug exists in dead/test-only code. Downgrading severity: not exploitable in current production binary."}, {"title": "inlineFetchOrWait sparse-block (fb==nil) path doesn't signal inflight waiters correctly", "file": "/tmp/dittofs-audit/pkg/block/engine/fetch.go", "line": 306, "severity": "LOW", "what": "When `resolveFileBlock` returns `fb==nil` (sparse block), `inlineFetchOrWait` returns `(nil, true, nil)` without setting `completed=true`. The deferred `completeInFlight(key, result, completionErr=nil)` fires and signals all waiting goroutines with `err=nil`. Piggybacked waiters receive `(nil, false, nil)` \u2014 `downloaded=false` \u2014 and set `needLocalReadAt=true` in `EnsureAvailableAndRead`, triggering an extra local-read attempt that will miss and zero-fill via the slow path.", "why": "The direct caller correctly zero-fills because it sees `downloaded=true, data=nil` (line 247-249). But every waiter that piggybacked on this in-flight entry takes the `case <-existing.done` branch (line 276-281), reads `result.err=nil`, and returns `(nil, false, nil)`. `EnsureAvailableAndRead` then sets `needLocalReadAt=true` and returns `(false, nil)` instead of `(true, nil)`. The upper call chain in `ensureAndReadFromLocal` then calls `readLocalByHash` (miss) and zero-fills. The result is correct but requires an extra round-trip per piggybacked waiter on sparse blocks under concurrent read load.", "fix": "Set `completed = true` before the `return nil, true, nil` at line 307, then call `m.completeInFlight(key, result, nil)` explicitly (same pattern as the success path at lines 361-362). This signals waiters before returning, and they decode the nil-error / nil-data from `result` to mean 'sparse block, use zero-fill' \u2014 although they still see `(nil, false, nil)` on the current waiter-return path, the fix at minimum stops the deferred completeInFlight from racing the explicit one.", "dimension": "bugs", "area": "block-engine-core", "confirmed": true, "verdict": "Confirmed: fb==nil path at line 306-308 returns (nil, true, nil) but completed stays false, so deferred completeInFlight fires with err=nil. Waiters receive (nil, false, nil) from line 280, EnsureAvailableAndRead sets needLocalReadAt=true (line 242-244) instead of zero-filling directly. Extra local-read round-trip per waiter on sparse blocks; behavior correct but wasteful."}, {"title": "requestLeaseImplWithMode: mutex unlocked but lm.mu.Unlock() called anyway when grantState==None and no break dispatched (double-unlock via early loop break)", "file": "/tmp/dittofs-audit/pkg/metadata/lock/leases.go", "line": 556, "severity": "LOW", "what": "When the loop at line 492 hits the `break` at line 556 (\"conflict already satisfied\") or line 584 (\"already-breaking, suppressed\"), it exits without setting `breakDispatched=true` and without releasing `lm.mu`. Control falls to line 648 where `if breakDispatched` is false, so the re-lock is skipped (mutex is already held). `bestGrantableState` is called. If it returns `LeaseStateNone`, the code reaches line 656 `lm.mu.Unlock()` \u2014 which is correct (one unlock for the one lock). However, when there are multiple conflicting leases in the slice (e.g., two already-breaking leases with the same target), the `break` at line 584 exits after processing only the FIRST match. The second conflicting lease is never AND-merged into `BreakingToRequired`.", "why": "If two distinct lease keys on the same handleKey are both already-breaking and both conflict with the new opener, only the first one has its `BreakingToRequired` tightened via the AND-merge. The second lease retains its prior `BreakingToRequired` which may be too permissive for the new opener's target. The new opener's `bestGrantableState` then incorrectly evaluates against the first lease's tightened target while ignoring the second lease's stale target.", "fix": "Replace `break` at line 556 and line 584 with `continue` so all conflicting leases in the slice are processed. The `break` was likely intended to exit after the first fresh-dispatch (line 640, which correctly uses `break` since `mu` was released), but was accidentally applied to the no-dispatch paths too.", "dimension": "bugs", "area": "metadata-lock-manager", "confirmed": true, "verdict": "leases.go:575-584 \u2014 when loop hits already-breaking lease, AND-merges BreakingToRequired then breaks. Second conflicting already-breaking lease in same handleKey slice never has its BreakingToRequired tightened. Double-unlock claim in description is wrong (no double-unlock occurs). bestGrantableState sees all locks including the unmodified second one, so grantability is still computed conservatively. Real but mild: BreakingToRequired of second lease remains too permissive while first is correctly tightened."}, {"title": "badger transaction GetFilesystemStatistics counts all file types in usedSize instead of regular files only", "file": "/tmp/dittofs-audit/pkg/metadata/store/badger/transaction.go", "line": 1141, "severity": "LOW", "what": "GetFilesystemStatistics (lines 1118-1178) increments usedSize += file.Size for every f: entry without filtering on file.Type == metadata.FileTypeRegular. The atomic usedBytes counter (maintained in PutFile/DeleteFile) only counts regular files.", "why": "FSSTAT AvailableBytes diverges from the atomic counter path: the iterator path includes directories/symlinks/devices while the atomic path does not. Directories have Size=0 today but the divergence is a latent correctness gap and symlinks may have non-zero Size in some backends. Also introduces O(n) scan per stats call inside a write transaction, which blocks other writers.", "fix": "Add if file.Type != metadata.FileTypeRegular { continue } before fileCount++ and usedSize += file.Size at line 1141. Better: use s.usedBytes.Load() from the atomic counter (same value without a scan) and compute fileCount-only from the iterator.", "dimension": "bugs", "area": "metadata-store-memory-badger", "confirmed": true, "verdict": "badger/transaction.go:1129-1149 GetFilesystemStatistics iterates all f: prefix entries accumulating file.Size without filtering file.Type==FileTypeRegular. initUsedBytesCounter (store.go:430) and PutFile (transaction.go:180) filter to regular files only. Divergence confirmed. Practical impact limited today since dirs have Size=0, but latent correctness gap for symlinks/other types with non-zero Size."}, {"title": "poolRow connection leak when Scan is never called", "file": "/tmp/dittofs-audit/pkg/metadata/store/postgres/pool_helpers.go", "line": 174, "severity": "LOW", "what": "queryRow acquires a pgxpool.Conn and wraps it in poolRow. poolRow.Scan() is the ONLY place conn.Release() is called. Unlike pgxpool's native QueryRow (which releases on rows.Close() via pgx internals), this custom wrapper has no fallback release. If any caller receives the poolRow and returns without calling Scan \u2014 for example if a pre-Scan nil-check fires, or if a future refactor introduces an early return \u2014 the connection is permanently leaked until the pool times it out. Currently every call site does call Scan, but there is no structural guarantee (no finalizer, no defer).", "why": "A single leaked connection under the 10-connection default pool blocks all subsequent pool acquisitions after 10 leaks, hanging the server.", "fix": "Add a sync.Once-guarded release in poolRow so the connection is released on the first of {Scan call, explicit Close call}. Expose a Close() method on poolRow and defer it at call sites: `row := s.queryRow(...); defer row.Close(); err := row.Scan(...)`. Or replace the custom wrapper with pgxpool's native QueryRow which handles this correctly.", "dimension": "bugs", "area": "metadata-store-postgres", "confirmed": true, "verdict": "pool_helpers.go:174 poolRow.Scan() is the only release site for conn. No defer fallback. Any caller that returns before calling Scan (future refactor, nil-guard, etc.) permanently leaks the connection. Currently all call sites invoke Scan, so not a live leak, but structural guarantee absent."}, {"title": "migrate_to_cas walkDir infinite loop risk on deeply-nested or cyclic metadata", "file": "/tmp/dittofs-audit/cmd/dfs/commands/migrate_to_cas.go", "line": 295, "severity": "LOW", "what": "`walkDir` recurses without tracking visited directory handles. If the metadata store contains a cycle (corrupt or hand-crafted data where a child directory's handle equals an ancestor), the function recurses indefinitely until stack overflow. The metadata store contract doesn't guarantee cycle-freedom for corrupted data.", "why": "Migration is an offline recovery tool run against potentially-corrupted stores. A stack overflow kills the migration process leaving the journal in an unknown state. The data is user-owned legacy content that may have been written by a buggy version.", "fix": "Maintain a `map[string]struct{}` of visited handles (using handle bytes as key) and return an error on re-visit: `if _, seen := visited[string(dirHandle)]; seen { return fmt.Errorf(\"cycle detected at handle %x\", dirHandle) }`.", "dimension": "bugs", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "Confirmed structurally: walkDir at line 295 recurses without visited-set. Go stack overflow on cycle. However severity is LOW \u2014 badger is a real store backed by a filesystem; cycles require corrupt data and this is an offline migration tool. Context cancellation (line 298) helps for other stuck cases but not for cycles. Real bug, reduced severity."}, {"title": "Mount temp directory leaked when raw umount used without Cleanup()", "file": "/tmp/dittofs-audit/test/e2e/nfsv41_disconnect_test.go", "line": 56, "severity": "LOW", "what": "The cleanup registered at line 56 runs only `exec.Command(\"umount\", \"-f\", mount.Path).Run()` and discards its return value. It does not call `os.RemoveAll(mount.Path)` nor `mount.Cleanup()`. The TempDir created by MountNFSWithVersion (via os.MkdirTemp) is therefore never removed. On repeated test runs this leaves stale directories in /tmp.", "why": "Mount.Cleanup() calls both Unmount() and os.RemoveAll(). By bypassing it with a raw umount the directory removal step is skipped. This causes /tmp accumulation across test runs and can confuse CleanupStaleMounts() which glob-matches these patterns.", "fix": "Replace the inline cleanup with `t.Cleanup(mount.Cleanup)` (same pattern used by every other test in the file). If force-unmount is specifically needed, override the unmount inside a Cleanup func that still calls os.RemoveAll(mount.Path) afterwards.", "dimension": "bugs", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed. nfsv41_disconnect_test.go:56: cleanup func calls only exec.Command(\"umount\", \"-f\", mount.Path).Run() with error discarded. No os.RemoveAll(mount.Path), no mount.Cleanup(). TempDir from os.MkdirTemp is never removed. /tmp accumulation across test runs."}, {"title": "GSS token mechanism OID not verified \u2014 foreign-mechanism tokens silently accepted", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/framework.go", "line": 202, "severity": "LOW", "what": "extractAPReq() parses the GSS-API initial context token but explicitly skips OID verification: '// Skip the OID bytes (we could verify it's the krb5 OID, but we trust the caller)'. Any 0x60-tagged token with any OID value is accepted; the raw bytes after the token ID are passed to kerbService.Authenticate().", "why": "The GSS-API OID identifies the security mechanism (RFC 2743 Section 3.1). Skipping OID verification violates RFC 2743 and RFC 4121. A client presenting a non-krb5 OID (e.g., SPNEGO, NTLM, or a crafted unknown OID) can force the server to attempt Kerberos parsing on arbitrary bytes. While the Kerberos parser will likely reject them, this also opens the door to mechanism-confusion attacks if a second GSS mechanism is ever added, and it makes the security boundary ambiguous.", "fix": "After reading oidLen and the OID bytes, compare them against kerbauth.KerberosV5OIDBytes (the 9-byte DER encoding of 1.2.840.113554.1.2.2). Reject with an error if they do not match. Remove the comment about trusting the caller.", "dimension": "security", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed OID skip at gss/framework.go:202. However, token ID check at line 216-218 validates `tokenID == kerbauth.GSSTokenIDAPReq` (0x0100), which rejects non-krb5 tokens before Kerberos parsing. OID skip is RFC 2743 violation and mechanism-confusion risk if a second GSS mechanism is added, but current exploitability is constrained by the token ID gate. Severity downgraded to LOW."}, {"title": "Auth cache key collision: share name containing ':' + digits can alias another share+credential tuple", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/doc.go", "line": 37, "severity": "LOW", "what": "authCacheKey builds a string by writing ctx.Share directly then appending ':'-prefixed uint values. No separator distinguishes the end of the share name from the start of the AuthFlavor field. A share named \"myshare:0\" with AuthFlavor=4294967295, UID=nil, GID=nil produces the same key as share \"myshare\" with AuthFlavor=0, UID=nil, GID=nil. The cached AuthContext (including ShareReadOnly, Identity with effective UID/GID after squash) from one credential set is returned for a different one.", "why": "If an operator has shares whose names contain ':' + digits, a client connecting as one (share, credential) pair can receive a cached AuthContext built for a different credential set \u2014 potentially one with ShareReadOnly=false instead of true, or with a non-squashed identity. Exploitability is constrained by attacker needing to control or predict share names.", "fix": "Use a length-prefixed or fixed-width encoding for the share name, e.g. fmt.Sprintf(\"%d:%s\", len(ctx.Share), ctx.Share) as the opening segment, or separate fields with a byte that cannot appear in a share name (e.g. '\\x00'). Alternatively use a struct as key and a sync.Map[authCacheKeyStruct, *metadata.AuthContext] with a properly typed struct key.", "dimension": "security", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed structurally. authCacheKey writes share name raw then appendUint writes ':<n>' for each subsequent field. Share name containing ':digits' can collide with different (share, credential) tuple. e.g. share 's:1:2' + AuthFlavor=3 + nil UID/GID produces same prefix as share 's' + AuthFlavor=1 + UID=2 + GID=3. Exploitability requires attacker-controlled or predictable share names with ':digit' patterns. Real but constrained; severity LOW."}, {"title": "Internal connection error details leaked in 400 response for metadata store Create", "file": "/tmp/dittofs-audit/internal/controlplane/api/handlers/metadata_stores.go", "line": 90, "severity": "LOW", "what": "`BadRequest(w, \"Failed to create metadata store: \"+err.Error())` forwards the raw Go error string from `CreateMetadataStoreFromConfig` to the HTTP response. For postgres connections this includes the target host:port, TLS negotiation details, and driver-level messages. For badger it includes filesystem paths and OS error strings.", "why": "Internal topology (private hostnames, IP addresses, port numbers, TLS cert subjects) is disclosed to the calling admin. In a multi-tenant operator deployment this leaks infrastructure details beyond what the calling user should see. Also covered by the SSRF finding above \u2014 they compound.", "fix": "Log `err` at Error level internally; return only a sanitized message to the caller: `BadRequest(w, \"Failed to create metadata store: invalid configuration\")`. Callers can inspect server logs for diagnostics.", "dimension": "security", "area": "controlplane-api-rest", "confirmed": true, "verdict": "Confirmed. metadata_stores.go:90: BadRequest(w, \"Failed to create metadata store: \"+err.Error()) forwards raw Go error string to HTTP response. For postgres includes host:port, TLS details; for badger includes filesystem paths and OS errors. Admin-only endpoint limits exposure but still leaks internal topology. Severity LOW given admin-only access."}, {"title": "TestCLI_List_TableMode: asserts truncated 8-char ID with fragile substring check that always passes regardless of actual truncation", "file": "/tmp/dittofs-audit/test/e2e/snapshot/snapshot_cli_test.go", "line": 309, "severity": "LOW", "what": "The seeded ID is \"snap-cli-list-1\" (16 chars). The test asserts `strings.Contains(res.stdout, \"snap-cli\")` (line 309) and that the full ID does NOT appear (line 312). But \"snap-cli\" is a prefix of \"snap-cli-list-1\"; if the table does NOT truncate (a bug), the full ID appears in stdout AND \"snap-cli\" still matches, so the first assert still passes. The second assert (`!strings.Contains(stdout, \"snap-cli-list-1\")`) would catch this, but only if the full ID appears verbatim. If the formatter pads/wraps columns or uses ellipsis, neither assertion fires correctly.", "why": "The negative assertion (full ID must NOT appear) is the real gate for truncation correctness. Using a 16-char ID where the 8-char prefix is indistinguishable from the truncated form means the positive assertion adds no signal \u2014 it can never distinguish between truncated and non-truncated output. The test is weak: a regression where truncation is removed would still pass the positive check.", "fix": "Use an ID like \"snap-table-list-very-long-id-001\" where the 8-char form (\"snap-tab\") does not prefix-match the full ID in an obvious way, and assert both presence of \"snap-tab\" AND absence of \"snap-table-list\". Or assert exact column widths using a regex.", "dimension": "slop", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed at lines 309/312. Seeded ID is \"snap-cli-list-1\" (15 chars). Positive assert checks \"snap-cli\" which is a prefix of full ID \u2014 zero signal. Negative assert at line 312 is the only real gate. If formatter wraps/pads with ellipsis the negative check also passes silently. Weakness is real."}, {"title": "qcow2_fixture: SHA256 pin is a placeholder sentinel \u2014 integrity check is permanently bypassed", "file": "/tmp/dittofs-audit/test/e2e/framework/qcow2_fixture.go", "line": 34, "severity": "LOW", "what": "qcow2BaseSHA256 = \"<freeze-at-first-run>\" (a placeholder). When this placeholder is active (as it is in the committed code), DownloadQcow2Base skips the integrity check on both cached and freshly downloaded files (lines 74, 126\u2013129). Any downloaded file \u2014 including a corrupted or tampered one \u2014 is accepted. The \"freeze-at-first-run\" design was never completed; the constant was never replaced with a real digest.", "why": "The qcow2 fixture is used by TestDEDUP03_VMFleet40Pct to generate the 40% dedup gate. Without a pinned SHA256, a CDN rotation, man-in-the-middle, or network error produces a silently wrong base image, causing the dedup test to pass or fail on bogus data. This is a fabricated safety check that never actually runs.", "fix": "Download the image once, compute its SHA256, and replace the placeholder with the real hex digest. Add a CI step that fails if the placeholder is still present.", "dimension": "slop", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed: `qcow2BaseSHA256 = \"<freeze-at-first-run>\"` (line 34). Lines 74, 77-80 skip the fatal SHA check when placeholder is active \u2014 cached file accepted without integrity verify. Design was never completed."}, {"title": "ReleaseSessionLeases primary-session re-election scans already-removed sessionMap entries", "file": "/tmp/dittofs-audit/internal/adapter/smb/lease/manager.go", "line": 517, "severity": "LOW", "what": "Phase 1 (lines 465-480) collects keys to release under `RLock`. Phase 1.5 (lines 483-491) calls `ReleaseLease` for each key, which calls `removeLeaseMapping` \u2014 removing the session's keys from `lm.sessionMap` under `Lock`. Phase 2 (lines 504-531) then acquires `Lock` and scans `lm.leaseClientGUID` for surviving candidates, reading `lm.sessionMap[clk.KeyHex]` to get `candidateSID`. The guard `candidateSID == sessionID` (line 518) is now dead: the released session's keys were already removed from `sessionMap` by phase 1.5, so `ok` is always false for them. If a concurrent goroutine calls `RequestLease` for the same `clientID/leaseKey` with a new session between phases 1.5 and 2, that new session's `sessionMap` entry could appear as a candidate \u2014 potentially electing a session that belongs to a different reconnect of the same client as 'primary' ahead of an older surviving session.", "why": "In steady-state this is harmless (the check correctly excludes absent entries via `!ok`). Under a tight race \u2014 concurrent durable reconnect of the same ClientGUID arriving between the unlock of phase 1.5 and the lock of phase 2 \u2014 the newly-registered session for the reconnect can be elected primary even though an older surviving session of the same GUID exists and should be preferred. Break notifications then route to the reconnect session instead of the oldest surviving session, violating the `client->connections` head semantics. Low probability in practice.", "fix": "Snapshot the set of released keys before phase 1.5 (already have it in `keysToRelease`), then in phase 2 explicitly skip any `clk.KeyHex` that appears in the released-key set rather than relying on the `sessionMap` absence check. Alternatively, hold `lm.mu.Lock()` across both phase 1.5 (ReleaseLease body) and phase 2, but that requires splitting ReleaseLease into a locked variant.", "dimension": "bugs", "area": "smb-lease-oplock-notify", "confirmed": true, "verdict": "Confirmed: phase 1.5 (line 484) calls ReleaseLease which removes session's keys from sessionMap. Phase 2 (line 517) reads sessionMap for those same keys \u2014 `ok` is always false for released session's entries. The guard `candidateSID == sessionID` at line 518 is dead for those. Race window exists between phase 1.5 unlock and phase 2 lock for reconnecting sessions."}, {"title": "serverBootTime initialized at package init, not at server start \u2014 write verifier may be stale", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v3/handlers/verf.go", "line": 11, "severity": "LOW", "what": "`serverBootTime = uint64(time.Now().Unix())` runs at package initialization. If the binary is imported (e.g. in tests or as a library) and later restarted without reloading the package, the verifier does not change. More practically: if the Go runtime loads the package seconds before the NFS listener actually opens, the verifier does not reflect the true server-start time.", "why": "RFC 1813 \u00a73.3.21 requires the write verifier to change on any server restart so clients can detect lost unstable writes. A stale verifier causes clients to believe their UNSTABLE writes survived a restart when they did not.", "fix": "Initialize `serverBootTime` when the Handler is constructed or when the NFS adapter starts listening, not at package init time. Expose a `SetBootTime(t uint64)` function or pass it as a Handler field set in the adapter's `Start()` method.", "dimension": "bugs", "area": "nfs-v3-handlers", "confirmed": true, "verdict": "Confirmed: `var serverBootTime = uint64(time.Now().Unix())` at package init (line 11). Fires at binary load, not at NFS listener start. If package is loaded seconds before listener opens, verifier is stale relative to actual server-start time."}, {"title": "lookupLockErrorRow: missing engine.ErrStoreClosed sentinel \u2014 lock ops on closed store return generic fault instead of stale-handle", "file": "/tmp/dittofs-audit/internal/adapter/common/lock_errmap.go", "line": 91, "severity": "LOW", "what": "lookupLockErrorRow does not have the engine.ErrStoreClosed early-exit that lookupErrorRow has (errmap.go:267-269). A LOCK/UNLOCK operation that hits a closed store (share removed mid-op) falls through to defaultCodes: NFS3ErrIO / NFS4ERR_SERVERFAULT / StatusInternalError instead of the stale-handle row (NFS3ErrStale / NFS4ERR_STALE / StatusFileClosed).", "why": "Clients receiving SERVERFAULT on a lock op retry indefinitely with backoff. Clients receiving STALE immediately re-mount/reopen. The asymmetry between the general and lock-context translators causes protocol-level confusion for the edge case of share hot-removal during an active lock operation.", "fix": "Add the same sentinel check at the top of lookupLockErrorRow: `if goerrors.Is(err, engine.ErrStoreClosed) { return errorMap[merrs.ErrStaleHandle] }` before the goerrors.As call, mirroring lookupErrorRow lines 267-269.", "dimension": "bugs", "area": "internal-adapter-common", "confirmed": true, "verdict": "Confirmed: lookupLockErrorRow (lock_errmap.go:91) has no engine.ErrStoreClosed early-exit. lookupErrorRow (errmap.go:267) does. Lock ops on closed store fall through to defaultCodes (SERVERFAULT/NFS3ErrIO) instead of stale-handle row. Fix: add goerrors.Is(err, engine.ErrStoreClosed) guard at top of lookupLockErrorRow."}, {"title": "extractNewLogs silently returns empty string if log shrinks", "file": "/tmp/dittofs-audit/test/e2e/nfsv4_delegation_test.go", "line": 486, "severity": "LOW", "what": "extractNewLogs compares len(logAfter) > len(logBefore) and returns empty string if the condition is false. If the log file is truncated between the two reads (e.g. log rotation, server restart that truncates the file), len(logAfter) < len(logBefore) and the function silently returns \"\". All callers then log 'no replay/session activity detected' as a non-failure warning, masking real test infrastructure failures.", "why": "Several tests (TestNFSv41EOSReplayOnReconnect, TestNFSv41SessionEstablishment, delegation tests) use extractNewLogs to detect evidence of server behaviour. A silent empty return causes the test to pass with a warning rather than surfacing the truncation. The condition should at minimum log a warning when logAfter is shorter.", "fix": "Add an explicit check: if len(logAfter) < len(logBefore) { t.Logf(\"WARNING: log file shrank (%d -> %d bytes), possible truncation\", ...) }. Return the full logAfter in that case so callers still see whatever content is there.", "dimension": "bugs", "area": "e2e-test-framework", "confirmed": true, "verdict": "Confirmed: extractNewLogs (line 487) returns \"\" silently when len(logAfter) <= len(logBefore). All callers treat empty return as non-fatal. Log rotation or server restart that truncates the file masks real test infrastructure failures."}, {"title": "integrity.go package comment swaps InitiatorSign/AcceptorSign numeric values", "file": "/tmp/dittofs-audit/internal/adapter/nfs/rpc/gss/integrity.go", "line": 12, "severity": "LOW", "what": "Package-level comment states 'For client->server: KeyUsageInitiatorSign (23)' and 'For server->client: KeyUsageAcceptorSign (25)'. Per RFC 4121 Section 2 and the definitions in types.go, KeyUsageInitiatorSign=25 and KeyUsageAcceptorSign=23 \u2014 the parenthetical numeric values are swapped.", "why": "Code uses the named constants so the crypto is correct. However the wrong inline numbers will mislead reviewers into thinking the constants are wrong and could cause a genuine swap if anyone replaces a constant reference with a literal. Audit tools and future contributors will be confused.", "fix": "Change comment to: 'For client->server: KeyUsageInitiatorSign (25)' and 'For server->client: KeyUsageAcceptorSign (23)'.", "dimension": "security", "area": "nfs-rpc-gss-xdr", "confirmed": true, "verdict": "Confirmed swap: types.go defines KeyUsageAcceptorSign=23 and KeyUsageInitiatorSign=25. integrity.go package comment (lines 12-13) states InitiatorSign=(23) and AcceptorSign=(25) \u2014 both parenthetical values inverted. Code uses named constants so crypto is correct, but comment misleads reviewers and any literal substitution would break the key usage direction."}, {"title": "pkg/adapter/nfs/dispatch.go sends PROC_UNAVAIL instead of PROG_UNAVAIL for unknown program number", "file": "/tmp/dittofs-audit/pkg/adapter/nfs/dispatch.go", "line": 206, "severity": "LOW", "what": "The default branch of the program switch in handleRPCCall (outer NFSConnection dispatch) sends rpc.RPCProcUnavail (accept_stat=3) for an unrecognised program number. RFC 5531 \u00a79 requires PROG_UNAVAIL (accept_stat=1) for this case. The inner consolidated Dispatch function at internal/adapter/nfs/dispatch.go:203 correctly uses RPCProgUnavail.", "why": "Sends a protocol-non-conformant error code. Well-behaved NFS clients use the accept_stat to decide how to retry; PROC_UNAVAIL suggests the program exists but the procedure is missing, which may cause incorrect client retry behaviour or confuse debugging tools.", "fix": "Change line 206 from rpc.RPCProcUnavail to rpc.RPCProgUnavail to match RFC 5531 and be consistent with the inner dispatch at internal/adapter/nfs/dispatch.go:203.", "dimension": "security", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "pkg/adapter/nfs/dispatch.go:206 confirmed: default branch sends RPCProcUnavail for unknown program. internal/adapter/nfs/dispatch.go:203 correctly sends RPCProgUnavail. RFC 5531 requires PROG_UNAVAIL(1) not PROC_UNAVAIL(3) for unrecognised program."}, {"title": "Modular bias in generateRandomSecret JWT key generation", "file": "/tmp/dittofs-audit/k8s/dittofs-operator/internal/controller/dittoserver_controller.go", "line": 712, "severity": "LOW", "what": "`result[i] = chars[int(randomBytes[i])%len(chars)]` \u2014 randomBytes[i] is uint8 (0-255), len(chars)=62. 256 is not a multiple of 62 (256 mod 62 = 8), so chars[0]-chars[7] are selected with probability 5/256 instead of 4/256. Over 32 bytes the bias is small (~0.4%) but the generated string is used as a JWT HMAC-SHA256 signing secret, not just a password.", "why": "Modular reduction of uniform bytes over a non-power-of-2 alphabet introduces non-uniform distribution. Standard fix is rejection sampling. auth_reconciler.go generateRandomPassword correctly uses base64 of raw bytes (no modulo), so the JWT path is inconsistent with the password path.", "fix": "Use the same pattern as generateRandomPassword: `base64.URLEncoding.EncodeToString(randomBytes)` for 32+ bytes, or implement rejection sampling: regenerate any byte where value >= 62*(256/62).", "dimension": "security", "area": "k8s-operator", "confirmed": true, "verdict": "dittoserver_controller.go:712 confirmed: chars len=62, randomBytes[i] is uint8 (0-255), 256%62=8 \u2192 first 8 chars slightly over-represented. auth_reconciler.go generateRandomPassword uses base64.URLEncoding (no modulo) \u2014 inconsistency confirmed."}, {"title": "`uniqueStrings` copy-pasted verbatim across two packages", "file": "/tmp/dittofs-audit/internal/adapter/smb/handlers/session_setup.go", "line": 1761, "severity": "LOW", "what": "Identical `uniqueStrings` function body in `auth/authenticator.go:313` and `handlers/session_setup.go:1761`. The test file adds a third copy renamed `uniqueStringsTest` (ntlmv2_crossvalidation_test.go:191).", "why": "Textbook AI copy-paste slop. Any future fix to the dedup logic must be applied to multiple sites; divergence has already occurred (test copy has different name). Both production copies are in the same module \u2014 one should be unexported from a shared location.", "fix": "Move `uniqueStrings` to the `auth` package (already there), delete the duplicate in `handlers/session_setup.go`, import and call `auth.UniqueStrings` (exported) or inline the simpler `slices.Compact(slices.Sorted(...))` idiom.", "dimension": "slop", "area": "smb-session-negotiate-auth", "confirmed": true, "verdict": "Identical uniqueStrings body confirmed in auth/authenticator.go:313 and handlers/session_setup.go:1761. Test file auth/ntlmv2_crossvalidation_test.go:191 has uniqueStringsTest (renamed copy). Three copies total."}, {"title": "Misplaced doc comment: TestBreakParentHandleLeasesOnCreate_WaitsForAck header prepended to TestMarkLeaseVersionIfUnset_StickyV2", "file": "/tmp/dittofs-audit/internal/adapter/smb/lease/manager_test.go", "line": 84, "severity": "LOW", "what": "Lines 84-90 are the doc comment for TestBreakParentHandleLeasesOnCreate_WaitsForAck (which actually appears at line 393), but are attached as the leading comment of TestMarkLeaseVersionIfUnset_StickyV2 (line 95). Comment text begins '// TestBreakParentHandleLeasesOnCreate_WaitsForAck asserts that...' and describes break-sequencing semantics, not version stickiness.", "why": "Copy-paste slop: an AI generated the TestMarkLeaseVersionIfUnset_StickyV2 function and prepended the wrong function's comment block. The mismatch means godoc/tooling surfaces incorrect documentation for TestMarkLeaseVersionIfUnset_StickyV2, and the actual TestBreakParentHandleLeasesOnCreate_WaitsForAck (line 393) has no leading comment at all.", "fix": "Remove lines 84-90 from above TestMarkLeaseVersionIfUnset_StickyV2 and replace with the correct one-liner: '// TestMarkLeaseVersionIfUnset_StickyV2: a lease first marked V2 stays V2 even when MarkLeaseVersionIfUnset is later called with isV2=false.' (which is already present as lines 91-94). Then add the displaced comment block above TestBreakParentHandleLeasesOnCreate_WaitsForAck at line 393.", "dimension": "slop", "area": "smb-lease-oplock-notify", "confirmed": true, "verdict": "manager_test.go:84-94 confirmed: comment block starting '// TestBreakParentHandleLeasesOnCreate_WaitsForAck asserts that...' is attached as preamble to TestMarkLeaseVersionIfUnset_StickyV2 (line 95). Actual TestBreakParentHandleLeasesOnCreate_WaitsForAck (~line 393) has no leading doc comment."}, {"title": "HandleSMB1Negotiate: SecurityBufferOffset off-by-one (128 vs 129)", "file": "/tmp/dittofs-audit/internal/adapter/smb/response.go", "line": 1273, "severity": "LOW", "what": "SecurityBufferOffset written as 128 with comment '64 (header) + 64 (fixed body)'. Body is `respBody = make([]byte, 65)` (StructureSize=65 per MS-SMB2 \u00a72.2.4), so offset should be 64+65=129 (0x81), not 128 (0x80). Wrong value copied from a plausible-but-incorrect mental model where NEGOTIATE response body is 64 bytes.", "why": "Per MS-SMB2 \u00a72.2.4, NEGOTIATE response SecurityBufferOffset must point to the first byte of the security buffer, which immediately follows the 65-byte fixed NEGOTIATE response body. SecurityBufferLength is 0 in this SMB1-upgrade path so no client dereferences the offset today \u2014 functionally harmless \u2014 but the value is provably wrong and will silently corrupt any future attempt to append a real security buffer to this response path.", "fix": "Change `binary.LittleEndian.PutUint16(respBody[56:58], 128)` to `binary.LittleEndian.PutUint16(respBody[56:58], 129)` and fix the comment to '64 (header) + 65 (fixed body) = 129'.", "dimension": "slop", "area": "smb-framing-dispatch-compound", "confirmed": true, "verdict": "response.go:1273 confirmed: respBody=make([]byte,65), SecurityBufferOffset written as 128 with comment '64 (header) + 64 (fixed body)'. Should be 64+65=129. Comment and value both wrong."}, {"title": "OPEN CLAIM_DELEGATE_CUR encodes beforeCtime as afterCtime in change_info4 (no post-op lookup)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/open.go", "line": 681, "severity": "LOW", "what": "handleOpenClaimDelegateCur calls encodeOpenResult with (beforeCtime, beforeCtime, nil) \u2014 both before and after ctimes are identical. Unlike CLAIM_NULL (line 389 which computes a real afterCtime) and CLAIM_PREVIOUS (which uses 0,0 with a comment), this path never fetches post-op parent attributes, so change_info4.after always equals change_info4.before even when the OPEN causes a state change.", "why": "Copy-paste from CLAIM_PREVIOUS zeroed-change_info but without the comment justifying it. For CLAIM_DELEGATE_CUR the current FH is the file (not newly created), so atomic=true with before==after is technically defensible, but omitting the post-op lookup is an inconsistency that could mislead clients doing attribute caching or delegation tracking.", "fix": "Either fetch the post-op parent attributes like CLAIM_NULL does and pass the real afterCtime, or pass (0, 0) and add a comment like CLAIM_PREVIOUS explaining the reclaim path intentionally returns zero change_info.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "open.go:681 confirmed: handleOpenClaimDelegateCur calls encodeOpenResult with (beforeCtime, beforeCtime, nil) \u2014 no post-op parent lookup, afterCtime==beforeCtime always. CLAIM_NULL path at line 389 computes real afterCtime; CLAIM_PREVIOUS at 463 uses 0,0 with comment justifying it. This path has neither."}, {"title": "REMOVE doc comment wrong RFC 7530 section number (16.25 vs correct 16.26)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/remove.go", "line": 17, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.25' for REMOVE. RFC 7530 \u00a716.25 is READDIR; REMOVE is \u00a716.26. Section 16.25 is also claimed by read.go for READ, which is also wrong (READ is \u00a716.23).", "why": "Copy-paste drift in AI-generated docstrings. Three operations claim Section 16.25: REMOVE (remove.go:17), READ (read.go:17), and by association the link/setattr cluster. False RFC citations make audit and protocol compliance verification harder.", "fix": "Change '// handleRemove implements the REMOVE operation (RFC 7530 Section 16.25).' to '// handleRemove implements the REMOVE operation (RFC 7530 Section 16.26).'", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "remove.go:17 confirmed: '// handleRemove implements the REMOVE operation (RFC 7530 Section 16.25).' RFC 7530 \u00a716.25 is READDIR; REMOVE is \u00a716.26."}, {"title": "READ doc comment wrong RFC 7530 section number (16.25 vs correct 16.23)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/read.go", "line": 17, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.25' for READ. RFC 7530 \u00a716.25 is READDIR; READ is \u00a716.23. Also references '16.25.4' on line 90 where the share_access enforcement paragraph is actually \u00a716.23.4.", "why": "Copy-paste drift producing conflicting RFC citations across files.", "fix": "Change '(RFC 7530 Section 16.25)' to '(RFC 7530 Section 16.23)' on line 17, and '16.25.4' to '16.23.4' on line 90.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "read.go:17 confirmed: 'RFC 7530 Section 16.25' for READ; also line 90 cites '16.25.4'. READ is \u00a716.23. Same wrong section as remove.go."}, {"title": "LINK doc comment wrong RFC 7530 section number (16.11 vs correct 16.9)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/link.go", "line": 15, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.11' for LINK. RFC 7530 \u00a716.11 is LOCKT; LINK is \u00a716.9.", "why": "Copy-paste drift \u2014 likely copied from lock.go's LOCKT handler.", "fix": "Change '(RFC 7530 Section 16.11)' to '(RFC 7530 Section 16.9)'.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "link.go:15 confirmed: 'RFC 7530 Section 16.11' for LINK. RFC 7530 \u00a716.11 is LOCKT; LINK is \u00a716.9."}, {"title": "SETATTR doc comment wrong RFC 7530 section number (16.34 vs correct 16.32)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/setattr.go", "line": 19, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.34' for SETATTR. RFC 7530 \u00a716.34 is SETCLIENTID_CONFIRM; SETATTR is \u00a716.32.", "why": "Copy-paste drift from setclientid.go's confirm handler which correctly cites 16.34.", "fix": "Change '(RFC 7530 Section 16.34)' to '(RFC 7530 Section 16.32)'.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "setattr.go:19 confirmed: 'RFC 7530 Section 16.34' for SETATTR. RFC 7530 \u00a716.34 is SETCLIENTID_CONFIRM; SETATTR is \u00a716.32."}, {"title": "SECINFO doc comment wrong RFC 7530 section number (16.33 vs correct 16.31)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/secinfo.go", "line": 31, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.33' for SECINFO. RFC 7530 \u00a716.33 is SETCLIENTID; SECINFO is \u00a716.31. Also line 56 and 107 reference '16.31.4' and '16.31' which are correct for the consuming-CurrentFH semantics but differ from the function-level claim.", "why": "The function header (line 31) is off by two sections from both the inline references and the actual RFC.", "fix": "Change line 31 '(RFC 7530 Section 16.33)' to '(RFC 7530 Section 16.31)'.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "secinfo.go:31 says 'RFC 7530 Section 16.33' for SECINFO. Lines 56 and 107 in the same file correctly cite \u00a716.31/\u00a716.31.4. RFC 7530 \u00a716.33 is SETCLIENTID. Function-level header is wrong by 2 sections."}, {"title": "RELEASE_LOCKOWNER doc comment wrong RFC 7530 section number (16.34 vs correct 16.37)", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/handlers/stubs.go", "line": 145, "severity": "LOW", "what": "Comment says 'RFC 7530 Section 16.34' for RELEASE_LOCKOWNER. RFC 7530 \u00a716.34 is SETCLIENTID_CONFIRM; RELEASE_LOCKOWNER is \u00a716.37.", "why": "Copy-paste drift.", "fix": "Change '(RFC 7530 Section 16.34)' to '(RFC 7530 Section 16.37)'.", "dimension": "slop", "area": "nfs-v4-handlers-ops", "confirmed": true, "verdict": "stubs.go:145 says 'RFC 7530 Section 16.34' for RELEASE_LOCKOWNER. \u00a716.34 is SETCLIENTID_CONFIRM; RELEASE_LOCKOWNER is \u00a716.37. Copy-paste drift confirmed."}, {"title": "handleNFSNull uses NFS3ErrAccess as fallback error status \u2014 wrong for NULL procedure", "file": "/tmp/dittofs-audit/internal/adapter/nfs/dispatch_nfs.go", "line": 157, "severity": "LOW", "what": "`handleNFSNull` passes `types.NFS3ErrAccess` (13) as the `errorStatus` argument to `handleRequest`. RFC 1813 \u00a73.3.1: NULL takes no arguments and always returns NFS3_OK. The fallback status is unreachable in practice (`DecodeNullRequest` always succeeds, `handler.Null` always returns NFS3_OK), but the value is semantically wrong and is copy-paste drift from adjacent read/access handlers.", "why": "Copy-paste drift: `handleNFSNull` and `handleNFSGetAttr`/`handleNFSLookup`/`handleNFSAccess` all use `NFS3ErrAccess` as their hardcoded fallback. For NULL, GETATTR, SETATTR the correct fallback is `NFS3ErrIO` (I/O failure) or `NFS3ErrServerfault`; `NFS3ErrAccess` implies a permission denial which cannot arise from a decode failure.", "fix": "Change `types.NFS3ErrAccess` to `types.NFS3ErrServerfault` (or `types.NFS3ErrIO`) for `handleNFSNull`, `handleNFSGetAttr`, `handleNFSSetAttr`, `handleNFSLookup`, and `handleNFSAccess`. For `handleNFSAccess` specifically `NFS3ErrAccess` is at least plausible but misleading as a decode-failure code.", "dimension": "slop", "area": "nfs-dispatch-connection-auth", "confirmed": true, "verdict": "handleNFSNull at dispatch_nfs.go:157 passes types.NFS3ErrAccess as errorStatus to handleRequest. RFC 1813 NULL always returns NFS3_OK; NFS3ErrAccess (permission denial) is semantically wrong as a decode-failure fallback. Unreachable in practice but slop confirmed."}, {"title": "DeleteWithRefCount on Syncer is dead code \u2014 no production caller", "file": "/tmp/dittofs-audit/pkg/block/engine/dedup.go", "line": 451, "severity": "LOW", "what": "`(*Syncer).DeleteWithRefCount` is fully implemented (calls fileBlockStore.DecrementRefCount, GetFileBlock, remoteStore.Delete per blockID) but has zero production callers. Only reference is a comment in pkg/metadata/store.go:358 (`engine.dedup.DeleteWithRefCount`). The method is exported on Syncer but not on any interface; nothing in the production runtime, adapters, or coordinator calls it.", "why": "Stub/orphan passing as done. The engine Delete path uses `MetadataCoordinator.DecrementRefCountAndReap` (readwrite.go:183,289) not this method. DeleteWithRefCount uses `fileBlockStore.DecrementRefCount(ctx, blockID string)` (by-ID, legacy FileBlockStore surface) while the live path uses the coordinator's by-offset reap. The function body is plausible but operates on a different abstraction layer and is never exercised. Risk: future callers may assume it is the canonical delete path when it is not.", "fix": "Remove the function or add a compile-time guard. If the intent is a GC-helper for legacy blockIDs, document it explicitly as legacy and add a test. Otherwise delete it \u2014 the canonical per-file delete path is engine.Delete \u2192 coordinator.DecrementRefCountAndReap.", "dimension": "slop", "area": "block-engine-core", "confirmed": true, "verdict": "DeleteWithRefCount on Syncer (dedup.go:451) has zero production callers. Only references are the method itself and a comment in store.go:358. Live delete path uses DecrementRefCountAndReap via coordinator (readwrite.go:183,289,441). Method is dead code."}, {"title": "TestWindowsFlagsRoundTrip: OI and CI labels inverted", "file": "/tmp/dittofs-audit/pkg/metadata/acl/flags_test.go", "line": 71, "severity": "LOW", "what": "Test cases {\"CI\", 0x01} and {\"OI\", 0x02} have names swapped. Windows OBJECT_INHERIT_ACE = 0x01 (OI, file inherit) and CONTAINER_INHERIT_ACE = 0x02 (CI, dir inherit). The production code in flags.go lines 19-22 correctly documents 0x01 = OI and 0x02 = CI, but the test labels them backwards.", "why": "AI-slop: copy-paste or fabricated label assignment. Test passes only because it checks round-trip math, not semantics. Future maintainers reading the test names to understand which Windows bit governs file vs directory inheritance will get the opposite answer, risking mis-wired callers.", "fix": "Swap the two test-case names: {\"OI\", 0x01} and {\"CI\", 0x02}. Also fix {\"CI+OI\", 0x03} label order to {\"OI+CI\", 0x03} for consistency with the OI-first naming used in flags.go comments.", "dimension": "slop", "area": "metadata-acl", "confirmed": true, "verdict": "flags_test.go:71 has {\"CI\", 0x01} and {\"OI\", 0x02}. Production flags.go maps 0x01=OBJECT_INHERIT_ACE (OI) and 0x02=CONTAINER_INHERIT_ACE (CI). Test labels are swapped. Round-trip math still passes so test does not catch semantic error."}, {"title": "shareNameToFSIDMinor \u2014 orphaned doc comment, function body never written", "file": "/tmp/dittofs-audit/internal/adapter/nfs/v4/attrs/encode.go", "line": 738, "severity": "LOW", "what": "Lines 738-739 are a Go doc comment describing `shareNameToFSIDMinor` (generates consistent FSID minor from share name using SHA-256). No function declaration or body follows \u2014 the file ends. The function does not exist anywhere in the codebase (verified by grep). This is a hallucinated stub description left as dead text.", "why": "AI-slop pattern: description of a function that was never implemented. Any caller that tries to reference this function will get a compile error; the comment misleads future developers into thinking FSID generation from share names was already solved.", "fix": "Either implement the function (e.g. `func shareNameToFSIDMinor(name string) uint64 { h := blake3.Sum256([]byte(name)); return binary.BigEndian.Uint64(h[:8]) }`) or delete the orphaned comment.", "dimension": "slop", "area": "nfs-v4-types-attrs-v41", "confirmed": true, "verdict": "encode.go ends at line 740. Lines 738-739 are a doc comment for shareNameToFSIDMinor; no function declaration or body follows. Function does not exist anywhere in codebase. Orphaned stub description."}, {"title": "NLM NULL response encodes NLM4Res (cookie+status) but spec requires void", "file": "/tmp/dittofs-audit/internal/adapter/nfs/nlm/handlers/null.go", "line": 29, "severity": "LOW", "what": "EncodeNullResponse serializes an NLM4Res struct (4-byte opaque length + cookie bytes + 4-byte status = at least 8 bytes) for the NULL procedure response. Open Group NLM v4 spec says NULL returns void (zero response bytes).", "why": "Strict NLM clients (e.g., Linux lockd parsing the reply) expect zero data bytes after the RPC accept header for procedure 0. Extra bytes in the reply body may cause the client's XDR parser to misalign on the next response or log a spurious decode error, though most implementations tolerate trailing bytes for NULL.", "fix": "Return []byte{} from EncodeNullResponse (same pattern as EncodeFreeAllResponse / UmountResponse.Encode). Remove the NLM4Res encoding from the NULL path in null.go and in the dispatch wrapper handleNLMNull in dispatch.go.", "dimension": "slop", "area": "nfs-nlm-nsm-portmap-mount", "confirmed": true, "verdict": "EncodeNullResponse in nlm/handlers/null.go serializes an NLM4Res struct (cookie + status) for the NULL procedure. NLM NULL spec requires void (zero response bytes). Extra bytes may misalign XDR parser on strict clients."}, {"title": "Stale/wrong comment in frameHeaderSize describes cap-based algorithm; code uses len-based", "file": "/tmp/dittofs-audit/pkg/block/encryption/frame.go", "line": 70, "severity": "LOW", "what": "Comment on line 69-70 reads 'cap(b) - cap(view.ciphertext) is robust regardless of how much trailing data we passed to tryDecodeFrame', but the actual expression on line 71 is `len(b) - len(view.ciphertext)`. The cap-based formula is never used.", "why": "Misleading comment signals slop from a refactor that changed the approach but left the old justification. A reader relying on the comment to audit correctness will reason about cap semantics that do not apply; the len-based formula is in fact correct for aliased subslices, making this a documentation-only defect that obscures review.", "fix": "Replace comment with: `// view.ciphertext aliases the tail of b; len(b) - len(view.ciphertext) == byte offset of ciphertext start == header length.`", "dimension": "slop", "area": "block-remote-s3-encryption-compression", "confirmed": true, "verdict": "frame.go:69-70 comment says 'cap(b) - cap(view.ciphertext)' but line 71 computes 'len(b) - len(view.ciphertext)'. Cap-based formula is never used. Comment misleads correctness auditors; the len-based formula is correct."}, {"title": "Vacuous nil-check on FileHandle in testDeleteShare", "file": "/tmp/dittofs-audit/pkg/metadata/storetest/store_surface.go", "line": 98, "severity": "LOW", "what": "createTestShare always fatals via t.Fatalf on any internal error before returning; it never returns a nil FileHandle. The `if root2 := createTestShare(...); root2 == nil` guard and the t.Fatal message are dead code that can never execute.", "why": "FileHandle is []byte. createTestShare calls t.Fatalf (runtime.Goexit) on CreateShare/CreateRootDirectory/EncodeFileHandle failure, so the goroutine exits before reaching return rootHandle. The nil-check therefore always evaluates to false, and the t.Fatal('re-CreateShare after DeleteShare returned nil root handle') body is unreachable. The REAL test (that createTestShare does not fatal) is valid, but the assertion text and the nil branch are misleading no-ops: any future reviewer might trust the nil check as meaningful.", "fix": "Replace the guard with a plain call that captures the return to satisfy Go's unused-variable rule, or add a comment: root2 := createTestShare(t, store, shareName); _ = root2 // createTestShare fatals internally on error; reaching here means success", "dimension": "slop", "area": "metadata-storetest-conformance", "confirmed": true, "verdict": "createTestShare (suite.go:154) calls t.Fatalf on every error path and never returns nil. The nil guard at store_surface.go:98 is dead code \u2014 the goroutine exits via runtime.Goexit before reaching return. The t.Fatal message is unreachable."}, {"title": "EnsureDefaultAdapters uses != instead of errors.Is for sentinel comparison", "file": "/tmp/dittofs-audit/pkg/controlplane/store/adapters.go", "line": 73, "severity": "LOW", "what": "`if err != models.ErrAdapterNotFound` compares error by pointer instead of using `errors.Is`.", "why": "Project idiom rule mandates `errors.Is`/`errors.As` for sentinel errors. A wrapped `ErrAdapterNotFound` (e.g. `fmt.Errorf(\"...: %w\", models.ErrAdapterNotFound)`) would bypass this guard and propagate as an unexpected error, causing `EnsureDefaultAdapters` to abort startup instead of continuing.", "fix": "Replace `if err != models.ErrAdapterNotFound` with `if !errors.Is(err, models.ErrAdapterNotFound)`.", "dimension": "slop", "area": "controlplane-store-models", "confirmed": true, "verdict": "adapters.go:73 uses 'err != models.ErrAdapterNotFound' (pointer equality) instead of errors.Is. Sentinel is errors.New so currently returned unwrapped by convertNotFoundError, making this low-risk in practice. Still violates project idiom and would silently break if wrapping is ever added."}, {"title": "Test assertions use != instead of errors.Is for sentinel error checks (3 sites)", "file": "/tmp/dittofs-audit/pkg/controlplane/store/adapter_settings_test.go", "line": 390, "severity": "LOW", "what": "Lines 390, 407, 459 compare errors with `!=` (`err != models.ErrDuplicateNetgroup`, `err != models.ErrNetgroupNotFound`, `err != models.ErrNetgroupInUse`) instead of `errors.Is`.", "why": "Fabricated-correct-looking assertions: if the store ever wraps these sentinels the tests would falsely pass (the `!=` check would evaluate to `true` even for the correct wrapped error, reporting a spurious test failure rather than catching the wrapping). Violates explicit project idiom rule.", "fix": "Replace all three with `!errors.Is(err, models.ErrXxx)` and import `errors` if not already imported.", "dimension": "slop", "area": "controlplane-store-models", "confirmed": true, "verdict": "Lines 390, 407, 459 confirmed: use `err != models.ErrDuplicate/NotFound/InUse` directly. Sentinels defined via errors.New in models/errors.go:78-80 and returned unwrapped by createWithID/convertNotFoundError \u2014 tests pass today, but != is wrong idiom; any future wrapping silently breaks assertions. Fix: replace with errors.Is."}, {"title": "dfs init help examples reference wrong binary name `dittofs`", "file": "/tmp/dittofs-audit/cmd/dfs/commands/init.go", "line": 23, "severity": "LOW", "what": "Long description examples show `dittofs init` / `dittofs init --config ...` / `dittofs init --force` \u2014 all wrong binary names.", "why": "Binary is `dfs`. Copy-paste slop from placeholder. Operator following `--help` runs `dittofs init` and gets command-not-found.", "fix": "Replace `dittofs init` with `dfs init` on lines 23, 26, 29.", "dimension": "slop", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "init.go lines 23/26/29 confirmed: Long description shows `dittofs init`, `dittofs init --config ...`, `dittofs init --force`. Binary name is `dfs`. Fix: replace `dittofs` with `dfs` in all three example lines."}, {"title": "dfs migrate help examples reference wrong binary name `dittofs`", "file": "/tmp/dittofs-audit/cmd/dfs/commands/migrate.go", "line": 24, "severity": "LOW", "what": "Long description examples show `dittofs migrate` / `dittofs migrate --config ...`.", "why": "Binary is `dfs`. Same copy-paste slop. Operator following `--help` runs `dittofs migrate` and gets command-not-found.", "fix": "Replace `dittofs migrate` with `dfs migrate` on lines 24, 27.", "dimension": "slop", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "migrate.go lines 24/27 confirmed: Long description shows `dittofs migrate` and `dittofs migrate --config ...`. Binary is `dfs`. Fix: replace `dittofs` with `dfs`."}, {"title": "config subcommand Long description references wrong binary name `dittofs init`", "file": "/tmp/dittofs-audit/cmd/dfs/commands/config/config.go", "line": 14, "severity": "LOW", "what": "Long description says `Use 'dittofs init' to create a new configuration file.`", "why": "Binary is `dfs`. Same copy-paste slop as version/init/migrate.", "fix": "Change `dittofs init` to `dfs init` on line 14.", "dimension": "slop", "area": "cli-cmd-dfs-dfsctl", "confirmed": true, "verdict": "config/config.go line 14 confirmed: Long description says `Use 'dittofs init' to create a new configuration file.` Binary is `dfs`. Fix: replace `dittofs init` with `dfs init`."}, {"title": "TestNFSv4DelegationRecall / TestNFSv41BackchannelDelegationRecall: log-scraping assertions never fail \u2014 delegation recall is never confirmed", "file": "/tmp/dittofs-audit/test/e2e/nfsv4_delegation_test.go", "line": 189, "severity": "LOW", "what": "Both tests check server logs for \"Delegation granted\" and \"CB_RECALL sent\" but treat absence as a non-failure (lines 195\u2013200, 448\u2013464). The critical assertion is data consistency (`assert.Equal`), which passes regardless of whether delegations were ever issued or recalled. The log-scraping delegation-detection path is entirely optional \u2014 the test passes whether or not the server ever grants a single delegation.", "why": "These are supposed to validate the full delegation lifecycle (grant \u2192 recall \u2192 data flush). A server that never grants delegations passes all tests. The optional log-scraping is vestigial documentation-as-test, not a correctness gate.", "fix": "Either expose a /metrics or /debug endpoint counting delegations_granted and delegations_recalled and assert counts > 0, or accept the current limitation and rename the tests to reflect they only test data consistency, not delegation behavior.", "dimension": "slop", "area": "e2e-test-framework", "confirmed": true, "verdict": "nfsv4_delegation_test.go lines 189-206 and 440-464 confirmed: log-scraping results for 'Delegation granted'/'CB_RECALL sent' only call t.Log, never t.Error/t.Errorf/t.Fatal. Data consistency assert (assert.Equal on file content) passes regardless of delegation lifecycle. Server can skip delegations entirely and both tests pass. Fix: add t.Errorf if grantFound is false after a client open that should trigger delegation, or skip test if feature is unimplemented."}]