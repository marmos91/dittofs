---
phase: 49-testing-and-documentation
plan: 05
type: execute
wave: 2
depends_on: [49-01, 49-02]
files_modified:
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/IMPLEMENTING_STORES.md
  - docs/NFS.md
  - docs/SMB.md
  - docs/TROUBLESHOOTING.md
  - docs/FAQ.md
  - docs/SECURITY.md
  - docs/CONTRIBUTING.md
  - docs/CLI.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "docs/ARCHITECTURE.md describes two-tier block store model with diagrams"
    - "docs/ARCHITECTURE.md covers per-share BlockStore isolation, local/remote stores, syncer"
    - "docs/CONFIGURATION.md documents block_store: config section with local/remote sub-sections"
    - "docs/CONFIGURATION.md shows new CLI examples with --local/--remote flags"
    - "docs/CONFIGURATION.md documents DITTOFS_BLOCK_STORE_* environment variables"
    - "docs/CONFIGURATION.md documents dfsctl cache stats and dfsctl cache evict commands"
    - "CLAUDE.md reflects pkg/blockstore/ directory structure"
    - "CLAUDE.md documents new CLI commands (cache stats/evict, store block local/remote)"
    - "CLAUDE.md contains updated architecture diagram"
    - "docs/IMPLEMENTING_STORES.md rewritten for LocalStore and RemoteStore interfaces"
    - "README.md uses block store terminology throughout"
    - "Zero references to 'payload store' in any documentation file"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Updated architecture documentation with block store model"
    - path: "docs/CONFIGURATION.md"
      provides: "Updated configuration guide with block store CLI and config"
    - path: "CLAUDE.md"
      provides: "Updated project guidance with new package structure"
    - path: "docs/IMPLEMENTING_STORES.md"
      provides: "Rewritten store implementation guide for LocalStore/RemoteStore"
  key_links:
    - from: "docs/ARCHITECTURE.md"
      to: "pkg/blockstore/"
      via: "documentation describes package structure"
      pattern: "pkg/blockstore"
    - from: "docs/CONFIGURATION.md"
      to: "pkg/config/config.go"
      via: "documentation matches config struct"
      pattern: "block_store:"
    - from: "CLAUDE.md"
      to: "pkg/blockstore/"
      via: "directory tree documentation"
      pattern: "pkg/blockstore"
---

<objective>
Comprehensive documentation update for the entire block store architecture. Update all documentation files to replace payload store terminology with block store terminology, document the two-tier architecture, per-share isolation, new CLI commands, and new configuration format.

Purpose: Ensures all documentation accurately reflects the current codebase state. Developers and operators can understand and configure the new block store architecture from documentation alone.
Output: All docs, README, and CLAUDE.md updated with zero payload store references.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-testing-and-documentation/49-CONTEXT.md
@.planning/phases/49-testing-and-documentation/49-01-SUMMARY.md
@.planning/phases/49-testing-and-documentation/49-02-SUMMARY.md

@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/IMPLEMENTING_STORES.md
@CLAUDE.md
@README.md
@docs/NFS.md
@docs/SMB.md
@docs/FAQ.md
@docs/CONTRIBUTING.md
@docs/CLI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core documentation overhaul (ARCHITECTURE, CONFIGURATION, IMPLEMENTING_STORES)</name>
  <files>
    docs/ARCHITECTURE.md
    docs/CONFIGURATION.md
    docs/IMPLEMENTING_STORES.md
  </files>
  <action>
1. **docs/ARCHITECTURE.md** - Full refactor:
   - Replace all "payload store" / "PayloadService" / "payload" sections with block store equivalents
   - Update the main architecture diagram to show:
     - Per-Share BlockStore (pkg/blockstore/engine/)
     - Local Store (fs / memory) replacing old cache layer
     - Syncer (async local-to-remote transfer) replacing old offloader
     - Remote Store (s3 / memory) replacing old payload store
     - L1 Read Cache (pkg/blockstore/readcache/) as memory tier
   - Add section: "Per-Share Block Store Isolation" explaining:
     - Each share gets its own BlockStore instance
     - Isolated local storage directories
     - Remote stores shared (ref counted) across shares
     - Block stores created/closed with share lifecycle
   - Add section: "Cache Tiers" explaining the three-tier model:
     - L1: In-memory LRU read cache (fastest, volatile)
     - L2: Local filesystem block store (fast, persistent across restarts)
     - L3: Remote store (S3) (slowest, durable)
   - Update the "How It Works" flow to include per-share BlockStore resolution
   - Remove any references to old cache/WAL/offloader/PayloadService architecture

2. **docs/CONFIGURATION.md** - Full update:
   - Replace `payload:` config section with `block_store:` section containing local/remote sub-sections
   - Document new YAML structure:
     ```yaml
     block_store:
       local:
         type: fs
         config:
           path: /data/cache
           max_size: 10GB
       remote:
         type: s3
         config:
           bucket: my-bucket
           region: us-east-1
     ```
   - Document `DITTOFS_BLOCK_STORE_*` environment variables
   - Update share creation examples to use `--local` and `--remote` flags:
     ```bash
     dfsctl store block add --kind local --name cache-fs --type fs --config '{"path":"/data/cache"}'
     dfsctl store block add --kind remote --name s3-remote --type s3 --config '{"bucket":"my-bucket"}'
     dfsctl share create --name /export --metadata meta1 --local cache-fs --remote s3-remote
     ```
   - Document cache management CLI:
     ```bash
     dfsctl cache stats                    # All shares
     dfsctl cache stats --share /export    # Specific share
     dfsctl cache evict --share /export    # Evict all cache
     dfsctl cache evict --l1-only          # Evict L1 memory only
     dfsctl cache evict --local-only       # Evict local blocks only
     ```
   - Document auto-configuration (WriteBufferMemory, ReadCacheMemory defaults from system resources)
   - Document migration from old `payload:` key (config validator detects and prints hint)
   - Remove all old payload store configuration examples

3. **docs/IMPLEMENTING_STORES.md** - Full rewrite:
   - Replace PayloadStore guide with two sections:
     a. **Implementing a Local Store** — `pkg/blockstore/local.LocalStore` interface:
        - Explain the interface methods (ReadAt, WriteAt, Flush, Evict, etc.)
        - Show example implementation pattern
        - Reference `pkg/blockstore/local/fs/` as reference implementation
        - Reference conformance tests
     b. **Implementing a Remote Store** — `pkg/blockstore/remote.RemoteStore` interface:
        - Explain the interface methods (ReadBlock, WriteBlock, DeleteBlock, HealthCheck)
        - Show example implementation pattern
        - Reference `pkg/blockstore/remote/s3/` as reference implementation
        - Explain ref counting (shared across shares)
        - Reference conformance tests
   - Keep the metadata store section updated with current interface
   - Remove all PayloadStore references
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && grep -rn 'payload.store\|PayloadStore\|PayloadService\|payload_store' docs/ARCHITECTURE.md docs/CONFIGURATION.md docs/IMPLEMENTING_STORES.md | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>ARCHITECTURE.md has updated diagrams and block store sections. CONFIGURATION.md documents new block_store: config, CLI, and env vars. IMPLEMENTING_STORES.md rewritten for LocalStore and RemoteStore interfaces. Zero payload references in these files.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md, README, and remaining docs update</name>
  <files>
    CLAUDE.md
    README.md
    docs/NFS.md
    docs/SMB.md
    docs/TROUBLESHOOTING.md
    docs/FAQ.md
    docs/SECURITY.md
    docs/CONTRIBUTING.md
    docs/CLI.md
  </files>
  <action>
1. **CLAUDE.md** - Full overhaul:
   - Update the "Architecture" section to match the new block store model
   - Update the main architecture diagram (ASCII art) to show per-share BlockStore
   - Update "Key Interfaces" section:
     - Replace PayloadService description with BlockStore description
     - Add BlockStore engine section (engine.BlockStore composes local + remote + syncer)
     - Update Runtime description to mention GetBlockStoreForHandle
     - Update Control Plane Store to mention BlockStoreConfigStore (not PayloadStoreConfigStore)
   - Update "Directory Structure" to show current pkg/blockstore/ layout:
     ```
     pkg/blockstore/
     ├── engine/        # BlockStore orchestrator
     ├── local/         # Local store interface
     │   ├── fs/        # Filesystem local store
     │   └── memory/    # In-memory local store
     ├── remote/        # Remote store interface
     │   ├── s3/        # S3 remote store
     │   └── memory/    # In-memory remote store
     ├── sync/          # Syncer (async local-to-remote)
     ├── readcache/     # L1 read cache
     ├── gc/            # Block garbage collection
     ├── io/            # Read/write I/O helpers
     └── storetest/     # Conformance test helpers
     ```
   - Update CLI section to include cache commands
   - Update "Store management" CLI example:
     ```bash
     dfsctl store block add --kind local --name cache-fs --type fs
     dfsctl store block add --kind remote --name s3-remote --type s3 --config '{...}'
     ```
   - Update "Adding a New Store Backend" section for Local and Remote stores
   - Update environment variables section: remove DITTOFS_PAYLOAD_*, add DITTOFS_BLOCK_STORE_*
   - Remove all references to old cache/WAL/offloader/PayloadService/PayloadStore patterns
   - Update "Write Coordination Pattern" to use per-share block store resolution

2. **README.md**:
   - Update quick start examples to use block store CLI
   - Update architecture overview to mention two-tier block store
   - Update "Available Test Configurations" to reflect new matrix
   - Replace all "payload store" references with "block store"

3. **docs/NFS.md**: Replace any "payload store" or "PayloadService" references with "per-share block store" or "BlockStore"

4. **docs/SMB.md**: Same payload -> block store terminology update

5. **docs/TROUBLESHOOTING.md**: Update any troubleshooting steps mentioning payload stores

6. **docs/FAQ.md**: Update FAQ entries referencing payload stores or old configuration

7. **docs/SECURITY.md**: Update any security considerations mentioning payload stores

8. **docs/CONTRIBUTING.md**: Update development workflow to use new store commands and block store terminology

9. **docs/CLI.md**: Update CLI reference if it documents payload store commands

10. **Final verification**: Run grep across ALL documentation files to ensure zero payload store references remain.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && grep -rn 'payload.store\|PayloadStore\|PayloadService\|payload_store' CLAUDE.md README.md docs/ | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>CLAUDE.md fully updated with new block store architecture. README.md uses block store terminology. All 8 docs/ files updated. Zero payload store references in any documentation file.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'payload.store\|PayloadStore\|PayloadService\|payload_store' CLAUDE.md README.md docs/` returns zero results
2. docs/ARCHITECTURE.md contains "block store" and "per-share isolation" sections
3. docs/CONFIGURATION.md contains `block_store:` YAML examples
4. CLAUDE.md contains `pkg/blockstore/` directory tree
5. docs/IMPLEMENTING_STORES.md contains LocalStore and RemoteStore interface guides
</verification>

<success_criteria>
- All 12 documentation files updated with block store terminology
- Architecture diagrams reflect two-tier model with per-share isolation
- Configuration guide documents new CLI, YAML, and env vars
- CLAUDE.md reflects current package structure
- IMPLEMENTING_STORES.md rewritten for LocalStore/RemoteStore
- Zero payload store references in any documentation
</success_criteria>

<output>
After completion, create `.planning/phases/49-testing-and-documentation/49-05-SUMMARY.md`
</output>
