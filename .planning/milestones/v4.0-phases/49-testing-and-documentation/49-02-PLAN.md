---
phase: 49-testing-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/controlplane/store/interface.go
  - pkg/controlplane/store/gorm.go
  - pkg/controlplane/models/stores.go
  - pkg/controlplane/store/adapter_settings_test.go
  - internal/controlplane/api/handlers/shares.go
  - test/e2e/payload_stores_test.go
  - test/e2e/store_matrix_test.go
  - test/e2e/nfsv4_store_matrix_test.go
  - test/e2e/helpers/stores.go
  - test/e2e/helpers/server.go
  - test/e2e/helpers/smb3_helpers.go
  - test/e2e/shares_test.go
  - test/e2e/backup_test.go
  - test/e2e/nfsv4_recovery_test.go
  - test/e2e/nfsv4_basic_test.go
  - test/e2e/grace_period_test.go
  - test/e2e/permissions_test.go
  - test/e2e/permission_enforcement_test.go
  - test/e2e/cross_protocol_test.go
  - test/e2e/cross_protocol_lock_test.go
  - test/e2e/cross_protocol_lease_test.go
  - test/e2e/file_operations_nfs_test.go
  - test/e2e/file_operations_smb_test.go
  - test/e2e/metadata_stores_test.go
  - test/e2e/nfsv4_controlplane_test.go
  - test/e2e/nfsv4_acl_test.go
  - test/e2e/nfsv4_kerberos_test.go
  - test/e2e/nfsv41_session_test.go
  - test/e2e/kerberos_test.go
  - test/e2e/smb_kerberos_test.go
  - test/e2e/smb3_kerberos_test.go
  - test/posix/setup-posix.sh
  - test/smb-conformance/configs/badger-fs.yaml
  - test/smb-conformance/configs/badger-s3.yaml
  - test/smb-conformance/configs/memory-fs.yaml
  - test/smb-conformance/configs/memory.yaml
  - test/smb-conformance/configs/postgres-s3.yaml
  - scripts/run-bench.sh
  - k8s/dittofs-operator/api/v1alpha1/dittoserver_types.go
  - k8s/dittofs-operator/docs/CRD_REFERENCE.md
  - k8s/dittofs-operator/config/crd/bases/dittofs.dittofs.com_dittoservers.yaml
  - k8s/dittofs-operator/chart/crds/dittoservers.yaml
  - .github/workflows/e2e-tests.yml
  - .github/instructions/copilot.instructions.md
  - pkg/config/config.go
  - cmd/dfs/commands/config/init.go
  - cmd/dfs/commands/config/validate.go
autonomous: true
requirements: [DOCS-04]

must_haves:
  truths:
    - "grep -rn 'payload.store|PayloadStore|payload_store|payload-store' across entire repo (excluding .planning/) returns zero results"
    - "All variable names, struct fields, comments, and log messages referencing 'payload store' now say 'block store' (local/remote)"
    - "Config YAML uses block_store: key instead of payload:"
    - "DITTOFS_PAYLOAD_* env vars renamed to DITTOFS_BLOCK_STORE_*"
    - "dfs config validate detects old payload: key and prints migration hint"
    - "dfs config init generates new format with block_store: section"
    - "All E2E test helpers use block store methods (CreateLocalBlockStore/CreateRemoteBlockStore)"
    - "payload_stores_test.go renamed to block_stores_test.go with updated content"
  artifacts:
    - path: "test/e2e/helpers/stores.go"
      provides: "Block store E2E helpers replacing payload store helpers"
    - path: "test/e2e/payload_stores_test.go"
      provides: "DELETED - renamed to block_stores_test.go"
  key_links:
    - from: "test/e2e/helpers/stores.go"
      to: "pkg/apiclient/block_stores.go"
      via: "CLI helper methods calling block store API"
      pattern: "CreateLocalBlockStore|CreateRemoteBlockStore"
---

<objective>
Remove every reference to "payload store" from the entire repository (Go code, tests, configs, scripts, K8s manifests, CI workflows, copilot instructions). This is a comprehensive mechanical rename: all variable names, struct fields, comments, log messages, YAML keys, and environment variables that say "payload store" become "block store" (local/remote).

Purpose: Clean break with zero tolerance for leftover payload terminology. User explicitly requested: "Remove any reference to old payload store and legacy code from the repository."
Output: Zero grep hits for payload store patterns across the entire repo (excluding .planning/).
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-testing-and-documentation/49-CONTEXT.md

@pkg/controlplane/store/interface.go
@pkg/controlplane/store/gorm.go
@pkg/controlplane/models/stores.go
@internal/controlplane/api/handlers/shares.go
@test/e2e/helpers/stores.go
@test/e2e/payload_stores_test.go
@test/e2e/store_matrix_test.go
@pkg/config/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename payload store references in Go packages and config</name>
  <files>
    pkg/controlplane/store/interface.go
    pkg/controlplane/store/gorm.go
    pkg/controlplane/store/adapter_settings_test.go
    pkg/controlplane/models/stores.go
    internal/controlplane/api/handlers/shares.go
    pkg/config/config.go
    cmd/dfs/commands/config/init.go
    cmd/dfs/commands/config/validate.go
  </files>
  <action>
1. Scan all files in the grep results for `payload.store|PayloadStore|payload_store|payload-store` patterns in Go packages (pkg/, internal/, cmd/).

2. For each file, rename:
   - `PayloadStoreConfigStore` -> already renamed to `BlockStoreConfigStore` in most places; verify and clean any leftovers
   - `PayloadStoreConfig` -> `BlockStoreConfig` (if any remain)
   - `payload_store` in GORM tags/column names -> `block_store` (check migrations handle this)
   - Comments/doc strings mentioning "payload store" -> "block store (local/remote)"
   - Variable names like `payloadStore`, `payloadStoreID` -> check if already renamed, clean leftovers
   - In `internal/controlplane/api/handlers/shares.go`: any references to payload store in share creation/update logic

3. Config file changes:
   - In `pkg/config/config.go`: if there's a `Payload` config section, rename to `BlockStore` with `Local` and `Remote` sub-sections. Update YAML tags from `payload:` to `block_store:`.
   - Rename environment variable mappings: `DITTOFS_PAYLOAD_*` -> `DITTOFS_BLOCK_STORE_*`
   - In `cmd/dfs/commands/config/init.go`: update default config template to use `block_store:` section
   - In `cmd/dfs/commands/config/validate.go`: add detection of old `payload:` YAML key with migration hint message: "Config key 'payload:' has been renamed to 'block_store:'. Please update your config file. See docs/CONFIGURATION.md for the new format."

4. Verify: `go build ./...` and `go test ./pkg/controlplane/... ./internal/controlplane/... ./pkg/config/...` pass.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && go build ./... && go test ./pkg/controlplane/... ./pkg/config/... -count=1 -timeout 120s</automated>
  </verify>
  <done>Zero payload store references remain in pkg/, internal/, and cmd/ Go files. Config uses block_store: YAML key. Environment variables use DITTOFS_BLOCK_STORE_* prefix. Config validator detects old payload: key.</done>
</task>

<task type="auto">
  <name>Task 2: Rename payload store references in tests, configs, scripts, K8s, CI</name>
  <files>
    test/e2e/payload_stores_test.go
    test/e2e/store_matrix_test.go
    test/e2e/nfsv4_store_matrix_test.go
    test/e2e/helpers/stores.go
    test/e2e/helpers/server.go
    test/e2e/helpers/smb3_helpers.go
    test/e2e/shares_test.go
    test/e2e/backup_test.go
    test/e2e/nfsv4_recovery_test.go
    test/e2e/nfsv4_basic_test.go
    test/e2e/grace_period_test.go
    test/e2e/permissions_test.go
    test/e2e/permission_enforcement_test.go
    test/e2e/cross_protocol_test.go
    test/e2e/cross_protocol_lock_test.go
    test/e2e/cross_protocol_lease_test.go
    test/e2e/file_operations_nfs_test.go
    test/e2e/file_operations_smb_test.go
    test/e2e/metadata_stores_test.go
    test/e2e/nfsv4_controlplane_test.go
    test/e2e/nfsv4_acl_test.go
    test/e2e/nfsv4_kerberos_test.go
    test/e2e/nfsv41_session_test.go
    test/e2e/kerberos_test.go
    test/e2e/smb_kerberos_test.go
    test/e2e/smb3_kerberos_test.go
    test/posix/setup-posix.sh
    test/smb-conformance/configs/badger-fs.yaml
    test/smb-conformance/configs/badger-s3.yaml
    test/smb-conformance/configs/memory-fs.yaml
    test/smb-conformance/configs/memory.yaml
    test/smb-conformance/configs/postgres-s3.yaml
    scripts/run-bench.sh
    k8s/dittofs-operator/api/v1alpha1/dittoserver_types.go
    k8s/dittofs-operator/docs/CRD_REFERENCE.md
    k8s/dittofs-operator/config/crd/bases/dittofs.dittofs.com_dittoservers.yaml
    k8s/dittofs-operator/chart/crds/dittoservers.yaml
    .github/workflows/e2e-tests.yml
    .github/instructions/copilot.instructions.md
  </files>
  <action>
1. **E2E test helpers** (`test/e2e/helpers/stores.go`):
   - Replace `CreatePayloadStore` / `DeletePayloadStore` / `WithPayloadRawConfig` with `CreateLocalBlockStore` / `CreateRemoteBlockStore` / `DeleteBlockStore` / `WithBlockRawConfig` etc.
   - Delete old payload store helper methods entirely
   - Update all type names and doc comments

2. **Rename file**: `test/e2e/payload_stores_test.go` -> delete and create `test/e2e/block_stores_test.go` with same test logic but using block store CLI commands (`dfsctl store block local/remote`). Rename test function `TestPayloadStoresCRUD` -> `TestBlockStoresCRUD`. Update all internal references.

3. **All E2E test files** (22+ files listed above): For each file, replace:
   - `CreatePayloadStore(...)` -> `CreateLocalBlockStore(...)` and/or `CreateRemoteBlockStore(...)` as appropriate
   - `DeletePayloadStore(...)` -> `DeleteBlockStore(...)` (or `DeleteLocalBlockStore`/`DeleteRemoteBlockStore`)
   - `WithPayloadRawConfig(...)` -> `WithBlockRawConfig(...)`
   - `payloadStoreName` variable names -> `localStoreName` / `remoteStoreName`
   - Comments mentioning "payload store" -> "block store"
   - Ensure the test logic correctly uses the new 2-param (local + optional remote) share creation pattern

4. **Store matrix tests** (`store_matrix_test.go`, `nfsv4_store_matrix_test.go`):
   - Update `storeConfig` struct: rename `payloadType` field to use block store terminology (this will be further restructured to 3D matrix in Plan 03, but for now just clean payload references)
   - Update comments and variable names

5. **Config files** (`test/smb-conformance/configs/*.yaml`): Rename `payload_store:` or `payload:` YAML keys to `block_store:` with local/remote sub-sections.

6. **Scripts** (`scripts/run-bench.sh`, `test/posix/setup-posix.sh`): Replace any payload store CLI invocations with block store CLI equivalents.

7. **K8s operator** (`k8s/dittofs-operator/`):
   - `api/v1alpha1/dittoserver_types.go`: Rename PayloadStore fields to BlockStore
   - CRD YAML files: Rename payload_store references to block_store
   - `docs/CRD_REFERENCE.md`: Update documentation

8. **CI/GitHub** (`.github/workflows/e2e-tests.yml`, `.github/instructions/copilot.instructions.md`): Update any payload store references.

9. **Final verification**: Run `grep -rn 'payload.store\|PayloadStore\|payload_store\|payload-store' --include='*.go' --include='*.yaml' --include='*.yml' --include='*.sh' --include='*.md' . | grep -v '.planning/'` and confirm zero results. Exclude `.planning/` directory.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && go build ./... && grep -rn 'PayloadStore\|payload_store\|payload-store' --include='*.go' --include='*.yaml' --include='*.yml' --include='*.sh' . | grep -v '.planning/' | grep -v '.git/' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Zero payload store references remain in tests, configs, scripts, K8s manifests, and CI. payload_stores_test.go deleted and replaced with block_stores_test.go. All E2E helpers use block store methods. All YAML configs use block_store: key. K8s CRDs updated.</done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles cleanly
2. `grep -rn 'PayloadStore\|payload_store\|payload-store' . | grep -v '.planning/' | grep -v '.git/'` returns zero results
3. `go vet ./...` passes
4. `go test ./pkg/controlplane/... -count=1` passes
</verification>

<success_criteria>
- Zero grep hits for payload store patterns across the entire repo (excluding .planning/)
- All E2E test helpers use block store methods
- payload_stores_test.go deleted and replaced with block_stores_test.go
- Config YAML uses block_store: key
- Environment variables use DITTOFS_BLOCK_STORE_* prefix
- K8s operator CRDs updated
- CI workflows updated
- Project still compiles and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/49-testing-and-documentation/49-02-SUMMARY.md`
</output>
