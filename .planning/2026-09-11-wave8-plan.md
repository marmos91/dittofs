# Wave 8 plan — cross-cutting queue + perf lens

Written 2026-09-11 after the post-Wave-5 follow-ups landed (repo at 5da8b2ad9 + docs e6b1f767d,
0 open PRs, 30 open issues, tree clean, builds green). This plan supersedes the "Continuously"
queue in the ROADMAP for the next sessions. Fan out where premises allow; the file-disjoint
groups below can run in parallel.

## Verified premises (2026-09-11, on develop)

- MapLockToSMB/errmap/lock_errmap/content_errmap: all GONE (folded in Wave 3.5). Old errmap-row
  language in the ROADMAP's "Continuously" section is stale — the mapper is now
  `internal/adapter/{nfs,nfs/v4,smb}/types` `StatusFor*`.
- #2436 (v4.1 never grants delegations) is CLOSED — the CBPathUp gap closed; so the #2437
  "restrict delegations to v4.1+" alternative is dead: delegations now exist on both.
- #2413/#2414 etc. all closed; the tranche issues #2512-#2518 are closed via the Wave 4 fix waves.
- v4/state deferred test files green: `go test -run 'TestClientRecovery|TestLockOwner|TestSession|TestClient|TestBackchannel' ./internal/adapter/nfs/v4/state/` ok in 10.9s.
- Banner separators: 2,695 matches for `^// ---|^// ===` across internal/ + pkg/ (the "962" in the
  ROADMAP is stale).

## Step 1 — small file-disjoint fixes (fan out in parallel; disjoint files, no shared state)

Each lane: read the issue, verify the premise on develop with rg (never stale line numbers),
write a failing check first, fix, then THREE reviews before PR open (correctness,
simplification/over-engineering, adversarial), assign marmos91, body carries `Closes #N`,
Copilot wired, babysit merge. File names must be domain-named — never wave\d/_w\d
(.planning/CONVENTIONS-WAVE-TEST-NAMES.md).

### Lane A — #2441 ErrCrossShare (pkg/metadata + errmap rows; pairs with nothing)
- Files: `pkg/metadata/service.go` (`requireSameShare`, callers `CreateHardLink`/`Move`),
  `internal/adapter/nfs/types/statusfor.go` + `internal/adapter/smb/types/status_map.go`
  (new `ErrCrossShare` arm), plus the NFS handler-level cross-share checks in
  `internal/adapter/nfs/v4/handlers/{rename,link}.go` stay as-is (pure status-shaping, they
  already return XDEV).
- Fix: add `ErrCrossShare` code; `requireSameShare` returns it instead of `ErrInvalidArgument`;
  map to `NFS3ErrXDev` / `NFS4ERR_XDEV` / `STATUS_NOT_SAME_DEVICE`.
- Red check: unit test on `requireSameShare` asserting errors.Is ErrCrossShare (red before fix —
  currently ErrInvalidArgument); SMB path test asserting the wire status becomes
  STATUS_NOT_SAME_DEVICE for `set_info.go` hardlink/rename callers (~:2644, :784, :1253 — verify
  with rg; the file moved in Wave 5).
- Disclosure: the e2e cross-protocol errno test (driven off `common.MapToNFS3`) gains a reachable
  EXDEV case automatically — note it in the PR body.

### Lane B — #2442 SEEK ISDIR (nfs v4.2)
- Files: the v4.2 SEEK handler (find by symbol: `rg -l "NFS4ERR_ISDIR|Seek" internal/adapter/nfs/v4/`),
  its stat-type gate. Fix: SEEK on non-regular files returns NFS4ERR_ISDIR **only** when the
  (verify knfsd/Linux behavior in the issue body first — the issue says "every non-regular file
  type"; check what RFC 7862/knfsd actually mandate before coding: directories vs sockets vs FIFOs
  may differ).
- Red check: unit test per file type; red-without-fix proven.

### Lane C — #2397 GET_DIR_DELEGATION CB_NOTIFY
- Files: the dir-delegation handler + callback path (find: `rg -l "GDD4|DirDelegation" internal/adapter/nfs/`).
- Fix per issue body: fire CB_NOTIFY after granting. Needs the callback client path — read the
  issue fully; if the fix needs a callback-channel change, STOP and report: this may be #2436-adjacent
  and could be Wave 9 size. Do not guess.

## Step 1 status (2026-09-11) — ALL THREE LANDED GREEN, reviews pending

- Lane A #2441: branch fix/2441-cross-share-code @ be802433c pushed (11 files, +66/-4:
  ErrCrossShare enum + re-export, requireSameShare, StatusFor arms nfs/nfs4/smb;
  orchestrator fixed the worker's wrong StatusNotSameDevice 0xC00000A4 -> 0xC00000D4 —
  two sources agree: impacket nt_errors.py and ntstatus.h both say 0xC00000D4).
- Lane B #2442: branch fix/2442-seek-isdir @ 955db8da0 pushed (2 files, +62/-1:
  seekTypeError NF4DIR->ISDIR / symlink->SYMLINK / else WRONG_TYPE; orchestrator corrected
  the comment: the per-type text lives in Section 15.10.3, 15.11.3 only points SEEK at
  15.10.3 for STATEIDS — Table 2 includes all three errors).
- Lane C #2397: branch fix/2397-dir-deleg-notify @ 8cb315b54 pushed (5 files, +231/-8:
  NotifyDirChange fired on CREATE/REMOVE/RENAME/LINK content paths — the only pre-existing
  caller was setattr.go; orchestrator re-applied the worker's create.go block lost to a
  git checkout restore AND fixed its P1: the notify must use parentHandle, not
  ctx.CurrentFH which handleCreate had already replaced with the new entry's handle).
- Red-without-fix proven on all three (A: TestRequireSameShare_CrossShareCode +
  TestStatusFor_EnumWalk, B: TestSeekNonRegularType, C: TestCreate_Notifies red when the
  notify block is neutralized).
- Next: THREE reviews per branch (correctness / over-engineering / adversarial) before PR open.

## Step 2 — sequential one-writer lane (all touch `internal/adapter/nfs/v4/state/`)

Serialize after Step 1 lanes land (same-day merge per PR, fresh rebase between, no stacking).

### PR 1 — auxsvc lock fix + rename → sidecar (pkg/adapter/auxsvc/, disjoint from state)
- `Group.Start` holds `g.mu` across `s.Start()` — a slow/blocking service start wedges every other
  Group op (Ready/Reconcile/StopAll). Fix: check-and-reserve under mu, do `s.Start(baseCtx)` outside
  the lock, re-acquire to insert; keep ErrAlreadyRunning semantics exact (the double-Start race
  must still lose). Rename `Group`→`sidecar` naming per the ROADMAP — **confirm with the user
  whether the rename is wanted** before doing it (it touches every call site; the lock fix alone
  is small).
- Red check: a test that a blocking Start doesn't prevent concurrent Ready()/Reconcile — red before
  the fix.

### PR 2 — #2483 seqid=0 v4.1 bypass applied to v4.0 (the last Wave-2 stateid item)
- Re-verify the premise from the ROADMAP: the seqid != 0 "v4.1 bypass convention" guard in
  `state/manager.go` cannot distinguish a v4.0-sent 0 from a v4.1-forced 0, so LOCKU with a bad
  lockseqid succeeds. Fix carries `ctx.SkipOwnerSeqid` into the state manager and adopts nfsd's
  initial-seqid rule for brand-new owners; touches all seqid-bearing ops (OpenFile, ConfirmOpen,
  CloseFile, DowngradeOpen, LockNew, LockExisting, UnlockFile).
- Reproduce first: `./run-pynfs.sh --minor-version 4.0 --tests "INIT LOOKFILE MKFILE LKU6b"` (nix
  provides pynfs-4.0). One writer; three reviews; race-verified.

## Step 3 — Wave 7 perf lens (needs a measurement plan BEFORE any code)

### PR 1 — #2423 upload concurrency windows
- Issue: "upload concurrency is a product of two windows, and the adaptive controller samples the
  wrong one". Read the issue body; write the measurement FIRST (connection ramp both protocols,
  v4.1 >64 concurrent ops per session, large-I/O allocation toward 16MB, bufpool tier sizing —
  the measurement plan lines from the MASTER-PLAN). Only then fix the controller.
- Files: `pkg/block/engine/` upload/adaptive-controller code (find by symbol; the engine moved
  files in Wave 3.5 — re-derive with rg).
- Guards: postgres tests are `//go:build integration` — green `go test ./...` proves nothing about
  postgres; check `pg_stat_activity` before dropdb (#2345); sign every commit.

## Standing hazards (carry into every lane prompt — from the ROADMAP, unchanged)

- Audits pinned at 40884ad4f predate #2356 — re-verify premises on develop, never from the audit.
- Merge via PUT pulls/{n}/merge -f sha=<re-read head> (or gh pr merge --squash --admin), sign every
  commit (`git commit -S`; on macOS bash use explicit key if the agent socket fails:
  `git -c user.signingkey=$HOME/.ssh/id_rsa.pub commit -S`).
- Rebase onto develop, never merge commits.
- Protocol/e2e suites are exclusive/brokered — no concurrent harness runs (shared ports).
- known-failures tally 178 = SMB 86 + NFS 92; count with regex
  `^\| *[A-Z]+[0-9]+[a-z]? *\|` (trailing-digit regex silently drops CSESS16a, EID5c-5g, EID6a-6g).
- KNOWN_FAILURES bookkeeping: read each file's OWN tally, never the ledger.
- pi dangerous-pattern blocks: `git push -f`, `git reset --hard`, `git branch -D`. Workarounds:
  `git push origin +HEAD:refs/heads/<branch>`, `git update-ref -d refs/heads/<branch>`, restore via
  `git show HEAD:path > path`.
- pi-lens churn re-dirties .planning/docs files after checkout/restore — restore immediately before
  any fan-out launch or child allocation fails ('worktree isolation requires a clean git working tree').
- macOS /bin/bash 3.2: no declare -A in scripts.
- Before opening any PR: THREE reviews (correctness, over-engineering, adversarial); assign
  marmos91; body has `Closes #N`; Copilot wired via `gh pr edit <n> --add-reviewer "@copilot"`;
  watch the PR for Copilot comments and address them; babysit the squash merge.
- Direct pushes to develop do NOT run full CI — PRs are the only full-suite gate; do not push docs
  from a stale local develop (force-push incident pattern: verify local is not behind origin first).
- Dependabot reports 2 vulnerabilities (1 high, 1 moderate) on the default branch — still
  unaddressed; surface to the user, do not fix silently.

## Suggested parallelization

- Step 1 lanes A, B, C: three parallel worktree-isolated workers (files fully disjoint).
- Step 2 PRs 1→2: one writer, sequential, after Step 1 lands (PR 1 is disjoint from state, so it
  could also run parallel to Step 1's lanes — but keep it after to keep the merge queue simple).
- Step 3 PR 1: single writer after the measurement plan exists; the measurement run itself is a
  bg_run shell task.
