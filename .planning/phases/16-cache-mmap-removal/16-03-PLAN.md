---
phase: 16-cache-mmap-removal
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - pkg/blockstore/engine/cache_mmap_unix.go
  - pkg/blockstore/engine/cache_mmap_windows.go
  - pkg/blockstore/engine/cache_mmap_test.go
  - pkg/blockstore/engine/perf_bench_unix_test.go
  - pkg/blockstore/engine/perf_bench_phase12_test.go
autonomous: true
requirements: []
tags: [blockstore, engine, cleanup, deletion]

must_haves:
  truths:
    - "pkg/blockstore/engine/cache_mmap_unix.go is deleted (D-09 + Phase 16 scope)"
    - "pkg/blockstore/engine/cache_mmap_windows.go is deleted"
    - "pkg/blockstore/engine/cache_mmap_test.go is deleted (D-09)"
    - "TestPerfGate_Phase12_MmapHotPath is removed from perf_bench_unix_test.go (D-08); perf_bench_unix_test.go is itself deleted if empty post-removal (Claude's Discretion ruling: YES — fold)"
    - "No remaining references to syscall.Mmap, readFromCAS, or mmap as a load path in pkg/blockstore/engine/"
    - "TestPerfGate_Phase12_MmapHotPath references in any sibling perf file (e.g., perf_bench_phase12_test.go) are also cleaned"
    - "Cross-OS build passes on Linux + Darwin + Windows (no per-OS cache file remains; verified via GOOS=windows go build ./...)"
  artifacts:
    - path: "pkg/blockstore/engine/"
      provides: "the deleted-files set"
      contains: "no cache_mmap_*.go, no perf_bench_unix_test.go (if empty)"
  key_links: []
---

<objective>
Delete the four mmap-related files in `pkg/blockstore/engine/` whose contents are now dead code after Plan 16-02's rewire: `cache_mmap_unix.go`, `cache_mmap_windows.go`, `cache_mmap_test.go`, and the `TestPerfGate_Phase12_MmapHotPath` test in `perf_bench_unix_test.go`. Fold `perf_bench_unix_test.go` away entirely if removing the perf gate leaves it empty (Claude's Discretion ruling: YES — small cleanup, no behavior change).

Purpose: Phase 16's headline outcome is removing the platform-aware mmap fast-path. Plan 16-02 swapped the call site; this plan removes the dead code so the cross-OS build matrix collapses to a single Cache implementation. After this plan, no per-OS file exists in the cache layer (D-08, D-09).

Output: Four file removals (or three removals + one targeted test-function deletion if `perf_bench_unix_test.go` retains other content); cross-OS build verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cache-mmap-removal/16-CONTEXT.md
@.planning/phases/16-cache-mmap-removal/16-PATTERNS.md
@.planning/phases/16-cache-mmap-removal/16-01-PLAN.md
@.planning/phases/16-cache-mmap-removal/16-02-PLAN.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete cache_mmap_unix.go + cache_mmap_windows.go + cache_mmap_test.go</name>
  <files>pkg/blockstore/engine/cache_mmap_unix.go, pkg/blockstore/engine/cache_mmap_windows.go, pkg/blockstore/engine/cache_mmap_test.go</files>
  <read_first>
    - pkg/blockstore/engine/cache_mmap_unix.go (entire 88-line file — final read to confirm nothing in it is referenced from a non-deleted file)
    - pkg/blockstore/engine/cache_mmap_windows.go (entire 32-line file — same)
    - pkg/blockstore/engine/cache_mmap_test.go (entire 204-line file — same)
    - Run `grep -rn 'readFromCAS\|cache_mmap' pkg/` BEFORE deletion to enumerate all remaining references; the build must be clean post-deletion
    - .planning/phases/16-cache-mmap-removal/16-CONTEXT.md (D-08, D-09 — file deletion rulings)
    - .planning/phases/16-cache-mmap-removal/16-02-PLAN.md (confirm Plan 16-02's Task 2 ported the worth-keeping generic asserts before this deletion runs)
  </read_first>
  <action>
    Delete the three files using `git rm`:
      - `git rm pkg/blockstore/engine/cache_mmap_unix.go`
      - `git rm pkg/blockstore/engine/cache_mmap_windows.go`
      - `git rm pkg/blockstore/engine/cache_mmap_test.go`

    Before staging the deletions, run a pre-flight grep: `grep -rn 'readFromCAS\|formatChunkName' pkg/blockstore/engine/ --include='*.go'`. If any file outside the three being deleted still references `readFromCAS` or `formatChunkName` (the test helper from cache_mmap_test.go), Plan 16-02 missed a callsite — STOP and surface the issue rather than push a broken build. Expected post-deletion residue per PATTERNS.md: zero remaining references to either symbol.

    Do NOT delete `perf_bench_unix_test.go` in this task — Task 2 owns that. Do NOT delete `perf_bench_phase12_test.go` — Task 2 only edits the `TestPerfGate_Phase12_MmapHotPath` reference if any.

    Verify Plan 16-02 already shipped the cache_test.go absorption (or documented subsumption) by running the cache_test suite first — if any TestCache_* test fails because Plan 16-02 missed coverage, STOP rather than delete.
  </action>
  <verify>
    <automated>! ls pkg/blockstore/engine/cache_mmap_unix.go pkg/blockstore/engine/cache_mmap_windows.go pkg/blockstore/engine/cache_mmap_test.go 2>/dev/null && go build ./... && go vet ./pkg/blockstore/engine/... && go test ./pkg/blockstore/engine/... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `ls pkg/blockstore/engine/cache_mmap_unix.go` exits non-zero (file gone).
    - `ls pkg/blockstore/engine/cache_mmap_windows.go` exits non-zero.
    - `ls pkg/blockstore/engine/cache_mmap_test.go` exits non-zero.
    - `git status --short pkg/blockstore/engine/cache_mmap_unix.go pkg/blockstore/engine/cache_mmap_windows.go pkg/blockstore/engine/cache_mmap_test.go` shows three `D` entries staged.
    - `grep -rn 'readFromCAS' pkg/ --include='*.go'` returns no results (full purge from production + test code).
    - `grep -rn 'syscall\.Mmap' pkg/ --include='*.go'` returns no results.
    - `grep -rn 'formatChunkName' pkg/blockstore/engine/ --include='*.go'` returns no results (the helper from cache_mmap_test.go is gone; if it was also used by perf_bench_unix_test.go, Task 2 will surface that and delete or relocate it).
    - `go build ./...` exits 0.
    - `go vet ./pkg/blockstore/engine/...` exits 0.
    - `go test ./pkg/blockstore/engine/... -count=1 -race` exits 0.
  </acceptance_criteria>
  <done>
    Three files removed via `git rm`; no remaining references to readFromCAS / syscall.Mmap / formatChunkName in pkg/; engine test suite green under `-race`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete TestPerfGate_Phase12_MmapHotPath and fold perf_bench_unix_test.go if empty</name>
  <files>pkg/blockstore/engine/perf_bench_unix_test.go, pkg/blockstore/engine/perf_bench_phase12_test.go</files>
  <read_first>
    - pkg/blockstore/engine/perf_bench_unix_test.go (entire 105-line file — confirm it contains ONLY TestPerfGate_Phase12_MmapHotPath + the formatChunkName helper per PATTERNS.md, with no other unrelated tests)
    - pkg/blockstore/engine/perf_bench_phase12_test.go (search for any reference to TestPerfGate_Phase12_MmapHotPath, readFromCAS, or doc-comment cross-references that need updating)
    - pkg/blockstore/engine/perf_bench_test.go (sibling cross-platform bench file — confirm it does NOT reference the deleted helper or test name)
    - .planning/phases/16-cache-mmap-removal/16-CONTEXT.md (D-08 — perf gate deletion ruling; Claude's Discretion item "fold perf_bench_unix_test.go if empty post-deletion")
    - .planning/phases/16-cache-mmap-removal/16-PATTERNS.md (section "perf_bench_unix_test.go — delete TestPerfGate_Phase12_MmapHotPath" — confirms file becomes empty and should be deleted)
  </read_first>
  <action>
    Read `pkg/blockstore/engine/perf_bench_unix_test.go` in full. Per PATTERNS.md the file contains only `TestPerfGate_Phase12_MmapHotPath` (lines 29–96) and its helper `formatChunkName(i int)` (lines 98–105). Both can be deleted; the file becomes empty.

    Action sequence (Claude's Discretion ruling per CONTEXT.md is YES — fold):
      1. If the file truly contains only `TestPerfGate_Phase12_MmapHotPath` + `formatChunkName`: `git rm pkg/blockstore/engine/perf_bench_unix_test.go` — delete the whole file (this also removes its `//go:build linux || darwin` tag, which has no remaining purpose).
      2. If the file contains other tests not covered by PATTERNS.md inventory: surgically delete only the `TestPerfGate_Phase12_MmapHotPath` function and the `formatChunkName` helper, leaving the file otherwise intact. Document the divergence in SUMMARY.

    Audit `pkg/blockstore/engine/perf_bench_phase12_test.go`: read the file end-to-end and search for any remaining reference to `TestPerfGate_Phase12_MmapHotPath`, `readFromCAS`, or `cache_mmap_*.go` in code or comments. Remove or update such references — a stale godoc comment that says "see TestPerfGate_Phase12_MmapHotPath" should be updated or deleted. Do NOT delete other Phase 12 perf gates that are unrelated to mmap (e.g., warm-cache perf gates that Plan 16-04 will validate against).

    Do NOT add a new perf gate in this plan — Plan 16-04 owns the warm-cache regression validation.
  </action>
  <verify>
    <automated>go build ./... && go vet ./pkg/blockstore/engine/... && GOOS=windows go build ./... && GOOS=linux go build ./... && GOOS=darwin go build ./... && go test ./pkg/blockstore/engine/... -count=1 -race -short && ! grep -rn 'TestPerfGate_Phase12_MmapHotPath\|readFromCAS\|syscall\.Mmap\|formatChunkName' pkg/ --include='*.go'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rn 'TestPerfGate_Phase12_MmapHotPath' pkg/ --include='*.go'` returns no matches (test name fully purged from code + comments).
    - `! grep -rn 'readFromCAS' pkg/ --include='*.go'` returns no matches (cumulative with Task 1 — must remain clean).
    - `! grep -rn 'syscall\.Mmap' pkg/ --include='*.go'` returns no matches.
    - `! grep -rn 'formatChunkName' pkg/ --include='*.go'` returns no matches (helper gone with the file).
    - If file was fully deleted (expected per PATTERNS.md): `ls pkg/blockstore/engine/perf_bench_unix_test.go` exits non-zero AND `git status --short pkg/blockstore/engine/perf_bench_unix_test.go` shows `D`.
    - If file was kept (divergence case): the file no longer contains `TestPerfGate_Phase12_MmapHotPath` or `formatChunkName`; SUMMARY documents the other content found.
    - `go build ./...` exits 0.
    - `GOOS=windows go build ./...` exits 0 (cross-OS build — Windows compiles cleanly with no per-OS cache file).
    - `GOOS=linux go build ./...` exits 0.
    - `GOOS=darwin go build ./...` exits 0.
    - `go vet ./pkg/blockstore/engine/...` exits 0.
    - `go test ./pkg/blockstore/engine/... -count=1 -race -short` exits 0.
  </acceptance_criteria>
  <done>
    D-33 perf gate removed; `perf_bench_unix_test.go` folded entirely (or surgically pruned with rationale in SUMMARY); no per-OS cache file remains in pkg/blockstore/engine/; cross-OS build matrix passes; engine tests pass under `-race`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (pure deletion plan) | this plan only removes code; no new trust boundaries are introduced or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | D (Denial of Service) | removal of D-33 mmap perf gate | accept | The gate measured mmap vs ReadFile — meaningless post-removal of mmap (D-08). Warm-cache regression coverage moves to Plan 16-04's `BenchmarkRandReadVerified` ≤1.02 gate. Cold-cache regression remains explicitly unverified (D-07 deferred). |
| T-16-03-02 | T (Tampering) | removal of mmap-specific page-fault test | accept | Page-fault semantics are mmap-implementation specific; with mmap deleted there is no semantic to defend against. Generic byte-correctness coverage was ported in Plan 16-02 (D-10). |
| T-16-03-03 | I (Information Disclosure) | platform-specific code paths removed | mitigate | Cross-OS build verification in Task 2 acceptance gates (GOOS=windows / linux / darwin all build clean) prevents a regression where a platform-specific code path silently breaks without local-developer attention. |
</threat_model>

<verification>
- `grep -rn 'readFromCAS\|syscall\.Mmap\|cache_mmap_unix\|cache_mmap_windows\|cache_mmap_test\|TestPerfGate_Phase12_MmapHotPath\|formatChunkName' pkg/ --include='*.go'` returns no matches.
- Cross-OS build matrix (Linux + Darwin + Windows) passes via `GOOS=<os> go build ./...`.
- `go test ./pkg/blockstore/engine/... -race -count=1` passes.
- `git status --short pkg/blockstore/engine/` shows 3–4 `D` entries (3 if perf_bench_unix_test.go was surgically pruned; 4 if fully deleted — expected case).
</verification>

<success_criteria>
- Three mmap files deleted; perf_bench_unix_test.go folded or surgically pruned.
- All grep gates return zero matches for the purged symbols.
- Cross-OS build passes on all three target platforms.
- Engine test suite green under `-race`.
- No per-OS cache file remains in pkg/blockstore/engine/.
</success_criteria>

<output>
After completion, create `.planning/phases/16-cache-mmap-removal/16-03-SUMMARY.md` listing the deleted files, the fold-vs-prune decision for perf_bench_unix_test.go (with rationale), and the cross-OS build verification results.
</output>
