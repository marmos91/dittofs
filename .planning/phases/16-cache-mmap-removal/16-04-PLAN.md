---
phase: 16-cache-mmap-removal
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - pkg/blockstore/engine/perf_bench_phase12_test.go
  - test/e2e/BENCHMARKS.md
autonomous: false
requirements: []
tags: [blockstore, engine, perf, gate, validation]

must_haves:
  truths:
    - "BenchmarkRandReadVerified warm-cache regression ≤1.02 vs pre-Phase-16 baseline (D-06)"
    - "Cross-OS build passes on Linux + Darwin + Windows (verified via GOOS=<os> go build ./...) — no per-OS cache file remains anywhere in pkg/"
    - "Race-detector run on pkg/blockstore/engine/... is clean (`go test -race -count=1`)"
    - "Cache.go docstring at the line referenced by PATTERNS.md (formerly ~line 54) is accurate and references no mmap symbols (validation pass — Plan 16-02 should already have shipped this; this is the audit step)"
    - "No cold-cache benchmark introduced in Phase 16 (D-07 — deferred to v0.17+)"
    - "BENCHMARKS.md is updated with the new warm-cache baseline number and a note explaining the mmap path was removed in v0.16.0 Phase 16"
  artifacts:
    - path: "pkg/blockstore/engine/perf_bench_phase12_test.go"
      provides: "warm-cache perf gate (Phase 12 BenchmarkRandReadVerified — kept; not the deleted D-33 mmap gate)"
      contains: "BenchmarkRandReadVerified"
    - path: "test/e2e/BENCHMARKS.md"
      provides: "documented new warm-cache baseline number + v0.16.0 Phase 16 rationale"
  key_links: []
---

<objective>
Validate that the Phase 16 deletions (Plans 16-01 → 16-03) did not regress warm-cache read performance beyond the D-06 ≤1.02 gate. Capture a fresh pre/post baseline via `BenchmarkRandReadVerified`. Verify cross-OS build matrix one final time. Audit `cache.go` docstrings for any residual mmap reference. Update `BENCHMARKS.md` with the new baseline number and a one-paragraph note explaining the mmap path was removed in v0.16.0 Phase 16. Pause for human review of the bench output (autonomous=false).

Purpose: This is the verification + documentation tail of Phase 16. It does not change behavior — it confirms the behavior change shipped in Plans 16-02 + 16-03 is performance-neutral within the locked D-06 tolerance and that downstream phases (17+) inherit a clean foundation.

Output: A documented bench-result pair (baseline + post-Phase-16), an updated BENCHMARKS.md, and a human-approved sign-off on the perf neutrality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cache-mmap-removal/16-CONTEXT.md
@.planning/phases/16-cache-mmap-removal/16-PATTERNS.md
@.planning/phases/16-cache-mmap-removal/16-01-PLAN.md
@.planning/phases/16-cache-mmap-removal/16-02-PLAN.md
@.planning/phases/16-cache-mmap-removal/16-03-PLAN.md
@test/e2e/BENCHMARKS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre/post baseline for BenchmarkRandReadVerified and assert ≤1.02 ratio</name>
  <files>pkg/blockstore/engine/perf_bench_phase12_test.go</files>
  <read_first>
    - pkg/blockstore/engine/perf_bench_phase12_test.go (entire file — locate `BenchmarkRandReadVerified`, understand the bench harness, the chunk-size matrix, and any ratio-assertion helper)
    - pkg/blockstore/engine/perf_bench_test.go (cross-platform sibling — confirm the shared bench infrastructure)
    - .planning/phases/16-cache-mmap-removal/16-CONTEXT.md (D-06, D-07 — perf gate ruling)
    - test/e2e/BENCHMARKS.md (existing baseline numbers — the Phase 12 baseline must be retrieved as the "pre-Phase-16" reference; if the file has a prior `BenchmarkRandReadVerified` number, that is the comparison anchor)
  </read_first>
  <action>
    Capture two bench runs and compare:

    1. Pre-Phase-16 baseline: check out the develop branch tip immediately before this phase began (commit `f8e2532d` per gitStatus) into a temporary worktree (`git worktree add /tmp/dittofs-pre-p16 f8e2532d`) or via `git stash` of current work + checkout. Run `go test -bench=BenchmarkRandReadVerified -benchtime=10s -count=3 ./pkg/blockstore/engine/... > /tmp/p16-bench-pre.txt`. Record the median ns/op for each chunk size.

    2. Post-Phase-16: return to the phase 16 branch tip (after Plans 16-01..16-03 merged into the working tree). Run the same bench: `go test -bench=BenchmarkRandReadVerified -benchtime=10s -count=3 ./pkg/blockstore/engine/... > /tmp/p16-bench-post.txt`. Record the median ns/op.

    3. Compute the ratio post/pre for each chunk size in the matrix. Assert every ratio ≤ 1.02 (D-06). If any ratio exceeds 1.02, STOP and surface the finding for human checkpoint review (do NOT silently continue). The expected outcome is ratios at ~1.00 because the warm-cache path never touched mmap — verifying empirically rather than assuming.

    4. Append a results stanza to the BenchmarkRandReadVerified godoc (or a sibling `_phase16_baseline.md` next to the bench file is also acceptable) documenting the pre/post numbers, the date, and the commit hashes compared. Phase 17+ will reference this as the new baseline.

    Do NOT modify the bench logic itself — this task is pure measurement + documentation.

    Do NOT introduce a cold-cache benchmark (D-07 explicitly defers this).

    Use `benchstat` if installed (`go install golang.org/x/perf/cmd/benchstat@latest`) for the comparison output — `benchstat /tmp/p16-bench-pre.txt /tmp/p16-bench-post.txt` produces a clean side-by-side that's easy to paste into BENCHMARKS.md.
  </action>
  <verify>
    <automated>go test -bench=BenchmarkRandReadVerified -benchtime=3s -count=1 -run=^$ ./pkg/blockstore/engine/... 2>&1 | tee /tmp/p16-bench-smoke.txt && grep -q 'BenchmarkRandReadVerified' /tmp/p16-bench-smoke.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/p16-bench-pre.txt` and `/tmp/p16-bench-post.txt` (or equivalent paths captured in SUMMARY) both exist with parseable bench output.
    - For every chunk size in the matrix, the ratio post/pre ≤ 1.02 (D-06). Any breach is surfaced as a BLOCKING human-checkpoint finding rather than silently passed.
    - `go test -bench=BenchmarkRandReadVerified -benchtime=3s -count=1 -run=^$ ./pkg/blockstore/engine/...` exits 0 and produces output containing `BenchmarkRandReadVerified` (smoke-level — the gated runs with -count=3 -benchtime=10s capture the real numbers).
    - The SUMMARY records both bench result files, the chunk-size ratio table, and a one-line declaration of pass/fail vs D-06.
    - If `benchstat` was used: its output is recorded verbatim in SUMMARY.
  </acceptance_criteria>
  <done>
    Pre/post bench results captured; ratios verified ≤1.02; output stored for the checkpoint reviewer; SUMMARY records the comparison.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final cross-OS build + race verification + cache.go docstring audit</name>
  <files>pkg/blockstore/engine/cache.go</files>
  <read_first>
    - pkg/blockstore/engine/cache.go (entire file — final audit pass; confirm Plan 16-02's docstring update landed and contains no residual mmap references)
    - .planning/phases/16-cache-mmap-removal/16-CONTEXT.md (D-08, D-09, D-10 — confirm none of the deletions left orphan references)
    - .planning/phases/16-cache-mmap-removal/16-PATTERNS.md (section "cache.go:54 — docstring update" — the suggested replacement text)
  </read_first>
  <action>
    Run the final cross-OS build matrix and the race-detector pass:

      `GOOS=linux go build ./...`
      `GOOS=darwin go build ./...`
      `GOOS=windows go build ./...`
      `go vet ./pkg/blockstore/engine/...`
      `go test -race -count=1 ./pkg/blockstore/engine/...`
      `go test -race -count=1 ./pkg/blockstore/local/...`

    All commands must exit 0.

    Audit `pkg/blockstore/engine/cache.go` for any residual reference to mmap, syscall.Mmap, readFromCAS, cache_mmap_unix, cache_mmap_windows in docstrings or comments. If Plan 16-02 missed any (e.g., a multi-paragraph CACHE-06 block that survived), update it in this task using the PATTERNS.md replacement text. Style must match the surrounding CACHE-* doc paragraphs.

    Run final repo-wide purge check: `grep -rn 'readFromCAS\|syscall\.Mmap\|TestPerfGate_Phase12_MmapHotPath\|formatChunkName\|cache_mmap_unix\|cache_mmap_windows' pkg/ --include='*.go'` MUST return zero matches.
  </action>
  <verify>
    <automated>GOOS=linux go build ./... && GOOS=darwin go build ./... && GOOS=windows go build ./... && go vet ./pkg/blockstore/engine/... && go test -race -count=1 ./pkg/blockstore/engine/... && go test -race -count=1 ./pkg/blockstore/local/... && ! grep -rn 'readFromCAS\|syscall\.Mmap\|TestPerfGate_Phase12_MmapHotPath\|formatChunkName' pkg/ --include='*.go'</automated>
  </verify>
  <acceptance_criteria>
    - `GOOS=linux go build ./...` exits 0.
    - `GOOS=darwin go build ./...` exits 0.
    - `GOOS=windows go build ./...` exits 0.
    - `go vet ./pkg/blockstore/engine/...` exits 0.
    - `go test -race -count=1 ./pkg/blockstore/engine/...` exits 0.
    - `go test -race -count=1 ./pkg/blockstore/local/...` exits 0.
    - `! grep -rn 'readFromCAS\|syscall\.Mmap\|TestPerfGate_Phase12_MmapHotPath\|formatChunkName' pkg/ --include='*.go'` returns no matches (final purge gate).
    - `! grep -vn '^//' pkg/blockstore/engine/cache.go | grep -E 'syscall\.Mmap|readFromCAS|cache_mmap_'` returns no matches in code (filtered to exclude any historical comment that the next phase may legitimately reference — but the audit run inside the action should remove any such comment too).
    - `grep -q 'Phase 16' pkg/blockstore/engine/cache.go` matches OR cache.go contains the post-Phase-16 docstring shape per PATTERNS.md replacement text (planner judgement — the rewrite text doesn't have to literally say "Phase 16" but the references to mmap must be gone).
  </acceptance_criteria>
  <done>
    Cross-OS build matrix clean; race-detector clean; cache.go fully purged of mmap references; final repo-wide grep gate passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test/e2e/BENCHMARKS.md with new warm-cache baseline + v0.16.0 Phase 16 note</name>
  <files>test/e2e/BENCHMARKS.md</files>
  <read_first>
    - test/e2e/BENCHMARKS.md (entire file — locate the existing BenchmarkRandReadVerified entry, the v0.15.0 baseline section, and the convention for recording phase-versioned baselines)
    - /tmp/p16-bench-pre.txt and /tmp/p16-bench-post.txt (from Task 1 — the numbers being recorded)
    - .planning/phases/16-cache-mmap-removal/16-CONTEXT.md (D-06, D-07, D-08 — the rationale to document)
  </read_first>
  <action>
    Add a new section (or append to the existing v0.15.0 baseline section) in `test/e2e/BENCHMARKS.md` documenting:

      1. The Phase 16 warm-cache baseline: a table of chunk-size → ns/op (post-Phase-16) and ratio vs pre-Phase-16.
      2. A one-paragraph rationale: "v0.16.0 Phase 16 removed the platform-aware mmap fast-path in `pkg/blockstore/engine/cache.go`; the warm-cache path never touched mmap so the regression is bounded by D-06 ≤1.02 — verified by direct measurement (see Plan 16-04 SUMMARY)."
      3. A note about D-07: "Cold-cache regression is intentionally unmeasured in Phase 16 — production workloads are warm; cold-cache bench deferred to v0.17+ if cold-read complaints surface."
      4. A pointer to the deleted D-33 gate: "The `TestPerfGate_Phase12_MmapHotPath` gate (D-33) was removed in Phase 16 because it measured mmap vs ReadFile — both meaningless post-removal of mmap."

    Style match the existing BENCHMARKS.md sections — markdown tables for numbers, prose paragraphs for rationale, links/anchors where the file already uses them.

    Do NOT introduce a new bench file — the existing `BenchmarkRandReadVerified` is the canonical warm-cache gate; this task only updates docs around it.
  </action>
  <verify>
    <automated>grep -q 'Phase 16\|v0.16.0' test/e2e/BENCHMARKS.md && grep -q 'BenchmarkRandReadVerified' test/e2e/BENCHMARKS.md && grep -q '1.02\|≤1.02' test/e2e/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Phase 16' test/e2e/BENCHMARKS.md` matches (new section present).
    - `grep -q '1.02\|≤1.02' test/e2e/BENCHMARKS.md` matches (D-06 ratio cited).
    - `grep -q 'mmap' test/e2e/BENCHMARKS.md` matches (the removal is documented).
    - `grep -q 'TestPerfGate_Phase12_MmapHotPath\|D-33' test/e2e/BENCHMARKS.md` matches (deleted gate's fate is documented).
    - The new section contains a markdown table with chunk-size → ns/op + ratio columns.
    - The file still renders as valid Markdown (no broken table syntax — visually verifiable by a human at the checkpoint).
  </acceptance_criteria>
  <done>
    BENCHMARKS.md updated with the Phase 16 warm-cache baseline, the ≤1.02 gate result, the cold-cache deferral rationale, and the D-33 deletion note.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human checkpoint — review bench numbers and approve Phase 16 ship</name>
  <what-built>
    Phase 16 (Cache RAM-only) shipped via Plans 16-01..16-03; this checkpoint pauses for human review of:
      - The pre/post `BenchmarkRandReadVerified` numbers captured in Task 1
      - The chunk-size ratio table (every entry must be ≤1.02 per D-06)
      - The updated `test/e2e/BENCHMARKS.md` section
      - A final visual scan of `git diff develop..HEAD` for the four PR-equivalent commits (Plans 01–03 + Plan 04 docs/bench)
  </what-built>
  <how-to-verify>
    1. Open `/tmp/p16-bench-pre.txt` and `/tmp/p16-bench-post.txt`. Confirm every chunk size's ratio is ≤1.02. If any ratio exceeds 1.02, do NOT approve — file a follow-up issue (D-06 breach is a Phase 16 ship-blocker).
    2. Run `git log --oneline develop..HEAD` and confirm the commit history is clean: one commit per plan (or a small handful of fix-up commits per plan). No `WIP`, no Co-Authored-By, no `claude-code` mentions in commit messages (project CLAUDE.md rules).
    3. Run `git diff develop..HEAD --stat` and confirm the file-change set matches expectations: ~3 files added/modified in `pkg/blockstore/local/`, ~3 files modified + ~3 files deleted in `pkg/blockstore/engine/`, `test/e2e/BENCHMARKS.md` updated.
    4. Run `grep -rn 'readFromCAS\|syscall\.Mmap\|cache_mmap_unix\|cache_mmap_windows\|TestPerfGate_Phase12_MmapHotPath\|formatChunkName' pkg/ --include='*.go'` — must return zero matches.
    5. Render `test/e2e/BENCHMARKS.md` (via `glow test/e2e/BENCHMARKS.md` or any Markdown viewer) and confirm the new section renders cleanly with a valid table.
    6. Reply `approved` to ship Phase 16, or describe issues for follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" to ship Phase 16, or describe issues for follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (validation + docs plan) | this plan does not introduce or alter any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | D (Denial of Service) | warm-cache regression > 1.02 (D-06 breach) | mitigate | Task 1 captures pre/post bench numbers and fails the plan if any chunk-size ratio exceeds 1.02; human checkpoint (Task 4) double-checks the table before approving ship. |
| T-16-04-02 | I (Information Disclosure) | residual mmap references in docs leak deleted internals | mitigate | Task 2 grep gate enforces zero remaining references in `pkg/`; Task 3 docs update reflects the new state in `test/e2e/BENCHMARKS.md`. |
| T-16-04-03 | E (Elevation of Privilege) | cross-OS build silently breaks | mitigate | Task 2 explicitly runs `GOOS=linux/darwin/windows go build ./...` before ship — a Windows-only break would otherwise hide until a downstream user hits it. |
| T-16-04-04 | R (Repudiation) / Audit | un-recorded perf baseline drift | mitigate | Task 3 lands the new baseline in `BENCHMARKS.md` with date + commit references so Phase 17+ has a stable anchor. |
</threat_model>

<verification>
- Warm-cache regression measured and ≤1.02 per D-06 — empirical, not assumed.
- Cross-OS build matrix (Linux + Darwin + Windows) passes.
- `go test -race -count=1` clean on both `pkg/blockstore/engine/...` and `pkg/blockstore/local/...`.
- Full purge of mmap symbols from `pkg/` (final grep gate in Task 2).
- BENCHMARKS.md updated and human-approved.
</verification>

<success_criteria>
- Phase 16's warm-cache perf neutrality is verified, not assumed.
- The full cross-OS build matrix is green.
- All mmap symbols are gone from `pkg/`.
- `test/e2e/BENCHMARKS.md` reflects the new baseline and rationale.
- A human reviewer has approved the ship at the Task 4 checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/16-cache-mmap-removal/16-04-SUMMARY.md` containing:
  - The pre/post bench-result table.
  - The ratio comparison vs D-06 (≤1.02 declared pass/fail).
  - The cross-OS build verification log.
  - The diff to `test/e2e/BENCHMARKS.md`.
  - The human-checkpoint approval text (verbatim).
  - A "Phase 16 complete — ready for Phase 17 (unified BlockStore interface)" sign-off line.
</output>
