---
phase: 20-backupable-interface-conformance-suite-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/backupfmt/format.go
  - internal/cli/backupfmt/format_test.go
autonomous: true
requirements: [CLN-01, CLN-02]

must_haves:
  truths:
    - "internal/cli/backupfmt/ directory no longer exists"
    - ".planning/phases/01-* through 07-* directories no longer exist"
    - "go build ./... still passes after deletions"
    - "No remaining Go imports reference backupfmt"
  artifacts: []
  key_links: []
---

<objective>
Delete orphaned v0.13.0 backup artifacts: the internal/cli/backupfmt/ package (confirmed zero external imports) and the stale planning phases 01-07 (v0.13.0 backup planning that was superseded by the v0.16.0 snapshot design).

Purpose: Removes dead code and stale planning artifacts that could confuse future agents or developers. Per D-20, git history serves as the archive.
Output: 2 file deletions (backupfmt) + 7 directory deletions (planning phases).
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-backupable-interface-conformance-suite-cleanup/20-CONTEXT.md
@.planning/phases/20-backupable-interface-conformance-suite-cleanup/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete backupfmt package</name>
  <files>
    internal/cli/backupfmt/format.go,
    internal/cli/backupfmt/format_test.go
  </files>
  <read_first>
    internal/cli/backupfmt/format.go,
    internal/cli/backupfmt/format_test.go
  </read_first>
  <action>
    Pre-deletion verification per CLN-01:
    - Run grep -rn 'backupfmt' --include='*.go' to confirm zero imports outside the package itself (RESEARCH.md verified this on 2026-05-27; re-verify before delete)
    - If any external import found, abort and report

    Delete:
    - rm internal/cli/backupfmt/format.go
    - rm internal/cli/backupfmt/format_test.go
    - rmdir internal/cli/backupfmt/

    Post-deletion verification:
    - go build ./... must exit 0
    - grep -rn 'backupfmt' --include='*.go' must return empty
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && test ! -d internal/cli/backupfmt && go build ./... && grep -rn 'backupfmt' --include='*.go' | grep -v '^Binary' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - internal/cli/backupfmt/ directory does not exist
    - go build ./... exits 0
    - grep -rn 'backupfmt' --include='*.go' returns 0 matches
  </acceptance_criteria>
  <done>Orphaned backupfmt package deleted with zero build impact</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale v0.13.0 planning phases 01-07</name>
  <files>
    .planning/phases/01-foundations-models-manifest-capability-interface/,
    .planning/phases/02-per-engine-backup-drivers/,
    .planning/phases/03-destination-drivers-encryption/,
    .planning/phases/04-scheduler-retention/,
    .planning/phases/05-restore-orchestration-safety-rails/,
    .planning/phases/06-cli-rest-api-surface/,
    .planning/phases/07-testing-hardening/
  </files>
  <read_first>
    .planning/phases/20-backupable-interface-conformance-suite-cleanup/20-CONTEXT.md
  </read_first>
  <action>
    Per D-20, delete .planning/phases/01-* through 07-* entirely. These are stale v0.13.0 backup planning artifacts from a feature that was never released. Git history is the archive -- no milestones directory creation needed.

    Pre-deletion verification:
    - List the 7 directories to confirm they match expected names (01-foundations-models-manifest-capability-interface, 02-per-engine-backup-drivers, 03-destination-drivers-encryption, 04-scheduler-retention, 05-restore-orchestration-safety-rails, 06-cli-rest-api-surface, 07-testing-hardening)
    - Confirm no directory named 08-* or higher is being caught by the glob

    Delete:
    - rm -rf .planning/phases/01-foundations-models-manifest-capability-interface
    - rm -rf .planning/phases/02-per-engine-backup-drivers
    - rm -rf .planning/phases/03-destination-drivers-encryption
    - rm -rf .planning/phases/04-scheduler-retention
    - rm -rf .planning/phases/05-restore-orchestration-safety-rails
    - rm -rf .planning/phases/06-cli-rest-api-surface
    - rm -rf .planning/phases/07-testing-hardening

    Post-deletion verification:
    - ls .planning/phases/0[1-7]-* should return no matches
    - ls .planning/phases/08-* should still exist (v0.15.0 phases preserved)
    - ls .planning/phases/20-* should still exist (current phase)
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && ls .planning/phases/0[1-7]-* 2>&1 | grep -c "No such file" | xargs test 1 -eq && ls .planning/phases/08-* >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - ls .planning/phases/0[1-7]-* returns "No such file or directory"
    - ls .planning/phases/08-* still returns entries (v0.15.0 phases preserved)
    - ls .planning/phases/20-* still returns entries (current phase preserved)
    - No directory named 08-* or higher was deleted
  </acceptance_criteria>
  <done>Seven stale v0.13.0 planning phase directories deleted; active phases preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Deletion-only plan; no runtime code modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Denial of Service | .planning/phases/ | mitigate | Explicit directory-name listing (not glob) prevents accidental deletion of active phases 08+ |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase |
</threat_model>

<verification>
test ! -d internal/cli/backupfmt && ls .planning/phases/0[1-7]-* 2>&1 | grep -q "No such file" && go build ./...
</verification>

<success_criteria>
- backupfmt package deleted, zero build impact
- 7 stale planning directories deleted
- Active phases (08+, 20+) preserved
</success_criteria>

<output>
Create `.planning/phases/20-backupable-interface-conformance-suite-cleanup/20-03-SUMMARY.md` when done
</output>
