---
phase: 17-unified-blockstore
plan: 11
type: execute
wave: 6
depends_on: [17-05-PLAN.md, 17-06-PLAN.md, 17-07-PLAN.md, 17-08-PLAN.md, 17-09-PLAN.md, 17-10-PLAN.md]
files_modified:
  - .planning/phases/17-unified-blockstore/17-VERIFICATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Phase 16 D-06 warm-cache benchmark gate held: `BenchmarkRandReadVerified` ratio is ≤1.02 vs Phase 16's locked baseline (0.890). Captured as JSON in 17-VERIFICATION.md."
    - "Cross-OS build clean: `go build ./...` and `go vet ./...` exit 0 on darwin-arm64 (developer's local). Linux + Windows cross-compile via `GOOS=linux go build ./...` and `GOOS=windows go build ./...` also exit 0 (no platform-specific drift)."
    - "Race-detector clean: `go test -race -count=1 -timeout 600s ./pkg/blockstore/... ./pkg/controlplane/... ./cmd/dfs/...` exits 0 (with external-service-gated tests honoring existing skip logic)."
    - "LoC reduction goal met: `pkg/blockstore/` net delta is at least 30% reduction (target per ROADMAP success criterion #13 — under 4k from current ~6k). Measured via `git diff --stat develop...HEAD -- pkg/blockstore/`. If gate fails, capture actual + rationale in VERIFICATION.md and surface to checker for decision."
    - "Phase 17 success criteria #1–#13 from ROADMAP each individually verified — observable checks for each, results captured in 17-VERIFICATION.md as a table"
    - "All D-01..D-11 from CONTEXT.md verified — locked decisions each have an observable proof"
    - "End-to-end migration smoke test: build a small legacy `.blk` fixture, run `dfs migrate-to-cas`, confirm sentinel written, attempt `dfs start` against the migrated store, confirm server boots. Captured as a manual checkpoint (human-verify gate) — automation requires a working server fixture that exceeds plan budget."
    - "Threat-model review: every `<threat_model>` block across plans 01–10 carries forward into 17-VERIFICATION.md as a consolidated STRIDE table; all dispositions still valid; no new threats surfaced during implementation."
  artifacts:
    - path: .planning/phases/17-unified-blockstore/17-VERIFICATION.md
      provides: "Phase 17 verification report: all 13 success criteria + D-01..D-11 + perf gate + LoC measurement + threat-model consolidation"
      contains: "Phase 17 Verification"
  key_links:
    - from: .planning/phases/17-unified-blockstore/17-VERIFICATION.md
      to: .planning/phases/16-cache-mmap-removal/16-VERIFICATION.md (D-06 baseline ratio 0.890)
      via: "perf-gate carry-forward"
      pattern: "BenchmarkRandReadVerified"
    - from: .planning/phases/17-unified-blockstore/17-VERIFICATION.md
      to: .planning/ROADMAP.md (Phase 17 success criteria 1–13)
      via: "criteria mapping"
      pattern: "Phase 17"
---

<objective>
Final verification of Phase 17. Execute every measurable success criterion from ROADMAP (1–13), assert every D-01..D-11 decision was honored, hold Phase 16's warm-cache D-06 perf gate, measure LoC reduction, consolidate STRIDE threat models, and run a manual end-to-end migration smoke test. Produce `17-VERIFICATION.md` as the single document a checker/reviewer reads to approve the mega-PR.

Purpose: This is the last quality gate before the mega-PR is opened against `develop`. Plans 01–10 made the code changes; this plan proves they hang together. If any gate fails, surface to checker rather than silently shipping a degraded phase.

Output: A signed verification report enumerating every success criterion + decision + perf result, with green/yellow/red status per item. Human-verify checkpoint for the smoke test ensures Plan 09 boot-guard + Plan 08 migration tool actually work together against a real fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/16-cache-mmap-removal/16-VERIFICATION.md
@.planning/phases/17-unified-blockstore/17-05-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-07-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-08-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-09-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-10-SUMMARY.md
@CLAUDE.md

<interfaces>
The 13 ROADMAP success criteria are reproduced in the action sections below for direct mapping. Each criterion gets one row in the VERIFICATION.md success-criteria table.

The 11 CONTEXT.md decisions (D-01..D-11) are reproduced similarly. Each gets a row in the decisions table.

Phase 16 baseline (from `.planning/phases/16-cache-mmap-removal/16-VERIFICATION.md`):
- BenchmarkRandReadVerified ratio: **0.890** (Phase 16 baseline vs pre-Phase-16)
- Phase 17 gate: ≤1.02 vs Phase 16's 0.890 = absolute ratio ≤ (0.890 * 1.02) = 0.908

VERIFICATION.md document structure (write to file path:
.planning/phases/17-unified-blockstore/17-VERIFICATION.md):

  # Phase 17 Verification
  
  ## Build + lint + test
  
  | Check | Command | Status | Notes |
  |...| ... | ... | ... |
  
  ## Success Criteria (ROADMAP)
  
  | # | Criterion | Status | Evidence |
  |---|...|...|...|
  | 1 | BlockStore interfaces | ✅ | pkg/blockstore/blockstore.go:NN |
  | ... | ... | ... | ... |
  | 13 | Mega-PR + 30–40% LoC reduction | ✅/🟡 | git diff --stat result |
  
  ## Locked Decisions (CONTEXT.md)
  
  | ID | Decision | Honored? | Evidence |
  |---|...|...|...|
  
  ## Perf gate (Phase 16 D-06 carry-forward)
  
  Baseline (Phase 16): 0.890
  This phase: <observed>
  Gate: ≤ 1.02 * baseline (≤ 0.908)
  Result: PASS / FAIL
  
  Raw benchmark output: ...
  
  ## LoC reduction
  
  `git diff --stat develop...HEAD -- pkg/blockstore/`:
  +X -Y net Z = N%
  
  ## STRIDE consolidation
  
  | Plan | Threat ID | Disposition | Mitigation status |
  | ... | ... | ... | ... |
  
  ## Smoke test (human-verify checkpoint result)
  
  ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification suite + write 17-VERIFICATION.md scaffolding</name>
  <files>.planning/phases/17-unified-blockstore/17-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 17 entry — the 13 success criteria + 11 decisions)
    - .planning/phases/16-cache-mmap-removal/16-VERIFICATION.md (Phase 16 perf baseline — 0.890)
    - .planning/phases/17-unified-blockstore/17-CONTEXT.md (D-01..D-11)
    - Each of plans 01..10's SUMMARY for evidence pointers (file:line, test names, etc.)
  </read_first>
  <action>
  Step 1 — capture the build + lint + test baseline. From repo root, run and record output:
  - `go vet ./...` — capture exit code + any output.
  - `GOOS=linux go build ./...` — capture exit code.
  - `GOOS=windows go build ./...` — capture exit code.
  - `go build ./...` (host) — capture exit code.
  - `go test -count=1 -timeout 600s ./pkg/blockstore/... ./pkg/controlplane/... ./cmd/dfs/...` — capture pass/fail tallies; honor existing skip guards for Localstack-gated tests.
  - `go test -race -count=1 -timeout 900s ./pkg/blockstore/... ./pkg/controlplane/...` — capture pass/fail + any DATA RACE reports (any race = automatic RED).
  - Record results in the VERIFICATION.md "Build + lint + test" table.
  
  Step 2 — measure LoC reduction. Compute via git:
  - From the branch's merge-base with develop: `git merge-base develop HEAD` — capture the commit hash.
  - `git diff --stat <merge-base>..HEAD -- pkg/blockstore/` — capture insertions + deletions + net delta.
  - Express as percentage: `(insertions - deletions) / pre_phase_LoC * 100`. The pre-phase LoC count: `git show <merge-base>:.` won't directly give it; instead run `git diff --stat <merge-base>..HEAD -- pkg/blockstore/ | tail -1`, parse the summary line.
  - Cross-check against the success-criterion target: "~30–40% net reduction in `pkg/blockstore/` (target under 4k from current ~6k)". Acceptable: net deletion ≥ 30% of pre-phase LoC.
  - Record in VERIFICATION.md "LoC reduction" section.
  
  Step 3 — execute the warm-cache D-06 benchmark:
  - From Phase 16 VERIFICATION's exact command: `go test -bench=BenchmarkRandReadVerified -benchtime=3x -benchmem -count=5 ./pkg/blockstore/engine/`
  - Capture all five run outputs.
  - Compute mean ns/op, compare against Phase 16's baseline ratio of 0.890.
  - Gate: current_ratio ≤ 1.02 × 0.890 = 0.908. Express as PASS if observed ratio ≤ 0.908, FAIL otherwise.
  - If FAIL: do NOT silently proceed; flag in VERIFICATION.md and surface to checker via the structured return at end of plan.
  - Record raw output + computed ratio in the "Perf gate" section.
  
  Step 4 — enumerate success criteria (1–13) per ROADMAP:
  - For each numbered criterion, identify the observable check (most are file-existence + grep-pattern + test-pass triples).
  - Examples:
    - **#1** `pkg/blockstore/blockstore.go` defines BlockStore + BlockStoreAppend + Meta — `test -f pkg/blockstore/blockstore.go && grep -c 'type BlockStore interface\\|type BlockStoreAppend interface\\|type Meta struct' pkg/blockstore/blockstore.go` returns 3.
    - **#2** RemoteStore renames + Walk collapse — `grep -c 'func .Put(\\|func .Get(\\|func .Walk(\\|func .Head(' pkg/blockstore/remote/remote.go` returns ≥ 4; `grep -c 'WriteBlock\\|ReadBlock\\|ListByPrefix\\|DeleteByPrefix\\|CopyBlock' pkg/blockstore/remote/remote.go` returns 0.
    - **#3** LocalStore narrowed + embeds BlockStoreAppend — `grep -c 'BlockStoreAppend' pkg/blockstore/local/local.go` ≥ 1; deleted helpers all return 0 via the negative grep.
    - **#4** write.go deleted — `test ! -f pkg/blockstore/local/fs/write.go`.
    - **#5** dual-read shim deleted — `grep -c 'ErrLegacyReadOnCASOnly\\|BlockLayoutCASOnly' pkg/blockstore/engine/` returns 0.
    - **#6** blockstoretest unified + localtest/remotetest deleted — `test -d pkg/blockstore/blockstoretest && test ! -d pkg/blockstore/local/localtest && test ! -d pkg/blockstore/remote/remotetest`.
    - **#7** ErrStopWalk + ErrLegacyLayoutDetected present — `grep -c 'ErrStopWalk\\|ErrLegacyLayoutDetected' pkg/blockstore/errors.go` ≥ 2.
    - **#8** dfs migrate-to-cas subcommand exists + PID-guarded + supports all flags — `test -f cmd/dfs/commands/migrate_to_cas.go && /tmp/dfs migrate-to-cas --help 2>&1 | grep -c -- '\\-\\-storage-dir\\|\\-\\-share\\|\\-\\-dry-run\\|\\-\\-json\\|\\-\\-config'` returns 5.
    - **#9** Idempotent + journaled — Plan 08's TestMigrateShareToCAS_JournalResume passes.
    - **#10** Sentinel atomic-rename + NewFSStore guard — Plan 08's TestMigrateShareToCAS_SentinelAtomic + Plan 09's TestNewFSStore_SentinelDetection pass.
    - **#11** start.go unwrap + exit 78 — Plan 09's TestStart_LegacyLayoutExitCode passes.
    - **#12** D-06 gate held + cross-OS clean + go vet + go test -race — covered by Step 1 + Step 3.
    - **#13** Mega-PR + 30–40% reduction + no compat shims — covered by Step 2 + the single-PR-on-develop git history check.
  - Record each as a row in the success-criteria table with PASS/FAIL/PARTIAL + evidence column (file path or test name).
  
  Step 5 — enumerate decisions D-01..D-11:
  - For each decision, identify the observable proof in shipped code:
    - **D-01** mega-PR shape — observable via `git log <merge-base>..HEAD` showing internal commit hygiene; not a code grep.
    - **D-02** offline subcommand placement — `test -f cmd/dfs/commands/migrate_to_cas.go && test ! -f cmd/dfsctl/commands/blockstore/migrate_to_cas.go`.
    - **D-03** idempotent journal — Plan 08 test pass + `grep -c '.dittofs-migrate-to-cas.state' pkg/blockstore/migrate/migrate_to_cas.go`.
    - **D-04** dry-run flag — `/tmp/dfs migrate-to-cas --help | grep -c -- '--dry-run'`.
    - **D-05** --share flag — same.
    - **D-06** progress + --json — same.
    - **D-07** Walk callback semantics — `grep -c 'ErrStopWalk' pkg/blockstore/blockstore.go`.
    - **D-08** Meta = {Size, LastModified} only — `grep -A3 'type Meta struct' pkg/blockstore/blockstore.go` shows exactly two fields.
    - **D-09** two-entrypoint conformance — `grep -c 'func BlockStoreConformance\\|func BlockStoreAppendConformance' pkg/blockstore/blockstoretest/`.
    - **D-10** sentinel atomic-rename — Plan 08 test pass.
    - **D-11** typed sentinel + errors.Is + exit 78 — Plan 09 test pass + `grep -c 'errors.Is.*ErrLegacyLayoutDetected' cmd/dfs/commands/start.go`.
  - Record results in VERIFICATION.md decisions table.
  
  Step 6 — consolidate STRIDE tables from plans 01–10:
  - Collect every `<threat_model>` block from PLAN files 01..10.
  - Aggregate into a single table: Plan ID / Threat ID / Category / Component / Disposition / Mitigation Status.
  - Mitigation Status: VERIFIED if the mitigation references a passing test or grep gate; ACCEPTED-AS-PLANNED if the disposition is "accept"; UNKNOWN if the mitigation has no measurable check.
  
  Step 7 — write all of the above to `.planning/phases/17-unified-blockstore/17-VERIFICATION.md` in the document structure shown in the <interfaces> block.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go vet ./... && go build ./... && go test -count=1 -timeout 600s ./pkg/blockstore/... ./cmd/dfs/... && test -f .planning/phases/17-unified-blockstore/17-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-unified-blockstore/17-VERIFICATION.md` exists; word count ≥ 800
    - `go vet ./...` exits 0
    - `go build ./...` exits 0
    - `GOOS=linux go build ./...` exits 0
    - `GOOS=windows go build ./...` exits 0
    - `go test -race -count=1 -timeout 900s ./pkg/blockstore/... ./pkg/controlplane/...` exits 0
    - VERIFICATION.md contains a success-criteria table with rows for each of 1–13 — `grep -cE '^\\| 1[0-3]? \\|' .planning/phases/17-unified-blockstore/17-VERIFICATION.md` returns at least 13
    - VERIFICATION.md contains a decisions table with rows for D-01..D-11 — `grep -cE 'D-0[1-9]|D-1[01]' .planning/phases/17-unified-blockstore/17-VERIFICATION.md` returns at least 11
    - Perf-gate result recorded with a numeric ratio + PASS/FAIL verdict
    - LoC reduction recorded with insertions/deletions/percent
    - Threat-model consolidation present
    - If perf gate FAILED or LoC reduction is < 30%: marked clearly and surfaced to checker; do NOT mark the phase as complete-pending-checker without flagging
  </acceptance_criteria>
  <done>
    Automated verification complete; all 13 success criteria + 11 decisions evaluated; perf gate + LoC measured; STRIDE consolidated; VERIFICATION.md written.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end migration smoke test (human-verify)</name>
  <what-built>
    Plans 01–10 collectively shipped: unified BlockStore interface; legacy `.blk` writer deleted; migration tool `dfs migrate-to-cas` with --dry-run/--share/--json; boot-guard via .cas-migrated-v1 sentinel + exit 78. Task 1 of this plan verified each piece in isolation via automated tests.
    
    This checkpoint verifies the integration end-to-end on a real fixture, against a real dfs binary, with the operator-facing experience (stderr text, exit codes, JSON progress, sentinel file contents) matching expectations.
  </what-built>
  <how-to-verify>
    1. Build the binary:
       ```
       cd /Users/marmos91/Projects/dittofs-409
       go build -o /tmp/dfs ./cmd/dfs/
       ```
    
    2. Prepare a temp legacy storage fixture:
       ```
       mkdir -p /tmp/dfs-phase17-smoke/storage/myshare/file-001
       dd if=/dev/urandom of=/tmp/dfs-phase17-smoke/storage/myshare/file-001/0.blk bs=1M count=4
       dd if=/dev/urandom of=/tmp/dfs-phase17-smoke/storage/myshare/file-001/1.blk bs=1M count=4
       ```
       The fixture has no `.cas-migrated-v1` sentinel — this is the un-migrated state.
       (You may need to also pre-populate the metadata store with matching legacy FileAttr records depending on Plan 08's MetadataAdapter implementation. If your Plan 08 implementation requires that, set up a config that points at an empty Badger or Memory metadata store and seed it; otherwise the migration tool may report 0 files to migrate. Adapt as needed and note in resume-signal.)
    
    3. Verify boot guard fires:
       ```
       /tmp/dfs start --config /tmp/dfs-phase17-smoke/config.yaml
       echo "exit code: $?"
       ```
       **Expected:**
       - Exit code: **78**
       - Stderr contains: "Detected legacy .blk layout"
       - Stderr contains: "dfs migrate-to-cas"
       - Stderr contains: "docs/CONFIGURATION.md"
    
    4. Run dry-run:
       ```
       /tmp/dfs migrate-to-cas --storage-dir /tmp/dfs-phase17-smoke/storage --dry-run
       ```
       **Expected:**
       - Exits 0
       - Reports file_count, total_bytes, estimated_dedup_ratio, estimated_migration_duration
       - `/tmp/dfs-phase17-smoke/storage/myshare/.cas-migrated-v1` is NOT created
       - `/tmp/dfs-phase17-smoke/storage/myshare/file-001/*.blk` files are STILL PRESENT
    
    5. Run actual migration with JSON progress:
       ```
       /tmp/dfs migrate-to-cas --storage-dir /tmp/dfs-phase17-smoke/storage --json
       ```
       **Expected:**
       - Exits 0
       - Each progress line is valid JSON with keys: ts, share, files_done, bytes_done, files_per_sec, mib_per_sec, dedup_hits, eta_seconds
       - `/tmp/dfs-phase17-smoke/storage/myshare/.cas-migrated-v1` is created
       - File contents parse as JSON with keys: Version, CompletedAt, ToolVersion, ShareDir
       - `/tmp/dfs-phase17-smoke/storage/myshare/.cas-migrated-v1.tmp` does NOT exist
       - Per-share journal `/tmp/dfs-phase17-smoke/storage/myshare/.dittofs-migrate-to-cas.state` is DELETED post-success
       - The legacy `.blk` files are DELETED from `myshare/file-001/`
    
    6. Verify boot guard now passes:
       ```
       /tmp/dfs start --config /tmp/dfs-phase17-smoke/config.yaml &
       sleep 3
       kill %1
       ```
       **Expected:**
       - Server starts successfully (no exit 78)
       - No legacy-layout directive on stderr
    
    7. Cleanup:
       ```
       rm -rf /tmp/dfs-phase17-smoke
       ```
    
    Append a "Smoke test" section to `.planning/phases/17-unified-blockstore/17-VERIFICATION.md` with the observed outputs (exit codes, stderr excerpts, sentinel file contents) and PASS/FAIL verdict.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - **`approved`** — all six smoke-test steps passed; VERIFICATION.md updated with results.
    - **`approved-with-notes: <text>`** — passed but with observations the checker should know (e.g., metadata-adapter fixture setup required extra work; expected-behavior verbiage off but functional).
    - **`failed: <step>: <description>`** — a specific step failed; describe what was observed; phase is NOT complete; planner should be re-engaged to fix the underlying gap.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker → VERIFICATION.md | The VERIFICATION.md document is the basis on which the mega-PR is approved. Inaccuracy here = silent shipping of a broken phase. |
| smoke-test fixture | Manual setup may differ from real operator environments (e.g., metadata store seeding). False positives possible if fixture is over-fitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11-01 | R (Repudiation) | VERIFICATION.md PASS verdict on an item that isn't actually green | mitigate | Every row's "Evidence" column points at a file path / test name / command output — checker can re-run independently. Acceptance criteria gate on counts (≥13 success-criteria rows, ≥11 decision rows). |
| T-17-11-02 | T (Tampering) | Smoke test passes against a contrived fixture but fails on real operator data | mitigate | Smoke-test instructions use random data and a fresh storage dir — mirrors a fresh upgrade. resume-signal includes notes channel for fixture quirks. |
| T-17-11-03 | D (DoS) | Long-running test suite (race + perf benchmark) times out in CI | mitigate | Task 1 acceptance criteria use 600s/900s timeouts that match existing repo conventions. If exceeded in practice, planner re-engagement to split. |
</threat_model>

<verification>
- 17-VERIFICATION.md exists with: build/lint/test table, success-criteria table (1–13), decisions table (D-01..D-11), perf-gate result, LoC measurement, STRIDE consolidation, smoke-test results
- Perf gate (BenchmarkRandReadVerified ≤ 0.908 absolute) PASS or escalated
- LoC reduction ≥ 30% or escalated
- Race-detector clean across pkg/blockstore + pkg/controlplane
- Cross-OS build clean (linux + windows cross-compile)
- Smoke-test human-verify checkpoint resolved
</verification>

<success_criteria>
A checker reading 17-VERIFICATION.md alone can decide whether to approve the mega-PR. Every Phase 17 ROADMAP success criterion has a green/yellow/red verdict with evidence. Every D-01..D-11 decision has a measurable proof. Perf gate held; LoC reduction met; threat model consolidated. End-to-end smoke test confirms boot guard + migration tool + sentinel + post-migration boot all hang together against a real fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-11-SUMMARY.md` referencing the now-existing 17-VERIFICATION.md
</output>
