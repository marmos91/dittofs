---
phase: 17-unified-blockstore
plan: 03
type: execute
wave: 2
depends_on: [17-01-PLAN.md]
files_modified:
  - pkg/blockstore/remote/remote.go
  - pkg/blockstore/remote/s3/store.go
  - pkg/blockstore/remote/s3/store_test.go
  - pkg/blockstore/remote/s3/verifier.go
  - pkg/blockstore/remote/memory/store.go
  - pkg/blockstore/remote/memory/store_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "RemoteStore interface methods are renamed per CONTEXT.md D-01 deletion list: `WriteBlock`→`Put`, `WriteBlockWithHash`→absorbed by `Put` (Put always takes hash as the key), `ReadBlock`→`Get`, `ReadBlockRange`→`GetRange`, `DeleteBlock`→`Delete`, `HeadObject`→`Head`, `ListByPrefix`+`ListByPrefixWithMeta`+`DeleteByPrefix`→`Walk`"
    - "`Put` signature: `Put(ctx context.Context, hash ContentHash, data []byte) error` — keyed by hash, not by opaque string blockKey"
    - "`Get`, `GetRange`, `Delete`, `Head` are keyed by `ContentHash`, not by opaque string blockKey"
    - "`Head` returns `blockstore.Meta{Size, LastModified}` — NOT the old `HeadResult{ContentLength, Metadata}` (D-08 — hash is the key, x-amz-meta-content-hash stays internal to the s3 backend for BSCAS-06 defense-in-depth, never echoed via Meta)"
    - "`Walk(ctx, fn)` mirrors `BlockStore.Walk` contract from Plan 01: ErrStopWalk for clean exit, any other error wraps as `walk halted at %s: %w`, ctx cancellation aborts immediately"
    - "`CopyBlock` method is DELETED from the RemoteStore interface and from `remote/s3/store.go` (Phase 17 D-01 — no longer needed; CAS keys are immutable so copy collapses to no-op + refcount bump handled at metadata layer)"
    - "`ReadBlockVerified` method REMAINS — the verified-read path is the production CAS read on s3 (BSCAS-06); it's a backend-specific extension method on `*s3.Store` (NOT part of the unified `BlockStore` interface) and continues to be called by engine via type assertion or backend-local wrapper. The unified `Get` returns raw bytes; verification stays in the engine's `dispatchRemoteFetch` (Plan 05 retargets this)."
    - "s3 backend's `x-amz-meta-content-hash` PUT header preserved for BSCAS-06; `Head` reads `Content-Length` + `LastModified` from S3 response but does NOT expose `Metadata` map"
    - "memory backend implements the same 7-method surface; test backends in `remotetest/` will be redirected to `blockstoretest/` in Plan 07"
    - "`go test ./pkg/blockstore/remote/...` passes (s3 + memory backend tests adapted to renamed methods)"
    - "`internal/adapter/` and `pkg/controlplane/` build clean (post-Phase-9 adapters do NOT call RemoteStore directly; engine retargeting lands in Plan 05)"
  artifacts:
    - path: pkg/blockstore/remote/remote.go
      provides: "Renamed RemoteStore interface (7 methods + ReadBlockVerified extension + Close/Healthcheck)"
      contains: "type RemoteStore interface"
    - path: pkg/blockstore/remote/s3/store.go
      provides: "s3 backend matching renamed RemoteStore"
      contains: "func (s *Store) Put"
    - path: pkg/blockstore/remote/memory/store.go
      provides: "memory backend matching renamed RemoteStore"
      contains: "func (s *Store) Put"
  key_links:
    - from: pkg/blockstore/remote/remote.go
      to: pkg/blockstore/blockstore.go (BlockStore interface — RemoteStore is now structurally compatible)
      via: "method signatures match"
      pattern: "Put|Get|GetRange|Delete|Head|Walk"
    - from: pkg/blockstore/remote/s3/store.go
      to: pkg/blockstore/remote/s3/verifier.go
      via: "ReadBlockVerified flow"
      pattern: "verifyReader|verifyMetadata"
---

<objective>
Rename `RemoteStore` interface methods to match the unified `BlockStore` contract from Plan 01, collapse the list/delete-by-prefix surface into `Walk`, delete `CopyBlock`, and apply the same renames to the s3 and memory backends. Keep `ReadBlockVerified` as a backend-specific extension method on `*s3.Store` so the engine can continue to demand BLAKE3 verification on production CAS reads (Plan 05 retargets the call site).

Purpose: Wave 2 retargets the remote-store consumer surface so that Wave 3 (engine) can switch to the unified type. The s3 backend's `x-amz-meta-content-hash` header stays for BSCAS-06 defense-in-depth — Plan 03 preserves the PUT-side stamping but does NOT expose it through `Meta` (D-08).

Output: Six files modified. Engine and other consumers still use `RemoteStore` (with new method names) — engine retargeting onto the unified `BlockStore` type lands in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/17-unified-blockstore/17-PATTERNS.md
@.planning/phases/17-unified-blockstore/17-01-SUMMARY.md
@CLAUDE.md
@pkg/blockstore/blockstore.go
@pkg/blockstore/remote/remote.go

<interfaces>
<!-- The new RemoteStore method set after Plan 03. -->

```go
// pkg/blockstore/remote/remote.go (target shape after this plan)
type RemoteStore interface {
    Put(ctx context.Context, hash blockstore.ContentHash, data []byte) error
    Get(ctx context.Context, hash blockstore.ContentHash) ([]byte, error)
    GetRange(ctx context.Context, hash blockstore.ContentHash, offset, length int64) ([]byte, error)
    Delete(ctx context.Context, hash blockstore.ContentHash) error
    Head(ctx context.Context, hash blockstore.ContentHash) (blockstore.Meta, error)
    Walk(ctx context.Context, fn func(hash blockstore.ContentHash, meta blockstore.Meta) error) error

    // ReadBlockVerified is a backend-specific extension (NOT part of BlockStore) —
    // engine uses type assertion to access it on s3 for verified CAS reads (BSCAS-06).
    ReadBlockVerified(ctx context.Context, hash blockstore.ContentHash, expected blockstore.ContentHash) ([]byte, error)

    Close() error
    HealthCheck(ctx context.Context) error
    Healthcheck(ctx context.Context) health.Report
}
```

Compare to existing pkg/blockstore/remote/remote.go:
- DELETE: WriteBlock, WriteBlockWithHash (collapsed into Put — hash always present, hash is the key), DeleteByPrefix, ListByPrefix, ListByPrefixWithMeta (collapsed into Walk), CopyBlock (D-01)
- RENAME: ReadBlock→Get, ReadBlockRange→GetRange, DeleteBlock→Delete, HeadObject→Head
- KEEP: ReadBlockVerified (engine still uses; signature updated to take ContentHash)
- KEEP: Close, HealthCheck, Healthcheck
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename pkg/blockstore/remote/remote.go interface methods + delete CopyBlock + collapse list/delete-by-prefix into Walk</name>
  <files>pkg/blockstore/remote/remote.go</files>
  <read_first>
    - pkg/blockstore/remote/remote.go (entire file — the interface being rewritten)
    - pkg/blockstore/blockstore.go (the unified contract from Plan 01)
    - pkg/blockstore/types.go (ContentHash type, FormatCASKey, ParseCASKey)
  </read_first>
  <behavior>
    - `RemoteStore` interface now declares exactly the methods listed in the <interfaces> block above
    - `HeadResult` struct is DELETED (replaced by `blockstore.Meta`)
    - `ObjectInfo` struct is DELETED (replaced by `blockstore.Meta` passed via Walk callback)
    - `Head` returns `blockstore.Meta` (NOT a backend-specific HeadResult); the s3 backend internally reads `x-amz-meta-content-hash` for BSCAS-06 verification but never echoes it via Meta
    - `Walk` godoc carries the ErrStopWalk contract from D-07: callback returning ErrStopWalk → Walk returns nil; any other error wraps as `walk halted at %s: %w, hash, err`; ctx cancellation aborts immediately
    - `ReadBlockVerified` signature: `ReadBlockVerified(ctx, hash ContentHash, expected ContentHash) ([]byte, error)` — both args keyed by hash; the redundancy is intentional (BSCAS-06 — caller may not yet have computed the canonical hash if they read from a CAS-keyed path themselves; verification proves byte-on-disk == hash-in-key == expected)
    - `go vet ./pkg/blockstore/remote/` clean (consumers in s3/memory subpackages are updated in Tasks 2-3)
  </behavior>
  <action>Rewrite `pkg/blockstore/remote/remote.go`:

  1. Update package doc comment at top — remove the legacy key-format documentation (`{payloadID}/block-{blockIdx}` lines 60-62); replace with: "RemoteStore is the unified remote-store interface for CAS-keyed block storage. All operations are keyed by `blockstore.ContentHash`. The CAS object key shape (`cas/{hh}/{hh}/{hex}`) is derived from the hash via `blockstore.FormatCASKey` and is an implementation detail backends may not expose."
  
  2. DELETE `type HeadResult struct` and `type ObjectInfo struct` (lines 11-55). Their data is now carried via `blockstore.Meta` from Plan 01.
  
  3. Replace the `RemoteStore interface` body (lines 63-149) with the target shape from the `<interfaces>` block. Godoc on each method:
     - `Put` — copy the `WriteBlockWithHash` godoc from the existing file (lines 67-75) and adjust wording: "Put writes data under the CAS-shaped key derived from hash. Backends MUST stamp the content hash atomically with the PUT (S3: x-amz-meta-content-hash header) — BSCAS-06 defense-in-depth. Idempotent on the same `(hash, data)` pair."
     - `Get` — verbatim wording from Plan 01's `BlockStore.Get` godoc, plus a note: "For S3, prefer `ReadBlockVerified` on the production read path — `Get` returns raw bytes WITHOUT BLAKE3 verification."
     - `GetRange` — adapt existing `ReadBlockRange` godoc (line 95-96); replace `blockKey string` with `hash ContentHash`.
     - `Delete` — adapt existing `DeleteBlock` godoc (line 98-99); replace `blockKey string` with `hash ContentHash`. Returns nil if missing.
     - `Head` — adapt existing `HeadObject` godoc (lines 114-128) — replace `key string` with `hash ContentHash`; return type is now `blockstore.Meta` (NOT HeadResult); the `x-amz-meta-content-hash` value is verified internally by the s3 backend (BSCAS-06) but is NOT echoed in Meta (D-08).
     - `Walk` — paste the contract block from PATTERNS.md lines 92-101 (D-07). Reference `blockstore.ErrStopWalk`.
     - `ReadBlockVerified` — keep godoc (lines 81-93) but update the signature: takes `hash ContentHash, expected ContentHash`; the doc explains both args because the canonical key derives from `hash` but verification compares body BLAKE3 against `expected` (caller-supplied — guards against key-vs-content mismatch).
     - `Close`, `HealthCheck`, `Healthcheck` — UNCHANGED (carry existing godoc verbatim).
  
  Do NOT change the `health.Report` import. Do NOT touch the s3 or memory backend in this task (Tasks 2 and 3 do those).</action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/remote/ &amp;&amp; grep -cE 'WriteBlock\b|ReadBlock\b|DeleteBlock\b|HeadObject\b|ListByPrefix\b|DeleteByPrefix\b|CopyBlock\b|WriteBlockWithHash' pkg/blockstore/remote/remote.go</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/remote/` exits 0
    - `grep -cE 'WriteBlock\b|ReadBlock\b|DeleteBlock\b|HeadObject\b|ListByPrefix\b|DeleteByPrefix\b|CopyBlock\b|WriteBlockWithHash' pkg/blockstore/remote/remote.go` returns 0 (zero remaining legacy method names; the `\b` word boundaries exclude `ReadBlockVerified`)
    - `grep -cE '\b(Put|Get|GetRange|Delete|Head|Walk)\(' pkg/blockstore/remote/remote.go` returns at least 6
    - `grep -c 'ReadBlockVerified' pkg/blockstore/remote/remote.go` returns at least 1 (extension method kept)
    - `grep -c 'HeadResult\|ObjectInfo' pkg/blockstore/remote/remote.go` returns 0 (both deleted)
    - `grep -c 'blockstore.Meta\|blockstore.ContentHash\|blockstore.ErrStopWalk' pkg/blockstore/remote/remote.go` returns at least 6 (the new types are used)
    - `go build ./...` returns NON-ZERO exit code (expected — s3 + memory backends do not yet match the renamed interface; this is fixed in Tasks 2-3)
  </acceptance_criteria>
  <done>
    Interface renamed; legacy method names absent from `remote.go`; the s3 + memory backends are now compile-broken until Tasks 2-3 land.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update pkg/blockstore/remote/s3/ — Put, Get, GetRange, Delete, Head, Walk + delete CopyBlock + adapt ReadBlockVerified</name>
  <files>pkg/blockstore/remote/s3/store.go, pkg/blockstore/remote/s3/store_test.go, pkg/blockstore/remote/s3/verifier.go</files>
  <read_first>
    - pkg/blockstore/remote/s3/store.go (the entire file; existing WriteBlock/WriteBlockWithHash/ReadBlock/ReadBlockRange/DeleteBlock/HeadObject/ListByPrefix/ListByPrefixWithMeta/DeleteByPrefix/CopyBlock impls)
    - pkg/blockstore/remote/s3/verifier.go (the BLAKE3 verifier and x-amz-meta-content-hash check)
    - pkg/blockstore/remote/s3/store_test.go (tests calling the legacy method names)
    - pkg/blockstore/remote/remote.go (the new interface — Task 1 just landed)
    - pkg/blockstore/types.go (FormatCASKey signature)
  </read_first>
  <behavior>
    - `*s3.Store` satisfies the new `RemoteStore` interface — `var _ remote.RemoteStore = (*Store)(nil)` compile-time assertion exists in `store.go`
    - `Put(ctx, hash, data)` derives the S3 key via `blockstore.FormatCASKey(hash)`, stamps `x-amz-meta-content-hash: hash.CASKey()` (BSCAS-06), and writes the body — collapses the existing `WriteBlock` + `WriteBlockWithHash` into one method (D-01)
    - `Get`, `GetRange`, `Delete` accept a `ContentHash` and derive the S3 key internally via `FormatCASKey`
    - `Head` returns `blockstore.Meta{Size: ContentLength, LastModified: response.LastModified}` — does NOT expose any `Metadata` map (D-08)
    - `Walk` wraps the existing `ListByPrefixWithMeta` over prefix `"cas/"`, parses each key via `ParseCASKey`, dispatches to the callback with `(hash, Meta{...})`, honors `ErrStopWalk` (returns nil) and wraps other errors as `walk halted at %s: %w` (D-07)
    - `ReadBlockVerified` signature updated: takes `hash ContentHash, expected ContentHash`; derives the S3 key via `FormatCASKey(hash)`; uses existing verifier logic; on success returns the verified body
    - `CopyBlock` method is DELETED from `store.go`
    - `ListByPrefix`, `ListByPrefixWithMeta`, `DeleteByPrefix` methods are DELETED from `store.go`
    - `WriteBlock`, `WriteBlockWithHash`, `ReadBlock`, `ReadBlockRange`, `DeleteBlock`, `HeadObject` methods are DELETED from `store.go`
    - `store_test.go` updated: every call to a renamed/deleted method uses the new method names; tests for deleted methods (CopyBlock, ListByPrefix/DeleteByPrefix) are removed
    - `go test ./pkg/blockstore/remote/s3/` passes (against existing fixtures + LocalStack if available; conformance against `blockstoretest/` lands in Plan 07)
  </behavior>
  <action>
  Step 1 — `store.go`:
  - Add `var _ remote.RemoteStore = (*Store)(nil)` compile-time assertion near the top of the file (after the type definition).
  - Add the new `Put(ctx, hash blockstore.ContentHash, data []byte) error` method. Body: `key := blockstore.FormatCASKey(hash)`; reuse the body from existing `WriteBlockWithHash` (lines 246-265 per PATTERNS.md) — the only changes are (a) compute key from hash via `FormatCASKey`, (b) the function name. Keep the `x-amz-meta-content-hash` header stamping for BSCAS-06.
  - Add `Get(ctx, hash) ([]byte, error)`. Body: derive S3 key via `FormatCASKey(hash)`; reuse existing `ReadBlock` body. Returns raw bytes WITHOUT BLAKE3 verification.
  - Add `GetRange(ctx, hash, offset, length) ([]byte, error)`. Reuse existing `ReadBlockRange` body with hash-derived key.
  - Add `Delete(ctx, hash) error`. Reuse existing `DeleteBlock` body with hash-derived key.
  - Add `Head(ctx, hash) (blockstore.Meta, error)`. Reuse existing `HeadObject` body but: derive key via `FormatCASKey`; return value is `blockstore.Meta{Size: *resp.ContentLength, LastModified: ts}` where `ts` is `*resp.LastModified` (Go time.Time from AWS SDK).
  - Add `Walk(ctx, fn) error`. Implementation per PATTERNS.md lines 616-630: call internal `listByPrefixWithMeta(ctx, "cas/")` (rename the existing private helper, or temporarily keep it as a private method), iterate, `ParseCASKey(o.Key)` skipping non-CAS keys, dispatch to `fn(hash, blockstore.Meta{...})`, on `errors.Is(cberr, blockstore.ErrStopWalk)` return nil, on any other non-nil callback error return `fmt.Errorf("walk halted at %s: %w", hash, cberr)`, check `ctx.Err()` after each iteration.
  - Adapt `ReadBlockVerified` to the new signature `ReadBlockVerified(ctx, hash, expected ContentHash) ([]byte, error)`. Derive the S3 key via `FormatCASKey(hash)`. The verifier logic (BLAKE3 of body + x-amz-meta-content-hash check) is unchanged — only the key derivation changes.
  - DELETE: `WriteBlock`, `WriteBlockWithHash`, `ReadBlock`, `ReadBlockRange`, `DeleteBlock`, `HeadObject`, `ListByPrefix`, `ListByPrefixWithMeta` (or convert to private helper if Walk depends on it; the conformance suite never calls it), `DeleteByPrefix`, `CopyBlock` methods.
  
  Step 2 — `verifier.go`:
  - The verifier compares body BLAKE3 against an expected hash. Update any external entry points it exposes so the hash-passed-in is `ContentHash` not a key-string. If the verifier is purely internal (only called from `ReadBlockVerified`), no signature change is needed.
  
  Step 3 — `store_test.go`:
  - Find every call to a renamed method (`grep -n 'WriteBlock\|WriteBlockWithHash\|ReadBlock\b\|ReadBlockRange\|DeleteBlock\b\|HeadObject\|ListByPrefix\|DeleteByPrefix\|CopyBlock' pkg/blockstore/remote/s3/store_test.go`). For each:
    - Tests of `WriteBlock` or `WriteBlockWithHash`: rename to `Put`, change args from `(ctx, blockKey, data)` or `(ctx, blockKey, hash, data)` to `(ctx, hash, data)`. The blockKey is now derived from hash; tests that asserted on a specific key string MUST switch to asserting on `FormatCASKey(hash)`.
    - Tests of `ReadBlock`, `ReadBlockRange`, `DeleteBlock`, `HeadObject`: rename to `Get`, `GetRange`, `Delete`, `Head`; arg signature change as above.
    - Tests of `ListByPrefix`, `ListByPrefixWithMeta`: rewrite as `Walk` callbacks; assert the collected hashes match expected set.
    - Tests of `DeleteByPrefix`: remove (no replacement — the concept is gone; conformance + Walk-based GC sweep cover correctness in Plan 07).
    - Tests of `CopyBlock`: DELETE (method removed).
    - Tests of `HeadObject` that asserted on `Metadata` map content: rewrite to use `ReadBlockVerified` (which exercises the BSCAS-06 header internally) — the unified `Head` does not expose the metadata map.
  
  Verify the test file still compiles. The test file MAY shrink considerably (CopyBlock + DeleteByPrefix paths gone); that is intended.</action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/remote/s3/ &amp;&amp; go test -count=1 ./pkg/blockstore/remote/s3/ &amp;&amp; grep -cE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/remote/s3/store.go</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/remote/s3/` exits 0
    - `go test -count=1 ./pkg/blockstore/remote/s3/` exits 0
    - `grep -cE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/remote/s3/store.go` returns 0 (zero legacy method names in source; the `\b` word boundaries exclude `ReadBlockVerified`)
    - `grep -c 'ReadBlockVerified' pkg/blockstore/remote/s3/store.go` returns at least 1 (kept; signature updated)
    - `grep -c 'var _ remote.RemoteStore = (\*Store)(nil)' pkg/blockstore/remote/s3/store.go` returns 1 (compile-time assertion)
    - `grep -c 'x-amz-meta-content-hash\|content-hash' pkg/blockstore/remote/s3/store.go` returns at least 1 (BSCAS-06 header preserved on Put)
    - All `Put`/`Get`/`GetRange`/`Delete`/`Head` method signatures use `blockstore.ContentHash` as the key parameter (not `blockKey string`); verified by `grep -E 'func \(s \*Store\) (Put|Get|GetRange|Delete|Head)\(' pkg/blockstore/remote/s3/store.go` showing `hash` parameter
  </acceptance_criteria>
  <done>
    s3 backend implements the new RemoteStore; legacy methods gone; tests pass; BSCAS-06 header preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update pkg/blockstore/remote/memory/ to match renamed RemoteStore interface</name>
  <files>pkg/blockstore/remote/memory/store.go, pkg/blockstore/remote/memory/store_test.go</files>
  <read_first>
    - pkg/blockstore/remote/memory/store.go (entire file — existing impl)
    - pkg/blockstore/remote/memory/store_test.go (tests using legacy method names)
    - pkg/blockstore/remote/remote.go (post-Task-1 interface)
    - pkg/blockstore/remote/s3/store.go (the just-renamed s3 backend — reference impl)
  </read_first>
  <behavior>
    - `*memory.Store` satisfies the new `RemoteStore` interface — compile-time assertion `var _ remote.RemoteStore = (*Store)(nil)` exists
    - Methods are renamed identically to the s3 backend; storage map is keyed by `ContentHash` internally (currently keyed by string blockKey)
    - The memory backend stamps `LastModified = time.Now()` at `Put` time (Phase 11 WR-4-02 requires non-zero LastModified from every backend)
    - `ReadBlockVerified` is provided on the memory backend too (engine type-asserts on `RemoteStore` not `*s3.Store`; the memory backend's impl is a trivial body-BLAKE3-then-compare — no header to check)
    - `go test ./pkg/blockstore/remote/memory/` passes
  </behavior>
  <action>
  Step 1 — pkg/blockstore/remote/memory/store.go:
  - The current memory backend uses a `map[string]*memBlock` keyed by `blockKey` string (see `pkg/blockstore/local/memory/memory.go` — confirm whether `remote/memory/store.go` follows the same pattern; if different, adapt accordingly).
  - Switch the map key type to `blockstore.ContentHash`. The internal `memBlock` struct gains a `LastModified time.Time` field.
  - Rename methods to match the new `RemoteStore`: `Put(ctx, hash, data)`, `Get(ctx, hash) ([]byte, error)`, `GetRange(ctx, hash, offset, length)`, `Delete(ctx, hash) error`, `Head(ctx, hash) (blockstore.Meta, error)`, `Walk(ctx, fn)`.
  - `Put` stamps `time.Now().UTC()` into LastModified.
  - `Head` returns `blockstore.Meta{Size: int64(len(mb.data)), LastModified: mb.LastModified}`.
  - `Walk` iterates the map under a read lock, dispatches to the callback for each `(hash, Meta)`. Honors ErrStopWalk and ctx cancellation per D-07.
  - `Get`, `GetRange`, `Delete` return `blockstore.ErrBlockNotFound` (existing sentinel) on missing hash.
  - `ReadBlockVerified(ctx, hash, expected)`: load body, compute BLAKE3, compare to `expected`, return `blockstore.ErrCASContentMismatch` on mismatch.
  - DELETE: `WriteBlock`, `WriteBlockWithHash`, `ReadBlock`, `ReadBlockRange`, `DeleteBlock`, `HeadObject`, `ListByPrefix`, `ListByPrefixWithMeta`, `DeleteByPrefix`, `CopyBlock` methods.
  - Add `var _ remote.RemoteStore = (*Store)(nil)` assertion.
  
  Step 2 — `memory_test.go`:
  - Same migration as `store_test.go` in Task 2: rename calls, drop CopyBlock + DeleteByPrefix tests, switch key strings to ContentHash via `blake3Sum` helper (you can write a local test helper if needed).</action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/remote/memory/ &amp;&amp; go test -count=1 ./pkg/blockstore/remote/memory/ &amp;&amp; grep -cE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/remote/memory/store.go</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/remote/memory/` exits 0
    - `go test -count=1 ./pkg/blockstore/remote/memory/` exits 0
    - `grep -cE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/remote/memory/store.go` returns 0
    - `grep -c 'var _ remote.RemoteStore = (\*Store)(nil)' pkg/blockstore/remote/memory/store.go` returns 1
    - `grep -c 'ReadBlockVerified' pkg/blockstore/remote/memory/store.go` returns at least 1
    - `go build ./...` STILL fails on engine consumers in this commit (expected; Plan 05 retargets engine) — verify only that `./pkg/blockstore/remote/...` builds clean: `go build ./pkg/blockstore/remote/...` exits 0
  </acceptance_criteria>
  <done>
    Memory backend matches renamed RemoteStore; tests pass; `pkg/blockstore/remote/...` builds clean as a whole.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network (S3 client → S3 server) | RemoteStore.Put/Get bytes traverse network; CAS hash verification on Get is the integrity boundary. |
| Process (server → memory.Store) | In-process map; no marshalling boundary; trust is local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | s3.Get returns raw bytes without BLAKE3 verification | mitigate | Engine continues to use `ReadBlockVerified` on the production CAS read path (Plan 05 preserves the call site); `Get` is documented as "raw bytes, callers MUST verify if they require integrity guarantees." The s3 backend still stamps `x-amz-meta-content-hash` on Put for BSCAS-06 defense-in-depth, verifiable by external `aws s3api head-object`. |
| T-17-03-02 | Tampering | x-amz-meta-content-hash header stripped during S3 transit by intermediate proxy | mitigate | `ReadBlockVerified` recomputes BLAKE3 over the body and compares against caller-supplied `expected` — header check is defense-in-depth, body hash is the truth source. |
| T-17-03-03 | Information Disclosure | Head response leaks x-amz-meta-content-hash | accept | Per D-08, `Head` returns only `Meta{Size, LastModified}` — the metadata map is NOT exposed. S3-internal verification still consults the header but it does not cross the interface boundary. |
| T-17-03-04 | Denial of Service | Walk over S3 with very large bucket | accept | Same pagination as existing `ListByPrefixWithMeta` (which Walk wraps); no new DoS surface introduced. Phase 18 may switch to cursor-based streaming. |
</threat_model>

<verification>
- `go vet ./pkg/blockstore/remote/...` exits 0
- `go build ./pkg/blockstore/remote/...` exits 0
- `go test ./pkg/blockstore/remote/...` exits 0
- `grep -rcE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/remote/ | grep -v ':0$' | grep -v '_test.go' | grep -v 'ReadBlockVerified'` returns no lines (all legacy method names absent from non-test source)
- `go build ./...` from repo root MAY still fail — engine consumers retargeted in Plan 05
</verification>

<success_criteria>
Plan 03 is complete when:
- [ ] All three tasks pass their automated `<verify>` commands
- [ ] `pkg/blockstore/remote/remote.go` declares the 7-method `RemoteStore` + `ReadBlockVerified` extension + Close/HealthCheck/Healthcheck
- [ ] s3 + memory backends compile against the renamed interface
- [ ] BSCAS-06 `x-amz-meta-content-hash` PUT header preserved on s3.Put
- [ ] All `HeadResult` + `ObjectInfo` references removed from `pkg/blockstore/remote/`
- [ ] No `CopyBlock` / `WriteBlock` / `WriteBlockWithHash` / `DeleteByPrefix` / `ListByPrefix*` symbols remain in the remote package source
- [ ] `go build ./...` from repo root MAY fail — engine consumers fixed in Plan 05 (expected mid-PR state per D-01 internal commit ordering)
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-03-SUMMARY.md` capturing:
- New `RemoteStore` method list with signatures
- Confirmation that `x-amz-meta-content-hash` is still stamped on Put
- Lines of test code removed (CopyBlock + DeleteByPrefix tests + ListByPrefix*)
- Any compile breakage in non-remote packages this commit leaves behind (expected: engine consumers in `pkg/blockstore/engine/`)
</output>
