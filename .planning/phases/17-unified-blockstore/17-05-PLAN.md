---
phase: 17-unified-blockstore
plan: 05
type: execute
wave: 3
depends_on: [17-03-PLAN.md, 17-04-PLAN.md]
files_modified:
  - pkg/blockstore/engine/syncer.go
  - pkg/blockstore/engine/upload.go
  - pkg/blockstore/engine/fetch.go
  - pkg/blockstore/engine/gc.go
  - pkg/blockstore/engine/dedup.go
  - pkg/blockstore/engine/sync_entry.go
  - pkg/blockstore/engine/engine.go
  - pkg/blockstore/engine/types.go
  - pkg/blockstore/engine/errors.go
  - pkg/blockstore/engine/engine_dualread_test.go
  - pkg/controlplane/runtime/shares/service.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Engine consumer call sites use the renamed `RemoteStore` method names: `WriteBlockWithHash`/`WriteBlock`→`Put(ctx, hash, data)`; `ReadBlock`→`Get(ctx, hash)`; `DeleteBlock`→`Delete(ctx, hash)`; `ReadBlockVerified` keeps the name but the arg signature is `(ctx, hash, expected)`"
    - "`engine/fetch.go::dispatchRemoteFetch` legacy branch (lines 58-82) DELETED: the BlockLayoutCASOnly gate + the legacy ReadBlock fallback are GONE; any FileBlock with `Hash.IsZero() == true` at this code path is migration drift and returns a hard error per the PATTERNS.md replacement"
    - "`metadata.BlockLayoutCASOnly` import in `engine/fetch.go` REMOVED (the only consumer is the deleted branch)"
    - "`engine/errors.go::ErrLegacyReadOnCASOnly` DELETED — the sentinel has no remaining consumer post-fetch.go cleanup; symbol removed entirely"
    - "`engine.BlockStore.BlockLayout()` method at `engine/engine.go:771` and `syncer.go::Syncer.BlockLayout()` and `SyncerConfig.BlockLayout` field DELETED — no consumer reads layout post-Phase-17 since the only branch on it (legacy fallback) is gone. The metadata.BlockLayout enum itself stays in `pkg/metadata/` for Phase 18 cleanup."
    - "`pkg/controlplane/runtime/shares/service.go` — the `SyncerConfig.BlockLayout` assignment site at share-creation MUST be deleted to keep `go build ./pkg/controlplane/...` green. Plan 05 owns this cleanup explicitly (file is in `files_modified`)."
    - "`syncer.go` legacy cleanup paths (`DeleteByPrefix(payloadID + \"/\")` at lines 769, 812; `ListByPrefix(prefix)` at line 774; `DeleteBlock(bk)` at line 785) REWRITTEN to use `Walk(ctx, fn)` over CAS keys — these were prefix-based legacy cleanups; rewritten to enumerate via Walk and filter to hashes belonging to the deletion set, OR (preferred) replaced with refcount-driven GC: the legacy path-keyed prefix delete is no longer correct post-CAS and should delegate to the GC sweep flow"
    - "`dedup.go` legacy DeleteByPrefix/DeleteBlock calls (lines 319, 339) similarly rewritten — refcount-driven block delete via `engine.Delete(payloadID)` already exists; legacy prefix-delete branches are pure legacy removal"
    - "`engine_dualread_test.go` adapted: tests still assert the CAS path routes through `ReadBlockVerified` (now via renamed `Get`/`ReadBlockVerified`); legacy-path tests (TestDualRead_LegacyRowRoutesToReadBlock and friends) DELETED because the legacy path itself is gone"
    - "Engine `m.local` consumer sites (engine.go:147/320/423/635/800/828, fetch.go:140/155/192/302/420/482, syncer.go:381, upload.go:168, dedup.go:248) are LEFT AS-IS — Plan 04 retains the transitional admin methods on the narrowed LocalStore through Phase 17; Phase 18 (Syncer rewrite) rewrites those sites onto BlockStore.Put/Get/Walk. See CONTEXT.md `<deferred>` Phase 18 carry-over."
    - "`go build ./pkg/blockstore/engine/` exits 0"
    - "`go build ./pkg/controlplane/...` exits 0 (after shares/service.go SyncerConfig.BlockLayout deletion)"
    - "`go test -count=1 ./pkg/blockstore/engine/` passes (engine_test.go + sync_entry_test.go + gc_test.go + dedup_test.go + perf_bench_phase12_test.go all green; some tests may need legacy-path scaffolding removal)"
    - "Phase 16 warm-cache D-06 gate STILL HOLDS — engine read path now strictly CAS; benchmark unchanged structurally; `go test -bench=BenchmarkRandReadVerified -benchtime=3x ./pkg/blockstore/engine/` exits 0"
  artifacts:
    - path: pkg/blockstore/engine/fetch.go
      provides: "CAS-only resolveFileBlock + dispatchRemoteFetch (no legacy branch)"
      contains: "ReadBlockVerified"
    - path: pkg/blockstore/engine/syncer.go
      provides: "Renamed Put/Get/Delete/Walk call sites; BlockLayout machinery removed"
      contains: "Put("
    - path: pkg/blockstore/engine/upload.go
      provides: "Renamed WriteBlockWithHash → Put"
      contains: "Put("
    - path: pkg/controlplane/runtime/shares/service.go
      provides: "SyncerConfig.BlockLayout assignment removed"
      contains: "SyncerConfig"
  key_links:
    - from: pkg/blockstore/engine/fetch.go
      to: pkg/blockstore/remote/remote.go (renamed RemoteStore)
      via: "remoteStore.ReadBlockVerified"
      pattern: "ReadBlockVerified"
    - from: pkg/blockstore/engine/syncer.go
      to: pkg/blockstore/remote/remote.go (renamed RemoteStore)
      via: "remoteStore.Put / remoteStore.Walk / remoteStore.Delete"
      pattern: "remoteStore\\.(Put|Walk|Delete)"
---

<objective>
Retarget the engine onto the renamed `RemoteStore` interface from Plan 03 + remove the legacy dual-read shim entirely. Delete `ErrLegacyReadOnCASOnly` and the `BlockLayout` machinery from the engine — the only consumer was the legacy fallback, which is gone. Clean the `SyncerConfig.BlockLayout` assignment in `pkg/controlplane/runtime/shares/service.go` so the controlplane still builds. Adapt dual-read tests: keep the CAS verified-read assertions, delete the legacy-path tests.

Purpose: Wave 3 closes the engine's view of the remote store. The legacy branch in `dispatchRemoteFetch` was the entire reason `BlockLayout` existed inside the engine — with the branch gone, the per-share gate enum becomes dead weight inside `pkg/blockstore/engine/`. The metadata-layer `BlockLayout` enum stays (cleaned up in Phase 18 per spec). Engine LocalStore consumer sites (`m.local.ReadAt`/`WriteAt`/etc.) are LEFT AS-IS per CONTEXT.md `<deferred>` Phase 18 carry-over — D-01's atomic-merge constraint forces deferral.

Output: Engine package + controlplane compile; tests pass against renamed RemoteStore; legacy fallback gone; D-06 gate preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/17-unified-blockstore/17-PATTERNS.md
@.planning/phases/17-unified-blockstore/17-03-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-04-SUMMARY.md
@CLAUDE.md
@pkg/blockstore/remote/remote.go
@pkg/blockstore/engine/fetch.go

<interfaces>
<!-- Engine consumer surface after Plan 05. -->

From renamed pkg/blockstore/remote/remote.go (post-Plan-03):
```go
remoteStore.Put(ctx, hash, data) error
remoteStore.Get(ctx, hash) ([]byte, error)
remoteStore.GetRange(ctx, hash, offset, length) ([]byte, error)
remoteStore.Delete(ctx, hash) error
remoteStore.Head(ctx, hash) (blockstore.Meta, error)
remoteStore.Walk(ctx, fn) error
remoteStore.ReadBlockVerified(ctx, hash, expected) ([]byte, error)
```

Legacy call sites (BEFORE this plan) and target rewrites:

| File:Line | Before | After |
|-----------|--------|-------|
| upload.go:132 | `remoteStore.WriteBlockWithHash(ctx, casKey, hash, data)` | `remoteStore.Put(ctx, hash, data)` |
| upload.go:177 | `remoteStore.WriteBlockWithHash(ctx, casKey, hash, data)` | `remoteStore.Put(ctx, hash, data)` |
| fetch.go:55 | `remoteStore.ReadBlockVerified(ctx, key, fb.Hash)` | `remoteStore.ReadBlockVerified(ctx, fb.Hash, fb.Hash)` — both args are the hash |
| fetch.go:58-82 | legacy fallback to ReadBlock | DELETED |
| gc.go:429 | `remoteStore.ListByPrefixWithMeta(ctx, listPrefix)` | `remoteStore.Walk(ctx, fn)` — walk-based mark/sweep |
| gc.go:475 | `remoteStore.DeleteBlock(ctx, obj.Key)` | `remoteStore.Delete(ctx, hash)` — Walk callback gives ContentHash directly |
| syncer.go:769 | `remoteStore.DeleteByPrefix(ctx, prefix)` (legacy share prefix) | DELETED — legacy path-keyed delete is gone with CAS |
| syncer.go:774 | `remoteStore.ListByPrefix(ctx, prefix)` (legacy enumeration) | DELETED |
| syncer.go:785 | `remoteStore.DeleteBlock(ctx, bk)` inside legacy loop | DELETED |
| syncer.go:812 | `remoteStore.DeleteByPrefix(ctx, payloadID+"/")` (legacy per-file cleanup) | DELETED or rewritten to enumerate file's BlockRef list + `Delete(ctx, br.Hash)` for each |
| dedup.go:319 | `remoteStore.DeleteByPrefix(ctx, payloadID+"/")` | DELETED — refcount-driven block delete already covers it |
| dedup.go:339 | `remoteStore.DeleteBlock(ctx, fb.BlockStoreKey)` | `remoteStore.Delete(ctx, fb.Hash)` — fb is post-CAS row, hash is non-zero |

LEFT AS-IS (Phase 18 carry-over per CONTEXT.md `<deferred>`):

| File:Line | Site | Why deferred |
|-----------|------|--------------|
| engine.go:147 | `bs.local.(recoverer)` type-assertion | Transitional admin path |
| engine.go:320 | `bs.local.WriteAt(...)` (public engine.WriteAt API) | Transitional admin |
| engine.go:423,635 | `bs.local.DeleteAllBlockFiles(...)` | Transitional admin |
| engine.go:800,828 | `bs.local.ReadAt(...)` (public engine.ReadAt API) | Transitional admin |
| fetch.go:140,302 | `m.local.WriteFromRemote(...)` | Transitional admin |
| fetch.go:155,192,420,482 | `m.local.IsBlockLocal(...)` | Transitional admin |
| syncer.go:381 | `m.local.Flush(...)` | Transitional admin |
| upload.go:168 | `m.local.GetBlockData(...)` | Transitional admin |
| dedup.go:248 | `m.local.DeleteAppendLog(...)` (now `DeleteLog` per Plan 04) | Renamed via BlockStoreAppend embedding |

Note on dedup.go:248: the rename `DeleteAppendLog` → `DeleteLog` happens via the BlockStoreAppend embedding on LocalStore (Plan 04). Plan 05 MUST rename the call site `m.local.DeleteAppendLog(...)` → `m.local.DeleteLog(...)` because the embedded interface method changed name. This is the ONE engine-LocalStore call site Plan 05 touches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite engine/fetch.go — delete legacy fallback branch + rename ReadBlockVerified args; remove BlockLayoutCASOnly import</name>
  <files>pkg/blockstore/engine/fetch.go, pkg/blockstore/engine/errors.go, pkg/blockstore/engine/types.go, pkg/blockstore/engine/engine.go, pkg/blockstore/engine/syncer.go, pkg/controlplane/runtime/shares/service.go</files>
  <read_first>
    - pkg/blockstore/engine/fetch.go (entire file — the dual-read shim home)
    - pkg/blockstore/engine/errors.go (ErrLegacyReadOnCASOnly definition)
    - pkg/blockstore/engine/types.go (lines 78-87 — SyncerConfig.BlockLayout)
    - pkg/blockstore/engine/engine.go (lines 762-775 — BlockStore.BlockLayout() method)
    - pkg/blockstore/engine/syncer.go (lines 10-30 + the Syncer struct fields — locate `blockLayout` field and `BlockLayout()` getter)
    - pkg/controlplane/runtime/shares/service.go (search for `SyncerConfig` literal — find the BlockLayout assignment site at share-creation)
  </read_first>
  <action>
  Step 1 — `pkg/blockstore/engine/fetch.go::dispatchRemoteFetch` (lines 42-83 per PATTERNS.md):
  - Replace the function body so the CAS branch is the ONLY branch. Specifically:
    - Keep the `if fb == nil { return "", nil, nil }` guard.
    - Keep the `if !fb.Hash.IsZero()` branch that calls `ReadBlockVerified`. Change the call to use the new signature `remoteStore.ReadBlockVerified(ctx, fb.Hash, fb.Hash)` — both args are the hash (the canonical key derives from fb.Hash; the expected hash is the same; this is the BSCAS-06 verified-read pattern). The returned key string is now `blockstore.FormatCASKey(fb.Hash)` — return it from the function as the first return value.
    - DELETE the entire legacy branch from line 58 through line 82 (everything from the `// Legacy path: pre-Phase-11 row, no hash, unverifiable.` comment block through the final `return fb.BlockStoreKey, data, err`).
    - REPLACE with a single fail-loud branch matching PATTERNS.md lines 560-564:
      ```
      // Legacy path deleted Phase 17 (subsumes Phase 15 A6). Any FileBlock
      // surfacing here without a CAS hash is migration drift — refuse the
      // read instead of returning silent zeros. Boot guard (cmd/dfs/start)
      // refuses to start against an un-migrated store; if this triggers
      // at runtime, the sentinel file was lost or hand-removed.
      return "", nil, fmt.Errorf("blockstore: legacy zero-hash FileBlock encountered post-migration: block_id=%s", fb.ID)
      ```
  - Remove the `pkg/metadata` import from `fetch.go` if it is no longer referenced after deletion of the BlockLayoutCASOnly branch. Use `goimports` mentally — verify there is no other `metadata.` reference in the file.
  - The package-level comment in the file about `// API-02 justification: Plan 14-02 (MIG-03 / D-A8) gates the dual-read shim's legacy fallback on the per-share BlockLayout` (lines 10-14) becomes stale — delete those comment lines.

  Step 2 — `pkg/blockstore/engine/errors.go`:
  - DELETE `var ErrLegacyReadOnCASOnly = errors.New("legacy read attempted on cas-only share (MIG-03)")` (line 24) and its godoc block (lines 5-23) ENTIRELY.
  - If the file becomes empty (only package declaration + imports), keep the package declaration; if imports become unused, remove them.

  Step 3 — `pkg/blockstore/engine/types.go`:
  - DELETE the `BlockLayout metadata.BlockLayout` field in `SyncerConfig` (around lines 78-87 per the grep output) and its godoc block.
  - DELETE the `// API-02 justification:` comment block above the field (lines 6-9) if it solely justified the import; the `metadata` import may itself become unused — `goimports` it out.

  Step 4 — `pkg/blockstore/engine/engine.go`:
  - DELETE the `func (bs *BlockStore) BlockLayout()` method (lines 762-775 per the grep output) and its godoc.
  - DELETE the `// API-02 justification:` import comment block at lines 13-15 if it solely justified `pkg/metadata` for BlockLayout; if other code in engine.go still uses `metadata.`, keep the import (it likely does — coordinator and similar).

  Step 5 — `pkg/blockstore/engine/syncer.go`:
  - Locate the `blockLayout metadata.BlockLayout` field on the Syncer struct (search for `blockLayout` after the syncer field block). DELETE the field.
  - Locate the `func (m *Syncer) BlockLayout()` getter. DELETE it.
  - At Syncer construction (likely a `NewSyncer` function), DELETE the `m.blockLayout = ...` assignment.

  Step 6 — `pkg/controlplane/runtime/shares/service.go` (W2 — file now in files_modified):
  - Search for the SyncerConfig literal at the share-creation site: `grep -n 'SyncerConfig\|BlockLayout' pkg/controlplane/runtime/shares/service.go`.
  - Locate the line where `SyncerConfig{... BlockLayout: ... ...}` is constructed (likely inside `Service.createBlockStoreForShare` or `Service.AddShare`).
  - DELETE the `BlockLayout: ...` line from the struct literal. The field is gone from `SyncerConfig` after Step 3.
  - If the surrounding code computes a `metadata.BlockLayout` value solely to pass into SyncerConfig, DELETE that computation too (likely a `layout := metadata.BlockLayoutCASOnly` or similar). Verify no other consumer in the same function.
  - If `pkg/controlplane/runtime/shares/service.go` had a Coordinator-related type-cast to access BlockLayout (e.g., `engine.BlockStore.BlockLayout()`), remove it — the method is gone post-Step 4.
  - The `metadata.BlockLayout` enum itself stays in `pkg/metadata/` (Phase 18 cleanup target).

  Step 7 — verify the engine package + controlplane compile:
  - `go vet ./pkg/blockstore/engine/` exits 0.
  - `go build ./pkg/blockstore/engine/` exits 0.
  - `go vet ./pkg/controlplane/...` exits 0.
  - `go build ./pkg/controlplane/...` exits 0.
  - The engine `loadByHash` path (Phase 16) is untouched; only fetch.go's dispatch was.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/engine/ ./pkg/controlplane/... &amp;&amp; go build ./pkg/blockstore/engine/ ./pkg/controlplane/... &amp;&amp; { grep -vE '^\s*//' pkg/blockstore/engine/*.go | grep -cE 'ErrLegacyReadOnCASOnly|BlockLayoutCASOnly|BlockLayoutLegacy|blockLayout|BlockLayout\(\)' || true; }</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/engine/` exits 0
    - `go build ./pkg/blockstore/engine/` exits 0
    - `go build ./pkg/controlplane/...` exits 0 (shares/service.go cleanup propagates)
    - `grep -rcE '\bErrLegacyReadOnCASOnly\b' pkg/blockstore/engine/ pkg/controlplane/ | grep -v ':0$' | grep -v _test.go | wc -l` returns 0
    - `grep -rcE '\bBlockLayoutCASOnly\b|\bBlockLayout\(\)' pkg/blockstore/engine/` returns 0 (engine no longer references the enum or the getter)
    - `grep -c 'BlockLayout' pkg/controlplane/runtime/shares/service.go` returns 0 (SyncerConfig.BlockLayout assignment removed)
    - The `metadata.BlockLayout` enum itself still exists in `pkg/metadata/types.go` (Phase 18 cleanup target — verify with `grep -c 'type BlockLayout' pkg/metadata/types.go` returns at least 1)
    - `pkg/blockstore/engine/fetch.go` no longer imports `pkg/metadata` — `grep -c 'pkg/metadata' pkg/blockstore/engine/fetch.go` returns 0
  </acceptance_criteria>
  <done>
    Legacy fallback branch deleted; ErrLegacyReadOnCASOnly + BlockLayout machinery purged from engine; controlplane shares wiring cleaned (SyncerConfig.BlockLayout assignment removed); engine + controlplane build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename engine call sites — upload/syncer/gc/dedup/sync_entry switch to Put/Get/Delete/Walk on RemoteStore</name>
  <files>pkg/blockstore/engine/upload.go, pkg/blockstore/engine/syncer.go, pkg/blockstore/engine/gc.go, pkg/blockstore/engine/dedup.go, pkg/blockstore/engine/sync_entry.go</files>
  <read_first>
    - pkg/blockstore/engine/upload.go (lines 130-180 — WriteBlockWithHash call sites)
    - pkg/blockstore/engine/syncer.go (lines 760-820 — legacy DeleteByPrefix / ListByPrefix call sites)
    - pkg/blockstore/engine/gc.go (lines 420-480 — ListByPrefixWithMeta + DeleteBlock in the GC sweep)
    - pkg/blockstore/engine/dedup.go (lines 315-345 — DeleteByPrefix + DeleteBlock in dedup cleanup; line 248 — DeleteAppendLog rename to DeleteLog)
    - pkg/blockstore/engine/sync_entry.go (line 43 — any remote method call)
    - pkg/blockstore/remote/remote.go (the new interface — Plan 03 output)
  </read_first>
  <action>

  Step 1 — `pkg/blockstore/engine/upload.go`:
  - At line ~132: replace `m.remoteStore.WriteBlockWithHash(ctx, casKey, hash, data)` with `m.remoteStore.Put(ctx, hash, data)`. The casKey was derived from hash via FormatCASKey; that derivation can stay if it's still used in the surrounding return value, otherwise drop it.
  - At line ~177: same replacement.
  - Remove any now-unused local variables.

  Step 2 — `pkg/blockstore/engine/syncer.go` (legacy enumeration/cleanup paths around lines 760-820):
  - The block at line 769 (`DeleteByPrefix(ctx, prefix)` for a legacy share-prefix sweep) is a Phase 11 / Plan 14-02 legacy path. With CAS-only stores, no legacy prefix exists. DELETE the surrounding helper function (likely a private `cleanupLegacyShare` or similar) entirely. Search outward for callers: `grep -rn 'cleanupLegacyShare\|deleteLegacyKeys' pkg/blockstore/engine/`; if call sites exist in syncer.go/sync_entry.go, also delete them.
  - The block at line 812 (`DeleteByPrefix(ctx, payloadID+"/")` per-file legacy cleanup): this fired on engine.Delete(payloadID) for files that had legacy keys. Post-CAS, file deletion routes purely through refcount + GC. DELETE the call. If the surrounding function has no other purpose, delete it entirely; if it still does CAS work, just remove the legacy cleanup call.
  - All other syncer methods (uploadOne, drain, etc.) that already used the new method names via aliasing — verify they call `m.remoteStore.Put` etc.; rename if any holdovers exist.

  Step 3 — `pkg/blockstore/engine/gc.go` GC sweep (lines ~407-490):
  - Replace `objects, err := remoteStore.ListByPrefixWithMeta(ctx, listPrefix)` with a `Walk` call:
    ```
    err := remoteStore.Walk(ctx, func(hash blockstore.ContentHash, meta blockstore.Meta) error {
        // existing per-object logic moves into the callback body
        // - check grace TTL (zero LastModified still aborts)
        // - check live set membership
        // - call remoteStore.Delete(ctx, hash) if eligible
        return nil
    })
    ```
  - The `obj.Key` references inside the body become irrelevant (the hash is the key); convert any `obj.Key`-based logging to log `hash` or `blockstore.FormatCASKey(hash)`.
  - `obj.LastModified` becomes `meta.LastModified`; same fail-closed check on zero (preserve the WR-4-02 invariant).
  - `obj.Size` becomes `meta.Size`.
  - Replace `remoteStore.DeleteBlock(ctx, obj.Key)` with `remoteStore.Delete(ctx, hash)`.
  - First-error capture pattern (the comment at line 407 referenced capturing the FIRST DeleteBlock error) — preserve it; just replace the method name.
  - If the GC needs to enumerate non-CAS keys (e.g., legacy `payloadID/block-{idx}` for the dual-read window) — that's gone in Phase 17; the Walk wrapper in s3 backend already filters non-CAS keys via `ParseCASKey`-error skip.

  Step 4 — `pkg/blockstore/engine/dedup.go`:
  - At line ~248: rename `m.local.DeleteAppendLog(ctx, payloadID)` → `m.local.DeleteLog(ctx, payloadID)`. This is the BlockStoreAppend embedding method rename from Plan 04.
  - At line ~319: `m.remoteStore.DeleteByPrefix(ctx, payloadID+"/")` — DELETE the call; refcount-driven delete handles cleanup.
  - At line ~339: `m.remoteStore.DeleteBlock(ctx, fb.BlockStoreKey)` → `m.remoteStore.Delete(ctx, fb.Hash)`. The `fb` row here is post-CAS (dedup operates after Phase-11 path), so `fb.Hash` is non-zero. Add a defensive check: if `fb.Hash.IsZero()` at this point, log Error and skip (instead of panicking on a stale row).

  Step 5 — `pkg/blockstore/engine/sync_entry.go`:
  - Find any `remoteStore.WriteBlock/ReadBlock/DeleteBlock/HeadObject/ListByPrefix*/DeleteByPrefix/CopyBlock` call (grep showed only one at line 43). Rename to the new method name per the mapping in the <interfaces> block above.

  Step 6 — verify no legacy method names remain in engine source (excluding ReadBlockVerified which stays):
  - `grep -rnE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/engine/ | grep -v _test.go | grep -v ReadBlockVerified` returns ZERO non-test, non-ReadBlockVerified lines.
  - `grep -rn 'DeleteAppendLog' pkg/blockstore/engine/` returns 0 lines (the rename to DeleteLog took effect at the dedup.go:248 call site).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/engine/ &amp;&amp; go build ./pkg/blockstore/engine/ &amp;&amp; { grep -rnE '\bWriteBlock\b|\bWriteBlockWithHash\b|\bReadBlock\b|\bReadBlockRange\b|\bDeleteBlock\b|\bHeadObject\b|\bListByPrefix\b|\bDeleteByPrefix\b|\bCopyBlock\b' pkg/blockstore/engine/ | grep -v '_test.go' | grep -v 'ReadBlockVerified' | grep -v '^\s*//' || true; } | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/engine/` exits 0
    - `go build ./pkg/blockstore/engine/` exits 0
    - Trailing grep pipeline in the verify command returns `0` (zero remaining legacy method names in non-test, non-comment engine source, excluding ReadBlockVerified)
    - `grep -c 'remoteStore\.Put\|remoteStore\.Get\|remoteStore\.Delete\|remoteStore\.Walk\|remoteStore\.ReadBlockVerified' pkg/blockstore/engine/*.go | grep -v ':0' | wc -l` returns at least 4 (multiple files use renamed methods)
    - `pkg/blockstore/engine/gc.go` GC sweep uses `Walk` not `ListByPrefixWithMeta` — `grep -c 'remoteStore\.Walk' pkg/blockstore/engine/gc.go` returns at least 1
    - `grep -rn 'DeleteAppendLog' pkg/blockstore/engine/` returns 0 lines (dedup.go:248 renamed to DeleteLog per Plan 04 BlockStoreAppend embedding)
  </acceptance_criteria>
  <done>
    All engine remote-store call sites use the renamed RemoteStore methods; legacy prefix-based cleanups deleted; GC sweep uses Walk; dedup.go renamed the one LocalStore method that Plan 04 actually renamed (DeleteAppendLog → DeleteLog).
  </done>
</task>

<task type="auto">
  <name>Task 3: Adapt engine_dualread_test.go — keep CAS tests, delete legacy-path tests</name>
  <files>pkg/blockstore/engine/engine_dualread_test.go</files>
  <read_first>
    - pkg/blockstore/engine/engine_dualread_test.go (entire file)
    - pkg/blockstore/engine/fetch.go (post-Task-1 — the new no-legacy-branch dispatchRemoteFetch)
  </read_first>
  <action>

  Step 1 — adapt the `spyingRemoteStore` test helper at line ~40:
  - Update method signatures to match the renamed RemoteStore. The spy's `ReadBlock`, `ReadBlockVerified`, `WriteBlock`, `WriteBlockWithHash` proxy methods all need rename/removal:
    - DELETE: `ReadBlock` proxy method (legacy path gone)
    - DELETE: `WriteBlock` proxy method
    - RENAME: `ReadBlockVerified(ctx, key string, expected)` → `ReadBlockVerified(ctx, hash ContentHash, expected ContentHash)`
    - RENAME: `WriteBlockWithHash` proxy → `Put(ctx, hash ContentHash, data []byte) error`
    - Update the `readVerifiedCalls` / `readCalls` counters to track the new method names; delete `readCalls` counter (only `readVerifiedCalls` survives).
    - Any other methods on the spy (Head, Walk, Get, Delete, GetRange, Close, HealthCheck, Healthcheck) must be added to satisfy the new RemoteStore interface — they can be simple passthroughs to the wrapped `remote.RemoteStore` instance (the spy embeds it).

  Step 2 — adapt the surviving tests:
  - `TestDualRead_CASRowRoutesToReadBlockVerified` (line ~115): Update the body — the legacy `casKey` string usage is replaced by `FormatCASKey(hash)` derivation; the spy's `WriteBlockWithHash` call (line 126) becomes `Put(ctx, hash, data)`; assertions on `env.rs.readVerifiedCalls` remain valid.
  - `TestDualRead_CorruptCASContent_FailsClosed` (around line 247): the legacy-path bytes-stash via `WriteBlock` (line 249) — this test stashed wrong bytes at the CAS key. Rewrite: use `env.innerRS.Put(ctx, hash, wrongBytes)` with a hash that DOES NOT match the wrongBytes content (the s3 backend will accept the bytes but stamp the header with the CALLER-supplied hash; the ReadBlockVerified path then mismatches body BLAKE3 against expected). Alternative: if the memory backend's Put recomputes hash internally and rejects mismatches, skip this test on memory backend.
  - `TestDualRead_NewCASRoundTripsThroughVerified` (around line 450): adapt method-name calls; assertions on `readVerifiedCalls` survive.

  Step 3 — DELETE legacy-path test functions ENTIRELY (they assert the legacy fallback that no longer exists):
  - `TestDualRead_LegacyRowRoutesToReadBlock` (line ~168) — DELETE
  - The two later tests around lines 359 and 410 that exercise the legacy gate (T-14-02-03 non-regression assertion) — DELETE both. These tests asserted that legacy shares still got the fallback; with the fallback gone, the assertions are obsolete.
  - Any test helper that exists ONLY to support deleted tests (search for unique callers) — DELETE.

  Step 4 — verify the test file builds + passes:
  - `go vet ./pkg/blockstore/engine/` exits 0.
  - `go test -count=1 -run 'TestDualRead' ./pkg/blockstore/engine/` exits 0.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/engine/ &amp;&amp; go test -count=1 -run TestDualRead ./pkg/blockstore/engine/ &amp;&amp; { grep -c 'TestDualRead_LegacyRow\|TestDualRead.*Legacy' pkg/blockstore/engine/engine_dualread_test.go || true; }</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/engine/` exits 0
    - `go test -count=1 -timeout 60s -run TestDualRead ./pkg/blockstore/engine/` exits 0
    - `grep -c 'TestDualRead_LegacyRow\|TestDualRead.*Legacy' pkg/blockstore/engine/engine_dualread_test.go` returns 0 (all legacy-path tests deleted)
    - `grep -c 'TestDualRead_CASRowRoutesToReadBlockVerified' pkg/blockstore/engine/engine_dualread_test.go` returns at least 1 (CAS-path test preserved)
    - `grep -c 'WriteBlock\b\|ReadBlock\b\|WriteBlockWithHash' pkg/blockstore/engine/engine_dualread_test.go` returns 0 (legacy method names removed from spy + tests; ReadBlockVerified survives via word-boundary exclusion)
    - Phase 16 D-06 perf gate preserved: `go test -bench=BenchmarkRandReadVerified -benchtime=3x -run=^$ ./pkg/blockstore/engine/` exits 0
  </acceptance_criteria>
  <done>
    Dual-read tests adapted: spy implements new RemoteStore surface, CAS scenarios preserved, legacy-path scenarios deleted, D-06 benchmark still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → renamed RemoteStore | Method-name rename only; semantics unchanged. No new untrusted-input surface. |
| engine fetch.go legacy-branch deletion | Removes a code path that, post-migration, could only have been reached via migration drift. Replaced with fail-loud error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | T (Tampering) | Post-deletion, a stray legacy `FileBlock` (Hash.IsZero == true) reaches dispatchRemoteFetch | mitigate | Replacement branch returns a hard error referencing block_id (PATTERNS.md lines 560-564); no silent zero-bytes return. Boot guard (Plan 09) prevents this state on a properly-migrated store. |
| T-17-05-02 | I (Information disclosure) | Renamed method calls log slightly different argument shapes | accept | The arg-shape change (hash vs blockKey string) is internal; existing log lines update naturally via grep-then-rewrite during task execution. |
| T-17-05-03 | D (DoS) | Walk-based GC sweep replaces ListByPrefixWithMeta — pagination behavior changes | mitigate | The s3 backend's Walk wraps the existing pagination; semantics preserved. Phase 18 may switch to cursor-based streaming. |
| T-17-05-04 | T (Tampering) | Engine LocalStore consumer sites left in place could break if Plan 04's narrowed interface drops them | mitigate | Plan 04's revision explicitly retains the 7 transitional methods (ReadAt/WriteAt/Flush/IsBlockLocal/GetBlockData/WriteFromRemote/DeleteAllBlockFiles) on LocalStore through Phase 17. CONTEXT.md `<deferred>` documents the Phase 18 deletion site. |
</threat_model>

<verification>
- `go vet ./pkg/blockstore/engine/ ./pkg/controlplane/...` exits 0
- `go build ./pkg/blockstore/engine/ ./pkg/controlplane/...` exits 0
- `go test -count=1 -timeout 120s ./pkg/blockstore/engine/` exits 0
- `go test -bench=BenchmarkRandReadVerified -benchtime=3x -run=^$ ./pkg/blockstore/engine/` exits 0 (D-06 gate preserved)
- Zero remaining `BlockLayout`-related symbols inside `pkg/blockstore/engine/`
- Zero `BlockLayout` references in `pkg/controlplane/runtime/shares/service.go`
- Engine LocalStore consumer sites (m.local.ReadAt/WriteAt/etc.) UNCHANGED — Phase 18 carry-over
</verification>

<success_criteria>
Plan 05 is complete when:
- [ ] All three tasks pass their automated `<verify>` commands
- [ ] Engine compiles against renamed RemoteStore from Plan 03
- [ ] Controlplane compiles after BlockLayout removal from shares/service.go
- [ ] Legacy fetch fallback branch + ErrLegacyReadOnCASOnly + BlockLayout machinery purged from engine package
- [ ] Dual-read tests adapted: CAS scenarios preserved, legacy-path scenarios deleted
- [ ] D-06 warm-cache benchmark still green
- [ ] dedup.go:248 calls `m.local.DeleteLog` (post-rename); no `DeleteAppendLog` references remain in engine
- [ ] Engine LocalStore consumer sites (m.local.{ReadAt,WriteAt,Flush,IsBlockLocal,GetBlockData,WriteFromRemote,DeleteAllBlockFiles}) UNTOUCHED — Phase 18 carry-over per CONTEXT.md `<deferred>`
- [ ] Commit signed; no Claude Code in message
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-05-SUMMARY.md` capturing:
- Confirmation that engine package + controlplane both compile
- List of deleted engine symbols (ErrLegacyReadOnCASOnly, BlockStore.BlockLayout, Syncer.BlockLayout, SyncerConfig.BlockLayout)
- shares/service.go changes (BlockLayout assignment line removed)
- Engine LocalStore sites left untouched (Phase 18 carry-over) — explicitly enumerate the 13 sites for Phase 18 planning
- D-06 benchmark result (ratio vs Phase 16 baseline)
- Any dual-read test scaffolding that needed deletion
</output>
