---
phase: 17-unified-blockstore
plan: 06
type: execute
wave: 3
depends_on: [17-02-PLAN.md, 17-03-PLAN.md, 17-04-PLAN.md]
files_modified:
  - pkg/blockstore/local/fs/fs_conformance_test.go
  - pkg/blockstore/local/fs/fs_get_conformance_test.go
  - pkg/blockstore/local/fs/appendlog_test.go
  - pkg/blockstore/local/memory/memory_test.go
  - pkg/blockstore/remote/s3/store_test.go
  - pkg/blockstore/remote/memory/memory_test.go
  - pkg/blockstore/local/localtest/suite.go
  - pkg/blockstore/local/localtest/appendlog_suite.go
  - pkg/blockstore/local/localtest/eviction_lsl08_suite.go
  - pkg/blockstore/local/localtest/doc.go
  - pkg/blockstore/remote/remotetest/suite.go
  - pkg/blockstore/remote/remotetest/doc.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Four backends invoke the unified `blockstoretest.BlockStoreConformance` entrypoint from Plan 02: `pkg/blockstore/local/fs/`, `pkg/blockstore/local/memory/`, `pkg/blockstore/remote/s3/`, `pkg/blockstore/remote/memory/` (D-09)"
    - "Both local backends also invoke `blockstoretest.BlockStoreAppendConformance`; remote backends do NOT (append is a local-only contract per D-09)"
    - "`pkg/blockstore/local/localtest/` directory DELETED in full"
    - "`pkg/blockstore/remote/remotetest/` directory DELETED in full"
    - "Backend-specific tests outside the conformance contract STAY (fs LSL-08 eviction, fs recovery, Phase 16 D-06 warm-cache benchmark, fs appendlog torn-write recovery, s3 x-amz-meta-content-hash BSCAS-06 defense-in-depth)"
    - "Zero residual `localtest` or `remotetest` imports anywhere in the repo"
    - "`go build ./...` + `go test -count=1 ./pkg/blockstore/local/... ./pkg/blockstore/remote/memory/` exit 0"
  artifacts:
    - path: pkg/blockstore/local/fs/fs_conformance_test.go
      provides: "fs backend wired to both conformance entrypoints"
      contains: "blockstoretest.BlockStoreConformance"
    - path: pkg/blockstore/local/memory/memory_test.go
      provides: "memory-local backend wired to both conformance entrypoints"
      contains: "blockstoretest.BlockStoreConformance"
    - path: pkg/blockstore/remote/s3/store_test.go
      provides: "s3 backend wired to BlockStoreConformance"
      contains: "blockstoretest.BlockStoreConformance"
    - path: pkg/blockstore/remote/memory/memory_test.go
      provides: "memory-remote backend wired to BlockStoreConformance"
      contains: "blockstoretest.BlockStoreConformance"
  key_links:
    - from: pkg/blockstore/local/fs/fs_conformance_test.go
      to: pkg/blockstore/blockstoretest/conformance.go (Plan 02)
      via: "import path"
      pattern: "pkg/blockstore/blockstoretest"
    - from: pkg/blockstore/remote/s3/store_test.go
      to: pkg/blockstore/blockstoretest/conformance.go (Plan 02)
      via: "import path"
      pattern: "pkg/blockstore/blockstoretest"
---

<objective>
Wire all four backends (fs, local memory, remote s3, remote memory) to the unified `blockstoretest/` conformance entrypoints created in Plan 02. Delete `localtest/` and `remotetest/` after wiring. Keep backend-specific tests (fs LSL-08 eviction, fs recovery, Phase 16 D-06 perf gate, s3 BSCAS-06 header check) in place — only the generic contract moves.

Purpose: Plan 02 shipped the unified test library; this plan retires the duplicates it replaced. After this, there is one source of truth for what `BlockStore` means.

Output: One conformance suite drives four backends; localtest/ + remotetest/ gone; LoC reduction begins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/17-unified-blockstore/17-PATTERNS.md
@.planning/phases/17-unified-blockstore/17-02-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-03-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-04-SUMMARY.md
@CLAUDE.md

<interfaces>
From pkg/blockstore/blockstoretest/conformance.go (Plan 02):

  type Factory func(t *testing.T) (blockstore.BlockStore, func())
  type AppendFactory func(t *testing.T) (blockstore.BlockStoreAppend, func())
  func BlockStoreConformance(t *testing.T, factory Factory)
  func BlockStoreAppendConformance(t *testing.T, factory AppendFactory)

Backend wiring shape:
- fs (local): both entrypoints
- memory (local): both entrypoints
- s3 (remote): BlockStoreConformance ONLY
- memory (remote): BlockStoreConformance ONLY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire local backends (fs + memory) to blockstoretest entrypoints</name>
  <files>pkg/blockstore/local/fs/fs_conformance_test.go, pkg/blockstore/local/fs/fs_get_conformance_test.go, pkg/blockstore/local/fs/appendlog_test.go, pkg/blockstore/local/memory/memory_test.go, pkg/blockstore/local/memory/memory_get_test.go</files>
  <read_first>
    - pkg/blockstore/local/fs/fs_conformance_test.go (current fs↔localtest binding; FSStoreOptions{UseAppendLog: true} factory shape)
    - pkg/blockstore/local/fs/fs_get_conformance_test.go (Phase 16 Get conformance — must dedupe vs BlockStoreConformance Get scenarios)
    - pkg/blockstore/local/fs/appendlog_test.go (current AppendLog suite invocation)
    - pkg/blockstore/local/memory/memory_test.go (current memory↔localtest binding)
    - pkg/blockstore/blockstoretest/conformance.go (Plan 02 — Factory + AppendFactory shapes)
    - pkg/blockstore/local/localtest/suite.go (source of scenarios that were moved into blockstoretest)
  </read_first>
  <action>
  Step 1 — `pkg/blockstore/local/fs/fs_conformance_test.go`:
  - Replace the existing `localtest.RunSuite(t, factory)` (or equivalent) call with two calls in the same file:
    - `blockstoretest.BlockStoreConformance(t, fsBlockStoreFactory)` where `fsBlockStoreFactory(t)` returns `(blockstore.BlockStore, func())`. Build via `fs.NewFSStore(t.TempDir(), ...)` with the same `FSStoreOptions{UseAppendLog: true, RollupStore: memoryRollup}` shape currently used at lines 22-30. Cleanup closure calls `store.Close()`.
    - `blockstoretest.BlockStoreAppendConformance(t, fsAppendFactory)` where `fsAppendFactory(t)` returns `(blockstore.BlockStoreAppend, func())`. Same construction; type-assert *FSStore to `blockstore.BlockStoreAppend` (post-Plan-04 embedding makes this trivial).
  - Drop the import of `pkg/blockstore/local/localtest`; add `pkg/blockstore/blockstoretest`.
  
  Step 2 — `pkg/blockstore/local/fs/fs_get_conformance_test.go`:
  - This file was added in Phase 16 to assert `*FSStore.Get(ctx, hash)` semantics. The Plan-02 BlockStoreConformance suite includes `Put_Get_Roundtrip`, `Get_NotFound`, `GetRange` scenarios that subsume those assertions.
  - If the assertions duplicate BlockStoreConformance scenarios verbatim, DELETE the file.
  - If it asserted fs-specific behavior (chunkstore.ReadChunk path, LRU touch on hit), keep only those fs-specific assertions; remove the duplicate Get/NotFound/Range scenarios.
  
  Step 3 — `pkg/blockstore/local/fs/appendlog_test.go`:
  - Drop any one-line wrapper that invoked `localtest.RunAppendLogSuite(t, factory)` — Task 1 step 1 now runs BlockStoreAppendConformance via fs_conformance_test.go.
  - Preserve fs-specific tests: torn-write recovery at the syscall level, LSL-06 fsync invariant, AppendWrite-bench scaffolding — these are NOT part of the generic contract.
  
  Step 4 — `pkg/blockstore/local/memory/memory_test.go`:
  - Replace `localtest.RunSuite(t, factory)` with both `BlockStoreConformance` and `BlockStoreAppendConformance` calls.
  - Factory constructs via `memory.New(...)` (check pkg/blockstore/local/memory/memory.go for the exact constructor signature). Return the *Store as both BlockStore and BlockStoreAppend.
  - Cleanup closure: no-op (or `Close()` if implemented).
  
  Step 5 — `pkg/blockstore/local/memory/memory_get_test.go`:
  - Same dedupe treatment as Step 2. If assertions are equivalent to BlockStoreConformance.Get_* scenarios, DELETE.
  
  Step 6 — verify:
  - `go vet ./pkg/blockstore/local/...` exits 0.
  - `go test -count=1 -timeout 120s ./pkg/blockstore/local/fs/ ./pkg/blockstore/local/memory/` exits 0.
  - Zero localtest imports in the four files: `grep -l 'pkg/blockstore/local/localtest' pkg/blockstore/local/fs/*.go pkg/blockstore/local/memory/*.go | wc -l` returns 0.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go vet ./pkg/blockstore/local/... && go test -count=1 -timeout 120s ./pkg/blockstore/local/fs/ ./pkg/blockstore/local/memory/</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/local/...` exits 0
    - `go test -count=1 -timeout 120s ./pkg/blockstore/local/fs/ ./pkg/blockstore/local/memory/` exits 0
    - `grep -l 'pkg/blockstore/local/localtest' pkg/blockstore/local/fs/*.go pkg/blockstore/local/memory/*.go 2>/dev/null | wc -l` returns 0
    - `grep -c 'blockstoretest.BlockStoreConformance' pkg/blockstore/local/fs/fs_conformance_test.go` returns at least 1
    - `grep -c 'blockstoretest.BlockStoreAppendConformance' pkg/blockstore/local/fs/fs_conformance_test.go` returns at least 1
    - `grep -c 'blockstoretest.BlockStoreConformance' pkg/blockstore/local/memory/memory_test.go` returns at least 1
    - Backend-specific tests still pass: `go test -count=1 -run 'Eviction|Recovery|TornWrite' ./pkg/blockstore/local/fs/` exits 0
  </acceptance_criteria>
  <done>
    fs + local-memory backends invoke unified conformance entrypoints; backend-specific tests preserved; localtest imports gone from local subtree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire remote backends (s3 + memory) to BlockStoreConformance</name>
  <files>pkg/blockstore/remote/s3/store_test.go, pkg/blockstore/remote/memory/memory_test.go</files>
  <read_first>
    - pkg/blockstore/remote/s3/store_test.go (current s3↔remotetest binding; Localstack/MinIO setup)
    - pkg/blockstore/remote/memory/memory_test.go (current memory-remote↔remotetest binding)
    - pkg/blockstore/remote/remotetest/suite.go (scenarios moved into blockstoretest)
    - pkg/blockstore/blockstoretest/conformance.go (Plan 02 — Factory signature)
    - pkg/blockstore/remote/s3/store.go (post-Plan-03 — renamed Put/Get/Delete/Head/Walk)
  </read_first>
  <action>
  Step 1 — `pkg/blockstore/remote/s3/store_test.go`:
  - Replace `remotetest.RunSuite(t, factory)` with `blockstoretest.BlockStoreConformance(t, s3Factory)`.
  - s3 factory: reuse the existing test fixture (Localstack container / MinIO testcontainer / httptest stub — whichever is currently present). Honor existing build tags and skip guards (e.g., `DITTOFS_S3_ENDPOINT` env). Cleanup tears down container/server.
  - Do NOT call `BlockStoreAppendConformance` — remote tier has no append contract.
  - PRESERVE all s3-specific tests outside the conformance contract:
    - `x-amz-meta-content-hash` header round-trip on PUT (BSCAS-06 defense-in-depth)
    - signing / retry / verifier-specific tests
    These are not in the generic Head scenario (D-08 — Meta exposes only `{Size, LastModified}`).
  
  Step 2 — `pkg/blockstore/remote/memory/memory_test.go`:
  - Replace `remotetest.RunSuite(t, factory)` with `blockstoretest.BlockStoreConformance(t, memFactory)`.
  - memFactory constructs via `memory.New(...)` (check pkg/blockstore/remote/memory/memory.go). Cleanup: no-op or Close().
  
  Step 3 — verify:
  - `go vet ./pkg/blockstore/remote/...` exits 0.
  - `go test -count=1 -timeout 180s ./pkg/blockstore/remote/memory/` exits 0 (no external deps).
  - `go test -count=1 -timeout 300s ./pkg/blockstore/remote/s3/` honors existing skip guards; exits 0 when env is available.
  - Zero remotetest imports remain: `grep -l 'pkg/blockstore/remote/remotetest' pkg/blockstore/remote/s3/*.go pkg/blockstore/remote/memory/*.go 2>/dev/null | wc -l` returns 0.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go vet ./pkg/blockstore/remote/... && go test -count=1 -timeout 180s ./pkg/blockstore/remote/memory/</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/remote/...` exits 0
    - `go test -count=1 -timeout 180s ./pkg/blockstore/remote/memory/` exits 0
    - `go test -count=1 -timeout 300s ./pkg/blockstore/remote/s3/` exits 0 when Localstack available; existing skip guards honored otherwise
    - `grep -l 'pkg/blockstore/remote/remotetest' pkg/blockstore/remote/s3/*.go pkg/blockstore/remote/memory/*.go 2>/dev/null | wc -l` returns 0
    - `grep -c 'blockstoretest.BlockStoreConformance' pkg/blockstore/remote/s3/store_test.go pkg/blockstore/remote/memory/memory_test.go` returns at least 2 in aggregate
    - NO `blockstoretest.BlockStoreAppendConformance` call appears in remote backends
    - s3 BSCAS-06 defense-in-depth tests preserved: `grep -rc 'x-amz-meta-content-hash' pkg/blockstore/remote/s3/` returns at least 1
  </acceptance_criteria>
  <done>
    s3 + remote-memory backends invoke BlockStoreConformance; remotetest imports gone; backend-specific tests preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete localtest/ and remotetest/ directories</name>
  <files>pkg/blockstore/local/localtest/suite.go, pkg/blockstore/local/localtest/appendlog_suite.go, pkg/blockstore/local/localtest/eviction_lsl08_suite.go, pkg/blockstore/local/localtest/doc.go, pkg/blockstore/remote/remotetest/suite.go, pkg/blockstore/remote/remotetest/doc.go</files>
  <read_first>
    - Output of `grep -rE 'pkg/blockstore/local/localtest|pkg/blockstore/remote/remotetest' --include='*.go' .` — MUST return zero non-self hits before deletion proceeds.
  </read_first>
  <action>
  Step 1 — final consumer scan (defensive). From repo root:
  - `grep -rE 'pkg/blockstore/local/localtest|pkg/blockstore/remote/remotetest' --include='*.go' .`
  - If ANY hit appears (other than the doc.go files themselves), STOP and fix the offending consumer first. Tasks 1 + 2 should have eliminated all import sites.
  
  Step 2 — delete `pkg/blockstore/local/localtest/` entirely:
  - Remove all files in the directory: `suite.go`, `appendlog_suite.go`, `eviction_lsl08_suite.go`, `doc.go`, plus any other files present.
  - Remove the directory itself (`git rm -r pkg/blockstore/local/localtest/`).
  
  Step 3 — delete `pkg/blockstore/remote/remotetest/` entirely:
  - Same pattern. `suite.go` + `doc.go` + directory.
  
  Step 4 — verify repo still builds + tests pass:
  - `go build ./...` exits 0.
  - `go vet ./...` exits 0.
  - `go test -count=1 -timeout 300s ./pkg/blockstore/...` exits 0 (excluding tests gated on external services via existing skip logic).
  - `find pkg/blockstore/local/localtest pkg/blockstore/remote/remotetest -type f 2>/dev/null | wc -l` returns 0.
  
  Step 5 — record LoC delta for the mega-PR SUMMARY:
  - Run `git diff --stat HEAD~ HEAD -- pkg/blockstore/local/localtest pkg/blockstore/remote/remotetest` (or equivalent against the wave-3 baseline). Capture in the plan SUMMARY: `Deleted N lines from localtest/ + M lines from remotetest/`. Carry these into the Wave 6 D-06 LoC accounting.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go build ./... && go vet ./... && test "$(find pkg/blockstore/local/localtest pkg/blockstore/remote/remotetest -type f 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `find pkg/blockstore/local/localtest pkg/blockstore/remote/remotetest -type f 2>/dev/null | wc -l` returns 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `go test -count=1 -timeout 300s ./pkg/blockstore/local/... ./pkg/blockstore/remote/memory/` exits 0
    - Repo-wide consumer check returns 0: `grep -rE 'pkg/blockstore/local/localtest|pkg/blockstore/remote/remotetest' --include='*.go' . | wc -l` returns 0
    - LoC delta captured in plan SUMMARY (informational only; not gating)
  </acceptance_criteria>
  <done>
    localtest/ + remotetest/ directories deleted; whole-repo build clean; LoC delta recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-package → backend | Conformance suite calls into backend constructors; trust boundary is internal (test code, not user input). No untrusted data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | T (Tampering) | blockstoretest scenarios silently weaker than localtest/remotetest | mitigate | Task 3 step 1 defensive grep + acceptance criterion comparing scenario counts; Plan 02 SUMMARY enumerated scenarios for diff. |
| T-17-06-02 | D (DoS via test flake) | s3 conformance over Localstack | accept | Existing skip guards (`DITTOFS_S3_ENDPOINT`) preserved; failures are CI infra issues not security. |
</threat_model>

<verification>
- `go vet ./...` exits 0
- `go build ./...` exits 0
- `go test -count=1 -timeout 300s ./pkg/blockstore/local/... ./pkg/blockstore/remote/memory/` exits 0
- Zero residual references to localtest/remotetest packages anywhere in repo
- Backend-specific tests (fs LSL-08, fs recovery, s3 BSCAS-06 header) still pass
</verification>

<success_criteria>
Four backends call blockstoretest.BlockStoreConformance; local backends also call BlockStoreAppendConformance; localtest/ + remotetest/ directories deleted; repo builds + tests pass; backend-specific tests preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-06-SUMMARY.md`
</output>
