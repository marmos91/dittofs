---
phase: 17-unified-blockstore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/blockstore/blockstore.go
  - pkg/blockstore/errors.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "`pkg/blockstore/blockstore.go` declares `BlockStore` interface, `BlockStoreAppend` interface (embedding `BlockStore`), and `Meta{Size int64, LastModified time.Time}` struct (D-08)"
    - "`BlockStore.Get(ctx context.Context, hash ContentHash) ([]byte, error)` is byte-identical to Phase 16's `local.LocalStore.Get` signature (Phase 16 D-01 carry-forward — engine call sites narrow without renaming)"
    - "`BlockStore.Walk(ctx, fn)` documents `ErrStopWalk` sentinel for clean early-exit + any-other-error wraps as `walk halted at %s: %w` (D-07)"
    - "`pkg/blockstore/errors.go` exposes `ErrStopWalk = errors.New(\"blockstore: stop walk\")` and `ErrLegacyLayoutDetected = errors.New(\"blockstore: legacy .blk layout detected (run `dfs migrate-to-cas`)\")` (D-10/D-11)"
    - "Sentinel detection convention chosen: `errors.Is(err, ErrLegacyLayoutDetected)` — both sentinels are `var = errors.New(...)` (not typed structs), so `errors.As` does not apply; this is the contract `cmd/dfs/start.go` will use in Plan 09"
    - "No consumer code is touched in this plan; the file is additive only — `go build ./...` clean after this plan"
  artifacts:
    - path: pkg/blockstore/blockstore.go
      provides: "BlockStore + BlockStoreAppend interfaces + Meta struct"
      contains: "type BlockStore interface"
    - path: pkg/blockstore/errors.go
      provides: "ErrStopWalk + ErrLegacyLayoutDetected sentinels"
      contains: "ErrStopWalk"
  key_links:
    - from: pkg/blockstore/blockstore.go
      to: pkg/blockstore/types.go (ContentHash type)
      via: "type reference"
      pattern: "ContentHash"
    - from: pkg/blockstore/blockstore.go
      to: pkg/blockstore/errors.go (ErrStopWalk)
      via: "doc reference"
      pattern: "ErrStopWalk"
---

<objective>
Land the unified `BlockStore` + `BlockStoreAppend` interfaces, the `Meta` struct, and the two new sentinel errors (`ErrStopWalk`, `ErrLegacyLayoutDetected`) as a purely additive change. No consumer is retargeted yet; this commit is the contract that Waves 2-6 will migrate the codebase to.

Purpose: Establish the single CAS-keyed contract that collapses `LocalStore` + `RemoteStore` so subsequent plans can rename / narrow / delete against a stable target. Phase 16's `local.Get(ctx, hash)` signature is locked verbatim as `BlockStore.Get` per D-01.

Output: One new file (`pkg/blockstore/blockstore.go`) and one modified file (`pkg/blockstore/errors.go`). Both compile against the existing tree with zero existing-test churn (interfaces have no implementers yet — that's Waves 2-4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/17-unified-blockstore/17-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Signature LOCK from Phase 16 (pkg/blockstore/local/local.go:71-85). BlockStore.Get MUST be byte-identical. -->

From pkg/blockstore/local/local.go:
```go
// Phase 16 — already shipped; do NOT alter signature:
Get(ctx context.Context, hash blockstore.ContentHash) ([]byte, error)
```

From pkg/blockstore/types.go (ContentHash already exists; reuse):
```go
type ContentHash [32]byte
func (h ContentHash) IsZero() bool
func (h ContentHash) CASKey() string  // "blake3:" + hex(h)
func FormatCASKey(h ContentHash) string  // "cas/{hh}/{hh}/{hex}"
func ParseCASKey(s string) (ContentHash, error)
```

From pkg/blockstore/errors.go (existing sentinel idiom — match exactly):
```go
// Existing pattern at lines 121-169:
//   - Doc paragraph above each var
//   - var ErrFoo = errors.New("blockstore: ...")
//   - wrapping via fmt.Errorf("...: %w", err)
//   - detection via errors.Is(err, ErrFoo)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ErrStopWalk + ErrLegacyLayoutDetected sentinels to pkg/blockstore/errors.go</name>
  <files>pkg/blockstore/errors.go</files>
  <read_first>
    - pkg/blockstore/errors.go (the file being modified — read entire file before adding)
    - pkg/blockstore/engine/errors.go (existing ErrLegacyReadOnCASOnly pattern — analog at lines 5-24)
  </read_first>
  <behavior>
    - `errors.Is(err, blockstore.ErrStopWalk)` returns `true` when called on `fmt.Errorf("...: %w", ErrStopWalk)`
    - `errors.Is(err, blockstore.ErrLegacyLayoutDetected)` returns `true` when called on `fmt.Errorf("%w: share path %s", ErrLegacyLayoutDetected, "/data/share-a")`
    - Both sentinel strings are non-empty and start with the `blockstore:` package prefix matching existing convention
    - `go vet ./pkg/blockstore/` clean
  </behavior>
  <action>Insert two new sentinel `var` declarations at the end of the `var ( ... )` block in pkg/blockstore/errors.go (after `ErrBlockRefMissing` on line 169, before the closing `)`). Match the existing sentinel idiom exactly: a doc paragraph above each var, then `var ErrFoo = errors.New("blockstore: ...")`. The two sentinels:

  1. `ErrStopWalk = errors.New("blockstore: stop walk")` — doc paragraph references D-07: callback returns this for clean early-exit (mirrors filepath.SkipDir / fs.SkipAll); Walk returns nil to outer caller; any other non-nil error halts and wraps with `fmt.Errorf("walk halted at %s: %w", hash, err)`. Include line "See BlockStore.Walk (Phase 17 D-07)."
  
  2. `ErrLegacyLayoutDetected = errors.New("blockstore: legacy .blk layout detected (run `dfs migrate-to-cas`)")` — doc paragraph references D-10/D-11: returned by `*fs.FSStore.NewFSStore` when share directory holds legacy `.blk` files but no `.cas-migrated-v1` sentinel; wrapping pattern `fmt.Errorf("%w: share path %s", ErrLegacyLayoutDetected, baseDir)`; detection pattern `errors.Is(err, ErrLegacyLayoutDetected)`; `cmd/dfs/start.go` unwraps and exits 78 (EX_CONFIG); operator action documented in `docs/CONFIGURATION.md §migration`. Include line "See Phase 17 D-10/D-11."
  
  Do NOT modify any existing sentinel doc or value. Do NOT change the `BlockStoreError` struct.</action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/ &amp;&amp; go build ./... &amp;&amp; grep -c "ErrStopWalk\|ErrLegacyLayoutDetected" pkg/blockstore/errors.go</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./pkg/blockstore/` exits 0
    - `go build ./...` exits 0 (no consumers yet — additive only)
    - `grep -c "ErrStopWalk" pkg/blockstore/errors.go` returns at least 1
    - `grep -c "ErrLegacyLayoutDetected" pkg/blockstore/errors.go` returns at least 1
    - File still contains pre-existing `ErrChunkNotFound`, `ErrCASContentMismatch`, `ErrBlockRefMissing` declarations (regression check via `grep -c 'ErrChunkNotFound\|ErrCASContentMismatch\|ErrBlockRefMissing' pkg/blockstore/errors.go` returns at least 3 lines counting only declarations)
  </acceptance_criteria>
  <done>
    Both sentinels exist with doc paragraphs matching existing tone; package builds; no behavior change in any consumer (none retargeted yet).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pkg/blockstore/blockstore.go with BlockStore + BlockStoreAppend interfaces and Meta struct</name>
  <files>pkg/blockstore/blockstore.go</files>
  <read_first>
    - pkg/blockstore/local/local.go (lines 71-85 — Phase 16 `local.Get` signature LOCK; D-01 forward-compat target)
    - pkg/blockstore/remote/remote.go (lines 63-149 — existing RemoteStore methods being collapsed; HeadResult struct at lines 20-29)
    - pkg/blockstore/types.go (ContentHash, FormatCASKey, ParseCASKey already exist)
    - pkg/blockstore/doc.go (existing tone/format for interface docs)
  </read_first>
  <behavior>
    - File declares `package blockstore`
    - Type `Meta struct { Size int64; LastModified time.Time }` exists (D-08, no hash field, no other fields)
    - Interface `BlockStore` declares exactly these methods, with `ctx context.Context` first on every method: `Put(ctx, hash ContentHash, data []byte) error`; `Get(ctx, hash ContentHash) ([]byte, error)` — signature byte-identical to `local.LocalStore.Get`; `GetRange(ctx, hash ContentHash, offset, length int64) ([]byte, error)`; `Has(ctx, hash ContentHash) (bool, error)`; `Delete(ctx, hash ContentHash) error`; `Head(ctx, hash ContentHash) (Meta, error)`; `Walk(ctx, fn func(hash ContentHash, meta Meta) error) error`
    - Interface `BlockStoreAppend` embeds `BlockStore` and adds the existing local append-log methods (`AppendWrite`, `DeleteLog`) — see Claude's Discretion below for exact method list
    - `Walk` godoc references `ErrStopWalk` and the wrap pattern `fmt.Errorf("walk halted at %s: %w", hash, err)` (D-07)
    - `Get` godoc carries the verbatim wording from `local.LocalStore.Get` (fresh allocation, caller-owned, no internal aliasing)
    - `Meta` godoc explicitly states ContentHash is the key and not echoed inside Meta (D-08), and that S3's x-amz-meta-content-hash stays inside the s3 backend (BSCAS-06)
    - `go vet ./pkg/blockstore/` clean; `go build ./...` clean — file is additive, no implementers exist yet
  </behavior>
  <action>Create the new file `pkg/blockstore/blockstore.go` declaring `package blockstore` with imports `context` and `time`. The file contains:

  1. `type Meta struct` per behavior block — minimal `{Size int64; LastModified time.Time}`. Doc paragraph above the type per D-08: ContentHash is the lookup key and is NEVER echoed inside `Meta`; S3's `x-amz-meta-content-hash` is preserved INSIDE the s3 backend as defense-in-depth (BSCAS-06) but invisible to `Meta` consumers.
  
  2. `type BlockStore interface` declaring the seven methods listed in <behavior>. Godoc on each method preserves the semantics of the analog being renamed (use Analog A from PATTERNS.md for `Get`; use Analog B for `Head` and `GetRange`). The `Walk` method's godoc carries the contract from PATTERNS.md lines 92-101 verbatim: ordering unspecified, ErrStopWalk for clean early exit, any other error halts and wraps as `walk halted at %s: %w`, context cancellation aborts immediately and callback is NOT re-invoked after ctx.Err() != nil.
  
  3. `type BlockStoreAppend interface` that embeds `BlockStore` and adds two methods. **Claude's Discretion (per CONTEXT.md):** the append surface that survives Wave 4 deletions is `AppendWrite(ctx context.Context, payloadID string, data []byte, offset uint64) error` (from existing appendwrite.go) and `DeleteLog(ctx context.Context, payloadID string) error` (renamed from `DeleteAppendLog` to match the conformance-suite test name `testDeleteLog`). The rollup loop is internal to the fs backend and not part of the interface. Both methods carry the existing godoc semantics from `pkg/blockstore/local/fs/appendwrite.go`. Note in a leading file-level comment that the LocalStore's other lifecycle/admin methods (`Truncate`, `EvictMemory`, `SetRetentionPolicy`, `SetEvictionEnabled`, `Stats`, `ListFiles`, `GetStoredFileSize`, `Healthcheck`, `SyncFileBlocks`, `SyncFileBlocksForFile`, `Flush`, `Start`, `Close`) stay on the narrowed `LocalStore` superset in Plan 04, NOT on `BlockStoreAppend`.
  
  4. File-level package godoc (5-15 lines) at top: "Unified BlockStore contract — Phase 17. Replaces the split LocalStore (22 methods) + RemoteStore (12 methods) with a single CAS-keyed surface. `BlockStoreAppend` extends `BlockStore` with the random-write absorber tier used by the fs backend only. See doc.go for sentinel-file conventions."
  
  Do NOT add any `var _ BlockStore = ...` compile-time assertions in this file — implementers land in Waves 2-4; assertions belong in their files. Do NOT modify `local.go`, `remote.go`, `types.go`, or any backend file. Do NOT delete `LocalStore` or `RemoteStore` yet — those are Wave 2 work.</action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 &amp;&amp; go vet ./pkg/blockstore/ &amp;&amp; go build ./... &amp;&amp; grep -c "^func\|^type BlockStore\|^type BlockStoreAppend\|^type Meta" pkg/blockstore/blockstore.go</automated>
  </verify>
  <acceptance_criteria>
    - File `pkg/blockstore/blockstore.go` exists and is non-empty
    - `go vet ./pkg/blockstore/` exits 0
    - `go build ./...` exits 0 (no implementers exist yet — interfaces are additive)
    - `grep -E "type BlockStore interface|type BlockStoreAppend interface|type Meta struct" pkg/blockstore/blockstore.go` returns 3 lines
    - `grep "BlockStoreAppend interface" pkg/blockstore/blockstore.go` shows the type embeds `BlockStore` (the next non-blank line contains the bare identifier `BlockStore`)
    - `grep -c "ErrStopWalk" pkg/blockstore/blockstore.go` returns at least 1 (Walk godoc references the sentinel)
    - `Get` method signature in this file matches `Get(ctx context.Context, hash ContentHash) ([]byte, error)` byte-for-byte
    - `Meta` struct contains EXACTLY two fields: `Size int64` and `LastModified time.Time` — no `Hash`, no `Key`, no anything else (regression check: `grep -A 4 "^type Meta struct" pkg/blockstore/blockstore.go | grep -cE '^\s+\w+\s+' ` returns 2)
  </acceptance_criteria>
  <done>
    File created; package builds; the three additive types exist with their locked signatures; Wave 2 can now retarget consumers against these types.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Additive type declarations only; no I/O, no input parsing, no network. No new attack surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | BlockStore interface contract | accept | Interface declarations are compile-time enforced; tampering would require a malicious commit which is out-of-scope for ASVS L1. |
| T-17-01-02 | Information Disclosure | Meta struct content | mitigate | D-08 explicitly excludes ContentHash from Meta; S3 backend keeps x-amz-meta-content-hash internal. Plan 03 will assert s3.Head does not leak the header via Meta. |
</threat_model>

<verification>
- `go vet ./pkg/blockstore/` exits 0
- `go build ./...` exits 0
- New file `pkg/blockstore/blockstore.go` exists; new sentinels exist in `pkg/blockstore/errors.go`
- No existing test fails (additive change; no consumer rewires yet)
</verification>

<success_criteria>
Plan 01 is complete when:
- [ ] Both tasks pass their automated `<verify>` commands
- [ ] `pkg/blockstore/blockstore.go` declares `BlockStore`, `BlockStoreAppend`, and `Meta` with the locked signatures
- [ ] `pkg/blockstore/errors.go` declares `ErrStopWalk` and `ErrLegacyLayoutDetected`
- [ ] `go test ./pkg/blockstore/...` passes (no behavioral change; sentinels are additive)
- [ ] Commit message follows project convention (no Claude Code, signed)
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-01-SUMMARY.md` capturing:
- Final method list in `BlockStoreAppend` (Claude's Discretion resolution)
- Exact signature of `Walk` and the godoc wording chosen
- Confirmation that `BlockStore.Get` is byte-identical to `local.LocalStore.Get`
- Any divergence from PATTERNS.md analog wording
</output>
