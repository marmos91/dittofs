---
phase: 17-unified-blockstore
plan: 07
type: execute
wave: 4
depends_on: [17-04-PLAN.md, 17-05-PLAN.md, 17-06-PLAN.md]
files_modified:
  - pkg/blockstore/local/fs/write.go
  - pkg/blockstore/local/fs/fs.go
  - pkg/blockstore/local/fs/manage.go
  - pkg/blockstore/local/fs/delete_truncate_test.go
  - pkg/blockstore/local/fs/appendwrite.go
  - pkg/blockstore/local/fs/rollup.go
  - pkg/blockstore/local/fs/fs_test.go
  - pkg/blockstore/local/fs/flush.go
  - pkg/blockstore/local/fs/flush_test.go
  - pkg/blockstore/local/fs/appendwrite_bench_test.go
  - pkg/blockstore/local/fs/fs_conformance_test.go
  - pkg/blockstore/local/memory/memory.go
  - pkg/blockstore/local/local.go
  - pkg/blockstore/types.go
  - pkg/blockstore/errors.go
  - pkg/blockstore/engine/fetch.go
  - pkg/blockstore/engine/sync_entry.go
  - pkg/blockstore/engine/upload.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "`pkg/blockstore/local/fs/write.go` DELETED (223 lines): `WriteAt`, `tryDirectDiskWrite`, `createBlockFile`, `directDiskWriteThreshold`, the `<share>/<file>/<idx>.blk` writer path, `memBlock` writer interaction — all gone (D-01 deletion list)"
    - "`pkg/blockstore/types.go::FormatStoreKey` DELETED — no remaining consumers after Plan 05 deleted the engine fallback (`engine/fetch.go` lines 231/431/491 and `engine/sync_entry.go` line 43 are part of this plan's cleanup)"
    - "Legacy helpers DELETED from `pkg/blockstore/local/fs/fs.go` and `manage.go` (subset that has zero remaining consumers post-Plan-05): `ExistsOnDisk`, `DeleteBlockFile`, `TruncateBlockFiles`. The `path-keyed .blk writer` (`write.go`) is also deleted. Engine-consumed admin methods (`ReadAt`, `WriteAt`, `Flush`, `IsBlockLocal`, `GetBlockData`, `WriteFromRemote`, `DeleteAllBlockFiles`) are KEPT on FSStore — they satisfy the transitional admin-superset on the narrowed `LocalStore` interface per Plan 04. Phase 18's Syncer rewrite is the natural deletion site for those engine sites + corresponding FSStore methods. See CONTEXT.md `<deferred>` Phase 18 carry-over for rationale (D-01 atomic-merge constraint)."
    - "Plan 07 deletes ONLY the path-keyed writer file and its directly-orphaned helpers: `pkg/blockstore/local/fs/write.go`, `FormatStoreKey`, `UseAppendLog` flag, `ErrAppendLogDisabled` sentinel. The engine-consumed admin methods remain on FSStore through Phase 17."
    - "`FSStoreOptions.UseAppendLog` field DELETED — append is now mandatory on the local tier (BlockStoreAppend embedding makes it part of the contract; no opt-out). `pkg/blockstore/local/fs/rollup.go::ErrAppendLogDisabled` sentinel DELETED."
    - "All test files that referenced `UseAppendLog: false` rewritten to drop the field (the test scenarios that depended on the opt-out are obsolete — they verified the legacy path-keyed writer is now gone)"
    - "`FlushedBlock` struct PRESERVED in `pkg/blockstore/local/local.go` (returned by the transitional `Flush` method retained on LocalStore per Plan 04 — engine consumer at syncer.go:381). `PendingBlock` DELETED only if grep confirms no remaining consumer post-write.go-deletion."
    - "`pkg/blockstore/local/memory/memory.go` updated: any method that used `FormatStoreKey` is removed or rewritten to use the CAS key; the memory backend conforms to the narrowed `LocalStore` interface from Plan 04"
    - "`var _ local.LocalStore = (*FSStore)(nil)` compile-time assertion RESTORED at `pkg/blockstore/local/fs/fs.go` (was commented out in Plan 04 with the `Plan 17-07 restores` marker); same restoration for memory backend. FSStore now satisfies the narrowed LocalStore interface because: (a) it embeds BlockStoreAppend via the CAS-keyed methods added in Plans 05/06, (b) the transitional admin methods (ReadAt/WriteAt/Flush/IsBlockLocal/GetBlockData/WriteFromRemote/DeleteAllBlockFiles) remain on *FSStore — Plan 07 does NOT delete them. Phase 18 deletes the consumers + the methods together."
    - "`go build ./...` exits 0 (whole-repo clean)"
    - "`go vet ./...` exits 0"
    - "`go test -count=1 -timeout 300s ./pkg/blockstore/...` exits 0"
  artifacts:
    - path: pkg/blockstore/local/fs/write.go
      provides: "DELETED — file no longer exists"
      contains: ""
    - path: pkg/blockstore/local/fs/fs.go
      provides: "FSStore narrowed; legacy helpers gone; LocalStore interface assertion restored"
      contains: "var _ local.LocalStore = (*FSStore)(nil)"
    - path: pkg/blockstore/types.go
      provides: "FormatStoreKey deleted"
      contains: "FormatCASKey"
  key_links:
    - from: pkg/blockstore/local/fs/fs.go
      to: pkg/blockstore/local/local.go (narrowed LocalStore from Plan 04)
      via: "compile-time interface assertion"
      pattern: "var _ local.LocalStore = \\(\\*FSStore\\)\\(nil\\)"
---

<objective>
Delete the legacy path-keyed `.blk` writer file and the fully-orphaned helpers from `pkg/blockstore/local/fs/`. Drop `FormatStoreKey` from `pkg/blockstore/types.go`. Remove `FSStoreOptions.UseAppendLog` + `ErrAppendLogDisabled` (append is now mandatory). Restore the `var _ local.LocalStore = (*FSStore)(nil)` compile-time assertion that Plan 04 commented out. Whole-repo build clean.

Purpose: Wave 4 is the structural deletion wave for the path-keyed writer and its directly-orphaned helpers. Engine-consumed admin methods (ReadAt/WriteAt/Flush/IsBlockLocal/GetBlockData/WriteFromRemote/DeleteAllBlockFiles) are NOT deleted here — D-01's atomic-merge constraint requires every commit be `go build ./...`-clean, and the engine consumer rewrite is naturally Phase 18's Syncer simplification work. Plan 07 deletes ONLY: write.go, FormatStoreKey, UseAppendLog flag, ErrAppendLogDisabled sentinel, and orphans (ExistsOnDisk/DeleteBlockFile/TruncateBlockFiles).

Output: `write.go` gone; `FormatStoreKey` gone; orphan helpers (ExistsOnDisk, DeleteBlockFile, TruncateBlockFiles) gone; `UseAppendLog` flag + `ErrAppendLogDisabled` gone; FSStore and memory backends both satisfy the narrowed LocalStore interface (BlockStoreAppend methods present + transitional admin methods retained).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-unified-blockstore/17-CONTEXT.md
@.planning/phases/17-unified-blockstore/17-PATTERNS.md
@.planning/phases/17-unified-blockstore/17-04-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-05-SUMMARY.md
@.planning/phases/17-unified-blockstore/17-06-SUMMARY.md
@CLAUDE.md
@pkg/blockstore/local/fs/write.go
@pkg/blockstore/local/fs/fs.go

<interfaces>
Plan 04 left FSStore failing the narrowed LocalStore interface (compile-time assertion commented out) because the BlockStoreAppend-contributed methods (Put/Has/Walk) weren't yet on FSStore. This plan restores the assertion by:
1. Deleting write.go (the path-keyed `WriteAt`/`tryDirectDiskWrite` file — replaced by the CAS-keyed Put plumbing added in Plans 05/06)
2. Deleting orphan helpers (ExistsOnDisk, DeleteBlockFile, TruncateBlockFiles) — methods with ZERO remaining consumers post-Plan-05
3. Removing UseAppendLog opt-out (append is mandatory)
4. Restoring `var _ local.LocalStore = (*FSStore)(nil)` line

KEPT on FSStore (transitional admin-superset; deleted in Phase 18 alongside their engine consumers): `ReadAt`, `WriteAt` (the legacy-typed `WriteAt(ctx, payloadID, data, offset)` — distinct from the deleted file `write.go`), `Flush`, `IsBlockLocal`, `GetBlockData`, `WriteFromRemote`, `DeleteAllBlockFiles`. These satisfy the narrowed LocalStore interface from Plan 04. **Important**: if `write.go` contains the `WriteAt` method body, the deletion of `write.go` must move the `WriteAt` implementation to another file (e.g., `pkg/blockstore/local/fs/write_transitional.go`) so the method survives on `*FSStore`. Verify by `grep -n 'func (.*FSStore) WriteAt' pkg/blockstore/local/fs/` after Step 1 — must return at least 1 line.

Engine consumers (Plan 05 retargeted ONLY the RemoteStore method names — the LocalStore engine sites are still legacy) continue calling `m.local.ReadAt`/`m.local.WriteAt`/etc. Plan 07 does NOT touch those engine sites. Phase 18 rewrites them onto BlockStore.Put/Get/Walk.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pkg/blockstore/local/fs/write.go and remove legacy helpers from fs.go + manage.go</name>
  <files>pkg/blockstore/local/fs/write.go, pkg/blockstore/local/fs/fs.go, pkg/blockstore/local/fs/manage.go</files>
  <read_first>
    - pkg/blockstore/local/fs/write.go (entire 223-line file — confirm WriteAt + tryDirectDiskWrite + createBlockFile + directDiskWriteThreshold are the only top-level symbols)
    - pkg/blockstore/local/fs/fs.go (find legacy methods on *FSStore: IsBlockLocal, GetBlockData, ExistsOnDisk, ReadAt, Flush)
    - pkg/blockstore/local/fs/manage.go (find legacy methods: DeleteBlockFile, DeleteAllBlockFiles, TruncateBlockFiles, WriteFromRemote)
    - pkg/blockstore/local/local.go (post-Plan-04 — confirm which symbols are NO LONGER on the LocalStore interface)
  </read_first>
  <action>
  Step 1 — Delete the PATH-KEYED writer from `pkg/blockstore/local/fs/write.go`, preserving the engine-consumed transitional `WriteAt(ctx, payloadID, data, offset)`:
  - First, inspect the file: `grep -n 'func ' pkg/blockstore/local/fs/write.go`. The path-keyed writer is `tryDirectDiskWrite`/`createBlockFile`/`directDiskWriteThreshold`. The engine-consumed `WriteAt(ctx, payloadID, data, offset)` may also live here — engine consumer at `engine.go:320`.
  - If `WriteAt(ctx, payloadID, ...)` is defined in write.go and engine still calls it (which it does — Plan 05 only retargeted the RemoteStore method renames; engine LocalStore consumer sites are untouched), MOVE just the `WriteAt` method body to a NEW file `pkg/blockstore/local/fs/write_transitional.go` with godoc: `// WriteAt is the engine-consumed transitional admin write path. Deleted in Phase 18 alongside its engine consumers (Syncer rewrite onto BlockStore.Put).`
  - Then `git rm pkg/blockstore/local/fs/write.go`.
  - This removes the path-keyed writer (`tryDirectDiskWrite`, `createBlockFile`, `directDiskWriteThreshold`, the `<share>/<file>/<idx>.blk` layout, the writer-side `memBlock` interaction) WHILE preserving `WriteAt` on `*FSStore` for engine consumption.
  - Verify post-step: `grep -c 'func (.*FSStore) WriteAt' pkg/blockstore/local/fs/` returns at least 1 (the transitional method survived the file deletion).
  
  Step 2 — `pkg/blockstore/local/fs/fs.go` — delete ORPHAN legacy methods only; KEEP transitional admin methods:
  - DELETE `(bc *FSStore) ExistsOnDisk(...)` (no remaining consumer post-Plan-05).
  - KEEP `(bc *FSStore) IsBlockLocal(...)` — transitional admin method per Plan 04's narrowed interface. Engine consumers at fetch.go:155/192/420/482.
  - KEEP `(bc *FSStore) GetBlockData(...)` — transitional admin. Engine consumer at upload.go:168.
  - KEEP `(bc *FSStore) ReadAt(...)` — transitional admin. Engine consumers at engine.go:800/828.
  - KEEP `(bc *FSStore) Flush(...)` — transitional admin. Engine consumer at syncer.go:381. (DO confirm this is the `Flush(ctx, payloadID) ([]FlushedBlock, error)` signature, not a different `Flush` tied solely to the deleted writer.)
  - Restore `var _ local.LocalStore = (*FSStore)(nil)` compile-time assertion (Plan 04 commented it out around line 47, marker `Plan 17-07 restores`). The narrowed interface now matches because FSStore has BlockStoreAppend methods (added in Plans 05/06) + the transitional admin methods (still present).
  - Drop any `memBlock`-related fields on FSStore that ONLY supported the path-keyed writer's local-store state. The LRU/chunk-store-side memBlock used by CAS reads STAYS. Trace each field's references via `grep -rn '<fieldName>' pkg/blockstore/local/fs/`; only remove if all callers were in the path-keyed writer code that just got deleted in Step 1.
  - The `getOrCreateMemBlock` / `getMemBlock` helpers at lines 669/698 — KEEP unless grep proves they were exclusively path-keyed-writer helpers (likely they are also called by the CAS read path's local-cache layer).
  
  Step 3 — `pkg/blockstore/local/fs/manage.go` — delete ORPHAN legacy methods only; KEEP transitional:
  - DELETE `(bc *FSStore) DeleteBlockFile(...)` (no remaining consumer post-Plan-05).
  - KEEP `(bc *FSStore) DeleteAllBlockFiles(...)` — transitional admin. Engine consumers at engine.go:423/635.
  - DELETE `(bc *FSStore) TruncateBlockFiles(...)` (no remaining consumer).
  - KEEP `(bc *FSStore) WriteFromRemote(...)` — transitional admin. Engine consumers at fetch.go:140/302.
  - DELETE any private helper in manage.go that is ONLY called by the just-deleted methods above (`DeleteBlockFile`, `TruncateBlockFiles`). Verify via `grep -rn '<helperName>' pkg/blockstore/`; only remove if grep returns 0 lines outside the deleted methods.
  
  Step 4 — `pkg/blockstore/local/local.go` — preserve types referenced by transitional methods:
  - KEEP `type FlushedBlock struct { ... }` — still returned by the transitional `Flush(ctx, payloadID) ([]FlushedBlock, error)` method (engine consumer at syncer.go:381).
  - `type PendingBlock struct { ... }`: search for callers via `grep -rn 'PendingBlock' pkg/blockstore/`. If grep returns 0 lines outside `local.go`, DELETE; otherwise PRESERVE.
  - Confirm no compile breakage in pkg/blockstore/local/ from this step.
  
  Step 5 — verify the fs package builds + the LocalStore interface assertion holds:
  - `go vet ./pkg/blockstore/local/fs/` exits 0.
  - `go build ./pkg/blockstore/local/fs/` exits 0.
  - The compile-time assertion `var _ local.LocalStore = (*FSStore)(nil)` succeeds.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && test ! -f pkg/blockstore/local/fs/write.go && go vet ./pkg/blockstore/local/fs/ && go build ./pkg/blockstore/local/fs/</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/blockstore/local/fs/write.go` does NOT exist
    - `go build ./pkg/blockstore/local/fs/` exits 0
    - `go vet ./pkg/blockstore/local/fs/` exits 0
    - `grep -c 'var _ local.LocalStore = (\*FSStore)(nil)' pkg/blockstore/local/fs/fs.go` returns at least 1 (assertion restored)
    - Orphan methods removed; transitional methods present. `grep -rcE '\bExistsOnDisk\b|\bDeleteBlockFile\b|\bTruncateBlockFiles\b' pkg/blockstore/ --include='*.go' | grep -v _test.go | grep -v ':0$' | wc -l` returns 0 (orphans gone)
    - `grep -rcE '\bIsBlockLocal\b|\bGetBlockData\b|\bDeleteAllBlockFiles\b|\bWriteFromRemote\b|\bReadAt\b|\bWriteAt\b|\bFlush\b' pkg/blockstore/local/fs/ --include='*.go' | grep -v _test.go | grep -v ':0$' | wc -l` returns at least 7 (transitional methods preserved on FSStore)
    - `grep -c 'type FlushedBlock' pkg/blockstore/local/local.go` returns at least 1 (preserved for Flush return type)
    - `grep -c 'func (.*FSStore) WriteAt' pkg/blockstore/local/fs/` (across all .go files) returns at least 1 (WriteAt method survived write.go deletion — moved to write_transitional.go if needed)
  </acceptance_criteria>
  <done>
    write.go deleted (path-keyed writer gone); WriteAt preserved on *FSStore via write_transitional.go; orphan legacy methods (ExistsOnDisk, DeleteBlockFile, TruncateBlockFiles) deleted; transitional admin methods (ReadAt, WriteAt, Flush, IsBlockLocal, GetBlockData, WriteFromRemote, DeleteAllBlockFiles) preserved with `Deprecated: removed in Phase 18` godoc; FlushedBlock struct preserved; LocalStore interface assertion restored on FSStore.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove UseAppendLog opt-out + ErrAppendLogDisabled + FormatStoreKey + legacy engine helpers</name>
  <files>pkg/blockstore/types.go, pkg/blockstore/errors.go, pkg/blockstore/local/fs/fs.go, pkg/blockstore/local/fs/appendwrite.go, pkg/blockstore/local/fs/rollup.go, pkg/blockstore/local/memory/memory.go, pkg/blockstore/engine/fetch.go, pkg/blockstore/engine/sync_entry.go, pkg/blockstore/engine/upload.go</files>
  <read_first>
    - pkg/blockstore/types.go (lines 240-258 — FormatStoreKey signature; lines 262+ — FormatCASKey to confirm survives)
    - pkg/blockstore/local/fs/fs.go (find FSStoreOptions struct; locate UseAppendLog field)
    - pkg/blockstore/local/fs/rollup.go (line 42 — ErrAppendLogDisabled return site)
    - pkg/blockstore/local/fs/appendwrite.go (search for UseAppendLog branch — should gate AppendWrite path)
    - pkg/blockstore/local/memory/memory.go (line 53 — FormatStoreKey call site)
    - pkg/blockstore/engine/fetch.go (lines 231, 431, 491 — FormatStoreKey call sites; were preserved through Plan 05 because the surrounding code wasn't the legacy fallback)
    - pkg/blockstore/engine/sync_entry.go (line 43 — FormatStoreKey)
    - pkg/blockstore/engine/upload.go (line 157 comment — was a tombstone reference)
  </read_first>
  <action>
  Step 1 — `pkg/blockstore/local/fs/fs.go` FSStoreOptions:
  - DELETE the `UseAppendLog bool` field from the FSStoreOptions struct.
  - DELETE the godoc that documented it.
  - Find every `if opts.UseAppendLog { ... }` branch in `NewFSStore` / `NewWithOptions`. The path was opt-IN to AppendWrite; with append mandatory, the branch's THEN body becomes unconditional. Inline it.
  
  Step 2 — `pkg/blockstore/local/fs/appendwrite.go`:
  - Find any `if !bc.opts.UseAppendLog { return ErrAppendLogDisabled }` (or equivalent gate) at the top of AppendWrite. DELETE it.
  - AppendWrite now always proceeds.
  
  Step 3 — `pkg/blockstore/local/fs/rollup.go`:
  - DELETE the `ErrAppendLogDisabled` sentinel (around line 42).
  - DELETE any `return ErrAppendLogDisabled` return sites in rollup.go.
  - If the rollup path had a `useAppendLog` field on a struct, delete it and inline the always-true branch.
  
  Step 4 — `pkg/blockstore/types.go`:
  - DELETE `FormatStoreKey(payloadID string, blockIdx uint64) string` (around line 240).
  - DELETE any helper that parsed the legacy `<payloadID>/block-{idx}` key shape (look for `ParseStoreKey`, `BlockStoreKey` parsing functions — distinct from the CAS-key parsers `FormatCASKey` / `ParseCASKey` which STAY).
  - The `BlockStoreKey` field on FileBlock struct: if it still exists, mark its omission for Phase 18 (it's a metadata-layer field, not directly in `pkg/blockstore/types.go`; only remove the helper functions here).
  
  Step 5 — `pkg/blockstore/local/memory/memory.go`:
  - Line 53 (current): `return blockstore.FormatStoreKey(payloadID, blockIdx)` — this is in a method that returned the legacy key for an external lookup. With FormatStoreKey gone, the method either disappears entirely or returns the CAS key via `blockstore.FormatCASKey(hash)`. Decide based on the method's caller. Trace upward — if no caller exists post-Plan-04 (likely), DELETE the entire method.
  - Restore the compile-time assertion `var _ local.LocalStore = (*MemoryStore)(nil)` (or equivalent for the type name in this file).
  
  Step 6 — `pkg/blockstore/engine/fetch.go` lines 231, 431, 491:
  - Each `key := blockstore.FormatStoreKey(payloadID, blockIdx)` site is in code that was NOT the deleted legacy fallback — it's in some other read path. Trace each site:
    - If the surrounding function is a legacy code path that Plan 05 missed, delete the function entirely.
    - If the surrounding function is part of the live CAS read path that happens to call FormatStoreKey to produce a remote `key` argument for a renamed method, the call is dead weight: the CAS key is derived from hash via `FormatCASKey(hash)`, and the renamed `RemoteStore.Get(ctx, hash)` takes hash directly. Delete the local `key` variable + the call.
    - Defensive: if a function still passes `key` to ReadBlockVerified (post-Plan-03 signature is `ReadBlockVerified(ctx, hash, expected)`), the caller is mis-wired — fix to pass hash.
  
  Step 7 — `pkg/blockstore/engine/sync_entry.go` line 43:
  - Same treatment as Step 6. Trace the caller. Delete the FormatStoreKey call.
  
  Step 8 — `pkg/blockstore/engine/upload.go` line 157:
  - The comment at line 157 mentions `FormatStoreKey path is gone from the upload write path. Reads of legacy ...`. With this plan, the comment becomes stale. Either delete the comment block entirely, or rephrase to past-tense ("FormatStoreKey was deleted in Phase 17"). Prefer deletion — comments documenting things-that-no-longer-exist accumulate.
  
  Step 9 — `pkg/blockstore/local/local.go`:
  - Search for any reference to UseAppendLog or ErrAppendLogDisabled (in godoc comments, the DeleteAppendLog method's godoc, etc.). Clean up stale references.
  
  Step 10 — verify whole-repo build clean:
  - `go vet ./...` exits 0.
  - `go build ./...` exits 0.
  - `grep -rE '\bUseAppendLog\b|\bErrAppendLogDisabled\b|\bFormatStoreKey\b' --include='*.go' .` returns 0 lines in non-test files.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` exits 0
    - `go build ./...` exits 0
    - `grep -rE '\bFormatStoreKey\b' --include='*.go' . | wc -l` returns 0 (whole-repo: helper is gone, no callers)
    - `grep -rE '\bUseAppendLog\b' --include='*.go' . | grep -v _test.go | wc -l` returns 0 in non-test source
    - `grep -rE '\bErrAppendLogDisabled\b' --include='*.go' . | wc -l` returns 0
    - `grep -c 'FormatCASKey\|ParseCASKey' pkg/blockstore/types.go` returns at least 2 (CAS-side helpers preserved)
    - `grep -c 'var _ local.LocalStore' pkg/blockstore/local/memory/memory.go` returns at least 1 (memory backend assertion restored)
  </acceptance_criteria>
  <done>
    UseAppendLog opt-out gone; ErrAppendLogDisabled gone; FormatStoreKey gone whole-repo; engine + memory backend cleaned; both backends assert LocalStore conformance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adapt remaining tests for deleted symbols + run full test suite</name>
  <files>pkg/blockstore/local/fs/delete_truncate_test.go, pkg/blockstore/local/fs/fs_test.go, pkg/blockstore/local/fs/flush.go, pkg/blockstore/local/fs/flush_test.go, pkg/blockstore/local/fs/appendwrite_bench_test.go, pkg/blockstore/local/fs/fs_conformance_test.go</files>
  <read_first>
    - pkg/blockstore/local/fs/delete_truncate_test.go (uses UseAppendLog: false at line 120, 427; UseAppendLog: true at line 228, 299, 328, 451)
    - pkg/blockstore/local/fs/fs_test.go (any usage of legacy methods)
    - pkg/blockstore/local/fs/flush.go (only the rollup flush — verify it survives)
    - pkg/blockstore/local/fs/flush_test.go (any UseAppendLog references)
    - pkg/blockstore/local/fs/appendwrite_bench_test.go (line 49 — UseAppendLog: true)
    - pkg/blockstore/local/fs/fs_conformance_test.go (post-Plan-06 — already updated; verify no UseAppendLog refs remain)
  </read_first>
  <action>
  Step 1 — `pkg/blockstore/local/fs/delete_truncate_test.go`:
  - Line 120: `bc := newFSStoreForTest(t, FSStoreOptions{UseAppendLog: false})` — the field is gone. Decide per-test:
    - If the test verified behavior of the legacy path-keyed writer (which is now deleted), DELETE the test function entirely.
    - If the test verified post-CAS DeleteBlock / Truncate behavior that survives in the narrowed interface, REMOVE the `UseAppendLog: false` field (default-construct the options).
  - Line 427: same treatment.
  - Lines 228, 299, 328, 451 (`UseAppendLog: true`): the field is gone; remove the field from the struct literal. The test continues to exercise the now-mandatory append path.
  
  Step 2 — `pkg/blockstore/local/fs/appendwrite_bench_test.go` line 49:
  - Remove `UseAppendLog: true` from the options literal.
  
  Step 3 — `pkg/blockstore/local/fs/fs_conformance_test.go`:
  - Post-Plan-06 this file already uses `FSStoreOptions{UseAppendLog: true, ...}` to construct the append factory. Remove `UseAppendLog: true` here as well (append is mandatory).
  
  Step 4 — `pkg/blockstore/local/fs/flush.go` + `flush_test.go`:
  - flush.go is the rollup flush — verify it does NOT reference any deleted symbol (`memBlock` writer fields, legacy `Flush` method body). The rollup flush is independent.
  - flush_test.go: clear stale references; ensure tests run against the post-deletion code path.
  
  Step 5 — `pkg/blockstore/local/fs/fs_test.go`:
  - Any test calling the FULLY-deleted methods (ExistsOnDisk, DeleteBlockFile, TruncateBlockFiles) MUST be deleted or rewritten:
    - If the test verified the legacy path-keyed behavior, DELETE.
    - If the test verified an invariant that survives in the CAS path (e.g., "data persists across Close/Reopen"), REWRITE to use Put/Get.
  - Tests calling the PRESERVED transitional methods (ReadAt, WriteAt, Flush, IsBlockLocal, GetBlockData, WriteFromRemote, DeleteAllBlockFiles) are LEFT AS-IS — those methods still exist on FSStore through Phase 17. Phase 18 will delete the tests alongside the methods.
  
  Step 6 — run the full test suite for the package:
  - `go test -count=1 -timeout 180s ./pkg/blockstore/local/fs/` exits 0.
  - `go test -count=1 -race -timeout 240s ./pkg/blockstore/local/fs/` exits 0 (race tests catch deletion-related deadlocks).
  
  Step 7 — repo-wide regression check:
  - `go test -count=1 -timeout 600s ./pkg/blockstore/...` exits 0 (excluding tests gated on external services via existing skip logic).
  - `go test -count=1 -timeout 600s ./pkg/controlplane/...` exits 0 (post-Plan-05 the controlplane was already cleaned; this is a defensive re-verify).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go vet ./... && go test -count=1 -timeout 300s ./pkg/blockstore/local/fs/ ./pkg/blockstore/local/memory/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -count=1 -timeout 300s ./pkg/blockstore/local/fs/` exits 0
    - `go test -count=1 -timeout 300s ./pkg/blockstore/local/memory/` exits 0
    - `go test -count=1 -race -timeout 300s ./pkg/blockstore/local/fs/` exits 0
    - `grep -rE '\bUseAppendLog\b' --include='*.go' pkg/blockstore/local/fs/ | wc -l` returns 0 (test files cleaned)
    - `grep -rE '\bExistsOnDisk\b|\bDeleteBlockFile\b|\bTruncateBlockFiles\b' --include='*.go' pkg/blockstore/local/fs/ | wc -l` returns 0 (orphan-method test references cleaned)
    - Tests for transitional methods (IsBlockLocal, GetBlockData, DeleteAllBlockFiles, WriteFromRemote, ReadAt, WriteAt, Flush) are LEFT AS-IS in test files — those methods exist through Phase 17
    - `go test -count=1 -timeout 600s ./pkg/blockstore/... ./pkg/controlplane/...` exits 0 (with external-service skip guards honored)
  </acceptance_criteria>
  <done>
    All tests adapted to deleted symbols; full pkg/blockstore/ + pkg/controlplane/ test suites green; race-detector tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → fs.FSStore | Public methods on FSStore are callable from the engine; narrowing the interface (deletion) removes attack surface. No untrusted input crosses inside this plan. |
| disk layout migration | Pre-existing `.blk` files on disk have no consumer post-this-plan. They are not deleted by this plan — Plan 08's migration tool handles that, and Plan 09's boot guard refuses to start until migration runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-01 | I (Information disclosure) | Pre-existing `.blk` files on disk become orphans | mitigate | Plan 09's boot guard refuses to start; Plan 08's migration tool consumes them. This plan does not delete them — that's destructive without journaled migration. |
| T-17-07-02 | D (DoS) | Tests passing UseAppendLog: false silently no-op after field removal (Go zero-value drops the field) | mitigate | Task 3 step 1 explicitly addresses each call site, decides delete-or-strip per test. Acceptance criteria require grep returns 0 in test files. |
| T-17-07-03 | T (Tampering) | Memory backend's deleted method might leave a nil interface satisfier hole | mitigate | Task 2 step 5 restores the compile-time `var _ local.LocalStore = (*MemoryStore)(nil)` assertion. Acceptance criteria require this grep to succeed. |
</threat_model>

<verification>
- `write.go` deleted
- `FormatStoreKey`, `UseAppendLog`, `ErrAppendLogDisabled`, `ExistsOnDisk`, `DeleteBlockFile`, `TruncateBlockFiles` — zero occurrences whole-repo
- Transitional methods (`IsBlockLocal`, `GetBlockData`, `DeleteAllBlockFiles`, `WriteFromRemote`, `ReadAt`, `WriteAt`, `Flush`, `FlushedBlock`) — STILL PRESENT on FSStore; deleted in Phase 18
- `var _ local.LocalStore = (*FSStore)(nil)` and analogous memory-backend assertion present
- `go build ./...` exits 0
- `go vet ./...` exits 0
- `go test -count=1 -race -timeout 600s ./pkg/blockstore/... ./pkg/controlplane/...` exits 0
</verification>

<success_criteria>
Path-keyed writer file (write.go) deleted; orphan legacy helpers (ExistsOnDisk, DeleteBlockFile, TruncateBlockFiles) deleted; FormatStoreKey deleted; UseAppendLog field + ErrAppendLogDisabled gone; transitional admin methods preserved on FSStore with `Deprecated: removed in Phase 18` godoc; FSStore and memory backends both compile-time satisfy the narrowed LocalStore interface; whole-repo build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-blockstore/17-07-SUMMARY.md`
</output>

