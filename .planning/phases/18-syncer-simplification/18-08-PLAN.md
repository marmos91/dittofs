---
phase: 18-syncer-simplification
plan: 08
type: execute
wave: 6
depends_on: [18-06, 18-07]
files_modified:
  - pkg/blockstore/local/local.go
  - pkg/blockstore/engine/syncer.go
  - pkg/blockstore/engine/syncer_flush_test.go
  - pkg/blockstore/engine/api_blockref_test.go
  - pkg/blockstore/engine/perf_bench_test.go
  - pkg/blockstore/engine/syncer_unit_test.go
  - pkg/blockstore/engine/syncer_crash_test.go
  - pkg/blockstore/engine/syncer_put_error_test.go
  - pkg/blockstore/engine/upload_test.go
  - test/e2e/dedup_objectid_population_test.go
autonomous: true
requirements: [D-11, D-13, D-14, D-18]
must_haves:
  truths:
    - "The 7 TRANSITIONAL-PHASE-18 methods on LocalStore are DELETED (ReadAt, WriteAt, Flush, IsBlockLocal, GetBlockData, WriteFromRemote, DeleteAllBlockFiles)"
    - "FlushedBlock struct DELETED"
    - "TRANSITIONAL-PHASE-18 doc-comment block in local.go DELETED"
    - "Phase 13 dedup conformance tests retargeted onto engine.Flush (D-13); spec-named TestSyncer_Flush_InvokesPostFlushHook RENAMED to a neutral name and rewritten to assert rollup-side ObjectID persistence (D-11)"
    - "All test files that referenced deleted symbols (persistFileBlocksAfterFlush, TrySpeculativeFileLevelDedup public wrapper, uploadOne, FlushedBlock, the 7 transitional methods) build and pass under -race"
    - "Tests that are no longer meaningful in the mirror-loop world (upload_test.go's in-Syncer dedup tests, syncer_unit_test.go's claim/upload tests) DELETED rather than ported"
    - "go test -race ./pkg/blockstore/... passes end-to-end"
    - "Admin/lifecycle methods (Truncate, EvictMemory, SetRetentionPolicy, Stats, ListFiles, Healthcheck, Start, Close, etc.) STAY untouched per D-18"
  artifacts:
    - path: pkg/blockstore/local/local.go
      provides: "LocalStore interface narrowed: 7 transitional methods removed + FlushedBlock removed + ListUnsynced retained (from Plan 04)"
      contains: "ListUnsynced"
    - path: pkg/blockstore/engine/syncer_flush_test.go
      provides: "Tests retargeted onto engine.Flush; the InvokesPostFlushHook test renamed and rewritten"
      contains: "engine.Flush"
  key_links:
    - from: pkg/blockstore/engine/syncer_flush_test.go
      to: pkg/blockstore/engine/engine.go
      via: "Tests call bs.Flush(ctx, payloadID) instead of m.syncer.Flush + m.syncer.TrySpeculativeFileLevelDedup"
      pattern: "bs.Flush"
---

<objective>
Delete the 7 `TRANSITIONAL-PHASE-18` admin methods on `LocalStore` (plus `FlushedBlock` struct and the bridge `Flush` return type), and sweep all Phase 13 + Phase 17 test files that referenced now-deleted symbols. Tests that retain meaning under the mirror-loop world are ported to the new entrypoints; tests that are structurally obsolete are deleted.

Purpose: After Plans 04–07 landed the new surface, this plan removes the bridge code and brings the test base back to green. Each test file is either ported or deleted in a single sweep to avoid neighbor-red CI (D-14).

Output: Narrowed LocalStore interface; ~6 test files ported or deleted; one renamed test (`TestSyncer_Flush_InvokesPostFlushHook` → `TestRollup_CommitChunks_PersistsObjectID` was already created in Plan 05, so this plan removes the old Syncer-side test stub); full `go test -race ./...` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-syncer-simplification/18-CONTEXT.md
@.planning/phases/18-syncer-simplification/18-PATTERNS.md
@.planning/phases/18-syncer-simplification/18-04-SUMMARY.md
@.planning/phases/18-syncer-simplification/18-05-SUMMARY.md
@.planning/phases/18-syncer-simplification/18-06-SUMMARY.md
@.planning/phases/18-syncer-simplification/18-07-SUMMARY.md

@pkg/blockstore/local/local.go

<interfaces>
LocalStore methods being DELETED (lines 154-198, plus FlushedBlock at lines 18-27 and the TRANSITIONAL doc block at lines 139-153):
- ReadAt, WriteAt, Flush (the one that returns []FlushedBlock — distinguish from local.Flush(payloadID) for rollup quiesce), IsBlockLocal, GetBlockData, WriteFromRemote, DeleteAllBlockFiles
- FlushedBlock struct

LocalStore methods being KEPT per D-18:
- Walk, Get, Put, Has, Delete, StoreChunk, Truncate, EvictMemory, SetRetentionPolicy, SetEvictionEnabled, Stats, ListFiles, GetStoredFileSize, Healthcheck, SyncFileBlocks, SyncFileBlocksForFile, Start, Close, DeleteAppendLog, StartRollup, ListUnsynced (added in Plan 04)

The bridge "Flush" return type — there are TWO Flush methods on the LocalStore interface today:
- Flush(ctx, payloadID) — rollup-quiesce, STAYS (consumed by Syncer.Flush after Plan 06)
- Flush(ctx, payloadID) returning []FlushedBlock — transitional, DELETED

Verify the actual signature distinction via grep on local.go before deletion. CONTEXT D-18 explicitly enumerates the 7 transitional methods + Flush + FlushedBlock — read it carefully. If there is only ONE Flush method on LocalStore and it returns []FlushedBlock, then DELETING it is correct AND the Syncer's `m.local.Flush(ctx, payloadID)` call from Plan 06 Task 2 NEEDS a replacement — likely `m.local.SyncFileBlocks(ctx, payloadID)` or `m.local.QuiesceAppendLog(ctx, payloadID)` or whatever rollup-trigger API exists (grep for "rollup" / "quiesce" in pkg/blockstore/local/local.go for candidates).
</interfaces>

@pkg/blockstore/engine/syncer_flush_test.go
@pkg/blockstore/engine/api_blockref_test.go
@pkg/blockstore/engine/perf_bench_test.go
@pkg/blockstore/engine/syncer_unit_test.go
@pkg/blockstore/engine/syncer_crash_test.go
@pkg/blockstore/engine/syncer_put_error_test.go
@pkg/blockstore/engine/upload_test.go
@test/e2e/dedup_objectid_population_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-sweep grep audit + Syncer.local.Flush call-site resolution</name>
  <files>(read-only audit; outputs notes for Tasks 2 + 3)</files>
  <read_first>
    - pkg/blockstore/local/local.go — confirm Flush signature(s); 18-PATTERNS.md section 19 sweep table
    - pkg/blockstore/engine/syncer.go — after Plan 06; find m.local.Flush(ctx, payloadID) call (around the new Flush body) and identify a replacement API if Flush() is being deleted
  </read_first>
  <action>
    Run the master sweep grep from 18-PATTERNS.md section 19 verbatim:
      grep -rn "TrySpeculativeFileLevelDedup\|persistFileBlocksAfterFlush\|FlushedBlock\|IsBlockLocal\|GetBlockData\|WriteFromRemote\|DeleteAllBlockFiles" pkg/ test/

    Capture the full output (paste into the SUMMARY for traceability). Every match falls into one of three buckets:
    (a) Production code outside engine/syncer.go — MUST be 0. If any exist, this plan must refactor them before deletion.
    (b) Test files — enumerated in this plan's files_modified; ported or deleted in Tasks 3-4.
    (c) Comment-only / doc-only references — scrubbed during the corresponding file edit.

    ALSO run:
      grep -rn "ReadAt\b\|WriteAt\b" pkg/blockstore/local/ pkg/blockstore/engine/

    Distinguish: LocalStore.ReadAt/WriteAt (the deletion targets) from any unrelated ReadAt/WriteAt on io.Reader/io.Writer interfaces or other types. The deletion targets are method calls on a LocalStore or *FSStore receiver. Confirm via inspection.

    For the m.local.Flush call site identified in Plan 06 Task 2: pick the replacement API. Options ranked by likelihood:
      - m.local.SyncFileBlocksForFile(ctx, payloadID) — most likely (already in the kept-methods list per D-18)
      - m.local.SyncFileBlocks(ctx) — broader, drains all
      - A new method we add (e.g., QuiesceAppendLog) — last resort, expand scope
    Pick the one whose godoc most closely matches "quiesce any pending append-log writes for this payloadID before iterating ListUnsynced." Document the choice in this task's notes.
  </action>
  <verify>
    <automated>echo "Pre-sweep audit task — manual review required; verification is the resolution choice for m.local.Flush replacement (documented in SUMMARY)."</automated>
  </verify>
  <acceptance_criteria>
    - Grep audit captured + pasted into 18-08-SUMMARY (when written)
    - Every production (non-test) reference to deleted symbols accounted for with a remediation plan
    - Replacement API for m.local.Flush(ctx, payloadID) identified by name and added to Task 2's checklist
  </acceptance_criteria>
  <done>Audit captured; replacement API chosen; Task 2 has a concrete swap target.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 7 TRANSITIONAL-PHASE-18 methods + FlushedBlock + bridge Flush from LocalStore interface; swap Syncer's m.local.Flush call to the replacement API</name>
  <files>pkg/blockstore/local/local.go, pkg/blockstore/local/fs/blockstore_methods.go, pkg/blockstore/engine/syncer.go</files>
  <read_first>
    - pkg/blockstore/local/local.go lines 18-27 (FlushedBlock), 139-153 (TRANSITIONAL doc block), 154-198 (7 methods)
    - pkg/blockstore/local/fs/blockstore_methods.go — any FSStore implementations of the 7 transitional methods that also need deletion
    - pkg/blockstore/local/memory/ (if exists) — second LocalStore implementer that may also have stubs
    - pkg/blockstore/engine/syncer.go — Task 1 identified replacement API call site
  </read_first>
  <action>
    Multi-file coordinated deletion (one commit):

    (a) pkg/blockstore/local/local.go — delete:
        - The TRANSITIONAL-PHASE-18 doc block (lines 139-153 — the "// Grep marker..." comment + any neighbor explanatory comments)
        - All 7 method signatures (ReadAt, WriteAt, Flush returning []FlushedBlock, IsBlockLocal, GetBlockData, WriteFromRemote, DeleteAllBlockFiles)
        - FlushedBlock struct (lines 18-27)
        - The bridge Flush return type — if Flush has TWO declarations and only one returns []FlushedBlock, delete that one; otherwise rename or restructure per Task 1's audit.

    (b) pkg/blockstore/local/fs/blockstore_methods.go — delete FSStore implementations of the same 7 methods. Locate via grep `func (.*) (ReadAt|WriteAt|IsBlockLocal|GetBlockData|WriteFromRemote|DeleteAllBlockFiles)` pkg/blockstore/local/fs/

    (c) pkg/blockstore/local/memory/ if present — same deletions on any MemoryStore implementer

    (d) pkg/blockstore/engine/syncer.go — change m.local.Flush(ctx, payloadID) (call placed in Plan 06 Task 2) to the replacement API identified in Task 1 (likely m.local.SyncFileBlocksForFile(ctx, payloadID)). Update the surrounding error wrap accordingly.

    NO Phase/D-NN references added. Verify the TRANSITIONAL-PHASE-18 grep marker is GONE from local.go after this commit.
  </action>
  <verify>
    <automated>go build ./pkg/blockstore/... &amp;&amp; go vet ./pkg/blockstore/... &amp;&amp; ! grep -q "TRANSITIONAL-PHASE-18" pkg/blockstore/local/local.go &amp;&amp; ! grep -q "type FlushedBlock" pkg/blockstore/local/local.go &amp;&amp; ! grep -qE "^\s*(ReadAt|WriteAt|IsBlockLocal|GetBlockData|WriteFromRemote|DeleteAllBlockFiles)\s*\(" pkg/blockstore/local/local.go</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TRANSITIONAL-PHASE-18" pkg/blockstore/local/local.go returns 0
    - grep -c "type FlushedBlock" pkg/blockstore/local/local.go returns 0
    - The 7 method signatures GONE from the LocalStore interface (grep returns 0 for each)
    - The kept methods (Walk, Get, Put, Has, Delete, StoreChunk, Truncate, EvictMemory, SetRetentionPolicy, SetEvictionEnabled, Stats, ListFiles, GetStoredFileSize, Healthcheck, SyncFileBlocks, SyncFileBlocksForFile, Start, Close, DeleteAppendLog, StartRollup, ListUnsynced) ALL still present — verify each with grep
    - FSStore (and any other LocalStore implementer) NO LONGER defines the 7 methods
    - Syncer's m.local.Flush call site swapped to the replacement
    - go build + vet exit 0 — interface tightened, all implementers + callers consistent
    - No "Phase 18" / "D-NN" references newly introduced
  </acceptance_criteria>
  <done>Interface narrowed; 7 methods + FlushedBlock + bridge Flush deleted; Syncer's quiesce call swapped; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Test-file sweep — port to engine.Flush or delete the structurally obsolete ones</name>
  <files>pkg/blockstore/engine/syncer_flush_test.go, pkg/blockstore/engine/api_blockref_test.go, pkg/blockstore/engine/perf_bench_test.go, pkg/blockstore/engine/syncer_unit_test.go, pkg/blockstore/engine/syncer_crash_test.go, pkg/blockstore/engine/syncer_put_error_test.go, pkg/blockstore/engine/upload_test.go, test/e2e/dedup_objectid_population_test.go</files>
  <read_first>
    - 18-PATTERNS.md section 19 — full sweep table specifying per-file action (port vs delete) and exact symbols touched per test
    - All 8 listed test files (read full files)
    - .planning/phases/18-syncer-simplification/18-09-PLAN.md (next plan) — confirm integration tests live there, so any here-deleted scenarios that need new integration coverage are accounted for
  </read_first>
  <action>
    File-by-file action plan per 18-PATTERNS.md §19:

    (a) pkg/blockstore/engine/syncer_flush_test.go (8 tests):
        - TestSyncer_Flush_InvokesPostFlushHook (line 135) — DELETE; the replacement TestRollup_CommitChunks_PersistsObjectID was created in Plan 05
        - TestSyncer_Flush_PartialQuiesceSkipsHook (194), TestSyncer_Flush_NilCoordinatorIsNoop (221), TestSyncer_Flush_NoBlocksForPayloadIsNoop (254) — RETARGET to call bs.Flush(ctx, payloadID) instead of m.syncer.Flush + dedup wrapper; assertions stay where they make sense, drop assertions on the deleted persist-after-flush observable
        - TestSyncer_Flush_FileLevelDedupHitSkipsUploadPump (372), TestSyncer_Flush_FileLevelDedupMissProceedsToUpload (460), TestSyncer_Flush_FileLevelDedupSkippedWhenObjectIDNonZero (517), TestSyncer_Flush_FileLevelDedupSkippedWhenSomeBlocksRemote (584) — RETARGET to call bs.Flush; the dedup hook now fires inside engine.Flush (per Plan 07 Task 1), so the Hit-skips-upload-pump assertion now means "the inner syncer.Flush is NEVER called when dedup hits" — assert via spy on the Syncer or by checking remote.Walk yields zero new chunks

    (b) pkg/blockstore/engine/api_blockref_test.go lines 215-245 — references to bs.syncer.persistFileBlocksAfterFlush. Retarget to call bs.Flush and rely on the rollup-side ObjectID persistence (Plan 05) producing the assertion's expected manifest state.

    (c) pkg/blockstore/engine/perf_bench_test.go lines 419, 433, 454, 492, 526 — bench calls to f.syncer.persistFileBlocksAfterFlush. Retarget to drive the same effect via f.bs.Flush, OR if the bench measured the persist hook in isolation and that no longer makes sense, DELETE the bench (no perf regression risk because the rollup-side persist is the new locus).

    (d) pkg/blockstore/engine/syncer_unit_test.go — TestClaimBatch_*, TestUploadOne_*, TestRecoverStaleSyncing, TestSyncNow_DrainsAllPendingViaCAS. uploadOne and the in-Syncer recover-stale-syncing logic are structurally obsolete in the mirror-loop world. DELETE this file entirely UNLESS some tests cover the periodic uploader's claimBatch logic that D-16 preserved — port those few only.

    (e) pkg/blockstore/engine/syncer_crash_test.go — TestSyncerCrash_PrePut / BetweenPutAndMeta / PostMeta / RecoveryRequeuesStale. Crash semantics changed under Put-then-Mark (D-07). Plan 09 covers crash-replay end-to-end via integration. DELETE this file; the integration test in Plan 09 subsumes it.

    (f) pkg/blockstore/engine/syncer_put_error_test.go — TestSyncFileBlock_PropagatesPutError. Port to call bs.Flush and assert the mirror loop surfaces Put errors as `"remote put <hash>: %w"`. One small unit test is sufficient.

    (g) pkg/blockstore/engine/upload_test.go — TestUploadOne_Dedup_SingleRowAfterShortCircuit / DonorRefCountIncrementedExactlyOnce / DeleteIdempotent. The in-Syncer dedup short-circuit (upload.go lines 90-127) was deleted in Plan 06 Task 3; these tests have no production target. DELETE the file.

    (h) test/e2e/dedup_objectid_population_test.go lines 9, 196 — godoc references to Syncer.persistFileBlocksAfterFlush. Update the godoc to reference rollup-side ObjectID persistence (the new home per D-10). Logic should still PASS as-is because the E2E doesn't depend on the symbol name.

    For each FILE that is DELETED rather than ported, leave a single-line commit message note in the SUMMARY explaining why. Do NOT leave skeleton stubs.

    NO Phase/D-NN references introduced; the deleted file commits naturally scrub provenance.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; go test -race -count=1 ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - go test -race -count=1 ./pkg/blockstore/... PASSES end-to-end
    - go test -race ./pkg/metadata/... PASSES (regression check on Wave 1+2)
    - go vet ./... exits 0
    - Deleted files truly gone: ls pkg/blockstore/engine/syncer_unit_test.go syncer_crash_test.go upload_test.go returns "No such file" (for whichever were deleted per plan)
    - Ported files mention bs.Flush (not m.syncer.Flush + m.syncer.TrySpec): grep -E "bs\.Flush|engine.*Flush" pkg/blockstore/engine/syncer_flush_test.go matches ≥ 1 per ported test
    - test/e2e/dedup_objectid_population_test.go compiles (e2e build tag): go test -tags=e2e -count=0 ./test/e2e/... (compile-only via -count=0) exits 0
    - No "Phase 18" / "D-NN" references newly added in any test file
  </acceptance_criteria>
  <done>All test files swept; engine package tests green; no orphan references to deleted symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Plan is pure deletion + test sweep; no new attack surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08-01 | Tampering | Deleted method still called by an out-of-tree consumer | accept | DittoFS has no production users (per project rule feedback_no_prod_users_delete_eagerly.md). The interface change is intentional + atomic; no compat shim. |
| T-18-08-02 | Repudiation | A retained test still implicitly depends on a deleted symbol via interface dispatch | mitigate | go build ./... gate at Task 3 verify; grep audit in Task 1 catches stragglers. |
| T-18-08-03 | Denial of Service | Test deletion hides a regression class that the new integration test in Plan 09 does not cover | mitigate | Each DELETED file in Task 3 has an explicit rationale in the SUMMARY; Plan 09's integration test catalogue is the systematic replacement for the crash + happy-path coverage from the deleted syncer_unit / syncer_crash files. |
</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test -race -count=1 ./pkg/... PASSES (full repository excluding e2e)
- ls pkg/blockstore/engine/ — confirm deleted files truly gone
- grep -rn "TRANSITIONAL-PHASE-18\|FlushedBlock\|persistFileBlocksAfterFlush" pkg/ test/ returns ZERO matches (full repo sweep)
- grep -rn "func .* (ReadAt|WriteAt|IsBlockLocal|GetBlockData|WriteFromRemote|DeleteAllBlockFiles)\b" pkg/blockstore/local/ returns ZERO matches for those exact method names on LocalStore implementers (be careful not to delete io.ReaderAt/io.WriterAt implementations on other types)
</verification>

<success_criteria>
- LocalStore interface tightened: 7 transitional methods + FlushedBlock + bridge Flush gone
- Phase 13 dedup conformance tests retargeted onto engine.Flush
- TestSyncer_Flush_InvokesPostFlushHook deleted (its replacement landed in Plan 05)
- Structurally-obsolete tests (uploadOne-centric, in-Syncer crash-recovery) deleted, not ported
- All pkg/blockstore tests green under -race
- No orphan references to deleted symbols anywhere in the repo
</success_criteria>

<output>
After completion, create .planning/phases/18-syncer-simplification/18-08-SUMMARY.md including the Task 1 grep audit verbatim, the replacement-API decision rationale, and the per-file PORT-vs-DELETE rationale list.
</output>
