---
phase: 19-write-path-ram-optimizations
plan: 10
type: execute
wave: 4
depends_on: [19-06, 19-07]
files_modified:
  - pkg/config/config.go
  - pkg/config/syncer.go
  - pkg/blockstore/engine/syncer.go
  - pkg/blockstore/local/fs/appendlog.go
  - pkg/blockstore/doc.go
autonomous: true
requirements: [D-01, D-18, D-23, D-24, D-25, D-26]
must_haves:
  truths:
    - "D-23 ClaimBatchSize cycle CLOSED: SyncerConfig.ClaimBatchSize field deleted from pkg/config/syncer.go (or config.go); ApplyDefaults branch deleted; Validate branch deleted; UploadConcurrency <= ClaimBatchSize cross-validation deleted; any _test.go that asserts the parsed value deleted"
    - "D-24 dead-method audit complete: pkg/blockstore/local/fs/*.go scanned for methods with zero in-tree callers post-Wave 2; audit result documented in SUMMARY.md (audit-finds-nothing is acceptable per CONTEXT.md)"
    - "D-25 TRANSITIONAL marker audit: every TRANSITIONAL-NEXT-MILESTONE: marker in pkg/blockstore/ either RESOLVED (deleted alongside the addressed change) or UPDATED with a concrete target milestone (e.g., TRANSITIONAL-V0.17:) where one is known"
    - "D-26 v0.17+ anchors present: appendlog.go gains the tmpfs spill TRANSITIONAL marker (the other four anchors landed in Plans 06 + 07: appendwrite O_DIRECT, chunkstore pinned-hot-tail + zstd, cache.go cold-cache prefetch)"
    - "pkg/blockstore/doc.go updated: convention table reflects D-23 cycle closure; D-25 audit summary noted; deprecation cycles preserved (Phase 18 D-18 admin-superset intentional)"
    - "D-01 mega-PR readiness: every commit in the Wave 1→2→3→4 sequence keeps go build + go vet + go test green; no flag-gated half-states"
  artifacts:
    - path: pkg/config/syncer.go (or wherever SyncerConfig lives)
      provides: "ClaimBatchSize field absent post-edit"
      contains: "SyncerConfig"
    - path: pkg/blockstore/local/fs/appendlog.go
      provides: "TRANSITIONAL-NEXT-MILESTONE: tmpfs spill marker"
      contains: "TRANSITIONAL-NEXT-MILESTONE: tmpfs spill"
    - path: pkg/blockstore/doc.go
      provides: "Updated convention table + D-23 cycle closure note"
      contains: "Phase 19"
  key_links:
    - from: pkg/config/{config,syncer}.go
      to: pkg/blockstore/engine/syncer.go
      via: "ClaimBatchSize field removal — consumer must also drop the read"
      pattern: "ClaimBatchSize"
---

<objective>
Cleanup sweeps that close Phase 19's housekeeping:

1. **D-23** — Delete `SyncerConfig.ClaimBatchSize` field + defaults + Validate + test assertions. Closes Phase 18 D-16 deprecation cycle.
2. **D-24** — Audit `pkg/blockstore/local/fs/*.go` for methods whose only callers were Phase 18 transitional code paths now deleted. Remove any with zero in-tree callers. Audit-finds-nothing is acceptable; the audit itself is the deliverable.
3. **D-25** — Grep + resolve every `TRANSITIONAL-NEXT-MILESTONE:` marker across `pkg/blockstore/`. Resolved markers deleted; deferred markers updated with concrete target milestone where known.
4. **D-26** — Add the tmpfs spill TRANSITIONAL marker in `appendlog.go` (the other four D-26 anchors landed in Plans 06 + 07 — appendwrite O_DIRECT, chunkstore pinned-hot-tail + zstd, cache.go cold-cache prefetch).
5. **doc.go** — Update the convention table to reflect D-23 cycle closure + D-25 audit results.

Purpose: A mega-PR (D-01) with both feature work AND clean inherited debt + new v0.17+ pointers is more reviewable than a follow-up cleanup PR. Plan 10 is the final Wave — partially parallelizable with Wave 2/3, but committed at the END of the staged commit order so the audit (D-24) can see the post-Wave 2 caller landscape.

Output: ClaimBatchSize fully gone; D-26 5/5 anchors present; D-25 marker audit results documented; doc.go reflects the cycle closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md
@.planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-23 — Delete SyncerConfig.ClaimBatchSize</name>
  <files>pkg/config/syncer.go, pkg/config/config.go, pkg/blockstore/engine/syncer.go, pkg/config/init_test.go</files>
  <read_first>
    - pkg/config/config.go around lines 88-142 (SyncerConfig struct + ApplyDefaults + Validate triplet)
    - pkg/config/syncer.go if it exists (PATTERNS.md mentions pkg/config/syncer.go alongside config.go — confirm actual location of ClaimBatchSize via grep)
    - pkg/blockstore/engine/syncer.go (consumer side — grep for `ClaimBatchSize` to find any reads of the field)
    - pkg/config/init_test.go (test assertions on the field's parsed value — D-23 deletion candidate)
    - pkg/config/defaults.go (umbrella ApplyDefaults — ensure no stale assignment lingers)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("SyncerConfig.ClaimBatchSize (DELETED — D-23)" section)
  </read_first>
  <action>
    Steps (perform in order to keep build green commit-to-commit per D-01):

    1. `grep -rn "ClaimBatchSize" .` — enumerate ALL references across the repo. Expected sites:
       - Field declaration in SyncerConfig.
       - ApplyDefaults branch (e.g., `if c.ClaimBatchSize <= 0 { c.ClaimBatchSize = 32 }`).
       - Validate branch (e.g., `if c.ClaimBatchSize <= 0 { return ... }`).
       - Possibly an `UploadConcurrency <= ClaimBatchSize` cross-validation.
       - Read sites in pkg/blockstore/engine/syncer.go (consumers; if any).
       - Test assertions in pkg/config/init_test.go (and possibly other test files).
       - Any documentation: docs/CONFIGURATION.md (if it documents the knob).

    2. Delete consumer reads FIRST (pkg/blockstore/engine/syncer.go). If a consumer reads the field for any reason post-Phase-18, the deletion needs analysis — but per Phase 18 D-16 the field has been deprecated since Phase 18 with no consumer reads remaining. Verify this assumption.

    3. Delete the field from SyncerConfig.

    4. Delete the ApplyDefaults branch.

    5. Delete the Validate branch + any cross-validation involving the field.

    6. Delete the test assertions in init_test.go (and other test files).

    7. Update docs/CONFIGURATION.md if it documents the knob (PATTERNS.md does not flag this explicitly but standard cleanup hygiene).

    8. Run `go build ./... && go vet ./... && go test ./...` — must exit 0.

    Verify with grep: `grep -rn "ClaimBatchSize" .` should return ZERO matches across the entire repo (or only matches in .planning/ historical docs — that's acceptable, the planning archives preserve context).

    Note: D-22b PATTERNS.md mentions that "The mapstructure/yaml tag 'claim_batch_size' becomes an unknown key. Viper silently ignores unknown keys by default — verify with pkg/config/init_test.go that no test explicitly asserts the key's presence in a parsed yaml." Add a one-line acceptance: confirm no test fixture YAML contains `claim_batch_size:`. If a fixture does, scrub the line from the fixture so the YAML round-trip doesn't carry dead state.
  </action>
  <verify>
    <automated>grep -rn "ClaimBatchSize" --include="*.go" . | grep -v ".planning/" | wc -l; grep -rn "claim_batch_size" --include="*.go" --include="*.yaml" --include="*.yml" --include="*.md" . | grep -v ".planning/" | grep -v docs/CONFIGURATION.md; go build ./... 2>&1 | tail -20; go vet ./... 2>&1 | tail -20; go test ./pkg/config/... -count=1 -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep -rn "ClaimBatchSize" --include="*.go" .` (excluding .planning/) returns ZERO matches
    - `grep -rn "claim_batch_size"` across .go + .yaml + .yml + .md (excluding .planning/) returns ZERO matches
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `go test ./pkg/config/...` PASS
    - docs/CONFIGURATION.md (if it documented the knob) is updated
  </done>
</task>

<task type="auto">
  <name>Task 2: D-24 — Dead LocalStore admin-method audit</name>
  <files>pkg/blockstore/local/fs/*.go (audit only — modifications conditional on findings)</files>
  <read_first>
    - Phase 18 D-18 from .planning/phases/18-syncer-simplification/18-CONTEXT.md (the admin-superset that intentionally STAYS — Truncate, EvictMemory, SetRetentionPolicy, Stats, ListFiles, etc.)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-24 — "if the audit finds nothing, the sweep is a no-op commit; acceptable")
  </read_first>
  <action>
    Audit procedure:

    1. List public methods on `*FSStore` (LocalStore is FSStore in v0.16.0+ — confirm via grep):
       ```bash
       grep -E "^func \(bc \*FSStore\)" pkg/blockstore/local/fs/*.go | grep -v "_test.go"
       ```
    2. For each method, run `grep -rn "\\.MethodName(" --include="*.go" .` (or equivalent) to find callers.
    3. Classify:
       - **STAYS** (Phase 18 D-18 admin-superset): Truncate, EvictMemory, SetRetentionPolicy, Stats, ListFiles, etc. Do NOT delete even if no current callers — these are explicit admin surface.
       - **DELETE candidate**: zero callers in non-test code AND not on the admin-superset list AND can be argued as Phase 18 transitional.
    4. For each DELETE candidate, perform deletion + run `go build ./...` to verify nothing breaks.

    Document the audit result in `.planning/phases/19-write-path-ram-optimizations/19-10-SUMMARY.md` with a table:
    - Method name → caller count → classification → action taken.

    If the audit finds zero DELETE candidates: this task's deliverable is the SUMMARY.md table itself (no code change). CONTEXT.md explicitly authorizes this: "the audit itself is the deliverable."
  </action>
  <verify>
    <automated>echo "D-24 audit deliverable is the SUMMARY.md table — no automated grep gate possible since the audit may legitimately be a no-op. Verifier reads SUMMARY.md after the plan completes."; grep -E "^func \(bc \*FSStore\)" pkg/blockstore/local/fs/*.go | grep -v "_test.go" | wc -l</automated>
  </verify>
  <done>
    - 19-10-SUMMARY.md (this plan's SUMMARY) contains an audit table for every public FSStore method
    - Any DELETE actions justified in the table + executed in the same commit
    - `go build ./...` still exits 0 after any deletions
  </done>
</task>

<task type="auto">
  <name>Task 3: D-25 — TRANSITIONAL-NEXT-MILESTONE marker audit + D-26 tmpfs spill anchor</name>
  <files>pkg/blockstore/local/fs/appendlog.go, all of pkg/blockstore/** (audit/edit only)</files>
  <read_first>
    - pkg/blockstore/doc.go around lines 180-205 (TRANSITIONAL convention)
    - Plans 06 + 07 SUMMARY (verify the 4 D-26 anchors they were responsible for landed: appendwrite.go O_DIRECT, chunkstore.go pinned-hot-tail, chunkstore.go zstd, cache.go cold-cache prefetch)
    - pkg/blockstore/local/fs/appendlog.go (Plan 10's responsibility — the tmpfs spill anchor)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-25 audit policy + D-26 5-anchor list)
  </read_first>
  <action>
    Three sub-steps:

    **A. D-25 audit:**
    1. `grep -rn "TRANSITIONAL-NEXT-MILESTONE:" pkg/blockstore/` — enumerate all markers.
    2. For each marker, determine:
       - **RESOLVED by Phase 19?** Delete the marker comment line (or block) alongside the change that resolved it. The most obvious candidate: the `TRANSITIONAL-NEXT-MILESTONE: claimBatch deprecation` marker (if it exists), which Phase 19 Task 1 (D-23) just resolved by deleting ClaimBatchSize. Delete the marker.
       - **STILL DEFERRED with known target milestone?** Update the marker text to reference the target (e.g., `TRANSITIONAL-V0.17:` if v0.17 will address it). The existing `TRANSITIONAL-NEXT-MILESTONE` convention is preserved per Phase 18 D-19; this audit just clarifies which markers have concrete targets vs which stay generic.
       - **STILL DEFERRED with no known target?** Leave as-is (it's the generic forward-pointer the convention encodes).

    Document each marker's disposition in SUMMARY.md.

    **B. D-26 tmpfs spill anchor:**
    Add to pkg/blockstore/local/fs/appendlog.go near the log-overflow site (where the appendlog file would spill from in-memory buffering to disk — search for the buffering threshold logic):

    ```go
    // TRANSITIONAL-NEXT-MILESTONE: tmpfs spill (see #519 "Deferred to v0.17+").
    // When tmpfs spill lands, the in-RAM appendlog buffer will spill to a
    // memory-backed temp tier (typically /dev/shm) before disk, eliminating
    // the SSD wear cost for log churn under burst write workloads. The
    // current implementation spills directly to the on-disk log fd; tmpfs
    // spill inserts an intermediate tier.
    ```

    **C. doc.go update:**
    Update pkg/blockstore/doc.go to reflect:
    - D-23 cycle closure: the convention table no longer mentions the ClaimBatchSize deprecation cycle (delete the row).
    - Add a one-liner about the Phase 19 marker audit results (or reference the SUMMARY.md if too detailed for doc.go).

    Verify the five D-26 anchors are all present after this plan completes:
    ```bash
    grep -n "TRANSITIONAL-NEXT-MILESTONE: pinned hot-tail RAM" pkg/blockstore/local/fs/chunkstore.go
    grep -n "TRANSITIONAL-NEXT-MILESTONE: zstd compression" pkg/blockstore/local/fs/chunkstore.go
    grep -n "TRANSITIONAL-NEXT-MILESTONE: tmpfs spill" pkg/blockstore/local/fs/appendlog.go
    grep -n "TRANSITIONAL-NEXT-MILESTONE: O_DIRECT" pkg/blockstore/local/fs/appendwrite.go
    grep -n "TRANSITIONAL-NEXT-MILESTONE: cold-cache prefetch" pkg/blockstore/engine/cache.go
    ```
    All five MUST return at least one match.
  </action>
  <verify>
    <automated>grep -c "TRANSITIONAL-NEXT-MILESTONE: pinned hot-tail RAM" pkg/blockstore/local/fs/chunkstore.go; grep -c "TRANSITIONAL-NEXT-MILESTONE: zstd compression" pkg/blockstore/local/fs/chunkstore.go; grep -c "TRANSITIONAL-NEXT-MILESTONE: tmpfs spill" pkg/blockstore/local/fs/appendlog.go; grep -c "TRANSITIONAL-NEXT-MILESTONE: O_DIRECT" pkg/blockstore/local/fs/appendwrite.go; grep -c "TRANSITIONAL-NEXT-MILESTONE: cold-cache prefetch" pkg/blockstore/engine/cache.go; grep -rn "ClaimBatchSize" --include="*.go" pkg/ | wc -l; go build ./... 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 5 D-26 anchors present (5 grep gates return at least 1 each)
    - D-25 audit results documented in SUMMARY.md
    - doc.go updated (D-23 row removed; Phase 19 audit summary noted)
    - Phase 19 changes leave `go build ./...` green
    - `grep -rn "ClaimBatchSize" --include="*.go" pkg/` returns 0 — D-23 closure verified
  </done>
</task>

</tasks>

<verification>
- `grep -rn "ClaimBatchSize" --include="*.go" .` (excluding .planning/) returns 0 (D-23 cycle closed)
- All 5 D-26 TRANSITIONAL-NEXT-MILESTONE anchors present
- D-24 audit table in SUMMARY.md
- D-25 audit dispositions in SUMMARY.md
- `go build ./... && go vet ./... && go test ./...` exits 0 (D-01 mega-PR readiness — every commit through Wave 1→2→3→4 keeps the tree buildable)
</verification>

<success_criteria>
1. D-23 ClaimBatchSize deletion complete — grep returns zero
2. D-24 admin-method audit documented (DELETE if findings; SUMMARY.md table either way)
3. D-25 TRANSITIONAL marker dispositions documented; resolved markers deleted; deferred markers retain or get concrete targets
4. D-26 5/5 v0.17+ anchors present across chunkstore.go, appendlog.go, appendwrite.go, cache.go
5. pkg/blockstore/doc.go reflects Phase 19 cycle closure + marker audit
6. D-01 mega-PR ready: all Phase 19 plans together yield a green tree at every internal commit boundary
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-path-ram-optimizations/19-10-SUMMARY.md` capturing:
- Full table of D-24 audit (method name → caller count → STAYS/DELETE → action)
- Full table of D-25 audit (marker location → disposition → resulting comment text)
- Confirmation of D-26 5-anchor presence (the grep gates' outputs)
- Phase 19 wrap: total LoC delta (informational; comparing pre-Phase-19 develop vs the final mega-PR), confirmation that D-21 ≤1.00 was met (cross-reference Plan 09 SUMMARY), and that all 27 D-NN decisions have been covered (cross-reference each plan's `requirements:` field).
</output>
</content>
</invoke>