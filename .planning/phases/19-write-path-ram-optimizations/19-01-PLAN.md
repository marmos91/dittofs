---
phase: 19-write-path-ram-optimizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/blockstore/store.go
  - pkg/metadata/errors.go
  - pkg/metadata/storetest/file_block_ops.go
autonomous: true
requirements: [D-04, D-21, D-22b, D-27]
must_haves:
  truths:
    - "D-04: FileBlockStore.AddRef(ctx, hash, payloadID, blockRef) error is on the FileBlockStore interface contract"
    - "D-22b: META-03 narrowed-to-6 comment in pkg/blockstore/store.go updated to 7 methods (AddRef joins existing 6)"
    - "D-04: ErrUnknownHash sentinel exists in pkg/metadata/errors.go and is returned by AddRef when the hash row is absent"
    - "D-21: pkg/metadata/storetest exposes new conformance scenarios that all 3 backends will exercise (AddRef_ExistingHash_BumpsRefCount, AddRef_MissingHash_ReturnsErrUnknownHash, AddRef_Concurrent_With_DecrementRefCountCascade)"
    - "D-27: AddRef godoc states explicitly that on success RefCount increments only and BlockState is UNCHANGED (no Pending→Syncing→Remote transition; no new row)"
  artifacts:
    - path: pkg/blockstore/store.go
      provides: "FileBlockStore interface with AddRef method"
      contains: "AddRef(ctx context.Context, hash"
    - path: pkg/metadata/errors.go
      provides: "ErrUnknownHash sentinel"
      contains: "ErrUnknownHash"
    - path: pkg/metadata/storetest/file_block_ops.go
      provides: "AddRef conformance subtests inside runFileBlockOpsTests"
      contains: "AddRef_ExistingHash_BumpsRefCount"
  key_links:
    - from: pkg/blockstore/store.go (FileBlockStore interface)
      to: pkg/metadata/errors.go (ErrUnknownHash sentinel)
      via: "AddRef godoc cross-reference + return-type contract"
      pattern: "AddRef.*ErrUnknownHash"
---

<objective>
Land the additive `FileBlockStore.AddRef` surface and its sentinel error on the FileBlockStore interface, plus the conformance scenarios in `pkg/metadata/storetest/`. No backend implementations yet — those come in Plan 02. This plan is interface-first per the Plan Set's interface-first task ordering: downstream plans (02 backend impls, 05 Opt 1 wire-in) build against this contract.

Purpose: Define the surface contract that Opt 1 (dedup LRU hit path, Plan 05) and the backend implementations (Plan 02) will both consume. STATE-01..03 invariant preservation (D-27) is encoded in the godoc so backend implementers cannot miss it.

Output: `FileBlockStore` interface gains `AddRef`; `ErrUnknownHash` lives in `pkg/metadata/errors.go`; `pkg/metadata/storetest/file_block_ops.go` gains three new subtests that will fail compilation until Plan 02 implements `AddRef` on each backend (acceptable Wave 1 intermediate — see D-01 atomic-merge note below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md
@.planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing FileBlockStore interface excerpt (pkg/blockstore/store.go around lines 60-95 — read in full before editing):

```go
// FileBlockStore is the 6-method narrowed interface (META-03, Phase 12).
type FileBlockStore interface {
    GetByHash(ctx context.Context, hash ContentHash) (*FileBlock, error)
    Put(ctx context.Context, block *FileBlock) error
    Delete(ctx context.Context, id string) error
    IncrementRefCount(ctx context.Context, id string) error
    DecrementRefCount(ctx context.Context, id string) (uint32, error)
    ListPending(ctx context.Context, limit int) ([]*FileBlock, error)
}
```

Existing sentinel error idiom in pkg/metadata/errors.go:

```go
var ErrFileBlockNotFound = errors.New("metadata: file block not found")
```

Existing conformance shape in pkg/metadata/storetest/file_block_ops.go (around line 39 — read in full):

```go
func runFileBlockOpsTests(t *testing.T, factory func(t *testing.T) FileBlockStoreFactory) {
    t.Run("Put_Get_RoundTrip", func(t *testing.T) { ... })
    t.Run("IncrementRefCount_BumpsCount", func(t *testing.T) { ... })
    t.Run("DecrementRefCount_DecrementsAndReturnsNew", func(t *testing.T) { ... })
    // ... more subtests ...
}
```

Critical: D-01 atomic-merge constraint allows internal commit ordering inside the mega-PR, but each commit must keep `go build` green. Plan 01 + Plan 02 together must land as a contiguous commit pair (interface first, then impls). The storetest subtests added here will fail at the `_ = fbs.AddRef(...)` reference site UNTIL Plan 02 lands — so Plan 02 MUST be the immediate next commit. This is the documented intermediate state, mirroring the precedent in Phase 16 Plan 03 SUMMARY (Task 1 leaves perf_bench_unix_test.go briefly broken until Task 2 deletes it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FileBlockStore interface with AddRef + add ErrUnknownHash sentinel</name>
  <files>pkg/blockstore/store.go, pkg/metadata/errors.go</files>
  <read_first>
    - pkg/blockstore/store.go (current FileBlockStore interface + META-03 6-method comment block at lines 10-11)
    - pkg/metadata/errors.go (existing sentinel error declarations including ErrFileBlockNotFound)
    - pkg/metadata/rollup_store.go (canonical interface+godoc+sentinel-error idiom analog — D-22b PATTERNS.md call-out)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-04, D-22b, D-27 specifics)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/metadata/file_block_store.go" section)
  </read_first>
  <action>
    In pkg/blockstore/store.go:
    1. Add new method to FileBlockStore interface immediately after DecrementRefCount:
       `AddRef(ctx context.Context, hash ContentHash, payloadID string, blockRef BlockRef) error`
    2. Godoc on AddRef must call out (per D-04 + D-27):
       - "Atomically increments RefCount on the FileBlock row indexed by hash."
       - "On success, RefCount is incremented; BlockState is UNCHANGED (no Pending→Syncing→Remote transition; no new row created)."
       - "Returns `metadata.ErrUnknownHash` if no FileBlock row exists for the given hash. Callers (see pkg/blockstore/local/fs/rollup.go LRU hit path) MUST fall back to the full Put path on this sentinel."
       - "Atomicity matches IncrementRefCount's contract. Multi-row-per-hash tolerance (Phase 11 IN-3-02): AddRef MAY operate on any one matching row."
       - "STATE-01..03 invariant preserved (D-27): AddRef references an existing block; the LRU hit path never creates a new block, so the 'every block must visit Pending' rule is not contradicted."
    3. Update the META-03 comment block at the top of the FileBlockStore interface (currently states "Narrowed to 6 methods in Phase 12"): change `6` to `7` and append `(AddRef joined in Phase 19 — see CONTEXT.md D-22b).`
    
    In pkg/metadata/errors.go:
    1. Add `var ErrUnknownHash = errors.New("metadata: hash not yet present in FileBlockStore (AddRef called before Put)")` adjacent to existing `ErrFileBlockNotFound` declaration.
    2. Match the existing sentinel ordering convention (alphabetical or by phase — read the file to confirm; preserve whichever convention is present).
  </action>
  <verify>
    <automated>go vet ./pkg/blockstore/... ./pkg/metadata/... 2>&1 | tee /tmp/p19-01-task1.out; grep -c "ErrUnknownHash" pkg/metadata/errors.go; grep -c "AddRef(ctx context.Context, hash" pkg/blockstore/store.go</automated>
  </verify>
  <done>
    - `pkg/blockstore/store.go` contains `AddRef(ctx context.Context, hash ContentHash, payloadID string, blockRef BlockRef) error` exactly once in the FileBlockStore interface body
    - `pkg/blockstore/store.go` META-03 comment now reads "7 methods"
    - `pkg/metadata/errors.go` contains `ErrUnknownHash = errors.New(` exactly once
    - AddRef godoc explicitly mentions: "RefCount", "UNCHANGED", "ErrUnknownHash", "STATE-01..03"
    - `go vet ./pkg/blockstore/... ./pkg/metadata/...` exits 0 (the interface extension does NOT break vet; backends will fail at impl-check, but that's a build error in Plan 02 territory, not a vet error here on the interface package)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AddRef conformance scenarios to storetest</name>
  <files>pkg/metadata/storetest/file_block_ops.go</files>
  <read_first>
    - pkg/metadata/storetest/file_block_ops.go (read the entire `runFileBlockOpsTests` function — exact subtest harness shape, factory injection convention, t.Helper() discipline)
    - pkg/metadata/storetest/file_block_ops.go specifically lines 953-1025 (`testTx_IncrementRefCount_RollsBack` — concurrent-pattern analog per PATTERNS.md)
    - pkg/metadata/rollup_store_suite.go (canonical RunRollupStoreSuite shape, especially `ConcurrentMonotone` at line 153 — analog for AddRef_Concurrent_With_DecrementRefCountCascade)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/metadata/storetest/file_block_ops.go" section — exact scenario list)
  </read_first>
  <action>
    Add three new t.Run subtests inside `runFileBlockOpsTests` in pkg/metadata/storetest/file_block_ops.go, following the existing harness shape (factory injection, t.Helper(), context.Background() usage). Place them after the existing `DecrementRefCount_*` subtests for readability — exact placement is planner's call, but they MUST be inside `runFileBlockOpsTests` so they flow to every backend automatically (per D-21).

    Subtest 1 — `t.Run("AddRef_ExistingHash_BumpsRefCount", func(t *testing.T) {...})`:
    1. Seed a single FileBlock via factory's Put with known hash H, RefCount=1, BlockState=Remote (use existing helper if present, otherwise hand-construct mirroring the Put_Get_RoundTrip subtest).
    2. Call `fbs.AddRef(ctx, H, "payload-1", blockstore.BlockRef{Hash: H, Offset: 0, Size: 4096})`.
    3. Assert err == nil.
    4. Call `fbs.GetByHash(ctx, H)`; assert resulting FileBlock.RefCount == 2.
    5. Assert resulting FileBlock.BlockState == BlockStateRemote (D-27 — STATE UNCHANGED).

    Subtest 2 — `t.Run("AddRef_MissingHash_ReturnsErrUnknownHash", func(t *testing.T) {...})`:
    1. Compute a fresh hash H that has NOT been Put.
    2. Call `fbs.AddRef(ctx, H, "payload-2", blockstore.BlockRef{Hash: H, Offset: 0, Size: 1024})`.
    3. Assert `errors.Is(err, metadata.ErrUnknownHash)`.
    4. Call `fbs.GetByHash(ctx, H)`; assert returns `(nil, nil)` (no error, no row materialized — D-04: "no row created").

    Subtest 3 — `t.Run("AddRef_Concurrent_With_DecrementRefCountCascade", func(t *testing.T) {...})`:
    1. Seed a single FileBlock at RefCount=10 (high enough that concurrent decrements cannot underflow before the test ends).
    2. Spawn N=16 goroutines via sync.WaitGroup: 8 call AddRef(ctx, H, ...) on the same H; 8 call DecrementRefCount(ctx, id) on the same id (look up id via GetByHash first under the test's setup goroutine).
    3. Wait on the WaitGroup.
    4. Assert final RefCount via GetByHash equals `10 + 8 - 8 = 10` (D-04 TOCTOU-free serialization invariant).
    5. Assert the row was NOT orphan-deleted (GetByHash returns a non-nil FileBlock).
    6. Mirror the ConcurrentMonotone subtest's t.Parallel() + race-detector compatibility (rollup_store_suite.go:153 analog).

    All three subtests use `t.Helper()` per the existing convention. Use the `metadata` package import alias already in scope. Reference `metadata.ErrUnknownHash` (sentinel from Task 1) and `blockstore.BlockRef` / `blockstore.ContentHash` types via the existing imports.

    Note (D-01 documented intermediate): These subtests compile-reference `fbs.AddRef(...)`. After this commit lands, the storetest package will not compile UNTIL Plan 02 implements AddRef on each backend type that satisfies FileBlockStore. This is the documented inter-commit state — Plan 02 is the immediate next commit. To make the commit individually buildable for `git bisect`, the subtests use the interface-typed `fbs` (not a backend-typed receiver), so the package compiles as long as the storetest package itself does not instantiate a concrete backend. **Verify: storetest package compiles standalone (`go build ./pkg/metadata/storetest/...`) even with the new subtests referencing the new interface method.** If standalone build fails, fall back to a `// AddRef tests added in Phase 19 Plan 01; backend impls land in Plan 02.` comment block guarding the subtest bodies under `if false { ... }` or `t.Skip` — but interface dispatch over `fbs FileBlockStore` should compile without backend cooperation.
  </action>
  <verify>
    <automated>go build ./pkg/metadata/storetest/... 2>&1 | tee /tmp/p19-01-task2.out; grep -c "AddRef_ExistingHash_BumpsRefCount" pkg/metadata/storetest/file_block_ops.go; grep -c "AddRef_MissingHash_ReturnsErrUnknownHash" pkg/metadata/storetest/file_block_ops.go; grep -c "AddRef_Concurrent_With_DecrementRefCountCascade" pkg/metadata/storetest/file_block_ops.go</automated>
  </verify>
  <done>
    - `pkg/metadata/storetest/file_block_ops.go` contains all three subtest names exactly once each
    - Subtest 1 asserts `RefCount == 2` AND `BlockState == BlockStateRemote` (D-27 — state preservation explicit)
    - Subtest 2 asserts `errors.Is(err, metadata.ErrUnknownHash)` AND `GetByHash` returns nil row
    - Subtest 3 asserts final RefCount == 10 AND row still present (TOCTOU-free)
    - `go build ./pkg/metadata/storetest/...` exits 0 (interface-typed dispatch compiles standalone)
    - Backend-package builds (`go build ./pkg/metadata/memory/...` etc.) will FAIL with missing method error — this is the documented intermediate state, resolved by Plan 02. Do NOT attempt to fix backend builds in this plan.
  </done>
</task>

</tasks>

<verification>
- `go vet ./pkg/blockstore/... ./pkg/metadata/storetest/...` exits 0
- `go build ./pkg/blockstore/... ./pkg/metadata/storetest/...` exits 0 (interface-only consumers compile)
- `go build ./...` is EXPECTED TO FAIL on backend packages (memory/badger/postgres) with "does not implement FileBlockStore (missing AddRef method)". This is the Wave 1 intermediate state. Plan 02 (next commit in the mega-PR commit order) resolves it.
- `grep -n "AddRef(ctx context.Context, hash" pkg/blockstore/store.go` returns exactly one line
- `grep -n "ErrUnknownHash" pkg/metadata/errors.go` returns at least one line
- All three new subtest names grep-findable in `pkg/metadata/storetest/file_block_ops.go`
</verification>

<success_criteria>
1. FileBlockStore interface in pkg/blockstore/store.go has `AddRef` method with full godoc covering RefCount-only / state-unchanged / ErrUnknownHash sentinel / atomicity / multi-row-per-hash tolerance
2. META-03 comment in pkg/blockstore/store.go updated from "6 methods" to "7 methods"
3. `metadata.ErrUnknownHash` sentinel exists in pkg/metadata/errors.go matching the existing sentinel idiom
4. Three conformance subtests added inside `runFileBlockOpsTests` covering: existing-hash RefCount-bumps + state-preserved, missing-hash sentinel + no-row-created, concurrent AddRef vs Decrement cascade with no negative RefCount and no orphan
5. storetest package compiles standalone (`go build ./pkg/metadata/storetest/...` exits 0)
6. Backend packages fail to compile until Plan 02 — DOCUMENTED INTERMEDIATE
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-path-ram-optimizations/19-01-SUMMARY.md` capturing:
- Exact AddRef signature shipped (in case planner debated `BlockRef` vs `BlockRef*` etc.)
- Exact sentinel name shipped (`ErrUnknownHash` vs the discussed alternative `ErrHashNotFound`)
- Whether the concurrent-cascade subtest seed RefCount differs from the 10 suggested above
- Confirmation that Plan 02 is the immediate next commit (interface→impls ordering)
</output>
</content>
</invoke>