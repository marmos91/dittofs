---
phase: 19-write-path-ram-optimizations
plan: 09
type: execute
wave: 3
depends_on: [19-05, 19-06, 19-07, 19-08]
files_modified:
  - pkg/blockstore/engine/cache_populated_on_rollup_test.go
  - pkg/blockstore/engine/small_file_eager_dedup_test.go
  - pkg/blockstore/local/fs/rollup_idempotent_dedup_bench_test.go
  - pkg/blockstore/local/fs/appendwrite_group_commit_bench_test.go
  - internal/bench/phase19_test.go
autonomous: true
requirements: [D-17, D-19, D-20, D-21]
must_haves:
  truths:
    - "D-17 hard-gate correctness: TestCache_PopulatedOnRollupComplete + TestSmallFileEagerDedup_BSCAS06 both PASS — these block merge if they fail"
    - "D-17 yellow-flag perf: BenchmarkRandWriteCAS_IdempotentBytes (Opt 1 Put count ≤ N for K identical writes) + BenchmarkAppendWrite_GroupCommit (Opt 2 fsync count ≤ writes/groupSize) report ratios without blocking merge"
    - "D-19 D-21 tightened: internal/bench/phase19_test.go aggregate runner emits RandWrite warm-cache ratio vs Phase 11 baseline ≤1.00 (was ≤1.02)"
    - "D-20 file placement: 4 new bench/test files at the exact paths CONTEXT.md specifies + the aggregate runner"
    - "Bench-on-race policy: yellow-flag benches honor Phase 11's raceEnabled constant pattern (skip under -race so D-21 doesn't false-fail on race-detector overhead)"
  artifacts:
    - path: pkg/blockstore/engine/cache_populated_on_rollup_test.go
      provides: "Opt 3 correctness gate — TestCache_PopulatedOnRollupComplete"
      contains: "TestCache_PopulatedOnRollupComplete"
    - path: pkg/blockstore/engine/small_file_eager_dedup_test.go
      provides: "Opt 4 correctness gate — TestSmallFileEagerDedup_BSCAS06"
      contains: "TestSmallFileEagerDedup_BSCAS06"
    - path: pkg/blockstore/local/fs/rollup_idempotent_dedup_bench_test.go
      provides: "Opt 1 yellow-flag bench — BenchmarkRandWriteCAS_IdempotentBytes"
      contains: "BenchmarkRandWriteCAS_IdempotentBytes"
    - path: pkg/blockstore/local/fs/appendwrite_group_commit_bench_test.go
      provides: "Opt 2 yellow-flag bench — BenchmarkAppendWrite_GroupCommit"
      contains: "BenchmarkAppendWrite_GroupCommit"
    - path: internal/bench/phase19_test.go
      provides: "D-21 aggregate runner — ≤1.00 vs Phase 11 baseline"
      contains: "Phase11Baseline"
  key_links:
    - from: internal/bench/phase19_test.go
      to: "Phase 11 baseline file"
      via: "ratio gate ≤1.00"
      pattern: "1\\.00"
---

<objective>
Land the correctness tests + perf benches + D-21 aggregate gate. Hard-gate the two correctness tests (Opt 3 + Opt 4); yellow-flag the two perf benches (Opt 1 + Opt 2). Tighten D-21 to ≤1.00 vs Phase 11 baseline (was ≤1.02 — D-19).

Purpose: Without correctness gates, the 4 opts merging together cannot be verified to not regress functionality. Without perf benches + D-21, the LoC delta has no quantitative justification. D-21 ≤1.00 means "the four opts must at least keep parity; no regression allowed" — this is the load-bearing measurement gate for merging.

Output: 4 new test/bench files at the exact paths CONTEXT.md D-20 specifies + the internal/bench aggregate runner updated to enforce ≤1.00.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md
@.planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt 3 correctness gate — TestCache_PopulatedOnRollupComplete</name>
  <files>pkg/blockstore/engine/cache_populated_on_rollup_test.go</files>
  <read_first>
    - pkg/blockstore/engine/cache_test.go (existing cache test patterns — Cache.Get/Put helpers, fixtures)
    - pkg/blockstore/engine/engine.go (Flush + chunker invocation — to construct a rollup that fires OnChunkComplete)
    - pkg/blockstore/local/fs/chunkstore.go (Plan 07 — verify OnChunkComplete is invoked from StoreChunk)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-17 hard-gate)
  </read_first>
  <behavior>
    - TestCache_PopulatedOnRollupComplete: construct a full BlockStore + Cache + FSStore via the canonical engine constructor; write data spanning N chunks; trigger rollup (engine.Flush); for EACH chunk hash, assert Cache.Get(hash) returns the chunk content WITHOUT triggering a disk fetch (instrument a counter on the chunkstore's disk-read path). All N hashes must be cached post-rollup.
    - Edge case: empty rollup → no chunks → no cache entries → no error.
    - Edge case: extremely large chunk (> Cache.maxBytes) — Cache.Put silently skips per its existing guard, but rollup itself succeeds (matches Plan 07 Test 2).
  </behavior>
  <action>
    Create pkg/blockstore/engine/cache_populated_on_rollup_test.go. Use the in-package test fixtures + Cache + FSStore + Coordinator factory functions; consult existing engine_test.go to copy the canonical construction pattern.

    Test body:
    1. Build a BlockStore with cache + storage roots.
    2. Open a payload, write N=4 chunks worth of data (e.g., 16 MiB total with FastCDC default chunking).
    3. Call engine.Flush.
    4. For each chunk's hash (read back via snapshotPendingBlockRefs or by capturing the OnChunkComplete invocations via the wired closure), call bs.cache.Get(hash); assert returned data byte-equals the original chunk bytes.
    5. Assert the underlying chunkstore.ReadChunk counter is 0 — no disk reads occurred for the cache lookups (proof of Cache population from write-side).

    This is the load-bearing Opt 3 correctness gate per D-17. If this test fails, the merge is blocked.
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/engine/... -run "TestCache_PopulatedOnRollupComplete" -v -count=1 -timeout 30s 2>&1 | tee /tmp/p19-09-task1.out; grep -c "PASS" /tmp/p19-09-task1.out; grep -c "FAIL" /tmp/p19-09-task1.out</automated>
  </verify>
  <done>
    - cache_populated_on_rollup_test.go exists with TestCache_PopulatedOnRollupComplete
    - Test PASSES under -race
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Opt 4 correctness gate — TestSmallFileEagerDedup_BSCAS06</name>
  <files>pkg/blockstore/engine/small_file_eager_dedup_test.go</files>
  <read_first>
    - pkg/blockstore/engine/dedup_test.go (existing dedup test fixtures + applyFileLevelDedupHit test patterns)
    - pkg/blockstore/engine/engine.go (Plan 08 — confirm tryEagerSmallFileDedup is hooked into Flush)
    - pkg/chunker (MinChunkSize constant — confirm value at 1 MiB)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-13 threshold; D-16 cache populate)
  </read_first>
  <behavior>
    - TestSmallFileEagerDedup_BSCAS06: e2e test of Opt 4 end-to-end.
      1. Write file-A with small content (e.g., 64 KiB — well under 1 MiB MinChunkSize); Flush; observe ObjectID O.
      2. Write file-B with IDENTICAL small content (different payloadID); Flush.
      3. Assert file-B's Flush completed via the eager path (instrument tryEagerSmallFileDedup with a counter).
      4. Assert chunker was NOT invoked for file-B (counter via test hook).
      5. Assert StoreChunk was NOT invoked for file-B.
      6. Assert appendlog for file-B's payloadID is empty (no log records written).
      7. Assert bs.cache.Get(O's hash) returns the content (D-16 cache warming).
      8. Assert ReadAt on file-B returns the same content as file-A (correctness: dedup short-circuit does NOT corrupt data).
    - Boundary test: TestSmallFileEagerDedup_AtThreshold — file exactly at MinChunkSize triggers eager path.
    - Boundary test: TestSmallFileEagerDedup_AboveThreshold — file at MinChunkSize+1 bypasses eager (speculative + rollup runs).

    This is the BSCAS-06 e2e gate per D-17. Naming suffix BSCAS-06 ties back to the v0.15.0 requirement family (CONTEXT.md preserves BSCAS-01..06).
  </behavior>
  <action>
    Create pkg/blockstore/engine/small_file_eager_dedup_test.go with the three behaviors above. Use the in-package fixtures (the engine-with-coordinator factory likely already exists for dedup_test.go).

    Counter instrumentation:
    - tryEagerSmallFileDedup invocation counter: wrap or expose a package-internal counter; check after each Flush.
    - chunker invocation counter: harder — chunker.NewChunker is the constructor; either wrap NewChunker via a test-only factory OR observe indirectly (the rollup path's StoreChunk counter is sufficient as a proxy: if StoreChunk is invoked, chunker also was).
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/engine/... -run "TestSmallFileEagerDedup" -v -count=1 -timeout 30s 2>&1 | tee /tmp/p19-09-task2.out; grep -E "^(--- )?PASS" /tmp/p19-09-task2.out | wc -l; grep -E "^(--- )?FAIL" /tmp/p19-09-task2.out | wc -l</automated>
  </verify>
  <done>
    - small_file_eager_dedup_test.go exists with TestSmallFileEagerDedup_BSCAS06 + boundary tests
    - All three PASS under -race
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Opt 1 yellow-flag bench — BenchmarkRandWriteCAS_IdempotentBytes</name>
  <files>pkg/blockstore/local/fs/rollup_idempotent_dedup_bench_test.go</files>
  <read_first>
    - pkg/blockstore/local/fs/rollup_test.go (existing rollup benches — pattern for benchmarks in this package)
    - pkg/blockstore/local/fs/dedup_lru.go + rollup.go (Plan 03 + Plan 05 — the code being measured)
    - Phase 11 raceEnabled constant location (search: `grep -rn 'raceEnabled' pkg/blockstore/local/fs/`)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-17 yellow-flag — report ratio, don't block)
  </read_first>
  <behavior>
    - BenchmarkRandWriteCAS_IdempotentBytes — write K identical 4 KiB blocks (or similar repeated unit) across a payload; measure:
      1. b.N iterations of the K-write pattern.
      2. Count StoreChunk invocations across the benchmark (via instrumentation).
      3. Report `chunks_stored_per_op` as a custom bench metric via b.ReportMetric.
      4. Expected ratio: StoreChunk count / total chunks emitted by FastCDC < 1.0 (LRU is preventing re-stores).
    - Skip-under-race per the raceEnabled pattern from Phase 11.
    - Yellow-flag: bench REPORTS the ratio in stdout but never `t.Fatal` — D-17 says yellow-flag perf benches "report ratios, don't block".
  </behavior>
  <action>
    Create pkg/blockstore/local/fs/rollup_idempotent_dedup_bench_test.go.

    Skeleton:
    ```go
    package fs

    import (
        "context"
        "testing"
    )

    func BenchmarkRandWriteCAS_IdempotentBytes(b *testing.B) {
        if raceEnabled {
            b.Skip("Phase 19 D-17 yellow-flag — skip under -race to avoid detector overhead")
        }

        ctx := context.Background()
        bc := newTestFSStoreWithLRU(b, 4096)  // canonical test fixture — confirm name
        defer bc.Close()

        const repetitions = 256
        chunkBytes := makeRepeatedChunk(4096)  // 4 KiB of identical bytes

        var totalStoreChunks int64
        bc.SetStoreChunkCounter(&totalStoreChunks)  // test hook; planner: add if absent

        b.ResetTimer()
        for i := 0; i < b.N; i++ {
            for r := 0; r < repetitions; r++ {
                if err := bc.StoreChunk(ctx, blake3ContentHash(chunkBytes), chunkBytes); err != nil {
                    b.Fatal(err)
                }
            }
        }

        // Yellow-flag: report, don't block.
        b.ReportMetric(float64(totalStoreChunks)/float64(b.N*repetitions), "stores_per_chunk")
    }
    ```

    The "canonical test fixture" name (`newTestFSStoreWithLRU`, `SetStoreChunkCounter`) is illustrative — read existing rollup_test.go to find the actual fixtures and add helpers if absent. The plan's correctness target is the YELLOW-FLAG ratio reporting, not the absolute number.

    If raceEnabled constant is absent in pkg/blockstore/local/fs/, define one in a small new file `raceenabled_norace.go` + `raceenabled_race.go` build-tagged pair (Phase 11 pattern; search project for `raceEnabled` to find the existing convention).
  </action>
  <verify>
    <automated>go test ./pkg/blockstore/local/fs/... -run "^$" -bench "BenchmarkRandWriteCAS_IdempotentBytes" -benchtime=10x -count=1 -timeout 60s 2>&1 | tee /tmp/p19-09-task3.out; grep -c "BenchmarkRandWriteCAS_IdempotentBytes" /tmp/p19-09-task3.out</automated>
  </verify>
  <done>
    - rollup_idempotent_dedup_bench_test.go exists with BenchmarkRandWriteCAS_IdempotentBytes
    - Bench runs successfully (yellow-flag — no PASS/FAIL gate, just runnable)
    - Custom metric `stores_per_chunk` reported via b.ReportMetric
    - Skip-under-race honored
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Opt 2 yellow-flag bench — BenchmarkAppendWrite_GroupCommit</name>
  <files>pkg/blockstore/local/fs/appendwrite_group_commit_bench_test.go</files>
  <read_first>
    - pkg/blockstore/local/fs/appendwrite_test.go (existing appendwrite test patterns)
    - pkg/blockstore/local/fs/groupcommit.go (Plan 03 — the coordinator being measured)
    - pkg/blockstore/local/fs/appendwrite.go (Plan 06 — the wire-in)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-17 yellow-flag)
  </read_first>
  <behavior>
    - BenchmarkAppendWrite_GroupCommit — measure fsync coalescence under concurrent AppendWrite load.
      1. Open a single payload's logFile.
      2. Spawn G=8 goroutines, each performing AppendWrite in a loop for b.N total operations.
      3. Count fsync invocations across the benchmark (via test-hook counter on lf.f.Sync or the coordinator's fsyncFn wrapping).
      4. Report `fsyncs_per_op` via b.ReportMetric. Expected: << 1 (multiple writes share one fsync).
    - Skip-under-race per the raceEnabled pattern.
    - Yellow-flag: report only; don't gate.
  </behavior>
  <action>
    Create pkg/blockstore/local/fs/appendwrite_group_commit_bench_test.go. Mirror Task 3's skeleton; substitute AppendWrite calls + the goroutine fan-out.

    Critical: in a benchmark, b.N controls the inner loop; the goroutine count G should be a fixed constant (e.g., 8). Each goroutine performs b.N/G iterations. Use sync.WaitGroup for coordination.

    Counter: wrap the coordinator's fsyncFn at construction time with a test-only helper that increments before delegating. Or instrument lf.f.Sync wrap if a Sync-counter wrapper exists. Either approach is acceptable.
  </action>
  <verify>
    <automated>go test ./pkg/blockstore/local/fs/... -run "^$" -bench "BenchmarkAppendWrite_GroupCommit" -benchtime=10x -count=1 -timeout 60s 2>&1 | tee /tmp/p19-09-task4.out; grep -c "BenchmarkAppendWrite_GroupCommit" /tmp/p19-09-task4.out</automated>
  </verify>
  <done>
    - appendwrite_group_commit_bench_test.go exists with BenchmarkAppendWrite_GroupCommit
    - Bench runs successfully
    - Custom metric `fsyncs_per_op` reported via b.ReportMetric
    - Skip-under-race honored
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: D-21 aggregate gate ≤1.00 — internal/bench/phase19_test.go</name>
  <files>internal/bench/phase19_test.go</files>
  <read_first>
    - internal/bench/ (existing aggregate runner — find the Phase 11 baseline file and the existing ratio-gate test that previously enforced ≤1.02)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-19 tightening rationale)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("internal/bench/phase19_test.go" section)
  </read_first>
  <behavior>
    - TestPhase19_AggregateRandWriteGate_LeqOne — runs the canonical RandWrite warm-cache benchmark via the aggregate runner; computes the ratio vs Phase 11 baseline; FAILS if ratio > 1.00.
    - The test reads the baseline values from whatever artifact the existing aggregate runner consumes (typically a small JSON or Go-struct file checked into internal/bench/).
  </behavior>
  <action>
    Create internal/bench/phase19_test.go. The previous phase's aggregate-runner pattern (Phase 11 / Phase 16 — read existing files) is the canonical template.

    Sketch:
    ```go
    package bench

    import (
        "testing"
    )

    // TestPhase19_AggregateRandWriteGate_LeqOne enforces D-19: the four
    // Phase 19 opts together must keep RandWrite warm-cache parity (≤1.00)
    // with the Phase 11 baseline. Tightened from ≤1.02 because Phase 19's
    // entire purpose is write-path throughput improvement.
    func TestPhase19_AggregateRandWriteGate_LeqOne(t *testing.T) {
        baseline := loadPhase11Baseline(t)  // existing helper
        current := runRandWriteWarmCacheBench(t)
        ratio := current.NsPerOp / baseline.NsPerOp
        if ratio > 1.00 {
            t.Fatalf("D-19: Phase 19 RandWrite warm-cache ratio %.3f exceeds 1.00 vs Phase 11 baseline (%.0f ns/op vs %.0f ns/op)", ratio, current.NsPerOp, baseline.NsPerOp)
        }
        t.Logf("D-19 PASS: RandWrite warm-cache ratio %.3f (target ≤1.00)", ratio)
    }
    ```

    Helper functions (`loadPhase11Baseline`, `runRandWriteWarmCacheBench`) almost certainly exist already from the Phase 16 D-06 gate. Read internal/bench/ to find them; if the previous gate used a constant `1.02`, locate and tighten to `1.00` rather than creating a new gate.

    Existing D-06, D-41, D-43 gates remain at their previous tolerances — only D-21 (RandWrite warm-cache) is tightened.

    Document in the test's godoc that this gate is the load-bearing merge gate for Phase 19 — if it fails, the mega-PR is blocked from merging.
  </action>
  <verify>
    <automated>go test ./internal/bench/... -run "TestPhase19_AggregateRandWriteGate_LeqOne" -v -count=1 -timeout 300s 2>&1 | tee /tmp/p19-09-task5.out; grep -E "^(--- )?PASS|FAIL" /tmp/p19-09-task5.out</automated>
  </verify>
  <done>
    - internal/bench/phase19_test.go exists
    - TestPhase19_AggregateRandWriteGate_LeqOne enforces ≤1.00 vs Phase 11 baseline
    - Test PASSES on the bench infra (if local dev-laptop variance trips the gate, document in SUMMARY.md and re-run on canonical bench infra per Phase 17 Plan 10 SUMMARY's note about D-06 dev-laptop vs bench infra)
    - Previous gate constant updated in-place (NOT a new gate alongside)
  </done>
</task>

</tasks>

<verification>
- `go test -race ./pkg/blockstore/engine/... -run "TestCache_PopulatedOnRollupComplete|TestSmallFileEagerDedup_BSCAS06" -count=1` PASS (hard-gate D-17)
- `go test ./pkg/blockstore/local/fs/... -run "^$" -bench "BenchmarkRandWriteCAS_IdempotentBytes|BenchmarkAppendWrite_GroupCommit" -benchtime=10x` runs without error (yellow-flag D-17)
- `go test ./internal/bench/... -run "TestPhase19_AggregateRandWriteGate_LeqOne" -count=1` PASS (≤1.00 — D-19)
- D-06, D-41, D-43 gates unchanged (unmodified — confirm by `git diff internal/bench/` showing only D-21 ratio change)
</verification>

<success_criteria>
1. D-17 hard-gate correctness: Opt 3 + Opt 4 e2e tests in place and PASSING
2. D-17 yellow-flag perf: Opt 1 + Opt 2 benches in place; report ratios; never gate
3. D-19 D-21 tightening: aggregate runner enforces ≤1.00 vs Phase 11 baseline
4. D-20 file placement: 4 new files at the exact paths CONTEXT.md specifies + aggregate runner updated
5. raceEnabled pattern honored for the perf benches
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-path-ram-optimizations/19-09-SUMMARY.md` capturing:
- Whether D-21 gate PASSed on dev-laptop or needs bench-infra re-run (mirror Phase 17 Plan 10 SUMMARY's policy)
- Exact bench numbers observed (Phase 11 baseline value vs current, ratio computed)
- Any test fixtures added (e.g., SetStoreChunkCounter, newTestFSStoreWithLRU)
- Final state: yellow-flag benches' typical reported `stores_per_chunk` and `fsyncs_per_op` values
</output>
</content>
</invoke>