---
phase: 19-write-path-ram-optimizations
plan: 06
type: execute
wave: 2
depends_on: [19-03]
files_modified:
  - pkg/blockstore/local/fs/appendwrite.go
  - pkg/blockstore/local/fs/fs.go
  - pkg/blockstore/local/fs/appendwrite_test.go
autonomous: true
requirements: [D-06, D-07, D-08, D-09]
must_haves:
  truths:
    - "D-06 + D-09: appendwrite.go:259 replaces `lf.f.Sync()` with `lf.groupCommit.Sync(ctx)` (or planner-equivalent named method) routing through the per-logFile groupCommit coordinator from Plan 03"
    - "D-07: each logFile owns its own groupCommit instance (per-file scope); different logFiles fsync independently"
    - "D-08 synchronous durability contract: AppendWrite caller blocks until its batch fsync completes (NFS COMMIT / SMB Flush callers see no async ack)"
    - "D-09 lock-order: per-file `mu` (bc.logLocks[payloadID]) held when calling lf.groupCommit.Sync; coordinator's groupCommit.mu acquired inside Sync; bc.logsMu NEVER touched by the coordinator"
    - "logFile struct gains `groupCommit *groupCommit` field; instantiated at logFile creation (newLogFile or equivalent) with `newGroupCommit(lf.f.Sync)`"
  artifacts:
    - path: pkg/blockstore/local/fs/appendwrite.go
      provides: "Group-commit-coordinated fsync at the previous lf.f.Sync() call site"
      contains: "lf.groupCommit.Sync"
    - path: pkg/blockstore/local/fs/fs.go
      provides: "logFile.groupCommit field + instantiation"
      contains: "groupCommit *groupCommit"
  key_links:
    - from: pkg/blockstore/local/fs/appendwrite.go (line ~259, post-edit)
      to: pkg/blockstore/local/fs/groupcommit.go (groupCommit.Sync)
      via: "per-file fsync coalesce"
      pattern: "lf\\.groupCommit\\.Sync"
---

<objective>
Wire Opt 2 (group commit / batched fsync) into the per-record fsync site at `pkg/blockstore/local/fs/appendwrite.go:259`. Replace `lf.f.Sync()` with `lf.groupCommit.Sync(ctx)`. Instantiate the per-logFile groupCommit coordinator at logFile creation.

Purpose: Under burst writes to a single payload (NFS-write-heavy workloads, qcow2 random writes, log files), concurrent AppendWrites share a single fsync — 5-10× small-write throughput on rotational, 2-3× on NVMe. The coordinator was unit-tested standalone in Plan 03; this plan integrates it.

Output: appendwrite.go:259 fsync site routes through groupCommit; logFile carries a coordinator; the synchronous durability contract is preserved (D-08); FIX-2/FIX-20 lock ordering holds (D-09 — coordinator never touches logsMu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md
@.planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md
@.planning/phases/19-write-path-ram-optimizations/19-03-PLAN.md
@CLAUDE.md

<interfaces>
Existing fsync site (pkg/blockstore/local/fs/appendwrite.go around lines 227-263 — read in full):
```go
n, err := writeRecord(lf.f, offset, data)
if err != nil {
    // ... FIX-2 / FIX-20 recovery posture preserved ...
}
if err := lf.f.Sync(); err != nil {
    return fmt.Errorf("log fsync: %w", err)
}
bc.logBytesTotal.Add(int64(n))
tree.Insert(offset, uint32(len(data)), time.Now())
```

Lock-ordering invariant (appendwrite.go:232-250):
```
// LOCK ORDERING (FIX-2): release the per-file mutex BEFORE
// acquiring bc.logsMu.Lock(). Any path that holds logsMu and
// waits on mu would otherwise deadlock against us.
```

logFile struct definition lives in either fs.go or a dedicated logfile.go — search for `type logFile struct` to find the canonical location. Plan adds `groupCommit *groupCommit` field there. Constructor (e.g., `newLogFile` or inline creation in `bc.getOrCreateLogFile`) instantiates `lf.groupCommit = newGroupCommit(lf.f.Sync)`.

The fsyncFn captured by the coordinator must be `lf.f.Sync` (bound method value — captures lf.f reference). At the time the coordinator fires the fsync, the per-file `mu` will already be released (depending on the synchronization model — Test 5 in Plan 03 covers this). Per D-08, however, the caller of `lf.groupCommit.Sync(ctx)` is itself blocking on the result channel, so the caller still observes the fsync result.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add logFile.groupCommit field + instantiation</name>
  <files>pkg/blockstore/local/fs/fs.go</files>
  <read_first>
    - pkg/blockstore/local/fs/fs.go (find `type logFile struct` definition + the constructor that opens lf.f — `newLogFile`, `openLogFile`, or inline in `getOrCreateLogFile`. PATTERNS.md notes the field naming + lifecycle hint.)
    - pkg/blockstore/local/fs/groupcommit.go (Plan 03 — confirm `newGroupCommit(fsyncFn func() error) *groupCommit` signature)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/blockstore/local/fs/appendwrite.go" section + "groupcommit.go" section)
  </read_first>
  <behavior>
    - Test 1: TestLogFile_GroupCommit_NonNilAfterConstruction — open a logFile via the canonical constructor; assert lf.groupCommit != nil.
    - Test 2: TestLogFile_GroupCommit_FsyncFn_BoundToLfFile — call lf.groupCommit.Sync(ctx); verify it actually fsyncs the underlying file (write a record, call Sync, close, reopen, verify durable). This is end-to-end durability via the coordinator.
  </behavior>
  <action>
    Modify pkg/blockstore/local/fs/fs.go (or wherever `type logFile struct` lives):

    1. Add field to logFile struct:
       ```go
       // groupCommit coalesces concurrent fsyncs against this log's fd into
       // a single underlying f.Sync() call (Phase 19 Opt 2, D-06/D-07/D-08).
       // Per-file scope — different logFiles fsync independently. Synchronous
       // durability preserved: Sync(ctx) blocks until fsync completes.
       groupCommit *groupCommit
       ```

    2. In the logFile constructor (read fs.go to find — typically `newLogFile`, `openLogFile`, or inline in `getOrCreateLogFile`), instantiate the coordinator immediately after `lf.f` is assigned:
       ```go
       lf.groupCommit = newGroupCommit(lf.f.Sync)
       ```

    3. If the logFile is destroyed/closed via a `close` method, no special teardown needed for the coordinator — the Plan 03 design has no goroutines outside the timer, and timer firing on a closed-fd will surface fsync error to any in-flight waiter (acceptable). Document this in the field godoc.

    4. The fsyncFn `lf.f.Sync` is a bound method value at the time of newGroupCommit. If lf.f is replaced later (file rotation? — read the package to confirm whether this happens), the coordinator captures a stale reference. PATTERNS.md does not flag file rotation; if planner discovers it during read_first, switch to a closure: `newGroupCommit(func() error { return lf.f.Sync() })` — but only if necessary. Default to the bound method value.
  </action>
  <verify>
    <automated>go test ./pkg/blockstore/local/fs/... -run "LogFile_GroupCommit" -v -count=1 2>&1 | tee /tmp/p19-06-task1.out; grep -E "^(--- )?PASS" /tmp/p19-06-task1.out | wc -l; grep -E "^(--- )?FAIL" /tmp/p19-06-task1.out | wc -l; grep -c "groupCommit \*groupCommit" pkg/blockstore/local/fs/fs.go; grep -c "newGroupCommit(lf.f.Sync)" pkg/blockstore/local/fs/fs.go</automated>
  </verify>
  <done>
    - logFile struct has `groupCommit *groupCommit` field
    - logFile constructor instantiates `lf.groupCommit = newGroupCommit(lf.f.Sync)` (or closure-equivalent if file rotation surfaces during read_first)
    - Tests 1-2 PASS
    - go build still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace lf.f.Sync() at appendwrite.go:259 with lf.groupCommit.Sync(ctx)</name>
  <files>pkg/blockstore/local/fs/appendwrite.go, pkg/blockstore/local/fs/appendwrite_test.go</files>
  <read_first>
    - pkg/blockstore/local/fs/appendwrite.go (read full file — at minimum lines 200-280 around the fsync site; note that "line 259" is from CONTEXT.md's pre-edit snapshot, the actual line may shift)
    - pkg/blockstore/local/fs/appendwrite.go specifically the FIX-2/FIX-20 godoc block at lines 232-250 (must remain accurate post-edit; if the godoc references "lf.f.Sync()" specifically, update it to mention the coordinator)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/blockstore/local/fs/appendwrite.go" section — exact swap + invariants)
    - .planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md (D-08 backpressure, D-09 lock-order; "per-file `mu` is STILL held at the sync site")
  </read_first>
  <behavior>
    - Test 1: TestAppendWrite_BatchedFsync_OneFsyncForConcurrentWrites — N=4 goroutines call AppendWrite concurrently on the same logFile within a 1ms window; instrument lf.f via a counter wrapper (or stat the underlying file's mtime) to confirm only ONE fsync system call observable; all four AppendWrite returns succeed.
    - Test 2: TestAppendWrite_SingleWriter_NoLatencyPenalty — one AppendWrite + lf.groupCommit.Sync(ctx) round-trip; total wall time < 1ms (depth-1 bypass — D-06 adaptive bypass).
    - Test 3: TestAppendWrite_FsyncError_PropagatesToCaller — inject fsync error via a test-only fsyncFn override (wrap the coordinator's fsyncFn at construction time, or use a test-only constructor); AppendWrite returns the wrapped error from `lf.groupCommit.Sync(ctx)`.
    - Test 4: TestAppendWrite_CtxCancel_StillFsyncs — start AppendWrite-A (it arms timer); cancel ctx-B on AppendWrite-B; B returns ctx.Err(); A's fsync still completes; the underlying file IS synced (verify via close+reopen+read). D-08 invariant.
    - Test 5: TestAppendWrite_LockOrder_PerFileMuStillHeldAcrossSync — invariant assertion: between writeRecord and Sync, the per-file mu (bc.logLocks[payloadID]) is held; after Sync returns, it is released by the caller in the same critical section (no change from pre-edit). Test by inspecting via a debug hook or, more simply, by running the existing appendwrite race tests and confirming no new race detections.
  </behavior>
  <action>
    Modify pkg/blockstore/local/fs/appendwrite.go around line 259 (locate by searching for `lf.f.Sync()`):

    Replace the existing line:
    ```go
    if err := lf.f.Sync(); err != nil {
        return fmt.Errorf("log fsync: %w", err)
    }
    ```

    With:
    ```go
    if err := lf.groupCommit.Sync(ctx); err != nil {
        // Phase 19 Opt 2 (D-06/D-07/D-08): fsync coalesced through the
        // per-logFile groupCommit coordinator. Synchronous durability
        // preserved — NFS COMMIT / SMB Flush callers see no async ack.
        return fmt.Errorf("log fsync: %w", err)
    }
    ```

    Critical invariants the edit must preserve:
    - The per-file `mu` (bc.logLocks[payloadID]) is STILL held by the outer caller across this Sync call (PATTERNS.md). The coordinator's internal mu is a separate mutex (D-09 — lock-order rule).
    - The error wrap message remains `"log fsync: %w"` — operator-visible error path unchanged; existing log parsers / monitoring keep working.
    - ctx threading: the call site must have ctx in scope. If ctx is not currently passed through to this function (the existing lf.f.Sync() takes no args), trace the call graph back to find the canonical ctx — typically AppendWrite's own ctx parameter. Plumb ctx if needed (this may surface 1-3 signature changes in helper functions — perform minimum surgery, document in SUMMARY.md).

    Update the FIX-2/FIX-20 lock-ordering godoc block (lines 232-250) to add a sentence noting the new lock-order rule:
    ```
    // LOCK ORDERING (FIX-2 / FIX-20 extension, Phase 19 D-09):
    //   per-file `mu` (held by caller across this region)
    //     → groupCommit.mu (acquired inside lf.groupCommit.Sync)
    //       → bc.logsMu (NEVER held while waiting on groupCommit.mu;
    //                    the coordinator NEVER references logsMu directly)
    ```

    Add TRANSITIONAL-NEXT-MILESTONE marker (D-26) near the new Sync site:
    ```go
    // TRANSITIONAL-NEXT-MILESTONE: O_DIRECT for log writes (see #519
    // "Deferred to v0.17+"). When O_DIRECT lands, the groupCommit
    // coordinator may need to switch from fsync to fdatasync or
    // fsync(O_SYNC) — revisit the fsyncFn closure at construction.
    ```
    Plan 10 (D-26 sweep) verifies this marker exists.

    Test file: extend pkg/blockstore/local/fs/appendwrite_test.go with Tests 1-5. Test 1 (single-fsync verification under burst) can use the FSStore test-fixture's underlying file ID + an os-level counter; Test 4 (ctx-cancel-still-fsyncs) is the most subtle — mirror Plan 03's Task 2 Test 5 implementation pattern.
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/local/fs/... -run "AppendWrite_BatchedFsync|AppendWrite_SingleWriter_NoLatencyPenalty|AppendWrite_FsyncError|AppendWrite_CtxCancel|AppendWrite_LockOrder" -v -count=1 -timeout 60s 2>&1 | tee /tmp/p19-06-task2.out; grep -E "^(--- )?PASS" /tmp/p19-06-task2.out | wc -l; grep -E "^(--- )?FAIL" /tmp/p19-06-task2.out | wc -l; grep -c "lf.groupCommit.Sync(ctx)" pkg/blockstore/local/fs/appendwrite.go; grep -c "lf.f.Sync()" pkg/blockstore/local/fs/appendwrite.go; grep -c "TRANSITIONAL-NEXT-MILESTONE: O_DIRECT" pkg/blockstore/local/fs/appendwrite.go</automated>
  </verify>
  <done>
    - appendwrite.go contains exactly one `lf.groupCommit.Sync(ctx)` call (the replacement)
    - appendwrite.go contains ZERO `lf.f.Sync()` references in production code (test fakes excluded — they may exist in *_test.go)
    - O_DIRECT TRANSITIONAL marker added (Plan 10 will verify)
    - FIX-2 lock-ordering godoc updated to mention groupCommit.mu
    - Full pkg/blockstore/local/fs test suite passes under -race
    - All 5 new behavior tests PASS
  </done>
</task>

</tasks>

<verification>
- `go test -race ./pkg/blockstore/local/fs/... -count=1 -timeout 120s` PASS (full package; including all existing appendwrite + write tests as regression coverage)
- `go build ./... && go vet ./...` exits 0
- `grep -n "lf.f.Sync()" pkg/blockstore/local/fs/appendwrite.go` returns ZERO production-code matches
- `grep -n "lf.groupCommit.Sync" pkg/blockstore/local/fs/appendwrite.go` returns at least one match
- `grep -n "groupCommit \*groupCommit" pkg/blockstore/local/fs/fs.go` returns at least one match (the field declaration on logFile)
- `grep -n "logsMu" pkg/blockstore/local/fs/groupcommit.go` returns 0 (Plan 03's invariant preserved through this wire-in — Plan 06 must not have added any logsMu touches)
</verification>

<success_criteria>
1. Opt 2 wired: per-record fsync at appendwrite.go:259 replaced with groupCommit coordinator call
2. Per-logFile coordinator instantiated at logFile construction with `newGroupCommit(lf.f.Sync)`
3. Synchronous durability preserved (D-08) — caller blocks until fsync result; ctx-cancel returns ctx.Err() but the in-flight fsync still runs
4. Lock-order discipline (D-09) — coordinator never touches bc.logsMu; per-file mu held by outer caller across Sync invocation
5. Adaptive bypass (D-06) — depth=1 single-writer path has zero latency penalty (Test 2)
6. Batched fsync observable under burst (Test 1) — 4 concurrent AppendWrites → 1 fsync syscall
7. O_DIRECT TRANSITIONAL-NEXT-MILESTONE marker present (Plan 10 D-26 will verify)
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-path-ram-optimizations/19-06-SUMMARY.md` capturing:
- Exact location of logFile struct (fs.go vs dedicated logfile.go) + constructor name
- Whether ctx had to be plumbed through (and if so, which helper signatures changed)
- Whether bound-method-value or closure form of fsyncFn was used (informed by file-rotation analysis during read_first)
- Whether bc.logsMu was even touched during this plan's implementation (must be zero touches — D-09)
</output>
</content>
</invoke>