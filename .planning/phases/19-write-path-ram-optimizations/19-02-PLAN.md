---
phase: 19-write-path-ram-optimizations
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - pkg/metadata/store/memory/objects.go
  - pkg/metadata/store/badger/objects.go
  - pkg/metadata/store/postgres/objects.go
autonomous: true
requirements: [D-04, D-21]
must_haves:
  truths:
    - "D-04: AddRef implemented on all 3 backends (memory, badger, postgres); each impl returns ErrUnknownHash when hash is not in metadata"
    - "D-21: storetest conformance subtests (added in Plan 01) PASS for all 3 backends — `go test ./pkg/metadata/store/memory/...` and equivalents exit 0"
    - "D-04: AddRef is atomic — no TOCTOU race against concurrent DecrementRefCount cascade (single SQL UPDATE / single badger db.Update txn / single s.mu.Lock for memory)"
    - "D-27: each backend impl mutates RefCount ONLY; BlockState field is never written; no new row is inserted on the ErrUnknownHash path"
  artifacts:
    - path: pkg/metadata/store/memory/objects.go
      provides: "MemoryMetadataStore.AddRef implementation"
      contains: "func (s *MemoryMetadataStore) AddRef"
    - path: pkg/metadata/store/badger/objects.go
      provides: "BadgerMetadataStore.AddRef implementation"
      contains: "func (s *BadgerMetadataStore) AddRef"
    - path: pkg/metadata/store/postgres/objects.go
      provides: "PostgresMetadataStore.AddRef implementation"
      contains: "func (s *PostgresMetadataStore) AddRef"
  key_links:
    - from: each backend's AddRef
      to: pkg/metadata/errors.go (ErrUnknownHash)
      via: "return on missing-hash branch"
      pattern: "return.*metadata\\.ErrUnknownHash"
    - from: each backend's AddRef
      to: pkg/blockstore/store.go (FileBlockStore interface)
      via: "satisfies interface dispatch in pkg/metadata/storetest harness"
      pattern: "FileBlockStore.*AddRef"
---

<objective>
Implement `FileBlockStore.AddRef` on all 3 metadata backends (memory / badger / postgres). Closes the Wave 1 intermediate state from Plan 01 — after this commit lands, `go build ./...` passes again and `go test ./pkg/metadata/...` exercises the conformance subtests on every backend.

Purpose: Provide the metadata-side implementations that Opt 1 (Plan 05) will call on LRU hit. STATE-01..03 preservation (D-27) is enforced at the per-backend mutation site; conformance suite (Plan 01) verifies the contract.

Output: Three new backend method implementations. All Plan 01 conformance subtests now PASS for memory + badger backends; postgres subtest passes when `DITTOFS_TEST_POSTGRES_DSN` is set (matching existing postgres-test convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-write-path-ram-optimizations/19-CONTEXT.md
@.planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md
@.planning/phases/19-write-path-ram-optimizations/19-01-PLAN.md
@CLAUDE.md

<interfaces>
Per-backend analog patterns (read each file before editing — exact field names, txn shapes, and SQL idioms differ between backends):

Memory backend reference (pkg/metadata/store/memory/objects.go around lines 275-285):
```go
func (s *MemoryMetadataStore) incrementRefCountLocked(_ context.Context, id string) error {
    if s.fileBlockData == nil { return metadata.ErrFileBlockNotFound }
    block, ok := s.fileBlockData.blocks[id]
    if !ok { return metadata.ErrFileBlockNotFound }
    block.RefCount++
    return nil
}
```

Memory backend GetByHash (objects.go around line 99) uses `s.fileBlockData.hashIndex[hash]` to translate hash→id. AddRef MUST consult this index.

Badger backend reference (pkg/metadata/store/badger/objects.go around line 163): IncrementRefCount uses `s.db.Update(func(txn *badger.Txn) error {...})` pattern. AddRef MUST be ONE atomic Update transaction (no separate Get-then-Update).

Postgres backend reference: existing IncrementRefCount uses an UPDATE statement with RETURNING; AddRef follows the same idiom (no separate SELECT-then-UPDATE).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AddRef on memory backend</name>
  <files>pkg/metadata/store/memory/objects.go</files>
  <read_first>
    - pkg/metadata/store/memory/objects.go (existing IncrementRefCount, incrementRefCountLocked, GetByHash, findFileBlockByHashLocked — all in this file)
    - pkg/metadata/store/memory/objects.go around line 99-103 (GetByHash; shows the `fileBlockData.hashIndex` lookup pattern)
    - pkg/metadata/store/memory/objects.go around line 275-285 (incrementRefCountLocked — direct analog)
    - pkg/blockstore/store.go (FileBlockStore interface — confirm AddRef signature added in Plan 01)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/metadata/store/memory/objects.go" section)
  </read_first>
  <action>
    Add `func (s *MemoryMetadataStore) AddRef(ctx context.Context, hash blockstore.ContentHash, payloadID string, blockRef blockstore.BlockRef) error` to pkg/metadata/store/memory/objects.go, placed adjacent to existing IncrementRefCount for code locality.

    Implementation (single atomic mutation under s.mu Write lock — no TOCTOU per D-04):
    1. `s.mu.Lock(); defer s.mu.Unlock()`
    2. Guard against nil fileBlockData: `if s.fileBlockData == nil { return metadata.ErrUnknownHash }` (no data means no rows means hash is unknown — matches the missing-hash sentinel branch).
    3. Translate hash→id via `id, ok := s.fileBlockData.hashIndex[hash]`. If `!ok` OR `id == ""`, return `metadata.ErrUnknownHash` (D-04 sentinel).
    4. Fetch the block: `block, ok := s.fileBlockData.blocks[id]`. If `!ok` (index/blocks desync — should not happen but defend), return `metadata.ErrUnknownHash`.
    5. `block.RefCount++` and return nil. **Do NOT mutate `block.BlockState`** (D-27 explicit).
    6. The `payloadID` and `blockRef` arguments are intentionally unused at the metadata layer in this minimal impl — they exist on the interface for GC traceability (D-04) and for future backends that may want to record an explicit reference edge. Add a one-line comment: `// payloadID + blockRef accepted for future GC traceability (D-04); memory backend records ref count only.` to suppress the unused-arg lint.

    Verify the memory backend conforms to FileBlockStore by checking the storetest registration point — typically `pkg/metadata/store/memory/objects_test.go` has a `runFileBlockOpsTests(t, factoryFn)` call already. The subtests from Plan 01 will auto-execute.
  </action>
  <verify>
    <automated>go test ./pkg/metadata/store/memory/... -run "FileBlockOps/AddRef" -v 2>&1 | tee /tmp/p19-02-task1.out; grep -c "AddRef_ExistingHash_BumpsRefCount.*PASS" /tmp/p19-02-task1.out; grep -c "AddRef_MissingHash_ReturnsErrUnknownHash.*PASS" /tmp/p19-02-task1.out; grep -c "AddRef_Concurrent_With_DecrementRefCountCascade.*PASS" /tmp/p19-02-task1.out</automated>
  </verify>
  <done>
    - `func (s *MemoryMetadataStore) AddRef(` exists exactly once in pkg/metadata/store/memory/objects.go
    - Implementation calls `s.mu.Lock()` (NOT RLock) and returns `metadata.ErrUnknownHash` on at least one branch
    - `go test ./pkg/metadata/store/memory/...` exits 0
    - All three Plan-01 AddRef subtests PASS for memory backend
    - The function body NEVER mentions `BlockState` (grep gate: `grep -A 20 "func (s \*MemoryMetadataStore) AddRef" pkg/metadata/store/memory/objects.go | grep -c BlockState` returns 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement AddRef on badger backend</name>
  <files>pkg/metadata/store/badger/objects.go</files>
  <read_first>
    - pkg/metadata/store/badger/objects.go (existing IncrementRefCount at line 163 — exact db.Update + unmarshal+mutate+marshal+Set pattern)
    - pkg/metadata/store/badger/objects.go (GetByHash implementation — shows the secondary-index key prefix and decode path)
    - pkg/metadata/store/badger/objects.go around line 530 (txn variant of IncrementRefCount — for understanding, NOT for implementation per D-21)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/metadata/store/badger/objects.go" section)
  </read_first>
  <action>
    Add `func (s *BadgerMetadataStore) AddRef(ctx context.Context, hash blockstore.ContentHash, payloadID string, blockRef blockstore.BlockRef) error` to pkg/metadata/store/badger/objects.go, placed adjacent to existing IncrementRefCount.

    Implementation (single `db.Update` txn — atomic per D-04, no TOCTOU):
    1. Open `s.db.Update(func(txn *badger.Txn) error { ... })`.
    2. Use the same hash→id secondary-index lookup that GetByHash uses (read GetByHash above to copy the exact key prefix; common badger pattern is `fileBlockByHashKey(hash)` returning the secondary key). On `badger.ErrKeyNotFound` from the secondary lookup → return `metadata.ErrUnknownHash` (NOT `ErrFileBlockNotFound`).
    3. Decode the secondary-index value to get the FileBlock id (the existing convention in this file).
    4. `txn.Get([]byte(fileBlockKey(id)))` — on `ErrKeyNotFound` here (index/value desync, should not happen) also return `metadata.ErrUnknownHash` (defend against orphan-index).
    5. Unmarshal the FileBlock, `block.RefCount++`, marshal back, `txn.Set(fileBlockKey(id), marshaled)`.
    6. **Do NOT touch any state-related field** (D-27).
    7. Return nil from the txn closure.
    8. Wrap as `return s.db.Update(...)` so caller sees the txn's success/failure.

    `payloadID` and `blockRef` unused at the metadata-layer here for the same reason as the memory backend — add the same one-line trailing comment to suppress lint.

    The badger backend's storetest registration is already in place (existing IncrementRefCount conformance runs via the same harness); the new AddRef subtests will auto-run.
  </action>
  <verify>
    <automated>go test ./pkg/metadata/store/badger/... -run "FileBlockOps/AddRef" -v 2>&1 | tee /tmp/p19-02-task2.out; grep -c "AddRef_ExistingHash_BumpsRefCount.*PASS" /tmp/p19-02-task2.out; grep -c "AddRef_MissingHash_ReturnsErrUnknownHash.*PASS" /tmp/p19-02-task2.out; grep -c "AddRef_Concurrent_With_DecrementRefCountCascade.*PASS" /tmp/p19-02-task2.out</automated>
  </verify>
  <done>
    - `func (s *BadgerMetadataStore) AddRef(` exists exactly once
    - Implementation uses exactly ONE `s.db.Update(` call (single-txn atomicity, no read-then-write across txns)
    - Returns `metadata.ErrUnknownHash` on both index-miss AND value-miss branches
    - All three Plan-01 AddRef subtests PASS for badger backend
    - `grep -A 25 "func (s \*BadgerMetadataStore) AddRef" pkg/metadata/store/badger/objects.go | grep -c BlockState` returns 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement AddRef on postgres backend</name>
  <files>pkg/metadata/store/postgres/objects.go</files>
  <read_first>
    - pkg/metadata/store/postgres/objects.go (existing IncrementRefCount SQL UPDATE pattern — exact table name, hash column name, ref_count column name, WithContext(ctx) wrapping)
    - pkg/blockstore/store.go around line 53 (the hash index migration comment about NON-UNIQUE partial index — explains why UPDATE may affect multiple rows per D-22b)
    - .planning/phases/19-write-path-ram-optimizations/19-PATTERNS.md ("pkg/metadata/store/postgres/objects.go" section)
  </read_first>
  <action>
    Add `func (s *PostgresMetadataStore) AddRef(ctx context.Context, hash blockstore.ContentHash, payloadID string, blockRef blockstore.BlockRef) error` to pkg/metadata/store/postgres/objects.go.

    Implementation (single UPDATE statement — atomic per D-04):
    1. Use the project's existing GORM (or raw sqlx) idiom — match exactly how IncrementRefCount is written in this file. Read IncrementRefCount first to confirm the layer (GORM `.Model().Where().Update()` vs raw `.WithContext(ctx).Exec()`).
    2. SQL (template — adapt to the project's actual column/table names; read the existing IncrementRefCount for the canonical naming):
       ```
       UPDATE file_blocks SET ref_count = ref_count + 1 WHERE hash = $1
       ```
       Note: Per D-22b, the hash index is NON-UNIQUE (partial); this UPDATE may affect multiple rows on the rare hash-collision-with-multi-row case. Per PATTERNS.md: "Backends MUST tolerate this without erroring." Do NOT add `LIMIT 1` — let all matching rows bump (the conformance test seeds a single block so observability is preserved).
    3. Use `result.RowsAffected()` (GORM idiom) or the equivalent. If `RowsAffected == 0` → return `metadata.ErrUnknownHash`.
    4. Wrap with `.WithContext(ctx)` to honor cancellation, mirroring the existing IncrementRefCount.
    5. **Do NOT include any `block_state =` clause** (D-27 — state never written).

    `payloadID` and `blockRef` are unused at the SQL layer; add the same one-line trailing comment as the other two backends.

    Postgres tests auto-skip without `DITTOFS_TEST_POSTGRES_DSN` (existing convention); the `go build ./pkg/metadata/store/postgres/...` step alone will verify compilation. Operator validation in CI lane that runs with DSN will exercise the three subtests.
  </action>
  <verify>
    <automated>go build ./pkg/metadata/store/postgres/... 2>&1 | tee /tmp/p19-02-task3.out; grep -c "func (s \*PostgresMetadataStore) AddRef" pkg/metadata/store/postgres/objects.go; grep -c "ref_count + 1" pkg/metadata/store/postgres/objects.go; grep -A 15 "func (s \*PostgresMetadataStore) AddRef" pkg/metadata/store/postgres/objects.go | grep -c block_state</automated>
  </verify>
  <done>
    - `func (s *PostgresMetadataStore) AddRef(` exists exactly once
    - SQL contains `ref_count + 1` (incrementing only)
    - SQL does NOT contain `block_state` anywhere inside the AddRef function body (D-27 — verified by the grep gate's last value being 0)
    - Returns `metadata.ErrUnknownHash` on RowsAffected == 0
    - `go build ./pkg/metadata/store/postgres/...` exits 0
    - `go build ./...` exits 0 — Wave 1 build-green restored
  </done>
</task>

</tasks>

<verification>
- `go build ./...` exits 0 (the intermediate state from Plan 01 is now resolved)
- `go vet ./...` exits 0
- `go test ./pkg/metadata/store/memory/... -run "FileBlockOps/AddRef"` PASS
- `go test ./pkg/metadata/store/badger/... -run "FileBlockOps/AddRef"` PASS
- `go build ./pkg/metadata/store/postgres/...` exits 0 (test suite gated on DSN env var; CI lane verifies)
- `grep -r "func.*AddRef(ctx context.Context, hash" pkg/metadata/store/ | wc -l` returns 3 (one per backend)
- No `BlockState` references inside any of the three AddRef function bodies (D-27 grep gate above)
</verification>

<success_criteria>
1. All three backends implement `AddRef` with atomic per-backend semantics (single Lock / single Update txn / single SQL UPDATE)
2. All three return `metadata.ErrUnknownHash` on the missing-hash branch
3. All three Plan-01 conformance subtests PASS for memory + badger
4. `go build ./...` exits 0 — Wave 1 closed, Wave 2 unblocked
5. STATE-01..03 preserved at every backend (grep-verified absence of state writes in the AddRef function bodies)
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-path-ram-optimizations/19-02-SUMMARY.md` capturing:
- Exact backend-specific atomicity choices (memory uses `s.mu.Lock()`; badger uses `db.Update`; postgres uses GORM/sqlx idiom)
- Any deviations from the per-backend analog (e.g., if postgres needed `LIMIT 1` for the multi-row-per-hash case)
- Confirmation that the storetest harness factory wiring picks up the subtests automatically (no per-backend test-file edits needed beyond what Plan 01 added centrally)
</output>
</content>
</invoke>