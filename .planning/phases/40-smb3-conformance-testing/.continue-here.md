---
phase: 40-smb3-conformance-testing
task: UAT
total_tasks: 10
status: blocked
last_updated: 2026-03-03T18:26:58.050Z
---

<current_state>
Running Phase 40 UAT (SMB3 Conformance Testing). Test 1 of 10 passed (dialect negotiation).
Test 2+ blocked by NTLM authentication failure — NTLMv2 validation fails across ALL dialects.
UAT file written at `.planning/phases/40-smb3-conformance-testing/40-UAT.md`.
</current_state>

<completed_work>

- Test 1: SMB 3.x Dialect Negotiation — PASS. Raw SMB2 NEGOTIATE confirms dialect=0x0311 (SMB 3.1.1) with signing enabled.
- Server setup: Built binaries, fresh DB, created stores/user/share/adapter, set max_dialect to SMB3.1.1.
- Wrote Go test program using go-smb2 library + raw negotiate. Tested all 3 dialects (2.1, 3.0, 3.1.1) — auth fails on ALL.
- Root cause investigation: NT hash storage verified correct. ComputeNTLMv2Hash logic matches go-smb2's ntowfv2. Domain handling matches.
- UAT file updated with detailed gap analysis including artifacts and missing debug steps.
</completed_work>

<remaining_work>

- Debug NTLM authentication failure (BLOCKER for all remaining tests)
- Tests 2-10: All pending, blocked by auth failure
- After auth fix: re-run full UAT suite autonomously
</remaining_work>

<decisions_made>

- Used go-smb2 library for programmatic testing (pure Go SMB client already in go.mod)
- Used raw SMB2 NEGOTIATE for Test 1 to verify dialect independent of auth
- Admin password set to `Admin123!`, testuser password `TestPass123!`
- SMB adapter on port 1445 (Windows kernel holds port 445)
- Max dialect setting was stuck at SMB2.1 from old DB — had to update via dfsctl and restart
</decisions_made>

<blockers>

- **NTLM Auth Failure (BLOCKER)**: NTLMv2 ValidateNTLMv2Response fails for all domain variants.
  - Server computes HMAC-MD5(NTLMv2Hash, ServerChallenge+ClientBlob) but result doesn't match client's NTProofStr
  - Affects ALL dialects (not preauth-specific)
  - NT hash is correctly stored (verified via models.ComputeNTHash)
  - Suspect: (1) ServerChallenge mismatch between CHALLENGE send and PendingAuth store, (2) SPNEGO unwrapping corrupts bytes, (3) TargetInfo AV_PAIR encoding issue, (4) ClientBlob extraction wrong
  - Key files: `internal/adapter/smb/v2/handlers/session_setup.go` (completeNTLMAuth), `internal/adapter/smb/auth/ntlm.go` (ValidateNTLMv2Response, BuildChallenge)
- **Windows /TCPPORT**: `net use` with `/TCPPORT:1445` gives error 67 on this Windows 11 build — no traffic reaches server. Known issue, workaround is programmatic testing with go-smb2.
</blockers>

<context>
The Handler has its own PendingAuth store (keyed by SMB session ID) separate from the Authenticator's pendingAuths sync.Map. The Handler path is what's actually used for authentication. The Authenticator's validateAgainstPendingAuths is a separate code path (possibly unused in current flow).

Debug approach: Add hex dump logging of ServerChallenge at both CHALLENGE-send time and validation time. Add hex dump of expected vs received NTProofStr in ValidateNTLMv2Response. This will immediately show whether it's a challenge mismatch or an NTLMv2Hash computation issue.

Note: hooks.go has uncommitted changes (preauth hash fix for SESSION_SETUP) — this is correct and needed for SMB 3.1.1 but NOT the cause of the current auth failure since auth fails on SMB 2.1 too.
</context>

<next_action>
Start with: `/gsd:debug` on the NTLM auth failure. Add hex-dump debug logging to:
1. `BuildChallenge()` — log the ServerChallenge bytes
2. `handleNTLMNegotiate()` — log ServerChallenge stored in PendingAuth
3. `completeNTLMAuth()` — log ServerChallenge from retrieved PendingAuth
4. `ValidateNTLMv2Response()` — log expected vs received NTProofStr, ntHash used, username+domain used
Then rebuild, restart server, run go-smb2 connect, and read logs to identify the exact mismatch point.
</next_action>
