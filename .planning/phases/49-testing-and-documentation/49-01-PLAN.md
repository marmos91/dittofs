---
phase: 49-testing-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dfsctl/commands/cache/cache.go
  - cmd/dfsctl/commands/cache/stats.go
  - cmd/dfsctl/commands/cache/evict.go
  - cmd/dfsctl/commands/root.go
  - pkg/apiclient/cache.go
  - internal/controlplane/api/handlers/cache.go
  - internal/controlplane/api/handlers/cache_test.go
  - pkg/controlplane/api/router.go
  - pkg/controlplane/runtime/runtime.go
  - pkg/controlplane/runtime/shares/service.go
autonomous: true
requirements: [DOCS-04]

must_haves:
  truths:
    - "dfsctl cache stats shows block counts by state, sizes, L1 hit/miss, offloader status"
    - "dfsctl cache stats --share X shows per-share stats only"
    - "dfsctl cache evict evicts both L1 and local blocks for all shares"
    - "dfsctl cache evict --share X evicts specific share only"
    - "dfsctl cache evict --l1-only evicts only L1 memory cache"
    - "dfsctl cache evict --local-only evicts only local FS blocks"
    - "Cache evict refuses if no remote store configured (safety)"
    - "--payload flag on share create returns hard error pointing to --local/--remote"
    - "dfsctl store payload commands are fully removed"
    - "REST /api/v1/payload-stores endpoints are fully removed"
  artifacts:
    - path: "cmd/dfsctl/commands/cache/cache.go"
      provides: "Cache root command under dfsctl"
    - path: "cmd/dfsctl/commands/cache/stats.go"
      provides: "Cache stats CLI with --share and -o flags"
    - path: "cmd/dfsctl/commands/cache/evict.go"
      provides: "Cache evict CLI with --share, --l1-only, --local-only, -v flags"
    - path: "pkg/apiclient/cache.go"
      provides: "CacheStats and CacheEvict API client methods"
    - path: "internal/controlplane/api/handlers/cache.go"
      provides: "Cache stats and evict HTTP handlers"
    - path: "pkg/controlplane/api/router.go"
      provides: "Cache routes wired into router"
  key_links:
    - from: "cmd/dfsctl/commands/cache/stats.go"
      to: "pkg/apiclient/cache.go"
      via: "CacheStats/CacheStatsForShare methods"
      pattern: "client\\.CacheStats"
    - from: "internal/controlplane/api/handlers/cache.go"
      to: "pkg/controlplane/runtime/shares/service.go"
      via: "Runtime.GetCacheStats/EvictCache methods"
      pattern: "rt\\..*Cache"
---

<objective>
Create the cache CLI commands (`dfsctl cache stats` and `dfsctl cache evict`) with full REST API backing, and implement the backward compatibility clean break by removing all old payload store CLI commands, API endpoints, and API client methods.

Purpose: Enables cache management for E2E tests and benchmarks (cache-tiers workload needs evict). Resolves GitHub issue #246. Removes legacy payload CLI/API per user decision (hard error, not deprecation).
Output: Working `dfsctl cache stats/evict` commands, REST endpoints, and zero payload CLI/API artifacts remaining.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-testing-and-documentation/49-CONTEXT.md

@pkg/controlplane/api/router.go
@internal/controlplane/api/handlers/block_stores.go
@pkg/apiclient/block_stores.go
@cmd/dfsctl/commands/store/block/
@pkg/controlplane/runtime/runtime.go
@pkg/controlplane/runtime/shares/service.go
@pkg/blockstore/engine/
@internal/cli/output/

<interfaces>
<!-- Key types the executor needs -->

From pkg/blockstore/engine/blockstore.go (approximate - read to confirm):
```go
type BlockStore struct { ... }
func (bs *BlockStore) ReadCacheStats() CacheStats  // or similar
func (bs *BlockStore) EvictL1Cache()
func (bs *BlockStore) EvictLocalBlocks() (int, int64, error)
```

From pkg/controlplane/runtime/shares/service.go:
```go
type Service struct { ... }
func (s *Service) GetBlockStore(shareName string) (*engine.BlockStore, error)
func (s *Service) ListShares() []ShareInfo
```

From internal/cli/output/ (reuse for table/JSON/YAML):
```go
func PrintTable(...)
func PrintJSON(...)
func PrintYAML(...)
```

From cmd/dfsctl/cmdutil/util.go:
```go
func NewAuthenticatedClient(cmd *cobra.Command) (*apiclient.Client, error)
func GetOutputFormat(cmd *cobra.Command) string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache REST API and Runtime methods</name>
  <files>
    internal/controlplane/api/handlers/cache.go
    internal/controlplane/api/handlers/cache_test.go
    pkg/controlplane/api/router.go
    pkg/controlplane/runtime/runtime.go
    pkg/controlplane/runtime/shares/service.go
  </files>
  <action>
1. Add cache methods to shares.Service or runtime.Runtime that delegate to per-share BlockStore instances:
   - `GetCacheStats(ctx, shareName string) (*CacheStatsResponse, error)` - returns block counts by state, size info, L1 hit/miss rate, offloader status (pending syncs, active uploads, queue depth). If shareName is empty, aggregate across all shares with per-share breakdown.
   - `EvictCache(ctx, shareName string, opts EvictOptions) (*EvictResult, error)` - where EvictOptions has L1Only and LocalOnly bools. Default evicts both L1 and local blocks. Safety: refuse local block eviction if share has no remote store configured (return error). Only evict blocks with state=Remote. Dirty/Local blocks stay.
   - Define response types (CacheStatsResponse, EvictResult, EvictOptions) in a shared location accessible to both handlers and apiclient.

2. Create `internal/controlplane/api/handlers/cache.go` with:
   - `CacheHandler` struct accepting Runtime reference
   - `Stats` handler: GET, reads optional `share` query param, delegates to runtime, returns JSON
   - `Evict` handler: POST, reads optional `share` query param + JSON body with `l1_only` and `local_only` bools, delegates to runtime, returns JSON result
   - Follow existing handler patterns (thin handler, delegate to runtime, use HandleStoreError for errors)

3. Write unit test `cache_test.go` covering stats and evict handlers with mock runtime.

4. Wire routes in `pkg/controlplane/api/router.go` inside the admin-only protected group:
   - `GET /api/v1/cache/stats` - global stats (aggregated + per-share)
   - `POST /api/v1/cache/evict` - global evict (all shares)
   - `GET /api/v1/shares/{name}/cache/stats` - per-share stats
   - `POST /api/v1/shares/{name}/cache/evict` - per-share evict

   Note: The per-share cache routes go inside the existing /shares/{name} route group.

5. Remove any remaining `/api/v1/payload-stores` routes from the router if they exist (clean break per user decision). Verify no payload store routes remain.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && go build ./... && go test ./internal/controlplane/api/handlers/ -run TestCache -v -count=1</automated>
  </verify>
  <done>Cache REST endpoints respond correctly. Stats returns per-share and aggregated data. Evict returns block/byte counts. Safety check prevents eviction without remote store. No payload store routes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Cache CLI commands and API client + payload CLI removal</name>
  <files>
    cmd/dfsctl/commands/cache/cache.go
    cmd/dfsctl/commands/cache/stats.go
    cmd/dfsctl/commands/cache/evict.go
    cmd/dfsctl/commands/root.go
    pkg/apiclient/cache.go
  </files>
  <action>
1. Create `pkg/apiclient/cache.go` with methods:
   - `CacheStats(ctx) (*CacheStatsResponse, error)` - GET /api/v1/cache/stats
   - `CacheStatsForShare(ctx, shareName string) (*CacheStatsResponse, error)` - GET /api/v1/shares/{name}/cache/stats
   - `CacheEvict(ctx, opts *CacheEvictRequest) (*EvictResult, error)` - POST /api/v1/cache/evict
   - `CacheEvictForShare(ctx, shareName string, opts *CacheEvictRequest) (*EvictResult, error)` - POST /api/v1/shares/{name}/cache/evict
   Follow existing apiclient patterns (use doRequest helper, handle errors).

2. Create `cmd/dfsctl/commands/cache/cache.go` with root `cache` command:
   - Short description: "Manage block store cache"
   - Register stats and evict subcommands

3. Create `cmd/dfsctl/commands/cache/stats.go`:
   - Flags: `--share <name>` (optional, specific share), standard `-o json|yaml|table`
   - Without --share: call CacheStats, display aggregated totals + per-share breakdown table
   - With --share: call CacheStatsForShare, display single share stats
   - Table columns: Share, BlocksTotal, BlocksDirty, BlocksLocal, BlocksRemote, SizeTotal, L1HitRate, PendingSyncs
   - Reuse internal/cli/output formatters for table/JSON/YAML

4. Create `cmd/dfsctl/commands/cache/evict.go`:
   - Flags: `--share <name>`, `--l1-only`, `--local-only`, `-v` (verbose)
   - Default: evict both L1 and local blocks
   - Output: quiet by default. With -v: "Evicted N blocks (X bytes freed), L1 entries cleared: M"
   - Error if safety check fails (no remote store): print clear message explaining why

5. Register cache command in `cmd/dfsctl/commands/root.go` alongside existing subcommands.

6. Remove legacy payload store CLI entirely:
   - Delete `cmd/dfsctl/commands/store/payload/` directory if it exists
   - Remove `--payload` flag from share create command. Replace with hard error: "Error: --payload flag has been removed. Use --local and --remote instead. See 'dfsctl share create --help' for usage."
   - Remove payload store methods from `pkg/apiclient/` (e.g., `stores.go` if it has PayloadStore methods - check and remove)
   - Update `cmd/dfsctl/commands/store/store.go` to remove payload subcommand registration if present
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs49 && go build ./cmd/dfsctl/... && go build ./cmd/dfs/... && go vet ./cmd/...</automated>
  </verify>
  <done>`dfsctl cache stats` and `dfsctl cache evict` compile and are registered. `dfsctl store payload` commands are gone. `--payload` flag returns hard error. API client has cache methods. All CLI binaries build cleanly.</done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles entire project
2. `go test ./internal/controlplane/api/handlers/ -run TestCache` passes
3. `go vet ./...` passes
4. `./dfsctl cache stats --help` shows expected flags
5. `./dfsctl cache evict --help` shows expected flags
6. `./dfsctl store payload` returns "unknown command" or similar
</verification>

<success_criteria>
- Cache CLI commands registered and buildable under `dfsctl cache` namespace
- REST API endpoints for cache stats/evict wired in router
- API client methods for cache operations exist
- All legacy payload store CLI, API endpoints, and client methods removed
- --payload flag returns hard error with migration message
</success_criteria>

<output>
After completion, create `.planning/phases/49-testing-and-documentation/49-01-SUMMARY.md`
</output>
