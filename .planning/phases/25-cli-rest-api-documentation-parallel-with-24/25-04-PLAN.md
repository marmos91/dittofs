---
phase: 25-cli-rest-api-documentation-parallel-with-24
plan: 04
type: execute
wave: 2
depends_on:
  - 25-01
# Wave-2 parallelism rationale: this docs plan depends only on the locked
# wire vocabulary + verify-rename + shared DTO contract from Plan 25-01.
# The CLI flag set and REST routes are fully specified in the frontmatters
# and <interfaces> blocks of Plans 25-02 and 25-03 (which are themselves
# locked by 25-CONTEXT.md), so the docs can be authored in parallel with
# 25-02 / 25-03's implementation work. The cross-check grep gates in
# <verification> require 25-02 / 25-03 source to be present at TEST time,
# not at write time — the executor for 25-04 runs the grep gates after
# 25-02 / 25-03 main tasks have shipped (the orchestrator schedules
# accordingly), or the gates run as the final wave-2 merge step.
files_modified:
  - docs/SNAPSHOTS.md
  - docs/ARCHITECTURE.md
  - docs/CLI.md
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - "`docs/SNAPSHOTS.md` exists and covers all 13 sections from D-25-20 (Overview through REST API reference)"
    - "`docs/ARCHITECTURE.md` has a 'Share Snapshots' section; the GC section references `SnapshotHoldProvider` (not `BackupHoldProvider`)"
    - "`docs/CLI.md` has a `dfsctl share snapshot` subsection covering all 5 subcommands with flag tables + examples"
    - "`README.md` contains a 'Share Snapshots' paragraph + 3-line example invoking `dfsctl share snapshot create` and `list`; no remaining mention of v0.13.0 backup or 'backup will ship in v0.16.0'"
    - "Every CLI flag documented matches the actual flag name shipped in Plan 25-03 (cross-checked via grep)"
    - "Every REST endpoint documented matches the actual route in `pkg/controlplane/api/router.go` (cross-checked via grep)"
    - "Every error sentinel referenced by name in SNAPSHOTS.md exists in `pkg/controlplane/models/errors.go`"
  artifacts:
    - path: "docs/SNAPSHOTS.md"
      provides: "Canonical operator guide"
      min_lines: 400
      contains: "## Restore runbook"
    - path: "docs/ARCHITECTURE.md"
      provides: "Share Snapshots section + updated GC section"
      contains: "SnapshotHoldProvider"
    - path: "docs/CLI.md"
      provides: "Full `dfsctl share snapshot` subtree section"
      contains: "share snapshot create"
    - path: "README.md"
      provides: "Snapshots feature paragraph replacing deprecated backup paragraph"
      contains: "dfsctl share snapshot"
  key_links:
    - from: "README.md snapshots paragraph"
      to: "docs/SNAPSHOTS.md"
      via: "markdown link"
      pattern: "docs/SNAPSHOTS.md"
    - from: "docs/CLI.md snapshot section"
      to: "docs/SNAPSHOTS.md restore runbook"
      via: "markdown link"
      pattern: "SNAPSHOTS.md#restore"
---

<objective>
Ship the four documentation deliverables for Phase 25:

- **DOC-01** — new `docs/SNAPSHOTS.md`: full operator guide, 13 sections per D-25-20, depth mirrors `docs/BLOCKSTORE_MIGRATION.md` (~400–600 lines).
- **DOC-02** — `docs/ARCHITECTURE.md`: full "Share Snapshots" section + GC-section update (rename `BackupHoldProvider` references to `SnapshotHoldProvider`).
- **DOC-03** — `docs/CLI.md`: full `dfsctl share snapshot` subtree section with flag tables + examples for each of the 5 leaf commands.
- **DOC-04** — `README.md`: replace deprecated "backup will ship in v0.16.0" / v0.13.0-backup paragraph with a "Share Snapshots" paragraph + 3-line example + link to `docs/SNAPSHOTS.md`.

Purpose: Operators read these docs before invoking the snapshots feature. The plan also pins the docs against the actual CLI/REST surface via grep gates so future drift is caught at the next planning cycle.

Output: 4 docs files matching the as-shipped CLI flags + REST routes.

Note on wave-2 parallelism: this plan runs in parallel with 25-02 and 25-03 because the wire vocabulary (`verify` not `sync gate`), the CLI flag set, the REST routes, and the error-sentinel mapping are all locked by 25-CONTEXT.md and the 25-02 / 25-03 plan frontmatters. The cross-check grep gates require 25-02 / 25-03 source to be present at TEST time; the executor coordinates merge timing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-01-PLAN.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-02-PLAN.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-03-PLAN.md
@./CLAUDE.md

@docs/BLOCKSTORE_MIGRATION.md
@docs/ARCHITECTURE.md
@docs/CLI.md
@README.md
@cmd/dfsctl/commands/share/snapshot/snapshot.go
@cmd/dfsctl/commands/share/snapshot/create.go
@cmd/dfsctl/commands/share/snapshot/list.go
@cmd/dfsctl/commands/share/snapshot/show.go
@cmd/dfsctl/commands/share/snapshot/delete.go
@cmd/dfsctl/commands/share/snapshot/restore.go
@pkg/controlplane/api/router.go
@pkg/controlplane/models/errors.go

<interfaces>
**Canonical as-shipped surface (after Plan 25-01 ships and per the locked frontmatters of 25-02/25-03):**

REST routes (under `/api/v1/shares/{name}/snapshots`, admin-only):
| Method | Path | Purpose | Status |
|---|---|---|---|
| POST | `/` | Create snapshot (async) | 202 + Location |
| GET | `/` | List snapshots | 200 + JSON array |
| GET | `/{id}` | Get snapshot | 200 + JSON object |
| DELETE | `/{id}` | Delete snapshot | 204 |
| POST | `/{id}/restore` | Restore snapshot (sync) | 200 + JSON object including `safety_snapshot_id` |

CLI subcommands (`dfsctl share snapshot ...`):
| Command | Flags |
|---|---|
| `create <share>` | `--name`, `--no-verify`, `--retry`, `--no-wait` |
| `list <share>` | `--state`, `--name-prefix`, `--no-relative` |
| `show <share> <id>` | (no flags beyond global `-o`) |
| `delete <share> <id>` | `--yes` |
| `restore <share> <id>` | `--yes`, `--force` |

Error sentinels (12 from Phase 22/23/24, mapped via Plan 25-02 `mapSnapshotError`):
`ErrSnapshotNotFound`, `ErrShareEnabled`, `ErrSnapshotNotDurable`, `ErrSnapshotMetadataDumpMissing`, `ErrMetadataStoreNotResetable`, `ErrSnapshotRetryTargetNotFound`, `ErrSnapshotRetryTargetNotFailed`, `ErrSnapshotDrainTimeout`, `ErrSnapshotBackupFailed`, `ErrSnapshotVerifyFailed`, `ErrRestoreSafetySnapFailed`, `ErrRestoreAborted`, `ErrRestoreVerifyFailed`.

9 D-24-13 failure modes (covered in §11 Failure modes & recovery — see `.planning/phases/24-restore-flow/24-CONTEXT.md` D-24-13).

Existing docs reference patterns to mirror:
- `docs/BLOCKSTORE_MIGRATION.md` — TOC at top, worked transcripts, "Recovery", "Out of scope", "Internals" sections, blockquote Known Limitation callouts.
- `docs/CLI.md` — hand-maintained convention (no GenMarkdown); flag table per command; examples block per command.
- `docs/ARCHITECTURE.md` — text-flow diagrams + section per subsystem.
- `README.md` — short feature paragraphs + links to deep-dive docs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/SNAPSHOTS.md (canonical operator guide)</name>
  <files>docs/SNAPSHOTS.md</files>
  <read_first>
    docs/BLOCKSTORE_MIGRATION.md (full file — tone + depth + structure template),
    .planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md (D-25-20),
    .planning/phases/24-restore-flow/24-CONTEXT.md (D-24-13 — 9 failure modes for §11),
    .planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-CONTEXT.md (manifest-on-disk = held; GC hold semantics),
    .planning/phases/23-snapshot-create-orchestration-sync-gate/23-CONTEXT.md (verify gate semantics for §9),
    cmd/dfsctl/commands/share/snapshot/*.go (actual flag names + help text — for §3 walkthroughs),
    pkg/controlplane/models/errors.go (error sentinel names for §11).
  </read_first>
  <action>
    Create `docs/SNAPSHOTS.md` with this exact structure (13 sections per D-25-20). Target 400–600 lines.

    1. **Header:** `# Share Snapshots` + 2-line subtitle + Table of Contents with anchors `[Section](#section-name)`.

    2. **§1 Overview** (~30 lines): What a snapshot is (point-in-time reference, metadata + manifest), what guarantees it gives (CAS holds the referenced blocks; metadata fully restorable), what it is NOT (not a portable archive — those ship in v0.17.0; not encrypted; not cross-share). Two-sentence comparison to deprecated v0.13.0 backup.

    3. **§2 Snapshot model** (~40 lines): metadata dump + hash manifest + GC hold. Diagram (ASCII):
       ```
       Snapshot
         ├─ metadata.dump         ← engine-native serialization
         ├─ manifest.json         ← BLAKE3 hashes of every referenced block
         └─ (GC hold)             ← manifest-on-disk = held; GC skips
       ```
       State machine: `creating → ready` or `creating → failed`. "Manifest-on-disk = held" invariant.

    4. **§3 CLI walkthrough** (~80 lines): Worked transcript for each of `create`, `list`, `show`, `delete`. Each transcript uses synthetic `/photos` share + plausible UUIDs. Run the actual `dfsctl ... --help` output style; do not invent flags. Pattern:
       ```
       $ dfsctl share snapshot create /photos
       Snapshot 7a3e... created (state: creating)
       → ready (3s)
       $
       ```
       Cover JSON/YAML output modes with `-o json` examples.

    5. **§4 Creating a snapshot** (~40 lines): Document `--name`, `--no-verify`, `--retry`, `--no-wait` semantics. Worked transcript for `--no-verify` (skip verify gate but GC hold still applies — explain the trade) and `--retry=<failed-id>` (resume from a prior failure).

    6. **§5 Listing & inspecting** (~30 lines): table-column meanings (ID is 8-char short of UUID; SIZE displays "-" in list and full block count in show — explain why). Filters: `--state=ready|creating|failed`, `--name-prefix=foo`. JSON/YAML modes.

    7. **§6 Deleting** (~30 lines): Y/N prompt + `--yes`; GC reclamation timing; safe to delete `pre-restore-*` safety snaps — no special-casing.

    8. **§7 Restore runbook** (~80 lines):
       ```
       1. Stop traffic (ensure no clients writing)
       2. dfsctl share disable /photos
       3. dfsctl share snapshot restore /photos <snap-id>
          (interactive Y/N; --yes to skip)
       4. Verify data integrity (sample files, check timestamps)
       5. dfsctl share enable /photos
       6. Verify safety snap exists, then delete after grace period:
          dfsctl share snapshot delete /photos <safety-snap-id>
       ```
       Full transcript example. `--force` for non-durable snapshots.

    9. **§8 Recovering from safety snapshot** (~40 lines): If restore looked successful but a bug was discovered later, restore the safety snapshot. The second restore creates ANOTHER safety snap — chain depth grows with each restore; operators clean up explicitly.

    10. **§9 The verify gate** (~30 lines): what `VerifyRemoteDurability` does (HEAD-probes every CAS block on remote with concurrency hardcoded to 16), why it matters, when it fires.

    11. **§10 GC hold semantics** (~30 lines): manifest-on-disk filter; failed-snap retention; delete-vs-GC race window.

    12. **§11 Failure modes & recovery** (~80 lines): D-24-13 taxonomy in operator language. For each of the 9 modes:
        - **share-enabled-at-restore** → exit 1 with hint; run disable first.
        - **snapshot-not-found** → 404; check `dfsctl share snapshot list`.
        - **snapshot-not-durable** → 412; re-run with `--force` if you accept the risk.
        - **metadata-dump-missing** → on-disk artifact lost; snapshot unrestorable; delete it.
        - **metadata-store-not-resetable** → should not happen in prod (memory backend only); file an issue.
        - **safety-snap-create-failed** → restore aborted; share data unchanged; investigate disk space.
        - **restore-aborted-mid-flight** → safety snap exists; re-restore the safety snap; investigate cause.
        - **post-restore-verify-failed** → safety snap exists; restore safety, investigate block availability on remote.
        - **upload-drain-timeout** during create → snapshot is `failed`; `--retry` after upload backlog drains.

    13. **§12 Limitations** (~20 lines): no cross-share restore; no encryption; no auto-cleanup of safety snaps; sync restore (HTTP request blocks; bounded by `snapshot.restore_http_timeout`, default 30m); single-node v0.16.0; no scheduled snapshots.

    14. **§13 REST API reference (brief)** (~30 lines): the endpoint table from `<interfaces>` block above + a brief paragraph on auth (admin only, JWT) + a note that the full spec lives in OpenAPI (TBD).

    Cross-doc links: link to `docs/ARCHITECTURE.md#share-snapshots` for design detail; link to `docs/CLI.md#dfsctl-share-snapshot` for flag reference.
  </action>
  <verify>
    <automated>test -f docs/SNAPSHOTS.md &amp;&amp; wc -l docs/SNAPSHOTS.md | awk '{ exit ($1 &gt;= 400 ? 0 : 1) }' &amp;&amp; grep -q "## Restore runbook\\|## Restore" docs/SNAPSHOTS.md &amp;&amp; grep -q "manifest.*held\\|hold semantics" docs/SNAPSHOTS.md &amp;&amp; grep -q "no-verify\\|--no-verify" docs/SNAPSHOTS.md &amp;&amp; grep -q "--force" docs/SNAPSHOTS.md &amp;&amp; grep -q "ErrSnapshotNotDurable\\|not remotely durable\\|not durable" docs/SNAPSHOTS.md</automated>
  </verify>
  <done>
    `docs/SNAPSHOTS.md` exists with all 13 sections, ≥400 lines, mentions every flag from Plan 25-03 (`--no-verify`, `--retry`, `--no-wait`, `--name`, `--state`, `--name-prefix`, `--no-relative`, `--yes`, `--force`), covers all 9 D-24-13 failure modes in §11, and uses "verify gate" terminology (no "sync gate" prose).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update docs/ARCHITECTURE.md + docs/CLI.md (DOC-02 + DOC-03)</name>
  <files>docs/ARCHITECTURE.md, docs/CLI.md</files>
  <read_first>
    docs/ARCHITECTURE.md (full file — find GC section + insertion point for Snapshots section, search for "BackupHoldProvider"),
    docs/CLI.md (full file — find dfsctl share section + Phase 14 migrate section for adjacency pattern),
    cmd/dfsctl/commands/share/snapshot/*.go (actual flag names + help strings — DOC-03 must match),
    pkg/controlplane/api/router.go (REST routes — DOC-02 architectural overview must match),
    .planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md (D-25-21).
  </read_first>
  <action>
    1. **`docs/ARCHITECTURE.md` updates:**

       a. **GC section update:** Search for any mention of `BackupHoldProvider`. Replace with `SnapshotHoldProvider`. Add a 2-sentence note: "Snapshots register a hold via `SnapshotHoldProvider`; the hold is satisfied by the existence of a manifest file on disk. GC enumerates all manifest files at sweep start and excludes the union of referenced hashes."

       b. **New section** `## Share Snapshots` (insert near other v0.16.0-era sections; pick a placement adjacent to the GC section since they are tightly coupled). ~150 lines covering:
          - **Overview** — what subsystem, where files live (`pkg/snapshot/`, `pkg/controlplane/runtime/snapshot*.go`, REST under `internal/controlplane/api/handlers/snapshot.go`, CLI under `cmd/dfsctl/commands/share/snapshot/`, shared wire DTOs under `pkg/controlplane/api/dto/`).
          - **Snapshot artifacts** — `localStoreDir/snapshots/<share>/<id>/{manifest.json,metadata.dump}` path layout; "manifest-on-disk = held" invariant.
          - **Create orchestration** (text-flow diagram):
            ```
            CreateSnapshot ─→ persist Snapshot row (state=creating)
                          ─→ DrainAllUploads (unless NoVerify)
                          ─→ Dump metadata to metadata.dump
                          ─→ Build hash manifest from CAS
                          ─→ VerifyRemoteDurability (unless NoVerify, concurrency=16)
                          ─→ Update row state=ready (or failed)
            ```
          - **Restore orchestration** (8-step):
            ```
            RestoreSnapshot ─→ Pre-flight: refuse if share enabled
                            ─→ Verify remote durability (unless AllowNonDurable)
                            ─→ Pre-restore safety snapshot (ID returned to caller)
                            ─→ Close metadata store
                            ─→ Reset (via Resetable interface)
                            ─→ Restore from metadata.dump
                            ─→ HashSetFromMetadataStore walk
                            ─→ Post-restore block verify
            ```
            Note that `Runtime.RestoreSnapshot` returns `(safetySnapshotID, err)` so REST + CLI surfaces can present the safety snap to the operator without a follow-up lookup.
          - **Per-share delete lock** — `SnapshotHoldProvider.AcquireDeleteLock(share)` serializes create/delete on the same share; documented as the reason `Runtime.DeleteSnapshot` is the canonical entry point.
          - **HTTP surface** — link to `docs/SNAPSHOTS.md#rest-api-reference`.
          - **Reference to operator guide** — explicit link `For operator procedures see [SNAPSHOTS.md](SNAPSHOTS.md).`

    2. **`docs/CLI.md` updates:**

       a. Place a new section `### Share Snapshots (v0.16.0)` adjacent to existing v0.16.0 share-management sections (or after the Phase 14 Block Store Migration section to mirror the established docs/CLI.md adjacency pattern).

       b. Section structure:
          - Heading + 2-sentence introduction.
          - Subsection per leaf command (`#### dfsctl share snapshot create`, `... list`, `... show`, `... delete`, `... restore`).
          - Each subcommand: synopsis line + flag table + 2–3 usage examples + exit-code table.

          Example flag table for `create`:
          | Flag | Default | Description |
          |---|---|---|
          | `--name` | `""` | Human-friendly name for the snapshot. |
          | `--no-verify` | `false` | Skip the verify gate (drain + remote HEAD probes). GC hold still applies. |
          | `--retry` | `""` | Resume from a failed snapshot ID. |
          | `--no-wait` | `false` | Return immediately with the new ID; do not block on `ready`. |

       c. Cross-link to `SNAPSHOTS.md` at the top of the section: `For workflows, recovery procedures, and the verify-gate explanation see [SNAPSHOTS.md](SNAPSHOTS.md).`

       d. Verify every flag listed matches a flag declared in `cmd/dfsctl/commands/share/snapshot/*.go`. If a flag was added or removed in Plan 25-03 that this plan does not yet reflect, update the table — docs must match shipped code.

    3. No GSD metadata in prose. Decision IDs are valid in `.planning/` references only (already prefixed with `.planning/...`), not in narrative docs prose.
  </action>
  <verify>
    <automated>grep -q "SnapshotHoldProvider" docs/ARCHITECTURE.md &amp;&amp; ! grep -q "BackupHoldProvider" docs/ARCHITECTURE.md &amp;&amp; grep -q "## Share Snapshots\\|Share Snapshots" docs/ARCHITECTURE.md &amp;&amp; grep -q "dfsctl share snapshot create\\|share snapshot create" docs/CLI.md &amp;&amp; grep -q "dfsctl share snapshot restore\\|share snapshot restore" docs/CLI.md &amp;&amp; grep -q -- "--no-verify" docs/CLI.md &amp;&amp; grep -q -- "--force" docs/CLI.md &amp;&amp; grep -q "SNAPSHOTS.md" docs/CLI.md docs/ARCHITECTURE.md</automated>
  </verify>
  <done>
    ARCHITECTURE.md: GC section uses `SnapshotHoldProvider` (and `BackupHoldProvider` is gone everywhere); `## Share Snapshots` section exists with overview + create/restore orchestration + delete-lock + link to SNAPSHOTS.md. CLI.md: `dfsctl share snapshot` section exists with flag table for all 5 subcommands; flags match Plan 25-03 verbatim.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md (DOC-04) — replace deprecated backup paragraph with snapshots paragraph + 3-line example</name>
  <files>README.md</files>
  <read_first>
    README.md (full file — find any paragraph mentioning "backup", "v0.13.0", or "will ship in v0.16.0"),
    docs/SNAPSHOTS.md (created in Task 1 — for cross-link),
    cmd/dfsctl/commands/share/snapshot/snapshot.go (exact parent command help text).
  </read_first>
  <action>
    1. Search README.md for any paragraph mentioning:
       - "backup will ship in v0.16.0"
       - "v0.13.0 backup"
       - "snapshots/backup are unreleased" or similar phrasing.

    2. Delete the entire deprecated paragraph(s).

    3. Insert a replacement section in the equivalent slot. Suggested text:

       ```markdown
       ### Share Snapshots

       DittoFS supports point-in-time **share snapshots** for protection
       against accidental writes or deletes. A snapshot is a reference —
       the metadata dump plus a manifest of every CAS block the share
       references at snapshot time. The block-store GC holds those blocks
       until the snapshot is explicitly deleted.

       ```bash
       dfsctl share snapshot create /photos
       dfsctl share snapshot list /photos
       dfsctl share disable /photos && dfsctl share snapshot restore /photos <id>
       ```

       See [docs/SNAPSHOTS.md](docs/SNAPSHOTS.md) for the full operator
       guide including the restore runbook, the verify gate, and recovery
       from the safety snapshot.
       ```

    4. If README.md has a "Features" or "What works" list, add a bullet:
       `- Share snapshots with reference holds (no data copy) and CLI/REST restore`.

    5. If there is a "Roadmap" / "Coming soon" section that lists `v0.16.0: backup`, change it to `v0.16.0: share snapshots ✓` (or remove if already-shipped items are not listed).
  </action>
  <verify>
    <automated>! grep -E "backup will ship in v0\\.16|v0\\.13\\.0 backup" README.md &amp;&amp; grep -q "Share Snapshots\\|share snapshot create" README.md &amp;&amp; grep -q "docs/SNAPSHOTS.md" README.md &amp;&amp; grep -q "dfsctl share snapshot create /" README.md</automated>
  </verify>
  <done>
    README.md no longer mentions "backup will ship in v0.16.0" or "v0.13.0 backup"; a "Share Snapshots" paragraph exists with a 3-line CLI example and a link to `docs/SNAPSHOTS.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator | Operators act on documented procedures. Incorrect docs cause incorrect actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Information Disclosure | Snapshot file paths in docs reveal internal layout | accept | Paths are operator-facing (operators need them for triage). No secrets in path layout. |
| T-25-04-02 | Tampering | Stale docs documenting non-existent flags | mitigate | Every flag in docs/CLI.md and docs/SNAPSHOTS.md cross-checked against `cmd/dfsctl/commands/share/snapshot/*.go` via grep gates in `<verify>`. |
| T-25-04-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan. |
</threat_model>

<verification>
- `test -f docs/SNAPSHOTS.md`
- `wc -l docs/SNAPSHOTS.md` ≥ 400
- `grep -q "## Restore runbook\\|## Restore" docs/SNAPSHOTS.md`
- `grep -q "SnapshotHoldProvider" docs/ARCHITECTURE.md`
- `! grep -q "BackupHoldProvider" docs/ARCHITECTURE.md`
- `grep -q "dfsctl share snapshot create" docs/CLI.md`
- `! grep -E "backup will ship in v0\\.16|v0\\.13\\.0 backup" README.md`
- For every flag declared in `cmd/dfsctl/commands/share/snapshot/*.go`, grep matches in `docs/CLI.md` (manual cross-check; record results in SUMMARY)
- For each documented REST endpoint in SNAPSHOTS.md §13, grep matches in `pkg/controlplane/api/router.go`
- `! grep -q "sync gate\\|sync_gate" docs/SNAPSHOTS.md docs/ARCHITECTURE.md docs/CLI.md README.md` (verify-vocabulary consistency)
</verification>

<success_criteria>
1. DOC-01 satisfied: `docs/SNAPSHOTS.md` is the canonical operator guide.
2. DOC-02 satisfied: `docs/ARCHITECTURE.md` references `SnapshotHoldProvider` (not `BackupHoldProvider`) and has a Snapshots section.
3. DOC-03 satisfied: `docs/CLI.md` has a `dfsctl share snapshot` section with flag tables for all 5 subcommands.
4. DOC-04 satisfied: `README.md` features snapshots instead of the deprecated backup paragraph.
5. No "sync gate" prose remains in any of the 4 docs files — the Plan 25-01 rename is reflected throughout the operator-facing surface.
</success_criteria>

<output>
Create `.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-04-SUMMARY.md` when done. Include: line counts for SNAPSHOTS.md per section, the list of every flag cross-checked against the CLI source, and the list of REST endpoints cross-checked against the router.
</output>
