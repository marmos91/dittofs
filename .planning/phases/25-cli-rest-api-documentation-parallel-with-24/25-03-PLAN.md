---
phase: 25-cli-rest-api-documentation-parallel-with-24
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - pkg/apiclient/snapshots.go
  - pkg/apiclient/snapshots_test.go
  - pkg/apiclient/client.go
  - cmd/dfsctl/commands/share/snapshot/snapshot.go
  - cmd/dfsctl/commands/share/snapshot/create.go
  - cmd/dfsctl/commands/share/snapshot/list.go
  - cmd/dfsctl/commands/share/snapshot/show.go
  - cmd/dfsctl/commands/share/snapshot/delete.go
  - cmd/dfsctl/commands/share/snapshot/restore.go
  - cmd/dfsctl/commands/share/snapshot/list_test.go
  - cmd/dfsctl/commands/share/snapshot/restore_test.go
  - cmd/dfsctl/commands/share/snapshot/delete_test.go
  - cmd/dfsctl/commands/share/share.go
  - cmd/dfsctl/cmdutil/util.go
  - cmd/dfsctl/cmdutil/util_test.go
  - test/e2e/snapshot/snapshot_cli_test.go
# Note: file count (16) is granular-by-design — 5 CLI leaf files + 3 CLI tests
# + 2 cmdutil files + 5 apiclient files + share.go registration + 1 e2e file.
# Each is small and cohesive; combining would inflate single-file complexity.
autonomous: true
requirements:
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
must_haves:
  truths:
    - "`dfsctl share snapshot create <share>` blocks until snap reaches ready/failed; --no-wait returns immediately with the new ID; --no-verify maps to NoVerify; --retry=<id> maps to RetryOf"
    - "`dfsctl share snapshot list <share>` prints table with columns ID(8-char) NAME STATE DURABLE CREATED SIZE; --state and --name-prefix filter; -o json|yaml works"
    - "`dfsctl share snapshot show <share> <id>` prints full detail view with manifest count + dump bytes + paths"
    - "`dfsctl share snapshot delete <share> <id>` prompts Y/N; --yes skips; on confirm calls DELETE endpoint"
    - "`dfsctl share snapshot restore <share> <id>` refuses on enabled share with exact hint string; on disabled share prompts Y/N; --yes skips; --force maps to allow_non_durable=true; on success prints 'Safety snap: <id>' line sourced from RestoreSnapshotResponse.SafetySnapshotID"
    - "apiclient.Client exposes CreateSnapshot, ListSnapshots, GetSnapshot, DeleteSnapshot, RestoreSnapshot, WaitForSnapshot — all 6 methods with typed Snapshot DTO"
    - "apiclient builder accepts a per-call WithRestoreTimeout(time.Duration) option (or equivalent) that overrides http.Client.Timeout for the restore call"
    - "apiclient re-exports / type-aliases dto.Snapshot (and its request/response DTOs) from pkg/controlplane/api/dto — no parallel struct definitions"
  artifacts:
    - path: "pkg/apiclient/snapshots.go"
      provides: "Re-exported DTO aliases + 6 client methods"
      contains: "func (c *Client) CreateSnapshot"
    - path: "cmd/dfsctl/commands/share/snapshot/snapshot.go"
      provides: "Parent cobra Cmd + init() wiring 5 leaves"
    - path: "cmd/dfsctl/commands/share/share.go"
      provides: "Cmd.AddCommand(snapshot.Cmd) line in init()"
      contains: "snapshot.Cmd"
    - path: "test/e2e/snapshot/snapshot_cli_test.go"
      provides: "Built dfsctl binary against in-process server — exercises table/JSON/YAML output, prompts, --yes, exit codes"
  key_links:
    - from: "CLI restore"
      to: "shareEnabled check → exit 1 with hint"
      via: "client.GetShare + Share.Enabled bool"
      pattern: "share is enabled"
    - from: "apiclient.RestoreSnapshot"
      to: "POST /api/v1/shares/{name}/snapshots/{id}/restore"
      via: "longer http.Client.Timeout (default 30m)"
      pattern: "RestoreSnapshot"
    - from: "pkg/apiclient/snapshots.go"
      to: "pkg/controlplane/api/dto.Snapshot"
      via: "type alias (`type Snapshot = dto.Snapshot`) or direct embed"
      pattern: "controlplane/api/dto"
---

<objective>
Ship the operator-facing CLI surface plus the typed Go client that backs it.

- `pkg/apiclient/snapshots.go`: re-export (via type alias) the wire DTOs from `pkg/controlplane/api/dto` (created in Plan 25-01) + 6 client methods (Create, List, Get, Delete, Restore, Wait) using existing `getResource[T]` / `createResource[T]` helpers. **No struct duplication** — the wire contract is the same `dto.Snapshot` used by the handler.
- `cmd/dfsctl/commands/share/snapshot/`: 5 leaf cobra commands (create, list, show, delete, restore) + parent `snapshot.go` that wires them. Mirrors `cmd/dfsctl/commands/share/permission/` exactly.
- `apiclient.Client` builder: accept a per-call restore-timeout override (default 30m) so `RestoreSnapshot` does not get killed by the default 30s `http.Client.Timeout`.
- E2E CLI test (`test/e2e/snapshot/snapshot_cli_test.go`): built `dfsctl` binary against an in-process server, exercises table/JSON/YAML output, Y/N prompt + `--yes`, exit codes for refused-on-enabled-share.

Covers requirements API-02, API-03, API-04, API-05, API-06, API-07.

**Parallel-with-25-02 viability:** This plan depends only on Plan 25-01 (the wire DTO and the extended `Runtime.RestoreSnapshot` signature). The apiclient code consumes the DTO directly; it does NOT need 25-02's handler to be built first because the contract is locked in `pkg/controlplane/api/dto/snapshot.go`. The unit tests use a stubserver that mimics the contract. The end-to-end CLI test in Task 3 DOES need the 25-02 handler stack at execution time — but only at execution time, not at plan-writing time. The orchestrator can choose to run Task 3 last or run it after both 25-02 and 25-03 main tasks have shipped.

Purpose: Operators interact with snapshots primarily through CLI. This plan turns the wire contract into a usable tool.

Output: 5 working CLI commands, 6 typed apiclient methods, CLI unit tests, e2e binary test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md
@.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-01-PLAN.md
@./CLAUDE.md

@cmd/dfsctl/commands/share/permission/permission.go
@cmd/dfsctl/commands/share/permission/grant.go
@cmd/dfsctl/commands/share/permission/list.go
@cmd/dfsctl/commands/share/share.go
@cmd/dfsctl/commands/share/delete.go
@cmd/dfsctl/cmdutil/util.go
@pkg/apiclient/client.go
@pkg/apiclient/helpers.go
@pkg/apiclient/blockstore.go
@pkg/controlplane/api/dto/snapshot.go

<interfaces>
**Wire DTOs come from `pkg/controlplane/api/dto` (defined in Plan 25-01).** This plan re-exports them as type aliases so existing apiclient consumers see `apiclient.Snapshot` rather than `dto.Snapshot`:

```go
package apiclient

import "github.com/marmos91/dittofs/pkg/controlplane/api/dto"

// Type aliases for ergonomic consumption. No struct redefinition.
type Snapshot = dto.Snapshot
type CreateSnapshotRequest = dto.CreateSnapshotRequest
type CreateSnapshotResponse = dto.CreateSnapshotResponse
type RestoreSnapshotRequest = dto.RestoreSnapshotRequest
type RestoreSnapshotResponse = dto.RestoreSnapshotResponse
```

**6 client methods:**
```go
func (c *Client) CreateSnapshot(share string, req CreateSnapshotRequest) (*CreateSnapshotResponse, error)
func (c *Client) ListSnapshots(share string) ([]Snapshot, error)
func (c *Client) GetSnapshot(share, id string) (*Snapshot, error)
func (c *Client) DeleteSnapshot(share, id string) error
func (c *Client) RestoreSnapshot(share, id string, req RestoreSnapshotRequest) (*RestoreSnapshotResponse, error)
func (c *Client) WaitForSnapshot(ctx context.Context, share, id string, pollEvery time.Duration) (*Snapshot, error)
```

URL pattern (every method):
```go
path := fmt.Sprintf("/api/v1/shares/%s/snapshots", url.PathEscape(normalizeShareNameForAPI(share)))
```

**apiclient.Client builder change:**
Read `pkg/apiclient/client.go` for the existing `New`/`NewWithToken` constructor pattern. Add an optional functional-option `WithRestoreTimeout(d time.Duration)` (or a builder method, whichever matches existing style). The restore method uses this timeout on a cloned http.Client when calling RestoreSnapshot; all other methods retain the default. Default = 30m matching `cfg.Snapshot.RestoreHTTPTimeout`.

**Cobra leaf-cmd pattern (read `cmd/dfsctl/commands/share/permission/grant.go` verbatim):**
- Package `snapshot`
- `var cmdName = &cobra.Command{ Use: "create <share>", ... }`
- Inside `RunE`: `client, err := cmdutil.GetAuthenticatedClient(); ...`
- Output format dispatch via `cmdutil.GetOutputFormatParsed()` → switch over JSON/YAML/table.
- Parent `snapshot.go` exports `var Cmd = &cobra.Command{Use: "snapshot", ...}` + `init() { Cmd.AddCommand(createCmd); ... }`.

**Confirmation prompt helper:**
Read `cmd/dfsctl/commands/share/delete.go` to see how it handles confirm. If no reusable helper exists, **add** `cmdutil.ConfirmDestructive(prompt string, yes bool) (bool, error)` in `cmd/dfsctl/cmdutil/util.go`.

**Pre-flight share-enabled check (restore.go):**
Read `pkg/apiclient/shares.go` for the existing `GetShare` method. If the returned share has `Enabled == true`, print the exact hint string to stderr and exit 1:
```
share /<name> is enabled; run 'dfsctl share disable /<name>' first
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apiclient DTO aliases + 6 typed client methods + WithRestoreTimeout option</name>
  <files>pkg/apiclient/snapshots.go, pkg/apiclient/snapshots_test.go, pkg/apiclient/client.go</files>
  <read_first>
    pkg/apiclient/client.go (constructor + Client struct fields),
    pkg/apiclient/helpers.go (getResource[T], createResource[T]),
    pkg/apiclient/blockstore.go (DTO + method pattern — verbatim template),
    pkg/apiclient/shares.go (GetShare pattern — needed by Task 3 restore preflight),
    pkg/controlplane/api/dto/snapshot.go (the shared wire DTO — IMPORT and re-export, do not redeclare),
    test files in pkg/apiclient/ for the stubserver pattern,
    .planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md (CLI / REST decisions).
  </read_first>
  <behavior>
    - `CreateSnapshot` POSTs to `/api/v1/shares/{name}/snapshots`, decodes 202 body → CreateSnapshotResponse.
    - `ListSnapshots` GETs and decodes a JSON array; nil response → empty slice.
    - `GetSnapshot` GETs `/api/v1/shares/{name}/snapshots/{id}`.
    - `DeleteSnapshot` DELETEs the same path; 204 → nil error.
    - `RestoreSnapshot` POSTs to `/snapshots/{id}/restore` with longer http.Client.Timeout (30m default or per-call override). Response includes `SafetySnapshotID` which originates from the handler's direct read of `Runtime.RestoreSnapshot` return.
    - `WaitForSnapshot(ctx, share, id, pollEvery)` polls GetSnapshot every `pollEvery` (default 500ms) until `state != "creating"` or ctx done; returns the final snapshot or ctx.Err().
    - URL share-name encoding uses `url.PathEscape(normalizeShareNameForAPI(share))` — same as blockstore.go.
    - Errors with problem+json bodies decode through existing `Errors` infrastructure.
  </behavior>
  <action>
    1. **Create `pkg/apiclient/snapshots.go`** starting with the type aliases:
       ```go
       package apiclient

       import (
           "github.com/marmos91/dittofs/pkg/controlplane/api/dto"
       )

       type Snapshot = dto.Snapshot
       type CreateSnapshotRequest = dto.CreateSnapshotRequest
       type CreateSnapshotResponse = dto.CreateSnapshotResponse
       type RestoreSnapshotRequest = dto.RestoreSnapshotRequest
       type RestoreSnapshotResponse = dto.RestoreSnapshotResponse
       ```
       Then the 6 client methods follow. Each body follows the `blockstore.go` template:
       - `CreateSnapshot`: `path := fmt.Sprintf("/api/v1/shares/%s/snapshots", url.PathEscape(normalizeShareNameForAPI(share))); return createResource[CreateSnapshotResponse](c, path, req)`. If `createResource` gates strictly on 200/201, add `createResourceAccepted[T]` next to it for 202.
       - `ListSnapshots`: use `getResource[[]Snapshot]` (verify the existing helper handles slice T; if not, inline a GET helper).
       - `GetSnapshot`: `getResource[Snapshot]`.
       - `DeleteSnapshot`: no return body; inline a small DELETE helper if none exists (look for an existing `deleteResource` in helpers.go).
       - `RestoreSnapshot`: same pattern as `CreateSnapshot` but on the longer-timeout http.Client (see step 3).
       - `WaitForSnapshot`: implement loop with `time.NewTicker(pollEvery)`; on each tick call `c.GetSnapshot(share, id)`; return when `state != "creating"`.

    2. **Test file `pkg/apiclient/snapshots_test.go`** — use the existing stubserver pattern. Tests:
       - `TestCreateSnapshot`: stub returns 202 + body; assert client returns parsed response.
       - `TestListSnapshots`: stub returns 200 + array; assert slice length + first element fields.
       - `TestListSnapshots_Empty`: stub returns `[]`; assert empty slice, no error.
       - `TestGetSnapshot`: 200 + body; 404 + problem+json (assert error decodes).
       - `TestDeleteSnapshot`: 204; assert nil error. 404; assert error.
       - `TestRestoreSnapshot`: 200 + body including `safety_snapshot_id` field; assert `resp.SafetySnapshotID` round-trips; 412 (problem+json with type for not-durable) → assert error type.
       - `TestWaitForSnapshot`: stub returns `state=creating` twice then `state=ready`; assert eventual ready return.
       - `TestWaitForSnapshot_ContextCanceled`: cancel context after 1 poll; assert ctx.Err returned.

    3. **`pkg/apiclient/client.go`** — read the constructor. Add `restoreHTTPTimeout time.Duration` field to `Client` (default 30 * time.Minute set in `New`). Add functional option:
       ```go
       func WithRestoreTimeout(d time.Duration) ClientOption { return func(c *Client) { c.restoreHTTPTimeout = d } }
       ```
       In `RestoreSnapshot`, introduce a small internal helper that builds a per-call http.Client using `c.restoreHTTPTimeout` (cloning `c.http`). Do NOT mutate `c.http` directly (not thread-safe). Godoc the option with a one-line behavioral summary; do not embed decision IDs.
  </action>
  <verify>
    <automated>go test ./pkg/apiclient/ -run 'TestCreateSnapshot|TestListSnapshots|TestGetSnapshot|TestDeleteSnapshot|TestRestoreSnapshot|TestWaitForSnapshot' -v -count=1 &amp;&amp; go build ./...</automated>
  </verify>
  <done>
    Six client methods exist with the signatures above; DTOs are type aliases of `dto.Snapshot` (no redeclaration); stubserver tests cover happy paths + 4xx error decoding + safety_snapshot_id round-trip + WaitForSnapshot polling and cancellation; `WithRestoreTimeout` option overrides http timeout for the restore call only; no GSD metadata in source comments/godoc.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cobra leaf commands — snapshot.go parent + create/list/show/delete/restore + share.go registration + cmdutil.ConfirmDestructive</name>
  <files>cmd/dfsctl/commands/share/snapshot/snapshot.go, cmd/dfsctl/commands/share/snapshot/create.go, cmd/dfsctl/commands/share/snapshot/list.go, cmd/dfsctl/commands/share/snapshot/show.go, cmd/dfsctl/commands/share/snapshot/delete.go, cmd/dfsctl/commands/share/snapshot/restore.go, cmd/dfsctl/commands/share/snapshot/list_test.go, cmd/dfsctl/commands/share/snapshot/restore_test.go, cmd/dfsctl/commands/share/snapshot/delete_test.go, cmd/dfsctl/commands/share/share.go, cmd/dfsctl/cmdutil/util.go, cmd/dfsctl/cmdutil/util_test.go</files>
  <read_first>
    cmd/dfsctl/commands/share/permission/permission.go (parent Cmd + init pattern — copy verbatim),
    cmd/dfsctl/commands/share/permission/grant.go (write-op pattern with body + table renderer),
    cmd/dfsctl/commands/share/permission/list.go (multi-row table renderer pattern),
    cmd/dfsctl/commands/share/delete.go (Y/N confirmation pattern),
    cmd/dfsctl/cmdutil/util.go (existing helpers — GetAuthenticatedClient, GetOutputFormatParsed),
    cmd/dfsctl/commands/share/share.go (where to register snapshot.Cmd),
    pkg/apiclient/shares.go (GetShare for enabled-check preflight),
    .planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md (CLI decisions).
  </read_first>
  <action>
    1. **`cmd/dfsctl/cmdutil/util.go`** — read for existing confirm helper. If absent, add:
       ```go
       var ConfirmInput io.Reader = os.Stdin

       // ConfirmDestructive prints prompt to stdout and reads a Y/N answer from
       // ConfirmInput. If yes==true, returns (true, nil) without prompting.
       func ConfirmDestructive(prompt string, yes bool) (bool, error) {
           if yes { return true, nil }
           fmt.Println(prompt)
           fmt.Print("Type 'y' to confirm: ")
           reader := bufio.NewReader(ConfirmInput)
           line, err := reader.ReadString('\n')
           if err != nil && err != io.EOF { return false, err }
           return strings.HasPrefix(strings.ToLower(strings.TrimSpace(line)), "y"), nil
       }
       ```
       Add a `util_test.go` case asserting yes-flag short-circuit + y-input + n-input + EOF behavior.

    2. **`cmd/dfsctl/commands/share/snapshot/snapshot.go`** — parent cobra Cmd. Mirror `permission/permission.go` exactly:
       ```go
       package snapshot

       var Cmd = &cobra.Command{
           Use:   "snapshot",
           Short: "Manage share snapshots (create, list, show, delete, restore)",
       }

       func init() {
           Cmd.AddCommand(createCmd)
           Cmd.AddCommand(listCmd)
           Cmd.AddCommand(showCmd)
           Cmd.AddCommand(deleteCmd)
           Cmd.AddCommand(restoreCmd)
       }
       ```

    3. **`create.go`** — implements `dfsctl share snapshot create <share>`:
       - Flags: `--name string`, `--no-verify bool`, `--retry string`, `--no-wait bool`.
       - RunE: `client, err := cmdutil.GetAuthenticatedClient()`; `resp, err := client.CreateSnapshot(share, apiclient.CreateSnapshotRequest{Name: name, NoVerify: noVerify, RetryOf: retry})`; print "Snapshot {resp.SnapshotID} created (state: creating)".
       - If `--no-wait`: print snap ID and exit 0.
       - Else: `ctx := cmd.Context(); snap, err := client.WaitForSnapshot(ctx, share, resp.SnapshotID, 500*time.Millisecond)`. Print "→ ready" on `snap.State=="ready"` (exit 0) or `Snapshot failed: {snap.Error}` on `state=="failed"` (exit 1).
       - Output format: respect `cmdutil.GetOutputFormatParsed()` for JSON/YAML.

    4. **`list.go`** — implements `dfsctl share snapshot list <share>`:
       - Flags: `--state string`, `--name-prefix string`, `--no-relative bool`.
       - RunE: `snaps, err := client.ListSnapshots(share)`; apply filters in-process; render via output-format dispatch.
       - Table renderer: 6 columns ID(8-char) NAME STATE DURABLE CREATED SIZE. SIZE is "-" in list (handler does not populate ManifestCount in list mode).
       - Newest-first sort.

    5. **`show.go`** — implements `dfsctl share snapshot show <share> <id>`:
       - RunE: `snap, err := client.GetSnapshot(share, id)`. Detail-view renderer.

    6. **`delete.go`** — implements `dfsctl share snapshot delete <share> <id>`:
       - Flags: `--yes bool`.
       - RunE: prompt via `cmdutil.ConfirmDestructive(...)`. On confirm: `client.DeleteSnapshot(share, id)`; print "Snapshot {id} deleted."

    7. **`restore.go`** — implements `dfsctl share snapshot restore <share> <id>`:
       - Flags: `--yes bool`, `--force bool` (= `RestoreSnapshotRequest.AllowNonDurable`).
       - Pre-flight: `s, err := client.GetShare(share)`. If `s.Enabled`, print exact hint string to stderr and exit 1:
         ```
         share /<name> is enabled; run 'dfsctl share disable /<name>' first
         ```
       - Prompt: `cmdutil.ConfirmDestructive(...)`.
       - On confirm: `resp, err := client.RestoreSnapshot(share, id, apiclient.RestoreSnapshotRequest{AllowNonDurable: force})`.
       - On success: print
         ```
         Restored snapshot <id> into share <name>.
         Safety snap: <resp.SafetySnapshotID> (delete with 'dfsctl share snapshot delete <share> <safety-id>' after verifying).
         ```
         If `SafetySnapshotID == ""`, omit the safety-snap line.
       - On 412: print "Snapshot {id} is not remotely durable. Re-run with --force to restore anyway." + exit 1.

    8. **`cmd/dfsctl/commands/share/share.go`** — add `import "github.com/.../cmd/dfsctl/commands/share/snapshot"` and in `init()` add `Cmd.AddCommand(snapshot.Cmd)` next to `Cmd.AddCommand(permission.Cmd)`.

    9. **Unit tests** (`list_test.go`, `restore_test.go`, `delete_test.go`) — use a fake `apiclient.Client` (interface-based fake or substitute `cmdutil.GetAuthenticatedClient` via test indirection). Cover:
       - `list_test.go`: empty list → renders header only; multi-row → assert column count + ID truncation to 8 chars + relative time formatting; `-o json` → JSON output; `--state=ready` filters; `--name-prefix=foo` filters.
       - `delete_test.go`: `--yes` skips prompt and calls Delete; "n" answer aborts; "y" answer calls Delete.
       - `restore_test.go`: enabled-share → exit 1 + exact hint string; disabled-share + "n" → aborts; disabled-share + "y" + 200 → prints safety snap line (from `RestoreSnapshotResponse.SafetySnapshotID`); empty SafetySnapshotID → no safety snap line; disabled-share + 412 → "Re-run with --force" + exit 1.

       Test prompt injection: `cmdutil.ConfirmInput = strings.NewReader("y\n")` before invoking the command.

    10. No GSD metadata in any prescribed comment, godoc, or test name.
  </action>
  <verify>
    <automated>go test ./cmd/dfsctl/commands/share/snapshot/... -v -count=1 &amp;&amp; go test ./cmd/dfsctl/cmdutil/ -v -count=1 &amp;&amp; go build ./cmd/dfsctl/...</automated>
  </verify>
  <done>
    5 leaf commands compile, run, and respect the flag set; share.go init wires `snapshot.Cmd`; `ConfirmDestructive` helper exists with tests; CLI unit tests cover prompt + filter + format dispatch + restore preflight + safety-snap line rendering; `go build ./cmd/dfsctl/...` exits 0; no GSD metadata in source.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CLI e2e test — built dfsctl binary against in-process server covers table/JSON/YAML + prompt + exit codes</name>
  <files>test/e2e/snapshot/snapshot_cli_test.go</files>
  <read_first>
    test/e2e/snapshot/main_test.go (created in Plan 25-02 Task 3),
    test/e2e/snapshot/snapshot_http_test.go (in-process stack from Plan 25-02 — reuse),
    cmd/dfsctl/commands/share/snapshot/ (binary surface to invoke),
    .planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-CONTEXT.md (CLI e2e decision),
    test/e2e/server_test.go (existing dfsctl binary-build pattern, if any).
    Note: this task assumes Plan 25-02's handler stack exists in the tree. The orchestrator should schedule Task 3 after both 25-02 main tasks and 25-03 Tasks 1–2 have shipped.
  </read_first>
  <action>
    1. **Build dfsctl once per test run:** `go build -o /tmp/dfsctl-e2e ./cmd/dfsctl` inside `TestMain`. Cache the path in a package-level var.

    2. **`snapshot_cli_test.go` cases** — each invokes `/tmp/dfsctl-e2e share snapshot ...` via `exec.Command` against the in-process httptest server from Plan 25-02 Task 3. Use a temp config file or env vars to point the binary at the test server with an admin JWT.
       - `TestCLI_CreateNoWait`: `--no-wait` → exit 0; stdout contains the snap ID.
       - `TestCLI_CreateBlock`: blocking create → exit 0; stdout contains `→ ready`.
       - `TestCLI_List_TableMode`: prints table with 6 columns; ID column is 8 chars.
       - `TestCLI_List_JSON`: `-o json` → valid JSON array, parsable into `[]apiclient.Snapshot`.
       - `TestCLI_List_YAML`: `-o yaml` → valid YAML.
       - `TestCLI_Show`: detail view contains all DTO fields.
       - `TestCLI_Delete_RefusesWithoutYes`: pipe "n\n" to stdin → exit 0 with "aborted"; snap still present.
       - `TestCLI_Delete_YesFlag`: `--yes` → exit 0; snap absent on next list.
       - `TestCLI_Restore_RefusesEnabled`: enable share → invoke restore → exit code != 0; stderr contains hint.
       - `TestCLI_Restore_Disabled_YesFlag_Success`: disable share → `--yes` restore → exit 0; stdout contains "Safety snap:" line.
       - `TestCLI_Restore_NotDurable_NoForce`: create snap with `--no-verify` → exit code != 0; stderr suggests `--force`.
       - `TestCLI_Restore_NotDurable_Force`: same setup → `--force --yes` → exit 0.

    3. Each test cleans up its snapshots after assertions.

    4. Keep behind `-tags=e2e`.
  </action>
  <verify>
    <automated>go test -tags=e2e ./test/e2e/snapshot/ -run 'TestCLI_' -v -count=1 -timeout=120s</automated>
  </verify>
  <done>
    Built-binary CLI test passes for table/JSON/YAML/prompt/exit-code cases; restore-preflight hint string exact match asserted; not-durable + --force flow asserted; safety-snap line presence asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal → CLI | Operator input crosses into argv/flags. CLI is run by trusted operator (admin). |
| CLI → API | Auth token threads through `cmdutil.GetAuthenticatedClient`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Repudiation | "I never confirmed the delete" | mitigate | Y/N prompt logs a single line on confirm; `--yes` flag opts the operator into uniform UX. |
| T-25-03-02 | Tampering | Restore --force misuse on non-durable snapshot | accept | Operator-facing flag; SNAPSHOTS.md documents consequences. |
| T-25-03-03 | Denial of Service | Long-running restore blocking CLI session | mitigate | `WithRestoreTimeout` defaults to 30m, matches server-side timeout. |
| T-25-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- `go test ./pkg/apiclient/ -count=1` passes
- `go test ./cmd/dfsctl/commands/share/snapshot/... -count=1` passes
- `go test ./cmd/dfsctl/cmdutil/ -count=1` passes
- `go build ./cmd/dfsctl/...` exit 0
- `go test -tags=e2e ./test/e2e/snapshot/ -run 'TestCLI_' -count=1 -timeout=120s` passes
- `grep -n "snapshot.Cmd" cmd/dfsctl/commands/share/share.go` returns ≥1 match
- `grep -c "func (c \\*Client) \\(CreateSnapshot\\|ListSnapshots\\|GetSnapshot\\|DeleteSnapshot\\|RestoreSnapshot\\|WaitForSnapshot\\)" pkg/apiclient/snapshots.go` returns 6
- `grep -n "type Snapshot = dto.Snapshot\\|type CreateSnapshotRequest = dto" pkg/apiclient/snapshots.go` returns ≥2 matches (aliases in place, no redeclaration)
- `grep -n "type Snapshot struct" pkg/apiclient/snapshots.go` returns 0 matches (no local struct duplication)
- `grep -rEn "D-[0-9]+-[0-9]+|Phase [0-9]+|per Phase" pkg/apiclient/snapshots.go pkg/apiclient/client.go cmd/dfsctl/commands/share/snapshot/ cmd/dfsctl/cmdutil/util.go` returns zero matches in this plan's edits
</verification>

<success_criteria>
1. Requirements API-02..API-07 all delivered: 5 CLI subcommands + 6 typed client methods.
2. CLI behavioral contracts honored exactly: block-by-default create, restore preflight on enabled share with the exact hint string, --force/--yes flag names.
3. Table output: 6 columns, ID truncated to 8 chars, newest-first.
4. JSON/YAML output round-trips through `apiclient.Snapshot` (= `dto.Snapshot`).
5. apiclient re-exports `dto.Snapshot` and the four other DTOs via type aliases — no struct duplication.
6. e2e CLI test validates the operator surface end-to-end against an in-process server.
7. No GSD metadata in source comments/godoc/test names.
</success_criteria>

<output>
Create `.planning/phases/25-cli-rest-api-documentation-parallel-with-24/25-03-SUMMARY.md` when done. Include: final shape of `WithRestoreTimeout`, exact `ConfirmDestructive` signature shipped, the table-renderer column widths, and confirmation that DTOs are aliases (no redeclaration).
</output>
