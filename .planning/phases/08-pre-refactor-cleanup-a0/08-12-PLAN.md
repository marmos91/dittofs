---
phase: 08-pre-refactor-cleanup-a0
plan: 12
type: execute
wave: 2
depends_on: [08-11]
files_modified:
  - go.mod
  - go.sum
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "`go mod tidy` leaves no residual direct dependency that was exclusively backup-consumed."
    - "If backup was the only consumer of `go.opentelemetry.io/otel`, that dependency is dropped from go.mod. Otherwise it is kept."
    - "`go build ./...` and `go test -race ./...` pass."
  artifacts:
    - path: go.mod
      provides: "Tidied dependency set with orphaned direct deps removed."
    - path: go.sum
      provides: "Regenerated checksum file."
  key_links:
    - from: go.mod
      to: (dependency graph)
      via: "go mod tidy"
      pattern: ""
---

<objective>
PR-B commit 7 (D-30 step 7) — Final PR-B step. Run `go mod tidy`; audit for orphaned direct deps post-backup-removal; drop `go.opentelemetry.io/otel` from `go.mod` IFF it was exclusively backup-consumed (per D-27 / D-28).

Purpose: Dependency hygiene. Close out PR-B cleanly.
Output: One atomic commit with go.mod + go.sum changes only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: go mod tidy + OTel audit (D-27, D-28, D-30 step 7)</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-27, D-28, D-30 step 7)
    - go.mod (current direct deps list — take a baseline)
    - go.sum (baseline)
  </read_first>
  <action>
    Step 1 (OTel audit) — Determine if the OTel dep is backup-only:
      `grep -rn "go.opentelemetry.io/otel" . --include='*.go' | grep -v "^vendor/"` → list every import site.
      - If ZERO matches remain (we deleted them all in prior commits), OTel is orphaned → it should be removed by tidy.
      - If non-backup matches remain (any file not previously deleted by PR-B), keep OTel.

    Step 2 (tidy) —
      `go mod tidy -v 2>&1 | tee /tmp/tidy.log`
      Review /tmp/tidy.log for "unused" / "removing" lines. Confirm the net effect matches audit expectations.

    Step 3 (verify) —
      `go build ./...` exits 0.
      `go test -count=1 -short -race ./...` exits 0.
      `git diff go.mod` shows only removals (or the expected upgrade/retain shape). `git diff go.sum` shows matching checksum updates.
      If OTel audit said "orphaned" but tidy did NOT drop it, investigate — something still references it; find and delete it, rerun tidy.

    Step 4 (commit) — signed:
      `git commit -S -m "build: go mod tidy after v0.13.0 backup removal (TD-03)"`
  </action>
  <verify>
    <automated>go mod tidy &amp;&amp; go build ./... &amp;&amp; go test -count=1 -short -race ./... &amp;&amp; test -f go.mod &amp;&amp; test -f go.sum</automated>
  </verify>
  <acceptance_criteria>
    - `go mod tidy` exits 0 with no manual follow-up needed (diff is clean on rerun).
    - `go build ./...` exits 0.
    - `go test -count=1 -short -race ./...` exits 0.
    - If the OTel audit found zero non-backup consumers, `grep "go.opentelemetry.io/otel" go.mod` → 0 matches.
    - If the OTel audit found remaining consumers, OTel remains in go.mod (documented in SUMMARY).
    - `git log -1 --format=%s` = `build: go mod tidy after v0.13.0 backup removal (TD-03)`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    go.mod tidied; OTel decision documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency supply chain (go.mod) | Removing a dep reduces attack surface; adding none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12-01 | T | Tidy accidentally bumps minor versions of unrelated deps | accept | `tidy` does not change versions; it only prunes. If a bump appears, investigate in review. |
| T-08-12-02 | I | Orphaned OTel package left in vendor if vendored | mitigate | If `vendor/` exists, plan includes a `go mod vendor` rerun; otherwise N/A. |
</threat_model>

<verification>
- `go mod tidy` idempotent (rerun shows no diff).
- Build + tests green.
</verification>

<success_criteria>
- D-27 / D-28 / D-30 step 7 complete. PR-B ready to open.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-12-SUMMARY.md` — commit SHA, list of deps removed by tidy, OTel-audit result and decision.
</output>
