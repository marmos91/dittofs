---
phase: 08-pre-refactor-cleanup-a0
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - pkg/blockstore/sync/syncer.go
  - pkg/blockstore/sync/syncer_test.go
autonomous: true
requirements: [TD-02]
must_haves:
  truths:
    - "syncFileBlock returns (or logs and returns) the error from PutFileBlock instead of swallowing it with `_ =`."
    - "Upstream caller (syncer loop / per-block sync) observes the error and treats it as a sync failure (retry / error counter increment / error log at Error level), consistent with other error paths in the syncer."
    - "Unit test fails with a stubbed PutFileBlock returning a sentinel error and asserts the error is observable upstream."
  artifacts:
    - path: pkg/blockstore/sync/syncer.go
      provides: "syncFileBlock propagates PutFileBlock errors (no `_ =`)."
    - path: pkg/blockstore/sync/syncer_test.go
      provides: "Regression test: PutFileBlock error is surfaced by syncFileBlock."
  key_links:
    - from: pkg/blockstore/sync/syncer.go (syncFileBlock)
      to: pkg/blockstore/sync/syncer.go (caller of syncFileBlock)
      via: "return err / error counter / Error log"
      pattern: "PutFileBlock\\(.+\\)[^_]|return.*err"
---

<objective>
PR-A commit 2 — Fix TD-02b: `syncFileBlock` silently swallows `PutFileBlock` return via `_ =`. Replace with proper error propagation so sync failures are observable.

Purpose: HIGH-severity bug fix (ROADMAP.md §Phase 08 Success Criteria #2).
Output: One atomic commit; `go test -race ./pkg/blockstore/sync/...` passes including a new regression test that asserts PutFileBlock errors surface upstream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/blockstore/sync/syncer.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate syncFileBlock errors (TD-02b)</name>
  <files>pkg/blockstore/sync/syncer.go, pkg/blockstore/sync/syncer_test.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, §"TD-02 bug sites")
    - pkg/blockstore/sync/syncer.go (focus: `syncFileBlock` definition + all its callers in the file; understand return signature and the sibling sync/fetch error paths for the pattern to match)
    - pkg/blockstore/sync/syncer_test.go (existing test style and any stub/mock Remote/FileBlockStore helpers already present)
    - pkg/blockstore/store.go (error conventions — any wrapping with `metadata.ExportError`?)
    - CLAUDE.md §"Architecture invariants" rule 6 (expected errors at Debug, unexpected at Error)
  </read_first>
  <behavior>
    - RED test: `TestSyncFileBlock_PropagatesPutError` — construct a minimal syncer with an `RemoteBlockStore` (or whatever abstraction PutFileBlock lives on — inspect syncer.go) fake that returns a sentinel `errBoom := errors.New("boom")`. Drive `syncFileBlock` against it. Assert the returned error wraps (or equals) errBoom; or — if syncFileBlock returns no error but increments a metric/sets a state — assert that side-effect happens. The test MUST fail on the pre-fix code (current `_ =` swallows the error).
    - GREEN implementation: In `syncFileBlock`, replace the `_ = rs.PutFileBlock(...)` (or equivalent) with error capture. If `syncFileBlock` already has an error-return, return the wrapped error (e.g., `return fmt.Errorf("put file block: %w", err)`). If it does not have an error-return, audit the caller: add a return and let the caller handle it as a sync failure (retry / error counter / Error log). Match the existing failure-handling pattern used elsewhere in syncer.go (e.g., for `GetFileBlock` errors).
    - REFACTOR: Keep log-level conventions (CLAUDE.md rule 6): `metadata.ExportError` values at Debug, unexpected at Error. Do NOT introduce new error types — plain `fmt.Errorf(..., %w, err)` is sufficient.
  </behavior>
  <action>
    Step 1 (RED) — Read `pkg/blockstore/sync/syncer.go` to find the exact `syncFileBlock` function and the PutFileBlock call. Locate its signature and all callers. Write the regression test in `syncer_test.go` that stubs PutFileBlock to return a sentinel error and asserts the error is observable.

    Step 2 (GREEN) — In `pkg/blockstore/sync/syncer.go`:
    - Find `_ = .*PutFileBlock\(` (or equivalent error-swallow). Remove the `_ =`. Capture the err.
    - If syncFileBlock already returns `error`, wrap and return: `if err != nil { return fmt.Errorf("put file block %q: %w", key, err) }`.
    - If it does not return error, add `error` to its return signature and propagate from every caller. Update callers to treat the error symmetrically with existing peer-operation failures (GetFileBlock / Delete paths).
    - Preserve cancellation semantics: if the context is cancelled, return ctx.Err() as in peer paths.

    Step 3 (verify) — `go test -race ./pkg/blockstore/sync/...`, `go build ./...`, `go vet ./pkg/blockstore/...`. All green.

    Step 4 (commit) — Stage only the modified files:
      `git commit -S -m "fix(blockstore): propagate syncFileBlock PutFileBlock errors (TD-02b)"`
    No Claude Code mention. No Co-Authored-By.
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/sync/... &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_ = .*PutFileBlock" pkg/blockstore/sync/syncer.go | wc -l` → 0.
    - `grep -n "PutFileBlock" pkg/blockstore/sync/syncer.go` shows the call with error capture (e.g., `err := ...PutFileBlock(` or `if err := ...PutFileBlock(`).
    - `grep -n "TestSyncFileBlock_PropagatesPutError\|TestSyncFileBlock.*Error" pkg/blockstore/sync/syncer_test.go` returns at least one match.
    - `go test -race ./pkg/blockstore/sync/...` exits 0.
    - `go build ./...` exits 0.
    - `git log -1 --format=%s` starts with `fix(blockstore): propagate syncFileBlock`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    PutFileBlock errors are observable. Regression test locks the contract. Atomic commit, signed, independently green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local syncer → remote block store | Outbound data plane; silent errors can cause durability illusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | R (Repudiation) | syncFileBlock silently swallows remote Put failures | mitigate | Fix propagates the error so ops + metrics can detect durability loss. Previously the system would claim blocks were synced when they weren't. |
| T-08-02-02 | I (Information disclosure) | error wrapping | accept | We wrap with %w; the error may contain keys/paths. Consistent with existing peer paths; not exposed to end users (internal log path). |
</threat_model>

<verification>
- `go build ./...` green.
- `go test -race ./pkg/blockstore/sync/...` green.
- Commit signed, message matches convention.
</verification>

<success_criteria>
- PutFileBlock errors are surfaced upstream (test-enforced).
- D-11 atomic commit preserved.
- Lands on `develop` (D-12).
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-02-SUMMARY.md` — files touched, test name, commit SHA, any caller-signature changes.
</output>
