---
phase: 08-pre-refactor-cleanup-a0
plan: 04
type: execute
wave: 1
depends_on: [08-03]
files_modified:
  - pkg/blockstore/local/fs/fs.go
  - pkg/blockstore/local/fs/write.go
  - pkg/blockstore/local/fs/eviction.go
  - pkg/blockstore/local/fs/fs_test.go
autonomous: true
requirements: [TD-02]
must_haves:
  truths:
    - "Local tier write hot path makes zero calls to anything named FileBlockStore or its interface methods."
    - "Eviction is driven by on-disk state (stat/walk/index) only, not by FileBlockStore queries."
    - "All existing unit + integration tests for the local tier still pass."
  artifacts:
    - path: pkg/blockstore/local/fs/write.go
      provides: "Write hot path with zero FileBlockStore dependency."
    - path: pkg/blockstore/local/fs/eviction.go
      provides: "Eviction decisions driven by on-disk state only."
    - path: pkg/blockstore/local/fs/fs_test.go
      provides: "Assertion/test preventing regression of the decoupling."
  key_links:
    - from: pkg/blockstore/local/fs/write.go
      to: on-disk state (filesystem walk / local index)
      via: "os.Stat / filepath.Walk / in-memory disk index"
      pattern: "FileBlockStore"
---

<objective>
PR-A commit 4 — Fix TD-02d + enforce D-19: eliminate `FileBlockStore` calls on the local tier's write hot path. Eviction (and any ancillary logic that currently pokes `FileBlockStore`) must be driven from on-disk state only. Keeps the architecture aligned with STATE-03 which A2 will further enforce.

Purpose: HIGH-severity bug fix; reduces cross-layer coupling; prerequisite for A1 groundwork.
Output: One atomic commit; tests prove hot path is decoupled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/blockstore/local/fs/fs.go
@pkg/blockstore/local/fs/write.go
@pkg/blockstore/local/fs/eviction.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate local write hot path from FileBlockStore (TD-02d / D-19)</name>
  <files>pkg/blockstore/local/fs/fs.go, pkg/blockstore/local/fs/write.go, pkg/blockstore/local/fs/eviction.go, pkg/blockstore/local/fs/fs_test.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, D-19, §"TD-02 bug sites")
    - pkg/blockstore/local/fs/fs.go (full file — map where FileBlockStore is stored on the struct and how it's consulted)
    - pkg/blockstore/local/fs/write.go (full file — identify every FileBlockStore call in the write path)
    - pkg/blockstore/local/fs/eviction.go (full file — identify eviction's use of FileBlockStore)
    - pkg/blockstore/local/fs/fs_test.go (test harness + existing mocks/fakes)
    - CLAUDE.md §"Architecture invariants" (esp. per-share block stores and write ordering)
  </read_first>
  <behavior>
    - Baseline: Identify every call site in write.go / eviction.go that dispatches to anything called `FileBlockStore` (method call, struct field use, interface type). Enumerate them (will be referenced in the SUMMARY).
    - GREEN: Replace each hot-path call with on-disk-state equivalent:
      - For "is block present?" → `os.Stat(blockPath)` or the local in-memory disk index (if one exists; otherwise lazy Stat is acceptable for Phase 08).
      - For "record a new block" → filesystem-level write only; drop FileBlockStore bookkeeping on the hot path. FileBlockStore consumers outside the hot path (e.g., refcount, sync) may remain but MUST NOT be called from Write.
      - Eviction: `filepath.Walk` the cache dir (or use the existing on-disk index) and evict by on-disk size/mtime rather than FileBlockStore queries.
    - RED-ish assertion: Add `TestLocalWritePath_NoFileBlockStoreCall` which uses a wrapper FileBlockStore that counts calls, runs a representative Write + evict cycle, and asserts the counter is 0 for the hot path. Test MUST fail before the change.
    - REFACTOR: Remove now-unreachable code paths; preserve public API of `FSStore`.
  </behavior>
  <action>
    Step 1 (inventory) — `grep -n "FileBlockStore" pkg/blockstore/local/fs/{fs,write,eviction}.go` and list every call site. Also check the struct: `grep -n "FileBlockStore" pkg/blockstore/local/fs/*.go`.

    Step 2 (RED test) — In `fs_test.go` add `TestLocalWritePath_NoFileBlockStoreCall`:
      - Define `type callCounter struct{ ... }` implementing the same interface the real `FileBlockStore` does (inspect fs.go's usage).
      - Construct FSStore with the counter.
      - Perform a typical write (WriteAt on a few blocks).
      - Trigger the write hot path end-to-end including the spot that currently called FileBlockStore.
      - Assert counter.calls == 0 for the hot-path methods.
    - Also test eviction: trigger eviction (set small budget) and assert counter.calls == 0.

    Step 3 (GREEN) —
      - In `write.go`: remove every FileBlockStore call along the write path. If a call is needed for non-hot-path maintenance, gate it off the hot path (e.g., via async bookkeeping or syncer). Use disk state (`os.Stat`, mtime, size) or an on-process local index.
      - In `eviction.go`: replace FileBlockStore queries with `filepath.Walk(localDir)` or the existing disk index. Sort candidates by mtime/size; evict atomically via file-rename-then-unlink if current code expects atomicity.
      - In `fs.go`: if the FileBlockStore field is no longer used at all on the hot path, keep it only if a non-hot-path consumer exists; otherwise reduce the surface (minimal change — do NOT remove field in this commit if any other file uses it).

    Step 4 (verify) — `go test -race ./pkg/blockstore/local/fs/... ./pkg/blockstore/engine/...`, `go build ./...`, `go vet ./pkg/blockstore/...`. Confirm the new assertion passes AND existing tests still pass.

    Step 5 (commit) — Stage only the modified files:
      `git commit -S -m "fix(blockstore): isolate local write path from FileBlockStore (TD-02d)"`
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/local/fs/... ./pkg/blockstore/engine/... &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FileBlockStore" pkg/blockstore/local/fs/write.go | wc -l` → 0.
    - `grep -n "FileBlockStore" pkg/blockstore/local/fs/eviction.go | wc -l` → 0.
    - `grep -n "TestLocalWritePath_NoFileBlockStoreCall\|TestLocalWrite.*NoFileBlockStore" pkg/blockstore/local/fs/fs_test.go` returns ≥ 1 match.
    - `go test -race ./pkg/blockstore/local/fs/...` exits 0.
    - `go build ./...` exits 0.
    - `git log -1 --format=%s` starts with `fix(blockstore): isolate local write path from FileBlockStore (TD-02d)`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    Hot path decoupled from FileBlockStore. Tests prove it. One atomic, signed commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local store write path → internal FileBlockStore | Internal; no external attacker reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T (Tampering) | Decoupling may miss an edge case where FileBlockStore held authoritative state | mitigate | Counter-based test + existing localtest conformance suite catches behavioral regressions. If an existing test breaks, investigate before proceeding. |
| T-08-04-02 | E (EoP) | None | n/a | No auth surface; purely internal refactor. |
</threat_model>

<verification>
- `go build ./...` green; `go test -race ./pkg/blockstore/local/fs/...` green.
- `grep -n "FileBlockStore" pkg/blockstore/local/fs/write.go pkg/blockstore/local/fs/eviction.go | wc -l` → 0.
- Commit signed.
</verification>

<success_criteria>
- D-11 atomic commit; D-19 invariant established.
- Regression test prevents backslide.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-04-SUMMARY.md` — files touched, enumeration of removed call sites, test name, commit SHA.
</output>
