---
phase: 08-pre-refactor-cleanup-a0
plan: 06
type: execute
wave: 2
depends_on: [08-05]
files_modified:
  - test/e2e/backup_chaos_test.go
  - test/e2e/backup_matrix_test.go
  - test/e2e/backup_restore_mounted_test.go
  - test/e2e/backup_test.go
  - test/e2e/helpers/backup.go
  - test/e2e/helpers/backup_metadata.go
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "All v0.13.0 backup e2e tests and e2e helpers are deleted."
    - "`go build ./...` and `go test -race ./...` pass (outside e2e sudo suite)."
    - "E2E suite compiles (`go build ./test/e2e/...`) with zero unused imports."
  artifacts: []
  key_links:
    - from: test/e2e/
      to: "(deleted backup tests)"
      via: "git rm"
      pattern: "backup_.*_test\\.go"
---

<objective>
PR-B commit 1 (D-30 step 1) — Delete all v0.13.0 backup e2e tests + helpers. This is the dependency-leaf-first step; removing tests is safe because pkg/backup still exists under them. After this commit the rest of PR-B can proceed without e2e compile breakage.

Purpose: Per D-26, start the backup removal from the leaves (tests) so each subsequent commit stays green. D-30 step 1/7.
Output: One atomic commit deleting 6 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove v0.13.0 backup e2e tests + helpers (D-26, D-30 step 1)</name>
  <files>test/e2e/backup_chaos_test.go, test/e2e/backup_matrix_test.go, test/e2e/backup_restore_mounted_test.go, test/e2e/backup_test.go, test/e2e/helpers/backup.go, test/e2e/helpers/backup_metadata.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, D-26, D-30, D-29)
    - test/e2e/helpers/backup.go (to understand if any non-backup test imports anything from it — we must keep those alive or migrate them)
    - test/e2e/helpers/backup_metadata.go (same check)
    - test/e2e/README.md (confirm no test runner script hard-codes the deleted filenames)
  </read_first>
  <action>
    Step 1 (audit) — Confirm no non-backup test imports from the soon-to-be-deleted helpers:
      `grep -rn "helpers.Backup\|helpers\\..*Backup\|backupMetadata\\|helpers\\.BackupMetadata" test/ --include='*.go' | grep -v "backup_"` → expect 0 matches. If any appear, STOP and migrate them (move symbols to a non-backup helper file) before deletion; otherwise proceed.
      `grep -rn "backup" test/e2e/run-e2e.sh test/e2e/README.md test/e2e/BENCHMARKS.md 2>/dev/null | grep -v "pkg/backup\|runtime/storebackups" | head` — note any script references to backup tests (harmless: they fail to match a non-existent file).

    Step 2 (delete) —
      ```bash
      git rm test/e2e/backup_chaos_test.go \
             test/e2e/backup_matrix_test.go \
             test/e2e/backup_restore_mounted_test.go \
             test/e2e/backup_test.go \
             test/e2e/helpers/backup.go \
             test/e2e/helpers/backup_metadata.go
      ```

    Step 3 (verify) —
      `go build ./test/e2e/... ./test/e2e/helpers/...` must exit 0.
      `go test ./... -count=1 -short -race` must exit 0 (use `-short` to skip sudo-required e2e runs; existing unit + integration tests exercise enough).
      `grep -rn "test/e2e/backup\|helpers/backup" test/ --include='*.go'` → 0 matches.

    Step 4 (commit) — signed:
      `git commit -S -m "test: remove v0.13.0 backup e2e tests (TD-03)"`
  </action>
  <verify>
    <automated>test ! -f test/e2e/backup_chaos_test.go &amp;&amp; test ! -f test/e2e/backup_matrix_test.go &amp;&amp; test ! -f test/e2e/backup_restore_mounted_test.go &amp;&amp; test ! -f test/e2e/backup_test.go &amp;&amp; test ! -f test/e2e/helpers/backup.go &amp;&amp; test ! -f test/e2e/helpers/backup_metadata.go &amp;&amp; go build ./test/e2e/... &amp;&amp; go test -count=1 -short -race ./...</automated>
  </verify>
  <acceptance_criteria>
    - 6 backup test/helper files no longer exist (all `test ! -f ...` conditions pass).
    - `go build ./test/e2e/... ./test/e2e/helpers/...` exits 0.
    - `go test -count=1 -short -race ./...` exits 0.
    - `grep -rn "helpers\\.Backup\|helpers\\.BackupMetadata" test/ --include='*.go'` → 0 matches.
    - `git log -1 --format=%s` = `test: remove v0.13.0 backup e2e tests (TD-03)`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    E2E backup tests and helpers deleted. Repo still compiles and -short tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test code only) | No runtime surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | R (Repudiation) | Loss of test coverage for routes that still exist (pkg/backup not yet deleted) | accept | Intentional per D-29: compile+router tests cover residual routes. The routes themselves will be deleted in subsequent PR-B commits. |
| T-08-06-02 | I | E2E helper might hold auth secrets in test fixtures | accept | Tests use test-only credentials; no production secrets affected. |
</threat_model>

<verification>
- Files deleted; `go build ./...` and `-short` test suite green.
- Commit signed, conventional message.
</verification>

<success_criteria>
- D-30 step 1 complete. Independently green (D-11).
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-06-SUMMARY.md` — commit SHA, confirmation of audit results.
</output>
