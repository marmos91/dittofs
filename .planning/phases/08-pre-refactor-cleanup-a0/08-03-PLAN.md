---
phase: 08-pre-refactor-cleanup-a0
plan: 03
type: execute
wave: 1
depends_on: [08-02]
files_modified:
  - pkg/blockstore/engine/engine.go
  - pkg/blockstore/engine/engine_test.go
autonomous: true
requirements: [TD-02]
must_haves:
  truths:
    - "engine.Delete removes on-disk .blk files for the deleted payload (via DeleteAllBlockFiles or equivalent)."
    - "After engine.Delete(payloadID), no .blk file remains under the local store dir for that payloadID."
    - "Integration/unit test covers: write blocks → Delete → assert zero .blk files remain."
  artifacts:
    - path: pkg/blockstore/engine/engine.go
      provides: "Delete invokes DeleteAllBlockFiles on the local store."
    - path: pkg/blockstore/engine/engine_test.go
      provides: "Regression test asserting disk cleanup after Delete."
  key_links:
    - from: pkg/blockstore/engine/engine.go (Delete)
      to: pkg/blockstore/local/fs (DeleteAllBlockFiles)
      via: "e.local.DeleteAllBlockFiles(ctx, payloadID) or equivalent"
      pattern: "DeleteAllBlockFiles"
---

<objective>
PR-A commit 3 — Fix TD-02c: `engine.Delete` currently leaves `.blk` files on disk (no `DeleteAllBlockFiles` call). Add the call; assert disk cleanup in a test.

Purpose: HIGH-severity bug fix. Prevents orphan `.blk` leaks that grow unbounded.
Output: One atomic commit; integration test proves disk cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/blockstore/engine/engine.go
@pkg/blockstore/store.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine.Delete calls DeleteAllBlockFiles (TD-02c)</name>
  <files>pkg/blockstore/engine/engine.go, pkg/blockstore/engine/engine_test.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, §"TD-02 bug sites")
    - pkg/blockstore/engine/engine.go (focus: Delete function; trace to local store dispatch)
    - pkg/blockstore/local/fs/fs.go (to confirm the `DeleteAllBlockFiles` method exists and signature; if absent, also inspect `manage.go`, `eviction.go`)
    - pkg/blockstore/engine/engine_test.go (test helper style — how are test FSStores / engines constructed?)
    - pkg/blockstore/store.go (BlockStore interface — confirm Delete and DeleteAllBlockFiles contracts)
  </read_first>
  <behavior>
    - RED test: `TestEngineDelete_RemovesBlockFiles` — create a temp-dir FSStore, wire an engine, write N blocks via engine.Write or engine.syncFileBlock, assert `len(filepath.Glob(tmp + "/**/*.blk"))` > 0, call `engine.Delete(ctx, payloadID)`, assert the glob returns 0. Test MUST fail on current code.
    - GREEN implementation: In `engine.Delete`, after the metadata-store delete (or in the correct ordering consistent with CLAUDE.md invariant #5 for deletions), call the local store's `DeleteAllBlockFiles(ctx, payloadID)`. Handle the error: return it; do not swallow. Preserve existing behavior (remote delete, refcount maintenance, etc.).
    - REFACTOR: Match the Close/Delete patterns already in engine.go. No new exported APIs.
  </behavior>
  <action>
    Step 1 (read) — `grep -n "func.*Delete\b" pkg/blockstore/engine/engine.go` to find the exact Delete method. `grep -n "DeleteAllBlockFiles" pkg/blockstore/local/fs/*.go` to verify the method exists on FSStore. If the method exists on a different type (interface), route the call through the local-store field on `engine.Engine` (typically `e.local` or similar — inspect the struct).

    Step 2 (RED) — In `engine_test.go`, add `TestEngineDelete_RemovesBlockFiles`:
      - `tmp := t.TempDir()`
      - Construct an FSStore rooted at tmp and an Engine wrapping it (copy construction pattern from existing tests).
      - Write at least 2 blocks for a payloadID (using whatever existing write helper tests use — `engine.WriteAt` or through syncer).
      - Force sync so `.blk` files appear on disk (check existing tests for the sync helper).
      - Glob `filepath.Glob(filepath.Join(tmp, "**", "*.blk"))` (or walk the dir) — assert ≥1.
      - Call `engine.Delete(ctx, payloadID)`.
      - Re-glob; assert 0 `.blk` files remain for that payloadID.
    Run the test: it MUST fail.

    Step 3 (GREEN) — In `pkg/blockstore/engine/engine.go`, locate `func (e *Engine) Delete(ctx..., payloadID string) error` (or similar). Add a call to `e.local.DeleteAllBlockFiles(ctx, payloadID)` (using the correct field name — inspect the struct). Order: follow CLAUDE.md invariant #5 style for deletions (coordinate with metadata/refcount teardown). Propagate the error.

    Step 4 (verify) — `go test -race ./pkg/blockstore/engine/... ./pkg/blockstore/local/fs/...`, `go build ./...`, `go vet ./pkg/blockstore/...` — all green.

    Step 5 (commit) — Stage only the two files:
      `git commit -S -m "fix(blockstore): engine.Delete removes on-disk block files (TD-02c)"`
  </action>
  <verify>
    <automated>go test -race ./pkg/blockstore/engine/... ./pkg/blockstore/local/fs/... &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DeleteAllBlockFiles" pkg/blockstore/engine/engine.go | wc -l` ≥ 1.
    - `grep -n "TestEngineDelete_RemovesBlockFiles\|TestEngineDelete.*Block" pkg/blockstore/engine/engine_test.go` returns ≥ 1 match.
    - `go test -race ./pkg/blockstore/engine/...` exits 0.
    - `go build ./...` exits 0.
    - `git log -1 --format=%s` starts with `fix(blockstore): engine.Delete removes on-disk block files (TD-02c)`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    Delete cleans on-disk blocks. Test locks it in. Atomic commit, signed, independently green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine.Delete → local disk | Internal control plane; bug causes slow disk fill, not data leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | D (Denial of Service) | Orphan .blk files fill disk over time (delete-and-recreate workloads) | mitigate | Fix calls DeleteAllBlockFiles on local tier; prevents unbounded disk growth. |
| T-08-03-02 | I (Information disclosure) | Stale blocks on disk after Delete | accept | Orphan .blk contains only previously-authorized payload bytes; no new info disclosed. Fix also reduces residual exposure by cleaning up promptly. |
</threat_model>

<verification>
- `go build ./...` green; `go test -race ./pkg/blockstore/...` green.
- Commit signed.
</verification>

<success_criteria>
- D-11 atomic commit; independently green.
- Regression test proves behavior.
- Lands on `develop`.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-03-SUMMARY.md` — files, test name, commit SHA, any coordination notes with metadata/refcount paths.
</output>
