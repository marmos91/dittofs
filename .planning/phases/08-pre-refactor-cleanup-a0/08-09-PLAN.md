---
phase: 08-pre-refactor-cleanup-a0
plan: 09
type: execute
wave: 2
depends_on: [08-08b]
files_modified:
  - pkg/controlplane/runtime/storebackups
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "`pkg/controlplane/runtime/storebackups/` directory no longer exists."
    - "OTel span wiring that was exclusive to backup (metrics.go) is gone."
    - "`go build ./...` and `go test -count=1 -short -race ./pkg/controlplane/... ./pkg/blockstore/...` pass."
  artifacts: []
  key_links:
    - from: "(deleted) pkg/controlplane/runtime/storebackups/"
      to: ""
      via: "git rm -r"
      pattern: ""
---

<objective>
PR-B commit 6 (D-30 step 6) — Delete the entire `pkg/controlplane/runtime/storebackups/` package tree. Prior commits (08-08 unwire, 08-08a store-layer removal, 08-08b metadata-layer removal) removed every external importer; now the package can be deleted cleanly.

Purpose: Remove the storebackups package (includes `backup_hold.go`, `metrics.go` with OTel wiring, plus all siblings).
Output: One atomic commit deleting the directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove storebackups package tree (D-01, D-28, D-30 step 6)</name>
  <files>pkg/controlplane/runtime/storebackups/*.go (entire directory)</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-01, D-28, D-30 step 6)
    - pkg/controlplane/runtime/storebackups/ (directory listing — confirm contents: backup_hold.go, doc.go, errors.go, metrics.go, orphan_sweep.go, restore.go, retention.go, and their _test.go siblings)
  </read_first>
  <action>
    Step 1 (pre-check) — No external importers should remain after plans 08 + 08a + 08b:
      `grep -rn "runtime/storebackups\"\\|storebackups\\." . --include='*.go' | grep -v "pkg/controlplane/runtime/storebackups/" | wc -l` → MUST be 0. If not, STOP and finish the unwire (08, 08a, or 08b) first.

    Step 2 (delete) —
      `git rm -r pkg/controlplane/runtime/storebackups`

    Step 3 (verify) —
      `test ! -d pkg/controlplane/runtime/storebackups` passes.
      `go build ./...` exits 0.
      `go test -count=1 -short -race ./pkg/controlplane/... ./pkg/blockstore/...` exits 0.
      `grep -rn "runtime/storebackups" . --include='*.go'` → 0 matches.

    Step 4 (Claude-Code hygiene) — `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true`.

    Step 5 (commit) — signed:
      `git commit -S -m "runtime: remove storebackups package (TD-03)"`
  </action>
  <verify>
    <automated>test ! -d pkg/controlplane/runtime/storebackups &amp;&amp; go build ./... &amp;&amp; go test -count=1 -short -race ./pkg/controlplane/... ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d pkg/controlplane/runtime/storebackups` passes.
    - `grep -rn "runtime/storebackups" . --include='*.go' | wc -l` → 0.
    - `go build ./...` exits 0.
    - `go test -count=1 -short -race ./pkg/controlplane/... ./pkg/blockstore/...` exits 0.
    - `git log -1 --format=%s` = `runtime: remove storebackups package (TD-03)`.
    - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes.
    - `git log -1 --show-signature` reports Good signature.
  </acceptance_criteria>
  <done>
    Storebackups gone from pkg/controlplane/runtime/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Internal package deletion only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | I | OTel backup spans gone — some observability dashboards may show gaps | accept | Feature was never released (MEMORY.md); no production dashboard references exist. v0.16.0 backup will wire fresh telemetry. |
| T-08-09-02 | T | Accidental deletion of shared helpers | mitigate | The pre-check `grep ... | wc -l` guards against non-backup importers remaining. |
</threat_model>

<verification>
- Directory gone; build + tests green.
</verification>

<success_criteria>
- D-30 step 6 complete; independently green.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-09-SUMMARY.md` — commit SHA; count of files deleted.
</output>
