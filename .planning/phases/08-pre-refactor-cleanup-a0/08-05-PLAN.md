---
phase: 08-pre-refactor-cleanup-a0
plan: 05
type: execute
wave: 1
depends_on: [08-04]
files_modified: []
autonomous: false
requirements: [TD-02]
user_setup:
  - service: github
    why: "Update GH issue #420 description to reflect scope expansion (D-08)."
    env_vars: []
    dashboard_config:
      - task: "Ensure `gh auth status` passes for the `marmos91/dittofs` repo; otherwise run `gh auth login` (this is the only human-only step in PR-A)."
        location: "local shell"
must_haves:
  truths:
    - "GitHub issue #420 body mentions the v0.13.0 backup system removal as part of Phase 08 scope."
    - "PR-A can be opened referencing the updated issue."
  artifacts:
    - path: "github.com/marmos91/dittofs/issues/420"
      provides: "Scope-expansion announcement reflected in issue tracker."
  key_links:
    - from: "PR-A description"
      to: "issue #420"
      via: "closes/references #420"
      pattern: "#420"
---

<objective>
PR-A commit 5 — D-08 scope-expansion announcement: update GH issue #420 description BEFORE PR-A lands to reflect that Phase 08 also removes the v0.13.0 backup system. This is an external-action step (uses `gh` CLI).

Purpose: Preserve issue-tracker truth so PR-A reviewers see the full scope; satisfies D-08.
Output: Updated issue body on GitHub; no git commit in this plan (issue edit is external).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GH issue #420 to reflect backup-system removal scope (D-08)</name>
  <files></files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-08, D-01, §"Decisions → Scaffolding & Backup Removal")
    - .planning/ROADMAP.md (lines 37-58 — canonical Phase 08 description to quote into issue body)
  </read_first>
  <action>
    Step 1 — Pull current issue body:
      `gh issue view 420 --repo marmos91/dittofs --json title,body > /tmp/issue-420-before.json`

    Step 2 — Compose updated body. Start from current body; append a new `### Scope expansion (2026-04-23)` section containing this block VERBATIM:

    ```
    ### Scope expansion (2026-04-23)

    Phase 08 now also removes the v0.13.0 backup system in full. v0.13.0 was never released (see MEMORY.md), so there are no external consumers to preserve. v0.16.0 will rebuild backup atop the new CAS foundation.

    Deleted in PR-B of this phase:
    - `pkg/backup/` (manifest/scheduler/destination/executor/errors/restore — ~39 Go files)
    - `pkg/controlplane/runtime/storebackups/` (~15 files)
    - `internal/controlplane/api/handlers/backup*.go` (6 files)
    - `cmd/dfsctl/commands/store/metadata/backup/` (30+ files)
    - `pkg/apiclient/backup*.go` (4 files)
    - `test/e2e/backup_*.go` + `test/e2e/helpers/backup*.go` (6 files)
    - `docs/BACKUP.md`; backup references pruned from ARCHITECTURE.md / CLI.md / FAQ.md / README.md

    Phase 08 ships as three themed PRs:
    - PR-A — TD-02 HIGH-severity bug fixes (4 atomic commits).
    - PR-B — v0.13.0 backup removal (7 atomic commits, reverse-import order).
    - PR-C — TD-01 merge + TD-03 remnants + TD-04 parser collapse (move → delete → collapse).
    ```

    Step 3 — Apply the edit:
      `gh issue edit 420 --repo marmos91/dittofs --body-file /tmp/issue-420-new.md`
      (Write the combined body to `/tmp/issue-420-new.md` first.)

    Step 4 — Verify:
      `gh issue view 420 --repo marmos91/dittofs --json body -q .body | grep -c "Scope expansion"` → ≥ 1.
      `gh issue view 420 --repo marmos91/dittofs --json body -q .body | grep -c "pkg/backup/"` → ≥ 1.

    If `gh auth status` fails, pause and ask the user to `gh auth login`, then retry. Do NOT silently skip.
  </action>
  <verify>
    <automated>gh issue view 420 --repo marmos91/dittofs --json body -q .body | grep -c "Scope expansion" | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue view 420 --repo marmos91/dittofs --json body -q .body` contains "Scope expansion (2026-04-23)".
    - The body lists the `pkg/backup/`, `storebackups/`, `internal/controlplane/api/handlers/backup*.go`, and `cmd/dfsctl/commands/store/metadata/backup/` paths.
    - The body lists the three PR-split plan (PR-A, PR-B, PR-C).
    - `/tmp/issue-420-before.json` exists (proof the original was captured before edit).
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    Issue #420 body is updated; PR-A can now reference it accurately.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → GitHub API (via gh CLI) | Authenticated outbound; `gh auth` token scope must be verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | T (Tampering) | `gh issue edit` overwrites body | mitigate | Capture original body to `/tmp/issue-420-before.json` before edit; easy revert. |
| T-08-05-02 | I (Information disclosure) | Issue body is public | accept | No secrets in body; all content already documented in CONTEXT.md. |
</threat_model>

<verification>
- `gh issue view 420 --repo marmos91/dittofs --json body -q .body` contains "Scope expansion".
- Pre-edit backup exists in /tmp/.
</verification>

<success_criteria>
- D-08 satisfied before PR-A opens.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-05-SUMMARY.md` — confirmation of issue edit, link to the updated issue, any gh-auth complications.
</output>
