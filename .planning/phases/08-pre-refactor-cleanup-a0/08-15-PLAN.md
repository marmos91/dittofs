---
phase: 08-pre-refactor-cleanup-a0
plan: 15
type: execute
wave: 3
depends_on: [08-14]
files_modified:
  - pkg/blockstore/types.go
  - pkg/blockstore/engine/syncer.go
  - pkg/blockstore/engine/gc.go
  - pkg/blockstore/local/fs/recovery.go
  - pkg/blockstore/local/fs/manage.go
  - pkg/blockstore/types_test.go
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TD-04]
must_haves:
  truths:
    - "5 block-key parsers collapsed to 2: `ParseStoreKey` for `{payloadID}/block-{N}` and `ParseBlockID` for `{payloadID}/{blockIdx}`."
    - "`parseStoreKeyBlockIdx`, `parsePayloadIDFromBlockKey`, `parseBlockID`, `extractBlockIdx` are gone."
    - "Both canonical parsers live in `pkg/blockstore/types.go` (D-15)."
    - "REQUIREMENTS.md and ROADMAP.md TD-04 wording corrected to '5 → 2' per D-13."
    - "`go build ./...` and `go test -count=1 -race ./pkg/blockstore/...` pass."
    - "Post-merge lint sweep clean (`go vet ./pkg/blockstore/...`)."
    - "`pkg/blockstore/types_test.go` exists before RED tests are written (W12 safety net)."
  artifacts:
    - path: pkg/blockstore/types.go
      provides: "Canonical `ParseStoreKey` and new `ParseBlockID`."
    - path: pkg/blockstore/types_test.go
      provides: "Round-trip + error-path tests for both parsers."
  key_links:
    - from: (all callers)
      to: pkg/blockstore/types.go (ParseStoreKey / ParseBlockID)
      via: "function call"
      pattern: "ParseStoreKey|ParseBlockID"
---

<objective>
PR-C block 3 (D-31 step 3 — TD-04 parser collapse): Consolidate 5 parsers → 2 canonical parsers per D-13 (not 4 → 1 as ROADMAP.md originally said). Delete the duplicates at all 4 sites (now all inside `pkg/blockstore/engine/` + `pkg/blockstore/local/fs/`). Correct the wording in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`. Run the D-20 lint sweep (scoped to `./pkg/blockstore/...`).

**Critical addition (plan-checker iteration 1):**
- **W12:** `pkg/blockstore/types_test.go` does NOT currently exist (confirmed 2026-04-23 `test -f` check). Add an explicit first step that creates the file with a minimal `package blockstore` declaration before writing RED tests.
- **W17:** REQUIREMENTS.md correction grep must match the "5 → 2" / "five...two" wording explicitly (not just `ParseBlockID`).

Purpose: TD-04 complete; Phase 08 work done.
Output: One atomic commit (or split if necessary; each signed, green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/blockstore/types.go
</context>

<interfaces>
<!-- Canonical parsers after this plan: -->
<!-- (signatures are planner-discretion per D-discretion; preserve callers' expectations) -->

// ParseStoreKey parses an external store key of the form "{payloadID}/block-{N}".
// (Already present at pkg/blockstore/types.go:181 — KEEP.)
func ParseStoreKey(key string) (payloadID string, blockIdx uint64, err error)

// ParseBlockID parses an internal blockID of the form "{payloadID}/{blockIdx}".
// (NEW — planner decides final signature based on existing callers `parseBlockID`
// in pkg/blockstore/local/fs/recovery.go:124 and `extractBlockIdx` in
// pkg/blockstore/local/fs/manage.go:193.)
func ParseBlockID(blockID string) (payloadID string, blockIdx uint64, err error)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TD-04 parser collapse + lint sweep + doc correction (D-13, D-15, D-16, D-20, D-31 step 3, W12, W17)</name>
  <files>pkg/blockstore/types.go, pkg/blockstore/types_test.go, pkg/blockstore/engine/syncer.go, pkg/blockstore/engine/gc.go, pkg/blockstore/local/fs/recovery.go, pkg/blockstore/local/fs/manage.go, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-13, D-14, D-15, D-16, D-20, D-31 step 3, §"Format Split Discovered During Discuss")
    - pkg/blockstore/types.go (focus: existing `ParseStoreKey` around line 181 — read its implementation to align `ParseBlockID` style with it)
    - pkg/blockstore/engine/syncer.go (find `parseStoreKeyBlockIdx` — pre-merge was sync/syncer.go:19; post-merge lives in engine/syncer.go)
    - pkg/blockstore/engine/gc.go (find `parsePayloadIDFromBlockKey` — was at gc/gc.go:263; post-merge in engine/gc.go)
    - pkg/blockstore/local/fs/recovery.go (find `parseBlockID` at ~line 124 — inspect signature; primary model for `ParseBlockID`)
    - pkg/blockstore/local/fs/manage.go (find `extractBlockIdx` at ~line 193 — inspect; secondary model)
    - All callers: `grep -rn "parseStoreKeyBlockIdx\|parsePayloadIDFromBlockKey\|parseBlockID\b\|extractBlockIdx" pkg/ --include='*.go'`
    - .planning/REQUIREMENTS.md (line 91: TD-04 wording — `Four block-key parsers collapsed to one canonical ParseStoreKey...`)
    - .planning/ROADMAP.md (line 47: TD-04 wording — `Four block-key parsers ... collapsed to one canonical ParseStoreKey`)
  </read_first>
  <behavior>
    - RED tests (in types_test.go): add `TestParseBlockID_RoundTrip` and `TestParseBlockID_Invalid` covering:
      * Valid: `"export/docs/report.pdf/7"` -> payloadID=`"export/docs/report.pdf"`, blockIdx=7.
      * Valid: edge case with payloadID containing slashes (ensure only the final `/` separates idx).
      * Invalid: missing slash, non-integer idx, empty string, trailing slash.
    - RED confirms by running against current code (ParseBlockID doesn't exist yet — compile fail counts as RED).
    - GREEN: implement `ParseBlockID` in pkg/blockstore/types.go with same style as ParseStoreKey. Signature matches existing duplicates' shape (`(payloadID string, blockIdx uint64, err error)`).
    - Rewrite each of the four duplicate parsers' call sites to use `ParseStoreKey` or `ParseBlockID`. Delete the duplicate function definitions.
    - REFACTOR: run `gofmt`, `go vet`.
  </behavior>
  <action>
    Step 0 (W12 safety net — create `types_test.go` if absent) —
      ```bash
      test -f pkg/blockstore/types_test.go || printf "package blockstore\n" > pkg/blockstore/types_test.go
      git add pkg/blockstore/types_test.go
      ```
      (If the file already existed, this is a no-op. If it was created, commit it along with the rest of the plan's work.)

    Step 1 (audit call sites) —
      `grep -rn "parseStoreKeyBlockIdx\|parsePayloadIDFromBlockKey\|parseBlockID\b\|extractBlockIdx" pkg/ --include='*.go'` — record every line.

    Step 2 (RED tests) — In `pkg/blockstore/types_test.go` (now guaranteed to exist per Step 0), add:
      - `TestParseBlockID_RoundTrip` — table-driven with valid inputs; asserts exact (payloadID, blockIdx) pairs.
      - `TestParseBlockID_Invalid` — asserts errors for malformed inputs (missing slash, non-integer idx, empty string, trailing slash).
      - `TestParseStoreKey_RoundTrip` — if not already present, add for symmetry.
      Run `go test -run TestParseBlockID -race ./pkg/blockstore/...` — expect compile failure (ParseBlockID missing). That's RED.

    Step 3 (GREEN — add `ParseBlockID`) — In `pkg/blockstore/types.go`, right below `ParseStoreKey`, add a `ParseBlockID` implementation that splits on the LAST `/`, parses the trailing segment as `strconv.ParseUint(..., 10, 64)`, and returns wrapped errors via `fmt.Errorf(... %w ...)`. Match ParseStoreKey's style.

    Step 4 (rewrite call sites) —
      - `pkg/blockstore/engine/syncer.go`: delete `parseStoreKeyBlockIdx`. Callers call `blockstore.ParseStoreKey` from `github.com/marmos91/dittofs/pkg/blockstore`. Add the import if absent. Use `_, idx, err := blockstore.ParseStoreKey(key)` where the old helper returned just the index.
      - `pkg/blockstore/engine/gc.go`: delete `parsePayloadIDFromBlockKey`. Callers call `pid, _, err := blockstore.ParseStoreKey(key)`.
      - `pkg/blockstore/local/fs/recovery.go`: delete `parseBlockID` at line 124. Call sites use `blockstore.ParseBlockID`.
      - `pkg/blockstore/local/fs/manage.go`: delete `extractBlockIdx` at line 193. Call sites use `blockstore.ParseBlockID` (keep only `blockIdx`).

    Step 5 (verify parsers gone) — `grep -rn "parseStoreKeyBlockIdx\|parsePayloadIDFromBlockKey\|parseBlockID\b\|extractBlockIdx" pkg/ --include='*.go' | wc -l` -> 0.

    Step 6 (docs correction per D-13, W17) —
      - `.planning/REQUIREMENTS.md` line 91: replace the existing TD-04 line with:
        `- [ ] **TD-04**: Five block-key parsers collapsed to two canonical parsers (ParseStoreKey + ParseBlockID; ParseCASKey added in A2/Phase 11 per BSCAS-01).`
        The critical W17 match anchor is the phrase "Five...two" or "5 → 2" in close proximity.
      - `.planning/ROADMAP.md`: find the Phase 08 Success Criteria line about parsers (currently line ~47: "Four block-key parsers ... collapsed to one canonical ParseStoreKey"). Rewrite to: `Five block-key parsers collapsed to two canonical parsers (ParseStoreKey + ParseBlockID)`.

    Step 7 (lint sweep per D-20) — Scoped to touched packages only. `go vet ./pkg/blockstore/...` -> exit 0. If `staticcheck` is installed: `staticcheck ./pkg/blockstore/...` -> fix any new findings.

    Step 8 (verify) — `go build ./...` exits 0. `go test -count=1 -race ./pkg/blockstore/... ./pkg/controlplane/...` exits 0. New parser tests pass. Conformance suites (`pkg/metadata/storetest`, `localtest`) still pass.

    Step 9 (Claude-Code hygiene) — `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true`.

    Step 10 (commit) — signed:
      `git commit -S -m "blockstore: collapse block-key parsers 5 to 2 (TD-04)"`
  </action>
  <verify>
    <automated>test -f pkg/blockstore/types_test.go &amp;&amp; (! grep -rn "parseStoreKeyBlockIdx\|parsePayloadIDFromBlockKey\|parseBlockID\b\|extractBlockIdx" pkg/ --include='*.go') &amp;&amp; grep -q "func ParseBlockID" pkg/blockstore/types.go &amp;&amp; grep -q "func ParseStoreKey" pkg/blockstore/types.go &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/blockstore/... &amp;&amp; go test -count=1 -race ./pkg/blockstore/... ./pkg/controlplane/...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pkg/blockstore/types_test.go` passes (W12 guarantee).
    - `grep -rn "parseStoreKeyBlockIdx\|parsePayloadIDFromBlockKey\|parseBlockID\b\|extractBlockIdx" pkg/ --include='*.go' | wc -l` -> 0.
    - `grep -n "func ParseBlockID" pkg/blockstore/types.go | wc -l` -> 1.
    - `grep -n "func ParseStoreKey" pkg/blockstore/types.go | wc -l` -> 1.
    - `grep -n "TestParseBlockID_RoundTrip\|TestParseBlockID_Invalid" pkg/blockstore/types_test.go | wc -l` -> ≥ 2.
    - **W17 (tightened):** `grep -qE "[Ff]ive.*two|5.*→.*2|5 *to *2" .planning/REQUIREMENTS.md` — the explicit "5 → 2" or "Five...two" wording is present.
    - `grep -qE "[Ff]ive.*two|5.*→.*2|5 *to *2|ParseBlockID" .planning/ROADMAP.md` — wording updated.
    - `go build ./...` exits 0.
    - `go vet ./pkg/blockstore/...` exits 0.
    - `go test -count=1 -race ./pkg/blockstore/... ./pkg/controlplane/...` exits 0.
    - `git log -1 --format=%s` = `blockstore: collapse block-key parsers 5 to 2 (TD-04)`.
    - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes.
    - `git log -1 --show-signature` reports Good signature.
  </acceptance_criteria>
  <done>
    TD-04 complete. 5 parsers -> 2. Docs corrected with explicit "5 → 2" / "Five...two" wording (W17). `types_test.go` safety net in place (W12). Lint clean (scoped). PR-C work done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Block-key parsing surface | Parsers are used for internal routing; malformed keys already return errors. Consolidation cannot weaken this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15-01 | T (Tampering) | New `ParseBlockID` may silently accept malformed inputs that the old per-site parsers rejected | mitigate | Invalid-input table tests assert rejection for missing-slash, non-int index, empty, trailing slash. |
| T-08-15-02 | D (DoS) | Very long blockID strings cause slow parsing | accept | `strings.LastIndex` + `strconv.ParseUint` are O(n); inputs come from trusted internal sources only. |
| T-08-15-03 | R | REQUIREMENTS.md / ROADMAP.md doc drift | mitigate | Step 6 edits both in the same commit; W17-tightened grep ensures "5 → 2" / "Five...two" phrasing present. |
</threat_model>

<verification>
- Parsers consolidated and tested.
- `go build ./...`, `go vet ./pkg/blockstore/...`, `go test -race ./pkg/blockstore/...` all green.
- Docs updated with explicit "5 → 2" wording (W17); lint clean.
- `types_test.go` guaranteed to exist (W12).
- Commit signed; Claude-Code hygiene check passes.
</verification>

<success_criteria>
- D-13, D-15, D-16, D-20, D-31 step 3 satisfied.
- W12 (types_test.go safety net) and W17 (tightened doc grep) addressed.
- PR-C ready to open containing plans 13+14+15's commits.
- Phase 08 work complete: all four TD-01..TD-04 requirements delivered; v0.13.0 backup system fully removed; HIGH-severity bugs fixed; docs consistent.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-15-SUMMARY.md` — commit SHA(s), final grep result, lint sweep output, REQUIREMENTS/ROADMAP snippets after edit (showing explicit "5 → 2" phrasing), confirmation that types_test.go existed before tests were written (or was created by Step 0).
</output>
