---
phase: 08-pre-refactor-cleanup-a0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/blockstore/local/fs/fs.go
  - pkg/blockstore/local/fs/fs_test.go
autonomous: true
requirements: [TD-02]
must_haves:
  truths:
    - "FSStore.Start() launched goroutine terminates before Close() returns (no goroutine leak)."
    - "Unit test using goleak (or manual goroutine snapshot) confirms zero leaked goroutines after Start→Close cycle."
  artifacts:
    - path: pkg/blockstore/local/fs/fs.go
      provides: "FSStore.Start/Close goroutine lifecycle join (done chan + waitgroup)."
    - path: pkg/blockstore/local/fs/fs_test.go
      provides: "Regression test for goroutine leak (TestFSStoreStartCloseNoLeak or equivalent)."
  key_links:
    - from: pkg/blockstore/local/fs/fs.go
      to: pkg/blockstore/local/fs/fs.go (Close)
      via: "done channel closed + <-sg.wg.Wait() in Close"
      pattern: "close\\(.*done\\)|sg\\.Wait\\(\\)"
---

<objective>
PR-A commit 1 — Fix TD-02a: `FSStore.Start()` goroutine leak. The goroutine launched in `Start()` must be joined (waited on) inside `Close()` so `FSStore` teardown is deterministic with no goroutine leak.

Purpose: HIGH-severity bug fix (MEMORY.md / ROADMAP.md). Also satisfies PROJECT.md invariant "Each step must compile and pass all tests independently".
Output: One atomic commit; `go test -race ./pkg/blockstore/local/fs/...` passes including a new regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/blockstore/local/fs/fs.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Join FSStore.Start goroutine on Close (TD-02a)</name>
  <files>pkg/blockstore/local/fs/fs.go, pkg/blockstore/local/fs/fs_test.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, D-12, §"TD-02 bug sites")
    - pkg/blockstore/local/fs/fs.go (full file — understand Start(), Close(), any existing goroutines/channels)
    - pkg/blockstore/local/fs/fs_test.go (existing test style — table-driven, naming, helpers)
    - At least one sibling test file in the same package (to match project test style)
    - go.mod (check whether `go.uber.org/goleak` is already a dependency — prefer it if present; otherwise use a manual pre/post `runtime.NumGoroutine()` comparison with 100ms settle time)
  </read_first>
  <behavior>
    - RED test: In `pkg/blockstore/local/fs/fs_test.go`, add `TestFSStoreStartCloseNoGoroutineLeak` that instantiates an `FSStore`, calls `Start(ctx)`, calls `Close(ctx)`, and asserts no goroutine leak. If goleak is available, use `goleak.VerifyNone(t)`; otherwise sample `runtime.NumGoroutine()` before Start and after Close (allowing ≤0 delta, with a 100ms settle `time.Sleep`). Test MUST fail on the current code (no wait-on-goroutine).
    - GREEN implementation: In `pkg/blockstore/local/fs/fs.go`, introduce a `done chan struct{}` (or equivalent) that the Start-goroutine selects on, and a `sync.WaitGroup` field. `Start` increments `wg.Add(1)` before `go func`, the goroutine calls `wg.Done()` on exit, and also respects `<-done`. `Close` performs (a) `close(done)` exactly once (guarded by `sync.Once` if Close is callable multiple times) and (b) `wg.Wait()` before returning. Preserve any existing error-handling contract of Close.
    - REFACTOR: Keep behavior identical for callers. No exported-API change. Idempotent Close.
  </behavior>
  <action>
    Step 1 (RED) — Add the failing test first. Run `go test -race -run TestFSStoreStartCloseNoGoroutineLeak ./pkg/blockstore/local/fs/...` and confirm it FAILS with a leak diagnostic. Commit test-only would be permissible but we satisfy D-11 with one atomic commit combining test+fix (the test still runs clean because test+fix land together).

    Step 2 (GREEN) — Modify `pkg/blockstore/local/fs/fs.go`:
    - Add `done chan struct{}` and `wg sync.WaitGroup` fields to the `FSStore` struct (or whatever the concrete type launched the goroutine from — inspect Start()).
    - In `Start()`: before `go`, do `sg.wg.Add(1)` (using the struct receiver field). Inside the goroutine wrap body with `defer sg.wg.Done()`, and have its select include `case <-sg.done:` for cancellation.
    - In `Close()`: guard `close(sg.done)` with a `sync.Once` to make Close idempotent. Then `sg.wg.Wait()`. Preserve any existing Close behavior (error returns, tier teardown, etc.) AFTER the wait.
    - Preserve all existing context-cancellation paths. Do NOT remove context-based shutdown — `done` is additive.

    Step 3 (verify) — Run `go test -race ./pkg/blockstore/local/fs/...` and confirm the new test PASSES. Run `go build ./...` and `go vet ./pkg/blockstore/...`.

    Step 4 (commit) — Stage only the two modified files. Commit with (signed):
      `git commit -S -m "fix(blockstore): join FSStore.Start goroutine on Close (TD-02a)"`
    No Claude Code mention. No Co-Authored-By lines. Concise message.
  </action>
  <verify>
    <automated>go test -race -run TestFSStoreStartCloseNoGoroutineLeak ./pkg/blockstore/local/fs/... &amp;&amp; go test -race ./pkg/blockstore/local/fs/... &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0.
    - `go test -race ./pkg/blockstore/local/fs/...` exits 0.
    - `grep -n "sync.WaitGroup\|done chan" pkg/blockstore/local/fs/fs.go` shows the waitgroup+done additions.
    - `grep -n "wg.Wait\(\)" pkg/blockstore/local/fs/fs.go` appears in `Close`.
    - `grep -n "TestFSStoreStartCloseNoGoroutineLeak\|TestFSStore.*NoLeak" pkg/blockstore/local/fs/fs_test.go` returns at least one match.
    - `git log -1 --format=%s` starts with `fix(blockstore): join FSStore.Start goroutine on Close (TD-02a)`.
    - `git log -1 --show-signature` shows Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    FSStore.Close deterministically joins the Start goroutine; regression test asserts no leak; commit is atomic, signed, and independently green under `go test -race ./...`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local process (FSStore lifecycle) | Internal API; no external input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | D (Denial of Service) | FSStore goroutine leak across repeated Start/Close | mitigate | Fix is precisely the mitigation: Close waits on Start's goroutine. Prevents slow-leak OOM in long-lived processes. |
| T-08-01-02 | T (Tampering) | Test-only additions | accept | Test file changes only; no production trust surface touched. |

No auth context, PII, or network surface is exposed by this change — risk is internal-only.
</threat_model>

<verification>
- `go build ./...` green.
- `go test -race ./pkg/blockstore/local/fs/...` green including new regression test.
- `go vet ./pkg/blockstore/...` green.
- Commit is signed (`git log -1 --show-signature` passes).
- Commit message matches convention `fix(blockstore): join FSStore.Start goroutine on Close (TD-02a)`.
</verification>

<success_criteria>
- One atomic commit; independently green (per D-11 and PROJECT.md).
- Regression test locks down the fix.
- Lands on `develop` (D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/08-pre-refactor-cleanup-a0/08-01-SUMMARY.md` capturing: files touched, test name, commit SHA, any discovered complications.
</output>
