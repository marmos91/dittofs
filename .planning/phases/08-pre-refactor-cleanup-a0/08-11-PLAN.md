---
phase: 08-pre-refactor-cleanup-a0
plan: 11
type: execute
wave: 2
depends_on: [08-10]
files_modified:
  - docs/BACKUP.md
  - docs/ARCHITECTURE.md
  - docs/CLI.md
  - docs/FAQ.md
  - README.md
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "`docs/BACKUP.md` is deleted."
    - "docs/ARCHITECTURE.md, docs/CLI.md, docs/FAQ.md, README.md contain no residual references to the v0.13.0 backup system."
    - "A release-note-style line mentioning `v0.15.0 removes v0.13.0 backup system; v0.16.0 will reintroduce atop CAS` exists in README.md or an equivalent changelog."
  artifacts:
    - path: README.md
      provides: "Release-note line covering backup removal."
  key_links:
    - from: docs/BACKUP.md
      to: "(deleted)"
      via: "git rm"
      pattern: ""
---

<objective>
PR-B commit 6 (D-30 step 6) — Delete `docs/BACKUP.md`; prune backup references from `docs/ARCHITECTURE.md`, `docs/CLI.md`, `docs/FAQ.md`, `README.md`; add a v0.15.0 release-note-style line to README.md noting the removal and v0.16.0 plan.

Purpose: Docs cleanup per D-23.
Output: One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove docs/BACKUP.md + audit backup references in docs (D-23, D-24, D-30 step 6)</name>
  <files>docs/BACKUP.md, docs/ARCHITECTURE.md, docs/CLI.md, docs/FAQ.md, README.md</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-23, D-24, D-30 step 6)
    - docs/BACKUP.md (read in full before deleting — take note of any sections that uniquely document now-deleted behavior; none of it survives)
    - docs/ARCHITECTURE.md (grep for "backup" to find references)
    - docs/CLI.md (grep for "backup" references; include `dfsctl backup` subcommand docs)
    - docs/FAQ.md (grep for "backup" mentions)
    - README.md (grep for "backup", including feature lists / quickstart / versioning)
  </read_first>
  <action>
    Step 1 (audit + catalog) —
      - `grep -ni "backup" docs/ARCHITECTURE.md docs/CLI.md docs/FAQ.md README.md` → list every line. Classify each:
        - v0.13.0 backup system (DittoFS own backup) → delete
        - pgBackRest / Percona Postgres backup (via operator CRD per D-25) → KEEP (it is unrelated)
        - "backup" as general English noun/verb unrelated to DittoFS backup → KEEP
      Err on the side of deletion only when the reference is specifically to `pkg/backup`, the REST API routes, or `dfsctl backup` commands.

    Step 2 (delete) —
      `git rm docs/BACKUP.md`

    Step 3 (prune) — Edit each of ARCHITECTURE.md, CLI.md, FAQ.md, README.md:
      - Remove full sections that document the v0.13.0 backup system.
      - Remove cross-links pointing at `docs/BACKUP.md` (replace with a sentence if needed, or just strip).
      - Remove CLI.md's `dfsctl backup ...` subsection.
      - Remove FAQ entries about backup limitations.
      - Remove README feature-list bullets mentioning backup scheduling, pin, restore, etc.

    Step 4 (release-note line) — Add to README.md (under a "Changelog" section; create if missing) or append to a natural section:
      > `v0.15.0`: Removed unreleased v0.13.0 backup system (REST API, `dfsctl backup` commands, `pkg/backup`, scheduler). A new backup system built atop content-addressable storage (CAS) will ship in v0.16.0. Since v0.13.0 was never released, there is no backward-compatibility concern.

    Step 5 (verify) —
      `test ! -f docs/BACKUP.md` passes.
      `grep -rni "pkg/backup\\|dfsctl backup\\|api/v1/backups\\|BACKUP\\.md" docs/ README.md | wc -l` → 0 (only v0.13.0-specific references remain — filter pgBackRest/generic use ahead of the grep or inspect all matches and ensure they're false positives like "pg-backup" from PerconaBackupConfig docs per D-25 — keep those).
      `grep -ni "v0.15.0" README.md | grep -c "backup"` → ≥ 1 (the release-note line present).
      `go build ./...` exits 0 (docs changes shouldn't affect build, but sanity-check).

    Step 6 (commit) — signed:
      `git commit -S -m "docs: remove v0.13.0 backup docs + prune references (TD-03)"`
  </action>
  <verify>
    <automated>test ! -f docs/BACKUP.md &amp;&amp; (! grep -rni "dfsctl backup\|api/v1/backups\|BACKUP\.md" docs/ README.md) &amp;&amp; grep -q "v0.16.0.*backup\|backup.*v0.16.0\|v0.15.0.*backup" README.md &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f docs/BACKUP.md` passes.
    - `grep -rni "BACKUP\\.md" docs/ README.md | wc -l` → 0.
    - `grep -rni "dfsctl backup\|api/v1/backups" docs/ README.md | wc -l` → 0.
    - `grep -niE "v0\\.(15|16)\\.0" README.md` includes a line about backup system removal/reintroduction.
    - `go build ./...` exits 0.
    - `git log -1 --format=%s` = `docs: remove v0.13.0 backup docs + prune references (TD-03)`.
    - `git log -1 --show-signature` reports Good signature.
  - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (W14 — no Claude Code or Co-Authored-By strings in commit message).
  </acceptance_criteria>
  <done>
    Docs purged; release-note line added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs (README + docs/) | Docs are public — ensure we don't leave broken links or promise-of-feature text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-01 | R | Users stumbling onto old docs in forks could believe backup works | mitigate | Release-note line documents the removal + v0.16.0 plan. README carries forward. |
| T-08-11-02 | I | Deleting docs with sensitive info | accept | BACKUP.md contains no secrets; only design/usage docs. |
</threat_model>

<verification>
- Docs + README cleaned; release-note line exists.
- Build green.
</verification>

<success_criteria>
- D-23 / D-24 satisfied; D-30 step 6 complete.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-11-SUMMARY.md` — commit SHA; list of docs sections removed from each file.
</output>
