---
phase: 08-pre-refactor-cleanup-a0
plan: 08
type: execute
wave: 2
depends_on: [08-07]
files_modified:
  - pkg/controlplane/runtime/runtime.go
  - pkg/controlplane/runtime/blockgc.go
  - pkg/controlplane/runtime/blockgc_test.go
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "runtime.go contains zero references to the storebackups package."
    - "runtime.go has removed ALL 8 delegation methods added for backup: `RegisterBackupRepo`, `UnregisterBackupRepo`, `UpdateBackupRepo`, `RunBackup`, `ValidateBackupSchedule`, `BackupStore()`, `DestFactoryFn()`, `StoreBackupsService()` (CONTEXT lines 417-498)."
    - "blockgc.go SAFETY-01 gate is fully deleted (CONTEXT lines 42-66): the resolved-hold logic using `r.BackupStore()`, `r.DestFactoryFn()`, `storebackups.NewBackupHold`, `gc.StaticBackupHold`. `gc.CollectGarbage` is called WITHOUT `BackupHold` in `gc.Options`."
    - "`SetBackupHoldWiringForTest` function is deleted from blockgc.go."
    - "blockgc_test.go drops the storebackups import AND any `SetBackupHoldWiringForTest` call (CONTEXT lines 15, 129)."
    - "The Runtime struct has no `storeBackupsSvc` field; the `SetRestoreBumpBootVerifier` method is deleted."
    - "`go build ./...` and `go test -count=1 -race ./pkg/controlplane/runtime/... ./pkg/blockstore/...` pass."
  artifacts:
    - path: pkg/controlplane/runtime/runtime.go
      provides: "Storebackups wiring fully removed (import, field, builder calls, SetRestoreBumpBootVerifier, startup block, and all 8 backup delegation methods)."
    - path: pkg/controlplane/runtime/blockgc.go
      provides: "SAFETY-01 gate fully removed; GC wiring calls CollectGarbage with no BackupHold option."
    - path: pkg/controlplane/runtime/blockgc_test.go
      provides: "Test file drops storebackups import + SetBackupHoldWiringForTest call."
  key_links:
    - from: pkg/controlplane/runtime/runtime.go
      to: "(deleted storebackups import + 8 delegation methods)"
      via: "import list + method block"
      pattern: "runtime/storebackups|RegisterBackupRepo|UnregisterBackupRepo|UpdateBackupRepo|RunBackup|ValidateBackupSchedule|BackupStore\\(\\)|DestFactoryFn\\(\\)|StoreBackupsService\\(\\)"
    - from: pkg/controlplane/runtime/blockgc.go
      to: "(deleted SAFETY-01 gate)"
      via: "resolved-hold logic removed; CollectGarbage called without BackupHold"
      pattern: "NewBackupHold|StaticBackupHold|SetBackupHoldWiringForTest|gc\\.Options\\{[^}]*BackupHold"
---

<objective>
PR-B commit 3 (D-30 step 3) — Drop all storebackups wiring from Runtime per D-21, including the full SAFETY-01 gate in blockgc.go and all 8 backup delegation methods on runtime.go. After this commit, `storebackups` still exists as a package but nothing imports it — ready for deletion in plan 08-09 (after plans 08-08a + 08-08b finish persistence-layer cleanup).

**Critical additions (plan-checker iteration 1):**
- 8 delegation methods at runtime.go lines 417-498 (not enumerated in original plan).
- blockgc.go SAFETY-01 gate (lines 42-66) is the ENTIRE resolved-hold block, not just the NewBackupHold call site — `r.BackupStore()` + `r.DestFactoryFn()` + `storebackups.NewBackupHold` + `gc.StaticBackupHold` + `gc.Options.BackupHold`.
- `SetBackupHoldWiringForTest` helper (blockgc.go ~lines 121-139 or 130-139) must be removed.
- blockgc_test.go cleanups (lines 15 import; line 129 call site).
- The actual method name is `SetRestoreBumpBootVerifier` (NOT `SetBumpBootVerifier` as CONTEXT originally stated — CONTEXT already corrected this).

Purpose: Unwire before delete. Per CLAUDE.md §invariants, Runtime is the composition layer; this change removes one sub-service (storebackups) while preserving the others.
Output: One atomic commit; Runtime.go + blockgc.go + blockgc_test.go have no storebackups surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
@pkg/controlplane/runtime/runtime.go
@pkg/controlplane/runtime/blockgc.go
@pkg/controlplane/runtime/blockgc_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop storebackups wiring from Runtime + blockgc.go + blockgc_test.go (D-21, D-30 step 3)</name>
  <files>pkg/controlplane/runtime/runtime.go, pkg/controlplane/runtime/blockgc.go, pkg/controlplane/runtime/blockgc_test.go</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-11, D-21, D-30 step 3)
    - pkg/controlplane/runtime/runtime.go (verify current line numbers — CONTEXT says: import line 18, field line 66, builder block 120-126, `SetRestoreBumpBootVerifier` method 132-145, startup block 395-400, AND 8 delegation methods at lines 417-498. The 8 are: `RegisterBackupRepo`, `UnregisterBackupRepo`, `UpdateBackupRepo`, `RunBackup`, `ValidateBackupSchedule`, `BackupStore()`, `DestFactoryFn()`, `StoreBackupsService()`. Anchor every range with `grep -n` before deleting.)
    - pkg/controlplane/runtime/blockgc.go (CONTEXT says SAFETY-01 gate is lines 42-66: the resolved-hold block using `r.BackupStore()`, `r.DestFactoryFn()`, `storebackups.NewBackupHold`, `gc.StaticBackupHold` culminating in `gc.Options{ ..., BackupHold: <expr> }`; and `SetBackupHoldWiringForTest` helper at ~121-139 or 130-139. Anchor precisely with grep.)
    - pkg/controlplane/runtime/blockgc_test.go (CONTEXT says line 15 imports storebackups; line 129 calls `SetBackupHoldWiringForTest`. Anchor with grep.)
    - pkg/controlplane/runtime/storebackups/backup_hold.go (understand `NewBackupHold` signature so the neutral-path intent is clear — we are NOT passing nil; we are REMOVING the `BackupHold` field from `gc.Options` entirely.)
    - CLAUDE.md §"Architecture invariants" (Runtime composition layer rule)
  </read_first>
  <action>
    Step 1 (runtime.go edits) — Open `pkg/controlplane/runtime/runtime.go`. Remove the following in a single pass, keeping the file compilable only at the very end (intermediate compile states are expected broken; no sub-commits until all surgery is done). Anchor each removal via `grep -n` before using `sed -n 'Xp'` or manual edit:

      (a) Import line: `"github.com/marmos91/dittofs/pkg/controlplane/runtime/storebackups"` (CONTEXT says line 18).

      (b) Struct field: `storeBackupsSvc *storebackups.Service` (CONTEXT says line 66).

      (c) Builder block `storebackups.New(...)` with options (CONTEXT says lines 120-125) plus the `resolver := storebackups.NewDefaultResolver(...)` line just above. This is the 5-option builder block: `WithShares`, `WithStores`, `WithMetadataConfigs`, resolver, and `rt.storeBackupsSvc = storebackups.New(...)` assignment. Preserve any non-storebackups lines in the surrounding `if s != nil { ... }` block (e.g., `settingsWatcher` — NOT storebackups).

      (d) Method `SetRestoreBumpBootVerifier` at CONTEXT lines 132-145 — delete the doc comment AND the method body in full. NOTE: the correct name is `SetRestoreBumpBootVerifier`; the acceptance criterion accepts either spelling for safety.

      (e) Startup ordering block in `Runtime.Serve` / `Runtime.Start` at CONTEXT lines 395-400 (`if r.storeBackupsSvc != nil { r.storeBackupsSvc.Start(ctx) }` or equivalent). Delete it in full. Also check Runtime's shutdown path for a corresponding `.Close()` / `.Stop()` and delete it.

      (f) Audit callers of `SetRestoreBumpBootVerifier` — `grep -rn "SetRestoreBumpBootVerifier\|SetBumpBootVerifier" internal/ pkg/ cmd/ --include='*.go'` — and remove every call site (typically wired from `internal/adapter/nfs/v4/handlers` per the removed doc comment).

      (g) **Delete all 8 delegation methods at CONTEXT lines 417-498:** `RegisterBackupRepo`, `UnregisterBackupRepo`, `UpdateBackupRepo`, `RunBackup`, `ValidateBackupSchedule`, `BackupStore()`, `DestFactoryFn()`, `StoreBackupsService()`. Anchor via `grep -n "func (.*Runtime) \\(RegisterBackupRepo\\|UnregisterBackupRepo\\|UpdateBackupRepo\\|RunBackup\\|ValidateBackupSchedule\\|BackupStore\\|DestFactoryFn\\|StoreBackupsService\\)" pkg/controlplane/runtime/runtime.go`. Delete each brace-balanced function body + its doc comment block. After deletion, audit callers of each method repo-wide (`grep -rn "\\.RegisterBackupRepo\\b\\|\\.UnregisterBackupRepo\\b\\|\\.UpdateBackupRepo\\b\\|\\.RunBackup\\b\\|\\.ValidateBackupSchedule\\b\\|\\.BackupStore()\\|\\.DestFactoryFn()\\|\\.StoreBackupsService()" . --include='*.go'`) — expected 0 hits at this point (plan 08-07 deleted REST handlers/apiclient/dfsctl; if any hit remains, follow the chain upward and delete in the same commit).

    Step 2 (blockgc.go edits) — Open `pkg/controlplane/runtime/blockgc.go`. Anchor each block with grep, then:

      (h) **Delete SAFETY-01 gate (CONTEXT lines 42-66)** — this is the ENTIRE resolved-hold logic, not just the `NewBackupHold` call. Specifically remove:
        - any local `bs := r.BackupStore()` / `df := r.DestFactoryFn()` assignments,
        - the `if bs != nil && df != nil { ... }` (or similar) block building a resolved hold,
        - the `storebackups.NewBackupHold(...)` call,
        - any fallback `gc.StaticBackupHold` usage,
        - the field `BackupHold` in the `gc.Options{...}` literal passed to `gc.CollectGarbage` (remove the key entirely, not set to nil).
        Anchor: `grep -n "BackupHold\|NewBackupHold\|StaticBackupHold\|BackupStore()\|DestFactoryFn()" pkg/controlplane/runtime/blockgc.go`. After surgery, the `gc.CollectGarbage(ctx, ...)` call receives `gc.Options` with NO `BackupHold` field.

      (i) **Delete `SetBackupHoldWiringForTest`** function at CONTEXT lines 121-139 (or 130-139 — verify with `grep -n "SetBackupHoldWiringForTest" pkg/controlplane/runtime/blockgc.go`). Delete the entire function and its doc comment. NOTE: this is a package-level helper used only by `blockgc_test.go`; once gone, the test file's call must also be removed (next step).

      (j) Remove any remaining `storebackups` import in this file. Remove any remaining import that becomes unused (e.g., `gc` may still be needed for `CollectGarbage` — KEEP that; only the `storebackups` and the `gc.StaticBackupHold` helper usage die).

    Step 3 (blockgc_test.go edits) — Open `pkg/controlplane/runtime/blockgc_test.go`:

      - Line 15 import (per CONTEXT): remove the `storebackups "github.com/marmos91/dittofs/pkg/controlplane/runtime/storebackups"` import.
      - Line 129 (per CONTEXT): remove the `SetBackupHoldWiringForTest(...)` call. If the call sets up state used by the surrounding test, ensure the rest of the test still passes (the GC behavior no longer depends on backup holds — the test may need trimming of the associated assertion).
      - Anchor with `grep -n "storebackups\|SetBackupHoldWiringForTest" pkg/controlplane/runtime/blockgc_test.go` before editing.

    Step 4 (grep sweep, all should yield 0 lines outside `runtime/storebackups/` itself) —
      `grep -c "BackupStore\\|DestFactoryFn\\|StoreBackupsService\\|RegisterBackupRepo\\|SetBackupHoldWiringForTest" pkg/controlplane/runtime/runtime.go pkg/controlplane/runtime/blockgc.go pkg/controlplane/runtime/blockgc_test.go` → 0.
      `grep -rn "storebackups" pkg/controlplane/runtime/*.go | grep -v "runtime/storebackups/" | wc -l` → 0.
      `grep -rn "SetRestoreBumpBootVerifier\\|SetBumpBootVerifier\\|storeBackupsSvc\\|NewBackupHold\\|StaticBackupHold" pkg/controlplane/runtime/ internal/ --include='*.go' | grep -v "runtime/storebackups/" | wc -l` → 0.

    Step 5 (verify) — `go build ./...` exits 0. `go test -count=1 -race ./pkg/controlplane/runtime/... ./pkg/blockstore/... ./internal/controlplane/...` exits 0. Fix any residuals by following the compiler errors upward (delete the offending call site rather than re-adding a removed symbol).

    Step 6 (Claude-Code hygiene) — `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true`.

    Step 7 (commit) — signed:
      `git commit -S -m "runtime: drop storebackups wiring + SAFETY-01 gate + delegation methods (TD-03)"`
  </action>
  <verify>
    <automated>[ "$(grep -c 'BackupStore\|DestFactoryFn\|StoreBackupsService\|RegisterBackupRepo\|SetBackupHoldWiringForTest' pkg/controlplane/runtime/runtime.go pkg/controlplane/runtime/blockgc.go pkg/controlplane/runtime/blockgc_test.go | awk -F: '{s+=$2}END{print s}')" = "0" ] &amp;&amp; [ "$(grep -rn 'storebackups' pkg/controlplane/runtime/*.go | grep -v 'runtime/storebackups/' | wc -l | tr -d ' ')" = "0" ] &amp;&amp; go build ./... &amp;&amp; go test -count=1 -race ./pkg/controlplane/runtime/... ./pkg/blockstore/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storebackups" pkg/controlplane/runtime/runtime.go` → 0.
    - `grep -c "storeBackupsSvc\\|SetRestoreBumpBootVerifier\\|SetBumpBootVerifier" pkg/controlplane/runtime/runtime.go` → 0.
    - `grep -cE "func \\(.*\\) (RegisterBackupRepo|UnregisterBackupRepo|UpdateBackupRepo|RunBackup|ValidateBackupSchedule|BackupStore|DestFactoryFn|StoreBackupsService)\\b" pkg/controlplane/runtime/runtime.go` → 0.
    - `grep -c "NewBackupHold\\|StaticBackupHold\\|SetBackupHoldWiringForTest\\|BackupHold" pkg/controlplane/runtime/blockgc.go` → 0.
    - `grep -c "storebackups\\|SetBackupHoldWiringForTest" pkg/controlplane/runtime/blockgc_test.go` → 0.
    - `grep -rn "SetRestoreBumpBootVerifier\\|SetBumpBootVerifier" internal/ pkg/ cmd/ --include='*.go' | grep -v "runtime/storebackups/"` → 0 matches.
    - `go build ./...` exits 0.
    - `go test -count=1 -race ./pkg/controlplane/runtime/... ./pkg/blockstore/...` exits 0.
    - `git log -1 --format=%s` = `runtime: drop storebackups wiring + SAFETY-01 gate + delegation methods (TD-03)`.
    - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes (no offending strings).
    - `git log -1 --show-signature` reports Good signature.
  </acceptance_criteria>
  <done>
    Runtime no longer composes storebackups; blockgc doesn't use BackupHold in any form (SAFETY-01 gate fully gone); blockgc_test.go drops its deps; 8 delegation methods deleted. Package `storebackups/` still exists, un-imported — ready to delete in plan 08-09 (after 08-08a + 08-08b).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime composition layer | Internal; sub-service removal must preserve AuthContext threading (CLAUDE.md invariant 2). |
| GC safety gate (SAFETY-01) | Previously prevented GC from deleting payloads with open backup holds. Obsolete — no backup holds exist post-PR-B. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | E (EoP) | Removing SetRestoreBumpBootVerifier must not leave adapter caller calling a nil method | mitigate | Step 1(f) audits all callers and deletes them in the same commit. Compile catches the rest. |
| T-08-08-02 | T (Tampering) | Startup-order block removal might affect API-server start timing | mitigate | Only removing "start backup scheduler before API server" — without the scheduler, nothing to order; Serve proceeds as before. Existing runtime tests cover this. |
| T-08-08-03 | R (Repudiation) | Loss of BackupHold metrics | accept | Intentional (v0.13.0 backup gone). v0.16.0 will rebuild. |
| T-08-08-04 | D (Denial of service) | Removing SAFETY-01 gate means GC could delete payloads that a hypothetical external backup scheduler was holding | accept | No such scheduler exists (v0.13.0 never released). v0.16.0 CAS design makes holds unnecessary (content-addressed immutability; see CONTEXT §"SUPERSEDED — Phase 5 BackupHold design"). |
| T-08-08-05 | I (Information disclosure) | 8 delegation methods were part of the Runtime public API (exported methods); removing them is a semantic break for any external consumer | accept | Runtime composition is internal to the binary per CLAUDE.md; no external consumers. v0.13.0 API unreleased. |
</threat_model>

<verification>
- `go build ./...` green; runtime + blockstore + internal/controlplane tests green.
- Greps confirm no stray references to any storebackups symbol.
- Commit signed; Claude-Code hygiene check passes.
</verification>

<success_criteria>
- D-21 / D-30 step 3 complete; independently green.
- Runtime composition simplified — storebackups no longer a sub-service, SAFETY-01 gate gone, 8 delegation methods gone.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-08-SUMMARY.md` — commit SHA; enumeration of runtime.go line ranges deleted per sub-step (a)-(g); blockgc.go line ranges deleted (h)-(j); blockgc_test.go lines deleted; list of call-site removals for `SetRestoreBumpBootVerifier` and the 8 delegation methods across the repo.
</output>
