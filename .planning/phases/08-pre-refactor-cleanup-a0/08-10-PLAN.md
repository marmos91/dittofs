---
phase: 08-pre-refactor-cleanup-a0
plan: 10
type: execute
wave: 2
depends_on: [08-09]
files_modified:
  - pkg/backup
autonomous: true
requirements: [TD-03]
must_haves:
  truths:
    - "`pkg/backup/` directory no longer exists."
    - "Manifest / scheduler / destination / executor / errors / restore subtrees are all gone."
    - "`go build ./...` and `go test -count=1 -short -race ./pkg/... ./internal/... ./cmd/...` pass (narrowed scope per checker warning W16 to avoid bench.* flakes)."
  artifacts: []
  key_links:
    - from: "(deleted) pkg/backup/"
      to: ""
      via: "git rm -r"
      pattern: ""
---

<objective>
PR-B commit 7 (D-30 step 7) — Delete `pkg/backup/` in full. By now (after plans 06/07/08/08a/08b/09) there are no importers.

Purpose: Remove the core backup package tree.
Output: One atomic commit deleting the directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove pkg/backup tree (D-01, D-30 step 7)</name>
  <files>pkg/backup/**/*</files>
  <read_first>
    - .planning/phases/08-pre-refactor-cleanup-a0/08-CONTEXT.md (D-01, D-30 step 7)
    - pkg/backup/ (directory listing — confirm contents: manifest/, scheduler/, destination/, executor/, errors/, restore/, plus top-level `backupable.go`, `clock.go`, `backupable_test.go`, `concurrent_write_backup_restore_test.go`)
  </read_first>
  <action>
    Step 1 (pre-check — macOS-compatible greps per checker W15, use `grep -E` with quoted alternation for portability) — Confirm nothing imports pkg/backup anymore:
      `grep -rnE '"github\.com/marmos91/dittofs/pkg/backup"|"github\.com/marmos91/dittofs/pkg/backup/' . --include='*.go' | grep -v '^pkg/backup/' | wc -l` → MUST be 0.
      Also confirm no `_test.go` outside pkg/backup imports its types:
      `grep -rnE 'backup\.(Manifest|Scheduler|Destination|Executor|Restore)' . --include='*.go' | grep -v '^pkg/backup/' | wc -l` → MUST be 0.
      (If these produce any non-zero count, STOP: a prior PR-B plan missed an importer. Fix upstream first.)

    Step 2 (delete) —
      `git rm -r pkg/backup`

    Step 3 (verify — narrowed test scope per checker W16; bench.* suite has known flakes per MEMORY.md) —
      `test ! -d pkg/backup` passes.
      `go build ./...` exits 0.
      `go test -count=1 -short -race ./pkg/... ./internal/... ./cmd/...` exits 0. (Narrower than full `./...` to exclude `./bench/...` flakes.)
      `grep -rn "marmos91/dittofs/pkg/backup" . --include='*.go' | wc -l` → 0.

    Step 4 (Claude-Code hygiene) — `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true`.

    Step 5 (commit) — signed:
      `git commit -S -m "backup: remove pkg/backup (TD-03)"`
  </action>
  <verify>
    <automated>test ! -d pkg/backup &amp;&amp; go build ./... &amp;&amp; go test -count=1 -short -race ./pkg/... ./internal/... ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d pkg/backup` passes.
    - `grep -rn "marmos91/dittofs/pkg/backup" . --include='*.go' | wc -l` → 0.
    - `go build ./...` exits 0.
    - `go test -count=1 -short -race ./pkg/... ./internal/... ./cmd/...` exits 0.
    - `git log -1 --format=%s` = `backup: remove pkg/backup (TD-03)`.
    - `git log -1 --format='%B' | grep -iEq "claude code|co-authored-by" && exit 1 || true` passes.
    - `git log -1 --show-signature` reports Good signature.
  </acceptance_criteria>
  <done>
    pkg/backup/ gone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Internal package deletion only; previously not shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | R | Loss of backup audit trail feature | accept | Never released. v0.16.0 rebuild on CAS. |
| T-08-10-02 | T | Residual import could cause silent build failure on another platform | mitigate | Pre-check grep (macOS-compatible `grep -E` with quoted alternation) + `go build ./...` on the current platform. |
</threat_model>

<verification>
- Directory gone; build + narrowed tests green.
</verification>

<success_criteria>
- D-30 step 7 complete.
</success_criteria>

<output>
`.planning/phases/08-pre-refactor-cleanup-a0/08-10-SUMMARY.md` — commit SHA; count of files deleted.
</output>
