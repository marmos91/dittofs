---
phase: 09-adapter-layer-cleanup-adapt
plan: 04
type: execute
wave: 4
depends_on: [09-01]
files_modified:
  - internal/adapter/common/write_payload.go
  - internal/adapter/common/doc.go
  - internal/adapter/nfs/v3/handlers/write.go
  - internal/adapter/nfs/v3/handlers/commit.go
  - internal/adapter/nfs/v4/handlers/write.go
  - internal/adapter/nfs/v4/handlers/commit.go
  - internal/adapter/smb/v2/handlers/write.go
  - internal/adapter/smb/v2/handlers/close.go
autonomous: true
requirements: [ADAPT-04]

must_haves:
  truths:
    - "All READ call sites in NFSv3, NFSv4, and SMB v2 route through common.ReadFromBlockStore (already satisfied by plan 01 — verified here)"
    - "All WRITE call sites in NFSv3, NFSv4, and SMB v2 route through a new common.WriteToBlockStore helper — one place where Phase 12 will add FileAttr.Blocks fetch + slice logic"
    - "All COMMIT/flush call sites route through a common helper (or reuse the WRITE helper with explicit flush semantics) — Phase 12 reads FileAttr.Blocks to understand what to flush"
    - "common/doc.go explicitly documents the Phase-12 []BlockRef seam: where the fetch/slice lands, what changes in Phase 12, and why call-site code will NOT change (D-12)"
    - "Engine signature is NOT changed in this phase — engine.BlockStore.ReadAt/WriteAt continue to accept their current shape (ReadAt returns (int, error); WriteAt returns error only); the seam is internal to common/"
    - "Wire protocols still communicate (offset, length) — []BlockRef remains internal plumbing; handler code speaks the same wire-protocol contract as before"
  artifacts:
    - path: "internal/adapter/common/write_payload.go"
      provides: "WriteToBlockStore (structural twin of ReadFromBlockStore)"
      contains: "func WriteToBlockStore"
    - path: "internal/adapter/common/doc.go"
      provides: "Updated Phase-12 seam documentation after WriteToBlockStore exists"
      contains: "WriteToBlockStore"
  key_links:
    - from: "internal/adapter/nfs/v3/handlers/write.go"
      to: "internal/adapter/common.WriteToBlockStore"
      via: "common.WriteToBlockStore(ctx, blockStore, payloadID, data, offset)"
      pattern: "common\\.WriteToBlockStore"
    - from: "internal/adapter/nfs/v4/handlers/write.go"
      to: "internal/adapter/common.WriteToBlockStore"
      via: "common.WriteToBlockStore"
      pattern: "common\\.WriteToBlockStore"
    - from: "internal/adapter/smb/v2/handlers/write.go"
      to: "internal/adapter/common.WriteToBlockStore"
      via: "common.WriteToBlockStore"
      pattern: "common\\.WriteToBlockStore"
---

<objective>
ADAPT-04 — Create the call-site seam that Phase 12 (META-01 + API-01) will feed `[]BlockRef` through. In Phase 09 this is a structural refactor: introduce `common.WriteToBlockStore` (twin of plan 01's `ReadFromBlockStore`), route every NFS/SMB WRITE and COMMIT call site through it plus `common.ReadFromBlockStore` (READ side already done in plan 01), and document the Phase-12 seam explicitly in `common/doc.go`. Engine signature does NOT change — per D-12, "Phase 09 ships call-site refactor only; Phase 12 wires actual `[]BlockRef` data". The engine still takes `(ctx, payloadID, buf, offset)` — `ReadAt` returns `(int, error)`; `WriteAt` returns `error` only.

Purpose: When Phase 12 changes `FileAttr.Blocks` to `[]BlockRef` (META-01) and `engine.BlockStore.ReadAt` / `WriteAt` to accept `[]BlockRef` (API-01), the "fetch FileAttr.Blocks → slice to [offset, offset+len) → hand resolved refs to engine" logic will land in exactly ONE place (inside `common.ReadFromBlockStore` / `common.WriteToBlockStore`) instead of being sprinkled across six handler files. This is the one-edit-point the seam is designed to create. Protocol handlers continue to receive `(offset, length)` from the wire (CIFS/NFS have no concept of blocks) and pass `(offset, length)` to the common helper — the protocol fidelity referenced in the specifics is preserved.

Output: One new file `common/write_payload.go` with `WriteToBlockStore`; call-site refactor in 6 handler files (3 WRITE, 3 COMMIT/close-flush); `common/doc.go` amended with an explicit "Phase-12 seam" section naming the helpers. One atomic signed commit labeled `ADAPT-04`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-adapter-layer-cleanup-adapt/09-CONTEXT.md
@.planning/phases/09-adapter-layer-cleanup-adapt/09-PATTERNS.md
@CLAUDE.md

# Phase 12 successor reference — shows what the seam will feed
# (Phase 12: META-01 reintroduces FileAttr.Blocks as []BlockRef; API-01 changes
# engine.BlockStore.ReadAt/WriteAt signatures to accept []BlockRef.)
@.planning/ROADMAP.md

# Call-site files to refactor (WRITE side)
@internal/adapter/nfs/v3/handlers/write.go
@internal/adapter/nfs/v3/handlers/commit.go
@internal/adapter/nfs/v4/handlers/write.go
@internal/adapter/nfs/v4/handlers/commit.go
@internal/adapter/smb/v2/handlers/write.go
@internal/adapter/smb/v2/handlers/close.go

# Plan 01 + 03 outputs — common/ package is writable
@internal/adapter/common/resolve.go
@internal/adapter/common/read_payload.go
@internal/adapter/common/doc.go

# Engine contract (DOES NOT CHANGE in this phase per D-12)
@pkg/blockstore/engine/engine.go

<interfaces>
Existing engine contract (UNCHANGED in Phase 09; changes in Phase 12 API-01). **Confirmed against pkg/blockstore/engine/engine.go:**
```
// ReadAt returns the number of bytes read plus an error.
func (bs *BlockStore) ReadAt(ctx context.Context, payloadID string, data []byte, offset uint64) (int, error)

// WriteAt returns ONLY an error — no byte count. Successful writes are
// full-length by contract; partial writes surface as an error.
func (bs *BlockStore) WriteAt(ctx context.Context, payloadID string, data []byte, offset uint64) error

// Commit/flush variants — confirm exact names during read-first.
```

Target common/ WRITE-side API (Phase 09 shape — signature mirrors engine.WriteAt, returns error only):
```
// WriteToBlockStore is the call-site seam that Phase 12 (META-01 + API-01) will
// extend to fetch FileAttr.Blocks and pass resolved []BlockRef to engine. Phase 09
// body is a direct engine.WriteAt passthrough — no FileAttr.Blocks exists yet
// (deleted in Phase 08 TD-03, reintroduced in Phase 12 META-01).
//
// Returns error only — mirrors engine.BlockStore.WriteAt. A nil error means
// the full `data` slice was written at `offset`; partial writes surface as an
// error per the engine contract.
func WriteToBlockStore(
    ctx context.Context,
    blockStore *engine.BlockStore,
    payloadID metadata.PayloadID,
    data []byte,
    offset uint64,
) error
```

Note: WRITE does NOT need a pooled buffer or a Release closure — callers own the source slice (SMB/NFS decode layer gives them the wire bytes). Only READ is pool-backed.

Plan 01's READ helper (already in place, unchanged):
```
func ReadFromBlockStore(ctx context.Context, blockStore *engine.BlockStore,
    payloadID metadata.PayloadID, offset uint64, count uint32) (BlockReadResult, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create common/write_payload.go with WriteToBlockStore helper; update common/doc.go with explicit Phase-12 seam section</name>
  <files>
    internal/adapter/common/write_payload.go,
    internal/adapter/common/doc.go,
    internal/adapter/common/write_payload_test.go
  </files>
  <read_first>
    - internal/adapter/common/read_payload.go (plan 01 shape — WriteToBlockStore mirrors this structurally, but signature differs: WriteAt returns error only)
    - internal/adapter/common/doc.go (plan 01 — existing Phase-12 seam doc to extend)
    - pkg/blockstore/engine/engine.go (WriteAt signature — CONFIRMED: ctx, payloadID string, data []byte, offset uint64 -> error ONLY; no int return)
    - .planning/phases/09-adapter-layer-cleanup-adapt/09-CONTEXT.md (D-12 — call-site refactor only; engine signature unchanged)
    - .planning/ROADMAP.md lines 151-176 (Phase 12 consumes this seam)
  </read_first>
  <behavior>
    - Test 1: WriteToBlockStore with a 1 KB payload calls engine.WriteAt with identical arguments (use a spy *engine.BlockStore or mock); assert passthrough behavior (same bytes, same offset, same payloadID). Assertion checks err == nil on success — there is no byte count to inspect.
    - Test 2: WriteToBlockStore propagates errors verbatim — no wrapping, no translation. The returned err must be the exact same error value the engine returned (assert with errors.Is or pointer-equality).
    - Test 3: WriteToBlockStore with a nil data slice returns the engine's behavior (typically nil, or an engine-specific error — assert observed behavior, not a prescriptive one).
  </behavior>
  <action>
TDD. Write tests FIRST.

Step 1 — RED: write `internal/adapter/common/write_payload_test.go` using either the real `engine.BlockStore` API or a narrow mock. Every assertion binds `err := ...` only (no `n` variable — WriteAt returns error only). Run `go test -run TestWriteToBlockStore -count=1 ./internal/adapter/common/` — MUST fail with "undefined: WriteToBlockStore".

Step 2 — GREEN: implement `internal/adapter/common/write_payload.go`:

```go
package common

import (
    "context"

    "github.com/marmos91/dittofs/pkg/blockstore/engine"
    "github.com/marmos91/dittofs/pkg/metadata"
)

// WriteToBlockStore is the structural twin of ReadFromBlockStore. In Phase 09
// it is a direct passthrough to engine.WriteAt — there is no FileAttr.Blocks
// to fetch (deleted in Phase 08 TD-03; reintroduced in Phase 12 META-01 as
// []BlockRef). The helper exists so that when Phase 12 changes the engine
// signature (API-01) and reintroduces the Blocks field, the fetch-and-slice
// logic lands here in exactly one place — every protocol handler (NFSv3,
// NFSv4, SMB v2) calls this function and is therefore unchanged by Phase 12.
//
// Returns error only — mirrors engine.BlockStore.WriteAt. The engine contract
// guarantees that a nil error means the full `data` slice was persisted at
// `offset`; partial writes surface as an error.
//
// Wire protocol fidelity: the wire carries (offset, length); NFS3/4 and SMB2/3
// do not know about blocks. Phase 09 preserves that by keeping the
// (payloadID, data, offset) signature identical to the current engine contract.
// Phase 12 adds the BlockRef resolution INSIDE this function body.
func WriteToBlockStore(
    ctx context.Context,
    blockStore *engine.BlockStore,
    payloadID metadata.PayloadID,
    data []byte,
    offset uint64,
) error {
    return blockStore.WriteAt(ctx, string(payloadID), data, offset)
}
```

Step 3 — extend `internal/adapter/common/doc.go` with an explicit "Phase-12 seam" section after the existing text. The section MUST:

- Name both helpers (`ReadFromBlockStore`, `WriteToBlockStore`) as the single seam.
- Reference Phase 12 issue (#423) and the specific requirements that consume the seam (META-01 for `FileAttr.Blocks []BlockRef`, API-01 for engine signature change).
- State that protocol handlers will NOT change in Phase 12 — only common/'s internals.
- State that the wire protocols continue to carry `(offset, length)` and are protocol-fidelity-correct both today and after Phase 12.

Append this block (expand the existing `# Phase-12 seam for []BlockRef` section plan 01 already added; replace its one-paragraph body with this expanded text):

```
// # Phase-12 seam for []BlockRef (ADAPT-04 / D-12)
//
// The helpers ReadFromBlockStore and WriteToBlockStore are the single place
// where Phase 12 (#423) will add []BlockRef plumbing. Today these helpers are
// thin passthroughs to engine.BlockStore.ReadAt / WriteAt, which accept
// (ctx, payloadID, buf, offset) — exactly what the wire protocols (NFS3 READ,
// NFS4 READ, SMB2 READ; NFS3 WRITE, NFS4 WRITE, SMB2 WRITE) hand us.
//
// In Phase 12:
//   - META-01 reintroduces FileAttr.Blocks as []BlockRef (sorted by offset,
//     populated at sync finalization).
//   - API-01 changes engine.BlockStore.ReadAt / WriteAt to accept []BlockRef
//     instead of (payloadID, offset). A binary search on []BlockRef resolves
//     the (offset, length) range to the chunks covering it.
//
// The Phase-12 change to these two functions will be, essentially:
//
//     1. fetch FileAttr.Blocks via the narrow MetadataService interface
//     2. slice the []BlockRef list to the range covering [offset, offset+len)
//     3. pass the resolved slice to the new engine.ReadAt/WriteAt signature
//
// Every protocol handler code path is UNCHANGED by Phase 12 because they all
// call common.ReadFromBlockStore / common.WriteToBlockStore. Wire protocol
// fidelity is preserved — handlers continue to receive and emit
// (offset, length) on the wire.
```

Step 4 — verify GREEN. Run `go test -run TestWriteToBlockStore -race -count=1 ./internal/adapter/common/...` — all tests pass. Run `go build ./...` + `go vet ./...` — clean.

Gotchas:

1. **WriteAt returns error only.** The engine's `WriteAt` signature is `func (bs *BlockStore) WriteAt(ctx, payloadID string, data []byte, offset uint64) error` — NO byte count. Do NOT write `n, err := blockStore.WriteAt(...)` anywhere; do NOT write `n, err := common.WriteToBlockStore(...)` at any call site. The compiler will reject `(int, error)` bindings.
2. WRITE has no pooled buffer — the `data []byte` is owned by the caller (wire decode layer). Do NOT add a Release pattern for WriteToBlockStore; that would couple to SMB's wire-buffer lifetime in a way that breaks NFS.
3. Keep the signature identical to the current `engine.BlockStore.WriteAt`. When Phase 12 changes the engine signature, common/write_payload.go is the single file that needs updating to feed `[]BlockRef`; every call site upstream is untouched.
4. Do NOT prematurely add a Commit helper. COMMIT/close flush paths typically call a different engine method (flush/sync), and their Phase-12 consumption pattern may differ. If task 2's call-site audit shows COMMIT/close-flush call sites would benefit from a dedicated `common.CommitBlockStore(ctx, blockStore, payloadID)` helper, add it in this same task — but only if there is a real call-site pattern to dedupe. Otherwise COMMIT call sites use `common.ResolveForWrite` (already from plan 01) + direct engine call.
  </action>
  <verify>
    <automated>test -f internal/adapter/common/write_payload.go && test -f internal/adapter/common/write_payload_test.go && grep -q "func WriteToBlockStore" internal/adapter/common/write_payload.go && grep -q "Phase-12 seam" internal/adapter/common/doc.go && grep -q "WriteToBlockStore" internal/adapter/common/doc.go && go test -run TestWriteToBlockStore -race -count=1 ./internal/adapter/common/... && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/adapter/common/write_payload.go` succeeds
    - `grep -q "func WriteToBlockStore" internal/adapter/common/write_payload.go` succeeds
    - `grep -q "return blockStore\\.WriteAt" internal/adapter/common/write_payload.go` succeeds (Phase 09 is a passthrough)
    - `grep -Eq ") error \\{$" internal/adapter/common/write_payload.go` succeeds (function returns error only — no (int, error))
    - `! grep -Eq "\\) \\(int, error\\)" internal/adapter/common/write_payload.go` — no (int, error) tuple return in the file
    - `grep -q "Phase-12 seam" internal/adapter/common/doc.go` succeeds
    - `grep -q "WriteToBlockStore" internal/adapter/common/doc.go` succeeds (helper named in seam doc)
    - `grep -q "ReadFromBlockStore" internal/adapter/common/doc.go` succeeds (both helpers named)
    - `grep -Eq "API-01|META-01" internal/adapter/common/doc.go` succeeds (Phase-12 requirements referenced)
    - `go test -run TestWriteToBlockStore -race -count=1 ./internal/adapter/common/...` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>common/write_payload.go created with WriteToBlockStore passthrough (error-only return matching engine.WriteAt) + unit tests; common/doc.go has an explicit Phase-12 seam section naming both helpers and referencing META-01/API-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor every NFS/SMB WRITE + COMMIT/close-flush call site to route through common.WriteToBlockStore (or common.ResolveForWrite + direct engine call for flush paths); verify READ sites already use common.ReadFromBlockStore from plan 01</name>
  <files>
    internal/adapter/nfs/v3/handlers/write.go,
    internal/adapter/nfs/v3/handlers/commit.go,
    internal/adapter/nfs/v4/handlers/write.go,
    internal/adapter/nfs/v4/handlers/commit.go,
    internal/adapter/smb/v2/handlers/write.go,
    internal/adapter/smb/v2/handlers/close.go
  </files>
  <read_first>
    - internal/adapter/common/write_payload.go (freshly created in task 1 — target API; single error return)
    - internal/adapter/common/resolve.go (plan 01 — ResolveForWrite is already in use at these files; verify)
    - internal/adapter/nfs/v3/handlers/write.go (call sites calling blockStore.WriteAt directly — convert to common.WriteToBlockStore)
    - internal/adapter/nfs/v3/handlers/commit.go (COMMIT call site)
    - internal/adapter/nfs/v4/handlers/write.go
    - internal/adapter/nfs/v4/handlers/commit.go
    - internal/adapter/smb/v2/handlers/write.go (call sites at lines ~292, 349, 371 per PATTERNS.md — already use ResolveForWrite from plan 01; WriteAt calls need migration)
    - internal/adapter/smb/v2/handlers/close.go (flush on close — line ~518, 558)
    - .planning/phases/09-adapter-layer-cleanup-adapt/09-CONTEXT.md (D-12 rationale for call-site refactor)
    - Run `grep -rn "blockStore\\.WriteAt\\|blockStore\\.Commit" internal/adapter/` FIRST to enumerate every call site before editing
  </read_first>
  <action>
Mechanical call-site refactor. Step 1 enumerates all sites; step 2 applies the substitution; step 3 verifies.

Step 1 — enumerate:

```
grep -rn "blockStore\\.WriteAt\\b" internal/adapter/
grep -rn "blockStore\\.ReadAt\\b" internal/adapter/   # sanity: should all be in common/ or already migrated in plan 01
grep -rn "blockStore\\.Commit\\b\\|blockStore\\.Flush\\b\\|blockStore\\.Sync\\b" internal/adapter/
```

Expected outcome of READ enumeration: only `internal/adapter/common/read_payload.go` still calls `blockStore.ReadAt`. If a handler file still calls `blockStore.ReadAt` directly, that's a plan-01 miss — migrate it in this task as an incidental finding (not a plan-04 scope creep).

Step 2 — substitute WRITE call sites:

For each `blockStore.WriteAt(ctx, string(payloadID), data, offset)` invocation in handler files, replace with `common.WriteToBlockStore(ctx, blockStore, payloadID, data, offset)`. **Engine.WriteAt returns `error` only — do NOT introduce an `n` variable; every call site binds `err := common.WriteToBlockStore(...)` only.**

Example (NFSv3 write.go — speculative line; confirm during read-first):

Before:
```
err := blockStore.WriteAt(ctx.Context, string(file.PayloadID), req.Data, req.Offset)
```

After:
```
err := common.WriteToBlockStore(ctx.Context, blockStore, file.PayloadID, req.Data, req.Offset)
```

Notice the `string(payloadID)` cast goes away — WriteToBlockStore takes `metadata.PayloadID` directly and does the cast internally. If an existing call site already does `n, err := blockStore.WriteAt(...)` and uses `n` downstream, that is a pre-existing bug against the engine contract (WriteAt does not return a byte count); fix the call site during migration — drop `n` and bind `err` only. Flag the fix in the commit message.

Step 3 — COMMIT / close-flush paths:

If a COMMIT handler calls a dedicated engine method (e.g., `blockStore.Commit(ctx, payloadID)` or `blockStore.SyncPending(ctx, payloadID)`), two options:

- Option A (preferred if the pattern is uniform across all three protocols): add `common.CommitBlockStore(ctx, blockStore, payloadID)` to `common/write_payload.go` in THIS task (fold it into the same new file) and route NFSv3 `commit.go`, NFSv4 `commit.go`, and SMB `close.go` flush paths through it.
- Option B (if patterns diverge): leave the COMMIT/flush direct engine calls alone for Phase 09. The Phase-12 seam for COMMIT gets addressed at that time. `common.ResolveForWrite` is already in use at the COMMIT call sites (from plan 01) which already gives the one-location seam for the block-store acquisition half. Engine-method variance is acceptable in Phase 09.

Choice rule: inspect the COMMIT call sites during read-first. If all three protocols call the same engine method with the same arguments, do Option A. Otherwise Option B. Record the choice explicitly in the commit message.

Step 4 — READ sanity check:

Run the READ enumeration. Every `blockStore.ReadAt` call outside `internal/adapter/common/` is a plan-01 miss. Migrate any found in this task.

Step 5 — full verification:

```
go build ./...
go vet ./...
go test -race -count=1 ./...
```

All must be green. NO behavioral change — WriteToBlockStore is a literal passthrough today.

Gotchas:

1. **payloadID type**: NFSv3 uses `file.PayloadID` which is already `metadata.PayloadID`. NFSv4 may use `string(...)` at some sites; common.WriteToBlockStore takes `metadata.PayloadID` — the conversion lives inside the helper now. Where call sites currently do `string(file.PayloadID)`, drop the cast.
2. **Error handling unchanged**: WriteToBlockStore propagates errors verbatim. All existing error mapping (`common.MapToNFS3` / `common.MapToSMB` from plan 03) continues to work identically.
3. **No byte-count binding**: `err := common.WriteToBlockStore(...)` is the ONLY shape. If you see `n, err := ...` anywhere after migration, the compiler will fail — that is the catch. Any pre-existing `n, err := blockStore.WriteAt(...)` binding is a latent bug (WriteAt returns `error` only); the migration fixes it. Document such incidental fixes in the commit message.
4. **Imports**: every migrated file gains `"github.com/marmos91/dittofs/internal/adapter/common"` if not already imported (plan 01 added it to most of these for ResolveFor* migration — confirm no duplicate add). Run `goimports -w internal/adapter/` before committing.
5. **Scope discipline**: this plan refactors CALL SITES ONLY. No signature changes to engine, no FileAttr.Blocks reintroduction, no BlockRef data type. Any of those is scope creep into Phase 12.
6. **If Option B is chosen for COMMIT**: the commit message MUST say "COMMIT paths remain on direct engine calls; Phase 12 handles the COMMIT seam alongside META-01." so reviewers / future maintainers see the explicit deferral.
  </action>
  <verify>
    <automated>grep -rq "common\\.WriteToBlockStore" internal/adapter/nfs/v3/handlers/ && grep -rq "common\\.WriteToBlockStore" internal/adapter/nfs/v4/handlers/ && grep -rq "common\\.WriteToBlockStore" internal/adapter/smb/v2/handlers/ && [ "$(grep -rE 'blockStore\\.WriteAt\\b' internal/adapter/ | grep -v '^internal/adapter/common/' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rE 'blockStore\\.ReadAt\\b' internal/adapter/ | grep -v '^internal/adapter/common/' | wc -l | tr -d ' ')" = "0" ] && go build ./... && go vet ./... && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rq "common\\.WriteToBlockStore" internal/adapter/nfs/v3/handlers/` — NFSv3 WRITE migrated
    - `grep -rq "common\\.WriteToBlockStore" internal/adapter/nfs/v4/handlers/` — NFSv4 WRITE migrated
    - `grep -rq "common\\.WriteToBlockStore" internal/adapter/smb/v2/handlers/` — SMB v2 WRITE migrated
    - `grep -rE 'blockStore\\.WriteAt\\b' internal/adapter/ | grep -v '^internal/adapter/common/'` returns 0 lines (no direct engine.WriteAt calls outside common/)
    - `grep -rE 'blockStore\\.ReadAt\\b' internal/adapter/ | grep -v '^internal/adapter/common/'` returns 0 lines (READ side stays in common/ only — confirms plan 01 completeness)
    - `go build ./...` exits 0 (compile catches any stray `n, err := common.WriteToBlockStore(...)` bindings)
    - `go vet ./...` exits 0
    - `go test -race -count=1 ./...` exits 0 — full suite green (passthrough = zero behavior change)
    - If Option B chosen: at least one `common.ResolveForWrite` call remains in commit.go files (confirms the block-store acquisition seam is already in place even when COMMIT engine-method variance is accepted)
  </acceptance_criteria>
  <done>Every NFS/SMB WRITE call site routes through common.WriteToBlockStore with error-only return binding; READ side confirmed clean (no direct engine.ReadAt outside common/); COMMIT/flush treated per the chosen option (A or B, documented in commit message); full test suite -race green with zero behavioral delta.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit ADAPT-04 as one atomic commit</name>
  <files>(git metadata only)</files>
  <read_first>
    - .planning/phases/09-adapter-layer-cleanup-adapt/09-CONTEXT.md (D-16)
    - CLAUDE.md (sign, no Claude Code mentions)
    - ~/CLAUDE.md
  </read_first>
  <action>
Stage and commit as one signed atomic commit. Message template:

```
adapter(common): add WriteToBlockStore seam for Phase-12 []BlockRef (ADAPT-04)

Introduce common.WriteToBlockStore as the structural twin of
ReadFromBlockStore. Today it is a direct engine.WriteAt passthrough
(error-only return, matching the engine contract). Phase 12 (#423)
will extend both helpers to fetch FileAttr.Blocks (META-01) and pass
resolved []BlockRef to engine (API-01) — protocol handlers will NOT
change.

Refactor NFSv3, NFSv4, and SMB v2 WRITE call sites to route through
the helper; no direct engine.WriteAt calls remain outside common/.
COMMIT/flush paths: [Option A OR B — document which was chosen and why].

common/doc.go documents the Phase-12 seam explicitly.

Refs #427.
```

Rules: `git commit -S`, no Claude Code / Co-Authored-By lines, stage explicit paths only, verify `go build && go vet && go test -race` green at HEAD.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -Eq "adapter\\(common\\).*ADAPT-04" && git log -1 --show-signature 2>&1 | grep -Eq "Good signature|gpg: Signature made|Good \"[A-Za-z]* signature\"" && ! git log -1 --format=%B | grep -iE "claude|co-authored-by"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `adapter\\(common\\).*ADAPT-04`
    - `git log -1 --show-signature` shows a good signature
    - `git log -1 --format=%B` does NOT contain "claude" (case-insensitive) or "Co-Authored-By"
    - `go test -race -count=1 ./...` exits 0 at HEAD
  </acceptance_criteria>
  <done>Plan 04 changes committed as one signed atomic commit labeled ADAPT-04; COMMIT/flush option choice documented in message; no Claude mentions; -race green at HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wire protocol (offset, length) -> common helper -> engine | Phase 09 preserves this chain unchanged; Phase 12 adds BlockRef resolution inside common/. The trust boundary between protocol-validated wire args and the engine's execution is exactly the common helper boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | Call-site refactor silently changes a passed argument (e.g., offset arithmetic) | mitigate | The refactor is a literal textual substitution with NO arg reordering or transformation; task 2 enumerates all sites BEFORE editing; full -race test suite at HEAD verifies zero behavioral delta. Code review spot-checks arg order per site. |
| T-09-14 | DoS | WriteToBlockStore leaks a large source slice into a long-lived closure | accept | No closures created; direct synchronous passthrough. Caller owns the src slice; common/ never retains it. |
| T-09-15 | Elevation of Privilege | Phase-12 seam executor forgets to re-check permissions when adding BlockRef fetch | accept (Phase-12 concern) | Phase 09 is call-site-only; auth is enforced at metadata layer (invariant 2) which is unchanged. Phase 12's BlockRef-fetch implementation will inherit the existing permission-check path (it's already in the metadata service). Flagged here for Phase-12 planners as a "do not forget" seam property. |
</threat_model>

<verification>
Full phase verification for plan 04:

- `go build ./...` exits 0
- `go vet ./...` exits 0
- `go test -race -count=1 ./...` exits 0
- `grep -rE 'blockStore\\.(ReadAt|WriteAt)\\b' internal/adapter/ | grep -v '^internal/adapter/common/'` returns 0 lines (single-edit-point contract)
- `grep -q "func WriteToBlockStore" internal/adapter/common/write_payload.go` — new helper exists
- `! grep -Eq "\\) \\(int, error\\)" internal/adapter/common/write_payload.go` — WriteToBlockStore returns error only (engine parity)
- `grep -q "Phase-12 seam" internal/adapter/common/doc.go` — seam doc updated
- `git log -1 --format=%B | grep -q "ADAPT-04"` — ADAPT-NN traceability
</verification>

<success_criteria>
- [ ] common/write_payload.go created with WriteToBlockStore passthrough + unit tests (error-only return matching engine.WriteAt)
- [ ] common/doc.go has explicit Phase-12 seam section naming both helpers + META-01/API-01 reference
- [ ] NFSv3, NFSv4, SMB v2 WRITE call sites all route through common.WriteToBlockStore with `err := ...` binding (no byte count)
- [ ] No direct engine.WriteAt / engine.ReadAt calls remain outside common/ (single edit point for Phase 12)
- [ ] COMMIT/flush paths handled per documented option (A or B) in the commit message
- [ ] Zero behavioral change — full test suite -race green
- [ ] One signed atomic commit labeled ADAPT-04
</success_criteria>

<output>
After completion, create `.planning/phases/09-adapter-layer-cleanup-adapt/09-04-SUMMARY.md` describing:
- Whether Option A or B was chosen for COMMIT/flush and why
- Number of call sites migrated per protocol (NFSv3 / NFSv4 / SMB v2 — expect ~2 each for WRITE, plus COMMIT if Option A)
- Any pre-existing `n, err := blockStore.WriteAt(...)` bindings found and fixed as part of the migration (latent-bug fixes against the engine contract)
- Any direct engine.ReadAt/WriteAt calls found outside common/ during enumeration (plan 01 miss — document + migrated)
- What's next (plan 05 adds cross-protocol conformance test + docs updates per D-13 and D-17; the seam established here is the Phase 12 consumption point)
</output>
</content>
</invoke>