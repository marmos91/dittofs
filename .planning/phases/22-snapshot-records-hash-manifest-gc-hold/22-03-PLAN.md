---
phase: 22-snapshot-records-hash-manifest-gc-hold
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - pkg/controlplane/store/interface.go
  - pkg/controlplane/store/snapshots.go
  - pkg/controlplane/store/snapshots_test.go
  - pkg/controlplane/store/gorm.go
  - pkg/controlplane/models/models.go
autonomous: true
requirements: [SNAP-01, SNAP-05]
must_haves:
  truths:
    - "Store interface exposes SnapshotStore sub-interface for Create/Get/List/Delete/UpdateState"
    - "GORMStore registers models.Snapshot in AllModels() so AutoMigrate creates the snapshots table"
    - "CreateSnapshot generates a UUID if ID empty and persists the row; rejects concurrent creating per share via the unique partial index"
    - "DeleteSnapshot in a transaction removes the row by (share_name, id); ErrSnapshotNotFound on miss"
    - "ListSnapshots(shareName) returns all states ordered by created_at DESC"
  artifacts:
    - path: "pkg/controlplane/store/snapshots.go"
      provides: "SnapshotStore GORM CRUD impl + idx_share_creating fallback registration"
    - path: "pkg/controlplane/store/snapshots_test.go"
      provides: "Round-trip CRUD + unique-partial-index + state-update + ordering tests against SQLite"
    - path: "pkg/controlplane/store/interface.go"
      provides: "SnapshotStore sub-interface composed into Store"
  key_links:
    - from: "pkg/controlplane/store/snapshots.go"
      to: "pkg/controlplane/models/snapshot.go (Snapshot, StateCreating, ErrSnapshotNotFound)"
      via: "import models"
      pattern: "models\\.Snapshot|models\\.ErrSnapshotNotFound|models\\.StateCreating"
    - from: "pkg/controlplane/store/gorm.go"
      to: "pkg/controlplane/models/models.go (AllModels)"
      via: "AutoMigrate(models.AllModels()...)"
      pattern: "&Snapshot\\{\\}"
---

<objective>
Land the control plane CRUD surface for snapshots: a new `SnapshotStore` sub-interface in `store/interface.go`, a `GORMStore` implementation in `store/snapshots.go`, automigrate registration via `models.AllModels()`, and a post-AutoMigrate fallback that ensures the `idx_share_creating` partial unique index exists when the GORM dialect cannot synthesize it from the struct tag.

Purpose: SNAP-05 needs full CRUD lifecycle for snapshot records. CONTEXT D-08 requires unique partial index (`where state='creating'`); D-13 specifies the five methods; D-14 mandates no filtering in `ListSnapshots`; D-15 specifies cascade behavior. Plan 22-01 produced the model — this plan persists it.

Output: SnapshotStore sub-interface composed into the top-level `Store`, GORM CRUD with `getByField`/`listAll` helper reuse, AllModels registration, idx_share_creating verified across SQLite + Postgres dialects via post-migrate raw SQL fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-CONTEXT.md
@.planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-01-PLAN.md

<interfaces>
<!-- Extracted from pkg/controlplane/store/{interface.go,shares.go,helpers.go,gorm.go} —
     consumers of this plan; the new SnapshotStore must follow these conventions. -->

Sub-interface composition (interface.go:531):

```go
type Store interface {
    UserStore
    GroupStore
    ShareStore
    PermissionStore
    MetadataStoreConfigStore
    BlockStoreConfigStore
    AdapterStore
    SettingsStore
    AdminStore
    HealthStore
}
```

Generic CRUD helpers (helpers.go:21, helpers.go:39):

```go
func getByField[T any](db *gorm.DB, ctx context.Context, field string, value any, notFoundErr error, preloads ...string) (*T, error)
func listAll[T any](db *gorm.DB, ctx context.Context, preloads ...string) ([]*T, error)
```

UUID generation pattern (shares.go:47): `id = uuid.New().String()` from `github.com/google/uuid`.

Unique-constraint detection (gorm.go:357): `isUniqueConstraintError(err) bool` checks both SQLite and Postgres error strings.

AllModels (models/models.go:5): returns `[]any` of all registered models for `db.AutoMigrate(models.AllModels()...)`.

AutoMigrate happens at gorm.go:254. Post-AutoMigrate raw-SQL fixups happen at gorm.go:289+ (precedent for the idx_share_creating fallback).

GORMStore.DB() (gorm.go:352): returns `*gorm.DB` — used by snapshots_test.go to drive raw SQL for partial-index existence assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SnapshotStore sub-interface + compose into Store + register model</name>
  <read_first>
    - pkg/controlplane/store/interface.go (lines 1-100 for UserStore shape + lines 520-543 for Store composition)
    - pkg/controlplane/models/models.go (full — AllModels)
    - pkg/controlplane/models/snapshot.go (Plan 22-01's model)
    - .planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-CONTEXT.md (D-13, D-14)
  </read_first>
  <behavior>
    - `store.SnapshotStore` interface declares exactly the 5 methods from D-13
    - `store.Store` embeds `SnapshotStore`
    - `models.AllModels()` includes `&Snapshot{}` so `db.AutoMigrate` creates the `snapshots` table
    - Code compiles even with no GORM impl yet (GORMStore does not implement `SnapshotStore` — that's Task 2; this intermediate state is acceptable because no caller asks for the full `Store` interface in the test files that build at this checkpoint)
  </behavior>
  <action>
    Edit `pkg/controlplane/store/interface.go` to add the new sub-interface BEFORE the `Store` composite at the bottom of the file. Place the block immediately AFTER `AdminStore` and BEFORE `HealthStore` (mirrors the snapshot subsystem's lifecycle position — admin/setup-adjacent, not data-plane).

    `SnapshotStore` declares per D-13 (do NOT add or rename methods):

      - `CreateSnapshot(ctx context.Context, snap *models.Snapshot) (string, error)` — generates UUID if `snap.ID == ""`; returns the persisted ID. `models.ErrDuplicateShare` analog: on unique-partial-index violation returns the same error message string but wrap-able via `errors.Is(err, models.ErrSnapshotStateConflict)` (the only legal "already exists" condition for snapshots is "another creating snapshot for this share")
      - `GetSnapshot(ctx context.Context, shareName, snapID string) (*models.Snapshot, error)` — returns `models.ErrSnapshotNotFound` on miss
      - `ListSnapshots(ctx context.Context, shareName string) ([]*models.Snapshot, error)` — D-14: returns ALL states; ordered by `created_at DESC` per D-09
      - `DeleteSnapshot(ctx context.Context, shareName, snapID string) error` — `models.ErrSnapshotNotFound` on miss; per D-07 is hard-delete (no `deleting` state); filesystem cleanup is Plan 22-05's job and lives on the Runtime, not the store
      - `UpdateSnapshotState(ctx context.Context, id, state string) error` — `models.ErrSnapshotNotFound` on miss; per D-06 must accept `creating -> ready`, `creating -> failed`, and `failed -> creating` (retry); reject any other transition with `models.ErrSnapshotStateConflict`

    Docstrings follow the same prose style as `ShareStore` / `UserStore` — one sentence per method describing the failure mode and any cross-method invariants. Make D-13/D-14 explicit in the godoc (e.g. "ListSnapshots returns rows in ALL states (creating, ready, failed); callers filter as needed — see D-14").

    Add `SnapshotStore` to the `Store` interface composite at line 531 immediately after `AdminStore` (matching the new sub-interface's position above).

    Edit `pkg/controlplane/models/models.go` to add `&Snapshot{}` to the `AllModels()` return slice. Place it AFTER `&Share{}` so the snapshots table is created after shares — matters in case a future FK CASCADE constraint is added at the DB layer (currently CASCADE is application-level per D-15).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go vet ./pkg/controlplane/store/... ./pkg/controlplane/models/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type SnapshotStore interface' pkg/controlplane/store/interface.go`
    - `grep -q 'CreateSnapshot(ctx context.Context, snap \*models.Snapshot) (string, error)' pkg/controlplane/store/interface.go`
    - `grep -q 'GetSnapshot(ctx context.Context, shareName, snapID string)' pkg/controlplane/store/interface.go`
    - `grep -q 'ListSnapshots(ctx context.Context, shareName string)' pkg/controlplane/store/interface.go`
    - `grep -q 'DeleteSnapshot(ctx context.Context, shareName, snapID string) error' pkg/controlplane/store/interface.go`
    - `grep -q 'UpdateSnapshotState(ctx context.Context, id, state string) error' pkg/controlplane/store/interface.go`
    - `grep -q 'SnapshotStore$\|	SnapshotStore' pkg/controlplane/store/interface.go` (composed into Store)
    - `grep -q '&Snapshot{}' pkg/controlplane/models/models.go`
    - `go vet ./pkg/controlplane/store/... ./pkg/controlplane/models/...` exits 0
    - `go build ./pkg/controlplane/models/...` exits 0 (GORMStore-as-Store may not yet compile cleanly — Task 2 closes that gap; this intermediate is acceptable per Wave 2 commit ordering)
  </acceptance_criteria>
  <done>SnapshotStore interface declared; composed into Store; model registered in AllModels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GORM SnapshotStore implementation + state-machine guard</name>
  <read_first>
    - pkg/controlplane/store/shares.go (lines 1-150 — Create/Get/List/Delete patterns)
    - pkg/controlplane/store/helpers.go (full — getByField / listAll generic helpers)
    - pkg/controlplane/store/gorm.go (lines 140-260 — GORMStore struct + isUniqueConstraintError + AutoMigrate site)
    - pkg/controlplane/models/snapshot.go (Plan 22-01)
    - .planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-CONTEXT.md (D-06, D-08, D-09, D-15)
  </read_first>
  <behavior>
    - `(*GORMStore).CreateSnapshot` generates UUID via `uuid.New().String()` if empty; transactional insert; surfaces `models.ErrSnapshotStateConflict` on unique-partial-index violation (a second `creating` for the same share)
    - `(*GORMStore).GetSnapshot` delegates to `getByField[models.Snapshot]` with a composite WHERE clause `share_name = ? AND id = ?` (mini-helper or direct `s.db.WithContext(ctx).Where(...).First(...)` is acceptable — getByField only handles single-field lookups, so a direct call is the right choice here)
    - `(*GORMStore).ListSnapshots` runs `s.db.WithContext(ctx).Where("share_name = ?", shareName).Order("created_at DESC").Find(&snaps)`
    - `(*GORMStore).DeleteSnapshot` is a transactional `Where("share_name = ? AND id = ?").Delete(&models.Snapshot{})`; if `RowsAffected == 0` returns `ErrSnapshotNotFound`
    - `(*GORMStore).UpdateSnapshotState` enforces the state machine per D-06: load the row, validate the transition, then update. Allowed: `creating -> ready`, `creating -> failed`, `failed -> creating`. Any other (e.g. `ready -> creating`, `ready -> failed`, `creating -> creating`, `failed -> failed`) returns `ErrSnapshotStateConflict`. Wrapped in a single transaction with `s.db.WithContext(ctx).Transaction(...)`
  </behavior>
  <action>
    Create `pkg/controlplane/store/snapshots.go` following `pkg/controlplane/store/shares.go` style (no `package` comment — already in `gorm.go`; imports: `context`, `errors`, `time`, `github.com/google/uuid`, `gorm.io/gorm`, `github.com/marmos91/dittofs/pkg/controlplane/models`).

    Five methods on `*GORMStore`:

      1. `CreateSnapshot`:
         - If `snap.ID == ""`, generate `uuid.New().String()` (D-09).
         - If `snap.State == ""`, default to `models.StateCreating`.
         - Capture `now := time.Now()`; set `snap.CreatedAt = now`; set `snap.UpdatedAt = now`.
         - Run inside `s.db.WithContext(ctx).Transaction(...)`; on `tx.Create(snap).Error`:
            - if `isUniqueConstraintError(err)` AND `snap.State == models.StateCreating` -> return `models.ErrSnapshotStateConflict`
            - else return the raw error
         - Return `snap.ID, nil`.

      2. `GetSnapshot`:
         - Single-row lookup with `s.db.WithContext(ctx).Where("share_name = ? AND id = ?", shareName, snapID).First(&snap).Error`
         - On `gorm.ErrRecordNotFound` return `models.ErrSnapshotNotFound` (via existing `convertNotFoundError` helper from gorm.go).
         - Otherwise return `&snap, nil`.

      3. `ListSnapshots`:
         - `var snaps []*models.Snapshot`
         - `s.db.WithContext(ctx).Where("share_name = ?", shareName).Order("created_at DESC").Find(&snaps)` (D-09 ordering)
         - Return `snaps, err`. Per D-14 do NOT filter by state.

      4. `DeleteSnapshot`:
         - Transactional `Where("share_name = ? AND id = ?", shareName, snapID).Delete(&models.Snapshot{})`
         - If `result.RowsAffected == 0` return `models.ErrSnapshotNotFound`
         - Plan 22-05 owns the filesystem cleanup hook in `Runtime.RemoveShare` per D-15; per D-07 the DB row is the source of truth, so hard-delete here is sufficient on its own.

      5. `UpdateSnapshotState`:
         - Run in transaction: load row by `id`; if not found return `models.ErrSnapshotNotFound`.
         - Validate transition via a `validateStateTransition(current, next string) error` private helper:
             - `(creating, ready)` -> nil
             - `(creating, failed)` -> nil
             - `(failed, creating)` -> nil (D-06 retry)
             - everything else (including same-state) -> `models.ErrSnapshotStateConflict`
         - On valid transition: `tx.Model(&snap).Updates(map[string]any{"state": state, "updated_at": time.Now()}).Error`.

    The validateStateTransition helper goes in `snapshots.go` (not interface.go) as a private function — keeps state-machine logic colocated with the impl.

    Also edit `pkg/controlplane/store/gorm.go` to add a Post-AutoMigrate fallback (idempotent) for the `idx_share_creating` partial unique index. Place inside `New(...)` AFTER `db.AutoMigrate(models.AllModels()...)` returns success and BEFORE `EnsureAdapterSettings`. Implementation: detect dialect via `config.Type`, then:

      - SQLite: `db.Exec("CREATE UNIQUE INDEX IF NOT EXISTS idx_share_creating ON snapshots(share_name) WHERE state = 'creating'")` — SQLite supports `WHERE` partial indexes.
      - Postgres: same `CREATE UNIQUE INDEX IF NOT EXISTS idx_share_creating ON snapshots(share_name) WHERE state = 'creating'`.

    Both dialects support this syntax — the fallback exists because GORM v1.31.x is known to drop the `where:` clause from struct-tag partial indexes on some adapters. Emit a `logger.Debug` line "snapshots: ensured idx_share_creating partial unique index". Wrap in `if err != nil { return nil, fmt.Errorf("failed to ensure idx_share_creating: %w", err) }` — startup must not silently proceed without the unique guard.

    No changes to `RemoveShare` here — Plan 22-05 owns the cascade-delete hook (D-15). The DB-level FK CASCADE in the struct tag is best-effort; the application-level cleanup is the source of truth.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./pkg/controlplane/... && go vet ./pkg/controlplane/...</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/controlplane/store/snapshots.go` exists
    - `grep -q 'func (s \*GORMStore) CreateSnapshot' pkg/controlplane/store/snapshots.go`
    - `grep -q 'func (s \*GORMStore) GetSnapshot' pkg/controlplane/store/snapshots.go`
    - `grep -q 'func (s \*GORMStore) ListSnapshots' pkg/controlplane/store/snapshots.go`
    - `grep -q 'func (s \*GORMStore) DeleteSnapshot' pkg/controlplane/store/snapshots.go`
    - `grep -q 'func (s \*GORMStore) UpdateSnapshotState' pkg/controlplane/store/snapshots.go`
    - `grep -q 'uuid.New().String()' pkg/controlplane/store/snapshots.go`
    - `grep -q 'isUniqueConstraintError' pkg/controlplane/store/snapshots.go`
    - `grep -q 'created_at DESC' pkg/controlplane/store/snapshots.go`
    - `grep -q 'validateStateTransition' pkg/controlplane/store/snapshots.go`
    - `grep -q 'idx_share_creating' pkg/controlplane/store/gorm.go` (post-AutoMigrate fallback installed)
    - `grep -q 'WHERE state = .creating.' pkg/controlplane/store/gorm.go`
    - `go build ./pkg/controlplane/...` exits 0 (GORMStore now implements the full Store interface)
    - `go vet ./pkg/controlplane/...` exits 0
  </acceptance_criteria>
  <done>GORMStore implements SnapshotStore; AutoMigrate runs against SQLite without error; idx_share_creating fallback installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SnapshotStore CRUD tests against SQLite</name>
  <read_first>
    - pkg/controlplane/store/shares.go (Create + tx patterns for fixture setup)
    - pkg/controlplane/store/stores_test.go (testing patterns — fixture setup with in-memory SQLite if present; otherwise use `t.TempDir()` + sqlite file)
    - pkg/controlplane/store/snapshots.go (Task 2)
    - pkg/controlplane/store/gorm.go (idx_share_creating fallback)
  </read_first>
  <behavior>
    - `TestSnapshot_CreateGetList_RoundTrip` — create 3 snapshots for one share (one `creating`, two `ready`); ListSnapshots returns all 3 ordered by created_at DESC
    - `TestSnapshot_CreateConcurrent_RejectsSecondCreating` — first CreateSnapshot for share `s` with State=creating succeeds; second CreateSnapshot for the same share with State=creating returns `models.ErrSnapshotStateConflict`
    - `TestSnapshot_CreateMultiple_ReadyAllowed` — many `ready` snapshots per share are permitted (D-08)
    - `TestSnapshot_Get_NotFound_ReturnsSentinel` — `GetSnapshot("noshare", "noid")` returns `models.ErrSnapshotNotFound`
    - `TestSnapshot_Delete_RemovesRow` — DeleteSnapshot removes the row; subsequent GetSnapshot returns ErrSnapshotNotFound; second DeleteSnapshot also returns ErrSnapshotNotFound
    - `TestSnapshot_UpdateState_AllowedTransitions` — table-driven: `creating->ready` OK, `creating->failed` OK, `failed->creating` OK
    - `TestSnapshot_UpdateState_RejectsDisallowed` — table-driven: `ready->creating`, `ready->failed`, `failed->failed`, `creating->creating` all return ErrSnapshotStateConflict
    - `TestSnapshot_idxShareCreating_ExistsAfterMigrate` — query `sqlite_master` (or `pg_indexes` if dialect == postgres) for an entry where the name matches `idx_share_creating` and confirm partial index installation (defense-in-depth around Task 2's fallback)
  </behavior>
  <action>
    Create `pkg/controlplane/store/snapshots_test.go`. Use the same test fixture pattern as `pkg/controlplane/store/shares_test.go` (if absent, follow `stores_test.go`) — typically `setupTestStore(t)` returns a `*GORMStore` backed by an in-memory or temp-dir SQLite. Reuse if present; do not invent a new fixture pattern.

    Each test calls `setupTestStore(t)`, populates a share fixture if one is required for FK satisfaction (the application-level FK is just a column with an index — the DB does not actually CASCADE so the FK target row is not strictly required, but tests should mirror real usage; if `Snapshot.ShareName` has no DB-level FK constraint, no share row is needed).

    The `TestSnapshot_idxShareCreating_ExistsAfterMigrate` test uses raw SQL against `s.DB()`:

      `SELECT name FROM sqlite_master WHERE type='index' AND name='idx_share_creating'`

    (Postgres equivalent: `SELECT indexname FROM pg_indexes WHERE indexname='idx_share_creating'`.) Skip the test under Postgres by default unless a `DITTOFS_TEST_POSTGRES_DSN` env var is set — mirror the precedent set by Phase 14 Plan 01 conformance tests.

    `TestSnapshot_UpdateState_RejectsDisallowed` is the load-bearing state-machine test — table-driven with rows describing the from-state and the rejected to-state.

    Helper `mkSnap(shareName, state string) *models.Snapshot` returns a minimal-fields snapshot (no ID set so CreateSnapshot generates one).

    Use `errors.Is(err, models.ErrSnapshotStateConflict)` and `errors.Is(err, models.ErrSnapshotNotFound)` in assertions — not string comparison.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go test ./pkg/controlplane/store/... -run 'TestSnapshot_' -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/controlplane/store/... -run 'TestSnapshot_' -count=1 -race` exits 0
    - `grep -c '^func TestSnapshot_' pkg/controlplane/store/snapshots_test.go` returns at least `8`
    - `grep -q 'TestSnapshot_CreateConcurrent_RejectsSecondCreating' pkg/controlplane/store/snapshots_test.go`
    - `grep -q 'TestSnapshot_UpdateState_RejectsDisallowed' pkg/controlplane/store/snapshots_test.go`
    - `grep -q 'idx_share_creating' pkg/controlplane/store/snapshots_test.go`
    - `grep -q 'errors.Is.*ErrSnapshotStateConflict' pkg/controlplane/store/snapshots_test.go`
    - `grep -q 'errors.Is.*ErrSnapshotNotFound' pkg/controlplane/store/snapshots_test.go`
    - No imports of `github.com/stretchr/testify`
  </acceptance_criteria>
  <done>All 8+ CRUD + state-machine tests pass on SQLite; idx_share_creating verified present; state-conflict sentinel surfaces correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> SnapshotStore | Caller-supplied IDs and state strings reach SQL. UUID is constrained to 36 chars by primary-key tag; state strings go through validateStateTransition. |
| GORM -> dialect | Partial-index synthesis is dialect-dependent — the post-AutoMigrate fallback is the mitigation for silent failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | UpdateSnapshotState | mitigate | validateStateTransition is exhaustive — any unrecognized transition returns ErrSnapshotStateConflict; D-06 transitions whitelisted explicitly |
| T-22-03-02 | Elevation of Privilege | CreateSnapshot | mitigate | CreateSnapshot does NOT accept a caller-supplied ID; UUID generation lives in the store layer so callers cannot collide with other resources' IDs |
| T-22-03-03 | Tampering | Concurrent creating snapshots | mitigate | idx_share_creating partial unique index enforced at DB layer; CreateSnapshot maps the constraint violation to ErrSnapshotStateConflict (no race window between SELECT-then-INSERT) |
| T-22-03-04 | Repudiation | DeleteSnapshot | accept | No audit log added — snapshot lifecycle events are not currently audited; if compliance requires it, add to the audit subsystem in a separate plan (out of scope per Less-Is-More) |
| T-22-03-SC | Tampering | go.mod additions | accept | All imports (`gorm.io/gorm`, `github.com/google/uuid`, stdlib) already in go.mod; no Package Legitimacy Gate trigger |
</threat_model>

<verification>
- `go build ./pkg/controlplane/...` exits 0
- `go vet ./pkg/controlplane/...` exits 0
- `go test ./pkg/controlplane/store/... -run 'TestSnapshot_' -count=1 -race` exits 0
- Existing store tests still pass: `go test ./pkg/controlplane/store/... -count=1` exits 0
</verification>

<success_criteria>
- SnapshotStore sub-interface composed into Store
- GORMStore implements all 5 methods with correct error semantics
- AutoMigrate creates the `snapshots` table; idx_share_creating partial unique index exists post-startup
- State-machine transitions enforced at the store layer per D-06
- All snapshot tests pass under `-race`
</success_criteria>

<output>
Create `.planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-03-SUMMARY.md` summarizing the interface shape, validateStateTransition table, the idx_share_creating fallback strategy, and any dialect-specific deviations.
</output>
