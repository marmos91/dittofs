---
phase: 22-snapshot-records-hash-manifest-gc-hold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/snapshot/manifest.go
  - pkg/snapshot/manifest_test.go
  - pkg/snapshot/doc.go
autonomous: true
requirements: [SNAP-02]
must_haves:
  truths:
    - "WriteManifest(io.Writer, *HashSet) writes one hex line per ContentHash, sorted ascending, terminated by \\n"
    - "ReadManifest(io.Reader) returns a *HashSet containing exactly the hashes written"
    - "WriteManifestAtomic writes temp file + fsync + rename so partial files are invisible to readers"
    - "Reader rejects malformed lines (wrong length, non-hex, blank with content) with a typed error"
  artifacts:
    - path: "pkg/snapshot/manifest.go"
      provides: "WriteManifest / WriteManifestAtomic / ReadManifest + ErrInvalidManifestLine sentinel"
    - path: "pkg/snapshot/manifest_test.go"
      provides: "Round-trip + atomicity + malformed-line + large-set scanner-buffer tests"
    - path: "pkg/snapshot/doc.go"
      provides: "Package godoc explaining manifest format (plain text, sorted hex, one per line)"
  key_links:
    - from: "pkg/snapshot/manifest.go"
      to: "pkg/blockstore/hashset.go (HashSet.Sorted, NewHashSet, HashSet.Add)"
      via: "import \"github.com/marmos91/dittofs/pkg/blockstore\""
      pattern: "blockstore\\.NewHashSet\\("
    - from: "pkg/snapshot/manifest.go"
      to: "pkg/blockstore/types.go (ContentHash.String, ParseContentHash)"
      via: "ContentHash hex round-trip"
      pattern: "ParseContentHash\\("
---

<objective>
Land the on-disk hash manifest writer/reader as a brand-new `pkg/snapshot` package. Three public functions: `WriteManifest(io.Writer, *blockstore.HashSet) error`, `WriteManifestAtomic(path string, hs *blockstore.HashSet) error`, `ReadManifest(io.Reader) (*blockstore.HashSet, error)`. Plus a typed `ErrInvalidManifestLine` sentinel and a package doc comment that pins the wire format.

Purpose: SNAP-02 specifies plain-text hex, one ContentHash per line, sorted ascending. Phase 22's `SnapshotHoldProvider` and Phase 23's sync gate both consume the reader; Phase 23's orchestrator uses `WriteManifestAtomic` to crash-safely materialize a snapshot. Per D-04 the manifest file is the ground truth — every other piece of Phase 22 reads through these helpers.

Output: a new self-contained package with no dependencies outside `pkg/blockstore` + stdlib. Independent of Plan 22-01 (this plan does not import `pkg/controlplane/models`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-CONTEXT.md
@.planning/phases/20-backupable-interface-conformance-suite-cleanup/20-CONTEXT.md

<interfaces>
<!-- Extracted from pkg/blockstore/hashset.go and pkg/blockstore/types.go — the package consumes these verbatim -->

From pkg/blockstore/hashset.go:

```go
type HashSet struct { /* unexported */ }
func NewHashSet(sizeHint int) *HashSet
func (s *HashSet) Add(h ContentHash)
func (s *HashSet) Contains(h ContentHash) bool
func (s *HashSet) Len() int
func (s *HashSet) Sorted() []ContentHash          // returned in ascending bytes.Compare order
func (s *HashSet) Hashes() map[ContentHash]struct{}
```

From pkg/blockstore/types.go:

```go
const HashSize = 32                                // BLAKE3-256
type ContentHash [HashSize]byte
func (h ContentHash) String() string                // hex.EncodeToString(h[:])  -> 64 hex chars
func ParseContentHash(s string) (ContentHash, error) // checks length, hex.DecodeString
```

`HashSet.Sorted()` already returns hashes in ascending bytes order — `WriteManifest` MUST consume that order verbatim (D-16). Do not re-sort on the wire.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package skeleton + doc.go + ErrInvalidManifestLine sentinel</name>
  <read_first>
    - pkg/blockstore/hashset.go (full)
    - pkg/blockstore/types.go (lines 1-100 — ContentHash + ParseContentHash)
    - pkg/blockstore/doc.go (style reference for new package godoc)
  </read_first>
  <behavior>
    - `pkg/snapshot/` directory exists with `package snapshot` declared in `doc.go`
    - `ErrInvalidManifestLine` is a package-level `error` declared via `errors.New` (consistent with sentinel convention elsewhere in repo)
    - `go build ./pkg/snapshot/...` succeeds against an otherwise-empty package
  </behavior>
  <action>
    Create `pkg/snapshot/doc.go` with:

      1. `// Package snapshot ...` godoc covering: this package owns the on-disk hash manifest format used by `pkg/controlplane/runtime` snapshot lifecycle. Wire format spec (verbatim per D-16/D-17/D-18):
         - Plain text, ASCII only
         - One hex-encoded blockstore.ContentHash per line (64 hex chars)
         - Lines terminated by `\n`
         - Lines sorted ascending in bytes order (matches `HashSet.Sorted()`)
         - No header, no footer, no comments, no blank lines
         - File extension `.hashes` by convention (see SnapshotDir layout)

         Document the atomic-write contract (D-19): WriteManifestAtomic uses temp file + fsync + rename so readers either see a complete manifest or no file at all.

      2. `package snapshot`

      3. A second file `pkg/snapshot/manifest.go` containing JUST:
         - `package snapshot`
         - imports left empty for now
         - `var ErrInvalidManifestLine = errors.New("snapshot: invalid manifest line")`

         This split keeps Task 2's diff focused on the three public functions and lets `go build` succeed at the end of Task 1 in isolation.

    Do NOT add `MarshalBinary`/`UnmarshalBinary` methods to HashSet (Phase 20 D-12 forbids). The wire format lives entirely in this package.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./pkg/snapshot/... && go vet ./pkg/snapshot/... && grep -q 'ErrInvalidManifestLine\s*=\s*errors.New' pkg/snapshot/manifest.go && grep -q '^package snapshot' pkg/snapshot/doc.go</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/snapshot/doc.go` exists and starts with a `// Package snapshot` doc comment block (`head -1 pkg/snapshot/doc.go` returns a `//` line; `grep -q '^// Package snapshot' pkg/snapshot/doc.go` exits 0)
    - `pkg/snapshot/manifest.go` exists and contains exactly the sentinel + package + imports — no function declarations yet
    - `grep -c '^func ' pkg/snapshot/manifest.go` returns `0` after Task 1
    - `go build ./pkg/snapshot/...` exits 0
    - `go vet ./pkg/snapshot/...` exits 0
  </acceptance_criteria>
  <done>Package compiles; doc + sentinel in place; ready for Task 2 to add the three public functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write + Read + Atomic-Write implementations</name>
  <read_first>
    - pkg/snapshot/manifest.go (file from Task 1)
    - pkg/blockstore/hashset.go (full)
    - pkg/blockstore/types.go (lines 1-100)
  </read_first>
  <behavior>
    - `WriteManifest(w, hs)` writes `len(hs.Sorted())` lines; each line is `hash.String() + "\n"`; uses a `bufio.Writer` per D-17; flushes before returning
    - `WriteManifestAtomic(path, hs)` writes to `<path>.tmp` in the SAME directory as `path`, calls `f.Sync()` before `f.Close()`, then `os.Rename(<path>.tmp, path)` (D-19)
    - `ReadManifest(r)` returns a `*blockstore.HashSet` populated via `blockstore.NewHashSet(0)` (or sized hint when discoverable) + `Add` per line; uses `bufio.Scanner` with a custom buffer sized for hex64+LF (~96 bytes) and an oversize Buffer cap of `1 << 20` (1 MB) for safety against pathological input
    - Each input line must match `^[0-9a-fA-F]{64}$` after `TrimRight(line, "\r")`; the reader uses `ParseContentHash` and wraps mismatch + parse errors as `fmt.Errorf("%w: line %d: %v", ErrInvalidManifestLine, lineNum, parseErr)`
    - Empty input (zero lines) returns an empty `*HashSet` with `nil` error — a snapshot with zero blocks is a valid edge case (no files in share at create time)
    - Trailing `\n` on the last line is required; reader is tolerant of `\r\n` (strip `\r`); writer always emits `\n`
  </behavior>
  <action>
    Edit `pkg/snapshot/manifest.go` adding the three public functions exactly per the signatures in D-17 / D-18 / D-19:

      `func WriteManifest(w io.Writer, hs *blockstore.HashSet) error`
      `func WriteManifestAtomic(path string, hs *blockstore.HashSet) error`
      `func ReadManifest(r io.Reader) (*blockstore.HashSet, error)`

    Imports needed: `bufio`, `errors`, `fmt`, `io`, `os`, `path/filepath`, `strings`, and `github.com/marmos91/dittofs/pkg/blockstore`.

    Implementation notes (NOT code):

      - `WriteManifest`: `bw := bufio.NewWriter(w)`; for each `h := range hs.Sorted()` write `h.String()` then `"\n"` (avoid `fmt.Fprintf` allocation on the hot path — use direct `bw.WriteString`); end with `bw.Flush()` and return any flush error wrapped with `fmt.Errorf("snapshot: write manifest: %w", err)`.

      - `WriteManifestAtomic`: derive `tmpPath := path + ".tmp"` in the SAME directory (NOT `os.TempDir()` — atomic rename requires same filesystem). `os.OpenFile(tmpPath, O_WRONLY|O_CREATE|O_TRUNC, 0o644)`. Call `WriteManifest` against the file handle. Then `f.Sync()`, `f.Close()`, then `os.Rename(tmpPath, path)`. On any error before rename: `os.Remove(tmpPath)` best-effort (defer + `if err != nil`). Per CONTEXT D-19, this is the canonical crash-safe write path; matches the temp+rename pattern used elsewhere in the codebase (e.g. `engine/gcstate.go` last-run.json writer).

      - `ReadManifest`: `hs := blockstore.NewHashSet(0)`; `sc := bufio.NewScanner(r)`; allocate explicit buffer per D-CONTEXT's "scanner buffer sizing" note in Claude's Discretion section — `buf := make([]byte, 0, 128); sc.Buffer(buf, 1<<20)`. Iterate `for sc.Scan() { ... }`. For each line: `line := strings.TrimRight(sc.Text(), "\r")`. Use `blockstore.ParseContentHash(line)` directly; on error return `fmt.Errorf("%w: line %d: %v", ErrInvalidManifestLine, lineNum, parseErr)`. On success `hs.Add(h)`. After loop, return `sc.Err()` wrapped if non-nil. Reject the corner case where two adjacent lines are identical? — Per D-CONTEXT D-16: HashSet de-dupes by definition, so duplicate lines collapse silently. Do NOT raise an error on dup; that matches HashSet.Add semantics.

    No multi-pass over the writer (D-17: "low write-side memory"). No re-sorting on read — HashSet is unordered by definition, only Sorted() imposes order.

    DO NOT add a `WriteManifestAtomicWithDirSync` or fsync the parent directory in this plan. CONTEXT D-19 specifies "temp-file + fsync + rename" — parent-dir fsync hardens crash semantics on power loss but is OUT OF SCOPE per Less-Is-More (feedback in MEMORY.md). If durability becomes load-bearing in Phase 23 orchestration, add it there as a single-line follow-up.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./pkg/snapshot/... && go vet ./pkg/snapshot/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func WriteManifest(w io.Writer, hs \*blockstore.HashSet) error' pkg/snapshot/manifest.go`
    - `grep -q 'func WriteManifestAtomic(path string, hs \*blockstore.HashSet) error' pkg/snapshot/manifest.go`
    - `grep -q 'func ReadManifest(r io.Reader) (\*blockstore.HashSet, error)' pkg/snapshot/manifest.go`
    - `grep -q 'bufio.NewWriter' pkg/snapshot/manifest.go` (D-17 buffered writer)
    - `grep -q 'bufio.NewScanner\|bufio.Scanner' pkg/snapshot/manifest.go` (D-18 scanner)
    - `grep -q 'sc.Buffer\|s\.Buffer' pkg/snapshot/manifest.go` (explicit buffer sizing per Claude's Discretion)
    - `grep -q 'os.Rename' pkg/snapshot/manifest.go` AND `grep -q '\.Sync()' pkg/snapshot/manifest.go` (D-19 atomic write)
    - `grep -q 'ParseContentHash' pkg/snapshot/manifest.go` (D-16 hex parsing via canonical helper)
    - `go vet ./pkg/snapshot/...` exits 0
    - Imports include `bufio`, `os`, and `github.com/marmos91/dittofs/pkg/blockstore`; do NOT include `path/filepath` for any reason other than dirname computation if used at all
  </acceptance_criteria>
  <done>Three public functions implemented per D-17/D-18/D-19; package builds clean; no panics on empty HashSet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Round-trip + atomicity + malformed-line + large-set tests</name>
  <read_first>
    - pkg/snapshot/manifest.go (Task 2 file)
    - pkg/blockstore/hashset.go (for *HashSet construction in tests)
  </read_first>
  <behavior>
    - `TestWriteRead_RoundTrip` — write a HashSet with 0, 1, and 1000 hashes; read back; assert `Len()` matches and `Contains(h)` true for every original
    - `TestWriteRead_SortedAscending` — write a HashSet with 5 hashes whose `Sorted()` order is not insertion order; assert the written-file bytes are in ascending hex line order (read file as string, `strings.Split(s, "\n")` returns sorted-ascending list)
    - `TestRead_RejectsMalformed` — input streams with: (a) line with 63 hex chars (short); (b) line with 65 hex chars (long); (c) line with non-hex char; each yields a non-nil error matching `errors.Is(err, ErrInvalidManifestLine)` and includes the line number in `err.Error()`
    - `TestRead_EmptyInput_ReturnsEmptySet` — `bytes.NewReader(nil)` returns an empty *HashSet with nil error
    - `TestRead_ToleratesCRLF` — input ending with `\r\n` parses correctly; assert one hash readable
    - `TestRead_LargeBuffer_Handles100k` — write 100,000 hashes (~6.4 MB), read back, assert `Len() == 100000` and at least 3 sampled hashes round-trip via Contains. Asserts the explicit `Buffer()` sizing covers realistic snapshot scales per CONTEXT D-18 sizing note
    - `TestWriteAtomic_CompleteFileOnly` — call WriteManifestAtomic, then verify the .tmp sidecar no longer exists after success
    - `TestWriteAtomic_NoPartialOnError` — pass a HashSet then a writer/path that fails (e.g. write to a path under a read-only dir created via `os.Chmod(0o500)`); assert the target path does NOT exist after the failed call (.tmp may exist but parent rename never happened); skip on Windows where permission semantics differ
    - `TestWriteAtomic_OverwritesExisting` — write manifest, then write a DIFFERENT manifest to the same path, read back: returns the SECOND manifest's contents
  </behavior>
  <action>
    Create `pkg/snapshot/manifest_test.go` with package `snapshot_test` (black-box; manifest API is fully public; matches `pkg/blockstore/hashset_test.go` if present, otherwise a fresh black-box test file). Tests use helper `mustHash(t, byteSeed byte) blockstore.ContentHash` that fills a ContentHash with a deterministic pattern via `for i := range h { h[i] = byteSeed + byte(i) }` — gives unique sortable hashes for tests.

    Test cases per the behavior list above. Use:

      - `bytes.NewBuffer` for write-then-read in-memory tests
      - `t.TempDir()` for atomic-write tests
      - `runtime.GOOS == "windows"` skip in `TestWriteAtomic_NoPartialOnError`
      - `strings.Split` + sort assertion (do NOT rely on map iteration order from HashSet)

    The 100k large-set test is the load-bearing scanner-buffer regression test — if Task 2's `Buffer()` sizing is missing or wrong, this is the test that catches it. Document the rationale inline.

    No use of testify or third-party assertion libs.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go test ./pkg/snapshot/... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/snapshot/... -count=1 -race` exits 0
    - `grep -c '^func Test' pkg/snapshot/manifest_test.go` returns at least `8`
    - `grep -q 'TestWriteRead_RoundTrip' pkg/snapshot/manifest_test.go`
    - `grep -q 'TestRead_LargeBuffer_Handles100k\|TestRead_LargeBuffer' pkg/snapshot/manifest_test.go`
    - `grep -q 'TestWriteAtomic' pkg/snapshot/manifest_test.go`
    - `grep -q 'errors.Is.*ErrInvalidManifestLine' pkg/snapshot/manifest_test.go`
    - `grep -q 't.TempDir()' pkg/snapshot/manifest_test.go`
    - No imports of `github.com/stretchr/testify`
    - `gofmt -l pkg/snapshot/` prints nothing
  </acceptance_criteria>
  <done>All 8+ tests pass under `-race`; round-trip semantics locked; atomicity contract pinned; malformed input rejected with line number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ReadManifest` input stream | Untrusted bytes from disk — could be corrupt, truncated, or hand-edited. ReadManifest is the parser. |
| `WriteManifestAtomic` filesystem | Disk failure mid-write must not leave a half-readable manifest visible at the canonical path (D-19). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | ReadManifest parser | mitigate | ParseContentHash enforces 32-byte length + hex; non-conforming lines return `ErrInvalidManifestLine` wrapped with line number |
| T-22-02-02 | Denial of Service | ReadManifest scanner | mitigate | Explicit `sc.Buffer(buf, 1<<20)` caps single-line memory at 1 MiB; hex64+LF is ~96 bytes, so 1 MiB headroom catches accidentally-concatenated data without unbounded allocation |
| T-22-02-03 | Tampering | WriteManifestAtomic crash window | mitigate | Temp file + fsync + rename per D-19; if the process crashes pre-rename, the canonical path still points at the previous manifest (or doesn't exist) — readers never see partial bytes |
| T-22-02-SC | Tampering | go.mod additions | accept | Plan adds zero new dependencies — `bufio`, `errors`, `fmt`, `io`, `os`, `strings` are stdlib; `pkg/blockstore` is in-tree; no Package Legitimacy Gate trigger |
</threat_model>

<verification>
- `go build ./pkg/snapshot/...` exits 0
- `go vet ./pkg/snapshot/...` exits 0
- `go test ./pkg/snapshot/... -count=1 -race` exits 0
- `gofmt -l pkg/snapshot/` prints nothing
</verification>

<success_criteria>
- WriteManifest, WriteManifestAtomic, ReadManifest exist with the exact CONTEXT D-17/D-18/D-19 signatures
- Round-trip preserves every hash through write + read on sets of size 0, 1, 100k
- Atomic write leaves no `.tmp` sidecar on success and no canonical file on failure
- Malformed input returns `ErrInvalidManifestLine` with line number
- Package builds, vets, and tests clean under `-race`
</success_criteria>

<output>
Create `.planning/phases/22-snapshot-records-hash-manifest-gc-hold/22-02-SUMMARY.md` summarizing the three functions, the sentinel, the scanner buffer size choice, and any deviation from D-17/D-18/D-19.
</output>
