---
phase: 11-cas-write-path-gc-rewrite-a2
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURE.md
  - docs/FAQ.md
  - docs/IMPLEMENTING_STORES.md
  - docs/CONFIGURATION.md
  - docs/CLI.md
autonomous: true
requirements: [BSCAS-01, BSCAS-06, GC-01, GC-02, GC-03, GC-04, STATE-01, STATE-02, STATE-03, INV-06, LSL-07, LSL-08, TD-09, INV-01, INV-03, INV-04, INV-05, BSCAS-03]
pr: docs (parallel with PR-C wave)
must_haves:
  truths:
    - "ARCHITECTURE.md describes the mark-sweep GC algorithm and the three-state lifecycle (replaces path-prefix GC description)"
    - "FAQ.md answers: How does GC work in v0.15.0? What is the dual-read window? Why are residual {payloadID}/block-... keys present after upgrade?"
    - "IMPLEMENTING_STORES.md documents the new MetadataStore.EnumerateFileBlocks cursor contract and the RemoteStore PUT-with-metadata-headers contract"
    - "CONFIGURATION.md lists every new gc.* and syncer.* knob with default + range + meaning"
    - "CLI.md documents dfsctl store block gc + gc-status"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "mark-sweep + three-state lifecycle + dual-read window sections"
    - path: "docs/CONFIGURATION.md"
      provides: "gc.* and syncer.* knob docs"
  key_links: []
---

<objective>
Land the documentation for Phase 11 changes (D-34). Runs in parallel with Wave 4 plans because docs are independent of build. Operator-visible: GC behavior, config knobs, CLI surface. Developer-visible: new conformance contract for MetadataStore backends.

Purpose: Without docs, an operator cannot tune the new knobs and a future contributor cannot implement a new MetadataStore backend that passes the EnumerateFileBlocks conformance suite. CHANGELOG is deferred to v0.15.0 ship per Phase 08 D-24.
Output: Updated docs files; CONTRIBUTING.md addition is at planner discretion (D-34) — included if the diff stays small.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-cas-write-path-gc-rewrite-a2/11-CONTEXT.md
@.planning/phases/11-cas-write-path-gc-rewrite-a2/11-PATTERNS.md
@CLAUDE.md
@docs/ARCHITECTURE.md
@docs/FAQ.md
@docs/IMPLEMENTING_STORES.md
@docs/CONFIGURATION.md
@docs/CLI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARCHITECTURE.md — replace path-prefix GC description; add three-state lifecycle + dual-read window + gc-state directory layout</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - docs/ARCHITECTURE.md (full file — find existing GC section + block-state section + S3 layout section)
    - .planning/phases/11-cas-write-path-gc-rewrite-a2/11-CONTEXT.md (D-34 list)
  </read_first>
  <action>
    1. Locate the existing GC description (likely under a "Garbage collection" heading) and replace it with a mark-sweep description covering:
       - Live set: union of every FileBlock's ContentHash across the metadata store (cross-share aggregation by remote identity, not share name)
       - On-disk live set under `<localStore>/gc-state/<runID>/` (Badger temp store + incomplete.flag)
       - Snapshot-time + grace TTL: sweep deletes only objects older than snapshot − grace (default 1h)
       - Sweep parallelism: bounded worker pool over 256 cas/XX/ prefixes
       - Fail-closed: any mark-phase error aborts the sweep; sweep-side per-prefix DELETE errors are captured and continue
       - Dry-run mode and observability via `gc-state/last-run.json`
    2. Locate the block-state section (likely under "Block lifecycle" or similar) and replace with a three-state diagram:
       ```
       Pending ──claim batch──▶ Syncing ──PUT success + meta txn──▶ Remote
                                  ▲                                       │
                                  └─────janitor (>claim_timeout)──────────┘
                                                                          │
                                                            (RefCount → 0)│
                                                                          ▼
                                                                    GC eligible
       ```
       Note: state lives ONLY on FileBlock.State indexed by ContentHash (STATE-03). No parallel state in memory or fd pools.
    3. Add a "Dual-read window (Phase 11 → Phase 14)" subsection explaining:
       - Engine routes per-block reads by FileBlock.Hash presence: non-zero → CAS path with BLAKE3 verification; zero → legacy `{payloadID}/block-{N}` path with no verification
       - The legacy code path is removed in Phase 15 after Phase 14 migration completes
    4. Add a "gc-state directory layout" subsection showing:
       ```
       <localStore>/gc-state/
         20260425T143022Z-abc/
           db/                          (Badger temp store)
           incomplete.flag              (removed by MarkComplete; cleaned by next run)
         20260425T153122Z-def/
           db/
           (no incomplete.flag — successful run)
         last-run.json                  (most recent GCRunSummary)
       ```
    5. Cross-reference: link to FAQ.md sections and CONFIGURATION.md knobs.
  </action>
  <verify>
    <automated>grep -n "mark-sweep\|three-state\|gc-state\|dual-read window" docs/ARCHITECTURE.md | wc -l</automated>
  </verify>
  <done>
    - `grep -ni "mark-sweep\|cas/{hash" docs/ARCHITECTURE.md` returns at least 2 lines
    - `grep -nE "Pending|Syncing|Remote" docs/ARCHITECTURE.md` returns at least 3 lines (state diagram or text)
    - `grep -n "dual-read window\|FileBlock.Hash" docs/ARCHITECTURE.md` returns at least 1 line
    - `grep -n "gc-state\|incomplete.flag\|last-run.json" docs/ARCHITECTURE.md` returns at least 2 lines
    - No remaining mention of path-prefix GC algorithm (`grep -i "path-prefix" docs/ARCHITECTURE.md` returns 0 lines unless explicitly referencing the legacy that was replaced)
  </done>
</task>

<task type="auto">
  <name>Task 2: FAQ.md, IMPLEMENTING_STORES.md, CONFIGURATION.md, CLI.md updates</name>
  <files>docs/FAQ.md, docs/IMPLEMENTING_STORES.md, docs/CONFIGURATION.md, docs/CLI.md</files>
  <read_first>
    - docs/FAQ.md (existing structure; section header style)
    - docs/IMPLEMENTING_STORES.md (existing MetadataStore + RemoteStore contract sections)
    - docs/CONFIGURATION.md (existing knob listing format — locate by reading)
    - docs/CLI.md (existing `dfsctl store block` reference style)
    - .planning/phases/11-cas-write-path-gc-rewrite-a2/11-CONTEXT.md (D-34 detailed list)
  </read_first>
  <action>
    1. **FAQ.md** — append three Q&A entries:
       - "How does garbage collection work in v0.15.0?" — short answer linking to ARCHITECTURE.md mark-sweep section + CLI command
       - "What is the dual-read window?" — explains why both `cas/.../` and `{payloadID}/block-{N}` keys may coexist after upgrade; lifespan Phase 11 → Phase 14
       - "Why are residual `{payloadID}/block-...` keys present after upgrading to v0.15.0?" — explains they are legacy data; will be migrated by `dfsctl blockstore migrate` in v0.15.0+ (Phase 14)
    2. **IMPLEMENTING_STORES.md** — add two contract sections:
       - "MetadataStore.EnumerateFileBlocks" — signature, semantics (streams every FileBlock's ContentHash, no full-set load, respects ctx.Done(), zero-hash blocks emitted, fn-error aborts), conformance suite reference (`pkg/metadata/storetest/`)
       - "RemoteStore.WriteBlockWithHash" — signature, semantics (set backend-native object metadata `content-hash` to `"blake3:" + hex(h)`; for S3 this becomes `x-amz-meta-content-hash`); conformance suite reference (`pkg/blockstore/remote/remotetest/`)
       - Also add "RemoteStore.ReadBlockVerified" — semantics (returns ErrCASContentMismatch on body or header mismatch; pre-checks header before body read; streaming verification)
    3. **CONFIGURATION.md** — add new knobs in the appropriate sections (inside an `engine:` or `blockstore:` block, depending on existing structure):
       ```yaml
       blockstore:
         syncer:
           tick: 30s                   # periodic sync interval; default 30s
           claim_batch_size: 32        # blocks claimed per syncer cycle; default 32
           upload_concurrency: 8       # parallel uploads per share; default 8, must be <= claim_batch_size
           claim_timeout: 10m          # janitor requeues Syncing rows older than this; default 10m
         gc:
           interval: 0                 # periodic GC interval; 0 = disabled (operator opt-in for v0.15.0)
           sweep_concurrency: 16       # parallel cas/XX/ prefix workers; default 16, max 32
           grace_period: 1h            # objects newer than (snapshot - grace) are not deleted; default 1h
           dry_run_sample_size: 1000   # max candidate keys reported in dry-run; default 1000
       ```
       Document each: meaning, default, range, when to tune.
    4. **CLI.md** — add subsection under `dfsctl store block`:
       ```
       ## dfsctl store block gc <share> [--dry-run]

       Run garbage collection for the named share. Mark phase enumerates every
       live ContentHash across all shares pointing at the same remote; sweep
       phase deletes any cas/.../ object absent from the live set whose
       LastModified is older than the configured grace period (default 1h).

       Flags:
         --dry-run    Skip deletes; print up to dry_run_sample_size candidate keys

       Output: GCStats with hashes_marked, objects_swept, bytes_freed, duration_ms,
       error_count.

       ## dfsctl store block gc-status <share>

       Print the most recent GCRunSummary for the named share, read from
       <localStore>/gc-state/last-run.json.
       ```
    5. **CONTRIBUTING.md (planner discretion per D-34):** Skip unless the file already has a recipe section for "Adding a metadata backend". If yes, add a one-paragraph "Adding EnumerateFileBlocks to a new metadata backend" recipe pointing at the conformance suite. If no, defer.
  </action>
  <verify>
    <automated>grep -n "EnumerateFileBlocks" docs/IMPLEMENTING_STORES.md && grep -n "gc.interval\|sweep_concurrency\|grace_period" docs/CONFIGURATION.md && grep -n "dfsctl store block gc" docs/CLI.md && grep -ni "dual-read window\|residual.*block-" docs/FAQ.md</automated>
  </verify>
  <done>
    - `grep -n "EnumerateFileBlocks\|WriteBlockWithHash\|ReadBlockVerified" docs/IMPLEMENTING_STORES.md | wc -l` returns at least 3
    - `grep -nE "gc\.(interval|sweep_concurrency|grace_period|dry_run_sample_size)|syncer\.(tick|claim_batch_size|upload_concurrency|claim_timeout)" docs/CONFIGURATION.md | wc -l` returns at least 8
    - `grep -n "dfsctl store block gc" docs/CLI.md` returns at least 2 lines (gc + gc-status)
    - `grep -ni "dual-read\|residual.*block-\|GC.*v0.15" docs/FAQ.md` returns at least 3 lines
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator | Docs are the operator's source of truth for tuning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-D-01 | Information Disclosure | Documenting internal state machine reveals attack surface | accept | The state machine is plain CAS — there is no secret to reveal. Operator visibility is a feature. |
| T-11-D-02 | Tampering | Out-of-date docs mislead operator into wrong tuning | mitigate | Docs land in the same PR as the code change (PR-C wave); review process catches drift. |
</threat_model>

<verification>
- All grep checks in done criteria pass
- No code changes; pure docs
</verification>

<success_criteria>
- All five docs files updated per D-34
- Operator can configure new knobs from CONFIGURATION.md alone
- New backend implementer can satisfy EnumerateFileBlocks contract from IMPLEMENTING_STORES.md alone
- CLI reference is complete for both new subcommands
</success_criteria>

<output>
After completion, create `.planning/phases/11-cas-write-path-gc-rewrite-a2/11-09-SUMMARY.md` capturing: whether CONTRIBUTING.md was updated (yes/no with one-line reason), and any cross-references between the five docs files added.
</output>
