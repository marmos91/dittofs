---
phase: 11-cas-write-path-gc-rewrite-a2
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - pkg/blockstore/engine/perf_bench_test.go
  - test/e2e/BENCHMARKS.md
autonomous: false
requirements: [INV-06]
pr: PR-B
must_haves:
  truths:
    - "Microbench measures cold-cache rand-read IOPS with verifier ENABLED vs DISABLED"
    - "Verifier-on regression vs verifier-off baseline is ≤5% IOPS (D-20)"
    - "Microbench measures rand-write IOPS with the new CAS upload path"
    - "CAS rand-write regression vs Phase 10 baseline is within the global ≤6% budget per STATE.md"
    - "Bench results recorded in test/e2e/BENCHMARKS.md with run date + git SHA"
  artifacts:
    - path: "pkg/blockstore/engine/perf_bench_test.go"
      provides: "BenchmarkRandReadVerified + BenchmarkRandReadUnverified + BenchmarkRandWriteCAS"
      contains: "BenchmarkRandRead"
    - path: "test/e2e/BENCHMARKS.md"
      provides: "Phase 11 perf gate row"
  key_links:
    - from: "BenchmarkRandReadVerified"
      to: "BenchmarkRandReadUnverified"
      via: "ratio comparison ≤1.05"
      pattern: "BenchmarkRandRead"
---

<objective>
Land the Phase 11 performance gate (D-20) and confirm the CAS write path does not regress rand-write beyond the global budget. Block PR-B from merging if either gate fails — these gates are the safety rail behind INV-06 (the streaming verifier could otherwise tank read IOPS) and the BSCAS-01 rewrite (the CAS PUT path could otherwise tank write IOPS).

Purpose: Catches regressions before they ship. Without this gate, a subtle BLAKE3 throughput cliff or an extra metadata round-trip in the syncer could silently violate STATE.md's ≤6% budget.
Output: New microbench file with three benchmark functions, BENCHMARKS.md update, human checkpoint to verify the gates pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-cas-write-path-gc-rewrite-a2/11-CONTEXT.md
@.planning/phases/11-cas-write-path-gc-rewrite-a2/11-PATTERNS.md
@CLAUDE.md
@test/e2e/BENCHMARKS.md
@pkg/blockstore/hash_bench_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add microbenches for rand-read verified/unverified and rand-write CAS</name>
  <files>pkg/blockstore/engine/perf_bench_test.go</files>
  <read_first>
    - pkg/blockstore/hash_bench_test.go (Phase 10 — pattern for benchmark setup against in-process fixtures)
    - pkg/blockstore/engine/syncer.go (post-plan-02 — Syncer surface to drive)
    - pkg/blockstore/remote/s3/store.go (post-plan-03 — ReadBlockVerified to call)
    - pkg/blockstore/remote/memory/store.go (in-memory remote — use for repeatable bench, no Localstack dep)
    - .planning/phases/11-cas-write-path-gc-rewrite-a2/11-CONTEXT.md (D-20 hard 5% gate; D-32 perf gates)
  </read_first>
  <action>
    1. Create `pkg/blockstore/engine/perf_bench_test.go` with three benchmarks:
       - `BenchmarkRandReadVerified`: in-memory RemoteStore prepopulated with 1024 known CAS objects (4 MiB each, BLAKE3 hash recorded). Loop: pick a random CAS key, call `ReadBlockVerified(ctx, key, hash)`, accumulate bytes. Report `b.SetBytes(4 * 1024 * 1024)` and `b.ReportAllocs()`.
       - `BenchmarkRandReadUnverified`: same fixture, call legacy `ReadBlock(ctx, key)` (no verification). Same SetBytes / ReportAllocs.
       - `BenchmarkRandWriteCAS`: in-memory FileBlockStore + RemoteStore. Loop: stage a 4 MiB random buffer, drive `Syncer.uploadOne` (or via SyncNow on a 1-block claim batch). SetBytes(4 MiB), ReportAllocs.
    2. Each benchmark must produce a stable IOPS metric: divide ns/op into ops/sec and emit via `b.ReportMetric(opsPerSec, "ops/s")`. The verifier comparison is `(unverified ops/s - verified ops/s) / unverified ops/s ≤ 0.05`.
    3. Use `b.ResetTimer()` after fixture setup so the prepopulation cost does not skew results.
    4. Use a deterministic random seed (e.g., `rand.New(rand.NewSource(42))`) so the benchmark is reproducible across runs.
    5. Document the gate at the top of the file as a Go comment block:
       ```go
       // Phase 11 performance gates (D-20):
       //   verifier_regression = 1 - (BenchmarkRandReadVerified / BenchmarkRandReadUnverified)
       //   MUST be <= 0.05 (5%).
       //
       // Phase 11 write-path gate (STATE.md ≤6% budget):
       //   BenchmarkRandWriteCAS MUST be within 6% of the Phase 10 rand-write baseline
       //   recorded in test/e2e/BENCHMARKS.md.
       ```
    6. Add a tiny test `TestPerfGate_VerifierWithinBudget` (in the same file or a sibling _test.go) that runs both rand-read benches with `testing.Benchmark(...)` (the programmatic API) and asserts the 5% gate inline. This makes the gate fail-closed under `go test ./pkg/blockstore/engine/...` even if no human runs the benchmark explicitly. Skip the test if `-short` is set (so unit-test runs are fast); only enforce on full runs.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-409 && go test ./pkg/blockstore/engine/ -run 'TestPerfGate_VerifierWithinBudget' -count=1 -v && go test -bench='BenchmarkRandRead|BenchmarkRandWriteCAS' -benchtime=2s ./pkg/blockstore/engine/ -run='^$' -benchmem</automated>
  </verify>
  <done>
    - `ls pkg/blockstore/engine/perf_bench_test.go` returns the file
    - `grep -nE "BenchmarkRandReadVerified|BenchmarkRandReadUnverified|BenchmarkRandWriteCAS|TestPerfGate_VerifierWithinBudget" pkg/blockstore/engine/perf_bench_test.go | wc -l` returns at least 4
    - `go test -run TestPerfGate_VerifierWithinBudget` exits 0 (the inline 5% gate enforces)
    - `go test -bench=BenchmarkRandRead -benchtime=2s` produces output showing both verified and unverified ops/s
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-run perf gates and update BENCHMARKS.md</name>
  <files>test/e2e/BENCHMARKS.md</files>
  <action>Run the benchmarks under the documented invocation, compute regressions, append a Phase 11 row to BENCHMARKS.md. See <how-to-verify> for the exact steps.</action>
  <verify><automated>grep -n "Phase 11" test/e2e/BENCHMARKS.md</automated></verify>
  <done>BENCHMARKS.md contains a Phase 11 row with date + SHA + three ops/s values + regression percentages</done>
  <what-built>
    Phase 11 performance gates implemented:
    - Inline 5% gate test (TestPerfGate_VerifierWithinBudget) for INV-06 verifier overhead
    - BenchmarkRandWriteCAS for the rewritten CAS upload path
  </what-built>
  <how-to-verify>
    1. Run the gates:
       ```bash
       cd /Users/marmos91/Projects/dittofs-409
       go test -run TestPerfGate_VerifierWithinBudget ./pkg/blockstore/engine/ -count=1 -v
       go test -bench='BenchmarkRandReadVerified|BenchmarkRandReadUnverified|BenchmarkRandWriteCAS' -benchtime=10s ./pkg/blockstore/engine/ -run='^$' -benchmem | tee /tmp/phase11-bench.txt
       ```
    2. Inspect /tmp/phase11-bench.txt:
       - Compute `(unverified ops/s - verified ops/s) / unverified ops/s` — MUST be ≤ 0.05.
       - Compare BenchmarkRandWriteCAS ops/s against the Phase 10 rand-write baseline (from test/e2e/BENCHMARKS.md after Phase 10 shipped). Regression MUST be ≤ 6%.
    3. Open `test/e2e/BENCHMARKS.md` and append a Phase 11 results row with: date, git SHA, hardware (uname -a), the three ops/s values, and the computed regression percentages.
    4. If either gate fails:
       - Profile (`go test -bench=BenchmarkRandReadVerified -cpuprofile=cpu.prof`).
       - Most likely culprits: BLAKE3 throughput on this CPU (try the `-tags=purego` vs default), excess allocations in verifyingReader, or extra metadata round-trip in the syncer.
       - Iterate on plan 03 (verifier) or plan 02 (syncer) until the gates pass. DO NOT MERGE PR-B until both gates are green.
    5. Confirm BENCHMARKS.md is committed.
  </how-to-verify>
  <resume-signal>Type "approved" if both gates pass and BENCHMARKS.md is updated, or describe which gate failed and by how much.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bench fixture → measurement | An over-eager fixture (e.g., warm cache) could mask a real regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-B-12 | Tampering | Benchmark fixture warms verifier cache, hides cold-cache cost | mitigate | Each iteration picks a uniformly-random CAS key from a population large enough to defeat any CPU-side cache (1024 × 4 MiB = 4 GiB, comfortably exceeds typical L3). Benchmark is cold-cache by construction. |
| T-11-B-13 | Repudiation | Hand-edited BENCHMARKS.md figures | accept | The numbers are recorded with git SHA + date so future runs can reproduce. Regression hunting can re-run any historical SHA. |
</threat_model>

<verification>
- `go test -run TestPerfGate_VerifierWithinBudget ./pkg/blockstore/engine/ -count=1` exits 0
- BENCHMARKS.md contains a Phase 11 row with three ops/s figures
- Both gates passed (verifier ≤5%, write ≤6%) per the human checkpoint
</verification>

<success_criteria>
- Phase 11 PR-B does not merge with verifier overhead > 5% on rand-read
- Phase 11 PR-A does not regress rand-write beyond the ≤6% budget vs Phase 10
- Future regressions are caught automatically by the inline gate test
</success_criteria>

<output>
After completion, create `.planning/phases/11-cas-write-path-gc-rewrite-a2/11-05-SUMMARY.md` capturing: the three measured ops/s values, computed regression percentages, hardware/git SHA, and any tuning that was needed (e.g., BLAKE3 build tag changes) to meet the gates.
</output>
