---
phase: 24-restore-flow
plan: 04
type: execute
wave: 2
depends_on: [24-03]
files_modified:
  - pkg/controlplane/runtime/snapshot_restore_test.go
autonomous: true
requirements: [REST-01, REST-02, REST-03]
must_haves:
  truths:
    - "Happy path: write files -> CreateSnapshot -> mutate (delete file) -> RestoreSnapshot -> deleted file is recovered AND share remains disabled"
    - "Enabled-share refusal: RestoreSnapshot on enabled share returns errors.Is(ErrShareEnabled) without touching metadata"
    - "Snapshot-not-found path: RestoreSnapshot on missing snapID returns errors.Is(ErrSnapshotNotFound) without touching metadata"
    - "Snapshot-not-ready path: RestoreSnapshot on snapshot in state=creating/failed refuses without touching metadata"
    - "Non-durable refused by default: RemoteDurable=false + AllowNonDurable=false returns errors.Is(ErrSnapshotNotDurable)"
    - "AllowNonDurable bypass works: RemoteDurable=false + AllowNonDurable=true proceeds past precheck"
    - "Pre-verify fails fast: missing manifest hash on remote returns errors.Is(ErrRestoreVerifyFailed) AND Reset was never called (metadata unchanged)"
    - "Post-verify fails: block deleted after Reset returns errors.Is(ErrRestoreVerifyFailed) AND safety snapshot dump exists on disk"
    - "Interrupted Reset: Reset error returns errors.Is(ErrRestoreAborted), share stays disabled, safety snapshot exists on disk, and a second RestoreSnapshot(safetyID) recovers original state"
  artifacts:
    - path: pkg/controlplane/runtime/snapshot_restore_test.go
      provides: "E2E integration test for Runtime.RestoreSnapshot covering 9 scenarios"
      contains: "func TestRestoreSnapshot_Integration"
  key_links:
    - from: "snapshot_restore_test.go"
      to: "Runtime.RestoreSnapshot"
      via: "fixture invocation"
      pattern: "RestoreSnapshot"
    - from: "snapshot_restore_test.go"
      to: "models.ErrXxx sentinels"
      via: "errors.Is assertions"
      pattern: "errors.Is.*Err"
---

<objective>
Land the E2E integration test `pkg/controlplane/runtime/snapshot_restore_test.go` covering the 9 scenarios listed in CONTEXT D-24-10 P24-04 — happy path + all failure modes from D-24-13 — using the memory metadata + memory `RemoteStore` + memory `LocalStore` fixture pattern established by Phase 22 D-21 / Phase 23 P23-06.

Purpose: This is the executable specification for REST-01/02/03. Phase 23's `snapshot_integration_test.go` is the verbatim analog (same fixture, same backend stack, same memory-only scoping rationale). Per-backend correctness lives in P24-01's `ResetThenRestoreConformance` against all three backends; this plan covers ORCHESTRATION correctness on memory.

Output: One new test file, no production code touched. Test must compile + pass within `go test ./pkg/controlplane/runtime/... -count=1` against the Wave 1 + P24-03 outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-restore-flow/24-CONTEXT.md
@.planning/phases/24-restore-flow/24-PATTERNS.md

# Phase 23 analog (the test file this plan mirrors structurally)
@pkg/controlplane/runtime/snapshot_integration_test.go

# Production targets
@pkg/controlplane/runtime/snapshot.go
@pkg/controlplane/runtime/restore_opts.go
@pkg/controlplane/models/errors.go

# Memory backends used by fixture
@pkg/metadata/store/memory/store.go
@pkg/blockstore/remote/memory/store.go

<interfaces>
<!-- Test dispatcher + scenario set per D-24-10 P24-04 -->

```go
func TestRestoreSnapshot_Integration(t *testing.T) {
    t.Run("HappyPath", testRestoreHappyPath)
    t.Run("EnabledShareRefuses", testRestoreEnabledShareRefuses)
    t.Run("SnapshotNotFound", testRestoreSnapshotNotFound)
    t.Run("SnapshotNotReady", testRestoreSnapshotNotReady)
    t.Run("NonDurableRefused", testRestoreNonDurableRefused)
    t.Run("AllowNonDurable", testRestoreAllowNonDurable)
    t.Run("PreVerifyFailsFast", testRestorePreVerifyFailsFast)
    t.Run("PostVerifyFails", testRestorePostVerifyFails)
    t.Run("InterruptedRestore", testRestoreInterruptedReset)
}
```

Fixture composition (Phase 23 lines 434-499 verbatim):
- `cpstore.NewSQLiteStore(":memory:")` for control plane
- `metadatamemory.NewMemoryMetadataStoreWithDefaults()` for metadata
- `bsmemory.NewMemoryStore()` for local block store
- `remotememory.NewMemoryRemote()` for remote — wrapped with `interceptingRemote` (existing Phase 23 helper) so failure-mode tests can simulate Head() misses
- `Runtime` constructed with `r.InjectShareForTesting(...)` + `r.SetLocalStoreDirForTesting(...)`

Critical assertion patterns (PATTERNS §"Failure-mode assertion pattern"):
- `errors.Is(err, models.ErrXxx)` for sentinel matching
- `mustFileNonEmpty(t, snap.MetadataDumpPath(fx.localStoreDir), "...")` for on-disk safety-snap retention
- After-test: `fx.rt.IsShareEnabled(...)` returns false (share-stays-disabled invariant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture composition + happy-path + precheck-failure scenarios</name>
  <files>pkg/controlplane/runtime/snapshot_restore_test.go</files>
  <read_first>
    - pkg/controlplane/runtime/snapshot_integration_test.go (entire file — fixture, helpers, sub-test structure all to be reused)
    - .planning/phases/24-restore-flow/24-PATTERNS.md §"pkg/controlplane/runtime/snapshot_restore_test.go" (entire section — imports, dispatcher pattern, fixture composition)
    - .planning/phases/24-restore-flow/24-CONTEXT.md D-24-10 (P24-04 scenario list), D-24-01 (share-stays-disabled invariant)
    - pkg/controlplane/runtime/snapshot.go (the implementation under test — confirm RestoreSnapshot signature post-P24-03)
  </read_first>
  <action>
    Create `pkg/controlplane/runtime/snapshot_restore_test.go` with `package runtime` (not `runtime_test` — Phase 23 uses internal package so it can access unexported helpers like `InjectShareForTesting`; mirror that). Imports per PATTERNS §"Imports pattern" (drop io / health / sync if not used).

    Declare the dispatcher `TestRestoreSnapshot_Integration` per `<interfaces>` above with `t.Run(...)` for each scenario.

    Reuse Phase 23's `newOrchestrationFixture(t)` if accessible (same package); if helper is unexported and lives in `snapshot_integration_test.go`, factor it out into a sibling helper file or just access it directly (same _test.go package). Add a fixture method `fx.disableShare()` if not already present — it should set `share.Enabled=false` directly via the test-only injection path (NOT via real DisableShare which may have side effects). The standard pattern: load the share via `fx.rt.sharesSvc.GetShareForTesting(name)`, set `Enabled=false`, save back. Mirror however Phase 23 toggles share state in `testRemoveShareCancelsInFlight` (PATTERNS reference lines 434-499+).

    Implement these scenarios in this task:

    **testRestoreHappyPath**:
    1. Build fixture; seed remote with 3 hashes; populate 2 files via the metadata store fixture helper.
    2. Disable share (`fx.disableShare()`).
    3. `snapID, err := fx.rt.CreateSnapshot(ctx, fx.shareName, CreateSnapshotOpts{})`; `_, err := fx.rt.WaitForSnapshot(ctx, fx.shareName, snapID)`; assert `snap.State == StateReady` and `snap.RemoteDurable == true`.
    4. Mutate metadata: delete one of the 2 files via the metadata store directly (simulating "user deleted a file we want to recover").
    5. `err := fx.rt.RestoreSnapshot(ctx, fx.shareName, snapID, RestoreSnapshotOpts{})`; assert err == nil.
    6. Assert: the deleted file is now present in the metadata store (read via fx.rt.GetMetadataStoreForShare + GetFile).
    7. Assert: `enabled, _ := fx.rt.IsShareEnabled(fx.shareName); assert.False(enabled)` — share STAYS disabled per D-24-01.
    8. Assert: a safety snapshot exists in `cpstore.ListSnapshots(ctx, fx.shareName)` distinct from the source snapID.

    **testRestoreEnabledShareRefuses**:
    1. Build fixture; create + complete one snapshot (share enabled by default in Phase 23 fixture).
    2. Call `RestoreSnapshot(ctx, fx.shareName, snapID, RestoreSnapshotOpts{})` WITHOUT disabling first.
    3. Assert `errors.Is(err, models.ErrShareEnabled)`.
    4. Assert metadata store is UNCHANGED (count files before and after — equal).

    **testRestoreSnapshotNotFound**:
    1. Build fixture; disable share; do NOT create any snapshot.
    2. Call `RestoreSnapshot(ctx, fx.shareName, "nonexistent-snap-id", RestoreSnapshotOpts{})`.
    3. Assert `errors.Is(err, models.ErrSnapshotNotFound)` (Phase 22 sentinel).
    4. Assert metadata store unchanged.

    **testRestoreSnapshotNotReady**:
    Two sub-cases (use t.Run inside if helpful):
    (a) State=failed: create a snapshot, force it to fail (via the `interceptingRemote` Head-fail trick from Phase 23 testDrainThenVerifyFails), wait for state=failed, then call RestoreSnapshot. Assert errors.Is(err, models.ErrSnapshotStateConflict) (or whatever sentinel P24-03 chose for the state-mismatch — see P24-03 SUMMARY).
    (b) State=creating: blocked by absence of an in-flight observation hook; if this is too synthetic for the memory fixture, skip with a t.Skip("state=creating is observed via in-flight registry; covered by Phase 23 unit tests") — but still cover state=failed unambiguously.

    **testRestoreNonDurableRefused**:
    1. Build fixture; disable share.
    2. `snapID, _ := fx.rt.CreateSnapshot(ctx, fx.shareName, CreateSnapshotOpts{NoSyncGate: true})`; wait for ready; assert `snap.RemoteDurable == false`.
    3. Call `RestoreSnapshot(ctx, fx.shareName, snapID, RestoreSnapshotOpts{})` (AllowNonDurable=false).
    4. Assert `errors.Is(err, models.ErrSnapshotNotDurable)`.
    5. Assert metadata unchanged.

    **testRestoreAllowNonDurable**:
    1. Same setup as above through step 2.
    2. BEFORE calling RestoreSnapshot, ensure remote DOES have the manifest hashes (seed remote — the NoSyncGate snapshot won't have synced them; manually `fx.seedRemoteAll(hashes)`) — otherwise pre-verify will fail.
    3. Call `RestoreSnapshot(ctx, fx.shareName, snapID, RestoreSnapshotOpts{AllowNonDurable: true})`.
    4. Assert err == nil; share stays disabled; data restored.

    Each test must clean up via `defer fx.close()` per Phase 23 convention.
  </action>
  <verify>
    <automated>go test ./pkg/controlplane/runtime/ -run "TestRestoreSnapshot_Integration/HappyPath|TestRestoreSnapshot_Integration/EnabledShareRefuses|TestRestoreSnapshot_Integration/SnapshotNotFound|TestRestoreSnapshot_Integration/SnapshotNotReady|TestRestoreSnapshot_Integration/NonDurableRefused|TestRestoreSnapshot_Integration/AllowNonDurable" -count=1 -v && grep -q "func TestRestoreSnapshot_Integration" pkg/controlplane/runtime/snapshot_restore_test.go && grep -c "errors.Is(err, models.Err" pkg/controlplane/runtime/snapshot_restore_test.go | awk '{exit ($1 < 5)}'</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/controlplane/runtime/snapshot_restore_test.go` exists with `package runtime` (internal)
    - Dispatcher `TestRestoreSnapshot_Integration` declares all 9 sub-tests (even if some are not yet implemented — `t.Skip("implemented in Task 2")` is acceptable for the 3 Task 2 scenarios)
    - 6 scenarios implemented in this task PASS: HappyPath, EnabledShareRefuses, SnapshotNotFound, SnapshotNotReady, NonDurableRefused, AllowNonDurable
    - Each assertion uses `errors.Is` against the documented sentinel
    - Share-stays-disabled invariant asserted in HappyPath
    - Metadata-unchanged invariant asserted in all pre-Reset failure scenarios
  </acceptance_criteria>
  <done>Six happy-and-precheck-failure scenarios run green; fixture composition + helpers ready for Task 2's destructive-path scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Destructive-path scenarios — pre-verify-fails-fast, post-verify-fails, interrupted-Reset</name>
  <files>pkg/controlplane/runtime/snapshot_restore_test.go</files>
  <read_first>
    - pkg/controlplane/runtime/snapshot_restore_test.go (Task 1 output — confirm fixture + scenario structure)
    - pkg/controlplane/runtime/snapshot_integration_test.go (Phase 23 — find `interceptingRemote` helper and `controlledBackupable` wrapper; they're the mechanisms used to inject failures into Head() / Backup() respectively)
    - pkg/metadata/resetable.go (P24-01 output)
    - .planning/phases/24-restore-flow/24-CONTEXT.md D-24-13 (failure-mode taxonomy — every assertion in this task ties to a specific failure mode)
    - .planning/phases/24-restore-flow/24-PATTERNS.md §"Failure-mode assertion pattern"
  </read_first>
  <action>
    Implement the remaining 3 scenarios. Replace the `t.Skip` stubs from Task 1.

    **testRestorePreVerifyFailsFast**:
    1. Build fixture; disable share; create snapshot via `fx.rt.CreateSnapshot(ctx, fx.shareName, CreateSnapshotOpts{})` (sync-gated, RemoteDurable=true).
    2. Wait for ready, capture snap state.
    3. AFTER snapshot is created, simulate "operator deleted a remote object" by reaching into `fx.remoteIntercept` and configuring Head() to return ErrBlockNotFound for one of the manifest hashes.
    4. Snapshot one of the populated files via the metadata fixture for a "marker" we can check is untouched.
    5. Record the pre-restore file count.
    6. Call `RestoreSnapshot(ctx, fx.shareName, snapID, RestoreSnapshotOpts{})`.
    7. Assert `errors.Is(err, models.ErrRestoreVerifyFailed)`.
    8. Assert metadata store file count is unchanged (Reset was NEVER called).
    9. Assert: no safety snapshot was created (CreateSnapshot in step 3 of orchestration runs AFTER pre-verify per D-24-09; pre-verify-fail means safety-snap was never reached). Verify via `cpstore.ListSnapshots(ctx, fx.shareName)` returning exactly 1 (the source snapshot, no safety-snap).

    **testRestorePostVerifyFails**:
    1. Build fixture; disable share; create + complete snapshot with sync-gated remote (RemoteDurable=true).
    2. Mutate metadata (delete a file) so Restore has meaningful work to do.
    3. AFTER snapshot create, configure `fx.remoteIntercept` to allow ALL Head() calls during pre-verify, but FAIL one specific hash AFTER the dump-restore happens. Mechanism: use a counter / phase flag on interceptingRemote so the first wave of Head() calls (pre-verify, step 2) succeeds and the second wave (post-verify, step 7) fails for one hash. If interceptingRemote doesn't already support this, extend it with a `failHashAfterCount(hash, count)` knob — read Phase 23's interceptingRemote source to find the cleanest extension point.
    4. Call `RestoreSnapshot(...)`.
    5. Assert `errors.Is(err, models.ErrRestoreVerifyFailed)`.
    6. Assert: metadata IS restored (step 6 ran before step 7 failed) — check that the deleted file is back. This proves we reached post-verify.
    7. Assert: safety snapshot exists on disk — `mustFileNonEmpty(t, safetySnap.MetadataDumpPath(fx.localStoreDir), "safety-snap dump retained")`. Locate safetySnap by listing snapshots and picking the one that's not snapID.
    8. Assert: share stays disabled.

    **testRestoreInterruptedReset**:
    Mechanism: wrap the metadata store with a `failableResetable` that succeeds on the first Reset call (the safety-snap path may also call Reset internally — verify by audit; if so, count calls) but fails on a specific invocation. Simplest path: inject a flag `failNextReset bool` into the wrapper; orchestration calls Reset exactly once (step 5) per the function body.

    1. Build fixture with the metadata store wrapped in `failableResetable` — the wrapper embeds the real memory store, forwards every MetadataStore method, and additionally implements Resetable + Backupable.
    2. Disable share; populate; snapshot; mutate (delete a file).
    3. Set `failableResetable.failNextReset = true`.
    4. Call `RestoreSnapshot(...)`.
    5. Assert `errors.Is(err, models.ErrRestoreAborted)`.
    6. Assert: safety snapshot exists on disk + in DB.
    7. Assert: share stays disabled.
    8. RECOVERY: clear the failure flag (`failNextReset = false`); identify safetyID via ListSnapshots; call `RestoreSnapshot(ctx, fx.shareName, safetyID, RestoreSnapshotOpts{})` to recover.
    9. Assert recovery err == nil; metadata is back to its mutated-but-recoverable state (whatever was on disk when the safety snap was taken — should include the deleted file since the safety snap was created BEFORE step 5's Reset).
    10. Assert share STILL disabled after recovery (D-24-01 invariant).

    Note on the safety-snap mid-Reset scenario: per D-24-13 "process crash mid-Reset" = safety snapshot is the recovery primitive; sync orchestration means no in-flight-restore DB row exists, so "startup recovery" tests are out of scope here. testRestoreInterruptedReset above covers the in-process equivalent (Reset fails with error rather than process dies — semantically equivalent for the safety-snap-recovery contract).
  </action>
  <verify>
    <automated>go test ./pkg/controlplane/runtime/ -run "TestRestoreSnapshot_Integration" -count=1 -v && go vet ./pkg/controlplane/runtime/... && grep -c "models.ErrRestoreVerifyFailed" pkg/controlplane/runtime/snapshot_restore_test.go | awk '{exit ($1 < 2)}' && grep -q "models.ErrRestoreAborted" pkg/controlplane/runtime/snapshot_restore_test.go && grep -q "failableResetable\|failNextReset" pkg/controlplane/runtime/snapshot_restore_test.go && grep -q "safetyID" pkg/controlplane/runtime/snapshot_restore_test.go</automated>
  </verify>
  <acceptance_criteria>
    - All 9 sub-tests in `TestRestoreSnapshot_Integration` pass (no t.Skip remaining for the 3 Task 2 scenarios)
    - PreVerifyFailsFast asserts ZERO safety snapshots were created (proves pre-verify ran before step 3)
    - PostVerifyFails asserts safety snapshot dump file exists on disk + metadata WAS restored (proves we reached step 7)
    - InterruptedRestore demonstrates the full recovery path: Reset fail -> errors.Is(ErrRestoreAborted) -> second RestoreSnapshot(safetyID) succeeds
    - All scenarios assert share-stays-disabled per D-24-01
    - `go test ./pkg/controlplane/runtime/... -count=1` exits 0 (all Phase 23 tests also still pass — no regression)
  </acceptance_criteria>
  <done>Failure-mode taxonomy from D-24-13 is fully covered by executable tests; recovery primitive (safety snapshot as second-RestoreSnapshot input) is demonstrated; integration coverage matches D-24-10 P24-04 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> Runtime under test | Fixture uses internal-package access (`package runtime`) to inject shares + disable shares for testing. Boundary is the test-only helpers; not exposed in production binary. |
| interceptingRemote -> production code | Test-only Head() injection lets us simulate remote-object deletion. Boundary is `_test.go`-scoped; never wired into Runtime construction in production builds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | Test-only helpers (InjectShareForTesting, SetLocalStoreDirForTesting) leak into production | mitigate | These helpers are documented as test-only via `ForTesting` suffix convention from Phase 23; not exposed in any non-test path. CI gate via grep for `ForTesting` callsites in non-test files. |
| T-24-04-02 | Information Disclosure | Integration test logs verbose error details | accept | Test logs are operator-developer-only; not surfaced to end users. |
| T-24-04-03 | Tampering | failableResetable wrapper accidentally reused in non-test code | mitigate | Lives in `_test.go` file with internal `package runtime`; cannot be imported by production code. |
| T-24-04-04 | Repudiation | Test flakiness masking real failures | mitigate | All assertions use `errors.Is` against specific sentinels (not message-string matching); deterministic interceptingRemote behavior (counter-based, not time-based); `t.Context()` for ctx scoping. |
| T-24-04-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies. |
</threat_model>

<verification>
- `pkg/controlplane/runtime/snapshot_restore_test.go` exists with 9 sub-tests
- All 9 sub-tests pass under `go test ./pkg/controlplane/runtime/... -count=1`
- Phase 23's TestCreateSnapshot_Integration still passes (no shared-fixture regression)
- Test asserts share-stays-disabled invariant after every scenario (success + failure)
- Test asserts metadata-unchanged invariant for pre-Reset failure modes
- Test demonstrates recovery via `RestoreSnapshot(safetyID)` for the InterruptedRestore case
- `go vet ./pkg/controlplane/runtime/...` exits 0
</verification>

<success_criteria>
- REST-01/02/03 each have at least one passing sub-test as direct evidence
- D-24-13 failure-mode taxonomy is exhaustively covered (pre-Reset failures, Reset failure, post-verify failure, recovery via safety snapshot)
- D-24-01 share-stays-disabled invariant verified in every sub-test
- Phase 23 P23-06 memory-only-integration-test precedent followed verbatim (no Postgres/Badger coverage at orchestration layer — those covered by P24-01 conformance suite per backend)
</success_criteria>

<output>
Create `.planning/phases/24-restore-flow/24-04-SUMMARY.md` when done. Include: the final 9 sub-test list, the mechanism used to inject pre/post-verify failures (counter-based knob on interceptingRemote or other), the `failableResetable` wrapper shape, and confirmation that all REST-XX requirements have at least one passing sub-test.
</output>
