---
phase: 24-restore-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/controlplane/models/errors.go
  - pkg/controlplane/models/errors_test.go
  - pkg/controlplane/runtime/restore_opts.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 25 REST handler can map each Phase 24 failure mode to an HTTP status via errors.Is on a single sentinel"
    - "RestoreSnapshotOpts struct exists with AllowNonDurable field; zero-value requests default behavior (refuse non-durable per D-24-06)"
    - "Each new sentinel survives wrap/unwrap via errors.Is across at least one fmt.Errorf wrapping layer"
  artifacts:
    - path: pkg/controlplane/models/errors.go
      provides: "7 typed error sentinels for Phase 24 restore orchestration per D-24-08"
      contains: "ErrShareEnabled"
    - path: pkg/controlplane/runtime/restore_opts.go
      provides: "RestoreSnapshotOpts struct with AllowNonDurable bool per D-24-06"
      contains: "type RestoreSnapshotOpts struct"
  key_links:
    - from: "pkg/controlplane/models/errors.go"
      to: "errors.Is detection sites"
      via: "package-level var sentinels"
      pattern: "errors.New"
---

<objective>
Land the type vocabulary that P24-03 (orchestration) and P24-04 (integration tests) consume:
  1. Seven typed error sentinels in `pkg/controlplane/models/errors.go` per CONTEXT D-24-08
  2. `RestoreSnapshotOpts` struct definition with `AllowNonDurable bool` per D-24-06

Purpose: Decouple the type declarations from the orchestration body so P24-03 has zero churn on these surfaces and Phase 25 can `errors.Is` against stable identifiers from the moment they're declared. Wave 1 sibling to P24-01 — no inter-dependencies; both ship before Wave 2.

Output: Six sentinel additions to existing `errors.go`, one new file `restore_opts.go` next to `snapshot.go` in `pkg/controlplane/runtime/`, and a small `errors_test.go` that round-trips each new sentinel through `errors.Is` after a `fmt.Errorf("%w")` wrap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-restore-flow/24-CONTEXT.md
@.planning/phases/24-restore-flow/24-PATTERNS.md

# Analog (Phase 22/23 sentinels in the same file)
@pkg/controlplane/models/errors.go

<interfaces>
<!-- Exact sentinel set per D-24-08 -->

Phase 23 precedent (from PATTERNS §"Exact pattern to follow"):
```go
// Phase 23 (D-23-12): orchestration sentinels surfaced to REST in Phase 25.
ErrSnapshotBackupFailed         = errors.New("snapshot backup failed")
ErrSnapshotVerifyFailed         = errors.New("snapshot verify failed: missing hashes on remote after drain")
ErrSnapshotDrainTimeout         = errors.New("snapshot drain timed out")
ErrSnapshotRetryTargetNotFound  = errors.New("snapshot retry target not found")
ErrSnapshotRetryTargetNotFailed = errors.New("snapshot retry target is not in failed state")
```

Phase 24 additions (D-24-08 — copy verbatim):
```go
// Phase 24 (D-24-08): restore orchestration sentinels surfaced to REST in Phase 25.
ErrShareEnabled                = errors.New("share must be disabled before restore")
ErrSnapshotNotDurable          = errors.New("snapshot is not remote-durable; pass AllowNonDurable to override")
ErrSnapshotMetadataDumpMissing = errors.New("snapshot metadata dump file is missing")
ErrMetadataStoreNotResetable   = errors.New("metadata engine does not implement Resetable")
ErrRestoreSafetySnapFailed     = errors.New("restore safety snapshot creation or wait failed")
ErrRestoreAborted              = errors.New("restore aborted; safety snapshot retained for rollback")
ErrRestoreVerifyFailed         = errors.New("restore verify failed: missing hashes on remote")
```

RestoreSnapshotOpts (D-24-06; SkipPreVerify omitted per CONTEXT planner-discretion note — can be added later non-breakingly):
```go
type RestoreSnapshotOpts struct {
    AllowNonDurable bool
}
```
Live in `pkg/controlplane/runtime/restore_opts.go` (sibling of `snapshot.go`). Phase 25 will surface AllowNonDurable as `--force-non-durable` CLI flag and a request-body field.

Future-compat: if a planner wants to add `SkipPreVerify bool` later, doing so is non-breaking (zero-value preserves default behavior). Decision deferred per CONTEXT.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 sentinels to errors.go + errors_test.go round-trip coverage</name>
  <files>pkg/controlplane/models/errors.go, pkg/controlplane/models/errors_test.go</files>
  <read_first>
    - pkg/controlplane/models/errors.go (entire file — see exact placement of Phase 22/23 sentinel block; new block appends after Phase 23's)
    - .planning/phases/24-restore-flow/24-PATTERNS.md §"pkg/controlplane/models/errors.go"
    - .planning/phases/24-restore-flow/24-CONTEXT.md D-24-08 (full list of 7 sentinels)
  </read_first>
  <behavior>
    - Test 1: each of the 7 new sentinels is non-nil and has a unique message
    - Test 2: wrapping each sentinel with `fmt.Errorf("restore %q: %w", "snap-1", sentinel)` preserves `errors.Is(wrapped, sentinel) == true`
    - Test 3 (anti-collision): each new sentinel is NOT errors.Is-equal to any Phase 22 / Phase 23 sentinel (catches accidental aliasing)
  </behavior>
  <action>
    Append a new block to `pkg/controlplane/models/errors.go` immediately after the existing Phase 23 sentinel block. Use the comment header `// Phase 24 (D-24-08): restore orchestration sentinels surfaced to REST in Phase 25.` exactly. Declare the 7 sentinels listed in `<interfaces>` above verbatim — message strings are normative (Phase 25 may surface in HTTP body). Keep them inside the existing `var ( ... )` block per Go convention; do not create a new var block.

    Create `pkg/controlplane/models/errors_test.go` (if it doesn't already exist; if it does, append). Package `models_test`. Three tests:
    (1) `TestPhase24Sentinels_NonNilAndUnique`: iterate over a slice of all 7 new sentinels (`[]error{ErrShareEnabled, ErrSnapshotNotDurable, ...}`), assert each is non-nil, assert all messages are unique within the slice.
    (2) `TestPhase24Sentinels_WrapRoundTrip`: for each sentinel, build `wrapped := fmt.Errorf("restore %q: %w: %v", "snap-1", sentinel, errors.New("inner"))`, assert `errors.Is(wrapped, sentinel)` returns true.
    (3) `TestPhase24Sentinels_NoCollisionWithPhase22or23`: build a slice of representative Phase 22/23 sentinels (`ErrSnapshotNotFound`, `ErrSnapshotBackupFailed`, `ErrSnapshotVerifyFailed`, `ErrSnapshotDrainTimeout`, `ErrSnapshotRetryTargetNotFound`, `ErrSnapshotRetryTargetNotFailed`, plus `ErrSnapshotStateConflict` if it exists in the file) and assert no Phase 24 sentinel is errors.Is-equal to any of them.

    DO NOT touch Phase 22 / Phase 23 sentinels in any way — that's a hard non-regression invariant.
  </action>
  <verify>
    <automated>go test ./pkg/controlplane/models/... -run "TestPhase24Sentinels" -count=1 && go vet ./pkg/controlplane/... && grep -c "^\s*ErrShareEnabled\s*=\s*errors.New" pkg/controlplane/models/errors.go | grep -q '^1$' && grep -c "^\s*ErrRestoreVerifyFailed\s*=\s*errors.New" pkg/controlplane/models/errors.go | grep -q '^1$' && grep -c "Phase 24 (D-24-08)" pkg/controlplane/models/errors.go | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 sentinels declared in `pkg/controlplane/models/errors.go`: ErrShareEnabled, ErrSnapshotNotDurable, ErrSnapshotMetadataDumpMissing, ErrMetadataStoreNotResetable, ErrRestoreSafetySnapFailed, ErrRestoreAborted, ErrRestoreVerifyFailed
    - Each sentinel's message string matches D-24-08 exactly (Phase 25 may surface these strings)
    - Comment header `// Phase 24 (D-24-08): restore orchestration sentinels surfaced to REST in Phase 25.` precedes the block (grep confirms presence)
    - All three errors_test.go tests pass
    - No Phase 22 / Phase 23 sentinel modified (grep + git diff of those lines shows zero changes)
    - go vet ./pkg/controlplane/... exits 0
  </acceptance_criteria>
  <done>Seven new sentinels are declared, tested for wrap/unwrap, tested for non-collision with prior phases; Phase 25 has stable identifiers to map to HTTP codes.</done>
</task>

<task type="auto">
  <name>Task 2: RestoreSnapshotOpts struct definition</name>
  <files>pkg/controlplane/runtime/restore_opts.go</files>
  <read_first>
    - .planning/phases/24-restore-flow/24-PATTERNS.md §"Doc + signature pattern" (CreateSnapshotOpts shape)
    - .planning/phases/24-restore-flow/24-CONTEXT.md D-24-06 (AllowNonDurable semantics + default-refuse rationale) and the planner-discretion note about SkipPreVerify
    - pkg/controlplane/runtime/snapshot.go (find CreateSnapshotOpts declaration via grep "type CreateSnapshotOpts" — confirm its file location and surrounding doc style)
  </read_first>
  <action>
    Create `pkg/controlplane/runtime/restore_opts.go` with `package runtime`. No imports needed for the struct alone.

    Declare `RestoreSnapshotOpts` struct with a single field `AllowNonDurable bool`. Doc-comment mirrors the structure of `CreateSnapshotOpts` (PATTERNS §"Doc + signature pattern" — read the actual `CreateSnapshotOpts` block from `pkg/controlplane/runtime/snapshot.go` before writing). Required content:
    - Top-line comment naming the type and stating "operator-facing configuration for one RestoreSnapshot invocation"
    - Zero-value behavior statement: "Zero-value (AllowNonDurable=false) requests the default behavior: refuse snapshots with RemoteDurable=false."
    - Field-level comment on AllowNonDurable referencing D-24-06: opts into restoring from snapshots created with `CreateSnapshotOpts.NoSyncGate=true`; pre-verify (step 2 of D-24-09) remains the real safety gate.
    - Brief forward-compat note: "Phase 25 surfaces this as `--force-non-durable` or an equivalent request-body flag."

    Per CONTEXT planner-discretion bullet, OMIT `SkipPreVerify` — it can be added in a future plan without breaking callers (zero-value preserves current behavior).

    DO NOT add the orchestration function `Runtime.RestoreSnapshot` here — that's P24-03's responsibility. This task is type-declaration only. Keeping the struct in its own file means P24-03 can place the orchestration function in `snapshot.go` (per ROADMAP "extend") without import-order churn or merge-conflict friction inside the Wave 2 PR sequencing.
  </action>
  <verify>
    <automated>go vet ./pkg/controlplane/runtime/... && grep -q "type RestoreSnapshotOpts struct" pkg/controlplane/runtime/restore_opts.go && grep -q "AllowNonDurable bool" pkg/controlplane/runtime/restore_opts.go && grep -q "D-24-06" pkg/controlplane/runtime/restore_opts.go && go build ./pkg/controlplane/runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/controlplane/runtime/restore_opts.go` exists with `package runtime`
    - Declares `type RestoreSnapshotOpts struct { AllowNonDurable bool }` with appropriate doc-comments
    - References D-24-06 in doc-comment
    - Does NOT declare `Runtime.RestoreSnapshot` (deferred to P24-03)
    - Does NOT declare `SkipPreVerify` (deferred per planner discretion)
    - `go build ./pkg/controlplane/runtime/...` succeeds
    - `go vet ./pkg/controlplane/runtime/...` exits 0
  </acceptance_criteria>
  <done>RestoreSnapshotOpts type is in place with AllowNonDurable only; P24-03 can immediately reference it from snapshot.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 25 REST handler -> sentinel mapping | Sentinels cross the trust boundary as wrapped errors; Phase 25 unwraps via `errors.Is` for HTTP-code mapping. |
| Sentinel message strings -> HTTP response body | Phase 25 may surface message strings to operator-facing API consumers; messages must not contain user-supplied or sensitive data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | Sentinel message strings | mitigate | All 7 sentinels are static `errors.New(...)` with no interpolation; messages contain only the failure category, no share names / snapshot IDs / paths. Operator-readable detail is added by P24-03 via `fmt.Errorf("%w: %v")` wrapping. |
| T-24-02-02 | Tampering | Sentinel identity drift | mitigate | errors_test.go non-collision test guarantees no Phase 24 sentinel collides with Phase 22/23 sentinels; round-trip test guarantees wrap/unwrap stability. |
| T-24-02-03 | Repudiation | Opts struct evolution | accept | Future additions to `RestoreSnapshotOpts` (e.g., `SkipPreVerify`) are non-breaking as long as zero-value preserves current behavior. Convention is documented in this plan's body. |
| T-24-02-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies. Pure Go stdlib `errors` package. |
</threat_model>

<verification>
- 7 new sentinels declared in `pkg/controlplane/models/errors.go` under D-24-08 comment header
- errors_test.go covers wrap/unwrap round-trip + non-collision with prior phases
- `RestoreSnapshotOpts` declared in `pkg/controlplane/runtime/restore_opts.go` with `AllowNonDurable bool` only
- `go test ./pkg/controlplane/...` + `go vet ./pkg/controlplane/...` exit 0
- Phase 22/23 sentinels untouched (git diff confirms)
</verification>

<success_criteria>
- All Phase 24 type vocabulary lands in Wave 1 with zero dependencies on P24-01
- P24-03 (Wave 2) can immediately reference all 7 sentinels + RestoreSnapshotOpts
- Phase 25 has a stable surface to map to HTTP status codes (per D-24-08 forward-looking note: ErrShareEnabled -> 400, ErrSnapshotNotDurable -> 412, others -> 500)
</success_criteria>

<output>
Create `.planning/phases/24-restore-flow/24-02-SUMMARY.md` when done. Include: the 7 sentinel identifiers + messages, a confirmation that wrap/unwrap round-trip tests pass, and a note on the SkipPreVerify deferral so P24-03 / Phase 25 can see why it's absent.
</output>
