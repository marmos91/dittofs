# Benchmarks

The benchmark documentation has been consolidated into a single canonical page:

→ [`docs/BENCHMARKS.md`](../../docs/BENCHMARKS.md)

It covers the end-to-end performance reports (DittoFS vs JuiceFS vs kernel NFS
on Scaleway infrastructure), how to run benchmarks (`dfsbench orchestrate` /
`dfsbench remote` / `go test -bench`), the local perf gates, and the snapshot
scale limits.
